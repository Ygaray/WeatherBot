API-key.md
Weatherbot_discord_webhook.md
env.local
.env

# Personal runtime config (non-secret, but per-user). Template: config.example.toml
config.toml

# SQLite data store (DATA-01) — created at runtime, never committed
data/

# Python / uv
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# Coverage artifacts (Phase-21 one-time branch audit — generated, never committed)
.coverage
.coverage.*
htmlcov/
coverage.xml

# grimp import-graph cache (import-hygiene gate reads fresh; never commit the cache)
.grimp_cache/
