---
phase: 34-test-gap-backfill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduler.py
autonomous: true
requirements: [HARD-TEST-01]
must_haves:
  truths:
    - "D-01: The F106 real-concurrency test and its meta-guard live in the existing tests/test_scheduler.py (no new consolidated module), next to the current false-green test at :817."
    - "D-02: Both the corrected test and the meta-guard name their finding id + requirement in the test name or docstring (F106 / HARD-TEST-01), so the fix-test-finding chain is auditable from source."
    - "D-03: test_concurrent_double_fire_delivers_once is genuinely concurrent — two worker threads call fire_slot on the SAME slot key, gated by threading.Barrier(2), against the shared file-backed tmp_db; exceptions collected in a shared list (never swallowed), no real sleeps."
    - "D-04: A co-located meta-guard monkeypatches a weakened SELECT-then-INSERT claim_slot shim and proves the barrier-threaded body then produces 2 deliveries — the test provably breaks if store.py INSERT OR IGNORE atomicity is removed (not decoration)."
    - "D-05: The exactly-once assertion (len(channel.sent_text) == 1 AND exactly one non-None fire_slot return AND errors == []) is the exact observable the F106 bug violated, so the corrected test is red-by-construction against a sequential/weakened claim."
    - "D-06: The Gate-1 self-UAT log records the documented red->green mutation spot-check for F106 (weaken claim_slot to SELECT-then-INSERT -> concurrent test sees 2 deliveries -> restore -> exactly 1); no mutation-testing dependency is added."
    - "concurrency edge (HARD-TEST-01): two fire_slot invocations racing the same slot key are guaranteed exactly-once delivery — this is the F106 real-thread double-fire test asserting one sent_log row and one channel POST."
    - "idempotency edge (HARD-TEST-01): running fire_slot twice against the same claim yields exactly one delivery — the F106 exactly-once assertion (one sent_log row) pins this."
  artifacts:
    - tests/test_scheduler.py
  key_links:
    - "fire_slot (daemon.py:140) claim path -> claim_slot (store.py:274) INSERT OR IGNORE against UNIQUE(location_name, send_time, local_date) (store.py:116) — the atomicity seam the F106 test and meta-guard exercise."
    - "file-backed tmp_db (conftest.py:53) shared across both threads so INSERT OR IGNORE is genuinely raced."
---

<objective>
Correct the F106 false-green test in `tests/test_scheduler.py`: the current
`test_concurrent_double_fire_delivers_once` (`:817`) calls `fire_slot` twice **sequentially**
(`:858-859`), which a SELECT-then-INSERT `claim_slot` would also pass — proving nothing about a
real race. Make it genuinely concurrent (two barrier-synchronized threads on the shared
file-backed `tmp_db`) and add a co-located meta-guard that proves the test breaks if the
`INSERT OR IGNORE`/`UNIQUE` atomicity is degraded.

Purpose: HARD-TEST-01 — the "concurrent double-fire" test must actually exercise concurrency, so
a weakened `claim_slot` fails it. This is the single most load-bearing false-green correction.
Output: A corrected `test_concurrent_double_fire_delivers_once` (real threads + barrier) and a
co-located `..._metaguard` test in `tests/test_scheduler.py`, both tagged `# F106 / HARD-TEST-01`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-test-gap-backfill/34-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
@.planning/phases/34-test-gap-backfill/34-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make test_concurrent_double_fire_delivers_once genuinely concurrent (F106)</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py:817-861 — the current false-green test body (sequential double fire_slot at :858-859; kwargs assembly at :840-854). This is what you replace.
    - tests/test_config_holder.py:89-133 — the canonical real-thread harness to copy: shared `errors: list[BaseException]`, `except BaseException: errors.append(exc)` (never swallow), bounded, no real sleeps, final `assert not errors`.
    - tests/conftest.py:52-69 — the file-backed `tmp_db` fixture (real sqlite file at tmp_path/"weatherbot.db"), required so both threads share one DB file and genuinely race.
    - weatherbot/scheduler/daemon.py:140-230 — `fire_slot` signature/kwargs (accepts config=, db_path=, client=, channel=, scheduled_dt=, late=; returns DeliveryResult | None, None on lost claim at :217) and the claim call at :210.
    - weatherbot/weather/store.py:274-310 — `claim_slot` INSERT OR IGNORE (:303-310) and the UNIQUE(location_name, send_time, local_date) constraint (:116) — the atomicity mechanism under test.
  </read_first>
  <behavior>
    - Two worker threads both call fire_slot(loc, slot, **kwargs) on the SAME slot key, blocked on a threading.Barrier(2) placed immediately before each fire_slot call so both enter the claim window near-simultaneously.
    - Exactly one delivery: len(channel.sent_text) == 1 (one shared _FakeChannel POST across both fires) AND exactly one of the two fire_slot returns is non-None (the winner) AND the other is None (lost claim).
    - Exactly one sent_log row exists (was_sent(...) is True after the race).
    - No thread raised: the shared errors list is empty.
  </behavior>
  <action>
    Replace the sequential body of `test_concurrent_double_fire_delivers_once` (tests/test_scheduler.py:817-861) with the real-thread harness from test_config_holder.py:89-133. Keep the existing kwargs assembly (:840-854): one shared `_FakeClient(load_fixture("onecall_imperial_clear.json"), load_fixture("onecall_metric_clear.json"))`, one shared `_FakeChannel()` that counts POSTs across both fires, `scheduled = datetime(2026, 6, 10, 7, 0, tzinfo=_NY)`, and `kwargs = dict(config=cfg, db_path=tmp_db, client=client, channel=channel, scheduled_dt=scheduled, late=True)`. Define a shared `errors: list[BaseException] = []`, a shared `results: list` for the two fire_slot return values, and a `barrier = threading.Barrier(2)`. Each worker calls `barrier.wait()` then `results.append(fire_slot(loc, slot, **kwargs))` inside a try/except BaseException that appends to errors (never swallow). Start both threads, join both. Assert: `not errors`; `len(channel.sent_text) == 1`; exactly one non-None entry in results and exactly one None; `was_sent(tmp_db, loc.id, slot.time, local_date) is True`. Tag the test docstring `# F106 / HARD-TEST-01` per D-02. Use no real `time.sleep` — the barrier plus the deterministic outcome assertion is the whole synchronization (D-03). Import threading at module top if not already present.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k concurrent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scheduler.py -k concurrent -x -q` exits 0.
    - Observable (34-VALIDATION.md F106): `len(channel.sent_text) == 1` AND exactly one sent_log row AND `errors == []`; exactly one non-None `fire_slot` return, one None.
    - Two worker threads gated by `threading.Barrier(2)`; no real `time.sleep` anywhere in the test body.
    - Docstring names `# F106 / HARD-TEST-01`.
  </acceptance_criteria>
  <done>test_concurrent_double_fire_delivers_once passes with two barrier-synchronized fire_slot threads on the shared file-backed tmp_db: exactly one channel POST, exactly one non-None fire_slot return, exactly one sent_log row, empty errors list, no real sleeps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the F106 meta-guard proving atomicity is exercised (D-04)</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py (Task 1 output) — the corrected barrier-threaded body to reuse verbatim for the meta-guard.
    - weatherbot/weather/store.py:251-310 — `was_sent` (:251) and `claim_slot` (:274) so the SELECT-then-INSERT shim can read-then-unconditionally-INSERT the same sent_log row shape.
    - weatherbot/scheduler/daemon.py:210 — the `claim_slot(...)` call site fire_slot routes through, so monkeypatching `weatherbot.weather.store.claim_slot` (and the name daemon imported it under) intercepts the race.
  </read_first>
  <behavior>
    - A co-located meta-guard test monkeypatches a weakened SELECT-then-INSERT claim_slot shim (read was_sent, then unconditionally INSERT a sent_log row) over the production claim_slot.
    - Running the SAME barrier-threaded body under the weakened shim produces 2 deliveries (len(channel.sent_text) == 2) — proving the real test breaks when INSERT OR IGNORE atomicity is removed.
    - The real store.py claim_slot is restored after the test (monkeypatch teardown), leaving the suite green.
  </behavior>
  <action>
    Add a co-located `test_concurrent_double_fire_metaguard` in tests/test_scheduler.py directly after Task 1's test. Define a module-local SELECT-then-INSERT shim: read `was_sent(db_path, location_id, send_time, local_date)`, and if not already sent, unconditionally INSERT the sent_log row (no INSERT OR IGNORE, no UNIQUE arbitration) and return True — i.e. it lacks the atomic guard, so two racing threads both "win". Monkeypatch this shim over `weatherbot.weather.store.claim_slot` AND over the symbol name daemon.py imported it under (patch both so fire_slot's call site at daemon.py:210 resolves to the shim). Run the identical barrier-threaded two-thread body from Task 1. Assert `len(channel.sent_text) == 2` (both delivered — the guard is gone) and `not errors`. Rely on monkeypatch fixture teardown to restore the real claim_slot. Tag the docstring `# F106 / HARD-TEST-01 — meta-guard (D-04): weakened claim_slot MUST make the concurrent test fail`. Do NOT weaken store.py itself — the weakening is a local monkeypatched shim only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "concurrent or metaguard" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scheduler.py -k "concurrent or metaguard" -x -q` exits 0.
    - Observable (34-VALIDATION.md F106 meta-guard): under the monkeypatched SELECT-then-INSERT claim_slot shim, the same barrier-threaded body yields `len(channel.sent_text) == 2` (test goes red) AND `errors == []`.
    - The weakening is a local monkeypatched shim only; `store.py` is unchanged and the real `claim_slot` is restored by teardown (full scheduler module green after).
    - Docstring names `# F106 / HARD-TEST-01` (meta-guard, D-04).
  </acceptance_criteria>
  <done>test_concurrent_double_fire_metaguard passes: under the monkeypatched SELECT-then-INSERT claim_slot the barrier-threaded body yields 2 deliveries, proving the real concurrent test exercises atomicity; the real claim_slot is restored and the full scheduler module stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan only adds/corrects pytest tests in `tests/test_scheduler.py`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering | tests/test_scheduler.py | low | accept | No new production code, inputs, endpoints, secrets, or trust boundaries change. This phase only adds/corrects pytest tests; no runtime code paths change. The meta-guard's weakened claim_slot is a local monkeypatched shim (teardown-restored), never touching store.py. |

No new attack surface — tests-only.
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py -k "concurrent or metaguard" -x -q` — both the real concurrent test and the meta-guard pass.
- `uv run pytest tests/test_scheduler.py -q` — full scheduler module green (trust exit code; ignore the syrupy "N snapshots failed" report line, the suite exits 0).
- Gate-1 self-UAT (D-06): documented red->green spot-check — weaken store.claim_slot to SELECT-then-INSERT, show the concurrent test goes red (2 deliveries), restore, show green (1 delivery).
</verification>

<success_criteria>
The F106 "concurrent double-fire" test is genuinely concurrent (barrier threads on the shared
file-backed DB), asserts exactly-once delivery by construction, and is provably atomicity-sensitive
via the co-located meta-guard. Both tests name `F106 / HARD-TEST-01`.
</success_criteria>

<output>
Create `.planning/phases/34-test-gap-backfill/34-01-SUMMARY.md` when done.
</output>
