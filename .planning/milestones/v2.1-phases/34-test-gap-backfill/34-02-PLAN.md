---
phase: 34-test-gap-backfill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_reliability.py
autonomous: true
requirements: [HARD-TEST-01, HARD-TEST-02]
must_haves:
  truths:
    - "D-01: The F114/F112 corrections and the F110 addition extend the existing tests/test_reliability.py next to their related tests (no new module)."
    - "D-02: Each corrected/added test names its finding id + requirement (F114 / F112 / F110 with HARD-TEST-01 or HARD-TEST-02) in the test name or docstring."
    - "D-05: Each test asserts by construction the exact value the bug violated — last_success_utc is None on a bare tick (F114), step <= wait <= step*1.5 (F112), two_burst_wait == RETRY_AFTER_CAP_S on a mid-pause 429 (F110) — so each is red against pre-fix/weakened behavior by design."
    - "D-06: The Gate-1 self-UAT log records the documented red->green spot-check for F114 (shim _heartbeat_tick to also stamp_success -> the last_success_utc-is-None assertion goes red -> restore -> green) and for F112 (a synthetic 149.0 wait passes the old <150.0 bound but fails the derived <=step*1.5 bound); no mutation-testing dependency is added."
    - "precision edge (HARD-TEST-01): the exact within-burst wait ceiling is derived from constants (step = BURST_SPREAD_S/(BURST_SIZE-1) = 85.71, ceiling = step*1.5 = 128.57), never a magic literal — F112 asserts 85.71 <= wait <= 128.57."
    - "boundary edge (HARD-TEST-01/02): the F112 test pins the within-burst bound at step and step*1.5 and keeps the attempt==BURST_SIZE mid-pause == MID_PAUSE_S boundary; the F110 test pins the one-step-either-side case where a capped Retry-After collapses the 2700s mid-pause to the 120s cap."
    - "idempotency edge (HARD-TEST-01): repeated bare heartbeat ticks stamp only last_tick_utc and never last_success_utc — the F114 tick/success separation test pins this."
  artifacts:
    - tests/test_reliability.py
  key_links:
    - "_heartbeat_tick (daemon.py:705) -> stamp_tick (store.py:456, last_tick_utc only); stamp_success (store.py:473, last_success_utc only); read_heartbeat (store.py:508) returns both columns — the F114 tick/success separation seam."
    - "two_burst_wait (yahir_reusable_bot/reliability/retry.py:146) + _within_burst_wait (retry.py:133) + RETRY_AFTER_CAP_S=120 (retry.py:67) + MID_PAUSE_S — driven app-side via the weatherbot/reliability/retry.py shim import; F112/F110 pin the contract, not hub source."
---

<objective>
Correct two false-green reliability tests and add one missing high-risk test in
`tests/test_reliability.py`:
- **F114 (fix):** `test_heartbeat_upsert` (`:606`) asserts `last_tick_utc is not None` but never
  asserts `last_success_utc` stays `None` after a bare tick — a bug stamping success on every tick
  would pass. Add the tick/success separation assertion.
- **F112 (fix):** `test_two_burst_wait_shape` (`:95`) uses a loose `assert 0.0 <= wait < 150.0`
  (`:100`) where the real jittered ceiling is `128.57s`. Tighten to a constant-derived bound.
- **F110 (add):** no test covers a `Retry-After` 429 landing on `attempt == BURST_SIZE`, where the
  2700s mid-pause base collapses to the 120s cap. Add it.

Purpose: HARD-TEST-01 (F114, F112 false-greens) + HARD-TEST-02 (F110 missing coverage).
Output: strengthened `test_heartbeat_upsert`, tightened `test_two_burst_wait_shape`, and a new
`test_retry_after_collapses_mid_pause` in `tests/test_reliability.py`, each tagged with its finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-test-gap-backfill/34-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
@.planning/phases/34-test-gap-backfill/34-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strengthen heartbeat tick/success separation (F114)</name>
  <files>tests/test_reliability.py</files>
  <read_first>
    - tests/test_reliability.py:606-618 — the current `test_heartbeat_upsert` body (only asserts `last_tick_utc is not None`).
    - weatherbot/scheduler/daemon.py:705-718 — `_heartbeat_tick` calls `stamp_tick(db_path)` only (never stamp_success).
    - weatherbot/weather/store.py:456-485 — `stamp_tick` (:456, writes ONLY last_tick_utc) and `stamp_success` (:473, writes ONLY last_success_utc); schema seeds both NULL (:140-146).
    - weatherbot/weather/store.py:508 — `read_heartbeat` returns {"last_tick_utc", "last_success_utc"}.
  </read_first>
  <behavior>
    - After init_db(tmp_db) + a single _heartbeat_tick(tmp_db): read_heartbeat -> last_tick_utc is not None AND last_success_utc is None (the tick/success separation).
    - After a separate stamp_success(tmp_db): last_success_utc is not None AND last_tick_utc is unchanged from the tick.
  </behavior>
  <action>
    In `test_heartbeat_upsert` (tests/test_reliability.py:606), after the existing `_heartbeat_tick(tmp_db)` and heartbeat read, ADD `assert row["last_success_utc"] is None` — the tick must not stamp success. Then call `stamp_success(tmp_db)` (import from weatherbot.weather.store), re-read via `read_heartbeat` (store.py:508), and assert `last_success_utc is not None` and `last_tick_utc` equals the tick value read before (unchanged). Prefer `read_heartbeat` over the local `_heartbeat` helper if it exposes both columns cleanly. Add `# F114 / HARD-TEST-01` to the test docstring per D-02. Do NOT change _heartbeat_tick or store.py — this is an assertion strengthening only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reliability.py -k heartbeat -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reliability.py -k heartbeat -x -q` exits 0.
    - Observable (34-VALIDATION.md F114): after a bare `_heartbeat_tick`, `read_heartbeat` → `last_tick_utc is not None` AND `last_success_utc is None`.
    - After `stamp_success`: `last_success_utc is not None` AND `last_tick_utc` equals the pre-success tick value (unchanged).
    - Docstring names `# F114 / HARD-TEST-01`; no production code changed.
  </acceptance_criteria>
  <done>test_heartbeat_upsert asserts last_success_utc is None after a bare tick and last_success_utc becomes non-None (with last_tick_utc unchanged) only after stamp_success; tagged F114 / HARD-TEST-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten the within-burst wait bound to the derived ceiling (F112)</name>
  <files>tests/test_reliability.py</files>
  <read_first>
    - tests/test_reliability.py:95-102 — the current `test_two_burst_wait_shape` (loose `assert 0.0 <= wait < 150.0` at :100; the attempt==BURST_SIZE == MID_PAUSE_S assertion at :102).
    - tests/test_reliability.py:285,294 — the in-file derived-bound precedent: `step = BURST_SPREAD_S / (BURST_SIZE - 1)` and `assert step <= no_ra_wait < step * 1.5` (copy this exact form, do not invent a magic literal).
    - .venv (yahir_reusable_bot)/reliability/retry.py:133-143 — `_within_burst_wait`: step = burst_spread_s/(burst_size-1); jitter = uniform(0, step*0.5); return step + jitter. Ceiling = step*1.5.
    - weatherbot/config/models.py:274-317 — the app Reliability defaults (attempts_per_burst=8, burst_spread_seconds=600) and worst_case within_max = step*1.5, confirming the 128.57 figure.
  </read_first>
  <behavior>
    - For every within-burst attempt (attempt in 1..BURST_SIZE-1 and BURST_SIZE+1..2*BURST_SIZE-1): step <= two_burst_wait(_State(n)) <= step*1.5, where step = BURST_SPREAD_S/(BURST_SIZE-1) = 85.71 and step*1.5 = 128.57.
    - The attempt==BURST_SIZE mid-pause case still returns MID_PAUSE_S (unchanged boundary).
  </behavior>
  <action>
    In `test_two_burst_wait_shape` (tests/test_reliability.py:95), replace the loose bound at :100 (`assert 0.0 <= wait < 150.0`) with a constant-derived ceiling matching the in-file precedent at :285/:294: compute `step = BURST_SPREAD_S / (BURST_SIZE - 1)` and `ceiling = step * 1.5`, then assert `step <= wait <= ceiling` for each within-burst attempt. Import BURST_SIZE / BURST_SPREAD_S from the same source the file already uses (the app shim weatherbot.reliability.retry or the constant already referenced at :285). Keep the existing `two_burst_wait(_State(BURST_SIZE)) == MID_PAUSE_S` assertion (:102) intact — the mid-pause boundary is unchanged. Never hard-code `128.57` as a literal (A1 — derive from constants). Add `# F112 / HARD-TEST-01` to the docstring.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reliability.py -k two_burst_wait -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reliability.py -k two_burst_wait -x -q` exits 0.
    - Observable (34-VALIDATION.md F112): for every within-burst attempt, `step <= wait <= step*1.5` where `step = BURST_SPREAD_S/(BURST_SIZE-1)` (= 85.71, ceiling 128.57) — derived from constants, NOT a literal `128.57`, and NOT the loose `< 150.0`.
    - The `two_burst_wait(_State(BURST_SIZE)) == MID_PAUSE_S` assertion is retained.
    - Docstring names `# F112 / HARD-TEST-01`.
  </acceptance_criteria>
  <done>test_two_burst_wait_shape bounds every within-burst wait by step <= wait <= step*1.5 (85.71..128.57), derived from BURST_SIZE/BURST_SPREAD_S constants, retains the attempt==BURST_SIZE == MID_PAUSE_S assertion; tagged F112 / HARD-TEST-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Retry-After 429 mid-pause collapse test (F110)</name>
  <files>tests/test_reliability.py</files>
  <read_first>
    - tests/test_reliability.py:82-87 — the `_State` / `_Outcome` RetryCallState stand-ins used to drive two_burst_wait directly.
    - tests/test_reliability.py:259-294 — the existing Retry-After honoring test (fires 429 on attempt 1) and the `_status_error(429, headers={"Retry-After": ...})` builder + derived-bound assertions to reuse.
    - .venv (yahir_reusable_bot)/reliability/retry.py:146-181 — `two_burst_wait`: at attempt_number == burst_size, _within_burst_wait returns mid_pause_s (2700); the collapse line `return min(max(base, ra), RETRY_AFTER_CAP_S)` (:180); RETRY_AFTER_CAP_S = 120 (:67).
  </read_first>
  <behavior>
    - two_burst_wait(_State(BURST_SIZE, _Outcome(_status_error(429, headers={"Retry-After": "9999"})))) == RETRY_AFTER_CAP_S (120.0) — the capped Retry-After collapses the 2700s mid-pause, never extends it.
    - Contrast: two_burst_wait(_State(BURST_SIZE)) (no Retry-After header) == MID_PAUSE_S (2700) — the mid-pause base is only shortened by a capped Retry-After, never on a bare mid-pause attempt.
  </behavior>
  <action>
    Add a new `test_retry_after_collapses_mid_pause` to tests/test_reliability.py near the existing Retry-After honoring test (:259). Build `state = _State(BURST_SIZE, _Outcome(_status_error(429, headers={"Retry-After": "9999"})))` reusing the file's `_State`/`_Outcome`/`_status_error` stand-ins (:82-87). Assert `two_burst_wait(state) == RETRY_AFTER_CAP_S` (120.0) — NOT MID_PAUSE_S (2700). Add the contrast assertion `two_burst_wait(_State(BURST_SIZE)) == MID_PAUSE_S` to pin that only a capped Retry-After shortens the mid-pause. Import RETRY_AFTER_CAP_S and MID_PAUSE_S from the same source the file already uses. Add `# F110 / HARD-TEST-02` to the docstring. This is assertion-by-construction (D-05) — no red-mutation spot-check required for F110.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reliability.py -k retry_after -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reliability.py -k retry_after -x -q` exits 0.
    - Observable (34-VALIDATION.md F110): `two_burst_wait(_State(BURST_SIZE, 429-with-Retry-After)) == RETRY_AFTER_CAP_S` (120.0), NOT `MID_PAUSE_S` (2700).
    - Contrast assertion: `two_burst_wait(_State(BURST_SIZE)) == MID_PAUSE_S` (a bare mid-pause attempt is not shortened).
    - Docstring names `# F110 / HARD-TEST-02`; drives the hub retry symbol via the app-side shim import (no hub edit).
  </acceptance_criteria>
  <done>test_retry_after_collapses_mid_pause proves a capped Retry-After 429 on attempt==BURST_SIZE collapses the 2700s mid-pause to the 120s cap while a bare mid-pause attempt stays 2700; tagged F110 / HARD-TEST-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan only adds/corrects pytest tests in `tests/test_reliability.py`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-02 | Tampering | tests/test_reliability.py | low | accept | No new production code, inputs, endpoints, secrets, or trust boundaries change. F110/F112 drive the hub retry symbols via the existing app-side shim import — no hub source is edited (a hub change is human-gated per ECOSYSTEM.md). Tests-only. |

No new attack surface — tests-only.
</threat_model>

<verification>
- `uv run pytest tests/test_reliability.py -k "heartbeat or two_burst_wait or retry_after" -x -q` — all three pass.
- `uv run pytest tests/test_reliability.py -q` — full reliability module green (trust exit code; ignore the syrupy report line).
- Gate-1 self-UAT (D-06): documented red->green for F114 (shim _heartbeat_tick to also call stamp_success -> the `last_success_utc is None` assertion goes red -> restore -> green) and F112 (a synthetic 149.0 wait passes old `< 150.0` but fails the new `<= step*1.5`).
</verification>

<success_criteria>
F114 pins tick/success column separation, F112 bounds the within-burst wait by the constant-derived
`step..step*1.5` ceiling (not the loose `< 150.0`), and F110 proves the mid-pause 429 collapse to the
120s cap. All three name their finding id + requirement.
</success_criteria>

<output>
Create `.planning/phases/34-test-gap-backfill/34-02-SUMMARY.md` when done.
</output>
