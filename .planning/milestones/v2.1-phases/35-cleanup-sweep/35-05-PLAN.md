---
phase: 35-cleanup-sweep
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/weather/uv.py
  - weatherbot/scheduler/uvmonitor.py
  - weatherbot/weather/client.py
  - tests/test_uv_monitor.py
  - tests/test_client.py
autonomous: true
requirements:
  - HARD-CLEAN-02
must_haves:
  truths:
    - "The UV pre-warn countdown reports honest minutes via `round(delta_min)` instead of `int(delta_min)` truncation, fixed with a regression test asserting 28.9 → '~29 min' (F60, D-01 fix, D-06 behavior-CHANGING → regression test)."
    - "The UV monitor tick counters reconcile: a location that raises inside the fetch loop is counted (an `errored` counter or counted in the except) so `fetched + skipped + errored == len(locations)`; log-only, behavior-preserving (F61, D-01 fix, PRESERVE)."
    - "`weather/client.py`'s 2xx-non-JSON response (captive-portal / HTML-200) is wrapped so `.json()` raising is mapped to a classified/retryable error matching the caller's transient/HTTPStatusError contract, with a regression test on a 2xx HTML body (F68, D-01 fix, D-06 behavior-CHANGING → regression test)."
    - "F67 (import-time global `httpx` logger setLevel) is resolved against Phase-30 redaction: since `_redact.py` now redacts appid at the raise sites (guarded by test_redact_hygiene.py), the global setLevel is superseded — it is removed if redaction fully covers the URL-log path, else annotated `# ACCEPTED (F67, v2.1): ...`; either way `test_redact_hygiene.py` stays green (Open-Q3, A4)."
    - "The genuinely-cosmetic/latent UV findings F59 (inclusive [sunrise,sunset] boundary), F72 (fixed fallback window), F73 (peak/max WR-02 trade-off), F58 (missing-sun-fields skip) are ACCEPTED with in-code `# ACCEPTED (F##, v2.1): ...` annotations at their sites (D-01 accept-with-rationale, D-02). (F62's models.py annotation is owned by Plan 06 to keep models.py single-owner within the wave.)"
  artifacts:
    - "weatherbot/weather/uv.py (F73/F72 accepted-annotations)"
    - "weatherbot/scheduler/uvmonitor.py (F60 round fix, F61 errored counter, F59/F58 accepted-annotations)"
    - "weatherbot/weather/client.py (F68 non-JSON 2xx classified error; F67 removed-or-accepted)"
    - "tests/test_uv_monitor.py (F60 rounding regression), tests/test_client.py (F68 non-JSON regression)"
  key_links:
    - "weatherbot/_redact.py + tests/test_redact_hygiene.py — the Phase-30 control that supersedes F67's setLevel; the F67 change must not regress it"
    - "weather/client.py .json() → caller retry/classification contract — F68 must map non-JSON to the same transient/HTTPStatusError shape callers expect"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/."
    - statement: "The F67 change must keep tests/test_redact_hygiene.py green — verify Phase-30 redaction covers the URL-log path BEFORE removing the setLevel; if coverage is unproven, ACCEPT-annotate instead of removing (A4)."
    - statement: "F59/F72/F73/F58 are ACCEPT-only — do NOT change their runtime behavior (no boundary flip, no fallback-window math change); annotation only. (F62 is handled in Plan 06.)"
---

<objective>
Sweep the weather/uv/client cluster: fix F60 (rounding, +test), F61 (counter reconcile), F68 (non-JSON 2xx classified error, +test), resolve F67 against Phase-30 redaction (remove-or-accept), and accept-with-rationale the genuinely-cosmetic/latent findings F59/F72/F73/F62/F58 with in-code annotations.

Purpose: SC-2 (HARD-CLEAN-02) — every remaining low finding fixed or annotated accepted, no silent debt.
Output: fixed/annotated uv.py, uvmonitor.py, client.py, models.py + two regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix UV pre-warn rounding (F60) + reconcile tick counters (F61) + accept F59/F58</name>
  <files>weatherbot/scheduler/uvmonitor.py, tests/test_uv_monitor.py</files>
  <read_first>
    - weatherbot/scheduler/uvmonitor.py:296-304 (the `f"... in ~{int(delta_min)} min ..."` pre-warn text — F60 truncation)
    - weatherbot/scheduler/uvmonitor.py:380-392 (the `fetched`/`skipped` counters + the try/except fetch loop — F61 raised-in-try locations counted as neither)
    - weatherbot/scheduler/uvmonitor.py:78-82 (`_is_daylight` inclusive `sunrise <= now_local <= sunset` — F59)
    - weatherbot/scheduler/uvmonitor.py:154 region (missing sun fields skips all-clear — F58)
    - tests/test_uv_monitor.py (existing prewarn / tick tests — slot the F60 regression alongside)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entries F60, F61, F59, F58
  </read_first>
  <behavior>
    - F60: a `delta_min` of 28.9 renders `~29 min` (round), not `~28 min` (trunc); 29.0 renders `~29 min`.
    - F61: after a tick where one location raises inside the fetch loop, `fetched + skipped + errored == len(snapshot.locations)` (the raised location is counted, not lost).
  </behavior>
  <action>
    F60: replace `int(delta_min)` in the pre-warn `time_close` text with `round(delta_min)`. Add a regression test in `tests/test_uv_monitor.py` named `# HARD-CLEAN-02 / F60` asserting a `delta_min` near-boundary (e.g. 28.9) renders `~29 min`; use the existing prewarn-render test analog in that module.
    F61: add an `errored` counter incremented in the fetch-loop `except` arm (or otherwise count the raised location) so the tick log reconciles `fetched + skipped + errored == len(locations)`; include `errored` in the tick log line. Behavior-preserving (log-only) — no test strictly required, but if a tick-log test exists, extend it.
    F59: add `# ACCEPTED (F59, v2.1): inclusive [sunrise,sunset] is intentional; exact-instant sunset trigger with UV≈0 is unreachable` at the `_is_daylight` return.
    F58: add `# ACCEPTED (F58, v2.1): missing-sun-fields skip is a missed courtesy all-clear (schema-drift only), not a missed warning` at the F58 site (cross-check Phase-32 F21/F58 lifecycle audit first; if already covered, mark FIXED@32 in the ledger instead — Plan 09).
  </action>
  <verify>
    <automated>uv run pytest tests/test_uv_monitor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - F60 regression test exists, names the finding id, is GREEN (fails against `int()` pre-fix); pre-warn text uses `round(delta_min)`.
    - `fetched + skipped + errored == len(locations)` holds in the tick counting (read of the loop confirms the raised path is counted).
    - `grep -q "# ACCEPTED (F59, v2.1):" weatherbot/scheduler/uvmonitor.py` and (if F58 accepted here) `grep -q "# ACCEPTED (F58, v2.1):" weatherbot/scheduler/uvmonitor.py`.
    - `uv run pytest tests/test_uv_monitor.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>F60 rounding fixed+tested, F61 counters reconcile, F59/F58 accepted-annotated; UV-monitor tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify non-JSON 2xx (F68) + resolve F67 setLevel vs redaction</name>
  <files>weatherbot/weather/client.py, tests/test_client.py</files>
  <read_first>
    - weatherbot/weather/client.py:78-90 and :110-122 (the two `response.raise_for_status()` + unguarded `return response.json()` sites — F68)
    - weatherbot/weather/client.py:31 (`from weatherbot._redact import redact_appid`) and :46 (`logging.getLogger("httpx").setLevel(logging.WARNING)` — F67)
    - tests/test_redact_hygiene.py (the Phase-30 control — appid never in logs; the F67 change must not regress this)
    - tests/test_client.py (existing client tests — slot the F68 regression here)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entries F67, F68 + §RESEARCH Security domain (F67 superseded by redaction)
  </read_first>
  <behavior>
    - F68: a 2xx response with a non-JSON (HTML) body no longer raises a bare `JSONDecodeError`; it raises a classified error matching the caller's transient/HTTPStatusError contract (retryable-or-classified, not an unhandled type).
    - F67: after the change, `tests/test_redact_hygiene.py` still passes — no appid leaks into httpx logs regardless of whether setLevel stays or goes.
  </behavior>
  <action>
    F68: wrap both `return response.json()` sites so a `json.JSONDecodeError` (2xx-with-non-JSON body) is caught and re-raised as the same classified/retryable error type the callers already handle for transient failures (match the existing `httpx.HTTPStatusError` / transient contract; redact the URL via `redact_appid` if the raised message could carry it). Add a regression test in `tests/test_client.py` named `# HARD-CLEAN-02 / F68` using the MockTransport pattern (see test_redact_hygiene.py `_handler` shape) that returns a 200 with an HTML body and asserts the classified error is raised (NOT a bare JSONDecodeError) and that no appid appears in the message.
    F67: verify Phase-30 redaction covers the URL-log path the setLevel was silencing (read client.py raise sites + `_redact.py` + test_redact_hygiene.py). If redaction fully covers it, REMOVE the import-time `logging.getLogger("httpx").setLevel(...)` global side effect. If coverage is unproven, keep it and add `# ACCEPTED (F67, v2.1): intentional httpx-URL-log suppression retained; redaction is the primary control` at line 46. Either way run test_redact_hygiene.py and confirm green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_client.py tests/test_redact_hygiene.py -q</automated>
  </verify>
  <acceptance_criteria>
    - F68 regression test exists, names the finding id, is GREEN: a 2xx HTML body raises the classified error, not a bare JSONDecodeError; no appid in the message.
    - `tests/test_redact_hygiene.py` passes after the F67 change (whether setLevel removed or accepted).
    - If F67 accepted: `grep -q "# ACCEPTED (F67, v2.1):" weatherbot/weather/client.py`. If removed: `! grep -q "getLogger(\"httpx\").setLevel" weatherbot/weather/client.py`.
    - `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>F68 non-JSON 2xx is classified+tested; F67 resolved against redaction with test_redact_hygiene.py green.</done>
</task>

<task type="auto">
  <name>Task 3: Accept-annotate cosmetic uv.py findings (F73, F72)</name>
  <files>weatherbot/weather/uv.py</files>
  <read_first>
    - weatherbot/weather/uv.py:264-272 (peak_uvi hourly-argmax vs daily max — F73, WR-02 trade-off documented at :267)
    - weatherbot/weather/uv.py:105-145 (fixed 06:00-20:00 fallback window, only on missing sun fields — F72, documented at :108/:127)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entries F73, F72
    - weatherbot/scheduler/daemon.py:54-60 (`# noqa: ... — rationale (phase)` house-style precedent)
  </read_first>
  <action>
    Add one inline accepted-annotation per finding at its site, no behavior change:
    F73 at `uv.py:267` (the peak_uvi derivation): `# ACCEPTED (F73, v2.1): WR-02 peak-clock coherence chosen over peak/max agreement; both rounded`.
    F72 at the `uv.py` fixed-fallback window line: `# ACCEPTED (F72, v2.1): fixed 06:00-20:00 fallback fires only on missing sun fields; mid-latitude deployment`.
    These satisfy SC-2's explicit-in-code-annotation requirement; the ledger (Plan 09) mirrors them as ACCEPTED. (F62's models.py annotation is landed in Plan 06 to keep models.py single-owner within Wave 1.)
  </action>
  <verify>
    <automated>uv run pytest tests/test_uv.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "# ACCEPTED (F73, v2.1):" weatherbot/weather/uv.py` returns 0 exit.
    - `grep -q "# ACCEPTED (F72, v2.1):" weatherbot/weather/uv.py` returns 0 exit.
    - git diff shows annotation-only changes at these sites (no runtime behavior change).
    - `uv run pytest tests/test_uv.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>F73/F72 carry accepted-with-rationale annotations at their sites; no behavior change; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenWeather API→client | The upstream HTTP response crosses into the client; F68 hardens the non-JSON 2xx path; F67 concerns the appid-in-log control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-05-SC | Information Disclosure | weather/client.py appid-in-logs (F67 context) | high | mitigate | Already mitigated by Phase-30 redaction (`_redact.py`, guarded by `test_redact_hygiene.py`). The F67 setLevel change (remove-or-accept) must keep that test green; redaction stays the authoritative control. |
| T-35-05-01 | Denial of Service | weather/client.py non-JSON 2xx (F68) | low | mitigate | A captive-portal HTML-200 no longer raises an unclassified `JSONDecodeError`; it maps to the transient/retry contract so the fetch path degrades predictably. Regression test on a 2xx HTML body. |
</threat_model>

<verification>
- `uv run pytest tests/test_uv_monitor.py tests/test_client.py tests/test_redact_hygiene.py tests/test_uv.py -q` exits 0.
- `uv run pytest -q` exits 0 (878 baseline; trust exit code over syrupy line).
- All accepted findings carry `# ACCEPTED (F##, v2.1):` at their sites (F59, F58?, F73, F72, F67?). (F62 lands in Plan 06.)
- No hub-path file in the diff.
</verification>

<success_criteria>
The uv/weather/client cluster is swept: F60/F61/F68 fixed (F60/F68 with regression tests), F67 resolved against redaction, F59/F72/F73/F62/F58 accepted-annotated; suite green.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-05-SUMMARY.md` when done.
</output>