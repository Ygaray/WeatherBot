---
phase: 35-cleanup-sweep
plan: 09
type: execute
wave: 3
depends_on:
  - 35-02
  - 35-03
  - 35-04
  - 35-05
  - 35-06
  - 35-07
  - 35-08
files_modified:
  - .planning/WHOLE-PROJECT-REVIEW.md
  - .planning/HUB-FINDINGS-HANDOFF.md
autonomous: true
requirements:
  - HARD-CLEAN-01
  - HARD-CLEAN-02
must_haves:
  truths:
    - "A new `## Disposition Ledger (v2.1)` table is appended (non-destructively) to `.planning/WHOLE-PROJECT-REVIEW.md` mapping EVERY WB + BOTH finding (88 WB + 11 BOTH = 99) to exactly one disposition: `FIXED@<phase>`, `ACCEPTED`, or `DEFERRED(target)`; every HUB finding (17) gets a `HUB (routed → HUB-FINDINGS-HANDOFF.md)` row for completeness (D-03, SC-3)."
    - "The already-fixed findings are recorded verify-then-mark WITHOUT re-touching code: F69@32, F65@32, F33@32, F35@32, F89@29, F90@29, F84@33, F28@33, F86@33, F104@(13/33), F58/F53 if verified-fixed, and F106-F116@34 — each verified against current source, then marked FIXED@<phase> (D-04)."
    - "The findings fixed/accepted by Plans 02-08 are recorded with their dispositions matching the in-code `# ACCEPTED (F##, v2.1)` annotations, so the ledger and the code annotations agree — no finding is silently left without a disposition (SC-2 no-silent-debt, SC-3 reconciles)."
    - "The 17 hub findings are confirmed present in `HUB-FINDINGS-HANDOFF.md` and marked out-of-milestone, and the 17-vs-18 discrepancy is annotated: the 17 = audit-surfaced hub defects (H01-H17); H18 (ready_gate.run no first-class fatal outcome) is a Phase-29-appended deferred enhancement inflating the handoff's severity line to 18 — not one of the 17 defects (D-03, A5)."
    - "No hub file is edited: the reconciliation only annotates the handoff DOC (`HUB-FINDINGS-HANDOFF.md`) and the review DOC — zero `yahir_reusable_bot/` or `../Reusable/` edits across the entire phase diff (SC-3 hub-untouched)."
  artifacts:
    - ".planning/WHOLE-PROJECT-REVIEW.md (appended ## Disposition Ledger (v2.1) table)"
    - ".planning/HUB-FINDINGS-HANDOFF.md (17-vs-18 reconciliation note)"
  key_links:
    - "the in-code `# ACCEPTED (F##, v2.1)` annotations from Plans 02-08 ↔ the ledger ACCEPTED rows — the two must agree (D-02/D-03 mirror)"
    - "the 88 WB + 11 BOTH + 17 HUB finding set from WHOLE-PROJECT-REVIEW.md §Severity Summary — the completeness contract for the ledger"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/; the hub reconciliation only annotates the handoff DOC and the review DOC."
    - statement: "The Disposition Ledger is APPENDED non-destructively — do NOT rewrite the audit's severity sections or per-finding prose (Pitfall: in-place surgery on severity sections)."
    - statement: "No finding may be silently dropped: every WB + BOTH finding gets exactly one disposition row; any DEFERRED finding names a target (no silent drop, D-03)."
    - statement: "Do not mark a finding FIXED@<phase> without verifying the current source state first (D-04) — a no-op 'fix' that fights a Phase-32/33 helper is the warning sign of a mis-classification."
---

<objective>
The Wave-3 reconciliation plan (pure-docs, no code): create the v2.1 Disposition Ledger in `.planning/WHOLE-PROJECT-REVIEW.md` mapping every in-scope WB/BOTH finding to FIXED@/ACCEPTED/DEFERRED and every HUB finding to routed-out-of-scope, and annotate the 17-vs-18 hub discrepancy. Depends on all fix/accept plans so it captures their final dispositions; runs last so nothing is silently left open.

Purpose: SC-3 — the v2.1 finding ledger reconciles; 17 hub findings confirmed routed out; no silent debt (the evidence the milestone audit reads).
Output: appended Disposition Ledger + hub reconciliation note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the v2.1 Disposition Ledger to WHOLE-PROJECT-REVIEW.md (D-03)</name>
  <files>.planning/WHOLE-PROJECT-REVIEW.md</files>
  <read_first>
    - .planning/WHOLE-PROJECT-REVIEW.md §Severity Summary (the 88 WB + 11 BOTH + 17 HUB = 116 finding counts — the completeness contract) and the full list of `### F##` headers
    - .planning/phases/35-cleanup-sweep/35-RESEARCH.md §"Reconciliation with Phases 29-34 (D-04) — Summary Table" (the already-fixed vs still-open buckets) and §"Ledger Write-Back Format (D-03)" (recommended appended-table shape)
    - the SUMMARY.md files from Plans 02-08 (the final FIX/ACCEPT disposition + phase for each finding they touched)
    - each `# ACCEPTED (F##, v2.1)` annotation landed by Plans 03/05/06/07/08 (grep `weatherbot/` for the full accepted set — the ledger ACCEPTED rows must match)
  </read_first>
  <action>
    Verify the current source state for every "already-fixed" finding (D-04) before marking it — grep the symbol/pattern; if clean, mark FIXED@<phase> and add nothing to code. Then APPEND (do not rewrite existing sections) a `## Disposition Ledger (v2.1)` table at the END of `.planning/WHOLE-PROJECT-REVIEW.md` with exactly one row per WB + BOTH finding (F01-F116, excluding the HUB-tagged F39/F41-F45/F93-F102), each mapped to `FIXED@<phase>` | `ACCEPTED` | `DEFERRED(target)`. Use the research buckets: verify-then-mark (F69@32, F65@32, F33@32, F35@32, F89@29, F90@29, F84@33, F28@33, F86@33, F104@13/33, F106-F116@34); Plan-02-08 dispositions (F46/F92/F76/F16 FIXED; F74/F75/F60/F61/F68/F105/F85/F70 FIXED-with-test; F59/F72/F73/F62/F58/F77/F56/F57/F52/F88/F103/F51/F80/F83/F79?/F82?/F67? ACCEPTED per what the plans landed). Add a HUB row block: every HUB finding (the 17: F39,F41-F45,F93-F102 → H01-H17) marked `HUB (routed → HUB-FINDINGS-HANDOFF.md, out-of-scope)`. Every WB/BOTH finding must appear exactly once; any DEFERRED names a target. Add a short header note stating the source of each disposition (in-code annotation for ACCEPTED, git provenance for FIXED@).
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - A `## Disposition Ledger (v2.1)` section exists at the end of `.planning/WHOLE-PROJECT-REVIEW.md` (`grep -q "## Disposition Ledger (v2.1)" .planning/WHOLE-PROJECT-REVIEW.md`).
    - Every WB + BOTH finding id appears exactly once in the ledger: a scripted check over `F01`..`F116` (excluding HUB-tagged ids) confirms each has exactly one disposition row (no missing, no duplicate).
    - Every `ACCEPTED` row corresponds to an in-code `# ACCEPTED (F##, v2.1)` annotation (the ledger and code agree): the set of `ACCEPTED` finding ids equals the set from `grep -ohE "# ACCEPTED \(F[0-9]+, v2.1\)" weatherbot/ -r | grep -oE "F[0-9]+" | sort -u`.
    - The existing §Severity Summary and per-finding prose are UNCHANGED (append-only; git diff shows only an added trailing section).
    - `uv run pytest -q` exits 0 (docs-only change; suite unaffected).
  </acceptance_criteria>
  <done>The v2.1 Disposition Ledger is appended non-destructively; every WB/BOTH finding has exactly one disposition matching its code annotation; HUB findings marked routed-out.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm hub routing + annotate the 17-vs-18 discrepancy (D-03, A5)</name>
  <files>.planning/HUB-FINDINGS-HANDOFF.md</files>
  <read_first>
    - .planning/HUB-FINDINGS-HANDOFF.md (the header "17 findings" claim, the severity line `high 2 · medium 5 · low 10 · cleanup 1 (total 18)`, and the H01-H18 entries; H18 = `ready_gate.run` no first-class fatal outcome, tagged "enhancement")
    - .planning/phases/35-cleanup-sweep/35-RESEARCH.md §"The 17-vs-18 discrepancy (D-03)" + Assumption A5
  </read_first>
  <action>
    Confirm H01-H17 (the 17 audit-surfaced hub defects) are present in `HUB-FINDINGS-HANDOFF.md` and marked out-of-milestone. Add a short reconciliation note (near the severity line) stating: "17 audit findings (H01-H17) + 1 Phase-29-appended deferred enhancement (H18 = ready_gate.run no first-class fatal outcome) = 18 rows; the milestone's out-of-scope count is the 17 defects. WeatherBot repins after the hub ships v0.1.2." Do NOT edit any hub source file — this annotates the handoff DOC only. Do not silently trust either bare number (D-03).
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "H01" .planning/HUB-FINDINGS-HANDOFF.md` and `grep -q "H17" .planning/HUB-FINDINGS-HANDOFF.md` and `grep -q "H18" .planning/HUB-FINDINGS-HANDOFF.md` all return 0 exit (the 17 defects + the H18 enhancement are present).
    - A reconciliation note explaining "17 audit findings (H01-H17) + 1 deferred enhancement (H18) = 18 rows" exists in the handoff doc (grep for a distinguishing phrase, e.g. `grep -q "17 audit findings" .planning/HUB-FINDINGS-HANDOFF.md`).
    - No `yahir_reusable_bot/` or `../Reusable/` file appears in the phase diff (`git diff --name-only` across the phase touches only `weatherbot/`, `tests/`, and `.planning/` files).
    - `uv run pytest -q` exits 0 (docs-only change).
  </acceptance_criteria>
  <done>The 17 hub findings are confirmed routed out-of-milestone and the 17-vs-18 discrepancy is annotated in the handoff doc; no hub source edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | This plan edits planning DOCS only (the review + handoff); no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-09-01 | (none) | planning docs (ledger + handoff) | low | accept | Docs-only reconciliation; no production code, no runtime surface. The hub boundary is honored (annotate the handoff doc, never a hub source file). |
</threat_model>

<verification>
- `grep -q "## Disposition Ledger (v2.1)" .planning/WHOLE-PROJECT-REVIEW.md`.
- Every WB/BOTH finding id appears exactly once in the ledger; every ACCEPTED row matches an in-code `# ACCEPTED (F##, v2.1)` annotation.
- The 17 hub findings are marked routed-out; the 17-vs-18 note is present.
- `git diff --name-only` across the whole phase shows NO `yahir_reusable_bot/` or `../Reusable/` file (SC-3 hub-untouched).
- `uv run pytest -q` exits 0 (878 baseline; trust exit code over the syrupy line).
</verification>

<success_criteria>
The v2.1 finding ledger reconciles: every in-scope WB finding is FIXED/ACCEPTED/DEFERRED, the 17 hub findings are confirmed routed out, the 17-vs-18 discrepancy is annotated, and the hub boundary was never crossed.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-09-SUMMARY.md` when done.
</output>