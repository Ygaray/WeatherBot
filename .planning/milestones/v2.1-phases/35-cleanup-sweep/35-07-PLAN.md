---
phase: 35-cleanup-sweep
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/weather/multiday.py
  - tests/test_multiday.py
autonomous: true
requirements:
  - HARD-CLEAN-02
user_setup:
  - service: none
    why: "F71 (Friday-as-weekend) is a domain-intent decision flagged for the user; the plan takes the conservative accept-with-rationale default (A1: latent, not a live double-send on the current single-slot deployment). If the user later configures overlapping weekday+weekend slots on one location, F71 becomes a live double-send bug to FIX+test."
must_haves:
  truths:
    - "F71 (`_WEEKEND_DAYS` includes 'fri', overlapping `_WEEKDAY_DAYS`) is ACCEPTED-with-rationale via an in-code `# ACCEPTED (F71, v2.1): ...` annotation at the `_WEEKEND_DAYS` tuple, because on the current single-slot deployment no location configures overlapping weekday+weekend slots so there is no live Friday double-send (A1); the annotation documents that this becomes a live bug to fix+test if overlapping slots are ever configured (D-01 accept, D-06 no behavior change, Open-Q2 conservative choice)."
    - "F70 (drop cannot override an explicit add of the same day) is FIXED so drop wins over add (or contradictory input is rejected), with a regression test proving drop-beats-add for the same day and that non-contradictory add/drop still behave as before (F70, D-01 fix, D-06 behavior-CHANGING → regression test)."
    - "The multiday day-selection behavior for all existing (non-contradictory) inputs is unchanged — the briefing-scheduling invariant is untouched (D-06)."
  artifacts:
    - "weatherbot/weather/multiday.py (F71 accepted-annotation; F70 drop-beats-add fix)"
    - "tests/test_multiday.py (F70 regression test)"
  key_links:
    - "multiday.select_days ← forecast slot day-selection — F70 add/drop token resolution feeds which days a multi-day forecast covers"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/."
    - statement: "Do NOT drop 'fri' from `_WEEKEND_DAYS` — F71 is ACCEPTED-with-rationale on the current deployment (A1); changing the weekday/weekend selection is a behavior change flagged for the user, not taken autonomously in this --auto run."
    - statement: "F70's fix must preserve behavior for every non-contradictory add/drop input; only the contradictory same-day +X/-X case changes (drop wins)."
---

<objective>
Sweep the multiday cluster: accept-with-rationale F71 (Friday-as-weekend — conservative Open-Q2 default, flagged for the user) and fix F70 (make drop win over a contradictory same-day add) with a regression test.

Purpose: SC-2 (HARD-CLEAN-02) — resolve or explicitly accept the low findings, no silent debt.
Output: annotated + fixed `multiday.py` + F70 regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Accept-annotate Friday-as-weekend (F71, Open-Q2 conservative default)</name>
  <files>weatherbot/weather/multiday.py</files>
  <read_first>
    - weatherbot/weather/multiday.py:32-33 (`_WEEKDAY_DAYS = ("mon",...,"fri")` and `_WEEKEND_DAYS = ("fri","sat","sun")` — the 'fri' overlap)
    - weatherbot/weather/multiday.py:84-86 (base day-set selection by `kind`)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entry F71 + .planning/phases/35-cleanup-sweep/35-RESEARCH.md Assumption A1 + Open Question 2
  </read_first>
  <action>
    Add an in-code `# ACCEPTED (F71, v2.1): Friday intentionally counted as weekend for the home/travel split; no live double-send because the current single-slot deployment configures no overlapping weekday+weekend slots on one location (A1). If overlapping weekday+weekend slots are ever configured, this becomes a live Friday double-send bug to FIX (drop 'fri' from _WEEKEND_DAYS) + regression-test.` at the `_WEEKEND_DAYS` tuple. No behavior change (D-06). This is the conservative --auto choice per Open-Q2; the plan's `user_setup` flags the domain-intent decision for the user at milestone review.
  </action>
  <verify>
    <automated>grep -q "# ACCEPTED (F71, v2.1):" weatherbot/weather/multiday.py && uv run pytest tests/test_multiday.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "# ACCEPTED (F71, v2.1):" weatherbot/weather/multiday.py` returns 0 exit; the annotation is at the `_WEEKEND_DAYS` site.
    - `_WEEKEND_DAYS` is UNCHANGED (still contains 'fri') — no behavior change.
    - `uv run pytest tests/test_multiday.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>F71 carries an accepted-with-rationale annotation documenting the latent-vs-live boundary; weekday/weekend selection unchanged; flagged for the user.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make drop win over contradictory same-day add (F70)</name>
  <files>weatherbot/weather/multiday.py, tests/test_multiday.py</files>
  <read_first>
    - weatherbot/weather/multiday.py:108-118 (the `desired` set build: `drop` applied to `upcoming`, then `for tok in add: desired.add(...)` — an add re-adds a dropped day; drop cannot override, F70)
    - tests/test_multiday.py:135-176 (the `select_days(kind, today, daily, add=..., drop=..., tz=_TZ)` call-shape + flag-token analogs to reuse)
    - .planning/phases/34-test-gap-backfill/34-PATTERNS.md §multiday flag-token analogs
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entry F70
  </read_first>
  <behavior>
    - `select_days(..., add={"sat"}, drop={"sat"}, ...)` → Saturday is NOT in the result (drop wins over the contradictory same-day add).
    - `select_days(..., add={"sat"}, drop=set(), ...)` → Saturday IS added (non-contradictory add unchanged).
    - `select_days(..., add=set(), drop={"sat"}, ...)` → Saturday dropped from the base block (unchanged).
    - No `IndexError`/crash on contradictory input.
  </behavior>
  <action>
    Make `drop` win over `add` for the same day: after building the additive `desired` set, subtract the `drop` days (or apply drop AFTER add), so a same-day `+X -X` resolves to dropped. Preserve behavior for every non-contradictory input (add-only, drop-only, disjoint add/drop). Add a regression test in `tests/test_multiday.py` named `# HARD-CLEAN-02 / F70` asserting: contradictory `add={"sat"}, drop={"sat"}` excludes Saturday; add-only includes it; drop-only excludes it; no crash. Reuse the `select_days` call-shape + `imp` fixture from the module.
  </action>
  <verify>
    <automated>uv run pytest tests/test_multiday.py -q -k "F70 or add or drop"</automated>
  </verify>
  <acceptance_criteria>
    - The F70 regression test exists, names the finding id, and is GREEN (fails against the pre-fix add-re-adds behavior for the contradictory case).
    - Non-contradictory add-only / drop-only / disjoint cases still behave as before (regression assertions green).
    - `uv run pytest tests/test_multiday.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>Drop wins over a contradictory same-day add; non-contradictory behavior preserved; a finding-tagged regression test pins it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config add/drop tokens→day selection | Hand-edited add/drop flags select forecast days; F70 makes contradictory input deterministic (drop wins). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-07-01 | (none) | multiday _WEEKEND_DAYS (F71) | low | accept | No behavior change; annotation documents the latent-vs-live boundary. On the current single-slot deployment there is no double-send (A1). |
| T-35-07-02 | Tampering | multiday select_days add/drop (F70) | low | mitigate | Drop-beats-add makes contradictory input deterministic; regression test + full suite green confirm non-contradictory behavior is preserved. |
</threat_model>

<verification>
- `uv run pytest tests/test_multiday.py -q` exits 0 (F70 regression green).
- `grep -q "# ACCEPTED (F71, v2.1):" weatherbot/weather/multiday.py`.
- `uv run pytest -q` exits 0 (878 baseline; trust exit code over syrupy line).
- No hub-path file in the diff.
</verification>

<success_criteria>
F71 accepted-with-rationale (flagged for the user), F70 fixed with a regression test; multiday behavior preserved for existing inputs; suite green.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-07-SUMMARY.md` when done.
</output>
