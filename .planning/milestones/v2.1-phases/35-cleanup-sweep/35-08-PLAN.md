---
phase: 35-cleanup-sweep
plan: 08
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - weatherbot/scheduler/daemon.py
  - weatherbot/scheduler/wiring.py
  - weatherbot/scheduler/context.py
  - tests/test_reload.py
  - tests/test_filewatch.py
  - tests/test_scheduler.py
autonomous: true
requirements:
  - HARD-CLEAN-01
  - HARD-CLEAN-02
must_haves:
  truths:
    - "The dead production functions `emit_online` and `_do_reload` are removed from scheduler/daemon.py, because Open-Q1 is resolved: the LIVE reload path routes through the hub `reload_engine.service_pending()` (not `_do_reload`) and the LIVE online-ping is inlined in `_run_daemon` (not `emit_online`) — both have ZERO runtime callers (only stale comment references) (F16, D-05, Open-Q1 traced-and-confirmed-dead)."
    - "Tests that exercise ONLY the dead twins are removed or migrated: `test_reload.py`/`test_filewatch.py` drive `_do_reload` as their reload entrypoint but the live reload behavior is covered by `test_reload_engine.py`, so the `_do_reload`-exclusive tests are removed (D-05) and the full suite stays green — a red suite after removal would mean the symbol was NOT dead (revert)."
    - "The LIVE F103 over-guard `getattr(send_result, 'ok', True)` in `_run_daemon`'s inlined online-ping (which masks a channel returning None on failure) is ACCEPTED-with-rationale via an in-code `# ACCEPTED (F103, v2.1): ...` annotation, because it is a single missed WARNING on a best-effort ping (D-01 accept, D-02)."
    - "The daemon/wiring/context latent findings F56 (pre-local_date swallow), F57 (retry-pause worker starvation), F52 (transient ConfigHolder in reload closures), F88 (context _fmt naive-dt), F53 (wiring scheduler.start in best-effort hook) are each ACCEPTED-with-rationale (or verified-already-fixed) — no silent debt (D-01/D-02, D-04)."
    - "Removal is behavior-preserving (D-06): the anchor invariant 'the morning briefing always goes out, exactly once' is untouched — full suite green (878) is the guard; if removing a symbol reddens the suite, it was live and the removal is reverted + re-classified."
  artifacts:
    - "weatherbot/scheduler/daemon.py (emit_online + _do_reload removed; F103-live/F56/F57 annotations)"
    - "weatherbot/scheduler/wiring.py (F52/F53 annotations)"
    - "weatherbot/scheduler/context.py (F88 annotation)"
    - "tests/test_reload.py, tests/test_filewatch.py, tests/test_scheduler.py (orphaned _do_reload/emit_online tests removed)"
  key_links:
    - "reload_engine.service_pending() (daemon.py:_run_daemon SIGHUP loop) — the LIVE reload path proving `_do_reload` is a dead twin"
    - "the inlined online-ping in _run_daemon (daemon.py:~1622) — the LIVE online path proving `emit_online` is a dead twin; also the surviving F103 over-guard site"
    - "test_reload_engine.py — the tests that cover the LIVE reload behavior (must stay green after `_do_reload` removal)"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/."
    - statement: "Do NOT remove any test that exercises the LIVE reload/online path (test_reload_engine.py stays). Remove ONLY tests that exercise exclusively the removed dead twins _do_reload/emit_online (D-05, Pitfall 2)."
    - statement: "If the full suite goes RED after removing `_do_reload` or `emit_online`, the symbol was NOT dead — REVERT the removal and re-classify (the acceptance gate is a green suite after removal)."
    - statement: "The removed dead-twin comment references (daemon.py:1277-1283, :1401, :1464, :1631, :1699 mentioning emit_online/_do_reload) must be cleaned or corrected so no stale reference to a removed symbol remains."
---

<objective>
The high-blast-radius, isolated Wave-2 plan: remove the dead `emit_online`/`_do_reload` twins from daemon.py (F16 — Open-Q1 traced and confirmed dead: live paths are `reload_engine.service_pending()` and the inlined `_run_daemon` online-ping), remove their exclusively-orphaned tests (D-05), and accept-annotate the surviving daemon/wiring/context latent findings (F103-live, F56, F57, F52, F88, F53). Isolated because it owns daemon.py/wiring.py/context.py and touches three heavily-used test modules; gated on the Wave-0 grep gate (Plan 01) so drift-back is loud and a red suite after removal is caught immediately.

Purpose: SC-1 (F16 dead-code removal) + SC-2 (daemon-cluster latent findings accepted, no silent debt).
Output: pruned daemon.py + orphaned tests removed + accepted-annotations across the daemon cluster.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trace-then-remove dead emit_online + _do_reload + orphaned tests (F16)</name>
  <files>weatherbot/scheduler/daemon.py, tests/test_reload.py, tests/test_filewatch.py, tests/test_scheduler.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py:1010 (`def _do_reload`) and :1286 (`def emit_online`) — the dead twins; the def bodies + the comment block :1277-1283 that says gate_until_healthy was removed@29 and defers these to Phase 35
    - weatherbot/scheduler/daemon.py:1690-1720 (the `_run_daemon` SIGHUP loop calling `reload_engine.service_pending(config_path)` — the LIVE reload path; `_do_reload` is NEVER called at runtime)
    - weatherbot/scheduler/daemon.py:1618-1626 (the inlined online-ping in `_run_daemon` — the LIVE online path; `emit_online` is NEVER called at runtime)
    - tests/test_reload.py (29 `_do_reload` references — the reload-behavior tests that drive the dead twin) and tests/test_filewatch.py (13 references) and tests/test_scheduler.py (4 `emit_online` references)
    - tests/test_reload_engine.py (the LIVE reload-behavior tests via `service_pending` — MUST stay green, do NOT touch)
    - .planning/WHOLE-PROJECT-REVIEW.md §Cleanup entry F16 + .planning/phases/35-cleanup-sweep/35-RESEARCH.md Open Question 1 + Pitfall 2
  </read_first>
  <action>
    First CONFIRM the trace (already established in research): `grep -n "emit_online(\|_do_reload(" weatherbot/` shows only the two defs + stale comments — zero runtime callers; the live reload is `reload_engine.service_pending()` and the live online-ping is inlined in `_run_daemon`. Then remove the `_do_reload` function and the `emit_online` function from `daemon.py`, and clean/correct the stale comment references to them (daemon.py:1277-1283, :1401, :1464, :1631, :1699) so no reference to a removed symbol remains. Remove the tests that exercise ONLY these dead twins: the `_do_reload`-driven tests in `test_reload.py`/`test_filewatch.py` (they assert reload behavior against the dead twin; the live reload behavior is covered by `test_reload_engine.py`) and the `emit_online`-driven tests in `test_scheduler.py`. Keep every test that covers a live path (the SIGHUP-flag semantics `test_sighup_triggers_reload`, the `_reload_cli`/pid-guard tests, and the filewatch observer tests, if any exercise the live observer rather than `_do_reload` — inspect each and keep the ones whose assertion survives without the dead twin). RUN THE FULL SUITE: if it goes red, the symbol was live — REVERT and re-classify (per the prohibition). A green 878-suite after removal is the acceptance gate.
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "def emit_online\(|def _do_reload\(" weatherbot/scheduler/daemon.py` returns 0 (both dead twins removed).
    - No stale reference to a removed symbol remains: `! grep -qE "emit_online|_do_reload" weatherbot/scheduler/daemon.py` (comments cleaned/corrected).
    - `test_reload_engine.py` still passes (live reload behavior intact): `uv run pytest tests/test_reload_engine.py -q` exits 0.
    - The Wave-0 gate now enforces F16 absence: `uv run pytest tests/test_dead_code_removed.py -q` exits 0.
    - `uv run pytest -q` exits 0 (878 baseline; a RED suite means the symbol was live — REVERT). Trust the exit code over the syrupy "2 snapshots failed" line.
    - `git diff --name-only` shows no `yahir_reusable_bot/` or `../Reusable/` file.
  </acceptance_criteria>
  <done>The dead `emit_online`/`_do_reload` twins and their exclusively-orphaned tests are removed; live reload/online behavior (test_reload_engine.py) stays green; full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Accept-annotate daemon/wiring/context latent findings (F103-live, F56, F57, F52, F88, F53)</name>
  <files>weatherbot/scheduler/daemon.py, weatherbot/scheduler/wiring.py, weatherbot/scheduler/context.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py:~1622 (the LIVE inlined online-ping `getattr(send_result, "ok", True)` over-guard — F103 SURVIVES here after emit_online removal)
    - weatherbot/scheduler/daemon.py:172-180 (`fire_slot` pre-`local_date` exception swallow — F56; triggers narrow: tz config-validated, ValueError arm unreachable from real caller)
    - weatherbot/scheduler/daemon.py:104-112 (heartbeat cadence / retry-pause worker note — F57; not reachable at 2-slot scale, misfire_grace_time=None delays not skips heartbeat)
    - weatherbot/scheduler/wiring.py:298-304 region (transient `ConfigHolder` in reload closures — F52) and :432-446 (`scheduler.start()` in the best-effort READY hook — F53; verify Phase 29-33 lifecycle work didn't already move it)
    - weatherbot/scheduler/context.py:45-48 (`_fmt` naive-datetime `astimezone` — F88; no caller feeds a naive dt)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entries F103, F56, F57, F52, F88, F53
  </read_first>
  <action>
    Add one in-code `# ACCEPTED (F##, v2.1): <rationale>` annotation per finding at its site (D-02), no behavior change:
    F103 at the LIVE online-ping over-guard: `# ACCEPTED (F103, v2.1): getattr(...,'ok',True) over-guard masks a channel returning None on failure; single missed WARNING on a best-effort ping`.
    F56 at the `fire_slot` pre-local_date arm: `# ACCEPTED (F56, v2.1): pre-local_date triggers unreachable given validated tz + real caller path`.
    F57 at the heartbeat/retry-pause note: `# ACCEPTED (F57, v2.1): not reachable at 2-slot scale; misfire_grace_time=None delays not skips heartbeat`.
    F52 at the transient-ConfigHolder site: `# ACCEPTED (F52, v2.1): identity-divergence smell only; no reachable wrong behavior (downgraded from medium)`.
    F88 at `context.py:_fmt`: `# ACCEPTED (F88, v2.1): no caller feeds a naive dt (all sources tz-aware)` — OR the cheap PRESERVE fix `assert dt.tzinfo is not None` if it does not perturb any test; planner default is annotate.
    F53 at `wiring.py` `scheduler.start()`: FIRST verify whether 29-33 lifecycle work already moved `start()` out of the swallowing hook. If still in the best-effort hook, prefer the cheap fix (let a `start()` raise fail readiness) OR annotate `# ACCEPTED (F53, v2.1): scheduler.start() failure not reachable on the single-drive path; READY ordering documented`. If already moved, mark FIXED@<phase> in the ledger (Plan 09), no edit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py tests/test_reliability.py tests/test_lifecycle_module.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Each surviving latent finding carries its marker: `grep -c "# ACCEPTED (F103\|# ACCEPTED (F56\|# ACCEPTED (F57" weatherbot/scheduler/daemon.py` reflects the accepted daemon findings; `grep -q "# ACCEPTED (F52" weatherbot/scheduler/wiring.py`; `grep -q "# ACCEPTED (F88\|assert dt.tzinfo" weatherbot/scheduler/context.py`; F53 either annotated in wiring.py or marked FIXED in the ledger.
    - No behavior change from the annotations (git diff shows comment/annotation-only edits, except an optional F53/F88 cheap guard).
    - `uv run pytest tests/test_scheduler.py tests/test_reliability.py tests/test_lifecycle_module.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>Every surviving daemon/wiring/context latent finding is accepted-annotated (or verified-fixed); no behavior change; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler→send spine | The daemon's fire/reload/online paths guard the exactly-once briefing invariant; this plan removes dead twins and annotates latents without touching the live spine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-08-01 | Tampering | daemon.py dead-twin removal (F16) | low | mitigate | Removal is gated on a green 878-suite; a red suite means the twin was live → revert. The Wave-0 negative-grep gate (Plan 01) keeps the symbols from drifting back. |
| T-35-08-02 | Repudiation | daemon.py online-ping over-guard (F103-live) | low | accept | A single missed WARNING on a best-effort ping; annotated accepted. No effect on the send spine. |
| T-35-08-03 | Denial of Service | daemon heartbeat/retry-pause (F57) | low | accept | Not reachable at 2-slot scale; misfire_grace_time=None delays (not skips) the heartbeat. Annotated. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 after each task (878 baseline; a red suite after F16 removal = revert). Trust exit code over the syrupy line.
- `tests/test_reload_engine.py` green (live reload behavior intact).
- `tests/test_dead_code_removed.py` (Plan 01) green (F16 absence enforced).
- Every surviving daemon-cluster latent finding carries `# ACCEPTED (F##, v2.1):` at its site (F103-live, F56, F57, F52, F88, F53?).
- No hub-path file in the diff.
</verification>

<success_criteria>
The daemon cluster is swept: dead `emit_online`/`_do_reload` removed with orphaned tests (F16), live reload/online behavior intact, and every surviving daemon/wiring/context latent accepted-annotated; suite green.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-08-SUMMARY.md` when done.
</output>