---
phase: 35-cleanup-sweep
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - weatherbot/ops/pidfile.py
  - weatherbot/ops/selfcheck.py
  - tests/test_golden_coverage_fill.py
autonomous: true
requirements:
  - HARD-CLEAN-01
must_haves:
  truths:
    - "The dead WeatherBot copy of the `-m` guard `_argv_is_weatherbot` is removed from ops/pidfile.py, and its exclusive test `test_argv_is_weatherbot_empty_and_forms` is removed with it, because that test exercises ONLY the dead function and no live path (F46, D-05)."
    - "The result-discarding `is_transient(exc)` call in ops/selfcheck.py is removed because both classifier branches already return NETWORK_NOT_READY regardless of its value; classification behavior is byte-identical after removal (F92, D-05, D-06 behavior-preserving)."
    - "Removal is behavior-preserving (D-06): the full suite stays green at 878 with no regression test needed, because neither symbol has a production caller (verified: pidfile guard's live path is the hub's _argv_matches_marker; selfcheck's real classification is is_auth_failure + the unconditional NETWORK_NOT_READY)."
    - "Cleanup follows the fix-by-default rule (D-01): both are genuinely-still-open dead code, so both are FIXED (removed), not accepted."
  artifacts:
    - "weatherbot/ops/pidfile.py (F46 symbol removed)"
    - "weatherbot/ops/selfcheck.py (F92 discarded call removed)"
    - "tests/test_golden_coverage_fill.py (orphaned F46 test removed)"
  key_links:
    - "yahir_reusable_bot/lifecycle/identity.py:_argv_matches_marker — the LIVE guard (H01, hub, OUT OF SCOPE); the WB copy being removed is its dead twin, do NOT port the hub fix into WB"
    - "weatherbot/ops/selfcheck.py is_auth_failure branch — the real classifier that stays"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/; F46's live guard is the hub's H01 and is human-gated — remove the WB dead copy, do NOT port the hub's corrected -m match into it (Pitfall 3)."
    - statement: "Do not remove the `is_transient` import from selfcheck.py unless ruff confirms it is unused after the discarded-call line is deleted (is_auth_failure branch may still reference the module)."
    - statement: "Do not delete any selfcheck/pidfile test that also covers a live path — only the F46 test that exercises exclusively the dead function is removed (D-05)."
---

<objective>
Remove the two genuinely-open dead-code items in the ops cluster: the dead `-m` guard copy `_argv_is_weatherbot` (F46) and the result-discarding `is_transient(exc)` call (F92). Both are verified STILL OPEN with zero production callers; both removals are behavior-preserving (D-06), so full-suite-green is the sufficient guard.

Purpose: SC-1 (HARD-CLEAN-01) — remove dead/divergent code the audit named.
Output: pruned `ops/pidfile.py`, `ops/selfcheck.py`, and the orphaned F46 test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead `_argv_is_weatherbot` guard copy and its exclusive test (F46)</name>
  <files>weatherbot/ops/pidfile.py, tests/test_golden_coverage_fill.py</files>
  <read_first>
    - weatherbot/ops/pidfile.py:124 (`def _argv_is_weatherbot(cmdline: bytes) -> bool` — the dead copy; confirm no production caller with `grep -rn "_argv_is_weatherbot" weatherbot/`)
    - tests/test_golden_coverage_fill.py:467-482 (`test_argv_is_weatherbot_empty_and_forms` — exercises ONLY the dead function, no live path)
    - .planning/WHOLE-PROJECT-REVIEW.md §Cleanup entry F46 + §RESEARCH Pitfall 3 (do NOT port the hub H01 fix)
    - .planning/HUB-FINDINGS-HANDOFF.md H01 (the LIVE guard `_argv_matches_marker` — hub, out of scope)
  </read_first>
  <action>
    Confirm via `grep -rn "_argv_is_weatherbot(" weatherbot/` that the only reference outside its own def is the one test. Delete the entire `_argv_is_weatherbot` function from `weatherbot/ops/pidfile.py` (and any now-unused import it alone required — let ruff flag it). Delete `test_argv_is_weatherbot_empty_and_forms` from `tests/test_golden_coverage_fill.py`. Do NOT touch the hub guard or port its corrected `-m` matching (that is H01, human-gated). This is a pure removal of a dead twin — the live PID guard remains the hub's `_argv_matches_marker`.
  </action>
  <verify>
    <automated>! grep -rq "_argv_is_weatherbot" weatherbot/ tests/ && uv run pytest tests/test_golden_coverage_fill.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "_argv_is_weatherbot" weatherbot/ tests/` returns 0 across all files.
    - `uv run pytest tests/test_golden_coverage_fill.py -q` exits 0.
    - `git diff --name-only` shows no `yahir_reusable_bot/` or `../Reusable/` file.
    - `uv run pytest -q` exits 0 (878 baseline; trust exit code over syrupy line).
  </acceptance_criteria>
  <done>The dead WB `-m` guard copy and its exclusive test are gone; the hub's live guard is untouched; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove discarded `is_transient(exc)` selfcheck call (F92)</name>
  <files>weatherbot/ops/selfcheck.py</files>
  <read_first>
    - weatherbot/ops/selfcheck.py:128-145 (the except arm: `is_auth_failure(exc)` branch returns AUTH; the bare `is_transient(exc)` at :142 whose result is discarded; comment 140-141 admits "consulted for clarity/parity")
    - weatherbot/ops/selfcheck.py:35 (`from weatherbot.reliability import is_auth_failure, is_transient` — check if is_transient stays needed)
    - .planning/WHOLE-PROJECT-REVIEW.md §Cleanup entry F92
  </read_first>
  <action>
    Remove the standalone discarded `is_transient(exc)` statement at `selfcheck.py:142` (and the two adjacent comment lines 140-141 that only explain the discarded call). Both branches of the except arm already return `NETWORK_NOT_READY` regardless, so removal is behavior-preserving. After removal, run ruff; if `is_transient` is now an unused import, prune it from line 35 (keep `is_auth_failure`). Do NOT alter the `is_auth_failure(exc)` classification branch or the returned reason — classification output must be byte-identical.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ops_selfcheck.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The bare discarded `is_transient(exc)` line is gone from `selfcheck.py` (manual read of the except arm confirms no result-discarding classifier call remains).
    - `is_auth_failure` branch and the `NETWORK_NOT_READY` return path are unchanged (git diff shows only the discarded line + its comment removed, plus optional import prune).
    - `uv run pytest tests/test_ops_selfcheck.py -q` exits 0.
    - `uv run ruff check weatherbot/ops/selfcheck.py` reports no unused-import error.
  </acceptance_criteria>
  <done>The result-discarding classifier call is removed; selfcheck classification behavior is byte-identical; module tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process→PID-signal (pidfile) | The PID guard decides whether `weatherbot reload` signals a process; the LIVE guard is the hub's (H01), out of scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-02-01 | Elevation of Privilege | ops/pidfile.py PID guard | low | transfer | The signal-misdelivery risk lives in the hub's live `_argv_matches_marker` (H01), human-gated upstream. Removing the WB dead copy neither adds nor removes live-guard behavior. |
| T-35-02-02 | Tampering | ops/selfcheck.py classification | low | mitigate | Removal is behavior-preserving (both branches already return NETWORK_NOT_READY); `test_ops_selfcheck.py` + full suite green is the guard. |
</threat_model>

<verification>
- `grep -rc "_argv_is_weatherbot" weatherbot/ tests/` == 0.
- The Wave-0 gate `tests/test_dead_code_removed.py` (Plan 01) now enforces F46/F92 absence — `uv run pytest tests/test_dead_code_removed.py -q` exits 0.
- `uv run pytest -q` exits 0 (878 baseline).
- No hub-path file in the diff.
</verification>

<success_criteria>
Both dead ops-cluster symbols removed, behavior-preserving, suite green; hub boundary untouched.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-02-SUMMARY.md` when done.
</output>
