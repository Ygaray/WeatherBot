---
phase: 35-cleanup-sweep
plan: 03
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - weatherbot/cli.py
autonomous: true
requirements:
  - HARD-CLEAN-01
  - HARD-CLEAN-02
must_haves:
  truths:
    - "The inert `verbose` param is dropped from `run_weather` and from the `verbose=args.verbose` call site in `_cmd_weather`, because the param is accepted but never read — the real `-v` plumbing is `main()`/`_configure_logging` (F76, D-05, behavior-preserving D-06)."
    - "The `send-now` implicit-fallthrough latent (F78) gets an explicit dispatch guard / early-return so a future subcommand lacking a `location` attr cannot silently run the send pipeline; behavior for today's commands is unchanged (D-01 fix-by-default, PRESERVE)."
    - "The `check` vs registry exit-code divergence (F77) is ACCEPTED with an in-code `# ACCEPTED (F77, v2.1): ...` annotation at the site, because both per-command exit-code conventions are documented-intentional and a monitor keying on the exact code is hypothetical (D-01 accept-with-rationale, D-02)."
    - "Behavior-preserving (D-06): the F76 and F78 changes require no new regression test beyond full-suite-green; F77 is annotation-only with no behavior change."
  artifacts:
    - "weatherbot/cli.py (F76 param removed, F78 dispatch guard added, F77 accepted-annotation added)"
  key_links:
    - "weatherbot/cli.py main()/_configure_logging — the REAL -v verbosity plumbing that stays"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/."
    - statement: "Do not remove or alter the `--verbose`/`-v` argparse flag or the `main()`/`_configure_logging` verbosity handling — only the inert `run_weather(verbose=...)` param and its call-site pass-through are dead (F76)."
    - statement: "The F78 guard must be behavior-preserving for every current subcommand — it only prevents a future command from falling through to the send pipeline; it must not change today's send-now / weather / check dispatch."
---

<objective>
Sweep the `cli.py` cluster: remove the dead `verbose` param on `run_weather` (F76), add an explicit `send-now` dispatch guard against future fallthrough (F78), and annotate the intentional `check`/registry exit-code divergence as accepted (F77). One already-open file, three findings, all behavior-preserving.

Purpose: SC-1 (F76 dead param) + SC-2 (F77 accepted-with-rationale, F78 latent fix, no silent debt).
Output: pruned + annotated `weatherbot/cli.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead `verbose` param from run_weather + call site (F76)</name>
  <files>weatherbot/cli.py</files>
  <read_first>
    - weatherbot/cli.py:334 (`verbose: bool = False` in `run_weather` signature) and the body 334-397 (verify `verbose` is never read)
    - weatherbot/cli.py:410-418 (`_cmd_weather` caller passing `verbose=args.verbose`)
    - weatherbot/cli.py:982 + main()/_configure_logging (the REAL verbosity plumbing that must NOT change)
    - .planning/WHOLE-PROJECT-REVIEW.md §Cleanup entry F76
  </read_first>
  <action>
    Confirm with `grep -n "run_weather(" weatherbot/ tests/` that `_cmd_weather` is the only caller passing `verbose`. Drop the `verbose: bool = False` parameter from the `run_weather` signature and remove the `verbose=args.verbose` argument from the `_cmd_weather` call site. Leave the `-v/--verbose` argparse flag, `main()`, and `_configure_logging` untouched — those are the live verbosity path. This is behavior-preserving (the param was inert).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The `run_weather` signature no longer contains a `verbose` parameter (read of `cli.py:334` region confirms).
    - No caller passes `verbose=` to `run_weather`: `! grep -q "run_weather([^)]*verbose" weatherbot/ tests/` (region-scoped to run_weather call sites).
    - The `--verbose`/`-v` argparse flag still exists (`grep -q "\"--verbose\"" weatherbot/cli.py`) — the live path is untouched.
    - `uv run pytest tests/test_cli.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>The inert `verbose` param is gone from `run_weather` and its call site; live `-v` verbosity plumbing unchanged; CLI tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add send-now dispatch guard (F78) + accept check/registry exit-code divergence (F77)</name>
  <files>weatherbot/cli.py</files>
  <read_first>
    - weatherbot/cli.py:1030 region (the `send-now` implicit fallthrough — a future subcommand without a `location` attr would run the send pipeline)
    - weatherbot/cli.py:938 region (the `check` exit-1 vs registry exit-2 site — F77)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entries F77, F78
    - .planning/phases/35-cleanup-sweep/35-RESEARCH.md §D (F78 = PRESERVE fix; F77 = ACCEPT)
    - weatherbot/scheduler/daemon.py:54-60 (`# noqa: ... — rationale (phase)` house-style precedent the ACCEPTED annotation mirrors)
  </read_first>
  <action>
    F78: add an explicit dispatch guard for `send-now` so a future command lacking a `location` attribute cannot fall through into the send pipeline — e.g. an early-return / explicit command-name check / `hasattr(args, "location")` guard at the `send-now` dispatch point that raises a clear error or is a no-op for non-send commands. Keep today's `send-now`, `weather`, `check`, `run` dispatch byte-identical (behavior-preserving for existing commands).
    F77: at the `check` exit-1 site (`cli.py:938`), add an inline `# ACCEPTED (F77, v2.1): documented per-command exit-code conventions are intentional; a monitor keying on the exact code is hypothetical`. No behavior change — annotation only. This satisfies SC-2's "explicit in-code annotation recording it as accepted."
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "# ACCEPTED (F77, v2.1):" weatherbot/cli.py` returns 0 exit (annotation present at the F77 site).
    - The `send-now` guard exists and every existing subcommand's dispatch is unchanged (git diff shows only the added guard + annotation; no change to today's dispatch results).
    - `uv run pytest tests/test_cli.py -q` exits 0; `uv run pytest -q` exits 0 (send-now/weather/check exit codes unchanged).
  </acceptance_criteria>
  <done>F78 send-now fallthrough is guarded; F77 exit-code divergence is annotated accepted; existing CLI behavior unchanged; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→CLI dispatch | CLI argv selects a command; the F78 guard prevents an unintended send pipeline on a future command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-03-01 | Tampering | cli.py send-now dispatch | low | mitigate | F78 explicit guard prevents a future subcommand from silently invoking the send pipeline; full-suite-green confirms today's dispatch is unchanged. |
| T-35-03-02 | (none) | cli.py verbose param | low | accept | F76 is dead-param removal, no runtime behavior; no threat surface. |
</threat_model>

<verification>
- `tests/test_dead_code_removed.py` (Plan 01) enforces F76 param absence — green.
- `grep -q "# ACCEPTED (F77, v2.1):" weatherbot/cli.py`.
- `uv run pytest -q` exits 0 (878 baseline; trust exit code over syrupy line).
- No hub-path file in the diff.
</verification>

<success_criteria>
The cli.py cluster is swept: F76 removed, F78 guarded, F77 accepted-annotated; existing behavior preserved; suite green.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-03-SUMMARY.md` when done.
</output>
