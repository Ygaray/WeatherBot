---
phase: 35-cleanup-sweep
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/interactive/commands/info.py
  - weatherbot/interactive/commands/weather_views.py
  - weatherbot/interactive/bot.py
  - weatherbot/interactive/lookup.py
  - weatherbot/weather/models.py
  - tests/test_command_views.py
  - tests/test_bot.py
autonomous: true
requirements:
  - HARD-CLEAN-01
  - HARD-CLEAN-02
must_haves:
  truths:
    - "`!locations` marks the default (first) location so the user can see which name a bare command resolves to (F105, D-01 fix, D-06 behavior-CHANGING → snapshot regression)."
    - "The `next_cloudy` hourly 'When' label includes the date (`%a %b %d %H:%M`) so it is unambiguous, matching the daily/wind-window branches that already carry the date (F85, D-01 fix, D-06 behavior-CHANGING → snapshot regression)."
    - "The `alerts` docstring in models.from_payloads is corrected to state alerts are read once from the imperial payload (unit-independent, coordinate-keyed), not 'from each payload' (F66 doc, D-01 fix, PRESERVE); F104's lookup_forecast docstring is confirmed already-accurate and marked FIXED in the ledger, not re-touched (D-04)."
    - "F62 (`day_i.get('uvi') or 0.0` falsy-coalesce in models.py) is ACCEPTED with an in-code `# ACCEPTED (F62, v2.1): ...` annotation, because it is display-only and the hint-affecting fields already use None-preserving raw values (D-01 accept, D-02); the annotation lands here so models.py stays single-owner within Wave 1."
    - "The genuinely-cosmetic interactive findings F82 (wind-degree truncation), F79 (`!panel` exact-match silent-drop), F51 (cached bake-time timestamp), F80 (perms getattr no-default), F83 (no-cloudy-day count) are each either a one-char PRESERVE fix or ACCEPTED with an in-code `# ACCEPTED (F##, v2.1): ...` annotation per D-01/D-02."
  artifacts:
    - "weatherbot/interactive/commands/info.py (F105 default marker)"
    - "weatherbot/interactive/commands/weather_views.py (F85 dated hourly label; F82 round-or-accept; F83 accepted)"
    - "weatherbot/interactive/bot.py (F79 accept-or-fix; F80 accept-or-getattr-default)"
    - "weatherbot/interactive/lookup.py (F51 accepted-annotation; F104 verify-only)"
    - "weatherbot/weather/models.py (F66 corrected alerts docstring; F62 accepted-annotation)"
    - "tests/test_command_views.py (F105 + F85 snapshot regressions), tests/test_bot.py (F79 assertion if fixed)"
  key_links:
    - "info.py:40 locations() render ← bare-command default resolution — the user needs to know which name a bare `!weather` resolves to (F105)"
    - "weather_views.py hourly/daily/wind-window 'When' label branches — F85 aligns the hourly branch to the dated style the peers already use"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/."
    - statement: "F104 is VERIFY-ONLY — its docstring is already accurate at HEAD; do NOT rewrite it. Confirm the exact string and mark FIXED in the ledger (Plan 09)."
    - statement: "Render/snapshot changes (F105, F85) will move syrupy golden snapshots — regenerate with --snapshot-update and eyeball the .ambr diff; trust the exit code over the 'N snapshots failed' line (project memory quirk)."
---

<objective>
Sweep the interactive/render cluster plus two models docstrings: fix F105 (default marker, +snapshot) and F85 (dated hourly label, +snapshot), correct the F66 alerts docstring, verify F104 already-clean, and resolve the cosmetic F82/F79/F51/F80/F83 as one-char fixes or accepted-annotations.

Purpose: SC-1 (F66/F104 docs), SC-2 (F105/F85 fixes + F82/F79/F51/F80/F83 fix-or-accept, no silent debt).
Output: fixed/annotated interactive views + corrected docstrings + snapshot regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mark default location in !locations (F105) + date the hourly When label (F85)</name>
  <files>weatherbot/interactive/commands/info.py, weatherbot/interactive/commands/weather_views.py, tests/test_command_views.py</files>
  <read_first>
    - weatherbot/interactive/commands/info.py:40 (`lines = tuple((loc.name, loc.timezone) for loc in config.locations)` — first is default per the docstring, but not marked — F105)
    - weatherbot/interactive/commands/weather_views.py:237 (hourly `("When", when.strftime("%a %H:%M"))` — F85, no date) vs :253 (daily `%a %b %d`) and :152 (wind-window `%a %b %d %H:%M`) — the dated peers to align to
    - tests/test_command_views.py (existing view snapshot tests + the syrupy `__snapshots__` for these commands)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entries F105, F85
  </read_first>
  <behavior>
    - F105: the first (default) location in `!locations` output is visibly marked (e.g. append "(default)" or a 📍 marker), matching the "(default)" convention Phase 33 established for bare-command replies.
    - F85: the `next_cloudy` hourly "When" value includes the date, rendering `%a %b %d %H:%M` (or the module's dated style), unambiguous across a week.
  </behavior>
  <action>
    F105: in `locations()`, mark the first/default location (e.g. append " (default)" to its name cell or prepend 📍) consistent with the Phase-33 default marker convention. Add/extend a `tests/test_command_views.py` snapshot or assertion named `# HARD-CLEAN-02 / F105` proving the default is marked.
    F85: change the hourly "When" strftime at `weather_views.py:237` from `"%a %H:%M"` to the dated style `"%a %b %d %H:%M"` so it matches the daily/wind-window branches. Add/extend a snapshot assertion named `# HARD-CLEAN-02 / F85`.
    Both change rendered output → regenerate the affected syrupy snapshots with `--snapshot-update` and eyeball the `.ambr` diff to confirm only the intended marker/date changed. Trust the exit code over the "N snapshots failed" line.
  </action>
  <verify>
    <automated>uv run pytest tests/test_command_views.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `!locations` output marks the default location (snapshot/assertion GREEN; would differ against pre-fix unmarked output).
    - The `next_cloudy` hourly "When" renders a dated label (`grep -q "%a %b %d %H:%M" weatherbot/interactive/commands/weather_views.py` at the hourly branch; no remaining bare `"%a %H:%M"` at :237).
    - Both regression tests name their finding ids and are GREEN.
    - `uv run pytest tests/test_command_views.py -q` exits 0; `uv run pytest -q` exits 0 (trust exit code over the syrupy line; only intended snapshots moved).
  </acceptance_criteria>
  <done>F105 marks the default location, F85 dates the hourly label, both with finding-tagged snapshot regressions; views tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct F66 alerts docstring + accept-annotate F62 + verify F104 already-clean + accept-annotate F51</name>
  <files>weatherbot/weather/models.py, weatherbot/interactive/lookup.py</files>
  <read_first>
    - weatherbot/weather/models.py:263-265 (docstring `"``daily[0]`` ... and ``alerts[]`` from each payload"`) vs :322 (`alerts = onecall_imp.get("alerts") or []` — read ONCE from imperial only) — F66 doc mismatch
    - weatherbot/weather/models.py:326-332 (`day_i.get("uvi") or 0.0` falsy-coalesce — F62; hint-affecting fields already use None-preserving raw values)
    - weatherbot/interactive/lookup.py:163-184 (`lookup_forecast` docstring — already accurately says it "simply DELEGATES to lookup_weather", a "NAMED seam") — F104 VERIFY-ONLY
    - weatherbot/interactive/lookup.py:140-148 (cached `{sent_at}`/`{checked_at}` bake-time stamps — F51)
    - .planning/WHOLE-PROJECT-REVIEW.md §entries F66, F62, F104, F51
    - weatherbot/scheduler/daemon.py:54-60 (`# noqa: ... — rationale (phase)` house-style precedent for the annotation form)
  </read_first>
  <action>
    F66: correct the `from_payloads` docstring so it states alerts are read ONCE from the imperial payload and are unit-independent / coordinate-keyed (not "from each payload"). Behavior-preserving (doc-only), since alerts are read once and coordinate-keyed.
    F62: add `# ACCEPTED (F62, v2.1): falsy-coalesce is display-only; hint-affecting fields use None-preserving raw values` at the `models.py:330` `or 0.0` coalesce site. Annotation only, no behavior change.
    F104: read the `lookup_forecast` docstring; confirm it no longer claims the cache routes through it (it already says "DELEGATES to lookup_weather"). Do NOT rewrite — this is verify-then-mark; record FIXED in the ledger (Plan 09). No code edit.
    F51: add `# ACCEPTED (F51, v2.1): cached render keeps its bake-time {sent_at}/{checked_at}; cosmetic within TTL, no cached-read indicator` at the lookup.py:144 site.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The `from_payloads` docstring no longer says alerts are read "from each payload"; it states imperial-once / unit-independent (manual read confirms; code unchanged).
    - `grep -q "# ACCEPTED (F62, v2.1):" weatherbot/weather/models.py` returns 0 exit.
    - `lookup_forecast` docstring confirmed already-accurate (no "routed through the cache" claim); no edit to it.
    - `grep -q "# ACCEPTED (F51, v2.1):" weatherbot/interactive/lookup.py` returns 0 exit.
    - `uv run pytest tests/test_models.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>F66 docstring corrected, F62 accepted-annotated, F104 confirmed already-clean (ledger FIXED), F51 accepted-annotated; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve cosmetic bot/view findings F82, F79, F80, F83 (fix-or-accept)</name>
  <files>weatherbot/interactive/commands/weather_views.py, weatherbot/interactive/bot.py, tests/test_bot.py</files>
  <read_first>
    - weatherbot/interactive/commands/weather_views.py:207 (wind `f"{compass(deg)} ({int(deg)}°)"` — F82 degree truncation) and :244 (`daily[2:]` count in no-cloudy message — F83)
    - weatherbot/interactive/bot.py:492 (`if content.strip() == "!panel":` exact-match — F79 silent-drop of `!panel please`) and :379 (`getattr(perms, name)` no default — F80)
    - tests/test_bot.py (existing bot-dispatch assertions — slot an F79 assertion if fixed)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entries F82, F79, F80, F83
  </read_first>
  <action>
    Apply the D-01 default per finding (each is a one-char PRESERVE fix or an ACCEPT):
    F82: prefer the one-char honest fix — `round(deg)` instead of `int(deg)` in the wind-direction parenthetical (compass sector labeling is already correct). If fixed, add/extend a `test_command_views` assertion; else annotate `# ACCEPTED (F82, v2.1): int() degree truncation is cosmetic; compass sector label is correct`.
    F79: prefer the cheap fix — accept `!panel` with trailing text (match a `startswith("!panel")` / prefix on the operator-gated branch) so `!panel please` is not silently dropped; add a `test_bot` assertion. Else annotate `# ACCEPTED (F79, v2.1): near-miss !panel-with-text follows the general unknown-command silent-drop behavior`.
    F80: prefer the cheap PRESERVE fix — `getattr(perms, name, False)` default so a missing REQUIRED_PANEL_PERMS name yields "missing" rather than AttributeError. Else annotate `# ACCEPTED (F80, v2.1): all 5 perm names resolve on pinned discord.py 2.7.1; trigger needs a downgrade/typo`.
    F83: annotate `# ACCEPTED (F83, v2.1): no-cloudy-day count reports len(daily) not scanned window; cosmetic message, needs an unusual hourly-empty/daily-full split`.
    For every ACCEPT, add the in-code `# ACCEPTED (F##, v2.1): ...` marker; for every FIX, keep behavior for existing inputs and add the noted test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bot.py tests/test_command_views.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Each of F82, F79, F80, F83 is resolved: either a fix (with the noted test/assertion GREEN and existing behavior preserved) OR an `# ACCEPTED (F##, v2.1):` marker at the site — verify per finding with `grep -c "# ACCEPTED (F82\|# ACCEPTED (F79\|# ACCEPTED (F80\|# ACCEPTED (F83" weatherbot/interactive/` reflecting the accepted ones.
    - No finding is silently skipped — each maps to FIX or ACCEPT (recorded in Plan 09 ledger).
    - `uv run pytest tests/test_bot.py tests/test_command_views.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>F82/F79/F80/F83 each resolved as a one-char fix or accepted-annotation; existing behavior preserved; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user→bot dispatch | Inbound message content selects a command; F79/F80 concern command matching + permission preflight (behavior-preserving here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-06-01 | Denial of Service | bot.py permission preflight (F80) | low | mitigate | `getattr(perms, name, False)` default (if fixed) turns a missing-perm-name AttributeError into a clean "missing" refusal; else accepted (all names resolve on pinned discord.py). No new surface. |
| T-35-06-02 | (none) | interactive render labels (F105/F85/F82/F83) | low | accept | Display-only rendering changes; no trust boundary crossed. Snapshot regressions guard the intended output. |
</threat_model>

<verification>
- `uv run pytest tests/test_command_views.py tests/test_bot.py tests/test_models.py -q` exits 0.
- `uv run pytest -q` exits 0 (878 baseline; trust exit code over the syrupy line; only intended snapshots moved).
- Every accepted interactive finding carries `# ACCEPTED (F##, v2.1):` at its site.
- No hub-path file in the diff.
</verification>

<success_criteria>
The interactive/render + models-docs cluster is swept: F105/F85 fixed (snapshots), F66 docstring corrected, F104 verified-clean, F82/F79/F51/F80/F83 fixed-or-accepted; suite green.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-06-SUMMARY.md` when done.
</output>
