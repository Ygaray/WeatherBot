---
phase: 33-interactive-panel-robustness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_lifecycle_module.py
  - weatherbot/scheduler/wiring.py
autonomous: true
requirements: [HARD-UI-02]
must_haves:
  truths:
    # Derived from D-04/F17/F22 (authoritative edges — CONTEXT.md + WHOLE-PROJECT-REVIEW.md)
    - "On config reload, `_on_applied` runs `cache.invalidate()` BEFORE the (slow) `channel.send(...)` reload-outcome post, so a slow Discord post can no longer delay invalidation and serve OLD coords to an inbound `!weather <loc>` (F17)."
    - "The reorder keeps both side-effects best-effort (each wrapped try/except … BLE001 — a hiccup never aborts the already-committed reload) and preserves the EXACT reload-outcome send string."
    - "A `SelectedContext` value naming a location the reloaded config no longer has (renamed/removed) is reconciled on hot-reload — reset to the default (config.locations[0].name) — so a later `resolve_location(selection.value)` cannot raise UnknownLocationError for a location the user never sees selected (F22)."
    - "The F22 reconcile is a best-effort sibling of `_prune_forecast_streaks` (same try/except style), keyed off the now-live desired location set."
    # Flagged assumption — deterministic edge probe could not classify HARD-UI-02 audit text (specless fallback §A):
    - { statement: "HARD-UI-02 (reload-wiring slice) audit-style requirement text is unclassified by the deterministic edge probe; its real edges are covered by the D-04/F17/F22 truths above — flagged assumption, not a silent drop.", verification: backstop }
  artifacts:
    - "weatherbot/scheduler/wiring.py — `_on_applied` invalidate-before-send reorder + SelectedContext reconcile-on-reload sibling"
    - "tests/test_lifecycle_module.py — invalidate-before-send ordering + selection-reconcile regressions"
  key_links:
    - "cache.invalidate() (Plan 02 generation bump) ← _on_applied ordering: invalidating before the slow send means the F13 generation bump lands before a slow post — the fixes compose."
    - "SelectedContext (wiring seed:512) → resolve_location(selection.value): the reconcile prevents a stale name reaching resolve_location after a rename/remove."
  prohibitions:
    - "The F17 reorder must NOT make either side-effect abort the reload — both stay best-effort; the reload is already committed before side-effects run."
    - "The F22 reconcile must NOT change the reload-outcome send string or the committed-reload semantics — it is an additive best-effort sibling."
    - "The reconcile must NOT introduce a lock on `SelectedContext` — it is single-writer on the gateway loop; the reconcile runs in the same context as its siblings."
---

<objective>
Fix the HARD-UI-02 reload-wiring slice (D-04): reorder `_on_applied` so `cache.invalidate()` fires BEFORE the slow `channel.send` reload-outcome post (F17 — a slow post currently delays invalidation and serves old coords), and add a best-effort `SelectedContext` reconcile-on-reload so a renamed/removed selected location can't leave a stale name that `resolve_location` later rejects (F22).

Purpose: A reload takes effect immediately for inbound lookups; the panel selection never dangles on a gone location.
Output: Reordered + reconciling `_on_applied` + regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-interactive-panel-robustness/33-CONTEXT.md
@.planning/phases/33-interactive-panel-robustness/33-RESEARCH.md
@.planning/phases/33-interactive-panel-robustness/33-PATTERNS.md
</context>

## Artifacts this phase produces (NEW symbols this plan creates)
- Reordered side-effect blocks in `_on_applied` (weatherbot/scheduler/wiring.py) — cache.invalidate block moved above the channel.send block (no new symbol; reordered logic).
- A new best-effort SelectedContext reconcile block in `_on_applied` — new local logic (planner-discretion: inline block or a small `_reconcile_selection(selection, holder)` helper).
- `tests/test_lifecycle_module.py::test_invalidate_before_send` and `::test_selection_reconcile_on_reload` — new test functions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED regressions — invalidate-before-send ordering + selection reconcile</name>
  <files>tests/test_lifecycle_module.py</files>
  <read_first>
    - weatherbot/scheduler/wiring.py (_on_applied 232-260 — current send-then-invalidate order; _prune_forecast_streaks sibling 252-255; SelectedContext seed 512)
    - tests/test_lifecycle_module.py (existing _on_applied / reload side-effect test patterns)
    - .planning/WHOLE-PROJECT-REVIEW.md (F17, F22 scenarios)
    - .planning/phases/33-interactive-panel-robustness/33-PATTERNS.md (wiring.py _on_applied + SelectedContext sections)
  </read_first>
  <behavior>
    - `test_invalidate_before_send`: record the call order of `cache.invalidate` and `channel.send` (e.g. spy both onto a shared order list); assert `invalidate` is called BEFORE `send` in `_on_applied`.
    - `test_selection_reconcile_on_reload`: seed SelectedContext with a location name, hot-reload a config that removes/renames it, assert the selection is reconciled to the default (config.locations[0].name), and a subsequent `resolve_location(selection.value)` does NOT raise UnknownLocationError.
  </behavior>
  <action>
    In tests/test_lifecycle_module.py add `test_invalidate_before_send`: build the `_on_applied` closure with a spying cache + channel that append their names to a shared list when invalidate/send are called; invoke `_on_applied("summary")`; assert the list has invalidate before send. Add `test_selection_reconcile_on_reload`: seed a `SelectedContext` with a name present in the initial config, apply a reload whose new config drops/renames that location, invoke `_on_applied`, assert `selection.value == config.locations[0].name` and `resolve_location(new_config, selection.value)` succeeds. Both RED before Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_lifecycle_module.py -k "invalidate_before_send or selection_reconcile" -x</automated>
  </verify>
  <done>Both tests exist and fail against current wiring.py (send-before-invalidate order; no reconcile).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reorder invalidate-before-send (F17) + add reconcile sibling (F22)</name>
  <files>weatherbot/scheduler/wiring.py</files>
  <read_first>
    - weatherbot/scheduler/wiring.py (_on_applied 232-260 — the exact blocks; the "SAME order + EXACT strings" comment 233)
    - weatherbot/config/loader.py (resolve_location — how a name maps to a location; default = config.locations[0])
    - .planning/phases/33-interactive-panel-robustness/33-PATTERNS.md (Best-effort side-effect wrapping)
  </read_first>
  <action>
    In `_on_applied` (wiring.py), move the `cache.invalidate()` best-effort block ABOVE the `channel.send(f"✅ config reloaded: {summary}")` block so invalidation (which bumps the Plan-02 generation) runs before the slow Discord post (F17). Keep both blocks best-effort (unchanged try/except … BLE001 style) and keep the reload-outcome send string byte-identical (the "SAME order + EXACT strings" comment refers to the SEND STRING, not the invalidate ordering — the reorder is intended; update/annotate the comment so it no longer implies the send must precede invalidate). Add a new best-effort reconcile block (sibling to `_prune_forecast_streaks`, same try/except … BLE001 style): if the `SelectedContext` value names a location the reloaded `holder.current()` no longer has, reset it to `config.locations[0].name`. `SelectedContext.set`/`.value` are single-writer on the gateway loop (no lock). Carrier is planner discretion (inline block or a small `_reconcile_selection` helper). Wire the `selection` reference into the `_on_applied` closure if not already captured.
  </action>
  <verify>
    <automated>uv run pytest tests/test_lifecycle_module.py -x</automated>
  </verify>
  <done>invalidate fires before send; a renamed/removed selected location is reconciled to the default on reload; both side-effects stay best-effort; send string unchanged; full test_lifecycle_module.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config reload → cache/selection state | A reload commits, then side-effects (invalidate, send, reconcile) run; ordering and reconcile must serve the new config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-03-01 | Info-disclosure | _on_applied send-before-invalidate (F17) | medium | mitigate | Reorder so invalidate precedes the slow send — inbound lookups get new coords immediately, not old ones for the post's duration. |
| T-33-03-02 | Tampering | stale SelectedContext after rename/remove (F22) | low | mitigate | Reconcile the selection to the default on reload so `resolve_location` never rejects a name the user never sees selected. |
| T-33-03-03 | Availability | best-effort side-effect abort | low | accept | Both side-effects stay best-effort; a hiccup can't abort the committed reload (existing invariant preserved). |
| T-33-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs; no `[ASSUMED]`/`[SUS]` packages → no install checkpoint. |
</threat_model>

<verification>
- `uv run pytest tests/test_lifecycle_module.py -x` green.
- Manual: confirm the reload-outcome send string in wiring.py is unchanged (grep the exact string), only its ORDER relative to invalidate moved.
</verification>

<success_criteria>
A reload invalidates the cache before posting its outcome, and reconciles a gone selected location to the default — both best-effort.
</success_criteria>

<output>
Create `.planning/phases/33-interactive-panel-robustness/33-03-SUMMARY.md` when done
</output>
