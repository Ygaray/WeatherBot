---
phase: 33-interactive-panel-robustness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cache.py
  - weatherbot/interactive/cache.py
autonomous: true
requirements: [HARD-UI-02]
must_haves:
  truths:
    # Derived from D-03/D-04/F13 (authoritative edges — CONTEXT.md + WHOLE-PROJECT-REVIEW.md)
    - "An off-loop fetch that STARTED before an `invalidate()` does NOT write its (pre-reload) result into the cache — the generation guard refuses the stale re-populate."
    - "The generation counter is captured INSIDE the same lock as the cache `get` (never after releasing the lock) so it is consistent with the miss and cannot race `invalidate()`."
    - "`invalidate()` bumps the generation counter under the lock (in addition to clearing) so in-flight fetches self-reject."
    - "The cache lock is NEVER held across `lookup_weather` — the off-loop-fetch design (no serialized lookups) is preserved (D-03 rejects lock-around-fetch)."
    - "The cache is bounded (TTLCache maxsize) AND the plain `!weather` entry (suffix=None key = bare loc_id) is never the one evicted, even under heavy forecast/flag-suffixed use."
    # Flagged assumption — deterministic edge probe could not classify HARD-UI-02 audit text (specless fallback §A):
    - { statement: "HARD-UI-02 (cache slice) audit-style requirement text is unclassified by the deterministic edge probe; its real edges are covered by the D-03/D-04/F13 truths above — flagged assumption, not a silent drop.", verification: backstop }
  artifacts:
    - "weatherbot/interactive/cache.py — generation counter (init, capture-in-lock, store-guard, invalidate-bump) + bounded/pinned eviction protecting the plain-weather key"
    - "tests/test_cache.py — stale-repopulate-rejected + plain-entry-protected regressions"
  key_links:
    - "invalidate() bump ← the F17 reorder (Plan 03) makes cache.invalidate fire before the slow channel.send, so the generation bump lands before a slow post — the two fixes compose."
    - "generation capture inside `with self._lock:` as the `get` → store re-check under the lock compares the same generation."
  prohibitions:
    - "The F13 fix must NOT reintroduce a lock held across the off-loop `lookup_weather` fetch (that re-serializes lookups and risks blocking the gateway path)."
    - "The generation must NOT be read after the `get` lock is released — that races `invalidate()` and can capture a post-invalidate generation, defeating the guard."
    - "The cache bounding must NEVER make the plain `!weather` (suffix=None) entry the evicted one — forecast/flag-suffixed entries are the evictable set."
---

<objective>
Fix the HARD-UI-02 cache slice: F13 stale re-populate (an in-flight off-loop fetch that started before a hot-reload `invalidate()` re-inserts a pre-reload result that survives to TTL) via a generation/epoch guard (D-03), and bound the cache so heavy forecast/flag use can't evict the plain-weather entry it should protect (D-04). No lock across the fetch — the off-loop design is preserved.

Purpose: Serve current config after a reload, never a pre-reload snapshot; keep the base weather entry warm.
Output: Generation-guarded, bounded ForecastCache + deterministic regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-interactive-panel-robustness/33-CONTEXT.md
@.planning/phases/33-interactive-panel-robustness/33-RESEARCH.md
@.planning/phases/33-interactive-panel-robustness/33-PATTERNS.md
</context>

## Artifacts this phase produces (NEW symbols this plan creates)
- `self._generation` (int, init 0) on `ForecastCache.__init__` — new instance field.
- A generation-capture-in-lock + store-guard branch in `ForecastCache.lookup` — new logic on the existing method.
- A generation bump inside `ForecastCache.invalidate` — new logic on the existing method.
- A bounded/pinned eviction policy for the plain-weather key (planner-discretion carrier: protected key / per-namespace cap / size-cap LRU with pin) — new local policy.
- `tests/test_cache.py::test_stale_repopulate_rejected` and `::test_plain_entry_protected` — new test functions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED regressions — stale-repopulate rejection + plain-entry protection</name>
  <files>tests/test_cache.py</files>
  <read_first>
    - weatherbot/interactive/cache.py (docstring 3-25 — off-loop-no-lock contract; __init__ 57-80; lookup 82-120; invalidate 122-126)
    - tests/test_cache.py (existing injectable `timer` pattern + stubbed lookup_weather, lines ~73-190)
    - tests/conftest.py (load_fixture)
    - .planning/WHOLE-PROJECT-REVIEW.md (F13 scenario)
    - .planning/phases/33-interactive-panel-robustness/33-RESEARCH.md (Pattern 1; Pitfall 2; Nyquist "F13 generation race")
  </read_first>
  <behavior>
    - `test_stale_repopulate_rejected`: a controllable fetch hook lets the test call `invalidate()` between the miss (get) and the store; after the fetch returns, assert the store was REFUSED (cache does not contain the stale pre-invalidate result).
    - `test_plain_entry_protected`: fill the cache past maxsize with forecast/flag-suffixed entries; assert the plain `!weather` (suffix=None) entry for the pinned location is still present (never evicted).
  </behavior>
  <action>
    In tests/test_cache.py add `test_stale_repopulate_rejected`: stub `lookup_weather` with a hook (e.g. a callable the test can fire) so the test can call `cache.invalidate()` between the get and the store, then assert the post-invalidate store is rejected (the stale result is NOT served on a subsequent lookup within TTL). Add `test_plain_entry_protected`: insert enough distinct forecast/flag-suffixed keys to exceed maxsize=16, then assert the suffix=None plain-weather key for the protected location remains present. Reuse the existing injectable `timer` (cache.py:63) for deterministic TTL; no executor/loop needed (lookup is plain blocking code called synchronously in tests). These are RED before Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -k "stale_repopulate or plain_entry" -x</automated>
  </verify>
  <done>Both tests exist and fail against current cache.py (no generation guard; unbounded/mis-evicting behavior).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generation guard (D-03) + bounded/pinned eviction (D-04)</name>
  <files>weatherbot/interactive/cache.py</files>
  <read_first>
    - weatherbot/interactive/cache.py (__init__ 57-80 — where self._lock lives; lookup 104-120; invalidate 122-126)
    - .planning/phases/33-interactive-panel-robustness/33-PATTERNS.md (cache.py section — the exact delta)
    - .planning/phases/33-interactive-panel-robustness/33-RESEARCH.md (Pattern 1 code; Anti-Patterns "Holding the cache lock across lookup_weather")
  </read_first>
  <action>
    In cache.py __init__, initialize `self._generation = 0` alongside `self._lock`. In `lookup`, capture `gen_at_start = self._generation` INSIDE the same `with self._lock:` block as the `get` (Pitfall 2 — never after releasing the lock). Run `lookup_weather` WITHOUT the lock (unchanged). In the store block, re-acquire the lock and write `self._cache[key] = result` only when `self._generation == gen_at_start`; otherwise drop the stale result. In `invalidate`, after `self._cache.clear()`, add `self._generation += 1` under the lock so in-flight fetches self-reject. For bounding: the cache is already `TTLCache(maxsize=16)` — add a policy so the plain `!weather` (suffix=None) entry is never the evicted one. Carrier is planner discretion (a protected/pinned key, a per-namespace cap, or a size-cap LRU with a pin) — the invariant is that the suffix=None base-weather entry survives heavy forecast/flag churn. Do NOT hold the lock across the fetch.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -x</automated>
  </verify>
  <done>Stale re-populate is refused when invalidate fires mid-flight; the plain-weather entry survives past-maxsize churn; no lock is held across lookup_weather; full test_cache.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config reload → cache | A hot-reload invalidates the cache; an in-flight fetch that predates the reload must not re-seed stale (old lat/lon/units/template) data. |
| Cache eviction policy → served result | Eviction decides which entry survives; the plain-weather entry must be protected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-02-01 | Info-disclosure | ForecastCache stale re-populate (F13) | medium | mitigate | Generation guard refuses a pre-reload write so a stale (wrong-location/units) result is never served for the TTL after a reload. |
| T-33-02-02 | Tampering | cache eviction (bounding) | low | mitigate | Bound the cache with the plain-weather key protected so heavy forecast/flag use can't poison-by-eviction the base entry. |
| T-33-02-03 | Denial of Service | unbounded cache growth | low | mitigate | TTLCache maxsize (already present) bounds memory; the pin policy keeps the base entry warm. |
| T-33-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (cachetools already pinned); no `[ASSUMED]`/`[SUS]` packages → no install checkpoint. |
</threat_model>

<verification>
- `uv run pytest tests/test_cache.py -x` green.
- Manual review: `grep -n "with self._lock" weatherbot/interactive/cache.py` confirms the lock is released before `lookup_weather` (no lock across the fetch).
</verification>

<success_criteria>
A reload mid-fetch never leaves a stale cached result; the plain `!weather` entry survives heavy forecast/flag use; the off-loop-no-lock design is intact.
</success_criteria>

<output>
Create `.planning/phases/33-interactive-panel-robustness/33-02-SUMMARY.md` when done
</output>
