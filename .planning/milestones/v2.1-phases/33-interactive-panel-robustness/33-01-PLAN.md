---
phase: 33-interactive-panel-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_bot.py
  - tests/test_dispatch.py
  - weatherbot/interactive/dispatch.py
  - weatherbot/interactive/bot.py
autonomous: true
requirements: [HARD-UI-01, HARD-UI-03]
must_haves:
  truths:
    # Derived from D-01/D-02/D-05/F02/F27 (authoritative edges — CONTEXT.md + WHOLE-PROJECT-REVIEW.md)
    - "Bare `!weather` (no arg) over the Discord surface returns a real weather embed for the default location (config.locations[0]), NOT the generic '_ERROR_REPLY' / 'something went wrong'."
    - "All six location-taking commands (weather, alerts, sun, wind, next-cloudy, uv) with no arg resolve the default location app-side and run the existing fetch path."
    - "The F02 fix lives entirely in weatherbot/ app code (dispatch.py + bot.py); zero bytes change under yahir_reusable_bot / .venv (hub stays weather-domain-free, v2.0 litmus + grimp gate)."
    - "The bare-command reply header renders `📍 {default_name} (default)` (e.g. `📍 Toronto (default)`); a named-location reply renders `📍 {name}` with no suffix (e.g. `📍 London`)."
    - "The inbound `!weather <loc>` path passes `location=` to `render_embed` at bot.py:504 so the 📍 header the panel always shows is no longer suppressed on the inbound surface (F27 parity)."
    - "A regression test captures the pre-fix bare-`!weather` crash (RED) and passes post-fix (GREEN) — the same repro is the test (D-02)."
    # Flagged assumptions — the 4 deterministic probe rows the edge engine could not classify (specless fallback §A; surfaced, never auto-backstopped, never dropped):
    - { statement: "HARD-UI-01 audit-style requirement text is unclassified by the deterministic edge probe; its real edges are covered by the D-01/D-02/D-05/F02/F27 truths above — flagged assumption, not a silent drop.", verification: backstop }
    - { statement: "HARD-UI-03 (default-marker slice) audit-style requirement text is unclassified by the deterministic edge probe; the marker-format edge is covered by the D-05 truth above — flagged assumption.", verification: backstop }
  artifacts:
    - "weatherbot/interactive/dispatch.py — app-side default-resolution shim for bare location-taking specs"
    - "weatherbot/interactive/bot.py — inbound render passes location= with '(default)' suffix on bare"
    - "tests/test_bot.py — bare-command crash-then-default regression (verify-first)"
    - "tests/test_dispatch.py — takes_location + arg=None forces fetch with default name"
  key_links:
    - "dispatch_spec (app shim) → _module_dispatch_spec (hub guard `arg is not None or spec.needs_flags`): the fix pre-resolves arg so the hub fetch fires unchanged."
    - "bot.py on_message → dispatch_spec → render_embed(reply, location=…): the resolved default name must thread back to the render call site for the marker."
  prohibitions:
    # Descriptor-less behavior-preservation invariants (specless fallback §B; no check_* scalars → dispose flagged-unverified)
    - "The F02 fix must NOT be a hub-source edit — a `takes_location` branch added to the hub dispatcher guard is out of jurisdiction (pinned wheel; human-gated repin)."
    - "The F02/F27 fix must NOT add another blanket `except` to the on_message envelope — it turns silent-swallow into correct behavior, not more catches."
    - "The default resolution must NOT pass user-typed text to the flag parser — the default is resolved via `resolve_location(config, None)` (no user string), preserving the V5 input-validation boundary."
    - "The non-bare (named-arg) path must stay byte-identical for CLI + panel + inbound — the shim only acts when `arg is None` for a location-taking, non-flags spec."
---

<objective>
Fix HARD-UI-01 (F02): a bare location-taking command (`!weather`/`!sun`/`!wind`/`!alerts`/`!uv`/`!next-cloudy` with no arg) currently skips the fetch (hub guard `arg is not None or spec.needs_flags` is False), leaves `result=None`, and crashes on `result.forecast` → AttributeError → generic error. Resolve the default location app-side so the existing fetch path runs, matching the CLI's `resolve_location(None)` behavior — with zero hub change (D-01). Also land the D-05 default-marker + F27 inbound 📍 restore that ride the same resolved-name threading.

Purpose: The primary crash-on-valid-input defect on the Discord surface. Verify-crash-first is mandatory (D-02).
Output: App-side dispatch shim + inbound render marker + verify-first regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-interactive-panel-robustness/33-CONTEXT.md
@.planning/phases/33-interactive-panel-robustness/33-RESEARCH.md
@.planning/phases/33-interactive-panel-robustness/33-PATTERNS.md
</context>

## Artifacts this phase produces (NEW symbols this plan creates)
- `tests/test_bot.py::test_bare_weather_crashes_pre_fix` (RED harness) and `::test_bare_weather_default` (GREEN) — new test functions.
- `tests/test_dispatch.py::test_takes_location_default_resolves` — new test covering all six bare location commands.
- A default-resolution branch inside `dispatch_spec` (weatherbot/interactive/dispatch.py) — new behavior on the existing function (no new public symbol; existing signature preserved).
- A `location=`-bearing `render_embed` call plus a bare/named marker-label computation at bot.py:504 — new local logic, no new public symbol.
- Threading of the resolved default name from the dispatch path back to the inbound render site (planner-discretion carrier: e.g. surface the resolved name on the reply or recompute `resolve_location(config, arg).name` at the call site) — new local wiring.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify-first — reproduce the bare-!weather crash (RED), then all-six dispatch scaffold</name>
  <files>tests/test_bot.py, tests/test_dispatch.py</files>
  <read_first>
    - weatherbot/interactive/bot.py (on_message envelope ~439-511; render_embed ~192-221; _ERROR_REPLY:71)
    - weatherbot/interactive/dispatch.py (dispatch_spec shim 93-129 — delegation to preserve)
    - weatherbot/interactive/registry.py (CommandSpec.takes_location:85; _SPECS:97-131 — six location commands are takes_location=True)
    - tests/conftest.py (fake_discord_message factory ~166; load_fixture ~27)
    - tests/test_bot.py (existing on_message test patterns)
    - tests/test_dispatch.py (existing dispatch test patterns)
    - .planning/WHOLE-PROJECT-REVIEW.md (F02 scenario/evidence)
  </read_first>
  <behavior>
    - RED: `test_bare_weather_crashes_pre_fix` drives `on_message` (via `fake_discord_message`, content="!weather", operator author) and asserts the sent payload is the generic error reply — proving the crash faithfully PRE-fix.
    - GREEN target: after Task 2, the same scenario sends a real weather embed for config.locations[0] (default), NOT the error reply.
    - `test_takes_location_default_resolves` parametrizes all six location commands (weather, alerts, sun, wind, next-cloudy, uv) with arg=None and asserts the fetch path runs with the resolved default name (assert on the stubbed `lookup_weather`/`cache.lookup` receiving a non-None default name, or the resulting reply being a real reply, not an error).
  </behavior>
  <action>
    Author the RED regression in tests/test_bot.py: a `test_bare_weather_crashes_pre_fix` that builds the on_message handler (build_on_message with holder/operator_id/cache), drives a bare "!weather" `fake_discord_message`, and asserts `channel.send` was awaited with the generic `_ERROR_REPLY` (the crash-swallow evidence, D-02). Add its GREEN sibling `test_bare_weather_default` asserting a real default-location embed post-fix (mark it xfail(strict=False) or skip until Task 2 lands, per the Wave-0 RED contract, then flip). In tests/test_dispatch.py add `test_takes_location_default_resolves` parametrized over the six `takes_location=True` non-flags specs (weather/alerts/sun/wind/next-cloudy/uv), stubbing `lookup_weather`, asserting the default name (config.locations[0].name) reaches the fetch when arg=None. Reuse conftest `fake_discord_message`/`load_fixture` and existing test fixtures — do NOT invent a gateway. Capture the pre-fix generic-error evidence in the test docstring per D-02.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bot.py -k bare_weather_crashes -x</automated>
  </verify>
  <done>test_bare_weather_crashes_pre_fix passes (asserts the generic error reply is sent for bare !weather, proving the crash); the GREEN sibling + dispatch parametrization exist and are RED/xfail pending Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App-side default resolution (D-01) + inbound 📍 marker (D-05/F27)</name>
  <files>weatherbot/interactive/dispatch.py, weatherbot/interactive/bot.py</files>
  <read_first>
    - weatherbot/interactive/dispatch.py (93-129 — the shim to extend BEFORE the delegation)
    - weatherbot/interactive/cache.py:38 (`from weatherbot.config import resolve_location` — reuse this import)
    - weatherbot/config/loader.py (resolve_location(config, None) → config.locations[0]; casefold matching, empty-config ValueError)
    - weatherbot/interactive/bot.py (on_message 468-504 — arg=parsed.arg:473, render_embed(reply):504; render_embed location handling 219-220)
    - weatherbot/scheduler/wiring.py:489 (`arg = selection.value if spec.takes_location else None` — the analog reader)
    - weatherbot/interactive/registry.py (CommandSpec.takes_location, needs_flags)
    - .planning/phases/33-interactive-panel-robustness/33-PATTERNS.md (dispatch.py + bot.py:504 sections)
  </read_first>
  <action>
    In dispatch_spec (weatherbot/interactive/dispatch.py), BEFORE the `_module_dispatch_spec` delegation, add: when `arg is None and getattr(spec, "takes_location", False) and flags is None`, set `arg = resolve_location(config, None).name` (the canonical CLI default from config/loader.py — do NOT re-derive "pick first"; import `resolve_location` the same way cache.py:38 does). Keep the delegation call otherwise byte-identical (parse_flags/cache_suffix passthrough unchanged). This makes the hub guard `arg is not None or spec.needs_flags` fire so the existing fetch/render path runs.
    Then in bot.py on_message: capture `was_bare = (arg is None)` at bot.py:473 BEFORE dispatch resolves the default; compute the resolved location label for the render (the resolved default name when bare, else the named arg's resolved name) and pass it as `location=` to `render_embed` at bot.py:504, appending the literal `" (default)"` suffix when `was_bare` is true (`📍 Toronto (default)`), plain otherwise (`📍 London`). Carrier for surfacing the resolved name back to the render site is planner discretion (recompute `resolve_location(config, arg).name` at the call site, or thread it via the reply) — keep it app-side and coordinate with the dispatch change. Do NOT add a new blanket except; do NOT let user text reach the flag parser.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bot.py -k "bare_weather" tests/test_dispatch.py -k takes_location_default -x</automated>
  </verify>
  <done>Bare !weather returns a default-location embed with header `📍 {default} (default)`; all six bare location commands resolve the default; named-arg replies stay unmarked; inbound path shows the 📍 header (F27); no diff under .venv/yahir_reusable_bot; Task 1 GREEN siblings now pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord operator → on_message | Inbound command text (`!weather …`) crosses into the app; only the operator is gated (existing hub `interaction_check`). |
| App dispatch → OpenWeather fetch | The resolved location name crosses into `lookup_weather`; the value is a config-derived name, never raw user text here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01-01 | Denial of Service | on_message bare-command path | medium | mitigate | The crash-on-bare-input IS the DoS-on-valid-input; the fix resolves the default so the command completes instead of erroring — the core deliverable. |
| T-33-01-02 | Tampering | flag parser via default path | low | mitigate | Default resolved via `resolve_location(config, None)` (no user string); user-typed arg never reaches the parser through the bare branch (V5 boundary preserved). |
| T-33-01-03 | Info-disclosure | on_message except envelope | low | accept | The fix does not broaden the generic `_ERROR_REPLY`; the adjacent F12 appid-in-exception path is out of scope (Phase 30, already fixed) and not widened here. |
| T-33-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan (RESEARCH: zero deps); no `[ASSUMED]`/`[SUS]` packages → no install checkpoint needed. |
</threat_model>

<verification>
- `uv run pytest tests/test_bot.py tests/test_dispatch.py -x` green.
- Litmus/grimp: `git diff --stat` shows no change under `.venv/` or `../Reusable/YahirReusableBot/`; `uv run pytest tests/test_import_hygiene.py -x` green (hub stays weather-domain-free).
</verification>

<success_criteria>
Bare location commands resolve the default over Discord (matching CLI), the bare reply is marked `(default)` in the 📍 header, the inbound path shows the 📍 header, and the fix is app-side only.
</success_criteria>

<output>
Create `.planning/phases/33-interactive-panel-robustness/33-01-SUMMARY.md` when done
</output>
