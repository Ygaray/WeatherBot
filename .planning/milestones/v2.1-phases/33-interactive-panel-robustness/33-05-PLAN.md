---
phase: 33-interactive-panel-robustness
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_models.py
  - tests/fixtures
  - weatherbot/weather/models.py
autonomous: true
requirements: [HARD-UI-03]
must_haves:
  truths:
    # Derived from D-08/F11/F107 (authoritative edges — CONTEXT.md + WHOLE-PROJECT-REVIEW.md)
    - "The daily briefing's metric daily entry is selected by MATCHING the imperial entry's `dt` (same local day), NOT by an independent positional/local-date selection over the metric array — so a length/ordering skew (e.g. one fetch across local midnight) never pairs a °F high with the wrong day's °C (F107)."
    - "When the imperial and metric daily arrays are dt-SKEWED, the rendered high/low pairs the same-dt entries; on no dt match the metric side degrades to `{}` (graceful), never a mispair."
    - "When one unit's daily high/low is present and the other is missing, `high_display`/`low_display` render the AVAILABLE unit rather than falling back to the current temp (`temp_display`) — a valid imperial high is no longer discarded because its metric twin is missing (F11)."
    - "The dt-anchoring reuses Phase-32 `weather/dates.py` (`select_today_daily`/`local_date_for`) — no new date math is invented."
    # Flagged assumption — deterministic edge probe could not classify HARD-UI-03 audit text (specless fallback §A):
    - { statement: "HARD-UI-03 (dt-pairing slice) audit-style requirement text is unclassified by the deterministic edge probe; its real edges are covered by the D-08/F11/F107 truths above — flagged assumption, not a silent drop.", verification: backstop }
  artifacts:
    - "weatherbot/weather/models.py — dt-matched metric-daily selection (F107) + one-unit-present high/low_display (F11)"
    - "tests/fixtures/ — a deliberately dt-SKEWED briefing payload fixture (existing fixtures are pre-aligned)"
    - "tests/test_models.py — dt-paired-briefing + metric-missing-keeps-imperial regressions"
  key_links:
    - "select_today_daily (weather/dates.py, Phase 32) → imperial day_i selection → dt match → metric day_m: the pairing anchors on day_i's dt, not an independent metric selection."
    - "high_imp/high_met → high_display: when only one is present, render that one instead of temp_display."
  prohibitions:
    - "The pairing must NOT trust `daily[i]` alignment across two independent fetches — match by `dt`; never positional."
    - "The F107 test fixture must NOT be pre-aligned — a pre-aligned fixture is the exact false-green that hid this bug; the new fixture must be deliberately dt-skewed."
    - "The dt-anchoring must NOT re-derive local-date math — reuse `weather/dates.py` (Phase 32) as the single source of truth for 'which local day'."
---

<objective>
Fix the HARD-UI-03 dt-pairing slice (D-08): F107 — the daily briefing selects imperial and metric daily entries INDEPENDENTLY (models.py:300-301) with no cross-dt check, so a skewed payload mispairs °F to the wrong day's °C; and F11 — `high/low_display` (models.py:415-426) discards a valid imperial high when the metric twin is missing, falling back to the current temp. Lift the existing forecast-path dt-pairing guard (forecast.py:126-129) into models.py and render the available unit when one is missing.

Purpose: Correct dt-anchored dual-unit temps in the daily briefing; stop discarding valid highs.
Output: dt-matched metric selection + one-unit-present display + a deliberately skewed fixture + regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-interactive-panel-robustness/33-CONTEXT.md
@.planning/phases/33-interactive-panel-robustness/33-RESEARCH.md
@.planning/phases/33-interactive-panel-robustness/33-PATTERNS.md
</context>

## Artifacts this phase produces (NEW symbols this plan creates)
- A dt-match selection for the metric daily entry in `Forecast.from_onecall`/`from_payloads` (weatherbot/weather/models.py) — new logic replacing the independent metric `select_today_daily`.
- Revised `high_display`/`low_display` one-unit-present branches (weatherbot/weather/models.py) — new logic on the existing properties.
- A new dt-skewed briefing fixture under `tests/fixtures/` — new fixture file(s).
- `tests/test_models.py::test_dt_paired_briefing` and `::test_metric_missing_keeps_imperial` — new test functions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED regressions — dt-paired briefing (skewed fixture) + metric-missing keeps imperial</name>
  <files>tests/test_models.py, tests/fixtures</files>
  <read_first>
    - weatherbot/weather/models.py (from_onecall/from_payloads 288-310 — independent day_i/day_m selection; high/low_display 415-426 — temp_display fallback)
    - weatherbot/interactive/commands/forecast.py (114-129 — the EXISTING dt-pairing guard to lift)
    - weatherbot/weather/dates.py (select_today_daily / local_date_for — Phase 32 anchoring)
    - tests/test_models.py (existing from_onecall / display test patterns)
    - tests/conftest.py (load_fixture ~27); tests/fixtures/ (existing pre-aligned briefing fixtures to base the skewed one on)
    - .planning/WHOLE-PROJECT-REVIEW.md (F11, F107 scenarios)
    - .planning/phases/33-interactive-panel-robustness/33-RESEARCH.md (Pattern 4; Nyquist "F107 dt-boundary temp mispairing", "F11 metric-missing")
  </read_first>
  <behavior>
    - `test_dt_paired_briefing`: feed a payload where metric `daily[0]` is a DIFFERENT local day than imperial `daily[0]` (length/ordering skew); assert the rendered °F high pairs with the SAME-dt °C, not `daily[0]`.
    - `test_metric_missing_keeps_imperial`: metric `daily[0].temp` partial/absent while imperial has a real max; assert `high_display` renders the imperial high (not `temp_display`).
  </behavior>
  <action>
    Create a deliberately dt-SKEWED briefing fixture under tests/fixtures/ (base it on an existing pre-aligned fixture; shift the metric daily array by one day or reorder it so metric daily[0].dt != imperial daily[0].dt for the target local date). In tests/test_models.py add `test_dt_paired_briefing` that loads it and asserts the rendered high/low pair by matching dt (the °F high corresponds to the same-dt °C, not the positional metric[0]). Add `test_metric_missing_keeps_imperial` that constructs a Forecast (or drives from_onecall) with high_imp present but high_met None and asserts `high_display` shows the imperial value, not `temp_display`. Both RED before Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -k "dt_paired_briefing or metric_missing" -x</automated>
  </verify>
  <done>Both tests exist and fail against current models.py (independent positional metric selection; temp_display fallback discards the imperial high).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dt-matched metric selection (F107) + one-unit-present high/low_display (F11)</name>
  <files>weatherbot/weather/models.py</files>
  <read_first>
    - weatherbot/weather/models.py (from_onecall/from_payloads 288-310; high/low_display 415-426)
    - weatherbot/interactive/commands/forecast.py (126-129 — the dt-pairing guard pattern to lift)
    - weatherbot/weather/dates.py (select_today_daily / local_date_for)
    - .planning/phases/33-interactive-panel-robustness/33-PATTERNS.md (models.py section — F107 site + F11 site)
  </read_first>
  <action>
    In models.py `from_onecall`/`from_payloads`: keep selecting `day_i = select_today_daily(onecall_imp.get("daily"), loc_tz, local_date) or {}` (imperial anchored by local date, unchanged). Replace the INDEPENDENT metric selection with a dt-MATCH lifted from forecast.py:126-129: read `dt_ts = (day_i or {}).get("dt")`; if `dt_ts is not None`, set `day_m = next((d for d in (onecall_met.get("daily") or []) if (d or {}).get("dt") == dt_ts), {})`; else fall back to the local-date selection (`select_today_daily(...) or {}`). This pairs the metric entry to the imperial day's dt, degrading to `{}` on no match (never a mispair). Reuse `weather/dates.py` anchoring — do NOT invent new date math.
    In `high_display`/`low_display` (models.py:415-426): when ONE unit is present and the other is missing, render the AVAILABLE unit (via the existing single-unit format path) rather than returning `temp_display`. Only fall back to `temp_display` when BOTH are missing. Preserve the existing `_temp_str` dual-unit output when both are present.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x</automated>
  </verify>
  <done>A dt-skewed payload pairs °F with the same-dt °C (not positional); a metric-missing daily renders the imperial high (not temp_display); dt-anchoring reuses weather/dates.py; full test_models.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenWeather payload → briefing model | Two independent (imperial/metric) One Call payloads cross into the model; a length/ordering skew must not mispair days. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-05-01 | Tampering | positional imperial/metric daily pairing (F107) | medium | mitigate | Match the metric daily entry by the imperial entry's `dt` so a skewed payload can't ship a wrong-day temp labeled as today's. |
| T-33-05-02 | Info-disclosure | high/low_display fallback discards valid high (F11) | low | mitigate | Render the available unit when one is present so a valid imperial high is shown, not the current temp. |
| T-33-05-03 | Info-disclosure | provider-controlled daily field content | low | accept | Values are numeric temps rendered through existing formatters; no new provider text reaches an un-clipped surface here. |
| T-33-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs; no `[ASSUMED]`/`[SUS]` packages → no install checkpoint. |
</threat_model>

<verification>
- `uv run pytest tests/test_models.py -x` green.
- `uv run pytest` full suite green (dt-pairing change must not regress the daily-briefing goldens; trust exit code + `.ambr` diff, not the "N snapshots failed" banner).
</verification>

<success_criteria>
The daily briefing dt-pairs imperial/metric temps (never positional across a skew) and preserves a valid single-unit high/low.
</success_criteria>

<output>
Create `.planning/phases/33-interactive-panel-robustness/33-05-SUMMARY.md` when done
</output>
