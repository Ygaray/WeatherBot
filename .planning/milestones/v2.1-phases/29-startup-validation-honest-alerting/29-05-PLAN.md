---
phase: 29-startup-validation-honest-alerting
plan: 05
type: execute
wave: 2
depends_on: [29-02, 29-03]
files_modified:
  - weatherbot/scheduler/wiring.py
  - weatherbot/scheduler/daemon.py
autonomous: true
requirements: [HARD-STARTUP-02, HARD-STARTUP-03]
must_haves:
  truths:
    - "A dedicated `fatal: threading.Event` is threaded through RuntimeParts (separate from `stop`)"
    - "`_on_fail` on a CONFIG_INVALID/CRITICAL result sets fatal + fires the best-effort alert + sets stop; AUTH_FAILED does NOT set fatal (re-probes)"
    - "`run_daemon` returns non-zero when the gate stops with fatal set, and 0 on a clean SIGTERM (fatal unset)"
    - "The one-time Discord online ping fires strictly AFTER `notifier.ready()` (moved out of `_on_online`)"
    - "`_announce_schedule` logs every briefing AND forecast slot, including disabled ones, with next_run_time"
    - "A config reload prunes `_forecast_failure_streaks` entries for removed/renamed forecast slots"
    - "The dead `gate_until_healthy` is removed; `emit_online`/`_do_reload` are left for Phase 35"
  artifacts:
    - weatherbot/scheduler/wiring.py
    - weatherbot/scheduler/daemon.py
  key_links:
    - "`fatal` Event is constructed in build_runtime next to `stop`, added to RuntimeParts, and read by run_daemon at the gate-return branch"
    - "F90 announce + F89 prune both use the single-source `_forecast_job_id`"
    - "The relocated online ping lives in run_daemon after ready_gate.run(stop) returns True (post-READY)"
---

<objective>
Land the composition-root corrections in daemon.py + wiring.py: (1) the fatal-marker plumbing (D-10 app-side) — a dedicated `fatal` Event set by `_on_fail` on a CONFIG_INVALID/CRITICAL result, read by `run_daemon` for a non-zero exit while a clean SIGTERM still returns 0; (2) F07 — move the online ping to strictly after `notifier.ready()`; (3) F90 — `_announce_schedule` logs forecast slots (incl. disabled); (4) F89 — prune `_forecast_failure_streaks` on reload; (5) remove the dead `gate_until_healthy` twin of the hub ReadyGate. All ride the hub's EXISTING extension points (`on_fail` hook + `stop` Event) — NO hub source is touched.

Purpose: The defense-in-depth fatal path exits honestly (both Discord + a non-zero exit for systemd), a disabled forecast slot is visible at boot, a slow webhook can't block systemd READY, and renamed forecast slots don't leak streak state.
Output: fatal plumbing + F07/F90/F89 fixes + dead-code removal across the two files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-startup-validation-honest-alerting/29-PATTERNS.md
@weatherbot/scheduler/wiring.py
@weatherbot/scheduler/daemon.py
</context>

<artifacts_this_phase_produces>
NEW / MODIFIED symbols this plan creates (exclude from drift verification):
- `weatherbot/scheduler/wiring.py`: `fatal: threading.Event` field on `RuntimeParts`; `fatal = daemon.threading.Event()` construction in `build_runtime`; a fatal branch in `_on_fail` (sets `fatal` + best-effort alert + `stop.set()`); removal of the online ping from `_on_online`; a `_prune_forecast_streaks(holder)` best-effort call in `_on_applied`.
- `weatherbot/scheduler/daemon.py`: `_prune_forecast_streaks(holder)` helper (F89); a forecast-slot loop in `_announce_schedule` (F90); the gate-return branch changed to `return 1 if parts.fatal.is_set() else 0`; the relocated online ping after the gate returns True (F07); removal of `gate_until_healthy` (:1108-1156).
- Depends on `daemon.CONFIG_INVALID` (29-03).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fatal-marker plumbing — RuntimeParts.fatal + _on_fail fatal branch + run_daemon exit code (HARD-STARTUP-02, D-10)</name>
  <files>weatherbot/scheduler/wiring.py, weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/wiring.py:84-105 (`RuntimeParts` fields incl. `stop: threading.Event`), :125-160 (`build_runtime`: `stop = daemon.threading.Event()` at ~:155; channel build from settings), :280-293 (`_on_fail`: stamps health then branches AUTH→critical / else→warning), :323-337 (the `return RuntimeParts(...)` construction).
    - weatherbot/scheduler/daemon.py:1394-1408 (`parts.*` unpacking: `stop = parts.stop`, etc.), :1465-1466 (the gate-stop `return 0` branch to change).
    - .planning/phases/29-startup-validation-honest-alerting/29-RESEARCH.md Pattern 3 + "Marker plumbing decision" (dedicated `fatal: threading.Event`, SEPARATE from `stop`, so the fatal-vs-clean-SIGTERM distinction survives); the anti-pattern "Reusing `stop` as the fatal marker".
    - The 29-02 `test_fatal_exit_code` / `test_clean_shutdown_returns_zero` / `test_auth_not_fatal` tests.
  </read_first>
  <behavior>
    - _on_fail on CheckResult(reason=CONFIG_INVALID, severity>=CRITICAL): sets fatal, fires one best-effort alert, sets stop.
    - _on_fail on AUTH_FAILED: does NOT set fatal (keeps existing critical log; re-probes). D-03 guard.
    - _on_fail on NETWORK_NOT_READY: does NOT set fatal (existing warning; re-probes).
    - run_daemon after ready_gate.run(stop) returns False: returns 1 iff fatal.is_set(), else 0.
  </behavior>
  <action>
    In wiring.py: add a `fatal: threading.Event` field to `RuntimeParts` next to `stop` (:94); construct `fatal = daemon.threading.Event()` in `build_runtime` next to `stop = daemon.threading.Event()`; include `fatal=fatal` in the returned `RuntimeParts(...)`. Extend `_on_fail` — KEEP the leading `daemon.stamp_health(db_path, reason=result.reason, detail=result.detail)`; add a FIRST branch BEFORE the auth branch: `if result.severity >= Severity.CRITICAL and result.reason == daemon.CONFIG_INVALID:` → `fatal.set()`, fire ONE best-effort fatal alert via the shared `channel.send(...)` try/except idiom (reuse `_fatal_config_exit`-style message; the channel closed over in build_runtime), then `stop.set()` to break the hub re-probe loop; the existing `elif result.reason == daemon.AUTH_FAILED:` critical log and the `else:` warning stay UNCHANGED. Reference `daemon.CONFIG_INVALID` / `daemon.threading.Event` / `Severity` through the daemon module object so the daemon-suite monkeypatch bites (per the daemon-namespace-resolution convention). In daemon.py: change the gate-stop branch (:1465-1466) from `return 0` to `return 1 if parts.fatal.is_set() else 0` — `parts.fatal` is read directly off `parts` (like `stop = parts.stop`). Do NOT reuse `stop` as the fatal signal (a clean SIGTERM also sets `stop`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "fatal_exit_code or clean_shutdown or auth_not_fatal" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_fatal_exit_code`, `test_clean_shutdown_returns_zero`, `test_auth_not_fatal` pass.
    - `RuntimeParts` has a `fatal` field distinct from `stop`; `grep -n 'fatal' weatherbot/scheduler/wiring.py` shows the field + construction + set.
    - The gate-return branch reads `parts.fatal.is_set()` (grep confirms) and returns non-zero only when set.
    - AUTH_FAILED does not set `fatal` (D-03); the auth/network log branches are unchanged.
  </acceptance_criteria>
  <done>A dedicated fatal Event is threaded through RuntimeParts; _on_fail sets it only for CONFIG_INVALID/CRITICAL; run_daemon exits non-zero on fatal and 0 on clean SIGTERM; AUTH stays non-fatal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: F07 — relocate the online ping to after notifier.ready() (HARD-STARTUP-03, D-12)</name>
  <files>weatherbot/scheduler/wiring.py, weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/wiring.py:300-313 (`_on_online`: `scheduler.start()`, `stamp_health("online")`, `stamp_tick`, the online `_log.info`, THEN the `channel.send("WeatherBot online — startup self-check passed.")` + the not-delivered warning — the ping to REMOVE from here).
    - weatherbot/scheduler/daemon.py:1465-1468 (post-gate: after `ready_gate.run(stop)` returns True, `_log.info("daemon started"...)` — READY=1 already emitted by the hub before this point).
    - ../Reusable/YahirReusableBot/yahir_reusable_bot/lifecycle/ready_gate.py:96-98 (the hub fires `on_online` BEFORE `notifier.ready()` — the ordering that makes the in-hook ping block READY; hub is UNCHANGED).
    - .planning/phases/29-startup-validation-honest-alerting/29-RESEARCH.md Pattern 5; the 29-02 `test_ping_after_ready`.
  </read_first>
  <behavior>
    - The online Discord ping fires strictly AFTER notifier.ready() (READY=1), so a slow/hung webhook cannot delay systemd readiness.
    - _on_online still starts the scheduler + stamps online/tick + logs BEFORE READY (the golden-sensitive scheduler.start()-before-READY invariant is preserved).
  </behavior>
  <action>
    REMOVE the online ping block from `_on_online` in wiring.py (the `channel.send("WeatherBot online — startup self-check passed.")` and its not-delivered warning) — KEEP `scheduler.start()`, `stamp_health("online")`, `stamp_tick`, and the online `_log.info`. RE-ADD the ping in daemon.py `run_daemon` AFTER the `if not ready_gate.run(stop): return ...` branch and around the `_log.info("daemon started", ...)` line (READY=1 is already emitted by the hub at this point), guarded on `channel is not None`, using the best-effort `channel.send(...)` try/except idiom (never re-raise; a hang here no longer gates READY). Preserve the not-delivered warning semantics (log if `send_result` is not ok). Keep the message string identical to avoid a golden-string diff unless a golden test dictates otherwise.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "ping_after_ready" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_ping_after_ready` passes (recorded order: ping strictly after `ready`).
    - The `channel.send("WeatherBot online` string no longer appears in `_on_online` (grep wiring.py) and now appears in daemon.py's post-gate section.
    - `_on_online` still contains `scheduler.start()` before the hub's `notifier.ready()` (invariant preserved).
    - The relocated ping is best-effort (try/except; never re-raises).
  </acceptance_criteria>
  <done>The online ping fires after READY=1; the scheduler.start()-before-READY invariant is intact; a hung webhook can no longer block systemd readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: F90 announce forecast slots + F89 streak prune + remove dead gate_until_healthy (HARD-STARTUP-03)</name>
  <files>weatherbot/scheduler/daemon.py, weatherbot/scheduler/wiring.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py:1029-1063 (`_announce_schedule`: briefing loop that `continue`s past disabled slots — F90), :556-571 (`_forecast_job_id(location, fc)` — single source), :689-713 (`_desired_job_ids(holder)` — returns `briefing_ids | forecast_ids`), :392 (`_forecast_failure_streaks`), :412-414 (write keyed by `_forecast_job_id`), :440 (pop on success), :1108-1156 (dead `gate_until_healthy` to remove — the twin of the hub ReadyGate; F16/State-of-the-Art).
    - weatherbot/config/models.py — `ForecastSchedule` fields (`kind`/`variant`/`time`/`days`/`enabled`) used in the announce line and `location.forecast`.
    - weatherbot/scheduler/wiring.py:205-228 (`_on_applied` committed-success hook — where the best-effort prune call lands, in the same try/except style as `channel.send`/`cache.invalidate`).
    - .planning/phases/29-startup-validation-honest-alerting/29-RESEARCH.md Patterns 4 & 6 + Pitfall 5 (do NOT remove `emit_online`/`_do_reload` — those are F16/Phase 35); the 29-02 `test_announce_forecast` and 29-02 `test_streak_prune`.
  </read_first>
  <behavior>
    - _announce_schedule logs a line per briefing slot AND per forecast slot; a disabled forecast slot appears with next_run_time=None (visible, not skipped).
    - A reload that removes/renames a forecast slot prunes its _forecast_failure_streaks entry; live entries are retained.
    - gate_until_healthy is gone; emit_online and _do_reload remain (Phase 35 scope).
  </behavior>
  <action>
    F90 (D-11): In `_announce_schedule`, keep the briefing loop but STOP `continue`-skipping disabled briefing slots (log them with `enabled=slot.enabled`/`next_run_time` too), and ADD a parallel loop over `location.forecast` keyed by the SHARED `daemon._forecast_job_id(location, fc)` — log each with `kind=f"forecast:{fc.kind}"`, `variant`, `time`, `days`, `enabled=fc.enabled`, and the computed `next_run_time` (a disabled slot has no registered job so `by_id.get(...)` is None → `next_run_time=None`, the visible "this slot is off" signal). Add `kind="briefing"` to the existing briefing log line to distinguish. Use the existing `_log.info("scheduled slot", ...)` shape; keep it outcome-only. F89 (D-13): Add `_prune_forecast_streaks(holder)` to daemon.py — compute `live_ids = _desired_job_ids(holder)` and pop `set(_forecast_failure_streaks) - live_ids` from the dict (set-difference is safe; the streak dict only ever holds forecast ids). Call it best-effort from `_on_applied` in wiring.py in the same try/except style as its siblings (`except Exception: ... _log.warning("forecast streak prune failed; reload unaffected")`). Dead code: REMOVE `gate_until_healthy` (:1108-1156) entirely (it is the dead hand-rolled twin of the hub `ReadyGate.run`; F16). Do NOT remove `emit_online` or `_do_reload` (those are formally F16→Phase 35 — leave them; note in the SUMMARY that they were deliberately left for Phase 35).
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "announce_forecast" tests/test_reload.py -k "streak_prune" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_announce_forecast` passes: a `kind="forecast:*"` line per forecast slot, disabled ones with `next_run_time=None`.
    - `test_streak_prune` passes: dead forecast key removed, live key retained.
    - `_prune_forecast_streaks` and both announce loops use `_forecast_job_id` (no recomputed id string).
    - `grep -n 'def gate_until_healthy' weatherbot/scheduler/daemon.py` returns nothing (removed); `grep -n 'def emit_online\|def _do_reload' weatherbot/scheduler/daemon.py` STILL match (left for Phase 35).
    - Full suite `uv run pytest -q` is green (no collateral breakage from the dead-code removal).
  </acceptance_criteria>
  <done>Announce shows every briefing + forecast slot incl. disabled (F90); reload prunes dead streak entries (F89); dead gate_until_healthy removed; emit_online/_do_reload deliberately left for Phase 35.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clean SIGTERM vs fatal → systemd exit code | The `fatal` marker vs `stop` distinction decides the exit code; conflating them is a lifecycle-tampering risk. |
| slow/hung webhook → systemd READY | An in-hook ping before READY can block startup past TimeoutStartSec (DoS on readiness). |
| in-process streak dict → reload | Unpruned entries leak across reloads (resource/state creep). |
| fatal alert `detail` → Discord/log | Must stay outcome-only (no secret/path). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-13 | Tampering (lifecycle) | `fatal` vs `stop` exit code | high | mitigate | Dedicated `fatal` Event kept separate from `stop`; Task 1 tests pin non-zero-on-fatal / zero-on-clean-SIGTERM / AUTH-non-fatal. |
| T-29-14 | Denial of Service (readiness) | pre-READY online ping | medium | mitigate | F07 relocates the ping strictly after `notifier.ready()`; best-effort send; `test_ping_after_ready` enforces the order. |
| T-29-15 | Information Disclosure | fatal alert in `_on_fail` | high | mitigate | The alert reuses the outcome-only `detail` (`type(exc).__name__`) and the best-effort send idiom; never echoes `str(exc)` or a secret (T-04-01, ASVS V7). |
| T-29-16 | Resource/state leak | `_forecast_failure_streaks` | low | mitigate | F89 prunes dead keys on every applied reload via the authoritative `_desired_job_ids` set. |
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py tests/test_reload.py -x -q` green (fatal-exit, clean-shutdown, auth-not-fatal, ping-after-ready, announce-forecast, streak-prune).
- `uv run pytest -q` full suite green (dead-code removal introduces no collateral breakage).
- No hub source under `../Reusable/YahirReusableBot/` is modified.
</verification>

<success_criteria>
- Fatal plumbing: dedicated marker, non-zero exit on fatal, 0 on clean SIGTERM, AUTH non-fatal (HARD-STARTUP-02, D-03/D-10).
- F07 ping after READY; F90 forecast slots (incl. disabled) announced; F89 streaks pruned on reload (HARD-STARTUP-03).
- Dead `gate_until_healthy` removed; `emit_online`/`_do_reload` left for Phase 35; no hub edit.
</success_criteria>

<output>
Create `.planning/phases/29-startup-validation-honest-alerting/29-05-SUMMARY.md` when done.
</output>
