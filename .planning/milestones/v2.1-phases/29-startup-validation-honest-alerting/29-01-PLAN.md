---
phase: 29-startup-validation-honest-alerting
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_cli.py
  - tests/test_ops_selfcheck.py
autonomous: true
requirements: [HARD-STARTUP-01, HARD-STARTUP-02]
must_haves:
  truths:
    - "A red (failing/xfail) test asserts `run` rejects a duplicate-id/name config at boot with a non-zero exit"
    - "A red test asserts `run` rejects a typo'd template token / missing template file at boot"
    - "A parity test feeds identical configs to `check-config` and the `run` boot-validate and asserts identical accept/reject"
    - "A subprocess test asserts `weatherbot run --config <bad.toml>` returns a non-zero PROCESS exit code"
    - "Red tests assert `run_self_check` classifies config/template/empty-locations errors as CONFIG_INVALID and `to_health_result` maps it to Severity.CRITICAL"
  artifacts:
    - tests/test_cli.py
    - tests/test_ops_selfcheck.py
  key_links:
    - "test_cli.py boot-validate fixtures reuse `_good_config_file` + the bad-template variant from `test_check_config_offline_fail`"
    - "test_ops_selfcheck.py imports CONFIG_INVALID from `weatherbot.ops` (added in plan 29-03)"
---

<objective>
Wave 0 test scaffolding for the boot-validation (HARD-STARTUP-01) and selfcheck-classification (HARD-STARTUP-02) surfaces. Create the failing/pending tests that the implementation plans (29-03, 29-04) will turn green. Nyquist requires these tests exist BEFORE the implementation that satisfies them.

Purpose: Lock the observable contract for the primary offline fatal gate and the defense-in-depth classifier before any production code changes, so a regression that reopens F05/F06 fails a test.
Output: New test functions in `tests/test_cli.py` and `tests/test_ops_selfcheck.py`, red or xfail until 29-03/29-04 land.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-startup-validation-honest-alerting/29-PATTERNS.md
@.planning/phases/29-startup-validation-honest-alerting/29-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan CREATES the following NEW test symbols (the source-grounding pass must treat these as newly-created, not drift):
- `tests/test_cli.py`: `test_run_boot_validate_rejects_duplicate_id`, `test_run_boot_template_rejects_missing_template`, `test_check_run_parity` (parametrized), `test_run_bad_config_exit_code` (subprocess)
- `tests/test_ops_selfcheck.py`: `test_config_invalid_on_bad_template`, `test_config_invalid_on_empty_locations`, `test_connect_error_still_network_not_ready`, `test_401_still_auth_failed`, `test_severity_map` (parametrized over reason→severity)
- These reference `CONFIG_INVALID` (added to `weatherbot.ops` by plan 29-03) and the `run` boot-validate gate (added by plan 29-04). Until those land, mark the source-dependent cases `@pytest.mark.xfail(strict=False, reason="impl lands in 29-03/29-04")` OR guard the import so the file collects.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: test_cli.py boot-validate + parity + subprocess exit-code scaffolding (HARD-STARTUP-01)</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py — the file to extend; copy `_good_config_file(tmp_path)` (:786-801), `test_check_config_offline_pass`/`test_check_config_offline_fail` (:804-828, shows `template="__does_not_exist__.txt"` bad variant), and the `main([...])`-returns-int idiom (:305-332).
    - weatherbot/cli.py:945-1006 — the `check-config` branch (catch set) and the `run` branch that plan 29-04 will gate.
    - weatherbot/config/loader.py:67 (`assert_unique_names`), :99 (`validate_config_and_templates`) — what a duplicate-id/name and bad-template config trip.
  </read_first>
  <action>
    Add four tests to tests/test_cli.py. (1) `test_run_boot_validate_rejects_duplicate_id`: write a config with two locations sharing the same id/name via a `_good_config_file`-style helper, invoke `main(["run", "--config", str(cfg)])`, assert the returned exit code is non-zero AND the scheduler never starts (monkeypatch `daemon.run_daemon` to a sentinel and assert it is NOT reached, OR assert on the returned int alone if `run` gates before `run_daemon`). (2) `test_run_boot_template_rejects_missing_template`: config pointing at `template="__does_not_exist__.txt"`, assert non-zero exit. (3) `test_check_run_parity`: parametrize over a valid config, a duplicate-id config, and a bad-template config; assert `main(["check-config", ...])` and the `run` boot-validate produce IDENTICAL accept/reject (both 0 or both non-zero) — this is the strongest F05-regression guard. To avoid `run` blocking on the daemon for the VALID config, monkeypatch `daemon.run_daemon` to return 0 so parity compares only the validation verdict. (4) `test_run_bad_config_exit_code`: launch `subprocess.run([sys.executable, "-m", "weatherbot", "run", "--config", str(bad)], capture_output=True, timeout=...)` and assert `returncode != 0` — the ONE true end-to-end process-exit-code proof (no existing analog; genuinely new scaffolding). Mark the three in-process cases `@pytest.mark.xfail(strict=False, reason="run boot-validate lands in 29-04")` since the `run` gate does not exist yet; the subprocess test may also xfail until 29-04.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "run_boot_validate or run_boot_template or check_run_parity or run_bad_config_exit_code" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cli.py -k "run_boot_validate or run_boot_template or check_run_parity or run_bad_config_exit_code" -q` collects and runs the four tests (xfail/xpass allowed pre-impl; MUST NOT error on collection).
    - Each test names its target behavior in its docstring and cites HARD-STARTUP-01.
    - The subprocess test uses `sys.executable -m weatherbot run` and asserts on `returncode`, not stdout parsing.
    - No fenced production code is duplicated into the test; fixtures reuse the existing `_good_config_file` shape.
  </acceptance_criteria>
  <done>Four boot-validate/parity/subprocess tests exist in test_cli.py, collect cleanly, and pin HARD-STARTUP-01's accept/reject + exit-code contract.</done>
</task>

<task type="auto">
  <name>Task 2: test_ops_selfcheck.py CONFIG_INVALID classification + severity-map scaffolding (HARD-STARTUP-02)</name>
  <files>tests/test_ops_selfcheck.py</files>
  <read_first>
    - tests/test_ops_selfcheck.py — the file to extend; copy `_config(template=...)` (:25-37), `_OkClient` (:40), `_RaisingClient` (:52-61), `_http_status_error` (:64-67), `test_auth_401_is_auth_failed` (:98-103), `test_transient_connect_error...` (:81-86), and the import block (:16-22).
    - weatherbot/ops/selfcheck.py:44-46 (reason constants), :79-124 (`run_self_check` pre-probe checks + catch set), :127-145 (`to_health_result` severity map) — the classifier under test.
    - weatherbot/ops/__init__.py:16-28 — the re-export surface CONFIG_INVALID joins (plan 29-03).
  </read_first>
  <action>
    Add a parametrized classification matrix and a severity-map test to tests/test_ops_selfcheck.py. Import `CONFIG_INVALID` from `weatherbot.ops` alongside the existing `AUTH_FAILED`/`NETWORK_NOT_READY`/`PASS` imports — guard so the file still COLLECTS if the symbol is absent pre-29-03 (e.g. `pytest.importorskip`-style try/except that xfails the dependent cases, OR a module-level `CONFIG_INVALID` fallback marked xfail). Cases: (i) `_config(template="__does_not_exist__.txt")` → `run_self_check` returns `reason == CONFIG_INVALID`; (ii) empty-locations config → `CONFIG_INVALID`; (iii) `_RaisingClient(httpx.ConnectError(...))` → still `NETWORK_NOT_READY` (D-03 guard); (iv) `_RaisingClient(_http_status_error(401))` → still `AUTH_FAILED` (D-03 guard). Add `test_severity_map` parametrized over `(CONFIG_INVALID→CRITICAL, AUTH_FAILED→CRITICAL, NETWORK_NOT_READY→WARNING)` asserting `to_health_result(CheckResult(ok=False, reason=r)).severity`. Assert `detail` on the CONFIG_INVALID cases equals `type(exc).__name__` (never `str(exc)`) so the secret/path-hygiene contract (T-04-01) is pinned. Mark impl-dependent cases xfail until 29-03 lands.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ops_selfcheck.py -k "config_invalid or severity or network_not_ready or auth_failed" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_ops_selfcheck.py -k "config_invalid or severity or network_not_ready or auth_failed" -q` collects and runs (xfail allowed pre-impl; MUST NOT ImportError on collection).
    - The transient (ConnectError→NETWORK_NOT_READY) and auth (401→AUTH_FAILED) cases are present as explicit D-03 regression guards.
    - A CONFIG_INVALID case asserts `detail == type(exc).__name__` (outcome-only, no `str(exc)`).
    - `test_severity_map` covers all three reason→severity mappings.
  </acceptance_criteria>
  <done>The selfcheck classification matrix + severity-map tests exist, collect cleanly, and pin HARD-STARTUP-02's CONFIG_INVALID/CRITICAL contract plus the D-03 auth/transient guards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator config file → validator | Local operator-controlled TOML/Jinja consumed by `validate_config_and_templates` and `run_self_check`; malformed input must fail with an actionable classified outcome, never a raw traceback. |
| fatal outcome → operator alert/log | The `detail` surfaced on a fatal/CONFIG_INVALID result must be outcome-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Information Disclosure | `run_self_check` CONFIG_INVALID `detail` | high | mitigate | Test Task 2 asserts `detail == type(exc).__name__`, never `str(exc)` — locks the T-04-01 no-path/no-secret contract at the classifier boundary. |
| T-29-02 | Tampering (lifecycle) | `run` boot exit code | medium | mitigate | Test Task 1 (parity + subprocess) proves a bad config yields a non-zero exit rather than a green boot that silently drops briefings. |
</threat_model>

<verification>
- `uv run pytest tests/test_cli.py tests/test_ops_selfcheck.py -q` collects and runs the new tests without collection errors.
- Every new test cites its HARD-STARTUP requirement and asserts an observable (exit code, classified reason, severity, or `detail` shape).
</verification>

<success_criteria>
- Boot-validate, parity, and subprocess-exit-code tests exist in test_cli.py (HARD-STARTUP-01).
- CONFIG_INVALID classification + severity-map + D-03 guard tests exist in test_ops_selfcheck.py (HARD-STARTUP-02).
- All new tests collect cleanly; impl-dependent cases are xfail (not errors) until 29-03/29-04.
</success_criteria>

<output>
Create `.planning/phases/29-startup-validation-honest-alerting/29-01-SUMMARY.md` when done.
</output>
