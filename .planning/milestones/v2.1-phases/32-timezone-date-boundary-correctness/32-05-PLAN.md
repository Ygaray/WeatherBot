---
phase: 32-timezone-date-boundary-correctness
plan: 05
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - weatherbot/scheduler/uvmonitor.py
autonomous: true
requirements: [HARD-TZ-02, HARD-TZ-04]

must_haves:
  truths:
    - "D-03: the UV all-clear branch no longer latches on a single instantaneous sub-threshold dip — it gates on the day's PREDICTED end-of-window from the SAME UvSummary compute_uv already returns (require below AND past_peak AND window_over, where past_peak = now_local >= summary.peak_time and window_over = now_local >= summary.window_end), so a momentary passing-cloud dip while UV is still at/above threshold and still peaking does NOT declare 'protect window over' (F15 CONFIRMED)."
    - "D-03: when window_end/peak_time are None (empty/missing hourly[]) the monitor degrades to NOT posting all-clear yet (defer to next-day reset) rather than latching a premature all-clear or adding a new persistence table — keeping the store data-model out of scope (F36/F37 deferred) and reusing the fail-safe degrade posture."
    - "D-04: the pre-warn↔crossing↔all-clear lifecycle has no never-fire gap — every reachable (in_daylight × current-vs-threshold × prior-claims) state has a reachable transition for the day, the WR-02 claim-crossing-before-moot-prewarn ordering never orphans a state or blocks a legitimate later crossing/all-clear, and a full-day tick sequence posts prewarn/crossing/all-clear exactly once each."
    - "D-08: uvmonitor.py's local-date derivation calls the ONE shared weatherbot.weather.dates.local_date_iso(now_utc, tz) primitive — the third _local_date_iso copy (uvmonitor:84) is deleted so the monitor's local_date can never diverge from the models render / store persist keying."
    - "EDGE boundary/adjacency (HARD-TZ-02): UV exactly == threshold vs one step either side does not spuriously latch all-clear — a dip to exactly threshold-epsilon while now < window_end and now < peak_time keeps the window open."
    - "EDGE empty (HARD-TZ-02): empty/None hourly[] (no window_end/peak_time) degrades to 'don't post all-clear yet' and never raises."
    - "EDGE idempotency (HARD-TZ-02): the durable once-per-day claim (claim_uv_alert INSERT OR IGNORE) keeps each of prewarn/crossing/all-clear posting at most once per day even under repeated ticks; the D-03 gate never re-opens an already-claimed all-clear."
  artifacts:
    - "weatherbot/scheduler/uvmonitor.py — _decide branch 3 (all-clear) replaced with a window-end/peak hysteresis gate (D-03); the lifecycle branches audited for no never-fire gap (D-04); local _local_date_iso deleted and local_date_iso imported from weatherbot.weather.dates (D-08)."
  key_links:
    - "The all-clear gate reuses UvSummary.peak_time/window_end/current already computed by compute_uv (weather/uv.py) — it does NOT fork a second interpolation."
    - "uvmonitor imports local_date_iso from weatherbot.weather.dates (created in plan 32-02); the WR-05 _daily0_matches_today guard stays (it is the D-05 precedent, unchanged here)."
    - "The WR-02 claim-crossing-before-moot-prewarn ordering (:267-272) is preserved so a crash between the two claims never leaves a suppressing prewarn without a crossing."
---

<!-- planner-discipline-allow: def _local_date_iso -->

<objective>
Give the intraday UV all-clear hysteresis (HARD-TZ-02, F15 CONFIRMED) and prove the lifecycle has no
never-fire gap (D-04), then migrate the monitor's third `_local_date_iso` copy onto the shared helper
(D-08). Branch 3 currently latches "protect window over" on a single instantaneous `current < threshold`
dip — one passing cloud at solar noon (UV 5.8 vs 6.0) claims all-clear durably (once/day) while UV is
still peaking, and the window can never re-open. Anchor all-clear to the day's predicted window-end from
the same `UvSummary`, and audit the branch state machine for reachability.

Purpose: a momentary sub-threshold dip while UV is still at/above threshold does not end the protect
window (F15), no lifecycle state is left unable to fire for the day (D-04), and the monitor's local_date
can never diverge from the render/store keying (D-08).
Output: `uvmonitor.py` with the hysteresis all-clear gate + audited lifecycle + shared-helper import;
the plan-32-01 `test_allclear_not_latched_on_momentary_dip` and `test_lifecycle_full_day_no_never_fire_gap`
turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-timezone-date-boundary-correctness/32-CONTEXT.md
@.planning/phases/32-timezone-date-boundary-correctness/32-RESEARCH.md
@.planning/phases/32-timezone-date-boundary-correctness/32-PATTERNS.md
@weatherbot/weather/uv.py
@weatherbot/weather/dates.py
</context>

## Artifacts this phase produces

No new public symbols. This plan modifies the body of `uvmonitor._decide` (all-clear branch + lifecycle
audit) and swaps the local-date derivation onto the shared helper. New names (e.g. `past_peak`,
`window_over`, `below` locals) are function-local. The local `_local_date_iso` in `uvmonitor.py` is
deleted; `weatherbot.weather.dates.local_date_iso` is imported.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: All-clear window-end hysteresis (D-03, F15) + D-08 helper swap</name>
  <files>weatherbot/scheduler/uvmonitor.py</files>
  <behavior>
    - A tick where current dips to 5.8 (< 6.0 threshold) but now_local < summary.window_end and now_local < summary.peak_time, with a prior crossing claim, does NOT post all-clear and does NOT claim an allclear row (F15)
    - A tick after the genuine end-of-window (now_local >= window_end AND now_local >= peak_time AND current < threshold, prior crossing, no prior allclear) DOES post all-clear exactly once
    - A tick with empty hourly[] (window_end/peak_time None) does NOT post all-clear (degrades to don't-post-yet), never raises
    - uvmonitor.py no longer defines its own def _local_date_iso; local_date derivation uses the shared local_date_iso(now_utc, tz)
  </behavior>
  <read_first>
    - weatherbot/scheduler/uvmonitor.py:84-90 (the local `_local_date_iso` to delete) and :148-157 (the `tz = ZoneInfo(location.timezone)` resolve + `local_date = _local_date_iso(now_utc, tz)` call at :157 to swap)
    - weatherbot/scheduler/uvmonitor.py:317-323 (branch 3, the F15 all-clear latch: `if summary.current < threshold and "crossing" in prior and "allclear" not in prior:` → claim + post)
    - weatherbot/weather/uv.py:34-53 (`UvSummary` fields: `current`, `peak_time`, `window_end`, `crossing_time` — all `None` when threshold never reached / empty hourly; the facts the gate reads)
    - weatherbot/weather/dates.py (`local_date_iso` — created in plan 32-02; the import target)
    - .planning/phases/32-timezone-date-boundary-correctness/32-RESEARCH.md §Pattern 3 + §Pitfall 3 (the window-end primary gate, empty-hourly degrade-to-don't-post, no new persistence table — A2/Open-Q1)
  </read_first>
  <action>
    In `weatherbot/scheduler/uvmonitor.py`: delete the local `def _local_date_iso(now_utc, tz)` (:84-90). Add `from weatherbot.weather.dates import local_date_iso` to the imports. Replace the call at :157 `local_date = _local_date_iso(now_utc, tz)` with `local_date = local_date_iso(now_utc, tz)`. Confirm no other reference to the deleted local copy remains (grep after edit).
    Replace branch 3 (:317-323) with the hysteresis gate. Compute `below = summary.current < threshold`, `past_peak = summary.peak_time is not None and now_local >= summary.peak_time`, `window_over = summary.window_end is not None and now_local >= summary.window_end`. Gate all-clear on `below and "crossing" in prior and "allclear" not in prior and window_over and past_peak`. Only when that holds, `claim_uv_alert(db_path, location.id, local_date, "allclear")` and post the existing `✅ UV back below {t} in {name} — protect window over.` message (do NOT invent a new user-facing string, D-05 discretion). When `summary.peak_time`/`summary.window_end` are `None` (empty/missing hourly), the `past_peak`/`window_over` guards are False → all-clear is NOT posted (degrade to don't-post-yet; the day's `crossing` claim simply does not get an all-clear until a future tick has a real window, or the next-day reset clears prior) — do NOT add a persistence table or a new store row kind (keeps F36/F37 out of scope). Add `# D-03`/`# F15` comment tags and a comment noting the empty-hourly degrade.
  </action>
  <verify>
    <automated>test $(grep -c "def _local_date_iso" weatherbot/scheduler/uvmonitor.py) -eq 0 && uv run pytest tests/test_uv_monitor.py::test_allclear_not_latched_on_momentary_dip tests/test_uv_monitor.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/scheduler/uvmonitor.py` no longer contains `def _local_date_iso(` (grep count 0) and imports `local_date_iso` from `weatherbot.weather.dates`
    - The local_date derivation at the tick site uses `local_date_iso(now_utc, tz)`
    - Branch 3 gates all-clear on `below AND window_over AND past_peak` (grep confirms `window_over`/`past_peak` are computed from `summary.window_end`/`summary.peak_time` and used in the all-clear condition)
    - `tests/test_uv_monitor.py::test_allclear_not_latched_on_momentary_dip` passes (GREEN)
    - The rest of `tests/test_uv_monitor.py` still passes (existing crossing/pre-warn/genuine-all-clear behavior unregressed)
    - No new store table / uv_alerts kind is introduced (grep confirms no new `claim_uv_alert(... "subthreshold"...)` or new table DDL)
  </acceptance_criteria>
  <done>All-clear gates on the predicted window-end + past-peak from the shared UvSummary (no latch on a momentary dip), degrades to don't-post on empty hourly, uses the shared local_date_iso helper, and defines no local _local_date_iso; the momentary-dip test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lifecycle no-never-fire-gap audit (D-04)</name>
  <files>weatherbot/scheduler/uvmonitor.py</files>
  <behavior>
    - A full-day tick sequence (pre-warn window → crossing → peak → genuine end-of-window) posts prewarn/crossing/all-clear exactly once each
    - No reachable (in_daylight × current-vs-threshold × prior-claims) state leaves all three branches unable to fire for the day
    - The WR-02 moot-pre-warn suppression (claim prewarn without posting, only after crossing claim) never blocks a legitimate later crossing/all-clear
    - The already-high-at-first-poll path still emits exactly one crossing message and suppresses the moot pre-warn
  </behavior>
  <read_first>
    - weatherbot/scheduler/uvmonitor.py:243-323 (the full `_decide`: WR-01 daylight-gating comment :243-251, branch 1 already-high/crossing :256-283 with the WR-02 claim ordering :267-272, branch 2 pre-warn :285-315, branch 3 all-clear :317-323 now with the D-03 gate from Task 1)
    - .planning/phases/32-timezone-date-boundary-correctness/32-CONTEXT.md §D-04 (enumerate the branch state machine; every state must have a reachable transition; the moot-pre-warn suppression must not orphan a state)
    - .planning/phases/32-timezone-date-boundary-correctness/32-PATTERNS.md §uvmonitor.py (branch reachability audit — already-high/crossing daylight-gated, pre-warn daylight-gated, all-clear not daylight-gated per WR-01)
  </read_first>
  <action>
    Enumerate the `_decide` branch state machine over (in_daylight ∈ {True, False}) × (current vs threshold: >=, <) × (prior claims ⊆ {prewarn, crossing, allclear}) and verify every reachable combination has a defined transition for the day. Confirm: branch 1 (already-high/crossing) fires when `in_daylight and current >= threshold and "crossing" not in prior`; branch 2 (pre-warn) fires when `in_daylight and current < threshold and "prewarn" not in prior and "crossing" not in prior` and (time_close or value_close); branch 3 (all-clear, D-03) fires when `current < threshold and "crossing" in prior and "allclear" not in prior and window_over and past_peak`. Identify any combination where NONE can fire that SHOULD be able to (e.g. a state where a legitimate later crossing or all-clear is blocked). If the audit finds a genuine gap or an orphaning by the WR-02 suppression, make the MINIMAL change to close it (e.g. ensure the moot-pre-warn suppression claim does not set a state that blocks a subsequent crossing — it must only suppress the pre-warn post, never the crossing/all-clear path). If the audit finds NO gap (the branches already jointly cover the day), record that finding explicitly in the SUMMARY with the enumerated table and add a `# D-04` comment at the top of `_decide` documenting the reachability invariant — do NOT add speculative code.
    The `test_lifecycle_full_day_no_never_fire_gap` test (plan 32-01) is the executable proof: it must pass — each of prewarn/crossing/all-clear posts exactly once across the day walk with no never-fire state. Preserve the WR-02 claim-crossing-before-moot-prewarn ordering (:267-272) unchanged unless the audit proves it orphans a state.
  </action>
  <verify>
    <automated>uv run pytest tests/test_uv_monitor.py::test_lifecycle_full_day_no_never_fire_gap tests/test_uv_monitor.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_uv_monitor.py::test_lifecycle_full_day_no_never_fire_gap` passes (GREEN) — prewarn/crossing/all-clear each post exactly once across the full-day walk
    - The full `tests/test_uv_monitor.py` suite passes (no lifecycle regression)
    - `weatherbot/scheduler/uvmonitor.py` `_decide` carries a `# D-04` reachability-invariant comment; the SUMMARY records the enumerated (daylight × current-vs-threshold × prior-claims) state table and the audit verdict
    - The WR-02 claim-crossing-before-moot-prewarn ordering is preserved (or, if a genuine orphan was found, the SUMMARY documents the minimal fix and the ordering invariant it restores)
  </acceptance_criteria>
  <done>The lifecycle state machine is enumerated and proven to have no never-fire gap (or the minimal gap-closing fix is applied), the full-day tick test is green, and the audit verdict + state table are recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UvSummary (from compute_uv over OpenWeather payload) → all-clear gate | Possibly None peak_time/window_end (empty/malformed hourly) crosses into the hysteresis decision; the gate must degrade, not latch/raise. |
| durable uv_alerts claim set → lifecycle branches | The once-per-day claim set gates each branch; a premature all-clear claim would burn the day's slot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-12 | Tampering | all-clear latch on momentary dip (F15) | high | mitigate | All-clear gates on `window_over AND past_peak` from the same UvSummary, not a bare instantaneous `current < threshold`; a passing-cloud dip while still peaking cannot claim the durable once-per-day all-clear. Pinned by `test_allclear_not_latched_on_momentary_dip`. |
| T-32-13 | Denial of Service (missed alert) | never-fire lifecycle gap (D-04) | medium | mitigate | Branch state machine enumerated + full-day tick test proves prewarn/crossing/all-clear each reachable; the WR-02 suppression cannot orphan a state. |
| T-32-14 | Tampering | empty/malformed hourly → premature latch | low | mitigate | None window_end/peak_time degrades to don't-post-yet (never a premature latch, never a raise); no new persistence table (F36/F37 stay out of scope). |
| T-32-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs (stdlib + already-pinned pytest). No legitimacy checkpoint needed. |
</threat_model>

<verification>
- `uv run pytest tests/test_uv_monitor.py -q` — full monitor suite green including the two newly-GREEN tests and all existing crossing/pre-warn/all-clear tests.
- `grep` confirms: no `def _local_date_iso(` in uvmonitor.py; `local_date_iso` imported from `weatherbot.weather.dates`; branch 3 uses `window_over`/`past_peak`; no new store table / uv_alerts kind.
- `uv run pytest -q` — full suite green (all nine RED tests from plan 32-01 now GREEN once plans 32-02..05 have landed).
</verification>

<success_criteria>
- A momentary solar-noon UV dip while still peaking does not post/claim all-clear (F15 fixed).
- Empty hourly degrades to don't-post-all-clear (no premature latch, no new table).
- The full-day lifecycle posts prewarn/crossing/all-clear exactly once with no never-fire gap (D-04).
- The monitor uses the ONE shared local_date_iso helper (D-08); no local _local_date_iso remains.
- `test_allclear_not_latched_on_momentary_dip` and `test_lifecycle_full_day_no_never_fire_gap` are GREEN; the monitor suite is unregressed.
</success_criteria>

<output>
Create `.planning/phases/32-timezone-date-boundary-correctness/32-05-SUMMARY.md` when done.
</output>
