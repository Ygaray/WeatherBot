---
phase: 20-isolation-hardening-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduler.py
  - tests/test_dispatch.py
autonomous: true
requirements: [PANEL-11]
must_haves:
  truths:
    - "A panel callback hanging on an await never delays or stops a concurrently-scheduled briefing — proven against a live BackgroundScheduler"
    - "The briefing path does not borrow the asyncio default executor used by dispatch.py (D-08b audit)"
    - "No production change is made to the isolation path (test-only proof, D-08)"
  artifacts:
    - path: "tests/test_scheduler.py"
      provides: "Live-scheduler hanging-callback isolation proof for the interaction path"
      contains: "def test_hanging_callback_never_stops_live_briefing"
    - path: "tests/test_dispatch.py"
      provides: "D-08b executor-sharing audit assertion"
      contains: "def test_briefing_path_not_on_default_executor"
  key_links:
    - from: "tests/test_scheduler.py"
      to: "weatherbot/interactive/panel.py"
      via: "drives PanelView.on_command with a handler/dispatch that hangs on await asyncio.Event().wait()"
      pattern: "asyncio\\.Event\\(\\)\\.wait"
    - from: "tests/test_dispatch.py"
      to: "weatherbot/interactive/dispatch.py"
      via: "asserts loop.run_in_executor(None, …) is the asyncio default executor, panel-only — distinct from the APScheduler pool"
      pattern: "run_in_executor"
---

<objective>
Re-prove the milestone's load-bearing failure-isolation guarantee (PANEL-11) for the
new interaction-callback path: a panel callback that **hangs** (await-shaped) never
delays, drops, or stops a concurrently-scheduled briefing. This is a **test-only**
proof (D-08) — zero production change to the isolation path — plus a one-time audit
(D-08b) that the briefing path does not borrow the asyncio default executor the panel's
read-only fetch uses.

Purpose: The raising case already has a seam (per-callback non-propagating envelope +
`View.on_error`) and a test (`test_callback_raise_isolated`). This phase closes the
**hanging** case against a *live* `BackgroundScheduler`, mirroring the Phase-15
raising-tick proof — and confirms the only genuine cross-thread coupling (the asyncio
default executor) is panel-only, never reached by the scheduler job.

Output: Two new tests in existing test files. No source changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-isolation-hardening-polish/20-RESEARCH.md
@.planning/phases/20-isolation-hardening-polish/20-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live-scheduler hanging-callback isolation proof (PANEL-11, D-08/D-08a)</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py:1919-1989 — `test_raising_uvmonitor_tick_never_stops_scheduler` (the EXACT live-scheduler skeleton to mirror: real `BackgroundScheduler`, sentinel `IntervalTrigger(seconds=0.1)` job, `threading.Event` deadline poll, `shutdown(wait=False)` in finally)
    - tests/test_panel.py:42-163 — the gateway-free panel harness (`_panel`, `_FakeHolder`, `_SpyCache`, `_make_panel`, `_stub_handler`, `_run = asyncio.run`)
    - tests/test_panel.py:475-497 — `test_callback_raise_isolated` (the panel-isolation harness shape: `_stub_handler(monkeypatch, "sun", …)`, `fake_interaction(user_id=_OPERATOR_ID, custom_id="wb:cmd:sun")`, drive `view.on_command`)
    - tests/conftest.py:178-227 — `fake_interaction` factory (AsyncMock `response.edit_message` / `edit_original_response`)
    - weatherbot/interactive/panel.py:481-541 — `on_command` (the callback to wedge) + its per-callback non-propagating envelope it INHERITS unchanged
    - weatherbot/interactive/dispatch.py:140-188 — `loop.run_in_executor(None, …)` off-loop fetch
    - .planning/phases/20-isolation-hardening-polish/20-PATTERNS.md (§"Hanging-callback live-scheduler proof") — the composed pattern (two templates)
  </read_first>
  <behavior>
    - Test `test_hanging_callback_never_stops_live_briefing`: a real `BackgroundScheduler` sentinel job (briefing stand-in, sub-second interval) STILL fires on time while a panel `on_command` callback is provably wedged on a never-completing `await`.
    - Assert: sentinel `threading.Event` is set within a `time.monotonic() + 5.0` deadline poll; AND `scheduler.running is True` after the wedge.
    - The hang MUST yield via `await asyncio.Event().wait()` (D-08a), NOT a CPU spin — the docstring documents WHY (all blocking panel work is already off-loop via `run_in_executor`, so the realistic wedge is a never-completing await; a `while True: pass` would prove GIL-throttling, a different thing — Pitfall 3).
  </behavior>
  <action>
    Add `test_hanging_callback_never_stops_live_briefing` to tests/test_scheduler.py, mirroring `test_raising_uvmonitor_tick_never_stops_scheduler` (1919-1989) for the live-scheduler skeleton and `test_callback_raise_isolated` (test_panel.py:475-497) for the panel-callback harness.

    Reuse, do not reinvent, the gateway-free panel harness — import the helpers from tests/test_panel.py (`_FakeHolder`, `_SpyCache`, `_make_panel`, `_stub_handler`, `_OPERATOR_ID`) and `fake_interaction` from conftest, OR construct the panel via `panel.PanelView(holder=_FakeHolder(["home"]), operator_id=…, cache=_SpyCache())`.

    Pick the cleanest wedge seam (Claude's Discretion per CONTEXT) so the hang yields via `await`: either `_stub_handler(monkeypatch, "sun", _hang)` where `_hang` blocks but matters less than the await-yield, OR monkeypatch `dispatch_spec` (or the panel's call into it) so the awaited coroutine never completes — the load-bearing requirement (D-08a) is that the wedge is a loop-yielding `await asyncio.Event().wait()`, not a GIL-holding spin. Drive `view.on_command(interaction, "sun")` on a SEPARATE daemon thread via `asyncio.run(...)` so it never returns and the main test thread proceeds to the sentinel deadline poll.

    Start a real `BackgroundScheduler` with a sentinel `IntervalTrigger(seconds=0.1)` job that sets a `threading.Event`; `scheduler.start()`; in a `try`, poll `while time.monotonic() < deadline and not sentinel_fired.is_set(): time.sleep(0.05)` with `deadline = time.monotonic() + 5.0`; assert `sentinel_fired.is_set()` (the "briefing" fired despite the hang) and `assert scheduler.running is True`; `scheduler.shutdown(wait=False)` in `finally`. Daemon-thread the wedge so a never-returning callback does not block test teardown.

    The test docstring MUST state the D-08a rationale (await-shaped, not CPU-spin) and that this is the test-only proof (D-08 — no production change). Do NOT add any `asyncio.wait_for` / timeout / watchdog to production code (D-09 — explicitly out of scope).
  </action>
  <acceptance_criteria>
    - `tests/test_scheduler.py::test_hanging_callback_never_stops_live_briefing` exists and PASSES.
    - The hang uses `await asyncio.Event().wait()` (grep: `asyncio\.Event\(\)\.wait` present in the new test; no `while True` CPU-spin hang).
    - The test starts a real `BackgroundScheduler` (not a fake), asserts the sentinel job fired within the deadline AND `scheduler.running is True`.
    - No production file under `weatherbot/` is modified by this task (`git diff --name-only` shows only `tests/`).
    - The test docstring documents the D-08a await-vs-spin choice.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k hanging_callback -x</automated>
  </verify>
  <done>A live-scheduler sentinel "briefing" job fires on time and the scheduler stays running while a panel `on_command` callback is wedged on `await asyncio.Event().wait()` — proving the briefing is isolated from a hanging interaction callback, with zero production change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-08b executor-sharing audit assertion (PANEL-11)</name>
  <files>tests/test_dispatch.py</files>
  <read_first>
    - weatherbot/interactive/dispatch.py:140-188 — the ONLY caller of `loop.run_in_executor(None, …)` (the asyncio DEFAULT executor) — the panel's read-only fetch path
    - tests/test_dispatch.py (whole file — the existing dispatch test conventions / imports)
    - .planning/phases/20-isolation-hardening-polish/20-PATTERNS.md (§"D-08b executor-sharing audit") — the audit target + recommended assertion shape
    - .planning/phases/20-isolation-hardening-polish/20-RESEARCH.md (Pitfall 4) — why an assertion beats a code-path note
  </read_first>
  <behavior>
    - Test `test_briefing_path_not_on_default_executor`: confirms the briefing/scheduler path never borrows the asyncio default `ThreadPoolExecutor` that `dispatch.py` uses via `loop.run_in_executor(None, …)`.
    - Observable: the default executor used by the panel's read-only fetch is a distinct object from (and not shared with) the scheduler's pool — OR a structural assertion that no scheduler-job code path reaches `loop.run_in_executor(None, …)`.
  </behavior>
  <action>
    Add `test_briefing_path_not_on_default_executor` to tests/test_dispatch.py as a lightweight audit (Pitfall 4 recommendation — an assertion is cheap regression insurance over a documented note).

    Implement the cleanest verifiable form (Claude's Discretion per CONTEXT D-08b): EITHER (a) assert that the asyncio default executor (the one `dispatch_spec` reaches via `loop.run_in_executor(None, …)`) and an APScheduler `BackgroundScheduler`'s own executor pool are distinct objects (the briefing runs under the latter, never the former); OR (b) a source/structural assertion that the briefing-firing code path (the scheduler job) contains no `loop.run_in_executor(None, …)` call — confirming `dispatch.py:166-188` is reached only from the panel's read-only fetch, never from the scheduler. The test docstring MUST cite `dispatch.py:166-188` as the audit target and note the expected-clean result (APScheduler has its own pool).

    This is a confirming audit, expected to come back clean; do NOT introduce a dedicated bounded executor (Option C — out of scope unless the audit unexpectedly finds real sharing).
  </action>
  <acceptance_criteria>
    - `tests/test_dispatch.py::test_briefing_path_not_on_default_executor` exists and PASSES.
    - The assertion is concrete (object-distinctness OR a structural/source check), not a bare comment — grep: `run_in_executor` referenced in the test.
    - The test docstring cites the `dispatch.py` default-executor audit target.
    - No production file under `weatherbot/` is modified by this task.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_dispatch.py -k executor -x</automated>
  </verify>
  <done>An automated audit confirms the briefing/scheduler path does not share the asyncio default executor that the panel's read-only fetch uses (D-08b clean) — the only genuine cross-thread coupling is ruled out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → panel component click | The single configured operator clicks a button/dropdown; the click crosses the Discord gateway into `PanelView` callbacks. No NEW boundary is introduced this plan — this is the existing Phase-17 interaction boundary, re-proven. |
| panel callback (gateway loop) → scheduled briefing (APScheduler thread) | The load-bearing security-adjacent property: a hanging/raising panel callback on the gateway loop must NOT be able to delay, drop, or stop the briefing running on the scheduler's own OS thread. |

## STRIDE Threat Register

> Scoped honestly (per security contribution): no new untrusted input, no new network/auth
> surface, no new dependency. The only load-bearing property is failure isolation.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Denial of Service | A panel callback that hangs on a never-completing `await` | mitigate | Re-proven by `test_hanging_callback_never_stops_live_briefing`: the briefing runs on APScheduler's own thread independent of the gateway loop, so a wedged callback cannot stop it. Existing per-callback non-propagating envelope + `View.on_error` cover the raising case (no change). |
| T-20-02 | Denial of Service | Hidden cross-thread coupling via a shared asyncio default executor | mitigate | `test_briefing_path_not_on_default_executor` (D-08b) audits that the briefing path never borrows the `loop.run_in_executor(None, …)` default pool the panel's read-only fetch uses. |
| T-20-03 | Tampering | npm/pip/cargo installs | accept | No package installs this plan — milestone Out of Scope forbids new deps; nothing to vet. |
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py -k hanging_callback -x` passes.
- `uv run pytest tests/test_dispatch.py -k executor -x` passes.
- `uv run pytest -q` full suite green (no production change → no anti-drift breakage from this plan).
- `git diff --name-only` shows ONLY `tests/test_scheduler.py` and `tests/test_dispatch.py` — no `weatherbot/` source change (D-08: test-only).
</verification>

<success_criteria>
ROADMAP SC#1 (isolation) is satisfied: a hanging panel callback, fired concurrently with
a live `BackgroundScheduler` briefing, leaves the briefing firing on time and the scheduler
running — with no production change to the isolation path (D-08), and the D-08b executor
audit comes back clean.
</success_criteria>

<output>
Create `.planning/phases/20-isolation-hardening-polish/20-01-SUMMARY.md` when done.
</output>
