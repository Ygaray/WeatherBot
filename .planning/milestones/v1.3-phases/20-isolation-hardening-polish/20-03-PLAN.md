---
phase: 20-isolation-hardening-polish
plan: 03
type: execute
wave: 2
depends_on: ["20-02"]
files_modified:
  - weatherbot/interactive/panel.py
  - tests/test_panel.py
autonomous: true
requirements: [PANEL-12, PANEL-13]
must_haves:
  truths:
    - "Every command/forecast/toggle button carries its locked D-05 emoji= glyph (text label kept)"
    - "The emoji SURVIVES the _render_view clone — present on the disabled-ack and collapse renders, not just the freshly-built __init__ view"
    - "The location dropdown marks SelectOption(default=True) for the selected location, re-derived from _selected_location on __init__ AND on every _render_view clone"
    - "D-03: the startup default stays locations[0] (home/first) — _selected_location = locations[0] at panel.py:319 is UNCHANGED; PANEL-12 only makes it visible, so on first render both the 📍 line and the dropdown default reflect locations[0]"
    - "The dropdown default mark is NEVER read back from Select.values (Pitfall 3 / discord.py #7284)"
    - "The panel result renders pass location= into render_embed (selected location for location-bearing commands, None for argless), so the 📍 line shows on every panel result path"
    - "_assert_layout stays green — zero new component slots (emoji/indicator/stamp are all non-component)"
  artifacts:
    - path: "weatherbot/interactive/panel.py"
      provides: "_EMOJI dict + emoji= on every button + dropdown default re-mark + _render_view clone carrying emoji + default + location threaded into the two render_embed result calls"
      contains: "_EMOJI"
    - path: "tests/test_panel.py"
      provides: "Clone-survival tests for emoji + dropdown default, indicator-on-render-path tests, plus refreshed anti-drift snapshots"
      contains: "emoji"
  key_links:
    - from: "weatherbot/interactive/panel.py _render_view clone"
      to: "discord.ui.Button / discord.ui.Select clones"
      via: "emoji=child.emoji on Button clones; options rebuilt with default=(value == self._selected_location) on the Select clone"
      pattern: "emoji=child\\.emoji"
    - from: "weatherbot/interactive/panel.py on_command / on_forecast"
      to: "render_embed"
      via: "render_embed(reply, location=arg) and render_embed(reply, location=self._selected_location)"
      pattern: "render_embed\\(reply, location="
---

<objective>
Land the panel-component polish over the assembled Phase-17–19 `PanelView`: emoji on every
command/forecast/toggle button (PANEL-13a, D-04/D-05, text labels kept), the dropdown
`SelectOption(default=True)` highlight re-marked from `_selected_location` (PANEL-12, D-02),
and — THE load-bearing trap — fixing the `_render_view` clone so emoji AND the dropdown
default **survive** every disabled-ack and collapse render (Pitfall 1). Also thread the
selected location into the two panel result renders so the Plan-20-02 `📍` indicator line
shows on every panel result path.

Purpose: The clone path (`panel.py:681-700`) today rebuilds *plain* `Button`/`Select`
clones copying only `label/custom_id/style/row/disabled` — it drops `emoji` and never
re-applies `SelectOption(default=)`. Unless fixed, the emoji and the dropdown highlight
silently die on EVERY ack/collapse render (the most common paths). Tests must render via
the clone path, not just first construction.

Output: `panel.py` gains an `_EMOJI` dict, `emoji=` on all buttons, the dropdown default
re-mark, a clone path that carries both, and `location=` on the two `render_embed` result
calls; clone-survival + indicator + emoji + default-mark tests pass; `_assert_layout` stays
green. A self-UAT log records byte-level evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-isolation-hardening-polish/20-RESEARCH.md
@.planning/phases/20-isolation-hardening-polish/20-PATTERNS.md
@.planning/phases/20-isolation-hardening-polish/20-UI-SPEC.md
@.planning/phases/20-isolation-hardening-polish/20-02-SUMMARY.md
@weatherbot/interactive/bot.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emoji on all buttons + dropdown default re-mark + the _render_view clone fix (PANEL-13a/PANEL-12, D-02/D-04/D-05)</name>
  <files>weatherbot/interactive/panel.py</files>
  <read_first>
    - weatherbot/interactive/panel.py:103-113 — `_LABELS` dict (mirror it with `_EMOJI`)
    - weatherbot/interactive/panel.py:164-186 — `CmdButton.__init__` (add `emoji=_EMOJI[name]`)
    - weatherbot/interactive/panel.py:188-227 — `ForecastButton.__init__` (thread an `emoji=` kwarg through, mirroring its `label=`)
    - weatherbot/interactive/panel.py:230-251 — `ForecastToggleButton.__init__` (label="Forecast" → add `emoji="📅"`)
    - weatherbot/interactive/panel.py:254-275 — `LocationSelect.__init__` (add `default=(n == panel._selected_location)` to each `SelectOption`)
    - weatherbot/interactive/panel.py:290-364 — `PanelView.__init__` (`_selected_location = locations[0]` at :319 set BEFORE `add_item(LocationSelect(...))` at :327; the four `ForecastButton` constructions at :339-362 — add their D-05 emoji)
    - weatherbot/interactive/panel.py:654-701 — `_render_view` (THE clone trap: Button clone :681-690 drops `emoji`; Select clone :691-700 blind-copies `options=list(child.options)` so `default` is not re-derived)
    - .planning/phases/20-isolation-hardening-polish/20-PATTERNS.md (§"_render_view clone — THE TRAP", §"Emoji on buttons", §"Dropdown default=True re-mark") — exact edit recipes
    - .planning/phases/20-isolation-hardening-polish/20-UI-SPEC.md (Copywriting Contract emoji table + clone-survival contract)
  </read_first>
  <behavior>
    - Each `CmdButton` carries its D-05 emoji (`weather 🌡️`, `uv 🧴`, `next-cloudy ☁️`, `sun ☀️`, `wind 💨`, `status 🟢`, `alerts ⚠️`); the `ForecastToggleButton` carries `📅`; the four `ForecastButton`s carry `Weekday Detailed 📋`, `Weekday Compact 📝`, `Weekend Detailed 🏖️`, `Weekend Compact 🌴`. Text labels are KEPT.
    - The `LocationSelect` marks `SelectOption(default=True)` for the option whose value equals `_selected_location`; all others `default=False`.
    - In `_render_view`: every Button clone carries `emoji=child.emoji`; the Select clone rebuilds its options with `default=(opt.value == self._selected_location)` — so emoji and the dropdown highlight survive the clone (disabled-ack AND collapse renders).
    - `_assert_layout` stays green — no new component slot is added.
  </behavior>
  <action>
    Add an `_EMOJI: dict[str, str]` mirroring `_LABELS` (keyed by command name) with the locked D-05 glyphs for the seven command buttons. In `CmdButton.__init__` (panel.py:175-180), pass `emoji=_EMOJI[name]` alongside the existing `label=_LABELS[name]` — `emoji=` is a SEPARATE param, NEVER concatenated into the label string (D-04). (Parallel dict vs inline is Claude's Discretion per CONTEXT; the parallel dict is recommended.)

    Thread an `emoji: str` kwarg through `ForecastButton.__init__` (panel.py:204-219), mirroring its existing `label=` kwarg, and pass it to `super().__init__(..., emoji=emoji, ...)`. At the four construction sites in `PanelView.__init__` (panel.py:339-362) pass the D-05 forecast glyphs (`📋` / `📝` / `🏖️` / `🌴`). Add `emoji="📅"` to `ForecastToggleButton.__init__` (panel.py:242-247).

    In `LocationSelect.__init__` (panel.py:265-271), rebuild the options comprehension as `discord.SelectOption(label=n, value=n, default=(n == panel._selected_location)) for n in locations` (D-02). `_selected_location` is already set (panel.py:319) before `add_item(LocationSelect(...))` (panel.py:327), so it is available. NEVER read selection from `Select.values` (Pitfall 3 / discord.py #7284) — derive the mark from `_selected_location` only.

    Fix `_render_view` (panel.py:681-700), THE clone trap: on the Button-clone branch add `emoji=child.emoji` to the `discord.ui.Button(...)` constructor (discord.py round-trips `.emoji` as a str/`PartialEmoji`). On the Select-clone branch, replace `options=list(child.options)` with a rebuilt list `[discord.SelectOption(label=o.value, value=o.value, default=(o.value == self._selected_location)) for o in child.options]` so the highlight is re-derived (not blindly copied from an un-re-marked source). These clones are PLAIN `discord.ui.Button`/`discord.ui.Select` (not the subclasses), so the emoji/default must be threaded into the plain constructors directly, independent of `_EMOJI`.

    Confirm `_assert_layout` / `_assert_layout_children` (panel.py:366-416) is UNTOUCHED and still green — emoji/default are non-component, no new slot (the grid stays 5/5 / 13 children).
  </action>
  <acceptance_criteria>
    - `_EMOJI` exists in panel.py with all seven command glyphs matching D-05 exactly.
    - A freshly built `PanelView`: every `CmdButton`/`ForecastButton`/`ForecastToggleButton` child has `.emoji` equal to its D-05 glyph; the `LocationSelect` option whose value == `locations[0]` has `default is True`.
    - A view from `view._render_view(expanded=True)` (the clone path): every Button clone has the correct non-None `.emoji`; the Select clone's selected option has `default is True` — proving clone-survival (NOT just the `__init__` view).
    - No `emoji` is concatenated into any button `label` (labels stay exactly `_LABELS` / the forecast Title-Case strings).
    - `_assert_layout` is unchanged and `uv run pytest tests/test_panel.py -k layout -x` stays green.
    - `uv run pytest tests/test_panel.py -x` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_panel.py -k "emoji or default_mark or layout" -x</automated>
  </verify>
  <done>Every panel button carries its locked emoji and the dropdown highlights the selected location, BOTH surviving the `_render_view` clone (disabled-ack + collapse renders) — the Pitfall-1 trap is closed; `_assert_layout` stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread location= into the panel result renders + render-path indicator/clone-survival tests (PANEL-12)</name>
  <files>weatherbot/interactive/panel.py, tests/test_panel.py</files>
  <read_first>
    - weatherbot/interactive/panel.py:481-541 — `on_command` (the result render at :534-538 uses `render_embed(reply)`; `arg` is computed at :499 = `_selected_location` for location-taking commands, `None` for argless)
    - weatherbot/interactive/panel.py:543-612 — `on_forecast` (result render at :602-606 uses `render_embed(reply)`; always location-bearing)
    - weatherbot/interactive/bot.py:194-261 — the Plan-20-02 `render_embed(reply, *, location=None)` signature this task calls
    - tests/test_panel.py:42-163 — gateway-free harness (`_make_panel`, `_SpyCache`, `_stub_handler`, `_run`, `fake_interaction`)
    - tests/test_panel.py:181-206 — `select.options` assertions (add a `default=` assertion; value list unaffected)
    - tests/test_panel.py:455-467 — panel weather parity vs `build_inbound_embed` (field/title only → stays green; confirm)
    - tests/test_panel.py:780-823 — panel forecast parity vs `render_embed(canonical)` (both sides gain the line → stays green; confirm)
    - .planning/phases/20-isolation-hardening-polish/20-RESEARCH.md (§"Anti-Drift Snapshot Inventory", §"Phase Requirements → Test Map") — the render-path indicator tests + which snapshots to refresh
    - .planning/phases/20-isolation-hardening-polish/20-UI-SPEC.md (§"Interaction & Isolation Contract" — render on EVERY path)
  </read_first>
  <behavior>
    - `on_command` result render passes `location=arg` (the `_selected_location` for location-taking commands, `None` for argless) into `render_embed`, so the `📍` line shows on location-bearing panel results and is suppressed on argless (status/alerts).
    - `on_forecast` result render passes `location=self._selected_location` (forecast is always location-bearing).
    - A panel weather/forecast result captured from the `edit_original_response` call carries `📍 {selected}` in the embed description; an argless (status/alerts) result does not.
    - The dropdown `default=True` mark survives the `_render_view` clone (clone-survival test).
    - Existing anti-drift snapshots stay green (description-level addition); the `select.options` test gains a `default=` assertion.
  </behavior>
  <action>
    In panel.py `on_command` (result edit at :534-538), change `render_embed(reply)` to `render_embed(reply, location=arg)` — `arg` is already computed at :499 (the selected location for location-taking commands, `None` for argless, so the indicator auto-suppresses on status/alerts). In `on_forecast` (result edit at :602-606), change `render_embed(reply)` to `render_embed(reply, location=self._selected_location)` (forecast always location-bearing). Do NOT alter the `UnknownLocationError` branches (they pass `embed=None`) or the single-ack discipline — only the embed= result render gains the kwarg.

    Add tests to tests/test_panel.py using the gateway-free harness:
    - An indicator test driving `view.on_command(interaction, "weather")` and asserting the embed passed to `edit_original_response` has `📍 home` in its `.description`.
    - An argless-suppression test driving a `status`/`alerts` command and asserting no `📍` in the result embed `.description`.
    - A clone-survival test calling `view._render_view(expanded=True)` and asserting the Select clone's selected option has `default is True` and each Button clone has its correct non-None `.emoji` (reinforces Task 1 from the test side).
    - Augment the existing `select.options` test (test_panel.py:181-206) with a `default=` assertion for the selected option; keep the value-list assertion intact.

    Run the FULL `tests/test_panel.py`; for each pre-existing break confirm ONLY the intended description/`default` addition changed (the weather-parity test at :455-467 and forecast-parity at :780-823 compare fields/title and should stay green per the anti-drift inventory — confirm, do not blanket-update). Refresh any genuinely-affected snapshot deliberately.

    <!-- planner-discipline-allow: 📍 -->
    Scope any 📍 presence/absence check to the specific `.description` value under test — do not negative-grep the glyph as a file-wide forbidden literal (it legitimately appears in the indicator assertions).
  </action>
  <acceptance_criteria>
    - panel.py `on_command` result render calls `render_embed(reply, location=arg)`; `on_forecast` calls `render_embed(reply, location=self._selected_location)` (grep: `render_embed\(reply, location=`).
    - Driving `on_command(interaction, "weather")` yields a result embed whose `.description` contains `📍 home`; driving an argless command yields a result embed `.description` with no `📍`.
    - The clone-survival test asserts the `_render_view(expanded=True)` Select clone's selected option `default is True` AND each Button clone's `.emoji` is its D-05 glyph.
    - The `select.options` test asserts `default=True` on the selected option.
    - `uv run pytest tests/test_panel.py -q` is fully green with anti-drift snapshots refreshed only where the intended addition changed.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_panel.py -q</automated>
  </verify>
  <done>Both panel result-render paths thread the selected location into `render_embed`, so the `📍` indicator shows on location-bearing panel results and is suppressed on argless; the dropdown default and emoji survive the clone; anti-drift snapshots are green.</done>
</task>

<task type="auto">
  <name>Task 3: Agent self-UAT — full suite + byte-level render evidence (Gate-1, PANEL-11/12/13)</name>
  <files>.planning/phases/20-isolation-hardening-polish/20-SELF-UAT.md</files>
  <read_first>
    - $HOME/.claude/CLAUDE.md (Two-Gate UAT policy — Gate-1 autonomous self-UAT format: per-criterion exact commands + evidence + PASS/FAIL/PARTIAL)
    - .planning/phases/20-isolation-hardening-polish/20-RESEARCH.md (§"Validation Architecture" → Sampling Rate / Phase gate; §"Assumptions Log" A1/A2 — the deferred Gate-2 items)
    - .planning/ROADMAP.md §"Phase 20" — the 4 success criteria to map evidence against
  </read_first>
  <action>
    After Tasks 1–2 (and the Wave-1 plans 20-01/20-02) are green, write a Gate-1 self-UAT log to `.planning/phases/20-isolation-hardening-polish/20-SELF-UAT.md`. Drive the real system at the data/byte level — do not stop at green unit tests. Record, per success criterion, the exact command, the captured evidence, and a PASS/FAIL/PARTIAL verdict:

    - SC#1 (isolation): run `uv run pytest tests/test_scheduler.py -k hanging_callback tests/test_dispatch.py -k executor -x`; paste the pass output as evidence.
    - SC#2 (indicator) + SC#4 (stamp): construct a `render_embed(reply, location="home")` and a `render_embed(reply)` (argless) IN A PYTHON SNIPPET (`uv run python -c …`), print `embed.description`, and paste the BYTE-LEVEL output proving the description contains `📍 home` and an `Updated <t:` token (with `:t` and `:R`), and that the argless description has no `📍`. Prove the `<t:` token is ABSENT from `embed.title`.
    - SC#2 (dropdown) + SC#3 (emoji): construct a `PanelView` from the gateway-free fakes, drive `_render_view(expanded=True)`, and print/paste each clone's `.emoji` and the Select clone's selected-option `.default` — proving emoji + default SURVIVE the clone.
    - Layout guard: paste evidence that `_assert_layout` still passes (the full panel constructs without raising) — `uv run pytest tests/test_panel.py -k layout -x`.
    - Full suite: run `uv run pytest -q` and paste the green summary.

    Record the DEFERRED Gate-2 (human-UAT) obligations explicitly as PARTIAL (mechanism verified, pixel render deferred), NOT skipped: (A1) live emoji pixel rendering on the operator's Discord client, (A2) `<t:R>` self-ageing visual on-device, and on-device visual confirmation of the `📍` line — all requiring a deploy + `sudo systemctl restart weatherbot` on host `yahir-mint`. Do NOT emit a blocking per-phase human checkpoint; these are milestone-close obligations.
  </action>
  <acceptance_criteria>
    - `.planning/phases/20-isolation-hardening-polish/20-SELF-UAT.md` exists with a per-success-criterion table: each row has the exact command, captured evidence, and a PASS/FAIL/PARTIAL verdict.
    - The byte-level evidence shows the embed description containing `📍 home` + the `<t:` token, and the argless description lacking `📍`, and the `<t:` token absent from the title.
    - The clone-survival evidence shows emoji + dropdown default present on a `_render_view` clone.
    - `uv run pytest -q` green output is captured.
    - The deferred Gate-2 human-UAT items (A1 emoji pixels, A2 `<t:R>` ageing, on-device 📍) are listed as PARTIAL/deferred, not as a phase blocker.
  </acceptance_criteria>
  <verify>
    <human-check>The 20-SELF-UAT.md log records, per ROADMAP success criterion, exact commands + byte-level evidence + PASS/FAIL/PARTIAL, and lists the deferred Gate-2 on-device items.</human-check>
    <automated>test -f .planning/phases/20-isolation-hardening-polish/20-SELF-UAT.md && uv run pytest -q</automated>
  </verify>
  <done>A persistent Gate-1 self-UAT log proves all four success criteria with exact commands and byte-level evidence (full suite green, description carries 📍 + the `<t:>` token, emoji/default survive the clone, layout green), and records the on-device emoji/`<t:R>` visual confirmation as deferred Gate-2 obligations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → panel component click | Existing Phase-17 interaction boundary; this plan adds presentation polish (emoji/default mark) over it. No new input crosses here. |
| panel state (`_selected_location`) → render layer | The in-memory selected location is threaded into `render_embed` for the `📍` line — a configured, non-secret value. No untrusted input. |

## STRIDE Threat Register

> Honest scope: presentation-only changes over the assembled panel; no new untrusted input,
> no new component slot, no auth/network surface, no deps. The load-bearing property remains
> the failure-isolation guarantee (re-proven in Plan 20-01; this plan does not alter the
> isolation path).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering | The `_render_view` clone silently dropping emoji/default (regressing PANEL-12/13a) | mitigate | Task 1 fixes the clone to carry `emoji=child.emoji` + re-derive `default`; Tasks 1–2 assert clone-survival (not just the `__init__` view). |
| T-20-07 | Denial of Service | Adding a component slot tripping `_assert_layout` at send time | mitigate | Zero new component slots this plan (emoji/indicator/stamp are non-component); `_assert_layout` stays untouched + green (asserted). |
| T-20-08 | Information Disclosure | The leak-free error path (`_ERROR_REPLY`) altered by the polish lines | accept | The `📍`/`Updated` lines render on the result/embed path only; the error path (`_safe_error_edit` / `_ERROR_REPLY`) is unchanged — confirmed by leaving the `UnknownLocationError`/error branches untouched. |
| T-20-09 | Tampering | npm/pip/cargo installs | accept | No package installs — milestone Out of Scope forbids new deps. |
</threat_model>

<verification>
- `uv run pytest tests/test_panel.py -q` fully green (clone-survival, emoji, default-mark, indicator, anti-drift refreshed).
- `uv run pytest -q` full suite green.
- Grep `render_embed\(reply, location=` finds both panel result-render call sites.
- `_assert_layout` unchanged; `-k layout` green.
- `.planning/phases/20-isolation-hardening-polish/20-SELF-UAT.md` present with per-criterion evidence + deferred Gate-2 items.
</verification>

<success_criteria>
ROADMAP SC#2 (visible selected-location indicator — embed line + dropdown highlight) and
SC#3 (emoji-coded button labels) are satisfied AND survive the `_render_view` clone on every
render path; the panel result renders thread the selected location into the shared
`render_embed`; `_assert_layout` stays green; a Gate-1 self-UAT log captures byte-level
evidence for all four phase success criteria with on-device visual confirmation deferred to
Gate-2.
</success_criteria>

<output>
Create `.planning/phases/20-isolation-hardening-polish/20-03-SUMMARY.md` when done.
</output>
