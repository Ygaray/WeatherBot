---
phase: 17-minimal-persistent-panel-core-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/interactive/registry.py
  - weatherbot/interactive/commands/weather_views.py
  - weatherbot/cli.py
  - tests/test_registry.py
autonomous: true
requirements: [PANEL-03]
must_haves:
  truths:
    - "A real `weather` registry command exists and routes through dispatch_spec → render_embed"
    - "`!weather [loc]` Discord reply is byte-identical to build_inbound_embed (Now / High·Low / Rain)"
    - "The full CLI still builds with no argparse conflicting-subparser error after adding the weather spec"
    - "`weather` appears exactly once in `weatherbot --help` (the standalone subparser wins)"
  artifacts:
    - path: "weatherbot/interactive/registry.py"
      provides: "CommandSpec('weather', 'Weather', ..., True) + _wire_handlers entry"
      contains: "\"weather\""
    - path: "weatherbot/interactive/commands/weather_views.py"
      provides: "weather(result) -> CommandReply handler (Now / High·Low / Rain)"
      contains: "def weather("
    - path: "weatherbot/cli.py"
      provides: "registry-loop skip-guard for hand-written subparser names"
      contains: "continue"
  key_links:
    - from: "weatherbot/interactive/registry.py"
      to: "weatherbot/interactive/commands/weather_views.py"
      via: "_wire_handlers maps 'weather' → weather_views.weather"
      pattern: "\"weather\":\\s*weather_views.weather"
    - from: "weatherbot/cli.py"
      to: "registry.COMMANDS loop"
      via: "skip names already registered as hand-written subparsers"
      pattern: "_HANDWRITTEN"
---

<objective>
W2 (D-07/D-08): turn `weather` into a real first-class registry command so every
panel button — including weather — routes uniformly through `dispatch_spec` →
`render_embed`, with NO panel-side special case. This is a BEHAVIOR-PRESERVING
REFACTOR, not a greenfield add: adding the spec ripples into four derived surfaces
(`!help`, the CLI subparser loop, the `test_registry.py` Weather-group anti-drift
assertion, and the now-live `!weather` reply), and the existing `!weather` reply
MUST stay byte-identical to `build_inbound_embed`.

Purpose: PANEL-10 (Phase 16) made the dispatch ladder the single source of truth;
the panel is its third caller. For the weather button to route through that one
ladder, `weather` must be a registry spec. W2 was explicitly chosen (D-07) over a
panel-side weather special-case. The CLI subparser collision (Pitfall 1) is the
real reason D-08 is "not purely additive" — it breaks the ENTIRE CLI if unguarded.
Output: a wired `weather` spec + handler + CLI skip-guard + updated registry test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-minimal-persistent-panel-core-wiring/17-RESEARCH.md
@.planning/phases/17-minimal-persistent-panel-core-wiring/17-PATTERNS.md
@weatherbot/interactive/registry.py
@weatherbot/interactive/commands/weather_views.py
@weatherbot/interactive/bot.py
@weatherbot/interactive/dispatch.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the weather CommandSpec + byte-identical handler, update the registry anti-drift test</name>
  <files>weatherbot/interactive/registry.py, weatherbot/interactive/commands/weather_views.py, tests/test_registry.py</files>
  <read_first>
    - weatherbot/interactive/registry.py (the `_SPECS` tuple at :50-81 — the sibling `CommandSpec("alerts", "Weather", ..., True)` row at :51; the `_wire_handlers` dict at :103-114; `BY_NAME`/`COMMANDS` derivation at :119-122)
    - weatherbot/interactive/commands/weather_views.py (the sibling `wind(result)` handler at :165-180 — the read-`result.forecast` + return-`CommandReply` shape; the module `CommandReply` import at :23)
    - weatherbot/interactive/bot.py (`build_inbound_embed` at :194-214 — the BYTE-IDENTICAL reference: title `f"Weather — {forecast.location}"` then fields `("Now", forecast.temp_display)`, `("High / Low", f"{high_display} / {low_display}")`, `("Rain", f"{forecast.rain_chance}%")`)
    - weatherbot/weather/models.py:203 (CRITICAL: `Forecast.location` is a plain `str`; `build_inbound_embed` uses `forecast.location`, NOT `result.location.name` — the handler MUST use `result.forecast.location` to stay byte-identical)
    - tests/test_registry.py:68-117 (the Weather-group assertion at :74; the `test_uv_command_registered_and_wired` sibling at :98-110 to mirror)
    - weatherbot/interactive/dispatch.py:88-96 (the catch-all branch #4 `handler(result)` — confirms a `takes_location` single-arg handler needs ZERO ladder edit)
  </read_first>
  <behavior>
    - Test: `BY_NAME["weather"]` exists, `.handler is weather_views.weather`, `.takes_location is True`, `.group == "Weather"`.
    - Test: the Weather group set equals `{"weather", "alerts", "sun", "wind", "next-cloudy", "uv"}`.
    - Test: `weather_views.weather(result)` returns a `CommandReply` whose `title == f"Weather — {result.forecast.location}"` and whose `lines` are exactly `(("Now", forecast.temp_display), ("High / Low", f"{forecast.high_display} / {forecast.low_display}"), ("Rain", f"{forecast.rain_chance}%"))` — i.e. `render_embed(weather(result))` produces the same title+fields as `build_inbound_embed(result.forecast)`.
  </behavior>
  <action>
    Add `CommandSpec("weather", "Weather", "Current conditions for a location.", True)` as the FIRST row of the Weather group in `_SPECS` (so the curated panel order weather→uv→next-cloudy→sun→wind reads naturally). Add `"weather": weather_views.weather` to the `_wire_handlers` handlers dict. In `weather_views.py`, add `def weather(result: LookupResult) -> CommandReply:` reading `f = result.forecast` and returning `CommandReply(title=f"Weather — {f.location}", lines=(("Now", f.temp_display), ("High / Low", f"{f.high_display} / {f.low_display}"), ("Rain", f"{f.rain_chance}%")))` — use `f.location` (the string) to match `build_inbound_embed`, NOT `result.location.name`. Update `tests/test_registry.py:74` to `assert weather == {"weather", "alerts", "sun", "wind", "next-cloudy", "uv"}` and add a positive `test_weather_command_registered_and_wired` mirroring `test_uv_command_registered_and_wired`. No `dispatch.py` ladder edit (the handler lands in catch-all #4).
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from weatherbot.interactive import registry as r; s=r.BY_NAME['weather']; assert s.takes_location and s.group=='Weather' and callable(s.handler)"` exits 0.
    - `uv run pytest tests/test_registry.py -q` exits 0 (the Weather-group assertion now includes `weather`; the new positive test passes).
    - The new `weather` handler uses `result.forecast.location` (string) — grep `weather_views.py` shows `f.location` in the weather handler, NOT `result.location.name`.
    - `weather` appears in `render_help()` under the Weather group (`uv run python -c "from weatherbot.interactive.registry import render_help; assert 'weather' in render_help()"` exits 0).
  </acceptance_criteria>
  <done>The `weather` spec + handler are wired, byte-identical to build_inbound_embed, and the registry test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CLI registry-loop skip-guard so the new weather spec doesn't double-register</name>
  <files>weatherbot/cli.py</files>
  <read_first>
    - weatherbot/cli.py:731-747 (the hand-written standalone `weather` subparser `p_weather` with its `-v/--verbose` flag and quiet-by-default path)
    - weatherbot/cli.py:813-838 (the registry loop `for _spec in _registry.COMMANDS: subparsers.add_parser(_spec.name, ...)` — the collision site at :815, NO skip-guard)
    - weatherbot/cli.py:860 vs ~922 (dispatch: `args.command == "weather"` routes to `_cmd_weather` BEFORE the registry resolution `_registry.BY_NAME.get(args.command)` — so the standalone weather path still wins at dispatch; the collision is PURELY at subparser-build time)
    - .planning/phases/17-minimal-persistent-panel-core-wiring/17-RESEARCH.md §Pitfall 1 and §Assumptions Log A1 (keep `p_weather`, skip-guard the loop)
    - .planning/phases/17-minimal-persistent-panel-core-wiring/17-PATTERNS.md → `weatherbot/cli.py` section (the `_HANDWRITTEN` set fix)
  </read_first>
  <action>
    Before the `for _spec in _registry.COMMANDS:` loop at cli.py:815, define `_HANDWRITTEN = {"weather", "run", "check", "check-config", "reload", "send-now", "geocode"}` (the names already registered as hand-written subparsers above). As the first line inside the loop body, add `if _spec.name in _HANDWRITTEN: continue` so the standalone subparsers win and only genuinely-new registry commands get a loop-built subparser. Do NOT delete `p_weather` (A1: preserves its `-v` flag + quiet-by-default path). Do NOT touch the dispatch tail (the `weather`→`_cmd_weather` route at :860 already precedes the registry resolution). Verify the set matches the exact hand-written subparser names registered at :731-803 — if any name there differs, use the actual registered names.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run weatherbot --help` exits 0 (no `argparse.ArgumentError: conflicting subparser`) and lists `weather` exactly once.
    - `uv run pytest tests/test_cli.py -q` exits 0 (no subparser collision).
    - `uv run weatherbot weather --help` still shows the `-v/--verbose` flag (the standalone subparser was preserved, not replaced by the loop-built one).
    - The dispatch tail is unchanged (grep confirms `_cmd_weather` is still routed for `args.command == "weather"`).
  </acceptance_criteria>
  <done>The CLI builds with `weather` registered once, the standalone subparser intact, all CLI tests green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (excluded from drift verification):
- `weatherbot/interactive/commands/weather_views.py`: `weather(result) -> CommandReply` handler
- `weatherbot/interactive/registry.py`: the `CommandSpec("weather", ...)` row + its `_wire_handlers` entry (so `BY_NAME["weather"]` is new)
- `weatherbot/cli.py`: the `_HANDWRITTEN` set + skip-guard `continue`
- `tests/test_registry.py`: `test_weather_command_registered_and_wired` (new test); the Weather-group assertion at :74 is MODIFIED to include `weather`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → argparse | Operator-supplied subcommand names; the registry loop builds subparsers from in-repo command names only (no external input) |
| registry name → every derived surface | Adding a name ripples to help/CLI/tests — all in-repo, caught by the contractual suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-SC | Tampering | npm/pip/cargo installs | accept | No package install (RESEARCH §Package Legitimacy Audit: N/A). discord.py already pinned; no new dep. |
| T-17-02-01 | Tampering | CLI subparser builder | mitigate | Skip-guard ensures the registry loop cannot silently shadow/duplicate a hand-written subparser → no argparse crash that would take down the whole CLI. Verified by `tests/test_cli.py`. |
| T-17-02-02 | Information Disclosure | new `weather` reply / handler | mitigate | The handler reads only `result.forecast` fields (no secret); reuses `render_embed`'s existing secret-free path; the OpenWeather appid / Discord token never enters the reply (reuse v1.1 secret-hygiene rule). |
| T-17-02-03 | Tampering | `!weather` byte-identical contract | mitigate | The handler reproduces `build_inbound_embed`'s exact fields (Now/High·Low/Rain) using `forecast.location`; the contractual registry suite proves no drift. |

No high-severity unmitigated threats.
</threat_model>

<verification>
- `uv run pytest tests/test_registry.py tests/test_cli.py tests/test_command.py tests/test_dispatch.py tests/test_bot.py -q` exits 0 (the full contractual anti-drift suite stays green).
- `uv run weatherbot --help` lists `weather` exactly once.
</verification>

<success_criteria>
- `weather` is a real registry spec routing through `dispatch_spec` → `render_embed`.
- The `weather` reply is byte-identical to `build_inbound_embed` (Now / High·Low / Rain), using `forecast.location`.
- The full CLI builds with no subparser collision; `weather` appears once in `--help`.
- The Weather-group anti-drift assertion is updated; the contractual suite is green.
</success_criteria>

<output>
Create `.planning/phases/17-minimal-persistent-panel-core-wiring/17-02-SUMMARY.md` when done.
</output>
