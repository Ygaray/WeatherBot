---
phase: 17-minimal-persistent-panel-core-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_panel.py
autonomous: true
requirements: [PANEL-02, PANEL-03, PANEL-04, PANEL-05, PANEL-06, PANEL-08]
must_haves:
  truths:
    - "tests/test_panel.py collects with every PANEL req node ID present (RED while panel.py is unbuilt)"
    - "A fake_interaction factory exists shaped like a discord.py Interaction (no discord import, no network)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "_make_fake_interaction factory + fake_interaction fixture"
      contains: "_make_fake_interaction"
    - path: "tests/test_panel.py"
      provides: "Wave-0 RED test stubs for PANEL-02/03/04/05/06/08, D-07/08/10/13, isolation"
      contains: "from weatherbot.interactive import panel"
  key_links:
    - from: "tests/test_panel.py"
      to: "weatherbot/interactive/panel.py"
      via: "deferred import inside _panel()"
      pattern: "from weatherbot.interactive import panel"
---

<objective>
Create the Wave-0 test surface for Phase 17: a gateway-free `fake_interaction`
factory in `tests/conftest.py` (sibling to the existing `_make_fake_discord_message`)
and `tests/test_panel.py` with the full node-ID list every later task verifies
against. These tests collect immediately (deferred import of the not-yet-built
`panel` module) and are RED until Plan 03 builds `panel.py` — they are the
sampling target the Nyquist validation map requires.

Purpose: No production code exists for the panel yet, so per the Nyquist rule the
test scaffold MUST exist first. Every PANEL requirement gets a named test node so
Plan 03's executor has concrete GREEN targets and no 3-task gap goes unverified.
Output: `tests/conftest.py` (factory + fixture added), `tests/test_panel.py` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-minimal-persistent-panel-core-wiring/17-RESEARCH.md
@.planning/phases/17-minimal-persistent-panel-core-wiring/17-PATTERNS.md
@.planning/phases/17-minimal-persistent-panel-core-wiring/17-UI-SPEC.md
@tests/conftest.py
@tests/test_bot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the gateway-free fake_interaction factory + fixture to conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (the `_make_fake_discord_message` factory at conftest.py:114-153 — the exact builder shape + the `fake_discord_message` fixture at :150-153 to mirror)
    - .planning/phases/17-minimal-persistent-panel-core-wiring/17-RESEARCH.md §"Wave 0 Gaps" (the precise attribute list the fake Interaction must expose) and §"Phase Requirements → Test Map"
    - .planning/phases/17-minimal-persistent-panel-core-wiring/17-PATTERNS.md → `tests/test_panel.py` section (the `_make_fake_interaction` mirror note, lines ~340-376)
  </read_first>
  <action>
    Add a `_make_fake_interaction(*, user_id=12345, user_bot=False, custom_id="wb:cmd:weather", is_done=False)` builder mirroring `_make_fake_discord_message`. It returns a `MagicMock` named "discord.Interaction" with NO discord import and NO network, exposing exactly: `.user.id` (= user_id), `.user.bot` (= user_bot), `.data` (= a dict `{"custom_id": custom_id}` so `(interaction.data or {}).get("custom_id")` works), `.response.edit_message` (AsyncMock — the single ack), `.response.send_message` (AsyncMock — the reject path), `.response.is_done` (MagicMock returning `is_done`), `.edit_original_response` (AsyncMock — the in-place result/error), and `.followup.send` (AsyncMock — the post-ack fallback). Then add a `fake_interaction` pytest fixture returning the factory, mirroring the `fake_discord_message` fixture. Do NOT remove or alter the existing message factory.
  </action>
  <verify>
    <automated>uv run python -c "import tests.conftest as c; assert hasattr(c, '_make_fake_interaction'); i=c._make_fake_interaction(custom_id='wb:cmd:status'); assert i.user.id==12345 and i.data['custom_id']=='wb:cmd:status'"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines `_make_fake_interaction` and a `fake_interaction` fixture.
    - The returned mock exposes `.user.id`, `.user.bot`, `.data["custom_id"]`, `.response.edit_message` (awaitable), `.response.send_message` (awaitable), `.response.is_done()` (returns the bool param), `.edit_original_response` (awaitable), `.followup.send` (awaitable).
    - No `import discord` is added to conftest; the existing `_make_fake_discord_message` factory is untouched (its node IDs in test_bot.py still pass: `uv run pytest tests/test_bot.py -q` exits 0).
  </acceptance_criteria>
  <done>The fake_interaction factory + fixture exist and construct without a discord import or network call.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_panel.py with the full RED node-ID list</name>
  <files>tests/test_panel.py</files>
  <read_first>
    - tests/test_bot.py (the `_bot()` deferred-import helper at :32-46, the `_run(coro)=asyncio.run` driver, `_patch_command_in_registry` at :795-809, and the isolation test `test_raising_command_handler_is_isolated` at :434-468 — the direct analog for the panel isolation test)
    - .planning/phases/17-minimal-persistent-panel-core-wiring/17-RESEARCH.md §"Phase Requirements → Test Map" (the exact node IDs to create) and §"Validation Architecture"
    - .planning/phases/17-minimal-persistent-panel-core-wiring/17-VALIDATION.md (the per-task verification map this file fulfills)
  </read_first>
  <action>
    Create `tests/test_panel.py` with a `_panel()` deferred-import helper (`from weatherbot.interactive import panel; return panel`), a `_run(coro)=asyncio.run(coro)` driver, and `_OPERATOR_ID = 12345`, mirroring test_bot.py. Define EXACTLY these test functions, each a real assertion against the panel API (not bare `pass`), using the `fake_interaction` fixture: `test_dropdown_from_config` (PANEL-02 — Select options derived from `holder.current().locations`), `test_dropdown_rederives_on_hot_reload` (PANEL-02 — options reflect a changed holder snapshot), `test_location_button_uses_selection` (PANEL-03 — a location button passes `_selected_location` as `arg` to `dispatch_spec`), `test_argless_button_ignores_selection` (PANEL-04 — status/alerts pass `arg=None`), `test_single_ack_before_fetch` (PANEL-05 — exactly one `response.edit_message` before the fetch; no second `response.*`), `test_result_renders_in_place` (PANEL-06 — result lands via `edit_original_response`; no new-message send), `test_non_operator_rejected_leak_free` (PANEL-08 — non-operator gets `response.send_message(ephemeral=True)` with the byte-exact string `This panel is in use by someone else.`, `interaction_check` returns False, no handler runs), `test_reject_does_not_call_on_error` (D-13 — clean `return False` does not invoke `View.on_error`), `test_view_persistent_and_layout_bounded` (D-10 — `view.is_persistent() is True`; every child `custom_id` ≤100 and `label` ≤80), `test_weather_spec_byte_identical` (D-07/08 — the `weather` reply renders to the same fields as `build_inbound_embed`), and `test_callback_raise_isolated` (isolation — a raising handler is swallowed, never re-raised, panel still answers generically). Patch handlers via `monkeypatch.setitem(registry.BY_NAME, ...)` mirroring test_bot's `_patch_command_in_registry`. These reference symbols Plan 03 creates (`panel.PanelView`, `_selected_location`); the deferred import keeps collection green while the bodies stay RED until Plan 03. Mark any genuinely-cross-plan dependency with a clear comment, never `xfail`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_panel.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_panel.py --collect-only -q` lists all 11 node IDs named above (collection succeeds via the deferred import).
    - Running `uv run pytest tests/test_panel.py -q` fails RED with `ModuleNotFoundError`/`AttributeError` on `weatherbot.interactive.panel` (NOT a collection error) — proving the stubs are real assertions awaiting Plan 03, not `pass`.
    - The file imports `panel` only inside `_panel()` (deferred), never at module top.
  </acceptance_criteria>
  <done>tests/test_panel.py collects all required node IDs and is RED pending panel.py.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates these NEW test symbols (excluded from drift verification — they do not yet exist in the codebase):
- `tests/conftest.py`: `_make_fake_interaction` factory, `fake_interaction` fixture
- `tests/test_panel.py` (new file) and its test node IDs: `test_dropdown_from_config`, `test_dropdown_rederives_on_hot_reload`, `test_location_button_uses_selection`, `test_argless_button_ignores_selection`, `test_single_ack_before_fetch`, `test_result_renders_in_place`, `test_non_operator_rejected_leak_free`, `test_reject_does_not_call_on_error`, `test_view_persistent_and_layout_bounded`, `test_weather_spec_byte_identical`, `test_callback_raise_isolated`; helpers `_panel`, `_run`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → production module | Tests import the panel module under test; no untrusted network/gateway input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan; pytest already present (RESEARCH §Package Legitimacy Audit: N/A). No `[ASSUMED]`/`[SUS]` packages. |
| T-17-01-01 | Information Disclosure | fake_interaction factory | mitigate | The factory carries no real token/webhook/secret; assertions use placeholder ids only — no secret enters a test fixture or log. |

No high-severity unmitigated threats. Test-only plan, no runtime attack surface.
</threat_model>

<verification>
- `uv run pytest tests/test_panel.py --collect-only -q` lists 11 node IDs.
- `uv run pytest tests/test_bot.py -q` still exits 0 (the existing message factory is untouched).
</verification>

<success_criteria>
- The `fake_interaction` factory + fixture exist and construct without a discord import.
- `tests/test_panel.py` collects every PANEL req node ID and is RED (awaiting Plan 03), with real assertions (not `pass`).
</success_criteria>

<output>
Create `.planning/phases/17-minimal-persistent-panel-core-wiring/17-01-SUMMARY.md` when done.
</output>
