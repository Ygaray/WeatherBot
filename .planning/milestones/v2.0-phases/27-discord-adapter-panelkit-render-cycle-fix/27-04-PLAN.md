---
phase: 27-discord-adapter-panelkit-render-cycle-fix
plan: 04
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - tests/test_panel.py
autonomous: true
requirements: [SEAM-07]

must_haves:
  truths:
    - "tests/test_panel.py::_make_panel constructs the MODULE PanelKit (via app contributors + injected render/marker/selection) instead of the deleted panel.PanelView — every existing assertion stays byte-identical"
    - "The _render_view / _clone_child / _selected_location-touching tests are rewired to the relocated shape (PanelKit clone path + SelectedContext) WITHOUT re-baselining any golden"
    - "tests/test_golden_custom_ids.py and tests/test_oracle_selfproof.py — which `from tests.test_panel import _FakeHolder, _SpyCache, _make_panel` — keep COLLECTING and stay green"
    - "After the rewire, tests/test_panel.py + tests/test_golden_custom_ids.py + tests/test_oracle_selfproof.py are byte-identical green (no .ambr re-baseline)"
  artifacts:
    - path: "tests/test_panel.py"
      provides: "Harness (_make_panel) rewired to construct the module PanelKit + app contributors; the internal-attr-touching tests realigned to the relocated shape, assertions byte-identical"
      contains: "PanelKit"
  key_links:
    - from: "tests/test_panel.py"
      to: "yahir_reusable_bot/discord/panelkit.py"
      via: "_make_panel constructs the module PanelKit with the app contributors + injected render/marker/selection"
      pattern: "PanelKit"
    - from: "tests/test_golden_custom_ids.py"
      to: "tests/test_panel.py"
      via: "imports _FakeHolder/_SpyCache/_make_panel — must keep collecting after the rewire"
      pattern: "from tests.test_panel import"
---

<objective>
Rewire the byte-identical panel oracle harness onto the relocated module adapter. Plan 02 deleted `panel.PanelView` + `_render_view`/`_clone_child`/`_selected_location` and moved the machinery to the module `PanelKit` (Plan 01) — but `tests/test_panel.py::_make_panel` (L134-140) still constructs `panel.PanelView(...)`, and `tests/test_golden_custom_ids.py` (L24) + `tests/test_oracle_selfproof.py` (L49) `from tests.test_panel import _FakeHolder, _SpyCache, _make_panel`. Without this rewire the byte-identical oracle collapses at COLLECTION across 4 test files.

This plan rewires `_make_panel` and the `_render_view`/`_clone_child`/`_selected_location`-touching tests to construct the module `PanelKit` (via the app contributors + injected `render`/`marker`/`selection`), PRESERVING every existing assertion byte-identically. It is the dedicated oracle-harness realignment (scope_sanity isolation) and runs in Wave 3 BEFORE Plan 03's full byte-identical oracle re-run (Wave 4).

Purpose: SEAM-07 — keep the byte-identical panel/custom_id oracle (the strongest sampling rate) intact across the `PanelView`→`PanelKit` relocation; no golden re-baseline.
Output: rewired `tests/test_panel.py`; `test_golden_custom_ids.py` + `test_oracle_selfproof.py` still collect + pass byte-identically.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-CONTEXT.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-01-SUMMARY.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
CHANGED test harness this PLAN realigns (NO new framework, NO golden re-baseline):
- `tests/test_panel.py` :: `_make_panel` (L134-140) rewired to construct the module `PanelKit` (app contributors + injected `render`/`marker`/`selection`) instead of `panel.PanelView`; the `_render_view`-touching tests (`test_emoji_survives_render_view_clone` L965, `test_dropdown_default_mark_survives_render_view_clone` L1042) re-pointed at the relocated clone path; the `_selected_location`-asserting tests (L460, L1030, L1058, the PANEL-03/04 selection tests) re-pointed at the `SelectedContext` seam (`.value` / `.set()`); the `render_embed` call sites (L484, L739, L768) unchanged (render_embed signature is unchanged per 27-02)
- DOWNSTREAM (confirmed, not edited): `tests/test_golden_custom_ids.py` + `tests/test_oracle_selfproof.py` keep importing `_FakeHolder`/`_SpyCache`/`_make_panel` and keep collecting + passing
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewire _make_panel + the internal-attr tests onto the module PanelKit (byte-identical)</name>
  <files>tests/test_panel.py</files>
  <read_first>
    - tests/test_panel.py L42-53 (`_panel()` deferred import — now imports the SHRUNK app panel: the contributor builders + `PANEL_MARKER`, no `PanelView`), L134-140 (`_make_panel` — the single centralized ctor call to rewire; the docstring promises "a ctor-signature change is a single edit here"), L171-221 (PANEL-02 dropdown-from-config + hot-reload — `view.children` Select read by `wb:loc:select`, the `panel.PanelView(...)` direct re-construct at L212), L229-292 (PANEL-03/04 — `cache.calls[-1][0] == "travel"` proves the in-memory selection reached the fetch; these read/assert `_selected_location` semantics), L448-462 (`test_freshly_built_view_is_persistent_and_defaults_location` — `view.is_persistent()` + `view._selected_location == "home"`), L470-492 (`test_weather_spec_byte_identical` — calls `render_embed(reply)` directly, UNCHANGED), L500-519 (`test_callback_raise_isolated` — `view.on_command(...)` envelope), L725-768 (`render_embed(canonical)` in-place-render assertion, UNCHANGED), L965-990 (`test_emoji_survives_render_view_clone` — `view._render_view()` + `view._render_view(disabled=True)`), L1019-1060 (`test_dropdown_default_marks_selected_location` + `test_dropdown_default_mark_survives_render_view_clone` — `view._selected_location` + `view._render_view()` clone highlight)
    - yahir_reusable_bot/discord/panelkit.py (Plan 01 — the `PanelKit.__init__` keyword-only params: `registry`/`command_names`/`marker`/`operator_id`/`selection`/`contributors`/`render` + holder/accessor; the relocated clone path method name — `_render_view` or its renamed equivalent — and whether `_selected_location` is now `selection.value`)
    - yahir_reusable_bot/discord/selection.py (Plan 01 — `SelectedContext(value)`, `.value`, `.set()`)
    - weatherbot/interactive/panel.py (post-Plan-02 — the app contributor builders + `PANEL_MARKER` the harness must assemble to feed `PanelKit`; the `LocationSelect`/`ForecastButton` cosmetic components + their `wb:` custom_ids)
    - tests/test_golden_custom_ids.py L24-30 + tests/test_oracle_selfproof.py L49 + L153-154 (the harness consumers — `_make_panel(...)` then `view.children[0].custom_id == "wb:loc:select"`; the rewire must keep `view.children` + the read custom_id byte-identical)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the harness-rewire section — the PanelKit construction recipe from the app contributors)
  </read_first>
  <action>
    Rewire `tests/test_panel.py` so the existing node IDs drive the relocated module `PanelKit` (Plan 01) through the app contributors (Plan 02), with EVERY existing assertion preserved byte-identically. Do NOT add/remove node IDs, do NOT weaken an assertion, do NOT re-baseline any golden.

    `_panel()` (L42-53): keep the deferred import, but it now imports the SHRUNK app panel module (the contributor builders + `PANEL_MARKER`), NOT `PanelView`. If the harness needs the module adapter, import it from `yahir_reusable_bot.discord` (deferred, same Wave-0 collect-safe discipline).

    `_make_panel` (L134-140) — THE single centralized rewire (the docstring already promises a ctor change is one edit here): instead of `panel.PanelView(holder=holder, operator_id=operator_id, cache=cache)`, assemble the module `PanelKit` exactly as the composition root does (27-02 Task 3): build the app contributor list (the Select contributor + the forecast-grid contributor from the app's `panel` builders), a `SelectedContext("home")` seeded to `locations[0]`, `marker=PANEL_MARKER` (`"wb:"`), `render=` a render adapter that calls `render_embed(reply, location=...)` (mirror the app `_render_bridge`; or pass `render_embed` wrapped so the harness's existing `render_embed`-based assertions stay byte-identical), `registry=`/`command_names=` the same curated command set today's `PanelView` builds, `operator_id=operator_id`, plus the `holder`/`cache` accessors the callbacks read per tap. Keep the signature `_make_panel(panel, *, holder, cache, operator_id=_OPERATOR_ID)` UNCHANGED so the ~14 call sites + the two downstream importers do not change. The returned object must expose `.children` (the Select at index 0 carrying `wb:loc:select`), `.is_persistent()`, `.on_command(...)`, and the clone path — byte-identical to what the tests read.

    The hot-reload re-construct at L212 (`panel.PanelView(holder=..., operator_id=..., cache=...)`) — route it through `_make_panel` (or the same PanelKit assembly) so it too builds a `PanelKit`; keep the `[opt.value for opt in select.options]` post-reload assertion byte-identical.

    `_selected_location` tests (L460, L1030, L1058, PANEL-03/04 L229-292): the in-memory selection is now the injected `SelectedContext`. Re-point these reads at the seam: where a test reads `view._selected_location`, read the selection the harness injected (e.g. `selection.value`, the `SelectedContext` the harness holds a handle to) — or expose it via the same name on the constructed view if `PanelKit` carries it. Where `on_select` sets it (the PANEL-03 "travel" selection), drive it through the contributor's `LocationSelect.callback` which now calls `ctx.set(...)`. The ASSERTED VALUES stay identical (`== "home"`, `== "travel"`, `cache.calls[-1][0] == "travel"`).

    `_render_view` clone tests (L965-990 emoji-survives, L1042-1060 default-mark-survives): re-point `view._render_view()` / `view._render_view(disabled=True)` at the relocated clone-path method on `PanelKit` (its name from Plan 01 — `_render_view` if preserved, else the renamed equivalent). The clone now re-invokes the app contributors (Plan 01 Pattern 1a) — the emoji + the `default`-mark must SURVIVE byte-identically (these are the highest-risk Pitfall-1/clone-render goldens). Keep the per-child emoji + `default` assertions UNCHANGED.

    `render_embed(...)` direct calls (L484, L739, L768): UNCHANGED — `render_embed`'s signature is unchanged (27-02), so `render_embed(reply)` / `render_embed(canonical)` keep working as-is. Do NOT route these through the bridge.

    Confirm collection of the two importers: after the rewire, `tests/test_golden_custom_ids.py` (`_make_panel(...).children[0]`-style custom_id read) and `tests/test_oracle_selfproof.py` (L153-154 reads a real `view.children[0].custom_id` == `"wb:loc:select"`) MUST still collect and pass byte-identically — the `_make_panel` return must keep `children[0]` as the Select carrying `wb:loc:select`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_panel.py tests/test_golden_custom_ids.py tests/test_oracle_selfproof.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'panel\.PanelView|\.PanelView\(' tests/test_panel.py` returns NOTHING — `_make_panel` (and the L212 hot-reload re-construct) build the module `PanelKit`, not `panel.PanelView`.
    - `grep -c 'PanelKit' tests/test_panel.py` is ≥ 1 — the harness constructs the module adapter.
    - `_make_panel`'s signature is UNCHANGED (`_make_panel(panel, *, holder, cache, operator_id=_OPERATOR_ID)`) so the ~14 in-file call sites + the two `from tests.test_panel import ... _make_panel` importers need no edit.
    - `tests/test_panel.py` passes with NO removed node IDs and NO weakened assertion (the operator gate, PANEL-03/04 selection, the per-callback isolation, the emoji-survives-clone + default-mark-survives-clone goldens all green).
    - `tests/test_golden_custom_ids.py` + `tests/test_oracle_selfproof.py` COLLECT and pass byte-identically — `_make_panel(...).children[0].custom_id == "wb:loc:select"` still holds; ZERO `.ambr` re-baseline.
    - `render_embed(reply)` / `render_embed(canonical)` direct calls (L484/L739/L768) are UNCHANGED and pass.
  </acceptance_criteria>
  <done>_make_panel + the _render_view/_clone_child/_selected_location-touching tests construct the module PanelKit (via app contributors + injected render/marker/selection); every assertion is byte-identical; no golden is re-baselined; test_golden_custom_ids.py + test_oracle_selfproof.py still collect and pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → module/app boundary | The harness is the byte-identical oracle's construction site; a wrong rewire silently re-baselines a golden |
| Harness → downstream importers | `_make_panel`'s shape is a contract `test_golden_custom_ids.py`/`test_oracle_selfproof.py` import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12 | Tampering | a silently re-baselined panel/custom_id golden during the rewire | mitigate | The rewire preserves every assertion + `_make_panel`'s return shape (`children[0]` == the `wb:loc:select` Select) byte-identically; ANY `.ambr` diff is a STOP-and-investigate, never a re-baseline (Phase-21 / D-06). The 27-03 oracle re-run (Wave 4) is the second gate. |
| T-27-13 | Denial of Service | the harness consumers failing at COLLECTION | mitigate | `_make_panel`'s signature is held UNCHANGED so `test_golden_custom_ids.py` + `test_oracle_selfproof.py` keep collecting; the verify runs all three files together to prove collection. |
| T-27-SC | Tampering | uv/pip installs | accept | No package install in this plan — test-harness-only edits. No new dependency enters the tree. |
</threat_model>

<verification>
- `uv run pytest tests/test_panel.py tests/test_golden_custom_ids.py tests/test_oracle_selfproof.py -q` — all three collect + pass byte-identically.
- `grep -nE 'panel\.PanelView|\.PanelView\(' tests/test_panel.py` returns NOTHING (harness off the deleted symbol).
- `grep -c 'PanelKit' tests/test_panel.py` ≥ 1 (harness on the module adapter).
- No `.ambr` snapshot file changes (zero golden re-baseline).
</verification>

<success_criteria>
- `_make_panel` + the internal-attr-touching tests construct the module `PanelKit` via app contributors + injected render/marker/selection.
- Every existing `test_panel.py` assertion is byte-identical; no node ID removed; no golden re-baselined.
- `test_golden_custom_ids.py` + `test_oracle_selfproof.py` keep collecting and pass byte-identically.
- The harness is ready for Plan 03's full byte-identical oracle re-run (Wave 4).
</success_criteria>

<output>
Create `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-04-SUMMARY.md` when done.
</output>
