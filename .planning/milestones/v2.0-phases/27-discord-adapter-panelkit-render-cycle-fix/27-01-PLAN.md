---
phase: 27-discord-adapter-panelkit-render-cycle-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yahir_reusable_bot/discord/__init__.py
  - yahir_reusable_bot/discord/selection.py
  - yahir_reusable_bot/discord/panelkit.py
  - yahir_reusable_bot/discord/gateway.py
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [SEAM-07]

must_haves:
  truths:
    - "The module ships a generic SelectedContext[I] with an unbound TypeVar and no base class (WeatherBot will use [str])"
    - "PanelKit lives in the module, builds command buttons from the Phase-26 registry, takes render/contributors/marker as REQUIRED no-default constructor params, and contains no wb: literal"
    - "BotThread + build_client + the create-before-delete summon orchestration live in the module and name no weather concept"
    - "discord.py is pinned exactly ==2.7.1 and uv.lock resolves to 2.7.1"
  artifacts:
    - path: "yahir_reusable_bot/discord/selection.py"
      provides: "Generic SelectedContext[I] holder (clone of ConfigHolder Generic[T])"
      contains: "class SelectedContext"
    - path: "yahir_reusable_bot/discord/panelkit.py"
      provides: "PanelKit view machinery + ownership test + clone path, marker/render/contributors injected"
      contains: "class PanelKit"
    - path: "yahir_reusable_bot/discord/gateway.py"
      provides: "BotThread + build_client + summon orchestration, relocated verbatim"
      contains: "class BotThread"
    - path: "yahir_reusable_bot/discord/__init__.py"
      provides: "Public re-exports of the adapter surface"
      exports: ["BotThread", "build_client", "PanelKit", "SelectedContext"]
  key_links:
    - from: "yahir_reusable_bot/discord/panelkit.py"
      to: "yahir_reusable_bot/registry"
      via: "PanelKit builds command buttons from the injected CommandRegistry"
      pattern: "registry"
    - from: "yahir_reusable_bot/discord/selection.py"
      to: "typing.Generic"
      via: "SelectedContext(Generic[I]) — unbound TypeVar, no base class"
      pattern: "Generic"
---

<objective>
Create the new `yahir_reusable_bot/discord/` adapter package by relocating the generic, weather-free Discord plumbing out of `weatherbot/interactive/{bot,panel}.py` — VERBATIM where possible — with the `wb:` marker, the app cosmetic components, and the embed `render` parameterized OUT (taken as required constructor args). Pin `discord.py==2.7.1`.

This plan creates the module side ONLY. The app does not yet import it (the app rewire is Plan 02). The module must import ZERO `weatherbot.*` code.

Purpose: SEAM-07 — the reusable Discord adapter "lives in the module"; resolve the `render_embed`↔`PanelView` cycle by ownership (the module owns no embed house style).
Output: 4 new module files (`selection.py`, `panelkit.py`, `gateway.py`, `__init__.py`), the tightened `discord.py==2.7.1` pin, regenerated `uv.lock`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-CONTEXT.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md
</context>

<artifacts_this_phase_produces>
NEW symbols this PLAN introduces (the module adapter surface):
- `yahir_reusable_bot/discord/` package (NEW)
- `yahir_reusable_bot/discord/selection.py` :: `SelectedContext` (Generic[I]), module-level `TypeVar I`
- `yahir_reusable_bot/discord/panelkit.py` :: `PanelKit(discord.ui.View)`, `CmdButton`, `ItemContributor` type alias, the relocated ownership predicate (marker-parameterized), the clone path, the per-callback envelope + `View.on_error` backstop + `_safe_error_edit`, the `_assert_layout`/cap guards
- `yahir_reusable_bot/discord/gateway.py` :: `BotThread`, `build_client`, the summon orchestration (function/method), optionally the relocated `_REQUIRED_PANEL_PERMS` perm-set constant (A4 — names Discord perms, not weather)
- `yahir_reusable_bot/discord/__init__.py` :: re-exports `BotThread`, `build_client`, `PanelKit`, `SelectedContext`
- `pyproject.toml` :: `discord.py==2.7.1` exact pin (tightened from `>=2.7.1,<3`)
- `uv.lock` :: regenerated, discord-py constrained to 2.7.1
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the package skeleton + the generic SelectedContext[I] (D-02)</name>
  <files>yahir_reusable_bot/discord/__init__.py, yahir_reusable_bot/discord/selection.py</files>
  <read_first>
    - yahir_reusable_bot/config/holder.py (the VERBATIM precedent: `ConfigHolder(Generic[T])`, unbound TypeVar at L37-43, NO base class, the lock-free read/mutate shape L56-72)
    - yahir_reusable_bot/registry/__init__.py (the barrel re-export shape to mirror)
    - weatherbot/interactive/panel.py L323 (today's hardcoded `_selected_location = locations[0]`), L488 (`self._selected_location = value` in on_select), L570 (button callbacks read the in-memory selection, NEVER re-read Select.values — Pitfall-3)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the selection.py section — the clone target)
  </read_first>
  <action>
    Create `yahir_reusable_bot/discord/__init__.py` and `yahir_reusable_bot/discord/selection.py`.

    `selection.py`: clone `ConfigHolder(Generic[T])` from `config/holder.py`. Declare a module-level `I = TypeVar("I")` UNBOUND (no bound, no base class — the holder precedent, per D-02). Define `class SelectedContext(Generic[I])` with `__init__(self, value: I)`, a `value` read property returning the held item, and a `set(self, value: I) -> None` mutator. Map the holder's `current()` to `value` (property) and `replace()` to `set()`. Do NOT carry the holder's `threading.Lock` — selection is single-writer (mutated only inside `on_select` on the gateway loop; RESEARCH Open Question 2). Name NO weather concept anywhere (no location term). Docstring: a typed holder for the panel's currently selected item of type I (D-02).

    `__init__.py`: start as the barrel mirroring `registry/__init__.py`. For now re-export only `SelectedContext` (the other two land in Tasks 2-3); end-state `__all__ = ["BotThread", "build_client", "PanelKit", "SelectedContext"]`. Re-export ONLY generic adapter names — never a weather/forecast/location/openweather/uv/briefing symbol.
  </action>
  <verify>
    <automated>uv run python -c "from yahir_reusable_bot.discord.selection import SelectedContext; c = SelectedContext('home'); assert c.value == 'home'; c.set('away'); assert c.value == 'away'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `yahir_reusable_bot/discord/selection.py` defines `class SelectedContext(Generic[I])` with `I` an unbound module-level `TypeVar` and NO base class beyond `Generic[I]`.
    - `SelectedContext('home').value == 'home'`; after `.set('away')`, `.value == 'away'`.
    - `grep -niE 'weather|forecast|location|openweather|\buv\b|briefing' yahir_reusable_bot/discord/selection.py` returns NOTHING (litmus-clean).
    - `selection.py` contains NO `threading` import and NO `Lock`.
  </acceptance_criteria>
  <done>SelectedContext[I] exists, is generic + litmus-clean, and round-trips a value via the value property + set().</done>
</task>

<task type="auto">
  <name>Task 2: Relocate PanelKit verbatim with marker/render/contributors parameterized out (D-03, D-04)</name>
  <files>yahir_reusable_bot/discord/panelkit.py, yahir_reusable_bot/discord/__init__.py</files>
  <read_first>
    - weatherbot/interactive/panel.py L137-177 (`_PANEL_MARKER = "wb:"` + `_is_owned_panel` — author + marker, the unforgeable static-marker ownership test), L180-203 (`CmdButton`, `custom_id=f"wb:cmd:{name}"`), L282-383 (`PanelView.__init__` + the build-children order: Select row 0, cmd rows 1-2, grid rows 3-4), L385-433 (`_assert_layout`/`_assert_layout_children` cap guards, `_MAX_CUSTOM_ID`), L435-476 (operator gate `interaction_check` — WR-03 asymmetry: bot reject sends NO ephemeral; D-12 identity-free reject copy), L490-492 + L624-642 + L726-764 (per-callback envelope + `View.on_error` + `_safe_error_edit` — never re-raises), L644-724 (`_render_view`/`_clone_child` — the clone path + the live-routing trap explanation at L656-674)
    - yahir_reusable_bot/registry/registry.py L39 (`CommandRegistry(specs)` — required arg, no baked default — the no-default injection idiom)
    - yahir_reusable_bot/scheduler/engine.py L41 (`SchedulerEngine(scheduler)` — opaque collaborator)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md (Pattern 1 — contributor callables 1a; Pattern 4 — marker parameterization + child-order contract; Pitfall 1 — clone-dead-after-first-tap; Pitfall 4 — child-order drift)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the panelkit.py section — the constructor + clone-via-contributor-re-invocation contract)
  </read_first>
  <action>
    Create `yahir_reusable_bot/discord/panelkit.py`. Relocate `PanelView` to `PanelKit(discord.ui.View)` and `CmdButton`, the ownership test, the clone path, the operator gate, the envelope + `View.on_error` + `_safe_error_edit`, and the `_assert_layout` cap guards — VERBATIM in behavior — with these parameterizations:

    Constructor (keyword-only, mirror the no-default injection idiom of `CommandRegistry(specs)`): `registry` (the Phase-26 CommandRegistry — command buttons built FROM it), `command_names: tuple[str, ...]` (curated ordered button names, app-supplied), `marker: str` (D-04, REQUIRED, no default), `operator_id: int` (D-06 — baked at construction; preserve v1 behavior, do NOT re-read per message), `selection: SelectedContext` (D-02), `contributors: list[ItemContributor]` (D-03 — app builds Select/grid here), `render: Callable` (D-01 — opaque app embed builder), plus the injected `holder`/config accessor needed for the per-tap reads. `marker`, `render`, and `contributors` MUST be required (no default) — the positive injection assertion (Plan 03) checks exactly this. Call `super().__init__(timeout=None)` (REQUIRED for persistence — Phase-18 discipline).

    Define `ItemContributor = Callable[["SelectedContext"], list[discord.ui.Item]]` — a contributor builds the app's items for ONE render.

    MARKER (D-04): replace `_PANEL_MARKER = "wb:"` with `self._marker` (the injected param). The ownership predicate uses `cid.startswith(self._marker)`. `CmdButton` builds `custom_id=f"{marker}cmd:{name}"` (marker injected). The module source must contain NO `wb:` literal.

    CHILD ORDER (load-bearing, Pitfall 4): `_build_children` must add items so the canonical view reproduces today's order byte-for-byte — Select (contributor, row 0), module command buttons (rows 1-2, from `command_names`), forecast grid (contributors, rows 3-4). Define an explicit row/position contract: order the contributor outputs + the registry-derived `CmdButton`s by row index so the children order matches the existing snapshot. (The golden gate is Plan 03; here, build to the documented order.)

    CLONE PATH (highest-risk, Pitfall 1): `_render_view` must NOT produce plain `discord.ui.Button`/`Select` (their base callback is a no-op `pass`, so the panel goes dead after first tap). Generalize today's `_clone_child` isinstance-branches (panel.py:710-723) into contributor RE-INVOCATION (RESEARCH Pattern 1a): rebuild the registry-derived `CmdButton`s AND re-invoke each app `contributor(selection)` to get fresh callback-bearing items, then `add_item` each. The module must NEVER isinstance-check an app component class. Apply `disabled` POST-construction (`child.disabled = disabled`) — the ctors take no `disabled` param (panel.py:688-690).

    OPERATOR GATE: relocate `interaction_check` verbatim with `operator_id` injected. Preserve the WR-03 asymmetry (bot author triggers reject, NO ephemeral) and the D-12 identity-free reject copy (never interpolate user/custom_id/command/operator). The reject log is the sole audit record.

    ENVELOPE + on_error + cap guards: relocate verbatim (name no weather).

    HOT-RELOAD: the relocated callbacks must keep reading live config through the injected holder/accessor per tap (the `holder.current()` reads at panel.py:520, 590, 680) — never a construction-time capture (Phase-24 contract). Thread the holder/accessor through the constructor; do not bake a config value.

    Add `from yahir_reusable_bot.discord.panelkit import PanelKit` to `__init__.py`.

    IMPORT no `weatherbot.*` — none, at module-top or in-function.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from yahir_reusable_bot.discord.panelkit import PanelKit; src=open('yahir_reusable_bot/discord/panelkit.py').read(); assert 'wb:' not in src; p=inspect.signature(PanelKit.__init__).parameters; req={n for n,v in p.items() if v.default is inspect._empty and n!='self'}; assert {'render','contributors','marker'} <= req, req; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `yahir_reusable_bot/discord/panelkit.py` defines `class PanelKit(discord.ui.View)`; `render`, `contributors`, `marker` are REQUIRED (no-default) `__init__` params.
    - `grep -c 'wb:' yahir_reusable_bot/discord/panelkit.py` returns `0` — no `wb:` literal in the module.
    - `grep -nE 'from weatherbot|import weatherbot' yahir_reusable_bot/discord/panelkit.py` returns NOTHING (no app import, top-level or in-function).
    - `grep -niE 'weather|forecast|location|openweather|\buv\b|briefing' yahir_reusable_bot/discord/panelkit.py` returns NOTHING (litmus-clean; `LocationSelect`/`ForecastButton`/📍/emoji stay app-side).
    - `_render_view` (the clone path) re-invokes the contributor callables — it contains NO `isinstance(...)` check against an app component class — and applies `disabled` post-construction.
    - `uv run python -c "import yahir_reusable_bot.discord.panelkit"` imports clean.
  </acceptance_criteria>
  <done>PanelKit is relocated, marker/render/contributors are required injected params, the clone path re-invokes contributors (no isinstance on app types), and the module is litmus-clean with no wb: literal and no weatherbot import.</done>
</task>

<task type="auto">
  <name>Task 3: Relocate BotThread + build_client + summon orchestration; pin discord.py==2.7.1 (D-05, D-06)</name>
  <files>yahir_reusable_bot/discord/gateway.py, yahir_reusable_bot/discord/__init__.py, pyproject.toml, uv.lock</files>
  <read_first>
    - weatherbot/interactive/bot.py L536-609 (`build_client` — Intents.none() + 3 intents, `setup_hook` with the DEFERRED `from weatherbot.interactive.panel import PanelView` at L583 to eliminate, the on_ready message_content assertion, the D-13 setup_hook-not-on_ready reason at L578-579), L612-709 (`BotThread` — own thread+loop via `asyncio.run(client.start(token))`, `start()` waits on `_loop_started` L652-662, `stop()` schedules `client.close()` cross-thread + joins L673-684, `_run` swallows LoginFailure/crash + sets `_failed` L686-702 — failure isolation D-11), L276-399 (`_handle_panel_summon` — the pin-scan, create-before-delete ordering L368-390, the deferred PanelView/`_is_owned_panel` import at L304-307 to eliminate, the panel_channel_id read + channel resolution L309-341, operator-copy strings L105-136, the idle embed L364-366, `_build_view` L356-362, the per-write Forbidden backstop L391-399), L78-95 (`_REQUIRED_PANEL_PERMS` + `_PANEL_PERM_LABELS` — names Discord perms, A4)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md (Pattern 3 — the summon split: module owns the ordering, app owns channel-read/idle-embed/copy/factory; Pitfall 5 — the dep-pin/two-package-wheel)
    - pyproject.toml L10 (`discord.py>=2.7.1,<3`), L26-27 (`packages = ["weatherbot", "yahir_reusable_bot"]` two-package wheel)
  </read_first>
  <action>
    Create `yahir_reusable_bot/discord/gateway.py`. Relocate `BotThread`, `build_client`, and the summon orchestration VERBATIM in behavior, cutting along the generic/app seam (D-06, RESEARCH Pattern 3):

    `BotThread` (relocate verbatim, L612-709): own thread+loop via `asyncio.run(client.start(token))` (NOT `Client.run`), `start()` waits on `_loop_started`, `stop()` schedules `client.close()` cross-thread + joins, `_run` swallows `LoginFailure`/any crash and sets `_failed` (failure isolation D-11). Names no weather — relocate as-is. The `operator_id` it threads stays baked-at-construction (preserve, do NOT lift — DEFERRED idea).

    `build_client` (relocate verbatim, L536-609): the `Intents.none()` + 3-intent setup, the on_ready `message_content` startup assertion. In `setup_hook`, ELIMINATE the deferred `from weatherbot.interactive.panel import PanelView` (L583): instead the app constructs the `PanelKit` (render + contributors + marker injected) and hands it (or a factory) to `build_client`, so `setup_hook` calls `client.add_view(panelkit)` with NO `weatherbot` import. Keep the D-13 reason verbatim (setup_hook runs once pre-connect, NOT on_ready).

    Summon orchestration: expose the generic orchestration as a function/method the app's thin summon calls. MODULE-side (generic): the pin-scan, the create-before-delete ordering (channel.send + msg.pin() FIRST, then delete prior owned, no zero-panel window — L368-390), the ownership predicate (marker-bound, reuse the panelkit ownership test), the per-write `discord.Forbidden` backstop, the permission preflight loop over a passed-in required-perm set. You MAY relocate `_REQUIRED_PANEL_PERMS` (L78-84) here (names Discord perms, not weather — A4). APP-side (the app passes IN — do NOT relocate): the `holder.current().bot.panel_channel_id` read + channel resolution (incl. the `hasattr(channel, "pins")` duck-type), ALL operator-feedback copy strings (L105-136 — name `[bot] panel_channel_id`, weather config concern), the idle embed (built via the injected render), the panel-factory. The summon function signature takes: resolved channel, required-perm set, the owned-panel predicate (marker-bound), the panel-factory callable, and the idle embed.

    Add `from yahir_reusable_bot.discord.gateway import BotThread, build_client` to `__init__.py`; finalize `__all__ = ["BotThread", "build_client", "PanelKit", "SelectedContext"]`.

    PIN (D-05): tighten `pyproject.toml` L10 `discord.py>=2.7.1,<3` to `discord.py==2.7.1`. Add an inline comment that this pin is owned by the adapter and moves to the module's own pyproject at the Phase-28 physical split. The `[tool.hatch...packages]` already lists `yahir_reusable_bot`; the new `discord/` subpackage rides it (hatchling recurses — no new packages entry). Regenerate `uv.lock` with `uv lock` and confirm it constrains discord-py to exactly 2.7.1.

    IMPORT no `weatherbot.*` in gateway.py — none, top-level or in-function.
  </action>
  <verify>
    <automated>uv run python -c "from yahir_reusable_bot.discord import BotThread, build_client, PanelKit, SelectedContext; src=open('yahir_reusable_bot/discord/gateway.py').read(); assert 'weatherbot' not in src; assert 'PanelView' not in src; print('OK')" && grep -q 'discord.py==2.7.1' pyproject.toml && echo PIN-OK</automated>
  </verify>
  <acceptance_criteria>
    - `from yahir_reusable_bot.discord import BotThread, build_client, PanelKit, SelectedContext` imports clean.
    - `grep -nE 'from weatherbot|import weatherbot|PanelView' yahir_reusable_bot/discord/gateway.py` returns NOTHING (no app import, no deferred PanelView edge).
    - `grep -niE 'weather|forecast|location|openweather|\buv\b|briefing' yahir_reusable_bot/discord/gateway.py` returns NOTHING (litmus-clean; channel-read/copy/idle-embed stay app-side).
    - `pyproject.toml` contains `discord.py==2.7.1` (exact) and no `>=2.7.1,<3` range.
    - `grep -E 'discord-py' uv.lock` shows version `2.7.1`.
    - `uv run pytest tests/test_import_hygiene.py -q` passes (the grimp gate auto-scales to the new `discord/` package and finds NO module-to-app edge).
  </acceptance_criteria>
  <done>BotThread + build_client + summon orchestration live in the module, name no weather, carry no PanelView import, the public barrel exports all four symbols, and discord.py is pinned ==2.7.1 with uv.lock regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user/bot tap → PanelKit interaction_check | Untrusted interaction author crosses the operator gate |
| Discord gateway callback → bot internals | A raising callback must not crash the gateway loop / scheduler thread |
| Bot logs / user replies | Secrets (token, appid) must never cross into a log or reply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Elevation of Privilege | `PanelKit.interaction_check` (operator gate) | mitigate | Relocate the gate VERBATIM — `author.id != operator_id` rejects; identity-free ephemeral reject copy (D-12, no interpolation of user/custom_id/command/operator); bot author → reject with NO ephemeral (WR-03 asymmetry). `operator_id` baked at construction (preserve v1). Pinned by `test_panel.py` operator-gate tests. |
| T-27-02 | Denial of Service | per-callback envelope + `View.on_error` + `BotThread._run` | mitigate | Relocate the non-propagating failure-isolation envelope + `_safe_error_edit` (never re-raises) + `_run` swallow-and-log VERBATIM (D-11). A raising callback is contained; the gateway thread + scheduler survive. |
| T-27-03 | Information Disclosure | reject log + summon logs + embed copy | mitigate | Relocate secret-free logging VERBATIM — only non-secret ids (channel_id, user_id, custom_id) reach logs; `panel_channel_id` is a non-secret id; the bot token / OpenWeather appid are never threaded into PanelKit/views. |
| T-27-04 | Tampering | live panel `custom_id` routing | mitigate | Freeze the `custom_id` byte strings (D-04 — `wb:` marker app-supplied, asserted by the byte-string golden in Plan 03) + pin `discord.py==2.7.1` (D-05) so the already-registered live panel keeps routing. |
| T-27-05 | Tampering | panel ownership (`_is_owned_panel`) | mitigate | Relocate the author + app-supplied marker ownership test VERBATIM (marker parameterized) — only the bot's own children carry the unforgeable `wb:` marker; a foreign message cannot be mistaken for the panel. |
| T-27-SC | Tampering | uv/pip installs | mitigate | No new package installed — the only dep change tightens an already-present, already-locked constraint (`discord.py` range → `==2.7.1`). RESEARCH Package Legitimacy Audit: discord.py OK (mature, github.com/Rapptz/discord.py), no new install. |
</threat_model>

<verification>
- `uv run python -c "from yahir_reusable_bot.discord import BotThread, build_client, PanelKit, SelectedContext"` imports clean.
- `uv run pytest tests/test_import_hygiene.py -q` passes — grimp finds NO `yahir_reusable_bot.discord` → `weatherbot` edge.
- `grep -rniE 'weather|forecast|location|openweather|\buv\b|briefing' yahir_reusable_bot/discord/` returns NOTHING (litmus-clean).
- `grep -rc 'wb:' yahir_reusable_bot/discord/` returns 0 for every file.
- `pyproject.toml` pins `discord.py==2.7.1`; `uv.lock` resolves discord-py to 2.7.1.
</verification>

<success_criteria>
- The 4 new module files exist, import clean, name no weather concept, and carry no `wb:` literal.
- `PanelKit.__init__` requires `render`, `contributors`, `marker` (no defaults).
- `SelectedContext[I]` is generic with an unbound TypeVar and no base class.
- The grimp import gate (test_import_hygiene.py) passes with the new adapter edges.
- `discord.py==2.7.1` pinned; `uv.lock` regenerated.
</success_criteria>

<output>
Create `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-01-SUMMARY.md` when done.
</output>
