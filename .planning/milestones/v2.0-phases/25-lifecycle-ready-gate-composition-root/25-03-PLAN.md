---
phase: 25-lifecycle-ready-gate-composition-root
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - tests/test_import_hygiene.py
  - tests/test_injection_registry.py
  - .planning/phases/25-lifecycle-ready-gate-composition-root/25-SELF-UAT.md
autonomous: true
requirements: [SEAM-05, APP-01, APP-02]
must_haves:
  truths:
    - "The 3-gate litmus (grimp + isolated-import + AST noun scan over yahir_reusable_bot/**) stays green with the D-13 term set UNCHANGED, now covering the new lifecycle edges."
    - "A positive injection-registry test asserts all four leak points (selected-location context, the config id-deriver/exactly-once key, the health-check, render_embed/panel cosmetics) are injected at the single root with no module-side baked default."
    - "READY=1 ordering is proven: the gate's online path emits READY strictly after the probe passes and scheduler.start() — verified via NOTIFY_SOCKET capture or a driven gate path."
    - "The full suite + Phase-21 goldens are green with ZERO non-empty snapshot diff (BHV-01/BHV-02), confirmed byte-level in a persistent self-UAT log."
  artifacts:
    - path: "tests/test_injection_registry.py"
      provides: "positive proof the four leak points are injected-at-root, not baked"
      contains: "build_runtime"
    - path: "tests/test_import_hygiene.py"
      provides: "the 3-gate litmus re-run over the grown module (new lifecycle edges)"
      contains: "_LITMUS"
    - path: ".planning/phases/25-lifecycle-ready-gate-composition-root/25-SELF-UAT.md"
      provides: "persistent Gate-1 self-UAT log (commands + byte-level evidence + per-criterion verdict)"
      contains: "PASS"
  key_links:
    - from: "tests/test_injection_registry.py"
      to: "weatherbot/scheduler/wiring.py"
      via: "asserts build_runtime injects all four leak points at the single root"
      pattern: "build_runtime"
    - from: "tests/test_import_hygiene.py"
      to: "yahir_reusable_bot/lifecycle"
      via: "grimp + isolated-import gates auto-scale to the new lifecycle module"
      pattern: "yahir_reusable_bot"
---

<objective>
Prove APP-02's BOTH halves and run the autonomous Gate-1 self-UAT. The existing 3-gate litmus proves
"no weather noun in the module"; ADD a positive injection-registry test proving "injected, not baked"
(D-05). Then drive the live READY-gate behavior and the byte-identical oracle, writing a persistent
self-UAT log that phase completion hinges on.

Purpose: turn APP-02 from "no weather noun" into "no weather noun AND wired-from-app", and discharge
the project's Gate-1 self-UAT policy for the device-verifiable success criterion (READY reaches
systemd only after the app probe passes) with byte-level evidence.

Output: extended `test_import_hygiene.py`, a new `test_injection_registry.py`, and `25-SELF-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-CONTEXT.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-PATTERNS.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-01-SUMMARY.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols/files created by this plan (drift-verification excludes them):
- `tests/test_injection_registry.py` — the positive "injected-at-root, not baked" proof (D-05)
- the new injection-registry assertion helpers + their self-proof (mirroring the _injected_app_leak discipline)
- `.planning/phases/25-lifecycle-ready-gate-composition-root/25-SELF-UAT.md` — the persistent Gate-1 self-UAT log
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the litmus + add the positive injection-registry test (D-05)</name>
  <files>tests/test_import_hygiene.py, tests/test_injection_registry.py</files>
  <read_first>
    - tests/test_import_hygiene.py (the WHOLE file — the 3-gate litmus + the _injected_app_leak / _scan_app_leaks / _public_names self-proof discipline to mirror; _LITMUS at L61 stays UNCHANGED)
    - weatherbot/scheduler/wiring.py (build_runtime — the single root the injection-registry test inspects for the four injected args)
    - weatherbot/interactive/panel.py L290-323, L481-512 (_selected_location — leak point 1, selected-location context)
    - weatherbot/weather/store.py (the UNIQUE(location_name, send_time, local_date) exactly-once key + _desired_job_ids — leak point 2, the config id-deriver)
    - weatherbot/ops/selfcheck.py (run_self_check — leak point 3, the health-check)
    - weatherbot/interactive/bot.py L194 (render_embed — leak point 4, panel cosmetics)
  </read_first>
  <action>
    Two additive changes, NO production behavior change and NO broadening of the locked _LITMUS term set
    (L61 — generic seam names SelectedContext/health/embed-render are exactly what the module exposes).

    (a) `tests/test_import_hygiene.py`: confirm the grimp + isolated-import gates auto-scale to the new
    `lifecycle` edges (the `_scan_app_leaks` prefix check + `walk_packages` pick them up with no per-module
    edit). Add a one-line assertion or comment documenting the lifecycle module is now in scope. Re-run
    `test_litmus_clean` to confirm the relocated lifecycle code is weather-noun-free (the gate must catch
    any `weatherbot online` event key, `is_weatherbot_pid` name, or `auth_failed` comparison that
    accidentally moved into the module — if it does, that is a real defect to fix in 25-01/25-02, not a
    test to weaken).

    (b) `tests/test_injection_registry.py` (new): the POSITIVE half of APP-02. Assert that each of the
    four leak points is supplied AS AN INJECTED ARG at the single root (`build_runtime`) with NO
    module-side baked default. Approach: introspect `build_runtime`'s constructed ReadyGate / panel /
    reload-engine wiring (or call build_runtime with recording fakes) and assert (1) the health_check is
    a closure the app passed (the module's ReadyGate has no default probe — constructing ReadyGate
    without a health_check is a TypeError / there is no module-side weather probe); (2) the exactly-once
    id-deriver / config key is supplied by the app (desired_jobs closure), not named in the module;
    (3) the selected-location context originates app-side; (4) render_embed is injected app-side (the
    module owns no render). Mirror the self-proof discipline: pair each positive assertion with a
    deliberately-broken variant (e.g. a stub that bakes a default) that the test PROVES would trip, so a
    green run proves the assertion bites. Keep the assertions structural (no weather noun added to the
    module surface).
  </action>
  <verify>
    <automated>pytest -q tests/test_import_hygiene.py tests/test_injection_registry.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/test_import_hygiene.py tests/test_injection_registry.py` exits 0.
    - The `_LITMUS` pattern at L61 is UNCHANGED (the D-13 locked term set is not broadened).
    - `test_litmus_clean` passes over the grown module (new lifecycle edges, no weather noun).
    - The injection-registry test has at least four positive assertions (one per leak point) each paired with a self-proof that a baked default would trip it.
  </acceptance_criteria>
  <done>
    The litmus re-runs green over the grown module with the term set unchanged, and a positive
    injection-registry test proves all four leak points are injected-at-root with no module-side baked
    default — APP-02's second half is proven.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate-1 autonomous self-UAT — drive the READY-gate + byte-identical oracle, write the persistent log</name>
  <files>.planning/phases/25-lifecycle-ready-gate-composition-root/25-SELF-UAT.md</files>
  <read_first>
    - .planning/phases/24-config-hot-reload-engine/24-03-SUMMARY.md (the Phase-24 SEAM-04 Gate-1 self-UAT precedent — the byte-level diff-against-baseline evidence format to mirror)
    - .planning/phases/21-characterization-golden-test-harness/21-CONTEXT.md (the golden oracle: schedule plan, reconcile-diff, keep-old, exactly-once, sent_log DB rows — the snapshots to diff)
    - weatherbot/scheduler/daemon.py (run_daemon — the gate→start→READY ordering to drive)
    - weatherbot/ops/sdnotify.py + yahir_reusable_bot/lifecycle/sdnotify.py (SystemdNotifier — set NOTIFY_SOCKET to a captured AF_UNIX/SOCK_DGRAM socket to observe READY=1 ordering)
    - CLAUDE.md (the Two-Gate UAT policy — Gate-1 is autonomous and gates the phase; the live reboot physical step is a deferred Gate-2 obligation)
  </read_first>
  <action>
    Run the autonomous Gate-1 self-UAT and write a persistent log at
    `.planning/phases/25-lifecycle-ready-gate-composition-root/25-SELF-UAT.md`. Per the project Two-Gate
    UAT policy, do NOT stop at green unit tests — DRIVE the behavior and prove values byte-level. For
    EACH success criterion, record: what was tested, the EXACT commands, the evidence/output, and a
    PASS/FAIL/PARTIAL verdict.

    Criteria to discharge:
    1. READY=1 reaches systemd ONLY after the app probe passes. DRIVE it: set `NOTIFY_SOCKET` to a
       captured `AF_UNIX`/`SOCK_DGRAM` datagram socket and run the gate path with (a) a probe that fails
       then passes, asserting NO `READY=1` datagram arrives until the passing probe AND after
       scheduler.start(); and (b) a stop set mid-probe, asserting NO READY=1 is ever sent and the gate
       returns 0 cleanly. Capture the received datagram bytes (`b"READY=1"`) + the call ordering as
       evidence. (This drives the live mechanism on a real socket — the only physical step left is an
       actual host reboot, recorded as deferred Gate-2.)
    2. Single composition root, zero duplicated module mechanism. Evidence: `pytest -q` full suite exit 0
       + the injection-registry test green + a grep showing build_runtime is the one wiring site.
    3. The four leak points injected, not baked (litmus clean). Evidence: `pytest -q tests/test_import_hygiene.py`
       + the litmus grep over `yahir_reusable_bot/**` returning only incidental/prose hits.
    4. Byte-identical oracle. Evidence: run the FULL suite + the Phase-21 golden tests and confirm ZERO
       non-empty snapshot diff against the committed baseline (mirror 24-03's diff-against-baseline
       method: no golden file is modified; any diff is investigated, never rubber-stamped). Record the
       exact golden-test command + the empty-diff result.

    Record the live `yahir-mint` host reboot / `systemctl restart` confirmation as a DEFERRED Gate-2
    milestone obligation (verdict PARTIAL for the physical reboot step ONLY — the mechanism + result are
    PASS via the driven socket capture). Phase completion hinges on a fully-PASS Gate-1 for criteria
    1-4 (mechanism level); the physical reboot is the only PARTIAL.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-lifecycle-ready-gate-composition-root/25-SELF-UAT.md && grep -q "PASS" .planning/phases/25-lifecycle-ready-gate-composition-root/25-SELF-UAT.md && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `25-SELF-UAT.md` exists with a per-criterion entry (what tested / exact commands / evidence / verdict).
    - Criterion 1 records a byte-level READY=1 socket capture proving the ordering (no READY before probe-pass + start; no READY on stop-preempt).
    - Criterion 4 records ZERO non-empty Phase-21 golden snapshot diff against baseline, with no golden file edited.
    - The full suite `pytest -q` exits 0.
    - The live host reboot is recorded as a deferred Gate-2 obligation (PARTIAL for the physical step only).
  </acceptance_criteria>
  <done>
    A persistent Gate-1 self-UAT log records, per success criterion, byte-level evidence (READY=1 socket
    capture, empty golden diff, green suite, clean litmus) with PASS verdicts for criteria 1-4 at the
    mechanism level and a single deferred-Gate-2 PARTIAL for the physical host reboot — phase completion
    hinges on it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness ↔ NOTIFY_SOCKET | the self-UAT binds a captured datagram socket to observe READY=1 ordering |
| module surface ↔ litmus gate | the standing negative gate + the new positive injection proof guard the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Repudiation | self-UAT evidence | mitigate | persistent log records exact commands + byte-level output per criterion (auditable; mirrors 24-03) |
| T-25-08 | Tampering | golden snapshots | mitigate | diff-against-baseline only; no golden file edited; any non-empty diff investigated, never rubber-stamped |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies — tests use stdlib socket + existing pytest/grimp |
</threat_model>

<verification>
- `pytest -q tests/test_import_hygiene.py tests/test_injection_registry.py` exits 0.
- `_LITMUS` term set unchanged (D-13 lock honored).
- `25-SELF-UAT.md` exists with byte-level READY=1 ordering evidence + empty golden diff + green suite.
- The full suite `pytest -q` exits 0 (BHV-01); Phase-21 goldens green with zero diff (BHV-02).
</verification>

<success_criteria>
APP-02 is proven on BOTH halves (negative litmus clean + positive injection-registry green, term set
unchanged). The Gate-1 self-UAT log proves byte-level that READY=1 reaches systemd only after the app
probe passes and scheduler.start(), that the four leak points are injected-at-root, and that the
Phase-21 goldens + full suite stay green with zero snapshot diff. The live host reboot is the only
deferred Gate-2 obligation.
</success_criteria>

<output>
Create `.planning/phases/25-lifecycle-ready-gate-composition-root/25-03-SUMMARY.md` when done.
Record: the READY=1 ordering capture method, the golden-diff result, and the deferred Gate-2 item.
</output>
