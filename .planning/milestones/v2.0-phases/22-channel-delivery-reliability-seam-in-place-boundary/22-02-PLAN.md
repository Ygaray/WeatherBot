---
phase: 22-channel-delivery-reliability-seam-in-place-boundary
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - yahir_reusable_bot/channels/base.py
  - yahir_reusable_bot/channels/__init__.py
  - weatherbot/channels/base.py
  - weatherbot/channels/__init__.py
  - weatherbot/channels/discord.py
  - weatherbot/scheduler/daemon.py
  - weatherbot/scheduler/uvmonitor.py
  - weatherbot/cli.py
autonomous: true
requirements: [SEAM-01, PKG-01]
must_haves:
  truths:
    - "The text-only `Channel` ABC (`send(text) -> DeliveryResult`) + `DeliveryResult` live in `yahir_reusable_bot.channels`"
    - "The module `Channel` has NO `send_briefing` and NO `Forecast` import (D-03)"
    - "Exactly ONE `Channel` class exists; `DiscordWebhookChannel` subclasses the module's `Channel` and `isinstance(ch, Channel)` holds"
    - "`weatherbot.channels` still exports `Channel`/`DeliveryResult` with `send_briefing` reachable app-side, so `send_now` and the suite stay byte-identical"
    - "The import-graph gate is green: zero `yahir_reusable_bot.channels.* -> weatherbot.*` edges"
    - "Full 732-test suite green with zero golden snapshot diff"
  artifacts:
    - path: "yahir_reusable_bot/channels/base.py"
      provides: "The one true `Channel` ABC (send-only) + `DeliveryResult` (D-03)"
      contains: "class Channel"
      min_lines: 20
    - path: "weatherbot/channels/base.py"
      provides: "App-side shim: re-exports module Channel/DeliveryResult + the app-side send_briefing default + Forecast import"
      contains: "send_briefing"
  key_links:
    - from: "weatherbot/channels/__init__.py"
      to: "yahir_reusable_bot/channels/__init__.py"
      via: "re-export of Channel + DeliveryResult from the module (one Channel class)"
      pattern: "from yahir_reusable_bot.channels import"
    - from: "weatherbot/channels/discord.py"
      to: "yahir_reusable_bot/channels/base.py"
      via: "DiscordWebhookChannel subclasses the app-side briefing-capable Channel (which IS-A module Channel)"
      pattern: "class DiscordWebhookChannel"
---

<objective>
Move the clean, text-only `Channel` abstraction + `DeliveryResult` out of `weatherbot/channels/
base.py` into the new `yahir_reusable_bot.channels` module (D-03), dropping `send_briefing` and
the `TYPE_CHECKING` `Forecast` import from the module abstraction — the single fix that satisfies
all three gates at once (D-12: the litmus `send_briefing`/`forecast: Forecast` signature hit
disappears, and the cross-package `weatherbot.weather.models` import edge disappears). Keep the concrete
`DiscordWebhookChannel` + its embed app-side (Phase 27 is its named home, D-04/D-05). Re-home the
app-side `send_briefing` default so `cli.py:send_now` and every existing test stay byte-identical.

Purpose: SEAM-01 (channel-agnostic `Channel` with zero weather coupling) + PKG-01 (module imports
zero app code). This is the lowest-risk seam — the existing `Channel` ABC is *already* almost the
clean seam; only `send_briefing` + the `Forecast` import need to leave.

Output: `yahir_reusable_bot/channels/base.py` (the one true `Channel`), re-export shims that keep
`weatherbot.channels` byte-identical for all importers, the app-side `send_briefing` re-home, and
re-pointed `weatherbot.channels.base` importers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-CONTEXT.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-RESEARCH.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-PATTERNS.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-VALIDATION.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-01-SUMMARY.md

# The exact source moving (read it — the move is verbatim minus two deletions)
@weatherbot/channels/base.py
@weatherbot/channels/__init__.py
@weatherbot/channels/discord.py

# The tests that LOCK the re-home shape (read these — they dictate (a) not (b))
@tests/test_channel.py
</context>

<artifacts_this_phase_produces>
- **`yahir_reusable_bot/channels/base.py`** — the ONE true `Channel` ABC (`send(text) ->
  DeliveryResult` only) + the `DeliveryResult` dataclass, moved verbatim from
  `weatherbot/channels/base.py` minus `send_briefing` (lines 52-61) and minus the
  `if TYPE_CHECKING: from weatherbot.weather.models import Forecast` block (lines 17-20).
- **`yahir_reusable_bot/channels/__init__.py`** — exports `Channel`, `DeliveryResult` (SUBSET —
  `DiscordWebhookChannel`/`build_channel` stay app-side, D-04).
- **App-side `send_briefing` re-home** — a briefing-capable `Channel` surface in `weatherbot`
  (re-adds the default `send_briefing(text, forecast) -> self.send(text)` + the `Forecast`
  TYPE_CHECKING import, APP-side so it does NOT re-introduce a module→app edge).
- **Phase-27 hand-off note** (D-05) recorded in the SUMMARY: the temporary two-home split (clean
  `Channel` in the module; concrete `DiscordWebhookChannel` + embed still app-side) is an
  intentional, named Phase-27 obligation, NOT an incomplete extraction.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move Channel + DeliveryResult into yahir_reusable_bot.channels (minus send_briefing + Forecast)</name>
  <read_first>
    - `weatherbot/channels/base.py` (WHOLE file — `DeliveryResult` lines 23-34, `Channel` ABC
      lines 37-50 KEEP verbatim; DELETE the `Forecast` import lines 17-20 and `send_briefing`
      lines 52-61)
    - 22-PATTERNS.md § "yahir_reusable_bot/channels/base.py" (the exact two deletions) and §
      "yahir_reusable_bot/channels/__init__.py" (the subset export)
    - 22-RESEARCH.md Pattern 2 + Pitfall 1 + Pitfall 3
  </read_first>
  <files>
    yahir_reusable_bot/channels/base.py,
    yahir_reusable_bot/channels/__init__.py
  </files>
  <action>
    Create `yahir_reusable_bot/channels/base.py` by copying `DeliveryResult` (the dataclass with
    `ok: bool` + `detail: str = ""`) and the `Channel` ABC (the `name: str = "channel"` class attr
    + the single `@abstractmethod def send(self, text: str) -> DeliveryResult`) VERBATIM from
    `weatherbot/channels/base.py`. Make exactly TWO deletions relative to the source: do NOT carry
    the `if TYPE_CHECKING: from weatherbot.weather.models import Forecast` block (this is the only
    module→app edge — its deletion is what makes the grimp gate pass), and do NOT carry the
    `send_briefing` method (D-03 — its deletion is what makes the litmus gate pass). Keep the
    `from __future__ import annotations`, `from abc import ABC, abstractmethod`, and
    `from dataclasses import dataclass` imports. The module docstring may keep describing the
    channel-agnostic seam in PROSE (the AST litmus ignores docstrings, D-11) but must NOT name a
    weather noun in any `def`/`class`/param/annotation. After this file, `Channel` is exactly
    `send(text) -> DeliveryResult` — the canonical text path SMS/Telegram reuse.

    Update `yahir_reusable_bot/channels/__init__.py` to `from .base import Channel, DeliveryResult`
    and set `__all__ = ["Channel", "DeliveryResult"]` (SUBSET — the module package exports ONLY the
    clean two; `DiscordWebhookChannel` + `build_channel` stay app-side per D-04).

    CRITICAL (Pitfall 3): this is now the ONLY `Channel` class definition in the codebase. The next
    task ensures `weatherbot/channels/base.py` no longer DEFINES a second `Channel` (it becomes a
    shim re-exporting THIS one).
  </action>
  <verify>
    <automated>uv run python -c "from yahir_reusable_bot.channels import Channel, DeliveryResult; import inspect; assert not hasattr(Channel, 'send_briefing'), 'module Channel must NOT have send_briefing'; assert inspect.isabstract(Channel); src=inspect.getsource(__import__('yahir_reusable_bot.channels.base', fromlist=['x'])); assert 'Forecast' not in src and 'weatherbot' not in src, 'module base.py must not reference Forecast/weatherbot'; print('module Channel clean')"</automated>
  </verify>
  <acceptance_criteria>
    - `yahir_reusable_bot.channels.Channel` is abstract, has `send` but NOT `send_briefing`.
    - `yahir_reusable_bot/channels/base.py` source contains neither `Forecast` nor `weatherbot`.
    - `yahir_reusable_bot.channels.__all__ == ["Channel", "DeliveryResult"]`.
  </acceptance_criteria>
  <done>
    The one true text-only `Channel` + `DeliveryResult` live in the module with zero weather
    coupling, verified by the introspection command.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shim weatherbot.channels, re-home send_briefing app-side, re-point base.py importers</name>
  <read_first>
    - `weatherbot/channels/__init__.py` (current lines 8-17 — the re-export surface to keep stable)
    - `weatherbot/channels/discord.py` (lines 26 `from .base import Channel, DeliveryResult`; line
      39 `class DiscordWebhookChannel(Channel)`; lines 54-70 the embed `send_briefing` override)
    - `tests/test_channel.py` lines 119-144 — `test_base_send_briefing_defaults_to_send_text`
      subclasses `Channel` (from `weatherbot.channels`) and calls `.send_briefing(...)`, AND
      `test_base_module_has_no_embed_reference` does `import weatherbot.channels.base as base_mod`
      and asserts no `DiscordEmbed` — so `weatherbot/channels/base.py` MUST stay importable AND
      the re-exported `Channel` MUST carry `send_briefing`
    - The four direct `weatherbot.channels.base` importers: `weatherbot/cli.py:57`,
      `weatherbot/scheduler/daemon.py:82`, `weatherbot/scheduler/uvmonitor.py:44`,
      `tests/test_reliability.py:35`, `tests/test_config_holder.py:27` (tests import names that
      still resolve through the shim — verify, don't necessarily edit them)
    - 22-RESEARCH.md Pattern 1, Pattern 2 (shape (a) lower-risk), Pitfall 2, Pitfall 3
    - 22-PATTERNS.md § "weatherbot/channels/__init__.py (MODIFIED)" incl. the cross-file caveat
  </read_first>
  <files>
    weatherbot/channels/base.py,
    weatherbot/channels/__init__.py,
    weatherbot/channels/discord.py,
    weatherbot/scheduler/daemon.py,
    weatherbot/scheduler/uvmonitor.py,
    weatherbot/cli.py
  </files>
  <behavior>
    - `from weatherbot.channels import Channel, DeliveryResult, DiscordWebhookChannel,
      build_channel` resolves exactly as before (test_channel.py:22).
    - `isinstance(DiscordWebhookChannel(...), Channel)` is True (test_channel.py:84) — exactly one
      module `Channel` underneath.
    - A non-Discord channel subclassing the `weatherbot.channels.Channel` symbol gets a
      `send_briefing(text, forecast)` default that delegates to `send(text)`
      (test_channel.py:119-137).
    - `import weatherbot.channels.base as base_mod` still works and its source has no `DiscordEmbed`
      (test_channel.py:140-144).
    - `weatherbot/cli.py:159` `channel.send_briefing(result_lr.text, result_lr.forecast)` stays
      byte-identical (test_send_now.py, test_cli.py).
  </behavior>
  <action>
    Adopt RESEARCH Pattern 2 shape (a) — the lower-risk byte-identical choice that
    `test_base_send_briefing_defaults_to_send_text` FORCES (a non-Discord channel subclassing the
    re-exported `Channel` must get `send_briefing`):

    Rewrite `weatherbot/channels/base.py` as a thin APP-side shim: import `Channel as _BaseChannel`
    and `DeliveryResult` from `yahir_reusable_bot.channels`, re-export `DeliveryResult` unchanged,
    and define a briefing-capable subclass that re-adds the default `send_briefing(self, text,
    forecast) -> DeliveryResult` (delegating to `self.send(text)`) plus the `if TYPE_CHECKING: from
    weatherbot.weather.models import Forecast` annotation import — this `Forecast` import lives
    APP-side so it does NOT re-introduce a module→app edge. Export this briefing-capable class under
    the name `Channel` so every existing `from weatherbot.channels(.base) import Channel` importer
    and `isinstance(ch, Channel)` keep working AND the subclass carries `send_briefing`. Because
    this app-side `Channel` IS-A `yahir_reusable_bot.channels.Channel`, there is still exactly ONE
    abstract `Channel` in the module (Pitfall 3 satisfied — `DiscordWebhookChannel` IS-A both).
    Keep `weatherbot/channels/base.py` free of any `DiscordEmbed` reference (test asserts this).

    Update `weatherbot/channels/__init__.py` to re-export `Channel`/`DeliveryResult` from the
    app-side `base` shim (Pattern 1) and keep `from .discord import DiscordWebhookChannel` +
    `from .factory import build_channel`; leave `__all__` (the four names) unchanged.

    Re-point `weatherbot/channels/discord.py:26` so `DiscordWebhookChannel` subclasses the
    briefing-capable app-side `Channel` (it must inherit `send_briefing`'s contract and override it
    with the embed build at lines 54-70 — unchanged). Keep its `from weatherbot.branding import
    BRIEFING_COLOR_HEX` and the `Forecast` TYPE_CHECKING import (legit app-side coupling, D-04).

    Verify (and only re-point if the symbol no longer resolves) the other direct
    `weatherbot.channels.base` importers — `cli.py:57`, `daemon.py:82` (both under TYPE_CHECKING),
    `uvmonitor.py:44`. Because the shim keeps `Channel`/`DeliveryResult` importable FROM
    `weatherbot.channels.base`, these should need NO change — confirm with the verify command;
    re-point to `from weatherbot.channels import ...` only if a name fails to resolve. Do NOT
    rewrite `fire_slot` or touch any reliability/alert code (that is Plan 03). Do NOT pull the
    Discord embed app-side-relocation forward (Phase 27, D-04). `cli.py:159` send_briefing dispatch
    stays EXACTLY as-is.
  </action>
  <verify>
    <automated>uv run pytest tests/test_channel.py tests/test_send_now.py tests/test_import_hygiene.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_channel.py` green: `isinstance(ch, Channel)`, the base `send_briefing` default, and
      `test_base_module_has_no_embed_reference` all pass.
    - `test_send_now.py` green: `send_now` dispatch byte-identical.
    - `test_import_hygiene.py` green: the grimp gate reports zero `yahir_reusable_bot.channels.* ->
      weatherbot.*` edges (the `Forecast` edge is gone); the litmus reports zero weather noun in
      the module surface.
    - `weatherbot/channels/base.py` is still importable and contains no `DiscordEmbed`.
  </acceptance_criteria>
  <done>
    `weatherbot.channels` is byte-identical for all importers, `send_briefing` is re-homed app-side
    via shape (a), exactly one module `Channel` exists, and the import-hygiene gate is green for the
    channels seam.
  </done>
</task>

<task type="auto">
  <name>Task 3: Self-UAT — full oracle suite byte-identical green; record the Phase-27 two-home hand-off</name>
  <read_first>
    - 22-VALIDATION.md § "Sampling Rate" (per-wave: full suite + zero golden diff) and § "What
      'fully covered' means" (byte-identical oracle: zero diff across ALL `tests/__snapshots__/`)
    - D-05 (the temporary two-home split is a NAMED Phase-27 hand-off, not an incomplete extraction)
    - The user CLAUDE.md Two-Gate UAT policy (autonomous self-UAT log; no per-phase human pause)
  </read_first>
  <files>.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-02-SELFUAT.md</files>
  <action>
    Run the FULL suite (`uv run pytest`) and confirm 732 tests pass with ZERO golden snapshot diff.
    The channels move is a relocation; the embed/CLI/schedule/DB/custom_id goldens must be
    byte-unchanged. Write a persistent self-UAT log recording, per SEAM-01/PKG-01/BHV-01/BHV-02
    criterion: what was checked, the exact command, the observed PASS/FAIL/PARTIAL. In the same log
    (and surfaced into the SUMMARY for the next phase), explicitly RECORD the D-05 Phase-27
    hand-off obligation: the clean text-only `Channel` now lives in `yahir_reusable_bot.channels`,
    but the concrete `DiscordWebhookChannel` + its embed build are INTENTIONALLY still app-side and
    relocate in Phase 27 — this two-home split is by design, not an incomplete extraction. If ANY
    golden diff appears, HALT and investigate (never `--snapshot-update`, Phase-21 D-04).
  </action>
  <verify>
    <automated>uv run pytest -q && git -c core.pager=cat status --porcelain tests/__snapshots__/ | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (live count, ~732).
    - `git status --porcelain tests/__snapshots__/` empty (zero golden diff).
    - `22-02-SELFUAT.md` records per-criterion evidence AND the D-05 Phase-27 two-home hand-off.
  </acceptance_criteria>
  <done>
    The channel seam lands byte-identical with the full oracle suite green, zero golden diff, and
    the Phase-27 hand-off recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → module | `weatherbot.channels` imports `Channel`/`DeliveryResult` from `yahir_reusable_bot` (one-way; module never imports app) |
| credential → channel surface | The webhook URL lives in `DiscordWebhookChannel._url` (app-side, unmoved) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering (architecture drift) | `yahir_reusable_bot.channels` import surface | mitigate | The grimp gate (TYPE_CHECKING-inclusive) + isolated-import smoke fail loud on any new `channels.* -> weatherbot.*` edge — this IS the boundary-integrity control. Deleting the `Forecast` import (D-03) removes the one existing leak. |
| T-22-05 | Information Disclosure | webhook credential | accept | The credential lives in the UNMOVED app-side `DiscordWebhookChannel._url`; the module's `Channel`/`DeliveryResult` surface never references it (`DeliveryResult.detail` must never carry a credential — existing contract preserved). |
| T-22-06 | Tampering (silent wrong-class) | dual `Channel` definition | mitigate | Exactly ONE module `Channel`; the app-side briefing subclass IS-A it. `isinstance(ch, Channel)` (test_channel.py:84) is the loud regression guard against a leftover second class (Pitfall 3). |
</threat_model>

<verification>
- `uv run pytest tests/test_channel.py tests/test_send_now.py tests/test_import_hygiene.py -x` green.
- `uv run pytest` — full 732-test suite green.
- `git status --porcelain tests/__snapshots__/` empty (zero golden diff).
- grimp gate: zero `yahir_reusable_bot.channels.* -> weatherbot.*` edges; litmus: zero weather noun in module surface.
</verification>

<success_criteria>
- The text-only `Channel` + `DeliveryResult` live in `yahir_reusable_bot.channels` with no
  `send_briefing`/`Forecast` (D-03); exactly one module `Channel`.
- `weatherbot.channels` is byte-identical for all importers; `send_briefing` re-homed app-side (shape (a)).
- Import-hygiene gate green for the channels seam; full suite green; zero golden diff.
- The D-05 Phase-27 two-home hand-off recorded.
</success_criteria>

<output>
Create `.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-02-SUMMARY.md` when done.
</output>
