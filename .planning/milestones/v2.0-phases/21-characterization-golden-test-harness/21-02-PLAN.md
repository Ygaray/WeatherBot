---
phase: 21-characterization-golden-test-harness
plan: 02
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - tests/test_golden_embeds.py
  - tests/test_golden_custom_ids.py
  - tests/test_oracle_selfproof.py
  - tests/__snapshots__/test_golden_embeds
  - tests/__snapshots__/test_golden_custom_ids
autonomous: true
requirements: [BHV-02]
user_setup: []

must_haves:
  truths:
    - "Full rendered Discord embeds are pinned byte-exact per command (weather/uv/next-cloudy/sun/wind/status/alerts/forecast variants), 📍-on once via a location-bearing reply and 📍-off once via the argless status reply (D-10)"
    - "The Updated <t:{epoch}:t> (<t:{epoch}:R>) stamp is captured with its format string preserved and the epoch frozen (not scrubbed) (D-11)"
    - "Every panel custom_id byte string (incl. the wb: marker) is pinned — inline literal for the wb:loc:select marker plus the full ordered set as a raw-bytes golden (D-03/D-02)"
    - "A deliberate field-reorder of a real render and a custom_id byte-flip each make a comparison FAIL, wrapped in pytest.raises(AssertionError) — the standing oracle self-proof (D-12, SC2)"
  artifacts:
    - path: "tests/test_golden_embeds.py"
      provides: "Embed-per-command golden cases (JSONSnapshot via embed_to_golden, frozen clock)"
      contains: "embed_to_golden"
    - path: "tests/test_golden_custom_ids.py"
      provides: "wb: custom_id byte pins (inline + SingleFile)"
      contains: "wb:loc:select"
    - path: "tests/test_oracle_selfproof.py"
      provides: "D-12 perturbation meta-test (field-reorder + custom_id byte-flip must raise AssertionError)"
      contains: "pytest.raises"
  key_links:
    - from: "tests/test_golden_embeds.py"
      to: "weatherbot/interactive/bot.py"
      via: "render_embed / build_inbound_embed driven gateway-free, projected via embed_to_golden"
      pattern: "build_inbound_embed|render_embed"
    - from: "tests/test_golden_custom_ids.py"
      to: "weatherbot/interactive/panel.py"
      via: "PanelView.children custom_id read-off (wb:cmd:*, wb:loc:select, wb:fc:*)"
      pattern: "custom_id"
    - from: "tests/test_oracle_selfproof.py"
      to: "weatherbot/interactive/bot.py"
      via: "drives a REAL render then perturbs it — fails red if the comparison is ever loosened"
      pattern: "pytest.raises\\(AssertionError\\)"
---

<objective>
Pin the `interactive`-package render surfaces as byte-exact goldens: the full rendered Discord embeds
per command × 📍/Updated states, the panel `custom_id` byte strings, and the D-12 oracle self-proof
that proves the comparison actually has teeth (a field reorder / byte flip FAILS). These are the
load-bearing visual-output snapshots the later Discord-adapter extraction (Phase 27) re-runs.

Purpose: Embed field-order and `custom_id` bytes are exactly what a re-home can silently alter while
intent-level tests stay green. Order-preserving JSON + raw-bytes snapshots catch a reorder/flip; the
self-proof guarantees the oracle itself never degrades into an order-insensitive compare.

Output: `tests/test_golden_embeds.py`, `tests/test_golden_custom_ids.py`, `tests/test_oracle_selfproof.py`
+ their committed `tests/__snapshots__/` goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-characterization-golden-test-harness/21-CONTEXT.md
@.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md
@.planning/phases/21-characterization-golden-test-harness/21-01-SUMMARY.md

# Read-first analog seams:
@tests/test_panel.py
@weatherbot/interactive/bot.py
@weatherbot/interactive/panel.py
</context>

## Artifacts this phase produces (Plan 02)

- `tests/test_golden_embeds.py` — one named case per command (weather/uv/next-cloudy/sun/wind/status/
  alerts) + each forecast variant (weekday/weekend × detailed/compact); 📍-on (location reply) and
  📍-off (argless status) each covered once (D-10). Each: `assert embed_to_golden(embed) == json_snapshot`
  under `time_machine.travel(FROZEN)`.
- `tests/test_golden_custom_ids.py` — inline pin `view.children[0].custom_id == "wb:loc:select"` (D-03)
  + a full ordered-set raw-bytes golden `"\n".join(ids).encode() == bytes_snapshot(name="all_custom_ids")`.
- `tests/test_oracle_selfproof.py` — `test_field_reorder_is_caught` + `test_custom_id_byteflip_is_caught`,
  each driving a REAL render/custom_id then perturbing it inside `pytest.raises(AssertionError)`.
- `tests/__snapshots__/test_golden_embeds/*.json`, `tests/__snapshots__/test_golden_custom_ids/*` (committed).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Embed goldens — per command × 📍/Updated states</name>
  <files>tests/test_golden_embeds.py, tests/__snapshots__/test_golden_embeds</files>
  <read_first>
    - @tests/test_panel.py (`test_weather_spec_byte_identical` ~470-492 — the field-read idiom `[(f.name, f.value) for f in embed.fields]`; `_FakeHolder`/`_SpyCache`/`_make_panel` ~87-140)
    - @weatherbot/interactive/bot.py (`render_embed` ~194 with `location=` kwarg → 📍 line ~221-222; `build_inbound_embed` ~402; `Updated <t:…>` ~219-223)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "test_golden_embeds.py"
    - @.planning/phases/21-characterization-golden-test-harness/21-01-SUMMARY.md (frozen-epoch smoke result + syrupy call shape)
  </read_first>
  <behavior>
    - One named JSON-snapshot case per command (weather, uv, next-cloudy, sun, wind, status, alerts) and
      per forecast variant (weekday/weekend × detailed/compact) — representative-subset, one-per-cell (D-10).
    - 📍-on covered once via a location-bearing reply (`location="home"`); 📍-off once via the argless
      status reply (`location=None`) — the 📍 line is additive/orthogonal so no cartesian cross-multiply (D-10).
    - Each case runs under `time_machine.travel(FROZEN, tick=False)` so the `Updated <t:{epoch}:t> (<t:{epoch}:R>)`
      stamp is a frozen literal with its `:t`/`:R` format preserved (D-11). `embed.timestamp` is excluded
      by `embed_to_golden`.
  </behavior>
  <action>
    Create tests/test_golden_embeds.py. Drive `render_embed`/`build_inbound_embed` gateway-free using the
    `load_fixture` recorded `onecall_*` JSON + the `_FakeHolder`/`_SpyCache` stand-ins from test_panel.py
    (import or copy as test_panel.py does). For each command/variant, build the embed inside
    `time_machine.travel(FROZEN, tick=False)` and assert `embed_to_golden(embed) == json_snapshot`
    (the conftest helper + fixture from Plan 01). Name each test for its exact cell so a failure diff
    names the command (D-10). Cover 📍-on once (a location-bearing reply via `render_embed(reply, location="home")`)
    and 📍-off once (argless status via `location=None`). Use the recorded fixtures that already exist for
    each command (e.g. `onecall_metric_clear.json`, `onecall_imperial_alert.json`, `onecall_imperial_highuv.json`,
    `onecall_8day_*` for forecast variants) — reuse, never fetch network. Generate goldens with one
    `--snapshot-update` run, then review each committed `.json` once for secret hygiene before commit
    (V7 — no appid/webhook/token string). Touch NO `weatherbot/` source.
  </action>
  <verify>
    <automated>uv run pytest tests/test_golden_embeds.py -q</automated>
  </verify>
  <done>
    Each command + forecast variant has a named passing JSON-snapshot case; 📍-on and 📍-off each covered
    once; the `Updated <t:…:t> (<t:…:R>)` format string is present verbatim in at least one committed
    golden with a frozen epoch; goldens committed under `tests/__snapshots__/test_golden_embeds/`; no
    secret string in any golden.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_golden_embeds.py -q` passes with zero `--snapshot-update` needed on a second run.
    - `grep -rl ':R>' tests/__snapshots__/test_golden_embeds/` is non-empty (Updated format preserved, not scrubbed).
    - `grep -rEi 'appid|webhook|[A-Za-z0-9_-]{30,}' tests/__snapshots__/test_golden_embeds/` returns no real-secret hit (V7).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: custom_id byte pins — inline marker + full ordered set</name>
  <files>tests/test_golden_custom_ids.py, tests/__snapshots__/test_golden_custom_ids</files>
  <read_first>
    - @weatherbot/interactive/panel.py (`_PANEL_MARKER == "wb:"` ~141; `CmdButton custom_id=f"wb:cmd:{name}"` ~194; `LocationSelect custom_id="wb:loc:select"` ~263; forecast button ids `wb:fc:<day>:<variant>`)
    - @tests/test_panel.py (`_FC_SUBGRID_IDS` ~642-647; `_make_panel` ~134-140; child-walk `[c.custom_id for c in view.children]` ~438-445)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "test_golden_custom_ids.py"
  </read_first>
  <behavior>
    - The first panel child's custom_id is exactly `wb:loc:select` (inline literal pin, D-03).
    - The full ordered list of children custom_ids joined by newline is byte-identical to the committed
      raw-bytes golden — a single byte flip in any id fails (D-02 SingleFile).
    - The expected set: `wb:loc:select`, `wb:cmd:{weather,uv,next-cloudy,sun,wind,status,alerts}`,
      `wb:fc:weekday:detailed`, `wb:fc:weekday:compact`, `wb:fc:weekend:detailed`, `wb:fc:weekend:compact`.
  </behavior>
  <action>
    Create tests/test_golden_custom_ids.py. Build a `PanelView` gateway-free via the `_make_panel` /
    `_FakeHolder(["home","travel"])` / `_SpyCache` idiom from test_panel.py. Add an inline-literal test
    pinning `view.children[0].custom_id == "wb:loc:select"` (D-03). Add a raw-bytes golden test:
    `ids = [c.custom_id for c in view.children]; assert "\n".join(ids).encode() == bytes_snapshot(name="all_custom_ids")`
    (D-02, confirm the `bytes_snapshot(name=...)` call form against installed syrupy per Plan 01's A1/A2
    note). Commit the golden under `tests/__snapshots__/test_golden_custom_ids/`. Touch NO `weatherbot/` source.
  </action>
  <verify>
    <automated>uv run pytest tests/test_golden_custom_ids.py -q</automated>
  </verify>
  <done>
    Inline `wb:loc:select` pin passes; the full ordered-id raw-bytes golden passes and is committed; the
    golden contains every `wb:cmd:*` / `wb:fc:*` id verbatim.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_golden_custom_ids.py -q` passes; second run needs no `--snapshot-update`.
    - `grep -rc 'wb:cmd:weather' tests/__snapshots__/test_golden_custom_ids/` is ≥ 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Oracle self-proof — perturbation MUST fail (D-12 / SC2)</name>
  <files>tests/test_oracle_selfproof.py</files>
  <read_first>
    - @tests/test_panel.py (`test_callback_raise_isolated` ~500-522 — the `pytest.raises` discipline idiom)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "test_oracle_selfproof.py"
    - @.planning/phases/21-characterization-golden-test-harness/21-CONTEXT.md (D-12)
  </read_first>
  <behavior>
    - `test_field_reorder_is_caught`: a REAL `build_inbound_embed` output projected via `embed_to_golden`,
      then a copy with `fields` reversed — `assert good == reordered` must raise `AssertionError` (an
      order-insensitive compare would NOT raise, turning this test red and exposing a loosened oracle).
    - `test_custom_id_byteflip_is_caught`: a real custom_id vs a one-byte-flipped variant — comparison
      must raise `AssertionError`.
  </behavior>
  <action>
    Create tests/test_oracle_selfproof.py as a STANDING meta-test (D-12). Drive an ACTUAL render
    (`build_inbound_embed(_forecast_from(load_fixture("onecall_metric_clear.json")))` under
    `time_machine.travel(FROZEN)`) and a REAL panel `custom_id` (not a hand literal) so the test also
    fails if the render/panel is ever loosened. Reverse the `fields` list / flip one byte and wrap the
    equality assertion in `pytest.raises(AssertionError)`. Do NOT use `xfail(strict=True)` (reads
    inverted, D-12-rejected). Touch NO `weatherbot/` source.
  </action>
  <verify>
    <automated>uv run pytest tests/test_oracle_selfproof.py -q</automated>
  </verify>
  <done>
    Both meta-tests pass (i.e. the perturbations DO raise AssertionError), driven off real render/custom_id
    output; no `xfail`; no production-source change.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_oracle_selfproof.py -q` passes.
    - `grep -c 'pytest.raises(AssertionError)' tests/test_oracle_selfproof.py` is ≥ 2.
    - `grep -c 'xfail' tests/test_oracle_selfproof.py` is 0 (D-12 — no inverted-reading marker).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed golden snapshot | Rendered embed JSON + custom_id bytes enter version control |

Purely additive test infra — no runtime attack surface (RESEARCH § Security Domain). The only
load-bearing concern is secret hygiene in committed snapshots.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Information Disclosure | committed embed / custom_id goldens | mitigate | Goldens are driven through gateway-free fakes carrying placeholder ids only (conftest); each committed `.json` reviewed once for `appid`/webhook/token strings before commit (Task 1 acceptance grep). Embeds snapshot only rendered fields, never the request URL. |
</threat_model>

<verification>
- `uv run pytest tests/test_golden_embeds.py tests/test_golden_custom_ids.py tests/test_oracle_selfproof.py -q` all green.
- Second run requires no `--snapshot-update` (zero flake — the oracle's trustworthiness gate).
- `Updated <t:…:t> (<t:…:R>)` format preserved in at least one embed golden; no secret in any committed golden.
- Full suite still green: `uv run pytest -q`.
</verification>

<success_criteria>
- Embeds pinned per command × 📍/Updated states (D-10), Updated format preserved + epoch frozen (D-11).
- custom_id bytes pinned inline + full ordered set (D-02/D-03).
- Oracle self-proof ships as a standing test proving the comparison's teeth (D-12 / SC2).
- BHV-02 partially satisfied (embeds + custom_ids surfaces).
</success_criteria>

<output>
Create `.planning/phases/21-characterization-golden-test-harness/21-02-SUMMARY.md` when done.
</output>
