---
phase: 21-characterization-golden-test-harness
plan: 03
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - tests/test_golden_cli.py
  - tests/test_golden_schedule.py
  - tests/test_golden_db.py
  - tests/__snapshots__/test_golden_cli
  - tests/__snapshots__/test_golden_schedule
  - tests/__snapshots__/test_golden_db
autonomous: true
requirements: [BHV-02]
user_setup: []

must_haves:
  truths:
    - "CLI stdout + exit code are pinned byte-exact per subcommand (offline subcommands + each forecast variant via fixture-injected client) — a single byte flip in stdout fails (D-02)"
    - "The registered-job schedule plan (job_id, str(trigger), frozen next_run_time) is pinned byte-exact, read off a never-started scheduler (D-11), sorted by job_id"
    - "The DB rows a briefing writes (weather_onecall imperial+metric, sent_log, alerts) are pinned byte-exact with an explicit ORDER BY and the autoincrement rowid scrubbed / clock-fields frozen (D-11)"
  artifacts:
    - path: "tests/test_golden_cli.py"
      provides: "cli.main(argv) stdout/exit goldens (SingleFile bytes + inline exit-code pins)"
      contains: "capsys"
    - path: "tests/test_golden_schedule.py"
      provides: "schedule-plan golden via _register_jobs + get_jobs (JSON, frozen)"
      contains: "schedule_plan_golden"
    - path: "tests/test_golden_db.py"
      provides: "weather_onecall/sent_log/alerts row goldens (JSON, scrubbed + ORDER BY)"
      contains: "onecall_rows_golden"
  key_links:
    - from: "tests/test_golden_cli.py"
      to: "weatherbot/cli.py"
      via: "main(argv) -> int + capsys stdout, fixture-injected client (no network/secret)"
      pattern: "from weatherbot.cli import main"
    - from: "tests/test_golden_schedule.py"
      to: "weatherbot/scheduler/daemon.py"
      via: "_register_jobs on a NOT-started BackgroundScheduler, read via get_jobs()"
      pattern: "_register_jobs"
    - from: "tests/test_golden_db.py"
      to: "weatherbot/weather/store.py"
      via: "persist/claim_slot/record_alert then SELECT ... ORDER BY (rowid scrubbed)"
      pattern: "persist|claim_slot|record_alert"
---

<objective>
Pin the CLI, scheduler-plan, and persisted-DB-row surfaces as byte-exact goldens. These three
non-visual outputs are exactly what the scheduler (Phase 23), config-reload (Phase 24), and
registry/dispatch (Phase 26) extractions re-run as the byte-identical oracle. They have zero file
overlap with Plan 02, so they execute in the same wave.

Purpose: CLI stdout/exit, the `(job_id, trigger, next_run_time)` plan, and the rows a briefing
writes are observable bytes intent-tests miss. Frozen clock + explicit ORDER BY + rowid scrub make
them deterministic without over-scrubbing the meaningful, frozen values (D-11).

Output: `tests/test_golden_cli.py`, `tests/test_golden_schedule.py`, `tests/test_golden_db.py`
+ their committed `tests/__snapshots__/` goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-characterization-golden-test-harness/21-CONTEXT.md
@.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md
@.planning/phases/21-characterization-golden-test-harness/21-01-SUMMARY.md

# Read-first analog seams:
@tests/test_cli.py
@tests/test_reliability.py
@tests/test_store.py
@weatherbot/cli.py
@weatherbot/scheduler/daemon.py
@weatherbot/weather/store.py
</context>

## Artifacts this phase produces (Plan 03)

- `tests/test_golden_cli.py` — `main(argv)` stdout (raw-bytes SingleFile golden) + exit-code (inline
  pin, D-03) per offline subcommand (`help`, `check`/`check-config`, `locations`, `status`) and each
  forecast variant (weekday/weekend × detailed/compact) via the `_FakeClient` + recorded `onecall_*`
  fixture-injection idiom; `weatherbot.cli.time.sleep` monkeypatched instant.
- `tests/test_golden_schedule.py` — schedule plan via `_register_jobs` on a never-started
  `BackgroundScheduler`, serialized with the conftest `schedule_plan_golden` helper, under `time_machine`.
- `tests/test_golden_db.py` — `weather_onecall` (imperial+metric), `sent_log`, `alerts` row goldens
  via `persist`/`claim_slot`/`record_alert` + the conftest `onecall_rows_golden` reader (ORDER BY,
  rowid scrubbed, clock frozen).
- Committed goldens under `tests/__snapshots__/test_golden_{cli,schedule,db}/`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI stdout/exit goldens — offline subcommands + forecast variants</name>
  <files>tests/test_golden_cli.py, tests/__snapshots__/test_golden_cli</files>
  <read_first>
    - @tests/test_cli.py (`_FakeClient` ~35-50; `capsys` idiom ~85-100; `weatherbot.cli.time.sleep` monkeypatch; `main(["check", "--config", str(bad)])` ~305)
    - @weatherbot/cli.py (`main(argv)` ~698; subparsers ~729-837; handlers `return 0/1` ~246-953; `help` needs no config ~836)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "test_golden_cli.py"
    - @.planning/phases/21-characterization-golden-test-harness/21-RESEARCH.md § "Pitfall 5" (never let real .env / network reach a snapshot)
  </read_first>
  <behavior>
    - Each offline subcommand (`help`, `check`/`check-config`, `locations`, `status`) and each forecast
      variant (weekday/weekend × detailed/compact, D-10) produces a byte-exact stdout golden
      (`capsys.readouterr().out.encode() == bytes_snapshot`) and an inline exit-code pin (`assert rc == 0`, D-03).
    - `weather`/forecast goldens inject `_FakeClient` returning recorded `onecall_*` JSON — NO network,
      NO real `.env`/`appid`. `weatherbot.cli.time.sleep` is monkeypatched so any retry pause is instant.
    - Runs under `time_machine.travel(FROZEN)` for any clock-derived stdout.
  </behavior>
  <action>
    Create tests/test_golden_cli.py. For each offline subcommand call `main([...])`, capture
    `capsys.readouterr().out`, pin `rc` inline (D-03), and assert `out.encode() == bytes_snapshot`
    (D-02 raw bytes — one byte flip fails). For `weather`/forecast variants reuse the test_cli.py
    `_FakeClient` + `load_fixture("onecall_*")` injection and monkeypatch `weatherbot.cli.time.sleep`.
    Provide config via the test_cli.py temp-config idiom — never let `load_settings()`'s real `.env`
    reach a snapshot (Pitfall 5 / V7). One named case per cell (D-10). Generate with one
    `--snapshot-update`, review each committed golden for secret hygiene before commit. Touch NO
    `weatherbot/` source.
  </action>
  <verify>
    <automated>uv run pytest tests/test_golden_cli.py -q</automated>
  </verify>
  <done>
    Each offline subcommand + forecast variant has a passing stdout byte-golden + inline exit pin;
    goldens committed under `tests/__snapshots__/test_golden_cli/`; no `appid`/key/path-secret in any golden.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_golden_cli.py -q` passes; second run needs no `--snapshot-update`.
    - `grep -rEi 'appid|OPENWEATHER|webhook|/home/' tests/__snapshots__/test_golden_cli/` returns no env-leak hit (V7 / Pitfall 5).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schedule-plan golden — (job_id, trigger, next_run_time)</name>
  <files>tests/test_golden_schedule.py, tests/__snapshots__/test_golden_schedule</files>
  <read_first>
    - @tests/test_reliability.py (`test_heartbeat_job_registered_with_slots` ~621-651 — `_register_jobs` on a NOT-started BackgroundScheduler, `get_jobs()` read)
    - @tests/test_status.py (`_FakeJob`/`DaemonState.next_fires` ~34-114)
    - @weatherbot/scheduler/daemon.py (`_register_jobs` ~596-680 — job ids `{name}|{time}|{days}` / forecast `{name}|fc|{kind}|{variant}|{time}|{days}`)
    - @weatherbot/interactive/state.py (`_next_fire` / `next_fires` ~37-58 — the `CronTrigger.get_next_fire_time` fallback)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "test_golden_schedule.py"; @21-RESEARCH.md § "Pitfall 3"
  </read_first>
  <behavior>
    - `_register_jobs` populates a never-started `BackgroundScheduler`; the golden captures each job's
      `id`, `str(trigger)` (the byte-exact PRIMARY — deterministic regardless of start), and a frozen
      `next_run_time` (computed via the same `get_next_fire_time(None, datetime.now(tz))` fallback
      `DaemonState.next_fires()` uses when the pending scheduler reports None) under `time_machine`.
    - Plan is sorted by `job_id` (explicit ORDER, D-11) — not insertion luck.
  </behavior>
  <action>
    Create tests/test_golden_schedule.py. Use the `holder_scheduler` conftest fixture (or build a
    `BackgroundScheduler()` directly, NEVER started — Pitfall 3 / RESEARCH anti-pattern) and call
    `daemon._register_jobs(scheduler, ConfigHolder(config), db_path=tmp_db, settings=None,
    stop_event=threading.Event())`. Serialize with the `schedule_plan_golden(scheduler)` helper from
    Plan 01 (it reads `str(job.trigger)` as primary + frozen `next_run_time`, sorts by id). Assert the
    plan `== json_snapshot` under `time_machine.travel(FROZEN)`. Snapshot `str(job.trigger)` even if
    `next_run_time` is None on a pending scheduler — the trigger spec is the load-bearing byte. Touch
    NO `weatherbot/` source.
  </action>
  <verify>
    <automated>uv run pytest tests/test_golden_schedule.py -q</automated>
  </verify>
  <done>
    The schedule-plan golden passes and is committed; it contains `str(trigger)` for every registered
    job, sorted by job_id; `next_run_time` (when present) is a frozen literal.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_golden_schedule.py -q` passes; second run needs no `--snapshot-update`.
    - `grep -rc 'cron' tests/__snapshots__/test_golden_schedule/` is ≥ 1 (trigger spec captured).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DB-row goldens — weather_onecall / sent_log / alerts</name>
  <files>tests/test_golden_db.py, tests/__snapshots__/test_golden_db</files>
  <read_first>
    - @tests/test_store.py (`_connect` ~48-50; `conn.execute(SELECT …)` ~79-150)
    - @weatherbot/weather/store.py (`persist` writes 2 weather_onecall rows imperial+metric; `claim_slot` writes sent_log; `record_alert` writes alerts; schema ~84-94; `_local_date_iso` ~193)
    - @tests/conftest.py (`tmp_db`, `seed_sent_row`)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "test_golden_db.py"; @21-RESEARCH.md § "Pattern 6"
  </read_first>
  <behavior>
    - `persist(tmp_db, LOC, forecast)` then read `weather_onecall` via the conftest `onecall_rows_golden`
      reader (explicit `ORDER BY units, location_name`, autoincrement `id` + `fetched_at_utc` scrubbed
      OR frozen, `target_local_date` frozen via `time_machine`) → JSON golden.
    - `sent_log` (via `claim_slot`/`seed_sent_row`) and `alerts` (via `record_alert`) likewise pinned
      with their rowid scrubbed and clock fields frozen.
    - `raw_json` carries only the OpenWeather response payload — verify no request URL / `appid` string (V7).
  </behavior>
  <action>
    Create tests/test_golden_db.py. Drive `persist`/`claim_slot`/`record_alert` against a `tmp_db`,
    load forecast input from `load_fixture("onecall_*")`, and read rows back through the conftest
    `onecall_rows_golden` helper (and analogous explicit-ORDER-BY readers for `sent_log`/`alerts`) under
    `time_machine.travel(FROZEN)`. Assert each row set `== json_snapshot`. Scrub ONLY the autoincrement
    rowid (D-11 — never sort-scrub, never blanket-scrub the meaningful frozen `target_local_date`/`*_at_utc`).
    Touch NO `weatherbot/` source. Note: `weatherbot/weather` is OUT of coverage scope (D-07) but its rows
    ARE snapshotted here — that is intentional.
  </action>
  <verify>
    <automated>uv run pytest tests/test_golden_db.py -q</automated>
  </verify>
  <done>
    `weather_onecall` (imperial+metric), `sent_log`, and `alerts` row goldens pass and are committed;
    each read path has an explicit `ORDER BY`; rowid scrubbed; clock fields frozen; no secret in `raw_json`.
  </done>
  <acceptance_criteria>
    - `uv run pytest tests/test_golden_db.py -q` passes; second run needs no `--snapshot-update`.
    - `grep -c 'ORDER BY' tests/test_golden_db.py` is ≥ 1 (explicit order in the read path, D-11).
    - `grep -rEi 'appid=|api.openweathermap.org' tests/__snapshots__/test_golden_db/` returns no hit (V7 — request URL never persisted).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed golden snapshot | CLI stdout bytes, schedule plan, and DB rows enter version control |
| CLI composition root in tests | `main(argv)` could reach `load_settings()` / real `.env` / network if not stubbed |

Purely additive test infra — no runtime attack surface (RESEARCH § Security Domain). The load-bearing
concerns are (a) no real secret/env value in a committed golden, (b) no network call.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03a | Information Disclosure | CLI stdout golden via real `.env` | mitigate | Drive CLI goldens through offline subcommands + `_FakeClient` fixture injection (test_cli.py precedent); temp config; never call `load_settings()` against the real `.env`. Task 1 acceptance grep rejects `appid`/`OPENWEATHER`/home-path leaks. |
| T-21-03b | Information Disclosure | DB-row `raw_json` golden | mitigate | `store.py` persists only the OpenWeather *response* payload (request URL carrying `appid` never stored); Task 3 acceptance grep verifies no `appid=`/API-URL string in committed `raw_json`. |
| T-21-03c | Tampering | accidental network in CLI/forecast golden | mitigate | Fixture-injected client only; `weatherbot.cli.time.sleep` monkeypatched; no live OpenWeather call in any golden path. |
</threat_model>

<verification>
- `uv run pytest tests/test_golden_cli.py tests/test_golden_schedule.py tests/test_golden_db.py -q` all green.
- Second run requires no `--snapshot-update` (zero flake).
- No secret/env value in any committed CLI or DB golden; explicit ORDER BY in schedule + DB read paths.
- Full suite still green: `uv run pytest -q`.
</verification>

<success_criteria>
- CLI stdout/exit pinned per subcommand × forecast variant (D-10, D-02/D-03).
- Schedule plan pinned (str(trigger) primary + frozen next_run_time, sorted) (D-11).
- DB rows pinned with ORDER BY + rowid scrub + frozen clock (D-11).
- BHV-02 partially satisfied (CLI + schedule + DB surfaces).
</success_criteria>

<output>
Create `.planning/phases/21-characterization-golden-test-harness/21-03-SUMMARY.md` when done.
</output>
