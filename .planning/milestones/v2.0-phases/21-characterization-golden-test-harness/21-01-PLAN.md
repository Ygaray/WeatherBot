---
phase: 21-characterization-golden-test-harness
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/conftest.py
autonomous: false
requirements: [BHV-01]
user_setup: []

must_haves:
  truths:
    - "syrupy and pytest-cov are installed and importable in the project venv (uv run python -c \"import syrupy, pytest_cov\" exits 0)"
    - "A single frozen instant FROZEN and the embed-to-golden / row-reader / schedule-plan serializer helpers exist in tests/conftest.py and are importable by every golden test"
    - "The [tool.coverage.run] (branch=true, 6 move-path packages) + [tool.coverage.report] blocks exist in pyproject.toml"
    - "time-machine demonstrably freezes the Updated <t:…> epoch in a real render_embed output (or the documented monkeypatch fallback is wired)"
    - "The full pre-existing suite (652 tests) still passes after the tooling/config additions (no regression)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "FROZEN constant, json_snapshot/bytes_snapshot fixtures, embed_to_golden + schedule_plan_golden + onecall_rows_golden helpers"
      contains: "embed_to_golden"
    - path: "pyproject.toml"
      provides: "syrupy + pytest-cov dev deps and [tool.coverage.*] branch-mode config scoped to the 6 move-path packages"
      contains: "tool.coverage.run"
  key_links:
    - from: "tests/conftest.py"
      to: "weatherbot/interactive/bot.py"
      via: "embed_to_golden projects render_embed/build_inbound_embed output (title, description, color, fields incl. inline) — excludes embed.timestamp"
      pattern: "embed_to_golden"
    - from: "pyproject.toml [tool.coverage.run]"
      to: "weatherbot/{channels,scheduler,config,reliability,ops,interactive}"
      via: "source list scopes the one-time branch audit to the move-path packages (weatherbot/weather excluded per D-07)"
      pattern: "weatherbot/interactive"
---

<objective>
Stand up the Wave-0 golden-test harness: install the two locked snapshot/coverage tools, add the
branch-coverage config block, and add the shared conftest helpers (frozen instant, syrupy extension
fixtures, embed/row/schedule serializers) that every Wave-1 golden file consumes. Discharge the
open `[ASSUMED]` smoke items (syrupy `use_extension` call shape, `time-machine` reaching
`discord.utils.utcnow`, `str(job.trigger)` stability) so Wave 1 builds on confirmed mechanics.

Purpose: This phase is the byte-identical ORACLE re-run after every later extraction (Phases 22–28).
The harness must be trustworthy (zero flake) before any golden is recorded — a flaky helper would
poison the oracle for the whole milestone. Everything in Waves 1–2 depends on this plan's helpers.

Output: `syrupy`/`pytest-cov` dev deps + `uv.lock`; `[tool.coverage.*]` config; the
`tests/conftest.py` shared helpers and fixtures; a recorded Wave-0 smoke-confirm note in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-characterization-golden-test-harness/21-CONTEXT.md
@.planning/phases/21-characterization-golden-test-harness/21-RESEARCH.md
@.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md

# Read-first analog seams:
@tests/conftest.py
@weatherbot/interactive/bot.py
@pyproject.toml
</context>

## Artifacts this phase produces (Plan 01)

- `tests/conftest.py` additions (no new file — MODIFY): `FROZEN` instant constant; `json_snapshot`
  and `bytes_snapshot` fixtures (syrupy `use_extension` of `JSONSnapshotExtension` /
  `SingleFileSnapshotExtension`); `embed_to_golden(embed) -> dict`; `schedule_plan_golden(scheduler)
  -> list[dict]`; `onecall_rows_golden(db_path) -> list[dict]`.
- `pyproject.toml`: `syrupy>=5.3.4` + `pytest-cov>=7.1.0` in `[dependency-groups] dev`;
  `[tool.coverage.run]` (branch=true, source = the 6 move-path packages) + `[tool.coverage.report]`
  (show_missing, exclude_also) blocks.
- `uv.lock` updated.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy checkpoint — verify syrupy + pytest-cov before install</name>
  <files>(none — human verification gate, no file change)</files>
  <action>BLOCKING-HUMAN gate before the Task 2 install. Pause and have the human verify the two
  packages on pypi.org per the steps below; do not run `uv add` until they type "approved". This
  gate is never auto-approvable (package-legitimacy checkpoint).</action>
  <what-built>
    Nothing yet — this gate precedes the `uv add --dev` in Task 2. RESEARCH.md § Package Legitimacy
    Audit returned a metadata-driven SUS (`unknown-downloads` / `too-new` / `no-repository`) for
    syrupy, pytest-cov, and the transitive coverage — NOT a slopsquat signal. Both top-level packages
    are CONTEXT-locked (D-01, D-05). Per the planner package-legitimacy gate, a blocking-human
    verification precedes the install.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/syrupy/ — confirm source repo github.com/syrupy-project/syrupy,
       latest 5.3.4, runtime dep is pytest only.
    2. Open https://pypi.org/project/pytest-cov/ — confirm source repo github.com/pytest-dev/pytest-cov,
       latest 7.1.0, requires coverage[toml]/pluggy/pytest.
    3. Confirm these are the two tools you chose in discuss-phase (D-01, D-05).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or name a concern.</resume-signal>
  <verify><human-check>Human types "approved" after verifying both packages on pypi.org.</human-check></verify>
  <done>Human approved the syrupy + pytest-cov install after pypi.org verification.</done>
  <acceptance_criteria>
    Human types "approved". No code runs in this task.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install syrupy + pytest-cov and add the branch-coverage config block</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - @pyproject.toml (existing `[dependency-groups] dev` with pytest/ruff/time-machine; `[tool.pytest.ini_options]`)
    - @.planning/phases/21-characterization-golden-test-harness/21-RESEARCH.md § "coverage.py Configuration (D-06/D-07/D-08/D-09)" (the exact toml block)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "pyproject.toml config block"
  </read_first>
  <action>
    Run `uv add --dev "syrupy>=5.3.4" "pytest-cov>=7.1.0"` (per D-01/D-05) — this updates the dev
    group and uv.lock. Then add to pyproject.toml a `[tool.coverage.run]` block with `branch = true`
    (D-06 — branch mode mandatory) and `source` set to exactly the six move-path packages
    `weatherbot/channels`, `weatherbot/scheduler`, `weatherbot/config`, `weatherbot/reliability`,
    `weatherbot/ops`, `weatherbot/interactive` (D-07 — weatherbot/weather is deliberately EXCLUDED
    even though its DB rows get snapshotted later). Add a `[tool.coverage.report]` block with
    `show_missing = true` and an `exclude_also` list for systematic defensive patterns
    (`if TYPE_CHECKING:`, `raise NotImplementedError`, bare ellipsis) per D-09. Do NOT add a
    `fail_under` gate (D-08 — this is a one-time audit, not a standing gate). Do NOT add any
    `--cov` flag to `[tool.pytest.ini_options] addopts` (the audit is invoked explicitly in Plan 05,
    not on every run).
  </action>
  <verify>
    <automated>uv run python -c "import syrupy, pytest_cov; print('ok')"</automated>
  </verify>
  <done>
    `uv run python -c "import syrupy, pytest_cov"` exits 0; pyproject.toml contains
    `[tool.coverage.run]` with `branch = true` and the 6-package source list and a
    `[tool.coverage.report]`; uv.lock is updated; no `fail_under` and no `--cov` in addopts.
  </done>
  <acceptance_criteria>
    - `uv run python -c "import syrupy, pytest_cov"` exits 0.
    - `grep -q 'branch = true' pyproject.toml` succeeds.
    - `grep -c 'weatherbot/' pyproject.toml` shows the six move-path source entries.
    - `grep -L 'fail_under' pyproject.toml` confirms no standing gate added (D-08).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add shared conftest helpers + fixtures + Wave-0 smoke confirms</name>
  <files>tests/conftest.py</files>
  <read_first>
    - @tests/conftest.py (existing `load_fixture`, `tmp_db`, `seed_sent_row`, `fake_interaction`, `holder_scheduler`)
    - @weatherbot/interactive/bot.py (`render_embed` ~line 194; `build_inbound_embed` ~line 402; `Updated <t:…>` build at ~219-223; `embed.timestamp` set at ~272/421)
    - @.planning/phases/21-characterization-golden-test-harness/21-PATTERNS.md § "Shared Patterns" (embed_to_golden, frozen-clock, syrupy fixtures) and § "test_golden_schedule" / "test_golden_db" (serializer helpers)
    - @.planning/phases/21-characterization-golden-test-harness/21-RESEARCH.md § "Assumptions Log" (A1/A3/A7 to discharge)
  </read_first>
  <behavior>
    - Smoke 1 (A3): inside `time_machine.travel(FROZEN, tick=False)`, a real `render_embed` /
      `build_inbound_embed` description contains the frozen epoch of FROZEN as the `Updated <t:{epoch}…>`
      value — proving time-machine reaches `discord.utils.utcnow`. If it does NOT, the test wires the
      documented fallback `monkeypatch.setattr("weatherbot.interactive.bot.discord.utils.utcnow", lambda: FROZEN)`
      and the SUMMARY records that the fallback path is in force.
    - Smoke 2 (A1): `json_snapshot` and `bytes_snapshot` fixtures resolve via syrupy `use_extension`
      against the installed 5.3.4 and a trivial `assert {...} == json_snapshot` round-trips on `--snapshot-update`.
    - `embed_to_golden(embed)` returns an order-preserving dict `{title, description, color, fields:[{name,value,inline}]}`
      and EXCLUDES `embed.timestamp` (outside the byte contract per D-11).
  </behavior>
  <action>
    Add to tests/conftest.py: (1) a module-level `FROZEN = datetime(2026, 6, 20, 13, 0, 0, tzinfo=timezone.utc)`
    shared instant (D-11 discretion — one instant for the whole harness; reuse whatever tz the recorded
    fixtures assume). (2) `json_snapshot` and `bytes_snapshot` pytest fixtures that return
    `snapshot.use_extension(JSONSnapshotExtension)` / `snapshot.use_extension(SingleFileSnapshotExtension)`
    respectively (D-02 — confirm the exact `use_extension` call shape against installed syrupy 5.3.4,
    A1). (3) `embed_to_golden(embed) -> dict` exactly as specified in PATTERNS § test_golden_embeds
    (title, description, color value, fields as ordered `{name,value,inline}`; exclude `embed.timestamp`).
    (4) `schedule_plan_golden(scheduler) -> list[dict]` returning `{job_id, trigger:str(job.trigger),
    next_run_time}` sorted by `job_id` (D-11 explicit ORDER, A7 — confirm `str(CronTrigger)` is stable).
    (5) `onecall_rows_golden(db_path) -> list[dict]` reading `weather_onecall` with an explicit
    `ORDER BY units, location_name`, selecting only the byte-contract columns (scrub the autoincrement
    `id`; freeze-or-omit `fetched_at_utc` per D-11). Add a Wave-0 smoke test `test_frozen_epoch_reaches_render`
    that asserts the FROZEN epoch appears in a real render's description (A3), wiring the monkeypatch
    fallback only if the assertion fails. Do NOT modify any `weatherbot/` source — additive test infra only.
  </action>
  <verify>
    <automated>uv run pytest tests/conftest.py tests/test_panel.py -q -k "frozen_epoch or weather_spec or panel" 2>&1 | tail -5; uv run pytest -q tests/ -k "frozen_epoch_reaches_render"</automated>
  </verify>
  <done>
    `FROZEN`, `json_snapshot`, `bytes_snapshot`, `embed_to_golden`, `schedule_plan_golden`,
    `onecall_rows_golden` all defined in tests/conftest.py and importable; the Wave-0 smoke test for
    the frozen epoch passes (or the monkeypatch fallback is wired and the SUMMARY says so); no
    `weatherbot/` source file changed.
  </done>
  <acceptance_criteria>
    - `uv run python -c "from tests.conftest import embed_to_golden, schedule_plan_golden, onecall_rows_golden, FROZEN"` exits 0 (or the helpers resolve via the pytest fixture path).
    - `uv run pytest tests/ -k frozen_epoch_reaches_render` passes.
    - `git diff --name-only weatherbot/` is empty (zero production-source change).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-dependency install (uv add) | Two new packages pulled from PyPI cross into the project venv |
| committed test config | `pyproject.toml` / `uv.lock` enter version control |

This phase adds NO runtime request handler, auth path, crypto, or data sink. It is purely additive
test infrastructure — no new production attack surface (per RESEARCH § Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-SC | Tampering | syrupy / pytest-cov PyPI install | mitigate | Task 1 blocking-human legitimacy checkpoint (both SUS-by-metadata, CONTEXT-locked); verify on pypi.org before `uv add`; `uv.lock` pins the resolved hashes. |
| T-21-01 | Information Disclosure | conftest helpers / fixtures | accept | Helpers read only recorded fixtures + fake interactions carrying placeholder ids (conftest T-17-01-01) — no real secret reachable; no snapshot recorded in this plan. |
</threat_model>

<verification>
- `uv run python -c "import syrupy, pytest_cov"` exits 0.
- `pyproject.toml` has `[tool.coverage.run] branch = true` + 6-package source + `[tool.coverage.report]`; no `fail_under`.
- conftest helpers defined and importable; Wave-0 frozen-epoch smoke passes (or fallback wired + noted).
- Full suite still green: `uv run pytest -q` (652 tests, no regression).
</verification>

<success_criteria>
- syrupy + pytest-cov installed; coverage config in place (branch mode, 6 move-path packages, no standing gate).
- Shared conftest harness (FROZEN, syrupy fixtures, embed/row/schedule serializers) ready for Wave 1.
- Wave-0 `[ASSUMED]` items A1/A3/A7 discharged and recorded in the SUMMARY.
- BHV-01: pre-existing 652-test suite still green.
</success_criteria>

<output>
Create `.planning/phases/21-characterization-golden-test-harness/21-01-SUMMARY.md` when done — record
the Wave-0 smoke results (does time-machine reach `discord.utils.utcnow`? exact syrupy `use_extension`
call shape? `str(job.trigger)` stable?) for Wave 1 to rely on.
</output>
