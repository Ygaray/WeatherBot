---
phase: 28-physical-repo-split-uv-git-dependency-extension-guide
plan: 04
type: execute
wave: 4
depends_on: ["28-02", "28-03"]
files_modified:
  - "deploy/REPIN-RITUAL.md (NEW)"
  - "deploy/PROMOTION-LEDGER.md (NEW)"
  - ".planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md (NEW)"
autonomous: true
requirements: [PKG-02, DOCS-01]
user_setup:
  - service: "yahir-mint host (live systemd weatherbot service)"
    why: "Gate-2 live restart UAT is a DEFERRED milestone-close obligation (secure host action + live Discord tap-through the tooling cannot synthesize). It is recorded, NOT executed, in this phase — it does not block the phase per the Two-Gate UAT policy."
must_haves:
  truths:
    - "A durable commit->push->tag->repin->deploy ritual is documented WeatherBot-side"
    - "A promotion ledger records sha promotions (date | tag | sha | note)"
    - "Gate-1 self-UAT proves, with exact commands + evidence, the clean-venv install + byte-identical suite/goldens + leak gate + direct_url.json sha read all pass against the pinned module"
    - "The live yahir-mint restart UAT is recorded as a deferred Gate-2 obligation (mechanism + data-level checks automated in Gate-1)"
  artifacts:
    - path: "deploy/REPIN-RITUAL.md"
      provides: "The durable commit->push->tag->uv lock --upgrade-package->uv sync --frozen->systemctl restart ritual (D-06)"
      contains: "uv lock --upgrade-package"
    - path: "deploy/PROMOTION-LEDGER.md"
      provides: "Append-only sha-promotion record (D-06)"
      contains: "v0.1.0"
    - path: ".planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md"
      provides: "Persistent Gate-1 self-UAT log (per-criterion: command, evidence, PASS/FAIL/PARTIAL)"
  key_links:
    - from: "deploy/REPIN-RITUAL.md"
      to: "deploy/PROMOTION-LEDGER.md"
      via: "the ritual's final step appends a row to the ledger"
      pattern: "PROMOTION-LEDGER"
---

<objective>
Stand up the durable process artifacts (D-06: repin ritual + promotion ledger, WeatherBot-side per RESEARCH A3) and run the **Gate-1 self-UAT** — the autonomous, per-criterion proof (clean-venv `uv sync --frozen` + byte-identical suite/goldens + `uv build --no-sources` leak gate + the `direct_url.json` sha read) that turns "works locally" into "works on host" and gates the phase. The live `yahir-mint` `systemctl restart` is recorded as the deferred **Gate-2** obligation — its automatable mechanism + data-level checks ARE proven here; only the secure-host restart + live-panel tap-through defer.

Purpose: Closes PKG-02 SC#2/SC#4 + DOCS-01 SC#4. Per the project Two-Gate UAT policy, a fully-passing Gate-1 completes the phase autonomously — no per-phase blocking human checkpoint; Gate-2 is a milestone-close obligation.
Output: `deploy/REPIN-RITUAL.md`, `deploy/PROMOTION-LEDGER.md`, and the `28-SELF-UAT.md` Gate-1 log; the deferred Gate-2 item recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-RESEARCH.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-PATTERNS.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-VALIDATION.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-02-SUMMARY.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-03-SUMMARY.md

# Placement analog for the process docs:
@deploy/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the repin ritual + promotion ledger process docs (D-06)</name>
  <read_first>
    - 28-PATTERNS.md § "repin-ritual + promotion-ledger docs" — placement analog `deploy/` (alongside the systemd unit); the ritual steps spelled out in RESEARCH Code Examples; the ledger is a net-new append-only table (date | tag | sha | note)
    - 28-RESEARCH.md § "Code Examples" (the exact ritual: commit→push→tag→`uv lock --upgrade-package yahir-reusable-bot`→commit lock→host pull→`uv sync --frozen`→`systemctl restart`) + § "Pitfall 2" (treat deploy tags as immutable; regenerate the lock after a repin, never force-push a tag)
    - 28-02-SUMMARY.md + 28-03-SUMMARY.md for the resolved `v0.1.0` sha + startup-log event key to seed the first ledger row
  </read_first>
  <files>deploy/REPIN-RITUAL.md, deploy/PROMOTION-LEDGER.md</files>
  <action>
    Author `deploy/REPIN-RITUAL.md` (WeatherBot-side per RESEARCH A3 — the app owns consume/deploy) documenting the durable repin loop step-by-step: in the module repo, commit → push → `git tag vX.Y.Z` (immutable; never force-push a deploy tag, Pitfall 2) → push the tag; in WeatherBot, bump the `[tool.uv.sources]` tag, run `uv lock --upgrade-package yahir-reusable-bot` (re-resolves only that package to the new tag's sha), commit the new `uv.lock`; on the host, `git pull` → `uv sync --frozen` → `sudo systemctl restart weatherbot` → confirm the startup-version-log line announces the new sha; append a ledger row. Include the local-co-dev note (D-05): use the uncommitted venv overlay `uv pip install -e ../YahirReusableBot`, reverted by `uv sync --frozen`; NEVER commit a path source (the `uv build --no-sources` gate is the backstop). Author `deploy/PROMOTION-LEDGER.md` as an append-only markdown table with columns `Date | Tag | Resolved SHA | Note`, seeded with the first row (today's date | `v0.1.0` | the resolved sha from 28-02-SUMMARY | "initial split — physical repo extraction"). Reference the startup-version-log event key (from 28-03-SUMMARY) as how to read the deployed sha off the host.
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/WeatherBot &amp;&amp; test -f deploy/REPIN-RITUAL.md &amp;&amp; grep -q 'uv lock --upgrade-package' deploy/REPIN-RITUAL.md &amp;&amp; test -f deploy/PROMOTION-LEDGER.md &amp;&amp; grep -q 'v0.1.0' deploy/PROMOTION-LEDGER.md</automated>
  </verify>
  <done>`deploy/REPIN-RITUAL.md` documents the full commit→push→tag→repin→deploy loop (incl. the never-commit-a-path-source rule and immutable-tag discipline), and `deploy/PROMOTION-LEDGER.md` is an append-only table seeded with the `v0.1.0` promotion row.</done>
</task>

<task type="auto">
  <name>Task 2: Run + record the Gate-1 self-UAT (autonomous; gates the phase)</name>
  <read_first>
    - $HOME/.claude/CLAUDE.md § "Verification Policy — Two-Gate UAT" — Gate-1 = autonomous agent self-UAT with per-criterion evidence (exact commands, output, PASS/FAIL/PARTIAL); a fully-passing Gate-1 completes the phase with NO per-phase human pause; the manual-only physical step is recorded as a deferred Gate-2 item with verdict PARTIAL, never skipped
    - 28-VALIDATION.md § "Manual-Only Verifications" (the Gate-2 live restart) + § "Sampling Rate" (the phase gate) + ROADMAP § Phase 28 the 4 success criteria
    - 28-CONTEXT.md D-08 (the Gate-1 command set) + the known syrupy quirk (suite may print "2 snapshots failed" but exits 0 — trust the exit code + .ambr diff)
  </read_first>
  <files>.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md</files>
  <action>
    Drive the autonomous Gate-1 self-UAT and write a persistent log to `28-SELF-UAT.md`. For EACH success criterion, record: what was tested, the EXACT command, the evidence/output, and PASS/FAIL/PARTIAL. The criteria (per ROADMAP SC#1-4 + D-08): (1) Clean-venv `rm -rf .venv && uv venv && uv sync --frozen` installs the module from the git pin — evidence: the install log + the resolved sha. (2) `uv run weatherbot check` + `uv run weatherbot --help` resolve through stable public names — evidence: exit 0 + output. (3) Full suite + Phase-21 goldens byte-identical against the pinned module — evidence: `uv run pytest` exit code (0) + a note that the syrupy "2 snapshots failed" print is known noise (trust exit code + `.ambr` diff); confirm the custom_id golden passes (the live-panel wire contract). (4) `uv build --no-sources` raises no leak + `python -m zipfile -l dist/*.whl` shows only `weatherbot/` — evidence: command output. (5) The startup-version-log mechanism: drive `_module_provenance()` (via its unit test 28-03 + a data-level check that the installed `direct_url.json` carries the resolved sha) — evidence: the sha matches the `uv.lock` sha (data-level proof, per the project policy: prove the value, don't infer it). Mark the live `yahir-mint` `systemctl restart` + panel tap-through as a deferred Gate-2 item with verdict PARTIAL (its mechanism — startup-log line, persistent-view re-bind, custom_id contract — and data-level checks ARE proven in Gate-1; only the secure-host restart + live Discord tap defer). A fully-passing Gate-1 completes the phase autonomously.
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/WeatherBot &amp;&amp; test -f .planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md &amp;&amp; grep -qi 'PASS' .planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md &amp;&amp; uv run pytest -q &gt;/dev/null 2>&amp;1 &amp;&amp; echo SELF_UAT_RECORDED</automated>
  </verify>
  <done>`28-SELF-UAT.md` records a per-criterion Gate-1 log (each with exact command + evidence + PASS/FAIL/PARTIAL): clean-venv install, console-script resolution, byte-identical suite/goldens, leak gate + wheel inspection, and the `direct_url.json` sha data-level check all PASS against the pinned module. The live `yahir-mint` restart + panel tap-through is recorded as a deferred Gate-2 item (verdict PARTIAL, mechanism proven). A fully-passing Gate-1 completes the phase autonomously.</done>
</task>

<task type="auto">
  <name>Task 3: Record the deferred Gate-2 live yahir-mint restart obligation (no per-phase blocking checkpoint)</name>
  <read_first>
    - $HOME/.claude/CLAUDE.md § "Verification Policy" — "Do NOT emit a per-phase blocking human-verify checkpoint; record human UAT as a deferred milestone-close obligation instead." The Gate-2 live restart blocks the MILESTONE, not this phase.
    - 28-VALIDATION.md § "Manual-Only Verifications" (the exact live-restart replay instructions) + the live-systemd-service MEMORY note (host yahir-mint, editable install, restart needed)
  </read_first>
  <files>.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md</files>
  <action>
    Per the project Two-Gate UAT policy, the live `yahir-mint` restart is a DEFERRED Gate-2 milestone-close obligation, NOT a per-phase blocking checkpoint — do not pause the phase on it. Record the obligation as a dedicated, clearly-marked section appended to `28-SELF-UAT.md` (the persistent log the human will later verify against), with the exact replay instructions for the eventual live run: (1) push `v0.1.0` to the real `YahirReusableBot` remote (the host needs a reachable git URL — note if 28-02 used a `file://` URL for Gate-1, a real remote is required first); (2) on `yahir-mint`: `cd ~/Projects/WeatherBot && git pull && uv sync --frozen` → `sudo systemctl restart weatherbot`; (3) `journalctl -u weatherbot -n 30 --no-pager` — confirm the startup-version-log sha == the promoted ledger sha + check the `editable` flag; (4) tap every button/dropdown on the already-pinned live panel — all route (custom_id contract + persistent-view re-bind intact), correct default location. Mark the verdict PARTIAL (mechanism + data-level checks proven in Gate-1; only the secure-host restart + live Discord tap defer). Flag it for carry-forward into STATE.md Deferred Items + the v2.0 milestone audit (so the milestone-close gate picks it up).
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/WeatherBot &amp;&amp; grep -qi 'Gate-2' .planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md &amp;&amp; grep -qi 'systemctl restart weatherbot' .planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-SELF-UAT.md</automated>
  </verify>
  <done>`28-SELF-UAT.md` carries a clearly-marked deferred Gate-2 section with the exact live `yahir-mint` restart + panel tap-through replay instructions (verdict PARTIAL), flagged for carry-forward into STATE.md Deferred Items + the v2.0 milestone audit. The phase does NOT block on the live run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed lock → host install | the promoted sha must match what the host installs |
| local dev tree → committed artifact | a leaked dev path would break the host deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-11 | Tampering | the deployed sha drifting from the promoted/tested sha | mitigate | The promotion ledger records the promoted sha; the startup-version-log announces the deployed sha; the ritual mandates `uv lock --upgrade-package` + committing the lock before deploy (Pitfall 2). Gate-2 step 3 cross-checks log sha == ledger sha. |
| T-28-12 | Tampering | a re-pointed mutable deploy tag | mitigate | REPIN-RITUAL.md documents tags as immutable (never force-push a deploy tag); `uv.lock` + `--frozen` pin the sha regardless of tag movement. |
| T-28-13 | Tampering / DoS | a dev path-override shipping to the host | mitigate | REPIN-RITUAL.md mandates the uncommitted venv-overlay for co-dev + the `uv build --no-sources` backstop (proven green in 28-02 / re-proven in the Gate-1 self-UAT). |
</threat_model>

<verification>
- `deploy/REPIN-RITUAL.md` documents the full repin→deploy loop with immutable-tag + never-commit-a-path-source discipline.
- `deploy/PROMOTION-LEDGER.md` is seeded with the `v0.1.0` promotion row.
- `28-SELF-UAT.md` records a per-criterion Gate-1 log (PASS evidence for clean-venv install, console script, byte-identical suite/goldens, leak gate, sha data-level check).
- The live `yahir-mint` restart is recorded as a deferred Gate-2 obligation (verdict PARTIAL) with exact replay instructions — it does NOT block the phase.
- The full suite passes (`uv run pytest`, exit 0).
</verification>

<success_criteria>
The durable process artifacts (repin ritual + promotion ledger) are stood up WeatherBot-side, the Gate-1 self-UAT proves the installed-artifact gate (clean-venv install + byte-identical suite/goldens + leak gate + sha data-level check) PASS against the pinned module with per-criterion evidence, and the live `yahir-mint` restart UAT is recorded as a deferred Gate-2 obligation — completing PKG-02 (SC#2/SC#4) + DOCS-01 (SC#4) autonomously per the Two-Gate UAT policy.
</success_criteria>

<output>
Create `.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-04-SUMMARY.md` when done. RECORD the Gate-1 per-criterion verdicts + the deferred Gate-2 obligation (to carry into STATE.md Deferred Items + the v2.0 milestone audit).
</output>
