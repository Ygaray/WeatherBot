---
phase: 28-physical-repo-split-uv-git-dependency-extension-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "../YahirReusableBot/ (NEW repo — fresh git init)"
  - "../YahirReusableBot/yahir_reusable_bot/ (copied tree, docstrings scrubbed)"
  - "../YahirReusableBot/pyproject.toml (NEW)"
  - "../YahirReusableBot/tests/test_import_hygiene.py (relocated + re-scoped)"
  - "../YahirReusableBot/EXTENSION-GUIDE.md (NEW)"
  - "../YahirReusableBot/.planning/ (module GSD init)"
  - "../YahirReusableBot/.gitignore (NEW)"
  - "../YahirReusableBot/uv.lock (NEW, tool-generated)"
autonomous: true
requirements: [PKG-02, DOCS-01]
must_haves:
  truths:
    - "A uv git install populates direct_url.json with vcs_info.commit_id (spike-confirmed before any startup-version-log is wired)"
    - "The YahirReusableBot repo exists as a fresh git init with a single clean import commit, tagged v0.1.0"
    - "yahir_reusable_bot/ in the new repo imports zero app code and carries no weatherbot.* prose"
    - "The module's own pytest suite (relocated import-hygiene, re-scoped) is green in the standalone repo"
    - "EXTENSION-GUIDE.md documents all six plug points with implemented-vs-deferred status"
    - "The module is initialized as its own GSD project recording durable-JobStore + 2nd Channel as deferred"
  artifacts:
    - path: "../YahirReusableBot/pyproject.toml"
      provides: "Module package identity (name=yahir-reusable-bot, no console script, discord.py==2.7.1 pin moved here)"
      contains: "name = \"yahir-reusable-bot\""
    - path: "../YahirReusableBot/tests/test_import_hygiene.py"
      provides: "Re-scoped import-hygiene gate (single-package grimp build; real-import self-proofs dropped/retargeted)"
    - path: "../YahirReusableBot/EXTENSION-GUIDE.md"
      provides: "DOCS-01 plug-point doc with implemented-vs-deferred status"
      contains: "JobStore"
    - path: "../YahirReusableBot/.planning/"
      provides: "Module GSD project recording deferred extension points"
  key_links:
    - from: "../YahirReusableBot/pyproject.toml"
      to: "../YahirReusableBot/yahir_reusable_bot/"
      via: "[tool.hatch.build.targets.wheel] packages = [\"yahir_reusable_bot\"]"
      pattern: "packages = \\[\"yahir_reusable_bot\"\\]"
---

<objective>
Create the standalone `YahirReusableBot` repo (D-01 fresh `git init` + single clean import commit), tag it `v0.1.0`, and prove a uv **git** install populates `direct_url.json` with `vcs_info.commit_id` BEFORE any consumer wires the startup-version-log. This plan produces the artifact the WeatherBot app will pin against in 28-02. It is the heaviest plan in the phase — repo creation, the intricate import-hygiene re-scope (Pitfall 6), and the DOCS-01 deliverables all land in the one clean import commit.

Purpose: The module repo must exist + be tagged/pushed before WeatherBot can `[tool.uv.sources]`-pin a git ref and lock it (PKG-02). The `direct_url.json` spike (RESEARCH Open Question 1) de-risks D-06 before 28-03 builds the provenance reader on it.
Output: `../YahirReusableBot/` repo (fresh git init, tagged `v0.1.0`), with the scrubbed module tree, module pyproject, re-scoped import-hygiene suite, `EXTENSION-GUIDE.md`, and the module's own `.planning/` GSD init.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-RESEARCH.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-PATTERNS.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-CONTEXT.md

# The file being relocated + re-scoped (read in full before editing — Pitfall 6 is intricate):
@tests/test_import_hygiene.py
# The analog the module pyproject derives from:
@pyproject.toml
# Module package docstrings are the EXTENSION-GUIDE content source:
@yahir_reusable_bot/__init__.py
@yahir_reusable_bot/ports/__init__.py
@yahir_reusable_bot/ports/jobstore.py
@yahir_reusable_bot/registry/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike — confirm a uv git install writes vcs_info.commit_id into direct_url.json</name>
  <read_first>
    - 28-RESEARCH.md § "Open Questions" Q1 + § "Pattern 4: Runtime deployed-sha" (the exact field names: vcs_info.commit_id, requested_revision, dir_info.editable)
    - 28-RESEARCH.md Assumption A2 (LOW-MEDIUM risk — editable variant VERIFIED this session, git-install variant must be confirmed empirically)
  </read_first>
  <files>/tmp/rb-spike/ (throwaway scratch venv + scratch git tag — no repo artifact)</files>
  <action>
    Resolve RESEARCH Open Question 1 empirically before any provenance code is written (per D-06 discretion, the runtime sha source must be confirmed, not assumed). Create a throwaway local git repo from a copy of `yahir_reusable_bot/` plus a minimal pyproject (name `yahir-reusable-bot`, hatchling, packages=["yahir_reusable_bot"]), `git init`, commit, `git tag v0.0.0-spike`. In a SEPARATE scratch directory create a fresh `uv venv` and install the module from the throwaway tag via a `[tool.uv.sources]` git pin (a `file://` git URL to the local spike repo is sufficient — no GitHub needed for the spike). Then locate and read the installed `direct_url.json` in the venv's `site-packages/yahir_reusable_bot-*.dist-info/`. Confirm it contains `vcs_info.commit_id` (the resolved sha) and `vcs_info.requested_revision` (the tag). Record the exact observed JSON shape in the SUMMARY (it is the contract 28-03's `_module_provenance()` reads). Delete the scratch dirs after. If the field names DIFFER from RESEARCH Pattern 4, record the actual names — 28-03 must use the confirmed names, not the assumed ones.
  </action>
  <verify>
    <automated>test -f /tmp/rb-spike/RESULT.txt &amp;&amp; grep -q 'commit_id' /tmp/rb-spike/RESULT.txt</automated>
  </verify>
  <done>The observed `direct_url.json` JSON shape from a real uv git install is recorded in the SUMMARY, confirming (or correcting) the `vcs_info.commit_id` / `requested_revision` field names RESEARCH Pattern 4 assumes. No production artifact changed — scratch only.</done>
</task>

<task type="auto">
  <name>Task 2: Create the YahirReusableBot repo — fresh git init, scrubbed module tree, module pyproject, tag v0.1.0</name>
  <read_first>
    - 28-CONTEXT.md D-01 (fresh git init, single clean import commit; WeatherBot retains extraction history), D-02 (name/import-root/no-console-script/hatchling/requires-python), D-03 (module deps = exactly discord.py==2.7.1 + httpx + structlog + tenacity)
    - 28-PATTERNS.md § "../YahirReusableBot/pyproject.toml (NEW)" (the derive-from skeleton, lines 64-101) and § "Manual scrub" (the two exact prose references: channels/__init__.py:6, ports/alerts.py:15)
    - 28-RESEARCH.md § "Code Examples — Repo creation + clean import commit" and § "Pattern 5" (module pyproject shape)
  </read_first>
  <files>../YahirReusableBot/ (NEW), ../YahirReusableBot/yahir_reusable_bot/, ../YahirReusableBot/pyproject.toml, ../YahirReusableBot/.gitignore</files>
  <action>
    Per D-01, create the sibling repo at `/home/yahir/Projects/YahirReusableBot` with a fresh `git init` (the rich Phase 22-27 extraction history stays in WeatherBot — do NOT use filter-repo/subtree; the default is fresh-init clean import). Copy the entire `yahir_reusable_bot/` tree verbatim (no logic change). Scrub EXACTLY the two stale prose `weatherbot.*` references the litmus ignores by construction (Pitfall 4): `yahir_reusable_bot/channels/__init__.py` line 6 and `yahir_reusable_bot/ports/alerts.py` line 15 — reword to generic prose, do not touch code. Author `pyproject.toml` from the 28-PATTERNS derive-from skeleton: `name = "yahir-reusable-bot"`, `version = "0.1.0"`, `requires-python = ">=3.12"`, `dependencies = ["discord.py==2.7.1", "httpx>=0.28.1", "structlog>=26.1.0", "tenacity>=9.1.4"]` (discord.py is the EXACT pin moved verbatim from WeatherBot — the live-panel wire contract, per D-03; do NOT loosen), `[build-system] hatchling`, `[tool.hatch.build.targets.wheel] packages = ["yahir_reusable_bot"]`, NO `[project.scripts]` (D-02 — library only), its own `[dependency-groups] dev` (pytest/ruff/syrupy/time-machine/grimp — duplicate the dev tooling so the module ships its own test suite as a GSD project, D-03 discretion), and `[tool.pytest.ini_options]`. Write a `.gitignore` (`.venv/`, `__pycache__/`, `*.egg-info/`, `dist/`, `.grimp_cache/`). Run `uv lock` + `uv build` in the new repo and inspect the wheel (`python -m zipfile -l dist/*.whl`) — confirm it contains `yahir_reusable_bot/` and ONLY it (Pitfall 7 — verify the wheel, do not trust the edit). Do NOT commit or tag yet (Task 3 relocates the test suite + docs into the same single clean import commit).
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/YahirReusableBot &amp;&amp; grep -q 'name = "yahir-reusable-bot"' pyproject.toml &amp;&amp; ! grep -rn 'weatherbot' yahir_reusable_bot/ &amp;&amp; ! grep -q 'project.scripts' pyproject.toml</automated>
  </verify>
  <done>`/home/yahir/Projects/YahirReusableBot` exists (fresh git init), `yahir_reusable_bot/` copied with the two prose references scrubbed (`grep -rn weatherbot yahir_reusable_bot/` returns nothing), `pyproject.toml` declares the module identity with the EXACT `discord.py==2.7.1` pin and NO console script, and the built wheel contains only `yahir_reusable_bot/`.</done>
</task>

<task type="auto">
  <name>Task 3: Relocate + re-scope the import-hygiene suite, author EXTENSION-GUIDE, init module GSD project, commit + tag v0.1.0</name>
  <read_first>
    - 28-PATTERNS.md § "tests/test_import_hygiene.py (MODIFIED → relocate + re-scope)" — the gate-by-gate fate table (lines 116-134): which self-proofs carry over UNCHANGED, which RE-SCOPE to single-package `build_graph(MODULE)`, which DROP/RETARGET (the real-import `_injected_app_leak` self-proofs), and which STAYS APP-SIDE (`test_no_deferred_cycle_import...` — does NOT move)
    - 28-RESEARCH.md § "Pitfall 6" + Open Question 2 (re-scope the self-proofs; keep synthetic-dict + litmus + isolated-import self-proofs; drop/retarget the two real-import self-proofs with a documented reason in the file docstring)
    - 28-PATTERNS.md § "../YahirReusableBot/EXTENSION-GUIDE.md" (the six-plug-point table + content source docstrings) and § "../YahirReusableBot/.planning/ GSD init"
  </read_first>
  <files>../YahirReusableBot/tests/test_import_hygiene.py, ../YahirReusableBot/EXTENSION-GUIDE.md, ../YahirReusableBot/.planning/</files>
  <action>
    Relocate `tests/test_import_hygiene.py` into the module repo and RE-SCOPE it for the standalone repo (Pitfall 6 — the single most intricate test step). Per the 28-PATTERNS fate table: (a) `test_module_imports_zero_app_code` and `test_discord_adapter_imports_zero_app_code` — change `grimp.build_graph(MODULE, APP, cache_dir=None)` to single-package `grimp.build_graph(MODULE, cache_dir=None)` since `weatherbot` no longer exists in this repo; the `_scan_app_leaks` prefix scan still catches any `weatherbot.*` STRING edge. (b) DROP the two real-import self-proofs (`test_selfproof_import_gate_catches_real_app_edge`, `test_selfproof_isolated_import_catches_real_app_edge`) and the `_injected_app_leak()` helper — they write a real `import weatherbot.config.models` and rely on a real `weatherbot` package resolving, which raises `ModuleNotFoundError` here. Document WHY in the file docstring (the standalone repo has no app package; the synthetic-dict + litmus + isolated-import-blocker self-proofs still prove the gate logic bites). (c) KEEP unchanged: `test_litmus_clean`, `test_selfproof_litmus_catches_weather_noun`, `test_selfproof_import_gate_catches_injected_app_edge` (synthetic dict), `test_config_module_never_imports_pydantic` (already single-package), `_scan_app_leaks`, `_public_names`, `_AppBlocker`, the isolated-import-blocker self-proof (it evicts and re-imports a synthetic-blocked name, not a real weatherbot module — verify it still works without a real app package; if it imports a real `weatherbot.*`, retarget to a throwaway sibling fixture). (d) DO NOT relocate `test_no_deferred_cycle_import_survives_in_app_interactive` — it is an APP invariant reading `weatherbot/interactive/`; it stays in WeatherBot's suite. Author `EXTENSION-GUIDE.md` at the repo root from the 28-PATTERNS six-plug-point table (Channel/JobStore/config-schema/health-check/command-registration/SelectedContext), each marked implemented-vs-deferred; lift the durable-`JobStore` serialization contract from `yahir_reusable_bot/ports/jobstore.py`'s payload docstring (the highest-value deferred entry); mark the 2nd `Channel` adapter deferred (DOCS-01). Initialize the module's own GSD `.planning/` (PROJECT.md naming the reusable-bot module + ROADMAP/REQUIREMENTS recording durable-`JobStore` impl + 2nd `Channel` adapter as deferred extension points under build-in-consumer-then-promote / rule-of-three, D-07). Run `uv run pytest` in the module repo — the re-scoped suite must be green. Then make the single clean import commit (`feat: initial import of yahir_reusable_bot reusable bot core`) and `git tag v0.1.0`.
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/YahirReusableBot &amp;&amp; uv run pytest tests/test_import_hygiene.py -q 2>&amp;1 | tail -5; test -f EXTENSION-GUIDE.md &amp;&amp; grep -q 'JobStore' EXTENSION-GUIDE.md &amp;&amp; test -d .planning &amp;&amp; git tag | grep -q '^v0.1.0$'</automated>
  </verify>
  <done>The re-scoped `test_import_hygiene.py` is green in the standalone repo (single-package grimp build; real-import self-proofs dropped with a documented reason; the cycle-import test left app-side). `EXTENSION-GUIDE.md` documents all six plug points with implemented-vs-deferred status incl. the durable-`JobStore` serialization contract. The module `.planning/` GSD project records the two deferred extension points. The clean import commit is tagged `v0.1.0`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WeatherBot repo → new module repo | the `yahir_reusable_bot/` tree crosses into a standalone (public-ish) repo |
| build → installed wheel | hatchling packages-config decides what code ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | secrets riding into the new module repo | mitigate | The move copies `yahir_reusable_bot/` ONLY (verified: it has no `.env`/config/secrets); the `.gitignore` excludes `.venv`/build artifacts; `grep -rn` for any secret name (OPENWEATHER/DISCORD/webhook/token) over the new repo must return nothing before commit. |
| T-28-02 | Tampering | discord.py version drift breaks the live panel | mitigate | EXACT `discord.py==2.7.1` pin in the module pyproject (the live-panel wire contract); never a range. Verified by the custom_id golden in 28-04. |
| T-28-03 | Tampering | hatchling silently drops a package on the wheel-packages edit | mitigate | Build + `python -m zipfile -l dist/*.whl` confirms the wheel contains exactly `yahir_reusable_bot/` (Pitfall 7). |
| T-28-SC | Tampering | discord.py / httpx / structlog / tenacity declarations | accept | All four are pre-existing production deps being RELOCATED, not newly installed (28-RESEARCH Package Legitimacy Audit — the SUS verdicts are PyPI-metadata-gap false-positives). No install checkpoint required. |
</threat_model>

<verification>
- The `direct_url.json` spike result is recorded (Task 1) — the contract 28-03 builds on is confirmed, not assumed.
- `grep -rn weatherbot /home/yahir/Projects/YahirReusableBot/yahir_reusable_bot/` returns nothing (prose scrubbed).
- The module wheel contains exactly `yahir_reusable_bot/` (Pitfall 7 inspection).
- `uv run pytest` is green in the standalone module repo (re-scoped import-hygiene suite).
- `git tag` shows `v0.1.0` on the single clean import commit.
- `EXTENSION-GUIDE.md` + module `.planning/` exist and record the two deferred extension points (DOCS-01).
</verification>

<success_criteria>
The `YahirReusableBot` repo exists as a fresh git init with one clean import commit tagged `v0.1.0`, the module tree is scrubbed of `weatherbot.*` prose, its pyproject declares the module identity (no console script, EXACT discord.py pin), the re-scoped import-hygiene suite is green standalone, and the DOCS-01 deliverables (EXTENSION-GUIDE + module GSD init) are present. A uv git install's `direct_url.json` field shape is empirically confirmed.
</success_criteria>

<output>
Create `.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-01-SUMMARY.md` when done. RECORD the observed `direct_url.json` JSON shape from Task 1 (28-03 reads it) and note the new repo path `/home/yahir/Projects/YahirReusableBot` + the resolved `v0.1.0` commit sha.
</output>
