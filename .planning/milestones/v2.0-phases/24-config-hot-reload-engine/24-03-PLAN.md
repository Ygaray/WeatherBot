---
phase: 24-config-hot-reload-engine
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - .planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md
autonomous: true
requirements: [SEAM-04]
must_haves:
  truths:
    - "An autonomous self-UAT log is written recording, per success criterion: what was tested, the exact commands, evidence/output, and PASS/FAIL/PARTIAL"
    - "The four SEAM-04 success criteria are each driven against the wired ReloadEngine with concrete evidence (not inferred from code)"
    - "Byte-level golden evidence is captured: the reconcile-diff +a -r ~c =u, keep-old-rollback, exactly-once-across-reload, and sent_log DB-row goldens are shown byte-identical"
    - "The SIGHUP path, the file-watch path, the check-config dry-run, the bad-edit-keep-old path, and the reconcile-rollback path are each exercised with recorded output"
  prohibitions:
    - "No per-phase blocking human-verify checkpoint is emitted — Gate 1 (autonomous self-UAT) gates the phase; human UAT is a deferred Gate-2 milestone obligation (Phase 28 live-host restart)"
    - "No production code is edited in this plan — it only drives the built behavior and records evidence; a discovered FAIL escalates rather than silently patches"
    - "No golden is --snapshot-update'd to manufacture a PASS"
  artifacts:
    - path: ".planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md"
      provides: "the persistent self-UAT log (per-criterion evidence + PASS/FAIL/PARTIAL verdicts)"
      contains: "SEAM-04"
  key_links:
    - from: ".planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md"
      to: "tests/test_reload.py"
      via: "self-UAT records the exact pytest commands + byte-level golden evidence"
      pattern: "test_reload|test_filewatch"
---

<objective>
Discharge Gate 1 (autonomous agent self-UAT) for SEAM-04 per the project's Two-Gate UAT policy: build/drive the REAL reload path (SIGHUP + file-watch + check-config + bad-edit-keep-old + reconcile-rollback) against the wired `ReloadEngine`, and write a persistent self-UAT log recording — per the four SEAM-04 success criteria — what was tested, the exact commands, the evidence/output (including byte-level golden + DB-row checks), and a PASS/FAIL/PARTIAL verdict each.

A fully-passing Gate 1 is sufficient to complete the phase and proceed automatically — NO per-phase human pause. Human UAT for this seam is a deferred Gate-2 obligation, batched to the v2.0 milestone close (the live `yahir-mint` restart UAT is Phase 28 / PKG-02). Halt only to escalate a genuine FAIL with evidence.

Purpose: prove byte-identically that the extracted engine reproduces every WeatherBot reload behavior — the milestone's "tests green is necessary but not sufficient; the goldens are the oracle" contract.
Output: `.planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-config-hot-reload-engine/24-CONTEXT.md
@.planning/phases/24-config-hot-reload-engine/24-RESEARCH.md
@.planning/phases/24-config-hot-reload-engine/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive the five reload paths and capture byte-level evidence</name>
  <files>.planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md</files>
  <read_first>
    - tests/test_reload.py (the keep-old / exactly-once-across-reload / send_time-change behaviors to drive + cite)
    - tests/test_filewatch.py (the save→one-reload / .env→zero / keep-old-through-watch / watch-set-rederived behaviors)
    - tests/test_reload_engine.py (the direct-engine behaviors from Plan 01 to drive)
    - .planning/phases/24-config-hot-reload-engine/24-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; the exact per-criterion automated commands)
    - .planning/phases/21-characterization-golden-test-harness/21-CONTEXT.md (the golden oracle to show byte-identical)
  </read_first>
  <action>
Exercise the five real reload paths against the wired engine and capture the exact command + output for each:
1. SIGHUP path — drive a reload via the engine's request_reload() + service_pending() (and/or the daemon's SIGHUP-handler test) and show the `+a -r ~c =u` reconcile-diff is byte-identical to the Phase-21 golden, with `__heartbeat__`/`__uvmonitor__` untouched (no extra `-2`).
2. File-watch path — show save→one-reload, `.env`→zero-reloads, keep-old-through-watch, and watch-set-rederived (run `tests/test_filewatch.py`).
3. check-config dry-run — drive `reload_engine.check(path)` (and the `weatherbot check` CLI wrapper) on a valid and an invalid config, showing validate-only (no swap, correct exit code).
4. bad-edit-keep-old — feed a malformed config, show the holder + job set stay untouched, the `⛔ config reload rejected` post fires before the re-raise, and the daemon does not crash.
5. reconcile-rollback — force a reconcile throw, show all-or-nothing rollback (holder + jobs restored to old, original error re-raised) and the `sent_log` DB rows are byte-identical to the golden (query the actual DB, do not infer).

For the golden + DB-row checks, run the actual snapshot/DB assertions and paste the proving output (byte-level/data-level, per the project's self-UAT policy — prove the value, don't infer it from code). Run the full suite `uv run pytest` once as the suite-green datapoint. Do NOT edit production code; if any path FAILs, stop and escalate with the captured evidence.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reload.py tests/test_filewatch.py tests/test_reload_engine.py tests/test_config_holder.py tests/test_import_hygiene.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five paths has captured command + output evidence (not a code-read inference)
    - The reconcile-diff, keep-old-rollback, exactly-once-across-reload, and sent_log DB-row goldens are shown byte-identical (zero diff)
    - The named pytest selection is green
  </acceptance_criteria>
  <done>All five reload paths are driven with recorded byte-level/data-level evidence proving byte-identical behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Write the self-UAT log with per-criterion PASS/FAIL/PARTIAL verdicts</name>
  <files>.planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md L322-327 (the four SEAM-04 success criteria — the per-criterion structure of the log)
    - .planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam (any prior self-UAT log shape to mirror, if present)
  </read_first>
  <action>
Write `.planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md` as a persistent Gate-1 log. Structure it per the four SEAM-04 success criteria (ROADMAP L322-327): (1) reload drives validate→swap→reconcile + watch + SIGHUP + check-config purely through injected callables, goldens green byte-identical; (2) the module config seam knows no app field names (litmus + pydantic-isolation gates green); (3) a bad edit half-applies nothing (validate-raise keeps old; reconcile-fail rolls back all-or-nothing); (4) `[uv]`/`Location`/templates + restart-policy stay app-side (no weather schema in the module holder). For each criterion record: what was tested, the exact command(s) run, the evidence/output captured in Task 1, and a PASS / FAIL / PARTIAL verdict. Mark any step that genuinely cannot be automated (e.g. the live-host `systemctl restart`) as a deferred Gate-2 milestone obligation with verdict PARTIAL (mechanism + result verified, only the physical host step deferred) — NOT skipped. Conclude with the overall Gate-1 verdict and an explicit note that human UAT is deferred to the v2.0 milestone close (Phase 28 live `yahir-mint` restart), not a per-phase blocker.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md && grep -qE 'PASS|FAIL|PARTIAL' .planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md && grep -q 'SEAM-04' .planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - The log exists and has a per-criterion verdict for all four SEAM-04 success criteria
    - Each criterion cites the exact command + evidence (not a bare assertion)
    - The deferred Gate-2 obligation (live-host restart, Phase 28) is recorded explicitly, not silently dropped
    - The overall Gate-1 verdict is stated
  </acceptance_criteria>
  <done>A persistent self-UAT log records per-criterion evidence + verdicts; Gate 1 is discharged and Gate 2 obligations are tracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-UAT evidence → phase-completion decision | The log is the record Gate-2 human UAT later verifies against; a fabricated PASS would let a real regression ship — hence byte-level/data-level evidence is mandatory, not inferred |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Repudiation | self-UAT log | mitigate | Each verdict cites the exact command + captured output (byte-level golden + DB-row evidence), so the Gate-1 record is auditable at Gate-2 close and a PASS cannot be a bare assertion. (Two-Gate UAT policy) |
| T-24-10 | Denial of Service | bad-edit-keep-old drive (path 4) | mitigate | The self-UAT explicitly drives a malformed config and proves the daemon does not crash + the old config stays live (keep-old), the same DoS mitigation Plan 02 wires — verified here end-to-end with evidence. (D-08 / SC-3) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase — pure verification + doc; RESEARCH.md Package Legitimacy Gate = N/A. |
</threat_model>

<verification>
- `.planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md` exists with per-criterion PASS/FAIL/PARTIAL verdicts + exact commands + byte-level evidence
- The named pytest selection + full suite are green
- No production code edited; no golden blind-updated
</verification>

<success_criteria>
- Gate 1 (autonomous self-UAT) discharged for SEAM-04 with byte-level/data-level evidence across all five reload paths
- The four SEAM-04 success criteria each have a recorded verdict
- Gate-2 human UAT recorded as a deferred milestone obligation (Phase 28 live-host restart) — no per-phase human pause
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

- `.planning/phases/24-config-hot-reload-engine/24-SELF-UAT.md` — the persistent Gate-1 self-UAT log (per-criterion evidence + PASS/FAIL/PARTIAL verdicts + deferred Gate-2 obligations)
</artifacts_produced>

<output>
Create `.planning/phases/24-config-hot-reload-engine/24-03-SUMMARY.md` when done
</output>
