---
phase: 13-multi-day-forecast-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/weather/models.py
  - weatherbot/weather/multiday.py
  - tests/test_multiday.py
  - tests/fixtures/onecall_8day_imperial.json
  - tests/fixtures/onecall_8day_metric.json
autonomous: true
requirements: [FCAST-01, FCAST-02, FCAST-04, FCAST-07]
must_haves:
  truths:
    - "A weekday forecast resolves to the still-upcoming Mon-Fri days, rolling to next week's full block when the current block is exhausted."
    - "A weekend forecast resolves to the still-upcoming Fri-Sat-Sun days, rolling forward when exhausted."
    - "+day flags append days, -day flags drop default days; the final set is deduped and sorted into calendar order."
    - "A +day flag naming a date beyond the One Call today+7 horizon produces a clear notice string, never a silent drop or IndexError."
    - "Each selected day exposes per-day high/low/sky/rain (compact) plus wind/uvi/feels-high/feels-low/sunrise/sunset (detailed), with imperial-primary display byte-identical to the daily briefing."
  artifacts:
    - path: "weatherbot/weather/multiday.py"
      provides: "Pure window/roll-forward day selector select_days()"
      contains: "def select_days"
    - path: "weatherbot/weather/models.py"
      provides: "ForecastDay per-day extraction model reading daily[i]"
      contains: "class ForecastDay"
    - path: "tests/fixtures/onecall_8day_imperial.json"
      provides: "8-element synthetic daily[] fixture with known dates"
      contains: "daily"
    - path: "tests/test_multiday.py"
      provides: "window/roll-forward/horizon + extraction tests"
      contains: "def test_"
  key_links:
    - from: "weatherbot/weather/multiday.py"
      to: "weatherbot/scheduler/days.py"
      via: "import of the _DAYS token vocabulary"
      pattern: "from weatherbot.scheduler.days import|days\\._DAYS|_DAYS"
    - from: "weatherbot/weather/models.py ForecastDay"
      to: "Forecast._temp_str imperial-primary display"
      pattern: "_temp_str|°F|°C"
---

<objective>
Build the two pure, testable foundations every other Phase 13 plan consumes: the per-day
extraction model `ForecastDay` (reads `daily[i]`, mirrors `Forecast._temp_str`) and the
`multiday.select_days` window/roll-forward selector (D-01) with out-of-window notices (D-03).
Plus the single most important Wave 0 asset: an 8-element synthetic `daily[]` fixture with
known dates (existing fixtures carry only `daily[0]`).

Purpose: These are the only genuinely-new logic in the phase (RESEARCH: ~20% new). They have
no consumers yet, so they parallelize with the renderer (Plan 02) and config/flags (Plan 03).
Output: `weatherbot/weather/multiday.py`, `ForecastDay` in `weatherbot/weather/models.py`,
the 8-day fixtures, and `tests/test_multiday.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multi-day-forecast-templates/13-CONTEXT.md
@.planning/phases/13-multi-day-forecast-templates/13-RESEARCH.md
@.planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `weatherbot/weather/models.py` → new dataclass `ForecastDay` with fields: `label: str`,
  `high_imp/high_met`, `low_imp/low_met`, `sky: str`, `rain_chance: int`,
  `wind_imp/wind_met`, `uvi: float`, `feels_high_imp/feels_high_met`,
  `feels_low_imp/feels_low_met`, `sunrise: int`, `sunset: int`, `primary: str`;
  plus a classmethod `from_daily(day_imp: dict, day_met: dict, *, label: str, primary: str) -> ForecastDay`
  and a `day_tokens(detailed: bool) -> dict[str, str]` flat-map method.
- `weatherbot/weather/multiday.py` (NEW module) → `select_days(kind, today_local, daily, add, drop) -> tuple[list[int], list[str]]`
  and module constants `_WEEKDAY_DAYS = ("mon","tue","wed","thu","fri")`,
  `_WEEKEND_DAYS = ("fri","sat","sun")`.
- `tests/fixtures/onecall_8day_imperial.json`, `tests/fixtures/onecall_8day_metric.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 8-element daily[] fixtures + ForecastDay extraction model</name>
  <files>tests/fixtures/onecall_8day_imperial.json, tests/fixtures/onecall_8day_metric.json, weatherbot/weather/models.py, tests/test_multiday.py</files>
  <read_first>
    - weatherbot/weather/models.py (lines 99-276 — `Forecast`, `from_payloads` daily[0] read,
      `_temp_str` lines 228-259, `placeholders` lines 261-276, `_local_date_iso` lines 34-49)
    - tests/fixtures/onecall_imperial_clear.json (existing shape; carries only daily[0])
    - .planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md (`ForecastDay` section — verbatim `_temp_str` copy, feels-like hi/lo derivation Pitfall 3, rain/uvi extraction)
  </read_first>
  <behavior>
    - `ForecastDay.from_daily` reads a single `daily[i]` dict (imperial) + its metric twin and
      returns a populated `ForecastDay`; high=`temp.max`, low=`temp.min`, sky=`weather[0].main`,
      rain_chance=`round((pop or 0.0)*100)`, uvi=`uvi or 0.0`.
    - feels-like high = `max(feels_like.values())`, feels-like low = `min(feels_like.values())`
      over the four dayparts (day/night/eve/morn) — NOT `feels_like.max` (KeyError; Pitfall 3).
    - `day_tokens(detailed=False)` returns exactly the 4 compact keys
      {label, high, low, sky}; `day_tokens(detailed=True)` returns all 11 detailed keys
      {label, high, low, sky, rain, wind, uvi, feels_high, feels_low, sunrise, sunset}.
    - high/low display is imperial-primary byte-identical to `Forecast._temp_str`
      (e.g. `"72°F (22°C)"`); metric-primary location flips to `"22°C (72°F)"`.
    - A present-but-null `daily[i]` field coalesces (uses `.get(...) or default`), never raises.
  </behavior>
  <action>
    Create `tests/fixtures/onecall_8day_imperial.json` and `tests/fixtures/onecall_8day_metric.json`:
    copy the top-level shape of `onecall_imperial_clear.json` but make `daily` an 8-element array
    (today + 7) with deterministic, known `dt` epochs spanning 8 consecutive local days for a
    fixed timezone (use `America/New_York` to match existing fixtures), each element carrying the
    RESEARCH-verified daily shape: `dt, sunrise, sunset, temp{min,max,day,night,eve,morn},
    feels_like{day,night,eve,morn}, humidity, pop, uvi, weather[{main,description}], wind_speed,
    wind_deg, clouds`. Vary `temp.max/min`, `pop`, `weather[0].main` per day so window/extraction
    tests are distinguishable. Metric twin carries the same dates with Celsius temps.

    In `weatherbot/weather/models.py` add a `ForecastDay` dataclass (mirror the `Forecast`
    dataclass house style) with the fields listed in `<artifacts_this_phase_produces>`. Copy the
    `_temp_str` body VERBATIM (PATTERNS lines for `Forecast._temp_str`) into a `ForecastDay._temp_str`
    so per-day temps match the briefing. Add classmethod `from_daily(day_imp, day_met, *, label,
    primary="imperial")` using the defensive `.get(...) or default` read idiom from
    `from_payloads`; derive feels-like high/low as max/min over `feels_like` daypart values
    (Pitfall 3 — do NOT look for `feels_like.max`). Add `day_tokens(detailed: bool) -> dict[str,str]`
    returning the flat `str->str` map keyed to the renderer's per-day token sets
    (compact = {label,high,low,sky}; detailed adds rain,wind,uvi,feels_high,feels_low,sunrise,sunset).
    Format `rain` as `f"{rain_chance}%"`, `uvi` as the rounded UV value, wind via the existing
    wind-display idiom (speed + cardinal from `wind_deg`), `sunrise`/`sunset` as local `HH:MM`
    in the location tz (reuse `ZoneInfo` + epoch-local conversion — same `_epoch_local` idiom as
    `weather_views`). Day-label assignment ("Today"/"Tomorrow"/`"Wed 6/25"`) is the caller's job
    (Plan 04/05), so `from_daily` takes `label` as a parameter — do not compute it here.
    NO fenced template logic; this is pure Python.
  </action>
  <verify>
    <automated>uv run pytest tests/test_multiday.py -k "forecastday or extract or feels or temp_display" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ForecastDay" weatherbot/weather/models.py` returns a match.
    - `python -c "import json; d=json.load(open('tests/fixtures/onecall_8day_imperial.json')); assert len(d['daily'])==8"` exits 0.
    - Test asserting `ForecastDay.from_daily(...).day_tokens(detailed=False).keys()` == {label,high,low,sky} passes.
    - Test asserting detailed `day_tokens` has exactly 11 keys passes.
    - Test asserting imperial-primary high display equals `Forecast._temp_str` output for the same values passes.
    - Test asserting `feels_high` == max of the four `feels_like` dayparts passes (no KeyError on missing `feels_like.max`).
  </acceptance_criteria>
  <done>8-day imperial+metric fixtures exist with 8 dated daily entries; `ForecastDay` extracts every per-day field defensively, derives feels-like hi/lo from dayparts, and renders imperial-primary temps identically to `Forecast`. Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: multiday.select_days window/roll-forward selector with horizon notices</name>
  <files>weatherbot/weather/multiday.py, tests/test_multiday.py</files>
  <read_first>
    - weatherbot/scheduler/days.py (the whole file — `_DAYS` line 25, `parse_days`; the dependency-free pure-module pattern to mirror)
    - weatherbot/weather/models.py (`_local_date_iso` lines 34-49 — the IANA-tz "today" authority idiom)
    - .planning/phases/13-multi-day-forecast-templates/13-RESEARCH.md (Pitfall 1, 2, 6; "Window selector signature" code example)
    - .planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md (`multiday.py` section — never index daily[] positionally; resolve date->index)
    - tests/fixtures/onecall_8day_imperial.json (from Task 1)
  </read_first>
  <behavior>
    - `select_days("weekday", today, daily, add=set(), drop=set())` on a Monday returns indices
      for the in-window Mon-Fri days in calendar order, no notices.
    - Run mid-block (e.g. Wednesday) → only the still-upcoming days (Wed-Fri); past days dropped.
    - Block fully exhausted (weekday run on Saturday) → rolls to next week's full Mon-Fri block;
      any next-week day beyond the today+7 horizon becomes a notice, not a crash.
    - `add={"sat"}` on a weekday forecast appends Saturday if in window; if Saturday is beyond
      the horizon, it is returned as a notice string (Pitfall 2), not silently dropped.
    - `drop={"mon"}` removes Monday from the base weekday set; result deduped and calendar-sorted.
    - Adding a day already in the base range is a no-op (dedup).
    - Each desired calendar date is mapped to a `daily[]` index by matching the LOCAL date of
      `daily[i].dt` (never positional day-of-week math; Pitfall 1).
  </behavior>
  <action>
    Create `weatherbot/weather/multiday.py` as a dependency-free pure module (mirror
    `scheduler/days.py` — no config/apscheduler/store import) so it stays acyclic and unit-testable.
    Import `_DAYS` from `weatherbot.scheduler.days` as the canonical `add`/`drop` token vocabulary
    (Don't Hand-Roll — one source of truth; A4: abbreviations only, presets are NOT valid flag
    tokens). Define module constants `_WEEKDAY_DAYS = ("mon","tue","wed","thu","fri")` and
    `_WEEKEND_DAYS = ("fri","sat","sun")`.

    Implement `select_days(kind: str, today_local: date, daily: list[dict], add: set[str],
    drop: set[str], tz) -> tuple[list[int], list[str]]` per RESEARCH's signature and docstring:
    (1) base day set = `_WEEKDAY_DAYS`/`_WEEKEND_DAYS` by kind (raise `ValueError` on unknown kind,
    fail-loud); apply `drop` then `add`; dedup; (2) compute each base day's concrete calendar date
    starting from `today_local`, choosing the still-upcoming occurrence; if EVERY base day is in
    the past, roll the whole block to next week; (3) sort desired dates into calendar order; (4) map
    each desired date to its `daily[]` index by converting each `daily[i]["dt"]` (UTC epoch) to a
    date in `tz` (`ZoneInfo`) and matching — desired dates with no in-window match become notice
    strings like `f"{label} is beyond the 7-day forecast horizon"` (Pitfall 2), collected in the
    second tuple element. NEVER index `daily[]` positionally by weekday math (Pitfall 1 — IndexError
    / silent wrap). Use the configured `tz` for "today" and all date resolution (Pitfall 6 — never
    `date.today()` or the API timezone field). NO fenced template logic; pure date arithmetic.
  </action>
  <verify>
    <automated>uv run pytest tests/test_multiday.py -k "weekday or weekend or flag or roll or horizon or notice" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def select_days" weatherbot/weather/multiday.py` returns a match.
    - `grep -n "from weatherbot.scheduler.days import\|days._DAYS\|_DAYS" weatherbot/weather/multiday.py` returns a match (reuses the day vocabulary).
    - `grep -c "date.today()" weatherbot/weather/multiday.py | grep -qx 0` (no naive today).
    - Test: weekday run on a fixture Monday returns 5 indices, 0 notices (FCAST-01).
    - Test: weekend run returns the Fri-Sat-Sun indices (FCAST-02).
    - Test: `add={"sat"}` beyond the horizon returns a notice string, not a dropped day (FCAST-04, Pitfall 2).
    - Test: weekday run on a fixture Saturday rolls forward to next week and never raises IndexError.
    - Test: `-mon +sat` yields a deduped, calendar-sorted index list.
  </acceptance_criteria>
  <done>`select_days` returns in-window calendar-ordered indices plus out-of-window notices, resolving dates against the actual `daily[]` array (no positional indexing), reusing `days._DAYS`, and computing "today" in the configured IANA tz. All window/roll-forward/flag/horizon tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| One Call payload → ForecastDay | Untrusted external JSON parsed into per-day fields (null/missing keys possible) |
| (none for multiday) | `select_days` operates only on already-fetched in-process data + a configured tz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `ForecastDay.from_daily` reading `daily[i]` | mitigate | Defensive `.get(...) or default` coalescing on every field; no `feels_like.max` access (Pitfall 3); malformed/null fields degrade, never raise |
| T-13-02 | Denial of Service | `select_days` indexing `daily[]` near the horizon edge | mitigate | Resolve date→index by matching local dates, never positional math; out-of-window dates become notices, never IndexError (Pitfall 1/2) |
| T-13-03 | Tampering | unknown `kind` reaching `select_days` | mitigate | Fail-loud `ValueError` on any kind not in {weekday, weekend} |
</threat_model>

<verification>
- `uv run pytest tests/test_multiday.py -x -q` green.
- `uv run pytest -q` full suite green (no regression in models/scheduler tests that import the module).
- `grep -c "import" weatherbot/weather/multiday.py` shows no config/apscheduler/store import (acyclic, pure).
</verification>

<success_criteria>
- `ForecastDay` extracts hi/lo/sky/rain (+ wind/uvi/feels/sun for detailed) from any `daily[i]`,
  imperial-primary display matches the briefing, feels-like hi/lo derived from dayparts.
- `select_days` implements D-01 (remaining-days/roll-forward) and D-03 (additive/subtractive flags
  + out-of-window notices), bounded by the today+7 horizon, in the configured IANA tz.
- 8-element fixtures exist and drive deterministic window tests.
</success_criteria>

<output>
Create `.planning/phases/13-multi-day-forecast-templates/13-01-SUMMARY.md` when done.
</output>
