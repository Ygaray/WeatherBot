---
phase: 13-multi-day-forecast-templates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/interactive/command.py
  - weatherbot/config/models.py
  - tests/test_flags.py
  - tests/test_config.py
autonomous: true
requirements: [FCAST-03, FCAST-04, FCAST-06]
must_haves:
  truths:
    - "An on-demand forecast argument string parses into a variant (detailed default / compact via +compact|--compact), an add-day set, a drop-day set, and a location."
    - "+day/-day tokens accept only the weekday abbreviations (mon..sun); an unknown flag token fails loud."
    - "The flag parser uses only str ops — never str.format/eval/exec/shell."
    - "A per-location forecast schedule slot (kind/variant/time/days/enabled) is a frozen, fail-loud config model; an absent [[locations.forecast]] table loads as an empty list."
  artifacts:
    - path: "weatherbot/interactive/command.py"
      provides: "parse_forecast_flags() + frozen ForecastFlags result"
      contains: "def parse_forecast_flags"
    - path: "weatherbot/config/models.py"
      provides: "ForecastSchedule model + Location.forecast list"
      contains: "class ForecastSchedule"
    - path: "tests/test_flags.py"
      provides: "+day/-day/+compact grammar tests"
      contains: "def test_"
  key_links:
    - from: "weatherbot/interactive/command.py parse_forecast_flags"
      to: "weatherbot/scheduler/days._DAYS"
      via: "import of the day-token vocabulary"
      pattern: "from weatherbot.scheduler.days import|_DAYS"
    - from: "weatherbot/config/models.py ForecastSchedule"
      to: "Schedule validators (_hhmm, parse_days)"
      via: "reuses the HH:MM + days validators"
      pattern: "parse_days|parsed_time|day_of_week"
---

<objective>
Two disjoint, consumer-free contracts the on-demand (Plan 04) and scheduled (Plan 05) paths both
need: (1) a shared `+day`/`-day`/`+compact` flag grammar in `command.py` returning a frozen
`ForecastFlags` (CLI and Discord parse the same way — Phase 6 shared-core principle), and (2) the
`ForecastSchedule` config model + `Location.forecast` list (separate frozen model, NOT extending
`Schedule`, so briefing/forecast job ids never collide).

Purpose: Defines the parse + config contracts before either surface consumes them (interface-first).
Touches command.py + config/models.py only — disjoint from Plan 01 (models/multiday) and Plan 02
(renderer/templates), so it runs Wave 1 in parallel.
Output: `parse_forecast_flags` + `ForecastFlags` in `command.py`; `ForecastSchedule` +
`Location.forecast` in `config/models.py`; `tests/test_flags.py` + extended `tests/test_config.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multi-day-forecast-templates/13-CONTEXT.md
@.planning/phases/13-multi-day-forecast-templates/13-RESEARCH.md
@.planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `weatherbot/interactive/command.py` → frozen dataclass `ForecastFlags` with fields
  `variant: str` ("detailed"|"compact"), `add: frozenset[str]`, `drop: frozenset[str]`,
  `location: str | None`; plus `parse_forecast_flags(arg: str | None) -> ForecastFlags`.
- `weatherbot/config/models.py` → `ForecastSchedule(BaseModel)` with `kind: str`
  ("weekday"|"weekend"), `variant: str = "detailed"`, `time: str`, `days: str`,
  `enabled: bool = True`, `parsed_time()`, `day_of_week` property; and a new
  `Location.forecast: list[ForecastSchedule] = Field(default_factory=list)`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared +day/-day/+compact flag grammar (parse_forecast_flags)</name>
  <files>weatherbot/interactive/command.py, tests/test_flags.py</files>
  <read_first>
    - weatherbot/interactive/command.py (the whole file — module docstring T-06-01 security contract lines 8-15; `ParsedCommand` frozen dataclass lines 76-87; `parse_command` lines 90-113 — the parse-don't-validate / casefold / slicing idiom)
    - weatherbot/scheduler/days.py (`_DAYS` line 25 — the only valid +day/-day tokens; A4 abbreviations-only)
    - .planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md (command.py section — frozen result + COPY the security contract)
    - .planning/phases/13-multi-day-forecast-templates/13-CONTEXT.md (D-02 variant default detailed; D-03 multiple add+subtract flags, dedup, calendar order is the selector's job)
  </read_first>
  <behavior>
    - `parse_forecast_flags("home +compact")` → variant="compact", add=∅, drop=∅, location="home".
    - `parse_forecast_flags("home --compact")` → variant="compact" (both spellings; +compact for Discord, --compact for CLI).
    - `parse_forecast_flags("home +sat +sun")` → add={"sat","sun"}, variant="detailed" (default).
    - `parse_forecast_flags("home -mon +sat")` → drop={"mon"}, add={"sat"}.
    - `parse_forecast_flags(None)` → variant="detailed", add=∅, drop=∅, location=None.
    - An unknown day token (e.g. `+xyz`) raises a fail-loud `ValueError` naming the allowed tokens.
    - Tokens are case-insensitive; the LOCATION substring keeps its raw case (mirror parse_command).
    - Parsing uses only str ops — no str.format/eval/exec/shell (T-06-01).
  </behavior>
  <action>
    In `weatherbot/interactive/command.py` add a frozen `ForecastFlags` dataclass (mirror
    `ParsedCommand`'s `@dataclass(frozen=True)` house style) with fields `variant: str`,
    `add: frozenset[str]`, `drop: frozenset[str]`, `location: str | None`. Add
    `parse_forecast_flags(arg: str | None) -> ForecastFlags`: split `arg` on whitespace, classify
    each token — `+compact`/`--compact` → variant="compact"; `+detailed`/`--detailed` →
    variant="detailed" (default when neither given); `+<day>` → add; `-<day>` → drop (validate the
    `<day>` against `_DAYS` imported from `weatherbot.scheduler.days`, raising `ValueError` listing
    `sorted(_DAYS)` on an unknown token — fail loud, A4 abbreviations-only); any remaining
    non-flag token(s) form the location substring (raw case preserved, joined with spaces).
    COPY the T-06-01 security contract: ONLY `str.strip`/`str.casefold`/`str.split`/slicing — NEVER
    `str.format`/`eval`/`exec`/shell. Dedup/calendar-sort of the day set is NOT done here (that is
    `multiday.select_days`' job, Plan 01) — this only collects raw `add`/`drop` sets. NO fenced code.
  </action>
  <verify>
    <automated>uv run pytest tests/test_flags.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def parse_forecast_flags\|class ForecastFlags" weatherbot/interactive/command.py` returns 2 matches.
    - `grep -n "from weatherbot.scheduler.days import\|_DAYS" weatherbot/interactive/command.py` returns a match (reuses day vocabulary).
    - `grep -c "str.format\|eval(\|exec(\|subprocess\|os.system" weatherbot/interactive/command.py` (filtered for code lines, `grep -v '^\s*#'`) shows no new occurrence.
    - Test: `+compact` and `--compact` both yield variant="compact".
    - Test: `+sat +sun` yields add={"sat","sun"}; `-mon` yields drop={"mon"}.
    - Test: `+xyz` raises ValueError naming the allowed tokens.
    - Test: `None` arg → detailed/empty/empty/None.
  </acceptance_criteria>
  <done>`parse_forecast_flags` returns a frozen `ForecastFlags` carrying variant + add/drop day sets + location, validates day tokens against `days._DAYS` fail-loud, uses only string ops, and defaults to the detailed variant. Tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ForecastSchedule config model + Location.forecast list</name>
  <files>weatherbot/config/models.py, tests/test_config.py</files>
  <read_first>
    - weatherbot/config/models.py (`Schedule` lines 31-79 — `model_config` line 45, `_hhmm` lines 51-61, `_days_valid` lines 63-69, `parsed_time` lines 71-74, `day_of_week` lines 76-79; `Location` lines 82-135, `schedule` field line 107, `_units_valid` enum idiom lines 130-135)
    - weatherbot/scheduler/days.py (`parse_days` — reused by the days validator)
    - .planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md (config/models.py section — separate model, copy Schedule validators verbatim)
    - .planning/phases/13-multi-day-forecast-templates/13-RESEARCH.md (Alternatives Considered — why a separate model, not extending Schedule)
  </read_first>
  <behavior>
    - A `[[locations.forecast]]` TOML table with kind/variant/time/days/enabled loads into a
      frozen `ForecastSchedule`.
    - `kind` accepts only "weekday"|"weekend"; `variant` only "detailed"|"compact" — anything else
      fails loud at load (field_validator raises).
    - `time` reuses the `Schedule` HH:MM validator; `days` reuses `parse_days` (presets + comma list).
    - `parsed_time()` and `day_of_week` behave identically to `Schedule`.
    - A location with NO `[[locations.forecast]]` table loads with `forecast == []` (zero migration).
    - `extra="forbid"` rejects an unknown key in the forecast table.
    - The model is `frozen=True` (ConfigHolder snapshot compatible).
  </behavior>
  <action>
    In `weatherbot/config/models.py` add a `ForecastSchedule(BaseModel)` with
    `model_config = ConfigDict(extra="forbid", frozen=True)` and fields `kind: str`,
    `variant: str = "detailed"`, `time: str`, `days: str`, `enabled: bool = True`. Reuse the
    `Schedule` validators VERBATIM: copy `_hhmm` for `time`, the `parse_days`-backed `_days_valid`
    for `days`, and the `parsed_time()`/`day_of_week` accessors. Add two `field_validator`s using
    the `_units_valid` enum idiom: `kind` ∈ {"weekday","weekend"} and `variant` ∈
    {"detailed","compact"}, each raising `ValueError` listing the allowed values (fail loud).
    On `Location` add `forecast: list[ForecastSchedule] = Field(default_factory=list)` (next to the
    existing `schedule` field) so an absent table loads as `[]`. Keep `Location` frozen. Do NOT
    extend `Schedule` with kind/variant fields (RESEARCH Alternatives — separate model avoids
    job-id collision and a polluted briefing slot). NO fenced code in the plan.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -k "forecast or schedule" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ForecastSchedule" weatherbot/config/models.py` returns a match.
    - `grep -n "forecast: list\[ForecastSchedule\]" weatherbot/config/models.py` returns a match.
    - `grep -n 'ConfigDict(extra="forbid", frozen=True)' weatherbot/config/models.py` shows the new model carries it.
    - Test: a TOML `[[locations.forecast]]` with valid kind/variant/time/days loads; `forecast[0].day_of_week` matches the `parse_days` normalization.
    - Test: `kind="weekly"` (invalid) raises at load.
    - Test: `variant="fancy"` (invalid) raises at load.
    - Test: a location with no forecast table loads `forecast == []`.
    - Test: an unknown key in the forecast table raises (extra="forbid").
  </acceptance_criteria>
  <done>`ForecastSchedule` is a frozen, fail-loud model reusing the `Schedule` time/days validators and accessors, with enum-validated kind/variant; `Location.forecast` defaults to an empty list so absent config is zero-migration. Tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user command string → ForecastFlags | Untrusted free text from CLI argv / Discord message |
| config.toml → ForecastSchedule | User-editable config file values reaching the scheduler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering / EoP | flag string reaching `parse_forecast_flags` | mitigate | Parse-don't-validate: only `str.strip`/`casefold`/`split`/slicing; never `str.format`/`eval`/`exec`/shell (T-06-01); unknown day token fails loud (V5 input validation) |
| T-13-08 | Tampering / DoS | malformed `kind`/`variant`/`time`/`days` in a forecast slot | mitigate | Fail-loud pydantic `field_validator`s at load + reload keep-old; a rejected config never swaps (same as `Schedule`) |
| T-13-09 | Tampering | unknown key in `[[locations.forecast]]` | mitigate | `extra="forbid"` rejects typo'd/unexpected keys at load |
</threat_model>

<verification>
- `uv run pytest tests/test_flags.py tests/test_config.py -x -q` green.
- `uv run pytest -q` full suite green (ConfigHolder snapshot compatibility unbroken — frozen models).
- `grep -c "class Schedule" weatherbot/config/models.py` still 1 (Schedule not mutated to carry forecast fields).
</verification>

<success_criteria>
- A shared, injection-safe `+day`/`-day`/`+compact` grammar (FCAST-03 variant select + FCAST-04
  additive/subtractive flags) returns a frozen result both surfaces consume identically.
- `ForecastSchedule` + `Location.forecast` (FCAST-06) is a separate frozen, fail-loud config model,
  reusing the day/time validators, with an absent table loading as empty.
</success_criteria>

<output>
Create `.planning/phases/13-multi-day-forecast-templates/13-03-SUMMARY.md` when done.
</output>
