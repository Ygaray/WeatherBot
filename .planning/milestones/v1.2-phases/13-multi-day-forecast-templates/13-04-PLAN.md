---
phase: 13-multi-day-forecast-templates
plan: 04
type: execute
wave: 2
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - weatherbot/interactive/commands/forecast.py
  - weatherbot/interactive/lookup.py
  - weatherbot/interactive/registry.py
  - weatherbot/interactive/cache.py
  - weatherbot/interactive/bot.py
  - weatherbot/cli.py
  - weatherbot/interactive/__init__.py
  - tests/test_forecast_lookup.py
  - tests/test_registry.py
autonomous: true
requirements: [FCAST-01, FCAST-02, FCAST-03, FCAST-04, FCAST-05, FCAST-07]
must_haves:
  truths:
    - "A user runs weekday-forecast/weekend-forecast on the CLI and the Discord bot and gets a multi-day forecast for a configured location."
    - "On demand the variant (detailed default / compact) and +day/-day flags are honored end-to-end."
    - "The on-demand forecast path writes NOTHING to the SQLite store (no store import, no db_path)."
    - "The forecast reuses the already-fetched One Call daily[] — no extra OpenWeather call beyond the existing dual fetch (FCAST-07)."
    - "An out-of-window +day flag surfaces a clear notice in the rendered output, not a silent drop."
  artifacts:
    - path: "weatherbot/interactive/commands/forecast.py"
      provides: "weekday_forecast/weekend_forecast read-only handlers returning CommandReply"
      contains: "def weekday_forecast"
    - path: "weatherbot/interactive/lookup.py"
      provides: "forecast lookup path reusing the dual One Call fetch"
      contains: "forecast"
    - path: "weatherbot/interactive/registry.py"
      provides: "weekday-forecast/weekend-forecast specs wired (lazy)"
      contains: "weekday-forecast"
    - path: "tests/test_forecast_lookup.py"
      provides: "read-only no-store-write spy + no-extra-fetch tests"
      contains: "def test_"
  key_links:
    - from: "weatherbot/interactive/commands/forecast.py"
      to: "weatherbot/weather/multiday.select_days + ForecastDay + render_forecast"
      via: "selects days, builds per-day tokens, renders"
      pattern: "select_days|ForecastDay|render_forecast"
    - from: "weatherbot/interactive/registry.py _wire_handlers"
      to: "weatherbot/interactive/commands/forecast.py"
      via: "lazy import + handlers entry"
      pattern: "forecast"
    - from: "weatherbot/cli.py + bot.py dispatch"
      to: "spec.name forecast special-casing (flags threaded)"
      pattern: "weekday-forecast|weekend-forecast|forecast"
---

<objective>
Wire the on-demand forecast surface: a read-only `forecast.py` handler that consumes the Plan-01
selector + extraction model and the Plan-02 renderer, a forecast lookup path reusing the existing
dual One Call fetch (FCAST-07), the two registry specs (derive-from-one-list → both surfaces), a
widened `ForecastCache` key so forecast/variant/flags never collide with a `!weather` result (A5),
and the CLI+Discord dispatch threading the Plan-03 flags. Zero store writes (FCAST-05).

Purpose: Delivers FCAST-01/02/03/04 on demand from both surfaces with FCAST-05 (read-only) and
FCAST-07 (no extra fetch) enforced by tests. Depends on Plans 01/02/03 (Wave 2).
Output: `forecast.py` handler, forecast lookup path, registry specs, widened cache key, dispatch
wiring, `tests/test_forecast_lookup.py`, extended `tests/test_registry.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multi-day-forecast-templates/13-CONTEXT.md
@.planning/phases/13-multi-day-forecast-templates/13-RESEARCH.md
@.planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md
@.planning/phases/12-command-registry-read-only-command-surface/12-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `weatherbot/interactive/commands/forecast.py` (NEW) → `weekday_forecast(result, flags) -> CommandReply`
  and `weekend_forecast(result, flags) -> CommandReply` (read-only; build day labels + ForecastDay
  per selected index + render via `render_forecast`).
- `weatherbot/interactive/lookup.py` → a forecast lookup path (reuses the dual `fetch_onecall`
  imperial+metric pair already in `lookup_weather`; no new endpoint).
- `weatherbot/interactive/registry.py` → two `CommandSpec`s ("weekday-forecast","weekend-forecast",
  group "Forecast", `takes_location=True`) added to `_SPECS` and wired lazily in `_wire_handlers`.
- `weatherbot/interactive/cache.py` → widened `ForecastCache.lookup` key (location.id + command +
  variant + flags suffix).
- CLI flag `weekday-forecast [location] [+compact|--compact] [+day/-day ...]` and Discord
  `!weekday-forecast ...` (and weekend) dispatchable.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read-only forecast lookup path + forecast.py handler</name>
  <files>weatherbot/interactive/commands/forecast.py, weatherbot/interactive/lookup.py, tests/test_forecast_lookup.py</files>
  <read_first>
    - weatherbot/interactive/lookup.py (`lookup_weather` lines 78-150 — dual fetch lines 115-123, template validate-at-load lines 125-131, lazy build_client lines 104-113, read-only contract lines 12-21)
    - weatherbot/interactive/commands/weather_views.py (handler contract lines 1-25; `_tz_for` line 88; `alerts` line 92 / `next_cloudy` line 181 — extra-arg precedent; reads `result.forecast.raw_onecall_imp`)
    - weatherbot/interactive/commands/__init__.py (`CommandReply` shape lines 21-35)
    - weatherbot/weather/models.py (`ForecastDay` from Plan 01; `Forecast.raw_onecall_imp`/`raw_onecall_met` lines 135-136)
    - weatherbot/weather/multiday.py (`select_days` from Plan 01)
    - templates/renderer.py (`render_forecast` + token sets from Plan 02)
    - weatherbot/interactive/command.py (`ForecastFlags` from Plan 03)
    - .planning/phases/13-multi-day-forecast-templates/13-PATTERNS.md (forecast.py + lookup.py sections; D-04 day labels; Pitfall 6 tz)
  </read_first>
  <behavior>
    - `weekday_forecast(result, flags)` reads `result.forecast.raw_onecall_imp`/`raw_onecall_met`
      (already-fetched `daily[]` — NEVER a second fetch), computes today's local date via
      `ZoneInfo(result.location.timezone)`, calls `select_days("weekday", today, daily_imp,
      flags.add, flags.drop, tz)`, builds a `ForecastDay` per selected index (imperial+metric twin),
      assigns labels ("Today"/"Tomorrow" for the first two by local-date diff, then `f"{abbr} {m}/{d}"`),
      loads the variant template + sibling line-format, and returns a `CommandReply(title=..., text=rendered)`.
    - `weekend_forecast` is identical with kind="weekend".
    - The compact variant selects the `*-compact.txt`/`.line.txt` pair and the compact day-token map;
      detailed selects the detailed pair (FCAST-03).
    - Out-of-window flag notices from `select_days` render into the `{notice}` token (D-03).
    - The forecast lookup path imports NOTHING from `weatherbot.weather.store`, takes no write
      `db_path`, and reuses the existing dual `fetch_onecall` — no new endpoint (FCAST-05/07).
    - Day labels use an explicit f-string, NOT `strftime("%-m/%-d")` (glibc-specific; Pitfall/State-of-the-Art).
  </behavior>
  <action>
    Add a forecast lookup path to `weatherbot/interactive/lookup.py` that returns a `LookupResult`
    carrying the already-fetched `Forecast` (reuse `lookup_weather`'s dual `fetch_onecall` imperial+
    metric — do NOT add a One Call call, do NOT touch `weather/client.py`; FCAST-07). The forecast
    handler may consume the SAME `LookupResult.forecast` the cache already holds — the lookup path
    only needs to ensure the payload is fetched once; reuse `lookup_weather` itself if it already
    fetches both units. Keep the read-only contract (no `db_path`, no store import).

    Create `weatherbot/interactive/commands/forecast.py` (mirror `weather_views.py` module contract:
    read-only, no store import, returns a surface-agnostic `CommandReply`). Implement
    `weekday_forecast(result, flags)` and `weekend_forecast(result, flags)`: read
    `result.forecast.raw_onecall_imp`/`raw_onecall_met` (`or {}` defensive), get
    `tz = ZoneInfo(result.location.timezone)`, compute `today_local`, call
    `multiday.select_days(kind, today_local, daily_imp, flags.add, flags.drop, tz)`; for each index
    build `ForecastDay.from_daily(daily_imp[i], daily_met[i], label=<computed>, primary=...)` and its
    `day_tokens(detailed=flags.variant=="detailed")`; pick the template pair by (kind, variant) and
    `load_template` both the `.txt` and `.line.txt`; call `render_forecast(template_text, line_fmt,
    day_token_maps, header_values, day_allowed)` where `header_values` carries `location`/`title`/
    `range_label`/`footer_note`/`notice` (notice = joined out-of-window notices or empty) and
    `day_allowed` is `FORECAST_DAY_TOKENS_DETAILED`/`_COMPACT`. Compute labels: first two selected
    days → "Today"/"Tomorrow" by local-date diff from `today_local`, rest → `f"{abbr} {dt.month}/{dt.day}"`
    (explicit f-string, NOT `%-m/%-d`). Return `CommandReply(title=..., text=rendered)`. Wrap any
    formatting in the existing handler discipline (no raw traceback leak). NO fenced code.
  </action>
  <verify>
    <automated>uv run pytest tests/test_forecast_lookup.py -k "no_store or readonly or no_extra_fetch or weekday or weekend or compact or notice" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def weekday_forecast\|def weekend_forecast" weatherbot/interactive/commands/forecast.py` returns 2 matches.
    - `grep -c "weatherbot.weather.store\|from weatherbot.weather import store\|import store" weatherbot/interactive/commands/forecast.py | grep -qx 0` (no store import — FCAST-05).
    - `grep -c "strftime" weatherbot/interactive/commands/forecast.py | grep -qx 0` (explicit f-string labels).
    - Test (reuse Phase-6 zero-store-writes spy): the forecast path writes none of the store functions (FCAST-05).
    - Test: a forecast lookup asserts `client.fetch_onecall` call count is unchanged vs. a plain `lookup_weather` (no extra fetch — FCAST-07).
    - Test: `weekday_forecast` over the 8-day fixture returns a `CommandReply.text` with one line per selected weekday (FCAST-01).
    - Test: `weekend_forecast` returns the Fri-Sat-Sun lines (FCAST-02).
    - Test: compact variant produces shorter per-day lines (only label/hi/lo/sky) than detailed (FCAST-03).
    - Test: a `+sat` beyond the horizon renders the notice text into the reply (D-03).
  </acceptance_criteria>
  <done>`forecast.py` exposes read-only weekday/weekend handlers that select in-window days, extract per-day data from the already-fetched `daily[]`, render the chosen variant via `render_forecast`, and surface out-of-window notices — writing nothing to the store and triggering no extra fetch. Tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Register forecast commands + widen cache key + thread flags through CLI/Discord dispatch</name>
  <files>weatherbot/interactive/registry.py, weatherbot/interactive/cache.py, weatherbot/interactive/bot.py, weatherbot/cli.py, weatherbot/interactive/__init__.py, tests/test_registry.py, tests/test_cli.py, tests/test_bot.py</files>
  <read_first>
    - weatherbot/interactive/registry.py (`_SPECS` lines 50-63, `_wire_handlers` lines 66-89, `COMMANDS_BY_KEYWORD_LEN_DESC` lines 100-102 — lazy wiring + longest-keyword-first)
    - weatherbot/interactive/cache.py (`ForecastCache.lookup` lines 82-108 — key line 93 `resolve_location(config, name).id`; lock discipline lines 95-108; `invalidate` lines 110-114)
    - weatherbot/cli.py (lines 593-615 registry dispatch envelope; lines 602-605 `next-cloudy` extra-arg special-casing; subparser-gen loop ~794-805)
    - weatherbot/interactive/bot.py (lines 199-239 dispatch; lines 227-230 `next-cloudy` special-casing)
    - weatherbot/interactive/command.py (`parse_forecast_flags`/`ForecastFlags` from Plan 03)
    - .planning/phases/12-command-registry-read-only-command-surface/12-03-SUMMARY.md (derive-from-one-list invariant; render_text/render_embed)
    - .planning/phases/13-multi-day-forecast-templates/13-RESEARCH.md (A5 cache-key collision; Open Question 2 recommendation)
  </read_first>
  <behavior>
    - `weekday-forecast` and `weekend-forecast` appear in `help` on BOTH surfaces (derive-from-one-list).
    - `weatherbot weekday-forecast home +compact +sat` (CLI) and `!weekday-forecast home +compact +sat`
      (Discord) both parse the location+flags via `parse_forecast_flags` and render the forecast.
    - The longest-keyword-first parser matches `weekday-forecast` before any shorter prefix.
    - `ForecastCache` serves a repeated identical forecast command from memory but never collides a
      `!weather home` result with a `!weekday-forecast home --compact +sat` result (distinct key suffix, A5).
    - A handler failure stays inside the existing non-propagating envelope (CMD-16) — never crashes
      dispatch and never leaks a traceback/secret.
  </behavior>
  <action>
    In `registry.py` add two specs to `_SPECS`: `CommandSpec("weekday-forecast","Forecast",
    "Multi-day weekday (Mon-Fri) forecast.", True)` and `CommandSpec("weekend-forecast","Forecast",
    "Multi-day weekend (Fri-Sat-Sun) forecast.", True)`. In `_wire_handlers` add a LAZY
    `from weatherbot.interactive.commands import forecast` and `handlers["weekday-forecast"] =
    forecast.weekday_forecast`, `handlers["weekend-forecast"] = forecast.weekend_forecast`
    (keep the import inside the function — acyclic discipline). Export the forecast handlers/flags
    surface from `interactive/__init__.py` if the existing export block lists handlers.

    In `cache.py` widen `ForecastCache.lookup` to accept an optional key-suffix
    (command name + variant + sorted flags) so the cache key becomes
    `(resolve_location(config, name).id, suffix)` — repeated identical forecast commands hit cache,
    distinct command/variant/flags never collide (A5). Preserve the lock discipline (lock only around
    the dict get/store; the network fetch stays OUTSIDE the lock) and the `invalidate()` reload hook.

    In `cli.py` and `bot.py`, special-case `spec.group == "Forecast"` (or `spec.name` endswith
    "-forecast") in the dispatch the SAME way `next-cloudy` is special-cased: parse the raw arg via
    `parse_forecast_flags`, resolve+fetch via the shared lookup (off-loop on Discord via
    `run_in_executor`), call the handler with `(result, flags)`, and render via the existing
    `render_text` (CLI) / `render_embed` (Discord) — the WHOLE dispatch stays inside the existing
    non-propagating envelope (CMD-16; do not add a second envelope). The subparser-gen loop already
    creates a `location nargs="?"` arg for `takes_location` specs; ensure the forecast subparsers
    also accept the trailing `+flag`/`-flag` tokens (treat unparsed extras as part of the location/
    flag arg string handed to `parse_forecast_flags`). NO fenced code.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py tests/test_cli.py tests/test_bot.py -k "forecast" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "weekday-forecast\|weekend-forecast" weatherbot/interactive/registry.py` returns 2+ (both specs present + wired).
    - Test: `registry.BY_NAME["weekday-forecast"].handler` is callable and equals `forecast.weekday_forecast`.
    - Test (derive-from-one-list): `help` output on the CLI lists both forecast commands (CMD-09 parity).
    - Test: the CLI `weekday-forecast <loc> +compact` dispatch parses flags and prints a forecast (exit 0).
    - Test: the Discord dispatch for `!weekend-forecast <loc> +sat` returns an embed/CommandReply.
    - Test (cache, A5): a `lookup` for `weekday-forecast`+compact and a `lookup` for a plain weather command on the same location return distinct cached entries (no collision).
    - Test (CMD-16): a raising forecast handler does NOT propagate out of `on_message` and yields the generic reply.
  </acceptance_criteria>
  <done>Both forecast commands are registry-registered, wired lazily, appear in help on both surfaces, dispatch through the existing envelope with flags threaded via `parse_forecast_flags`, and the cache key is widened so forecast/variant/flag results never collide with a weather lookup. Tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord/CLI command input → forecast dispatch | Untrusted free text (location + flags) crosses into the handler |
| forecast handler → SQLite store | Read-only path must never cross into a store write (FCAST-05) |
| forecast handler → Discord channel | Rendered forecast posts to a channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | EoP / Access Control | forecast commands bypassing the guard ladder | mitigate | Forecast dispatch sits INSIDE the existing Phase-12 operator-id / `!`-prefix / non-propagating envelope (V4); no new bypass path — same ladder as `!weather` (CMD-16) |
| T-13-11 | Tampering | on-demand forecast writing the SQLite time series | mitigate | `forecast.py` + the forecast lookup path import nothing from `weatherbot.weather.store`, take no write `db_path`; proven by the zero-store-writes spy (FCAST-05) |
| T-13-12 | DoS | a forecast handler failure crashing dispatch / the scheduler | mitigate | Handler runs inside the single non-propagating try/except (Pitfall 5 / CMD-16); a raised error yields the generic reply, never a traceback or a crashed loop |
| T-13-13 | Information Disclosure | appid/webhook/token leaking in a forecast reply or log | mitigate | Outcome-only logging (location/kind/variant); the One Call URL is never constructed or logged on this path (existing T-04-01 discipline); content is weather data + fixed labels |
| T-13-14 | Tampering | mention/`@everyone` injection in a posted forecast | mitigate | Content is config-sourced location names + weather data + fixed labels; mirror `emit_online`'s no-mention discipline (no free user text echoed into the post) |
</threat_model>

<verification>
- `uv run pytest tests/test_forecast_lookup.py tests/test_registry.py tests/test_cli.py tests/test_bot.py -k "forecast" -x -q` green.
- `uv run pytest -q` full suite green (no regression in the Phase-12 registry/dispatch tests).
- `grep -c "weekday-forecast" weatherbot/interactive/registry.py` ≥ 1; both surfaces derive from the one list (no second command list in cli.py/bot.py).
- Zero-store-writes spy asserts no store function is called on any forecast path (FCAST-05).
- `client.fetch_onecall` call-count assertion confirms no extra fetch (FCAST-07).
</verification>

<success_criteria>
- A user can request weekday/weekend forecasts on demand from BOTH the CLI and Discord (FCAST-01/02),
  selecting variant (FCAST-03) and add/drop days (FCAST-04), with out-of-window notices.
- The on-demand path is read-only (FCAST-05) and reuses the already-fetched `daily[]` with no extra
  OpenWeather call (FCAST-07), all inside the Phase-12 guard ladder + isolation envelope.
</success_criteria>

<output>
Create `.planning/phases/13-multi-day-forecast-templates/13-04-SUMMARY.md` when done.
</output>
