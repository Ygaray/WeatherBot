---
phase: 12-command-registry-read-only-command-surface
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - weatherbot/interactive/bot.py
  - weatherbot/interactive/registry.py
  - weatherbot/interactive/__init__.py
  - weatherbot/cli.py
  - weatherbot/scheduler/daemon.py
  - tests/test_bot.py
  - tests/test_cli.py
  - tests/test_interactive_package.py
autonomous: false
requirements: [CMD-09, CMD-10, CMD-11, CMD-12, CMD-13, CMD-14, CMD-15, CMD-16]
user_setup: []
must_haves:
  truths:
    - "The Discord bot dispatches every registered command through the registry, INSIDE the existing non-propagating try/except and off the event loop — the guard ladder steps (1)-(3) are byte-for-byte unchanged (CMD-16)."
    - "The CLI exposes one subcommand per registry spec (location-taking specs accept an optional location), generated from the same COMMANDS list — CLI and Discord can never drift."
    - "run_daemon constructs a read-only DaemonState (scheduler, db_path, started_at, bot_alive) and threads it into the bot/registry context so status works on the live daemon."
    - "A command handler that raises surfaces as a generic reply and NEVER reaches the scheduler thread or drops a briefing (CMD-16 failure isolation)."
    - "The operator confirms on the live yahir-mint daemon that help/locations/status/sun/wind/alerts/next-cloudy all answer correctly."
  artifacts:
    - path: "weatherbot/interactive/bot.py"
      provides: "registry-driven on_message dispatch (handlers wired) inside the unchanged guard ladder + isolation envelope"
      contains: "parse_command"
    - path: "weatherbot/cli.py"
      provides: "registry-generated subparsers + dispatch for every command"
      contains: "COMMANDS"
    - path: "weatherbot/scheduler/daemon.py"
      provides: "DaemonState construction + threading into BotThread/registry context; started_at capture"
      contains: "DaemonState"
  key_links:
    - from: "weatherbot/interactive/bot.py"
      to: "weatherbot/interactive/registry.py handlers"
      via: "parse_command → spec.handler dispatched via run_in_executor inside the existing try/except"
      pattern: "parse_command|spec.handler"
    - from: "weatherbot/scheduler/daemon.py"
      to: "weatherbot/interactive/state.py DaemonState"
      via: "constructed in run_daemon, threaded into the bot"
      pattern: "DaemonState"
    - from: "weatherbot/cli.py"
      to: "weatherbot/interactive/registry.py COMMANDS"
      via: "one subparser per spec"
      pattern: "registry|COMMANDS"
---

<objective>
Wire the registry and handlers into both surfaces and the daemon: registry-driven `on_message` dispatch in `bot.py` (guard ladder + isolation envelope unchanged), one CLI subparser per registry spec in `cli.py`, and a read-only `DaemonState` constructed in `run_daemon` and threaded into the bot so `status` works live. Finish with a human-verify checkpoint on the live `yahir-mint` daemon.

Purpose: This makes every command reachable from both surfaces from ONE registry (CMD-09 anti-drift), routes them through the SAME operator-id guard ladder and the SAME non-propagating try/except as `!weather` (CMD-16), and surfaces live scheduler/heartbeat state to `status` (CMD-12). Because the running daemon loads Python modules at start, a restart + operator verification closes the loop.

Output: registry-wired `bot.py`/`cli.py`/`daemon.py`, updated package exports, tests, and an approved live checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-command-registry-read-only-command-surface/12-CONTEXT.md
@.planning/phases/12-command-registry-read-only-command-surface/12-RESEARCH.md
@.planning/phases/12-command-registry-read-only-command-surface/12-PATTERNS.md
@.planning/phases/12-command-registry-read-only-command-surface/12-01-SUMMARY.md
@.planning/phases/12-command-registry-read-only-command-surface/12-02-SUMMARY.md
@weatherbot/interactive/bot.py
@weatherbot/cli.py
@weatherbot/scheduler/daemon.py
</context>

<artifacts_produced>
This phase produces (Plan 03 portion):
- `weatherbot/interactive/bot.py` — `build_on_message` step (4) now uses `parse_command` (registry) + dispatches `spec.handler` via `run_in_executor` inside the existing try/except; a render step turns the handler's CommandReply into a `discord.Embed`. New param threaded: `daemon_state` (read-only, for status). The guard ladder steps (1)-(3) and the isolation envelope are unchanged.
- `weatherbot/interactive/registry.py` — `COMMANDS` specs gain their real `handler` callables (wired here, completing the Plan 01 placeholders).
- `weatherbot/cli.py` — registry-generated subparsers (one per spec; `takes_location` specs get `location` `nargs="?", default=None`) + a registry-driven dispatch branch that loads config and invokes the spec handler, rendering the CommandReply as plain text.
- `weatherbot/scheduler/daemon.py` — `run_daemon` captures `started_at = datetime.now(timezone.utc)` and constructs a read-only `DaemonState(scheduler, db_path, started_at, bot_alive=bot.is_alive)`, threaded into the bot.
- `weatherbot/interactive/__init__.py` — export the new public symbols (registry, parse_command, DaemonState as needed).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry-driven Discord dispatch + handler wiring (CMD-16 isolation preserved)</name>
  <files>weatherbot/interactive/bot.py, weatherbot/interactive/registry.py, weatherbot/interactive/__init__.py, tests/test_bot.py, tests/test_interactive_package.py</files>
  <read_first>
    - weatherbot/interactive/bot.py (build_on_message lines 104-150 — guard ladder steps (1)-(3) and the non-propagating try/except at 124-148 that MUST stay byte-for-byte; build_inbound_embed lines 61-81 for the embed house style; BotThread/build_client signatures lines 84-89/153-191/204-215)
    - weatherbot/interactive/registry.py (CommandSpec.handler / COMMANDS / BY_NAME from Plan 01)
    - weatherbot/interactive/commands/ (the handlers + CommandReply from Plan 02)
    - weatherbot/interactive/state.py (DaemonState — status needs it)
    - .planning/phases/12-command-registry-read-only-command-surface/12-PATTERNS.md (bot.py "registry dispatch, ladder UNCHANGED"; Pitfall 5 — do NOT add a second try/except)
    - tests/test_bot.py (the gateway-free handler test driving build_on_message with a fake message; the ladder + isolation tests)
  </read_first>
  <behavior>
    - The guard ladder steps (1) author.bot drop, (2) operator_id check, (3) "!" prefix are unchanged; only step (4) becomes parse_command (registry).
    - A registered command (e.g. "!sun home") dispatches its spec.handler via loop.run_in_executor and replies with an embed built from the handler's CommandReply.
    - A non-command ("!nonsense") and a non-operator message are silently dropped exactly as before.
    - UnknownLocationError from a location-taking handler replies with the corrective-hint message (reused sub-except), no embed.
    - A handler that raises is caught by the EXISTING non-propagating try/except → generic error reply, logged, never re-raised (CMD-16); a separate test proves the exception does not escape on_message.
    - status dispatch reaches the injected DaemonState and replies (live scheduler/heartbeat data in tests via a fake DaemonState).
  </behavior>
  <action>
    Wire the real handler callables into `registry.COMMANDS` in `registry.py` (replace the Plan 01 `handler=None` placeholders): `alerts`/`sun`/`wind`/`next_cloudy` from `commands.weather_views`, `help_cmd`/`locations` from `commands.info`, `status` from `commands.status`. Use lazy/local imports if needed to avoid an import cycle (registry is imported by command.py and bot.py; the handlers import lookup/models — keep the import direction acyclic, mirroring the lazy-import precedents in lookup.py/daemon.py).

    In `weatherbot/interactive/bot.py`, modify `build_on_message` step (4) ONLY: replace `parse_weather_command(content[1:])` with `parse_command(content[1:])`; on `spec is None` return (NOT_A_COMMAND). For a matched spec, dispatch INSIDE the EXISTING try/except (Pitfall 5 — do NOT add a second envelope): build the handler call (location-taking handlers go through `cache.lookup(arg, config)` off-loop then call the handler with the LookupResult + threshold from `config.cloud_threshold`; `status` calls its handler with the threaded `daemon_state`; `help`/`locations` call directly with the config/registry — no fetch). Keep the off-loop `run_in_executor` for any blocking handler. Render the returned CommandReply into a `discord.Embed` via a small `render_embed(reply)` helper (reuse the `build_inbound_embed` house style: title + add_field per line + utcnow timestamp). Preserve the `UnknownLocationError` sub-except. Thread a new `daemon_state` param (read-only, default None for the gateway-free tests) through `build_on_message`/`build_client`/`BotThread.__init__` alongside `cache`. Keep `operator_id` baked at construction (do NOT re-read per message). Do NOT touch guard steps (1)-(3) or the outer try/except structure.

    Update `weatherbot/interactive/__init__.py` exports to include the registry surface (`parse_command`, `CommandSpec`, `render_help`, `DaemonState`) as appropriate.

    In `tests/test_bot.py`, extend the gateway-free tests: registry dispatch for a weather view (fake cache.lookup), for `help`/`locations` (no fetch), and for `status` (fake DaemonState); the ladder still drops bots/non-operators/non-commands; and the isolation test asserts a raising handler does NOT propagate out of on_message (CMD-16). Update `tests/test_interactive_package.py` for the new exports.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_bot.py tests/test_interactive_package.py -x` passes.
    - Source assertion: `grep -c 'parse_command' weatherbot/interactive/bot.py` ≥ 1 AND `grep -c 'parse_weather_command' weatherbot/interactive/bot.py` shows the old call is replaced in the ladder (no longer the dispatch path).
    - Source assertion: `grep -c 'try:\|except Exception' weatherbot/interactive/bot.py` shows NO net-new outer envelope was added (the whole dispatch stays inside the existing one — Pitfall 5). Reviewer confirms steps (1)-(3) unchanged.
    - Source assertion: `grep -c 'handler=' weatherbot/interactive/registry.py` ≥ 7 (every spec now has a real handler wired).
    - Behavior assertion: a test makes a wired handler raise and asserts on_message does NOT raise (failure isolation, CMD-16) and a generic reply is sent.
    - Behavior assertion: "!sun home" with a fake cache produces an embed; a non-operator author is dropped.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_bot.py tests/test_interactive_package.py -x</automated>
  </verify>
  <done>bot.py dispatches registry commands through the unchanged guard ladder + isolation envelope, off-loop, rendering CommandReply embeds; registry.COMMANDS handlers are wired; package exports updated; tests green incl. the no-propagation isolation test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry-generated CLI subparsers + daemon DaemonState threading (CMD-09/12)</name>
  <files>weatherbot/cli.py, weatherbot/scheduler/daemon.py, tests/test_cli.py</files>
  <read_first>
    - weatherbot/cli.py (the add_subparsers block lines 593-667; the args.command dispatch lines 685-765; _cmd_weather + _load_config_reporting lines 330-346; run_weather for the read-only-print precedent)
    - weatherbot/interactive/registry.py (COMMANDS / CommandSpec.takes_location)
    - weatherbot/interactive/commands/ (handlers + CommandReply rendering to plain text)
    - weatherbot/scheduler/daemon.py (run_daemon construction site lines 1037-1095 — where scheduler/holder/cache/bot are built; _announce_schedule for the job-id key)
    - weatherbot/interactive/state.py (DaemonState constructor signature from Plan 02)
    - .planning/phases/12-command-registry-read-only-command-surface/12-PATTERNS.md (cli.py "registry-driven subparsers"; Open Question 3 — CLI has no Discord guard ladder, terminal == operator)
    - tests/test_cli.py (subcommand test style + exit-code assertions)
  </read_first>
  <behavior>
    - The CLI exposes one subcommand per registry spec; `takes_location` specs accept an optional `location` (nargs="?", default=None → default-first location, D-01); `help`/`locations`/`status` take no location.
    - `weatherbot sun <loc>` / `wind` / `alerts` / `next-cloudy` print the handler's CommandReply as plain text and exit 0; an unknown location prints the corrective hint and exits non-zero (reusing the run_weather exit-code precedent).
    - `weatherbot help` and `weatherbot locations` print without any network/config-fetch (help needs no config; locations needs config only).
    - `weatherbot status` reports the daemon state — for the CLI one-shot it reads what is available read-only (e.g. heartbeat from the db); document the CLI status scope vs the live-daemon status.
    - run_daemon captures started_at and builds a read-only DaemonState(scheduler, db_path, started_at, bot_alive=bot.is_alive) threaded into the bot so the Discord `status` is fully populated live.
    - CLI inherits the registry + read-only + failure-isolation guarantees but NOT the Discord-author guards (no remote actor — Open Question 3).
  </behavior>
  <action>
    In `weatherbot/cli.py`, generate subparsers from `registry.COMMANDS`: loop the specs and `subparsers.add_parser(spec.name, parents=[config_parent], help=spec.summary)`; for `spec.takes_location` add `location` `nargs="?", default=None` (mirror the `weather` subparser lines 600-605). `help`/`locations`/`status` need no location arg; `help` may even skip `config_parent` (it needs no config) — keep `locations`/`status` on `config_parent`. Add a registry-driven dispatch branch in the `args.command` chain: resolve the spec via `registry.BY_NAME[args.command]`, load config via `_load_config_reporting` where needed (reuse the exit-2-on-bad-config precedent), build the handler input (location-taking → `lookup_weather`/cache read for the LookupResult + `config.cloud_threshold`; `status` → a CLI-scoped read-only state read; `help`/`locations` → direct), call the handler, and print the CommandReply as plain text (a `render_text(reply)` helper — same content as the embed, D-04), returning the run_weather-style exit codes. Keep the existing `weather`/`run`/`send-now`/`geocode`/`check`/`reload` branches intact. Document (in code comment + SUMMARY) that the CLI has no Discord guard ladder (terminal == operator, Open Question 3) but inherits read-only + isolation.

    In `weatherbot/scheduler/daemon.py` `run_daemon`: capture `started_at = datetime.now(timezone.utc)` near the top of the function (A5). After the bot is constructed, build `DaemonState(scheduler=scheduler, db_path=db_path, started_at=started_at, bot_alive=bot.is_alive)` (read-only) and thread it into the `BotThread`/`build_client` constructor alongside `cache` (matching the new `daemon_state` param from Task 1). Keep it READ-ONLY — DaemonState must not be handed any write capability. Leave the Phase-15 monitor slot unset (None → "not running"). Do not change scheduler registration, the heartbeat job, or the reload/file-watch wiring.

    In `tests/test_cli.py`, add subcommand tests: each location-taking command prints + exits 0 on a good config/fake lookup; unknown location exits non-zero with the hint; `help`/`locations` print correctly; the subparser set matches registry.COMMANDS (a test that iterates COMMANDS and asserts each name parses). Add/extend a daemon test asserting run_daemon constructs a DaemonState threaded into the bot (or assert via the bot receiving a non-None daemon_state).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_cli.py -x` passes (and `uv run pytest tests/test_scheduler.py -x` stays green).
    - Source assertion: `grep -c 'registry\|COMMANDS' weatherbot/cli.py` ≥ 1 (subparsers generated from the registry — not a hand-maintained second list, CMD-09 anti-drift).
    - Source assertion: `grep -c 'DaemonState' weatherbot/scheduler/daemon.py` ≥ 1 AND `grep -c 'started_at' weatherbot/scheduler/daemon.py` ≥ 1.
    - Behavior assertion: a test iterates registry.COMMANDS and asserts `main([spec.name, ...])` parses for every spec (CLI/Discord derive-from-one-list).
    - Behavior assertion: `weatherbot sun <loc>` with a fake lookup prints the sun reply and returns 0; unknown location returns the run_weather non-zero code with the hint.
    - Source assertion: no write call (`add_job`/`holder.replace`/`stamp_`/`persist`) is introduced on the DaemonState path in daemon.py beyond the existing daemon wiring.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_scheduler.py -x</automated>
  </verify>
  <done>cli.py generates one subparser per registry spec and dispatches each handler to plain-text output with run_weather-style exit codes; run_daemon captures started_at and threads a read-only DaemonState into the bot; tests green and the subparser set is derived from COMMANDS.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite gate + deploy-restart note</name>
  <files>tests/test_bot.py, tests/test_cli.py, tests/test_command_views.py, tests/test_status.py, tests/test_registry.py</files>
  <read_first>
    - .planning/phases/12-command-registry-read-only-command-surface/12-RESEARCH.md (Runtime State Inventory — the running daemon must be RESTARTED to load new modules; editable install on yahir-mint)
    - .planning/STATE.md (the live systemd service note + the status next-scheduled-send carry-forward concern)
  </read_first>
  <action>
    Run the full suite and confirm green. Confirm the per-wave sampling commands from 12-VALIDATION/RESEARCH pass together: `uv run pytest tests/test_registry.py tests/test_command.py tests/test_bot.py tests/test_command_views.py tests/test_status.py tests/test_cli.py tests/test_client.py tests/test_store.py tests/test_config.py`. Then run the WHOLE suite `uv run pytest`. In the SUMMARY, record the deploy step required for the live daemon: because new Python modules and the widened `exclude` only load on the NEXT process start (hot-reload covers config/templates, not modules), the `weatherbot` systemd service on `yahir-mint` must be RESTARTED after deploy (MEMORY.md / Runtime State Inventory). No code change in this task — it is the green-gate + handoff note before the human checkpoint.
  </action>
  <acceptance_criteria>
    - `uv run pytest` exits 0 (whole suite green).
    - The SUMMARY notes the required `systemctl restart weatherbot` (or equivalent) on yahir-mint before the live checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest</automated>
  </verify>
  <done>Full suite green; SUMMARY records the daemon-restart-on-deploy requirement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live operator verification on yahir-mint (all commands, both surfaces)</name>
  <what-built>
    The full read-only command surface — `help`, `locations`, `status`, `sun`, `wind`, `alerts`, `next-cloudy` — on both the CLI and the Discord bot, all driven by one command registry and routed through the existing operator guard ladder + failure-isolation envelope. The One Call client now keeps `hourly[]` for `next-cloudy`, and `status` reports live next-send/uptime/liveness/last-briefing.
  </what-built>
  <how-to-verify>
    1. Deploy the new code to `yahir-mint` and RESTART the service so the new modules + widened `exclude` load: `sudo systemctl restart weatherbot` (new Python modules do NOT hot-reload; config/templates do).
    2. Confirm the daemon came up healthy: `systemctl status weatherbot` (active/running) and the journal shows the inbound bot ready with no CRITICAL.
    3. In your private Discord server, as the operator, send each and confirm a correct reply:
       - `!help` → grouped list of every command with one-line descriptions (Weather + Info groups).
       - `!locations` → your configured location names.
       - `!status` → "alive" + uptime, next scheduled send per location, bot active (+ UV monitor "not running"), and the last briefing result.
       - `!sun` (bare → default location) and `!sun <other loc>` → sunrise/sunset local times.
       - `!wind <loc>` → current wind speed + a compass direction.
       - `!alerts <loc>` → active alerts, or a clear "no active alerts".
       - `!next-cloudy <loc>` → the next cloudy day/time at the 60% default threshold, or "no cloudy day in the next N days".
       - `!sun bogusplace` → the corrective-hint message listing valid names (unknown-location path).
    4. From the CLI on the host run the same commands as subcommands, e.g. `weatherbot help`, `weatherbot locations`, `weatherbot status`, `weatherbot sun <loc>`, `weatherbot wind <loc>`, `weatherbot alerts <loc>`, `weatherbot next-cloudy <loc>` and confirm matching content (plain text).
    5. Sanity-check isolation: confirm the morning briefing still fires normally (the command path must never gate/delay/drop a briefing) — check the journal/last-briefing in `!status` after the next scheduled send, or trust the isolation tests if no send is due during verification.
  </how-to-verify>
  <resume-signal>Type "approved" if every command answers correctly on both surfaces and the briefing path is unaffected, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord operator → on_message guard ladder | Untrusted gateway messages; the load-bearing ladder (bot-drop → operator-id → "!" prefix → registry parse) is the access control |
| Command handler → scheduler/briefing thread | A handler failure must NOT cross into the scheduler thread (CMD-16) |
| CLI invocation → local operator | Terminal == operator; no remote actor (Open Question 3) |
| run_daemon → DaemonState | Live scheduler/db/start-time injected read-only; must not become a write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Elevation of Privilege | non-operator issuing commands | mitigate | Guard ladder step (2) `author.id != operator_id` silently drops — unchanged, reused for ALL commands (CMD-16) |
| T-12-11 | DoS / availability | a command failure stalling/dropping a briefing | mitigate | WHOLE registry dispatch stays inside the existing non-propagating try/except; handlers run off the event loop via run_in_executor; isolation test proves no propagation (CMD-16, Pitfall 5) |
| T-12-12 | DoS | bot feedback loop / prefix collision | mitigate | Guard step (1) `author.bot` drop + word-boundary/longest-first parse (Plan 01) — unchanged |
| T-12-13 | Information Disclosure | secret in a reply or log | mitigate | Outcome-only logging unchanged (appid/webhook/token never logged); status reports state only; UnknownLocationError carries no secret (lookup.py:52) |
| T-12-14 | Elevation of Privilege | DaemonState used to mutate state | mitigate | DaemonState is read-only (no write capability handed in); status reports, never mutates (D-02) |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | None — no new packages added (uv.lock unchanged) |
</threat_model>

<verification>
- `uv run pytest` whole suite green.
- bot.py guard ladder steps (1)-(3) + isolation envelope unchanged; only step (4) dispatch is registry-driven.
- CLI subparsers + Discord dispatch both derive from registry.COMMANDS (no second list).
- run_daemon threads a read-only DaemonState into the bot.
- Operator-approved live checkpoint on yahir-mint.
</verification>

<success_criteria>
- Every command (help/locations/status/sun/wind/alerts/next-cloudy) answers on BOTH CLI and Discord from one registry (CMD-09/10/11/12/13/14/15).
- All commands route through the same guard ladder + non-propagating isolation; a handler failure never affects the briefing path (CMD-16).
- status reports next-send/uptime/liveness/last-briefing live (CMD-12).
- Live operator verification passed.
</success_criteria>

<output>
Create `.planning/phases/12-command-registry-read-only-command-surface/12-03-SUMMARY.md` when done.
</output>
