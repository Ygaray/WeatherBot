---
phase: 14-uv-index-on-demand-daily-briefing
plan: 02
type: tdd
wave: 2
depends_on: ["14-01"]
files_modified:
  - weatherbot/weather/uv.py
  - tests/test_uv.py
autonomous: true
requirements: [UV-02]
must_haves:
  truths:
    - "A pure compute_uv(onecall_imp, onecall_met, threshold, *, tz, now) returns a frozen UvSummary with current, max, category, peak, interpolated crossing time, protect window, and stays_below"
    - "The threshold crossing time is linearly interpolated between hourly[] points to ~minute precision (not whole-hour)"
    - "When UV never reaches the threshold across today's daytime, stays_below is True and crossing/window/peak fields are None"
    - "The helper imports NOTHING from the interactive layer (so Phase 15's monitor can reuse it)"
    - "All time math uses the CONFIGURED location IANA tz, not the API timezone field"
  artifacts:
    - path: "weatherbot/weather/uv.py"
      provides: "compute_uv() + UvSummary + uv_category()"
      contains: "def compute_uv"
    - path: "tests/test_uv.py"
      provides: "UV-02 compute_uv math coverage incl. interpolation + edge cases"
  key_links:
    - from: "weatherbot/weather/uv.py compute_uv"
      to: "hourly[]/daily[0]/current"
      via: "defensive raw.get() reads + linear interpolation"
      pattern: "compute_uv"
---

<objective>
Build the one genuinely new piece of logic in Phase 14: a pure, stateless
`compute_uv` helper (+ `UvSummary` frozen dataclass + `uv_category`) in a NEW
`weatherbot/weather/uv.py`. It reads the already-fetched One Call payloads + the
configured threshold and emits current / today's max / WHO category / peak time /
interpolated threshold-crossing time / protect-window / stays-below.

Purpose: this is the shared reuse seam. The `Forecast` briefing fields (Plan 14-03),
the `uv <loc>` command (Plan 14-04), AND the Phase-15 monitor all call this exact
helper. Getting the interface + interpolation math right here makes the rest fall
out cleanly. Kept free of any interactive-layer import so the Phase-15 background
monitor can reuse it without a cycle.

Output: `weatherbot/weather/uv.py` (compute_uv, UvSummary, uv_category, the daytime
scan + interpolation helpers) and `tests/test_uv.py` covering every interpolation
edge case against the Plan 14-01 fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-uv-index-on-demand-daily-briefing/14-CONTEXT.md
@.planning/phases/14-uv-index-on-demand-daily-briefing/14-RESEARCH.md
@.planning/phases/14-uv-index-on-demand-daily-briefing/14-PATTERNS.md
@weatherbot/interactive/commands/weather_views.py
@weatherbot/weather/models.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-02 portion)

- **New module:** `weatherbot/weather/uv.py`
- **Class:** `UvSummary` (frozen dataclass) — fields `current`, `max`, `category`,
  `peak_uvi`, `peak_time`, `crossing_time`, `window_start`, `window_end`,
  `stays_below`, `hourly_points`
- **Function:** `compute_uv(onecall_imp, onecall_met, threshold, *, tz, now=None) -> UvSummary`
- **Function:** `uv_category(uvi: float) -> str` (WHO bands, round-then-band)
- **Internal helpers:** `_today_daytime_points`, `_first_up_cross`, `_epoch_local`
- **Test file:** `tests/test_uv.py`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute_uv + UvSummary + uv_category (pure helper, UV-02 math)</name>
  <files>weatherbot/weather/uv.py, tests/test_uv.py</files>
  <read_first>
    - weatherbot/interactive/commands/weather_views.py (`_epoch_local` lines 59-61 — copy VERBATIM; `_is_daytime` sunrise/sunset bounding lines 64-85 incl. the 06:00-20:00 fixed-window fallback; the `next_cloudy` hourly-scan guard structure lines 195-203)
    - weatherbot/weather/models.py (`_local_date_iso` lines 47-62 — the CONFIGURED-tz "today" idiom; `ForecastDay._local_hhmm` lines 426-437)
    - .planning/phases/14-uv-index-on-demand-daily-briefing/14-RESEARCH.md (Patterns 1-4 + the Code Examples lines 224-272: daytime-point selection, `_first_up_cross` interpolation, `uv_category` bands; Pitfall 6 — current.uvi for now, daily[0].uvi for max, hourly[] ONLY for crossing/window/peak; A1 — read onecall_imp only, accept onecall_met for parity and ignore it; A2 — round-then-band)
    - .planning/phases/14-uv-index-on-demand-daily-briefing/14-PATTERNS.md (the `weather/uv.py` section — frozen UvSummary shape, dependency-free-of-interactive-layer rule, defensive payload access)
    - tests/fixtures/onecall_imperial_uvcross.json, tests/fixtures/onecall_imperial_uvbelow.json, tests/fixtures/onecall_imperial_highuv.json (the Plan 14-01 fixtures with hourly[])
  </read_first>
  <behavior>
    - Up-cross (uvcross fixture, threshold 6, pinned now): crossing_time is the
      linearly-interpolated instant between the two hourly points straddling 6 (assert
      to the minute, NOT the whole hour); window_start == crossing_time;
      window_end == the first subsequent down-cross instant (interpolated), sunset-bounded.
    - Stays-below (uvbelow fixture, threshold 6): stays_below is True; crossing_time,
      window_start, window_end are None; peak_time still reflects the day's hourly argmax.
    - Already-above-at-sunrise (highuv fixture, first daytime point >= 6): crossing_time ==
      window_start == the first daytime point's time (no interpolation).
    - current == current.uvi verbatim (Pitfall 6); max == daily[0].uvi verbatim; the
      displayed peak value should agree with daily[0].uvi, the peak CLOCK derives from the
      hourly argmax.
    - category(5.6) == "High" (round-then-band, A2); boundary cases 2→Low, 3→Moderate,
      6→High, 8→Very High, 11→Extreme.
    - Missing daily[0].sunrise/sunset: falls back to the fixed daytime window (06:00-20:00
      local) rather than raising (Pitfall 5).
    - A malformed/empty hourly[] (or a bucket with dt/uvi None) is skipped, never
      subscripted blind (WR-01) — and with no usable points the helper returns
      stays_below=True with None crossing/window, never raises.
    - compute_uv uses the passed-in tz (ZoneInfo) for "today"/daytime, NOT any API
      timezone field (Pitfall 3).
  </behavior>
  <action>
    Create `weatherbot/weather/uv.py` with `from __future__ import annotations` and
    only stdlib + dataclasses imports (NO import from `weatherbot.interactive` — the
    Phase-15 reuse rule). Define the frozen `@dataclass(frozen=True) class UvSummary`
    with fields `current: float`, `max: float`, `category: str`, `peak_uvi: float`,
    `peak_time: datetime | None`, `crossing_time: datetime | None`,
    `window_start: datetime | None`, `window_end: datetime | None`,
    `stays_below: bool`, `hourly_points: tuple[tuple[datetime, float], ...]`. Copy
    `_epoch_local` VERBATIM from `weather_views.py`. Write `_today_daytime_points(raw,
    tz, now)` modeled on the RESEARCH Code Example: select hourly buckets whose
    location-local date == `now`-in-tz date AND fall within `[sunrise, sunset]` from
    `daily[0]` (defensive `raw.get("daily") or [{}]`, `raw.get("hourly") or []`, skip
    `dt`/`uvi` None), with the fixed 06:00-20:00 fallback when sun data is absent.
    Write `_first_up_cross(points, threshold)` per the RESEARCH Code Example (linear
    interpolation `t0 + (t1-t0)*(threshold-u0)/(u1-u0)`; already-above → first point's
    time) and a sibling first-down-cross-after-up for `window_end`. Write
    `uv_category(uvi)` as a small ordered threshold table (round-then-band, A2 — NOT an
    inline if-chain hardcoded in compute_uv). `compute_uv(onecall_imp, onecall_met,
    threshold, *, tz, now=None)` reads UV numbers from `onecall_imp` ONLY (A1; accept
    and ignore `onecall_met` for signature parity — document why), defaults `now` to
    `datetime.now(tz)`, uses `current.uvi` for `current`, `daily[0].uvi` for `max`,
    and the daytime hourly points for `peak_time`/`crossing_time`/`window_*`. NO fenced
    code in the template — display-string formatting is NOT done here (that is Plan
    14-03's `Forecast` job); this helper returns the structured `UvSummary` only.
    Write `tests/test_uv.py` driving every behavior case above off the Plan 14-01
    fixtures with a pinned `now=` and an explicit `ZoneInfo`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_uv.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def compute_uv" weatherbot/weather/uv.py` → 1; `grep -c "class UvSummary" weatherbot/weather/uv.py` → 1; `grep -c "def uv_category" weatherbot/weather/uv.py` → 1
    - `grep -c "from weatherbot.interactive" weatherbot/weather/uv.py` → 0 (Phase-15 reuse rule: no interactive-layer import)
    - `uv run pytest tests/test_uv.py -x` passes: up-cross interpolated-to-the-minute, down-cross/window-end, already-above-at-sunrise, stays-below, missing-sunrise fallback, empty-hourly no-raise, category boundaries (5.6→"High")
    - `uv run python -c "from weatherbot.weather.uv import uv_category; print(uv_category(5.6))"` → `High`
  </acceptance_criteria>
  <done>compute_uv returns a correct UvSummary for every fixture scenario with minute-precision interpolation; the module is interactive-layer-free; all edge cases are tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenWeather payload → compute_uv | The raw `hourly[]`/`daily[0]`/`current` dicts (possibly partial/malformed) cross into the pure helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | DoS | malformed/empty `hourly[]` crashing the helper | mitigate | Defensive `raw.get(...) or []`/`or {}` + skip-None-bucket guards (WR-01); no usable points → `stays_below=True`, never a raise. **Briefing-spine isolation (CRITICAL):** because compute_uv degrades to stays_below instead of raising on missing/empty `hourly[]`/`uvi`, the Plan 14-03 briefing render can never crash/gate/delay a send on bad UV data |
| T-14-05 | Tampering | wrong-day hourly selection (API tz trap) | mitigate | "today" + daytime bounding use the passed-in CONFIGURED tz (Pitfall 3), never the API `timezone` field |
</threat_model>

<verification>
- `uv run pytest tests/test_uv.py -x` green.
- Full suite green.
- `compute_uv` proven to return `stays_below=True` (never raise) on an empty/missing `hourly[]` — the briefing-spine isolation guarantee Plan 14-03 relies on.
</verification>

<success_criteria>
- `compute_uv` emits a correct `UvSummary` (current/max/category/peak/crossing/window/stays_below) for all three fixtures (UV-02 math).
- Crossing/window edges are linearly interpolated to ~minute precision.
- The module imports nothing from the interactive layer (Phase-15 reuse).
</success_criteria>

<output>
Create `.planning/phases/14-uv-index-on-demand-daily-briefing/14-02-SUMMARY.md` when done
</output>
