---
status: testing
milestone: v2.0
milestone_name: Bot Module Extraction ("The Great Decoupling")
phases: [21, 22, 23, 24, 25, 26, 27, 28]
gate: 2
created: 2026-07-07
module_pin: yahir_reusable_bot @ v0.1.0 / 138a907d57ac1d1d8499399b019f1509e43d02f1
host: yahir-mint
---

# v2.0 "The Great Decoupling" — Milestone Gate-2 Human UAT

**What this is.** v2.0 was a *pure, byte-identical extraction*: the reusable bot infrastructure
moved to its own repo (`yahir_reusable_bot` @ `v0.1.0` / `138a907`) and WeatherBot now imports it
via a uv git pin. No new user-facing behavior. Every phase's automated **Gate-1** already passed
(776 tests + byte-identical goldens + clean-venv install + leak gate), and every phase **deferred
its one human/device check to this single live session**.

**Goal.** Confirm the live WeatherBot, now running against the *extracted + pinned* module, behaves
identically — same panel, same briefings, same CLI — across a real restart. Any FAIL is almost
certainly an extraction regression to fix upstream in the module (cross-repo jurisdiction) before
closing the milestone.

**Run it all in one sitting on host `yahir-mint`.** Commands assume `cd ~/Projects/WeatherBot`;
use `uv run weatherbot <cmd>` (or `.venv/bin/weatherbot <cmd>`). Mark each item PASS / FAIL + notes.

---

## Pre-flight (do NOT restart yet)

- [ ] **P1 — Locate the live pinned panel.** In your Discord channel, confirm the pinned control
  panel is present. We will test that *this existing panel* (registered **before** the restart)
  still routes *after* it — that's the persistent-view re-bind proof (Phase 27), so don't re-summon
  it yet.
- [ ] **P2 — Baseline.** `uv run weatherbot status` → shows daemon liveness + next scheduled sends.

---

## 1. Deploy & startup against the pinned module — Phases 25, 28

- [ ] **1a.** `sudo systemctl restart weatherbot`
- [ ] **1b.** `systemctl is-active weatherbot` → `active`
- [ ] **1c.** `journalctl -u weatherbot -n 50 --no-pager` and confirm:
  - a **`module provenance`** line with `module_sha=138a907d…`, `module_ref=v0.1.0`,
    `editable=False` — **this is the headline of the whole milestone**: it proves the live daemon
    is running the extracted, pinned module, not local source.
  - no traceback/crash; systemd READY (`Type=notify`) reached; Discord gateway/bot connected;
    scheduled jobs registered.

**Expected:** clean boot against `yahir_reusable_bot @ 138a907`.
**Result:** ⬜

---

## 2. Panel persistent-view re-bind + full routing — Phases 26, 27  *(the core test)*

On the **same pinned panel from P1** (registered before the restart):

- [ ] **2a — every button routes.** Tap each command button (Status, Alerts, UV, Sun, Wind,
  Next-cloudy, Weekday-forecast, Weekend-forecast…). Each replies in-place; **no "interaction
  failed" toast on any** (frozen `custom_id`s re-bound via `add_view` after the module swap).
- [ ] **2b — location dropdown.** Select each configured location → the **📍 indicator updates**;
  then tap a command → the reply is for the selected location.
- [ ] **2c — forecast sub-grid.** Tap the Forecast toggle → the 2×2 sub-grid appears
  (Weekday/Weekend × Detailed/Compact) → tap each of the 4 variants → each renders.
- [ ] **2d — cosmetic polish survives re-render (WR-01/WR-02).** After several taps/collapses, the
  📍 line, button emoji, and the `Updated <t:…>` timestamp are all still present/correct.
- [ ] **2e — operator gate (if you have a second account).** A non-operator tapping a control gets
  an ephemeral reject; you (operator) are unaffected.

**Expected:** byte-identical panel behavior; every control still routes after extraction + restart.
**Result:** ⬜

---

## 3. Live config hot-reload, no restart — Phase 24

- [ ] **3a.** Edit your config (e.g. tweak a briefing template, or a location's name/schedule).
- [ ] **3b.** `uv run weatherbot reload` (SIGHUP to the running daemon).
- [ ] **3c.** `journalctl -u weatherbot -n 20 --no-pager` → a reload-applied line; then tap a panel
  command (or `uv run weatherbot weather`) → it reflects the edit **without any restart**.
- [ ] **3d.** Revert your edit → `uv run weatherbot reload` again.

**Expected:** the running daemon picks up config changes live (hot-reload engine works from the
extracted module).
**Result:** ⬜

---

## 4. Briefing delivery end-to-end — Phases 22, 23

- [ ] **4a.** `uv run weatherbot send-now` → the templated daily briefing posts to your Discord
  channel for the default location; correct location, today's high/low/rain/UV, formatting intact.
- [ ] **4b.** `uv run weatherbot send-now "<a travel location>"` → briefing for that location.
- [ ] **4c.** `uv run weatherbot status` → next scheduled sends look right (scheduler engine
  registered the jobs correctly).

**Expected:** delivery + scheduling identical (Channel abstraction + scheduler engine work from the
module).
**Result:** ⬜

---

## 5. CLI one-shot surface — Phases 7, 26

- [ ] **5a.** `uv run weatherbot --help` → the full command list.
- [ ] **5b.** `uv run weatherbot check` → config + templates valid, one reachability probe OK.
- [ ] **5c.** `uv run weatherbot locations` → your configured locations.
- [ ] **5d.** `uv run weatherbot weather` and `uv run weatherbot uv "<location>"` → correct one-shot
  output (the `weatherbot` console script resolves through stable public names into the module).

**Expected:** every CLI command works against the extracted module.
**Result:** ⬜

---

## Sign-off

- **All sections PASS** → v2.0 Gate-2 satisfied. Record + close:
  `/gsd-verify-work 28` (marks `28-UAT.md` passed) → `/gsd-audit-milestone` (optional) →
  `/gsd-complete-milestone`.
- **Any FAIL** → note the exact symptom. Because v2.0 is byte-identical extraction, a live failure
  is almost certainly a regression in the extracted module — fix it upstream in
  `yahir_reusable_bot` (cross-repo jurisdiction; ship via the repin ritual), then re-test that item.

*This log is the single record the human verifies against for v2.0 Gate-2, batching the deferred
per-phase obligations (recorded across the `*-SELF-UAT.md` / `*-VERIFICATION.md` logs) into one
live-host session.*
