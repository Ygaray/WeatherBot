---
phase: 10-file-watch-auto-reload
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - pyproject.toml
  - uv.lock
  - weatherbot/config/models.py
autonomous: false
requirements: [CFG-03]
must_haves:
  truths:
    - "`uv run python -c 'import watchfiles'` succeeds (dependency installed)"
    - "A `config.toml` with no `[reload]` table still loads (Config.reload defaults to ReloadConfig(watch=True))"
    - "`[reload] watch = false` parses to Config.reload.watch == False; an unknown `[reload]` key raises ValidationError (extra='forbid')"
  artifacts:
    - path: "weatherbot/config/models.py"
      provides: "ReloadConfig frozen model + Config.reload default-factory field (D-03 toggle)"
      contains: "class ReloadConfig"
    - path: "pyproject.toml"
      provides: "watchfiles>=1.2.0 runtime dependency (D-01)"
      contains: "watchfiles>=1.2.0"
  key_links:
    - from: "weatherbot/config/models.py Config"
      to: "ReloadConfig"
      via: "reload: ReloadConfig = Field(default_factory=ReloadConfig)"
      pattern: "reload: ReloadConfig"
---

<objective>
Add the two foundation pieces the observer wiring (Plan 10-03) depends on: the `watchfiles>=1.2.0` runtime dependency (D-01) and the `[reload] watch` config toggle as a frozen `ReloadConfig` model + `Config.reload` field (D-03). Both are light, disjoint-file config work structurally identical to the existing `Reliability` / `Config.reliability` pattern.

Purpose: file-watch needs `watchfiles` importable and a validated, ON-by-default `watch` toggle that the `ConfigHolder` owns — so an existing `[reload]`-less config loads unchanged and `[reload] watch = false` cleanly disables auto-reload (the explicit SIGHUP/`weatherbot reload` trigger always works regardless).
Output: installed `watchfiles` (+ updated `uv.lock`) and the `ReloadConfig` model + `Config.reload` field.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-file-watch-auto-reload/10-CONTEXT.md
@.planning/phases/10-file-watch-auto-reload/10-RESEARCH.md
@.planning/phases/10-file-watch-auto-reload/10-PATTERNS.md
@weatherbot/config/models.py
@pyproject.toml
</context>

<artifacts_this_phase_produces>
This plan (10-02) produces:
- `pyproject.toml` — `watchfiles>=1.2.0` added to `[project].dependencies` (alphabetical position after `tenacity`).
- `uv.lock` — regenerated by `uv add watchfiles`.
- `weatherbot/config/models.py` — NEW `ReloadConfig(BaseModel)` (frozen, `extra="forbid"`, `watch: bool = True`)
  and a NEW `Config.reload: ReloadConfig = Field(default_factory=ReloadConfig)` field.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Verify watchfiles package legitimacy on PyPI (T-10-SC)</name>
  <action>HUMAN CHECKPOINT — do not auto-approve. Verify watchfiles legitimacy on pypi.org per how-to-verify below BEFORE Task 1 runs `uv add watchfiles`. This gate is blocking-human (slopcheck unavailable this session).</action>
  <what-built>About to add `watchfiles>=1.2.0` as a NEW runtime dependency. RESEARCH treats it as `[VERIFIED: PyPI]` (Samuel Colvin / pydantic author, 304M downloads/month, Rust `notify` backend, named explicitly by the ROADMAP), but slopcheck was not installable this session, so this belt-and-suspenders gate confirms legitimacy before install (T-10-SC).</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/watchfiles/ — confirm latest is `1.2.0` (uploaded 2026-05-18), `Requires: Python >=3.10`, maintainer `samuelcolvin`, source `github.com/samuelcolvin/watchfiles`.
    2. Confirm `watchfiles` is the library the ROADMAP Phase 10 line names (it is) and the locked D-01 pick.
    3. Confirm it is NOT a typosquat (not `watch-files`, `watchfile`, `whatchfiles`).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `uv add watchfiles`, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add watchfiles>=1.2.0 runtime dependency via uv</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (the sorted `[project].dependencies` block, lines 6-14 — insert alphabetically after `tenacity`)
    - .planning/phases/10-file-watch-auto-reload/10-RESEARCH.md (Standard Stack → Installation: `uv add watchfiles`; Version verification: latest 1.2.0, requires-python >=3.10 satisfies project >=3.12)
    - .planning/phases/10-file-watch-auto-reload/10-PATTERNS.md (pyproject.toml section — exact alphabetical placement and the `watchfiles>=1.2.0` pin)
  </read_first>
  <action>
    Run `uv add watchfiles` (NOT `pip install`, NOT `--dev`). This adds `watchfiles>=1.2.0` to the `[project].dependencies` list in pyproject.toml and regenerates `uv.lock`. Confirm the resolved version is `>=1.2.0`. The pin must land in the runtime dependency list (alphabetical, after `tenacity`), never the `[dependency-groups].dev` list. Do NOT add `twilio`/`python-telegram-bot` (deferred channel deps — out of scope).
  </action>
  <verify>
    <automated>uv run python -c "import watchfiles; print(watchfiles.__version__)" && grep -c 'watchfiles>=1.2.0' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import watchfiles"` exits 0 (importable).
    - `grep 'watchfiles' pyproject.toml` shows it under `[project].dependencies`, NOT under `[dependency-groups].dev`.
    - `uv.lock` contains a `watchfiles` entry at a resolved version >= 1.2.0.
    - Full suite import smoke is unbroken: `uv run python -c "import weatherbot.scheduler.daemon"` exits 0.
  </acceptance_criteria>
  <done>`watchfiles>=1.2.0` is a resolved runtime dependency, importable, with `uv.lock` updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ReloadConfig frozen model + Config.reload field</name>
  <files>weatherbot/config/models.py</files>
  <behavior>
    - A `Config` built with no `reload` kwarg has `config.reload.watch is True` (ON by default, D-03).
    - A config TOML with no `[reload]` table loads unchanged (existing configs keep working).
    - `[reload] watch = false` → `config.reload.watch is False`.
    - An unknown key under `[reload]` (e.g. `[reload] foo = 1`) raises `pydantic.ValidationError` (`extra="forbid"`).
    - Rebinding `config.reload` on a built Config raises `pydantic.ValidationError` (`frozen_instance`) — the model is frozen like the other config models.
  </behavior>
  <read_first>
    - weatherbot/config/models.py (the `Reliability` model at lines 150-227 as the frozen `extra="forbid"` analog, and the `Config` class at ~228-250 with its `reliability: Reliability = Field(default_factory=Reliability)` field — the EXACT default-factory idiom to copy; `Field`, `ConfigDict`, `BaseModel` already imported at line 12, no new imports)
    - .planning/phases/10-file-watch-auto-reload/10-PATTERNS.md (ReloadConfig section — the frozen-model header `ConfigDict(extra="forbid", frozen=True)` and the default-factory field idiom)
    - .planning/phases/10-file-watch-auto-reload/10-RESEARCH.md (Pattern 3 — `ReloadConfig.watch: bool = True` under a new `[reload]` table)
  </read_first>
  <action>
    Add a new `ReloadConfig(BaseModel)` class beside `Reliability` with `model_config = ConfigDict(extra="forbid", frozen=True)` and a single field `watch: bool = True` (D-03: ON by default; `[reload] watch = false` disables). Add a `reload: ReloadConfig = Field(default_factory=ReloadConfig)` field to `Config` (mirror the existing `reliability` field exactly), so a config with no `[reload]` table loads unchanged. No new imports needed. No validator beyond the type (a single bool — pydantic coerces/rejects). Do NOT expose the debounce window here (D-05 keeps it a module constant in daemon.py, NOT config).
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x && uv run python -c "from weatherbot.config.models import Config, ReloadConfig; assert Config(locations=[]).reload.watch is True; assert ReloadConfig(watch=False).watch is False; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `Config(locations=[]).reload.watch is True` (default ON).
    - `ReloadConfig(watch=False).watch is False`.
    - `ReloadConfig(foo=1)` raises `pydantic.ValidationError` (extra forbidden) — assert in a quick `python -c` or a model test.
    - Rebinding `cfg.reload = ...` on a built Config raises `pydantic.ValidationError` of type `frozen_instance` (NOT `dataclasses.FrozenInstanceError`).
    - `uv run pytest tests/test_models.py` stays green (the existing model suite is unaffected).
    - `uv run pytest` full suite: the test_filewatch.py nodes that depend ONLY on the toggle (`test_watch_toggle_off_no_observer`) advance past the `Config.reload` AttributeError (may still be RED on the absent observer — that is Plan 10-03).
  </acceptance_criteria>
  <done>`ReloadConfig` + `Config.reload` exist, frozen, `extra="forbid"`, default `watch=True`; existing model tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → project deps | A new third-party package (`watchfiles`) enters the runtime; supply-chain trust boundary. |
| operator config edit → Config validation | The `[reload]` table is operator-controlled, parsed by frozen `extra="forbid"` pydantic — no injection surface (just a bool). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | `uv add watchfiles` (supply chain) | mitigate | Blocking human-verify checkpoint above verifies `watchfiles` on pypi.org (author, version, source, non-typosquat) BEFORE install; RESEARCH `[VERIFIED: PyPI]` with audit row. |
| T-10-03 | Tampering | unknown `[reload]` key smuggled in config | mitigate | `extra="forbid"` on `ReloadConfig` rejects unknown keys at load (fail-loud, not silent). |
| T-10-04 | Tampering | secrets leaking into the `[reload]` toggle | accept | `ReloadConfig` carries ONLY a bool `watch`; no secret field (CONF-02 posture preserved). Secrets stay in `.env` (restart boundary, Pitfall #12). |
</threat_model>

<verification>
- `uv run python -c "import watchfiles"` → 0.
- `uv run python -c "from weatherbot.config.models import Config, ReloadConfig; assert Config(locations=[]).reload.watch is True"` → ok.
- `uv run pytest tests/test_models.py` → green.
</verification>

<success_criteria>
`watchfiles>=1.2.0` is installed and importable; `ReloadConfig` + `Config.reload` exist as a frozen, `extra="forbid"`, default-`watch=True` field; an existing `[reload]`-less config loads unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-file-watch-auto-reload/10-02-SUMMARY.md` when done.
</output>
