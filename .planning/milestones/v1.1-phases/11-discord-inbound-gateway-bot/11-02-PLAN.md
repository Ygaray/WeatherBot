---
phase: 11-discord-inbound-gateway-bot
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - pyproject.toml
  - uv.lock
  - weatherbot/config/models.py
  - weatherbot/config/settings.py
  - .env.example
  - deploy/README.md
autonomous: false
requirements: [CMD-02, CMD-07]
user_setup:
  - service: discord
    why: "Inbound gateway bot needs a bot token + the message_content privileged intent"
    env_vars:
      - name: DISCORD_BOT_TOKEN
        source: "Discord Developer Portal -> Applications -> (your app) -> Bot -> Reset Token"
    dashboard_config:
      - task: "Enable the Message Content Intent (privileged)"
        location: "Discord Developer Portal -> Applications -> (your app) -> Bot -> Privileged Gateway Intents -> Message Content Intent"
must_haves:
  truths:
    - "discord.py>=2.7.1,<3 and cachetools>=6,<8 are runtime deps in pyproject.toml and pinned in uv.lock"
    - "A config.toml with a [bot] table sets Config.bot.operator_id (int); a [bot]-less config still loads (Config.bot is None)"
    - "Settings requires DISCORD_BOT_TOKEN and raises ValidationError at load if absent"
    - "An unknown key under [bot] fails loud (extra=forbid)"
  artifacts:
    - path: "weatherbot/config/models.py"
      provides: "BotConfig frozen model + optional Config.bot"
      contains: "class BotConfig"
    - path: "weatherbot/config/settings.py"
      provides: "discord_bot_token secret field"
      contains: "discord_bot_token"
    - path: "pyproject.toml"
      provides: "discord.py + cachetools runtime deps"
      contains: "discord.py"
  key_links:
    - from: "weatherbot/config/models.py Config"
      to: "BotConfig"
      via: "optional field bot: BotConfig | None = None"
      pattern: "bot:\\s*BotConfig\\s*\\|\\s*None"
    - from: "weatherbot/config/settings.py Settings"
      to: "DISCORD_BOT_TOKEN env var"
      via: "pydantic-settings case-insensitive field map"
      pattern: "discord_bot_token:\\s*str"
---

<objective>
Lay the config + secrets + dependency foundation the bot surface (11-03/11-04) builds on:
add `discord.py` and `cachetools` as runtime deps (D-01 stack), a frozen `BotConfig`
(`operator_id`, D-06) optional on `Config`, and the required `discord_bot_token` secret on
`Settings` (D-14). Update `.env.example` and deploy docs so the new fail-loud secret and
the privileged-intent portal toggle (D-02) are documented, not a surprise 500 at startup.

Purpose: every downstream task imports `discord`/`cachetools` and reads
`config.bot.operator_id` / `settings.discord_bot_token`.
Output: two new runtime deps (pinned), `BotConfig` + `Config.bot`, `Settings.discord_bot_token`,
and updated deploy/secret docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-discord-inbound-gateway-bot/11-CONTEXT.md
@.planning/phases/11-discord-inbound-gateway-bot/11-RESEARCH.md
@.planning/phases/11-discord-inbound-gateway-bot/11-PATTERNS.md
@weatherbot/config/models.py
@weatherbot/config/settings.py
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)

- `weatherbot/config/models.py` — `BotConfig` class (`model_config = ConfigDict(extra="forbid", frozen=True)`, `operator_id: int`); `Config.bot: BotConfig | None = None`
- `weatherbot/config/settings.py` — `Settings.discord_bot_token: str`
- pyproject deps: `discord.py>=2.7.1,<3`, `cachetools>=6,<8`; `uv.lock` entries
- New env var: `DISCORD_BOT_TOKEN`; new `[bot]` config section
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (discord.py, cachetools)</name>
  <what-built>
    Nothing yet — this is the slopcheck human-verify gate BEFORE the `uv add`. RESEARCH.md
    "Package Legitimacy Audit" flagged that slopcheck could not run in the research sandbox
    (A1), so both packages are treated as requiring a brief human-verify ack per the
    graceful-degradation rule, despite being long-established, high-trust deps.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/discord.py/ — confirm the distribution name is
       `discord.py` (NOT the abandoned `discord` package), latest stable is 2.7.x, source
       repo is github.com/Rapptz/discord.py (~14k stars), tens-of-millions monthly downloads.
    2. Open https://pypi.org/project/cachetools/ — confirm source repo github.com/tkem/cachetools,
       10+ years old, tens-of-millions monthly downloads.
    3. Confirm the install string will be `uv add 'discord.py>=2.7.1' 'cachetools>=6'`
       (resolves to the Rapptz package, import name `discord`).
  </how-to-verify>
  <action>
    No code change. Verify the two PyPI package pages (links in how-to-verify) and
    acknowledge the install string before the `uv add` in Task 2 runs. This is the
    graceful-degradation legitimacy gate (slopcheck unavailable in research, A1).
  </action>
  <acceptance_criteria>
    - Operator confirms both packages are the legitimate, maintained projects (not the
      abandoned `discord` PyPI package).
  </acceptance_criteria>
  <resume-signal>Type "approved" once both PyPI pages are verified, or name the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add discord.py + cachetools deps; uv lock</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml lines 6-15 (the `dependencies = [...]` array — house style: version
      floors with major-version ceilings, e.g. `apscheduler>=3.11.2,<4`, alphabetical order)
    - .planning/phases/11-discord-inbound-gateway-bot/11-RESEARCH.md (Standard Stack: `<3`/`<8`
      ceilings; "Installation" — `uv add`, NOT pip/dev; Phase 10 watchfiles precedent)
  </read_first>
  <action>
    Run `uv add 'discord.py>=2.7.1' 'cachetools>=6'` to add both as RUNTIME deps (not dev),
    matching the Phase 10 watchfiles precedent. Then hand-edit pyproject.toml if needed so
    the version constraints carry the house-style major ceilings: `discord.py>=2.7.1,<3`
    and `cachetools>=6,<8`, placed in alphabetical position within the `dependencies` array
    (cachetools after apscheduler/before discord-webhook; discord.py after discord-webhook).
    Re-run `uv lock` if you hand-edited so `uv.lock` reflects the pins. Verify the import
    name resolves: `uv run python -c "import discord, cachetools; print(discord.__version__)"`
    prints a 2.7.x version.
  </action>
  <verify>
    <automated>uv run python -c "import discord, cachetools; assert discord.__version__.startswith('2.7'), discord.__version__; print('ok', discord.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'discord.py' pyproject.toml` shows `discord.py>=2.7.1,<3`.
    - `grep -n 'cachetools' pyproject.toml` shows `cachetools>=6,<8`.
    - `grep -c 'discord.py\|cachetools' uv.lock` is >= 2 (both pinned in the lock).
    - `uv run python -c "import discord, cachetools; print(discord.__version__)"` prints 2.7.x.
    - The full existing suite still imports: `uv run pytest --collect-only -q` shows no new errors.
  </acceptance_criteria>
  <done>Both runtime deps added, pinned with house-style ceilings, and importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BotConfig + Config.bot + Settings.discord_bot_token + docs</name>
  <files>weatherbot/config/models.py, weatherbot/config/settings.py, .env.example, deploy/README.md, tests/test_models.py, tests/test_settings.py</files>
  <read_first>
    - weatherbot/config/models.py lines 138-147 (`WebhookIdentity` frozen model), 230-245
      (`ReloadConfig` frozen model), 248-261 (`Config` — how `webhook`/`reliability`/`reload`
      are wired with defaults)
    - weatherbot/config/settings.py lines 14-29 (`Settings` class: `openweather_api_key`,
      `discord_webhook_url`, `SettingsConfigDict(env_file=".env", extra="ignore")`)
    - .planning/phases/11-discord-inbound-gateway-bot/11-PATTERNS.md (BotConfig + Settings sections)
    - .planning/phases/11-discord-inbound-gateway-bot/11-RESEARCH.md (Pattern 5: single
      `operator_id: int` NOT a list; `bot: BotConfig | None = None` NOT default_factory;
      Open Question Q1 — token is REQUIRED `str` per D-14)
  </read_first>
  <behavior>
    - Test: a config.toml with `[bot]\noperator_id = 555` loads → `config.bot.operator_id == 555`.
    - Test: a config.toml with NO `[bot]` table loads → `config.bot is None`.
    - Test: `[bot]` with an unknown key (e.g. `extra = 1`) raises pydantic ValidationError (extra=forbid).
    - Test: rebinding `config.bot.operator_id` raises ValidationError (frozen).
    - Test: `Settings()` with no `DISCORD_BOT_TOKEN` in env/.env raises ValidationError; with it set, `settings.discord_bot_token` returns the value.
  </behavior>
  <action>
    In weatherbot/config/models.py add a `BotConfig(BaseModel)` with
    `model_config = ConfigDict(extra="forbid", frozen=True)` and a single field
    `operator_id: int` (Discord user ID — non-secret identity, D-06; single int NOT a list
    per RESEARCH Pattern 5 / A3). Wire it onto `Config` as `bot: BotConfig | None = None`
    (plain optional with `None` default — NOT `Field(default_factory=...)` — because absence
    MUST mean "no bot", per PATTERNS.md). Place the `bot` field alongside the existing
    `webhook`/`reliability`/`reload` fields.

    In weatherbot/config/settings.py append `discord_bot_token: str` after
    `discord_webhook_url` (line 29). It is REQUIRED (no default) per D-14 — pydantic-settings
    maps it case-insensitively to `DISCORD_BOT_TOKEN`. Do NOT change `SettingsConfigDict`
    (`extra="ignore"` is correct).

    Update `.env.example`: add a `DISCORD_BOT_TOKEN=` line with a comment that it is the
    inbound gateway bot token from the Discord Developer Portal and is REQUIRED at startup.
    Update `deploy/README.md`: document (a) the new required `DISCORD_BOT_TOKEN` env var,
    (b) the mandatory Discord Developer Portal "Message Content Intent" privileged-intent
    toggle (D-02 deploy step), and (c) that an existing `.env` lacking the token now
    fails loud at startup (intended). NEVER put the token in config.toml.

    Add the behavior tests above to tests/test_models.py (BotConfig cases) and
    tests/test_settings.py (or the existing settings test module) — GREEN after this task.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -k "bot or BotConfig" tests/test_settings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'class BotConfig' weatherbot/config/models.py` returns the class.
    - `grep -nE 'bot:\s*BotConfig\s*\|\s*None' weatherbot/config/models.py` returns the field.
    - `grep -nE 'discord_bot_token:\s*str' weatherbot/config/settings.py` returns the field (no `| None`, no default).
    - `grep -c '^#' .env.example >/dev/null; grep -v '^#' .env.example | grep -c DISCORD_BOT_TOKEN` is >= 1.
    - `grep -ci 'message content intent' deploy/README.md` is >= 1.
    - `uv run pytest tests/test_models.py -k "bot or BotConfig" tests/test_settings.py -x` passes.
    - Full suite green: `uv run pytest -q` (existing 215+ tests still pass; no [bot]-less config regression).
  </acceptance_criteria>
  <done>BotConfig + optional Config.bot + required Settings.discord_bot_token exist, docs updated, tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `.env` | `DISCORD_BOT_TOKEN` bearer secret crosses here (git-ignored) |
| config.toml → process | non-secret `operator_id` crosses here (reloadable, untrusted-file but schema-validated) |
| PyPI → build | two new third-party packages installed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Information disclosure / Elevation | bot token committed in config.toml or git | mitigate | Token ONLY in `.env` via `Settings` (D-14); never in `config.toml`/holder; `.env.example` documents it; deploy docs note the pre-commit secret-scan addition |
| T-11-03 | Tampering | unknown/extra key under `[bot]` smuggles config | mitigate | `BotConfig` `extra="forbid"` fails loud (test-asserted) |
| T-11-04 | Spoofing | wrong/abandoned `discord` PyPI package | mitigate | Human-verify legitimacy gate (Task 1) confirms `discord.py` / Rapptz before `uv add` |
| T-11-SC | Tampering | uv installs of discord.py / cachetools | mitigate | Blocking human-verify legitimacy checkpoint (Task 1) precedes the install; `uv.lock` pins exact versions |
</threat_model>

<verification>
- `uv run python -c "import discord, cachetools; print(discord.__version__)"` → 2.7.x.
- `uv run pytest tests/test_models.py -k "bot or BotConfig" tests/test_settings.py -x` → GREEN.
- `uv run pytest -q` → full suite green (no [bot]-less config regression).
- `.env.example` + `deploy/README.md` document the token and the portal intent toggle.
</verification>

<success_criteria>
discord.py + cachetools are pinned runtime deps; `BotConfig`/`Config.bot` and
`Settings.discord_bot_token` exist with the frozen + fail-loud semantics; the privileged-intent
portal step and the new required secret are documented.
</success_criteria>

<output>
Create `.planning/phases/11-discord-inbound-gateway-bot/11-02-SUMMARY.md` when done
</output>
