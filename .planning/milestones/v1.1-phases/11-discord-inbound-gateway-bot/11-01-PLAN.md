---
phase: 11-discord-inbound-gateway-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_bot.py
  - tests/test_cache.py
  - tests/conftest.py
  - tests/test_reload.py
autonomous: true
requirements: [CMD-02, CMD-06, CMD-07, CMD-08, CFG-07]
must_haves:
  truths:
    - "All Phase 11 behavior node IDs COLLECT under pytest while failing RED on the not-yet-built bot/cache symbols"
    - "A fake_discord_message factory exists in conftest with an AsyncMock channel and configurable author"
    - "CFG-07 success + rejection posting tests exist in test_reload.py and fail RED"
  artifacts:
    - path: "tests/test_bot.py"
      provides: "RED guard-ladder / reply-embed / executor / isolation node IDs (CMD-02/07/08, Pitfall 1)"
      min_lines: 80
    - path: "tests/test_cache.py"
      provides: "RED TTL hit/miss/expiry node IDs (CMD-06)"
      min_lines: 40
    - path: "tests/conftest.py"
      provides: "fake_discord_message factory fixture"
      contains: "fake_discord_message"
    - path: "tests/test_reload.py"
      provides: "CFG-07 reload-outcome posting node IDs"
      contains: "cfg07"
  key_links:
    - from: "tests/test_bot.py"
      to: "weatherbot.interactive.bot"
      via: "deferred per-test import"
      pattern: "import.*interactive\\.bot|from weatherbot\\.interactive import bot"
    - from: "tests/test_cache.py"
      to: "weatherbot.interactive.cache"
      via: "deferred per-test import"
      pattern: "interactive\\.cache|ForecastCache"
---

<objective>
Lay the Nyquist RED scaffold for Phase 11: every behavioral node ID for the inbound
Discord bot (CMD-02/06/07/08) and the CFG-07 reload-outcome post must COLLECT under
pytest while failing RED on the not-yet-built `weatherbot.interactive.bot` /
`weatherbot.interactive.cache` symbols. This follows the project's established Wave-0
deferred-import idiom (Phase 8/9/10 lesson: defer the not-yet-built import into a
per-test lazy import / helper so node IDs COLLECT instead of erroring at collection and
hiding the IDs).

Purpose: Make the implementation plans (11-03/11-04) GREEN-able against a fixed,
visible contract — the executor sees the expected behavior before writing the code.
Output: `tests/test_bot.py`, `tests/test_cache.py` (NEW), a `fake_discord_message`
factory in `tests/conftest.py`, and CFG-07 posting tests appended to `tests/test_reload.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-discord-inbound-gateway-bot/11-CONTEXT.md
@.planning/phases/11-discord-inbound-gateway-bot/11-RESEARCH.md
@.planning/phases/11-discord-inbound-gateway-bot/11-PATTERNS.md
@tests/conftest.py
@tests/test_reload.py
</context>

<artifacts_produced>
## Artifacts this phase produces (new symbols — exclude from drift verification)

- `weatherbot/interactive/bot.py` — module; `build_client(*, holder, operator_id, cache)`, `build_inbound_embed(forecast)`, `BotThread` class (`start()`, `stop()`, `_run()`, `_amain()`), `on_ready`/`on_message` handlers
- `weatherbot/interactive/cache.py` — module; `ForecastCache` class (`lookup(name, config)`, `invalidate()`)
- `weatherbot/config/models.py` — `BotConfig` class with `operator_id: int`; `Config.bot: BotConfig | None`
- `weatherbot/config/settings.py` — `Settings.discord_bot_token: str` field
- `tests/test_bot.py`, `tests/test_cache.py` — new test modules
- `tests/conftest.py` — `fake_discord_message` fixture
- pyproject deps: `discord.py>=2.7.1,<3`, `cachetools>=6,<8`
- New env var: `DISCORD_BOT_TOKEN`
- New config section: `[bot]` with `operator_id`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: fake_discord_message factory + RED test_bot.py / test_cache.py</name>
  <files>tests/conftest.py, tests/test_bot.py, tests/test_cache.py</files>
  <read_first>
    - tests/conftest.py (the existing fixture style: `_load_fixture`, `tmp_db`, `holder_scheduler`, `_make` — mirror this factory voice)
    - .planning/phases/11-discord-inbound-gateway-bot/11-RESEARCH.md (section "How to test a discord.py handler WITHOUT a live gateway" + "Phase Requirements → Test Map")
    - .planning/phases/11-discord-inbound-gateway-bot/11-PATTERNS.md (Pattern 2 guard ladder ORDER, Pattern 4 cache contract)
    - weatherbot/interactive/lookup.py (lookup_weather signature + UnknownLocationError carrying .valid_names)
    - weatherbot/interactive/command.py (CommandKind.NOT_A_COMMAND / DEFAULT / LOCATED)
  </read_first>
  <action>
    Add a `fake_discord_message` pytest fixture to tests/conftest.py: a factory returning
    an object (build with `unittest.mock.MagicMock`) exposing `author.bot` (bool),
    `author.id` (int), `content` (str), and `channel` where `channel.send` is an
    `AsyncMock`, `channel.typing` returns an async-context-manager mock (an object whose
    `__aenter__`/`__aexit__` are AsyncMocks). Default kwargs: `author_bot=False`,
    `author_id=12345`, `content="!weather home"`. Keep the factory pure (no network, no
    discord import).

    Create tests/test_bot.py with RED node IDs covering, via the deferred-import idiom
    (import `weatherbot.interactive.bot` INSIDE each test or a per-test helper, NOT at
    module top, so collection succeeds and each test fails RED on ModuleNotFoundError):
    - `test_guard_webhook_author_fires_nothing` (CMD-07): author.bot=True → assert the
      lookup spy was NOT called and `channel.send` was NOT awaited.
    - `test_guard_non_operator_silently_ignored` (CMD-07): author.bot=False, author.id !=
      operator_id → assert no send, no lookup.
    - `test_located_reply_builds_embed` (CMD-02): `!weather home` from operator → assert
      `channel.send` awaited with an `embed=` kwarg (the embed built from forecast).
    - `test_unknown_location_replies_valid_names` (CMD-02 error path): lookup raises
      `UnknownLocationError(requested, valid_names)` → assert `channel.send` got text
      containing the valid_names.
    - `test_blocking_work_runs_off_loop` (Pitfall 1): assert the cache/lookup call is
      dispatched through `loop.run_in_executor` (patch/spy run_in_executor) and that
      `lookup_weather` is never called directly on the coroutine.
    - `test_handler_exception_does_not_propagate` (CMD-08): make the executor call raise
      → assert `on_message` returns without raising and an error reply was sent.

    Create tests/test_cache.py with RED node IDs (deferred import of
    `weatherbot.interactive.cache.ForecastCache`):
    - `test_second_lookup_within_ttl_hits_cache` (CMD-06): patch `lookup_weather` with a
      counting spy; call `cache.lookup("home", cfg)` twice → assert spy called once.
    - `test_lookup_after_ttl_refetches` (CMD-06): advance clock past TTL (inject a
      controllable timer into TTLCache OR use time-machine) → assert a second fetch.
    - `test_distinct_locations_distinct_entries`: two different configured names → two
      fetches.
    - `test_invalidate_clears_cache`: after `cache.invalidate()` a repeat call refetches.

    Mark every test that asserts behavior of the unbuilt module so it fails RED on
    import, NOT on collection. Do NOT write the production modules in this plan.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bot.py tests/test_cache.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_bot.py tests/test_cache.py --collect-only -q` lists all
      10 node IDs above with zero collection errors.
    - `uv run pytest tests/test_bot.py tests/test_cache.py -x` fails RED (ModuleNotFoundError
      or AttributeError for `weatherbot.interactive.bot` / `.cache`), NOT a collection error.
    - `grep -n 'fake_discord_message' tests/conftest.py` returns the fixture definition.
    - No top-of-module `import weatherbot.interactive.bot` / `.cache` in either test file
      (verify: `grep -n '^from weatherbot.interactive.bot\|^import weatherbot.interactive.bot' tests/test_bot.py` returns nothing).
  </acceptance_criteria>
  <done>Both new test files collect cleanly and fail RED; the message factory exists.</done>
</task>

<task type="auto">
  <name>Task 2: CFG-07 reload-outcome posting tests in test_reload.py</name>
  <files>tests/test_reload.py</files>
  <read_first>
    - tests/test_reload.py (the existing `_do_reload` test harness: how it constructs holder/scheduler, passes `channel=`, and triggers apply / keep-old / rollback)
    - weatherbot/scheduler/daemon.py lines 549-560 (`_do_reload` signature — already takes `channel`), 586-594 (PHASE-1 reject branch: `_log.error("reload rejected", reason=str(exc))`), 637-646 (success: `summary = f"+{added} -{removed} ~{changed} ={unchanged}"`)
    - .planning/phases/11-discord-inbound-gateway-bot/11-RESEARCH.md (Pattern 6, D-13 "capture the tuple, not the log line")
  </read_first>
  <action>
    Append CFG-07 RED tests to tests/test_reload.py (node IDs contain the substring
    `cfg07` so the research's `-k cfg07` selector works):
    - `test_cfg07_success_posts_summary`: call `_do_reload` with a valid config edit and a
      spy `channel` (a stub whose `.send` records calls) → assert `channel.send` received
      a message containing the `+{added} -{removed} ~{changed} ={unchanged}` summary string.
    - `test_cfg07_rejection_posts_reason`: call `_do_reload` with an invalid config
      (bad TOML or duplicate name) and a spy `channel` → assert `_do_reload` still raises
      (keep-old contract preserved) AND `channel.send` received a message containing the
      validation reason text (`str(exc)`).
    - `test_cfg07_channel_send_failure_does_not_abort_reload`: spy `channel.send` raises →
      assert the reload outcome is unchanged (success still swaps, rejection still raises
      the ORIGINAL validation error, not the send error).

    These fail RED today because `_do_reload` does not yet call `channel.send` on either
    branch (11-04 adds it). Reuse the existing test_reload.py fixtures/harness; do NOT
    rewrite passing reload tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reload.py -k cfg07 --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reload.py -k cfg07 --collect-only -q` lists the 3 cfg07
      node IDs with zero collection errors.
    - `uv run pytest tests/test_reload.py -k cfg07 -x` fails RED (channel.send not yet
      called by `_do_reload`), NOT a collection error.
    - Pre-existing test_reload.py tests still COLLECT: `uv run pytest tests/test_reload.py --collect-only -q` shows no new collection errors.
  </acceptance_criteria>
  <done>CFG-07 success/rejection/send-failure node IDs collect and fail RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → unbuilt modules | Deferred import only; no live gateway, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | RED scaffold masks node IDs via top-level import | mitigate | Deferred per-test import idiom (Phase 8/9/10 lesson) — collection must succeed, tests fail RED on import |
| T-11-SC | Tampering | npm/pip/uv installs | mitigate | No installs in this plan; package legitimacy gate is in 11-02 |
</threat_model>

<verification>
- `uv run pytest tests/test_bot.py tests/test_cache.py tests/test_reload.py --collect-only -q` — all node IDs collect, zero errors.
- `uv run pytest tests/test_bot.py tests/test_cache.py -x` — RED on the unbuilt modules.
- Pre-existing suite unaffected at collection: `uv run pytest --collect-only -q` shows no new errors.
</verification>

<success_criteria>
All CMD-02/06/07/08 + CFG-07 behavior node IDs exist, COLLECT, and fail RED via the
deferred-import idiom; the `fake_discord_message` factory is in conftest.
</success_criteria>

<output>
Create `.planning/phases/11-discord-inbound-gateway-bot/11-01-SUMMARY.md` when done
</output>
