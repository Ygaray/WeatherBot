---
phase: 06-shared-lookup-core-command-parser
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - weatherbot/cli.py
  - weatherbot/interactive/__init__.py
  - tests/test_interactive_package.py
autonomous: true
requirements:
  - "foundation — no v1.1 requirement closes here; this plan delivers Success Criterion #4 (send_now byte-identical) and the package barrel both surfaces import from"
must_haves:
  truths:
    - "send_now delegates its read-only HEAD (resolve→fetch→build→validate→render) to lookup_weather, passing schedule_placeholders via extra_placeholders, and keeps its existing deliver+persist TAIL untouched"
    - "send_now produces byte-identical scheduled briefings after the refactor — the existing tests/test_send_now.py stays unmodified and green (criterion #4)"
    - "weatherbot.interactive re-exports lookup_weather, LookupResult, UnknownLocationError, parse_weather_command, Command, CommandKind from a single barrel"
    - "import weatherbot.cli and import weatherbot.interactive both succeed (no circular import)"
  artifacts:
    - path: "weatherbot/interactive/__init__.py"
      provides: "Public package barrel re-exporting both leaf modules with explicit __all__"
      contains: "__all__"
      exports: ["lookup_weather", "LookupResult", "UnknownLocationError", "parse_weather_command", "Command", "CommandKind"]
    - path: "weatherbot/cli.py"
      provides: "send_now refactored to delegate fetch→render to lookup_weather; tail unchanged"
      contains: "lookup_weather"
    - path: "tests/test_interactive_package.py"
      provides: "Barrel re-export + no-import-cycle smoke test"
      contains: "from weatherbot.interactive import"
  key_links:
    - from: "weatherbot/cli.py"
      to: "weatherbot.interactive.lookup_weather"
      via: "send_now delegation"
      pattern: "lookup_weather\\("
    - from: "weatherbot/cli.py"
      to: "schedule_placeholders"
      via: "extra_placeholders kwarg into lookup_weather"
      pattern: "extra_placeholders="
---

<objective>
Wire the read-only core into the v1.0 scheduled path and ship the package barrel.
`send_now` (weatherbot/cli.py) delegates its read-only HEAD to `lookup_weather`,
passing its `schedule_placeholders(...)` dict through the `extra_placeholders` seam,
and keeps its EXISTING deliver+persist TAIL byte-for-byte (D-08, criterion #4). Create
the `weatherbot/interactive/__init__.py` barrel that re-exports both leaf modules so P7
and P11 can `from weatherbot.interactive import lookup_weather, parse_weather_command`.

Purpose: Single source of truth for fetch→render with zero duplication, while the exactly-once delivery/persist ordering that v1.0 depends on is provably unchanged (the existing tests/test_send_now.py is the contractual byte-identical gate).
Output: refactored `send_now` in `weatherbot/cli.py`, the `weatherbot/interactive/__init__.py` barrel, and a small `tests/test_interactive_package.py` smoke test.

This plan is Wave 2 — it depends on 06-01 (command.py) and 06-02 (lookup.py + UnknownLocationError),
because the barrel re-exports both and send_now imports lookup_weather.

LOCKED DECISIONS adopted: D-08 (send_now delegates; only the read-only HEAD changes;
delivery/persist ordering untouched; byte-identical is the bar). RESEARCH Pattern 1 Option A
(extra_placeholders seam preserves the exact merge order). Pitfall 1 (do NOT drop/reorder the
schedule_placeholders merge). Pitfall 3 (top-level `from weatherbot.interactive import
lookup_weather` in cli.py is safe because the cli→interactive resolution is the lazy
build_client edge on the lookup side).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-lookup-core-command-parser/06-CONTEXT.md
@.planning/phases/06-shared-lookup-core-command-parser/06-RESEARCH.md
@.planning/phases/06-shared-lookup-core-command-parser/06-PATTERNS.md
@weatherbot/cli.py
</context>

<artifacts_this_phase_produces>
This plan (06-03) creates / modifies:
- `weatherbot/cli.py` — `send_now` refactored: read-only HEAD (L113-162) replaced by a `lookup_weather(...)` call passing `extra_placeholders=schedule_placeholders(schedule_ctx, sent_dt, checked_dt)`; the deliver+persist TAIL (channel.send_briefing → persist on result.ok → log → return) is preserved exactly, now reading `.text`/`.forecast`/`.location` off the LookupResult.
- `weatherbot/interactive/__init__.py` — barrel re-exporting `lookup_weather`, `LookupResult`, `UnknownLocationError` (from `.lookup`) and `parse_weather_command`, `Command`, `CommandKind` (from `.command`) with explicit `__all__`.
- `tests/test_interactive_package.py` — barrel re-export + no-import-cycle smoke test.

(This completes the phase artifact inventory: __init__.py [here], command.py [06-01], lookup.py [06-02], resolve_location upgrade [06-02], send_now delegation [here], test_command.py [06-01], test_lookup.py [06-02], test_interactive_package.py [here].)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the interactive package barrel</name>
  <files>weatherbot/interactive/__init__.py</files>
  <read_first>
    - weatherbot/config/__init__.py (lines 1-21: the barrel re-export + explicit `__all__` house style to copy)
    - .planning/phases/06-shared-lookup-core-command-parser/06-PATTERNS.md § "interactive/__init__.py" (lines ~318-337)
  </read_first>
  <action>
    Create weatherbot/interactive/__init__.py mirroring config/__init__.py: `from .command import Command, CommandKind, parse_weather_command` and `from .lookup import LookupResult, UnknownLocationError, lookup_weather`, plus an explicit `__all__ = ["Command", "CommandKind", "LookupResult", "UnknownLocationError", "lookup_weather", "parse_weather_command"]`. Add a one-line module docstring describing the package as the shared read-only lookup core + command parser for the CLI (P7) and Discord bot (P11) surfaces. Do not add logic — barrel only.
  </action>
  <verify>
    <automated>uv run python -c "from weatherbot.interactive import lookup_weather, LookupResult, UnknownLocationError, parse_weather_command, Command, CommandKind; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `from weatherbot.interactive import lookup_weather, LookupResult, UnknownLocationError, parse_weather_command, Command, CommandKind` succeeds
    - weatherbot/interactive/__init__.py defines `__all__` listing all six public names
    - `uv run ruff check weatherbot/interactive/__init__.py` passes
  </acceptance_criteria>
  <done>The barrel re-exports all six public symbols from the two leaf modules with an explicit __all__.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor send_now to delegate to lookup_weather (byte-identical)</name>
  <files>weatherbot/cli.py</files>
  <read_first>
    - weatherbot/cli.py (lines 90-184: the full send_now — HEAD L113-162 to replace, TAIL L164-184 to preserve EXACTLY; the schedule-timing computation L153-155 stays in send_now; the lazy daemon import precedent L478)
    - weatherbot/interactive/lookup.py (the lookup_weather signature + extra_placeholders semantics from 06-02 — values.update(extra_placeholders) overrides the on-demand timing keys, preserving send_now's merge precedence)
    - tests/test_send_now.py (ALL tests — the byte-identical regression gate; specifically the dual-fetch order, metric-primary temp_display, schedule-note rendering, and persist row-count assertions; DO NOT modify this file)
    - .planning/phases/06-shared-lookup-core-command-parser/06-RESEARCH.md § "Pattern 1" Option A (lines ~177-213) and § "Pitfall 1" (lines ~298-302)
    - .planning/phases/06-shared-lookup-core-command-parser/06-PATTERNS.md § "send_now refactor" (lines ~207-237)
  </read_first>
  <action>
    Add a top-level import `from weatherbot.interactive import lookup_weather` to cli.py (safe per Pitfall 3 — the cli→interactive cycle is broken on the lookup side via the lazy build_client import). In `send_now`, REPLACE the read-only HEAD (resolve_location → client guard → dual fetch → Forecast.from_payloads → load_template/validate_template → the render() merge, currently L113-162) with a single `lookup_weather(...)` call. Keep the schedule-timing computation in send_now (L153-155: `tz = schedule_ctx.tz if schedule_ctx is not None else ZoneInfo(location.timezone)`, `sent_dt = datetime.now(tz)`, `checked_dt = datetime.now(tz)`) and pass `extra_placeholders=schedule_placeholders(schedule_ctx, sent_dt, checked_dt)` into lookup_weather so the scheduled timing keys layer ON TOP exactly as today (Pitfall 1 — preserve merge order/precedence). Call form: `result_lr = lookup_weather(location_name, config=config, settings=settings, client=client, templates_dir=templates_dir, extra_placeholders=schedule_placeholders(schedule_ctx, sent_dt, checked_dt))`. Preserve the EXISTING channel-build guard (L119-122 — channel is a send_now concern, NOT moved into lookup_weather) and the EXISTING TAIL untouched in ordering: `result = channel.send_briefing(result_lr.text, result_lr.forecast)`; `if result.ok: persist(db_path, result_lr.location, result_lr.forecast)`; the `_log.info("send_now complete", location=result_lr.location.name, delivered=result.ok)`; `return result`. Do NOT change the persist ordering, the result.ok gate, or the return type (DeliveryResult). After the edit, remove any now-unused imports in cli.py ONLY if they are genuinely unreferenced (e.g. if resolve_location/Forecast/load_template/render are no longer used directly in cli.py) — verify with ruff; if still referenced elsewhere in cli.py, leave them.
  </action>
  <verify>
    <automated>uv run pytest tests/test_send_now.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_send_now.py -q` exits 0 with NO modification to tests/test_send_now.py (byte-identical gate — criterion #4)
    - `uv run pytest -q` (full suite) exits 0 (no regression across test_renderer/test_store/test_cli)
    - `grep -n 'lookup_weather' weatherbot/cli.py` shows send_now calling lookup_weather
    - `grep -n 'extra_placeholders=' weatherbot/cli.py` shows schedule_placeholders passed through extra_placeholders
    - The deliver+persist tail order is unchanged: send_briefing → `if result.ok: persist(...)` → log → return (persist still gated on result.ok)
    - `git diff tests/test_send_now.py` is empty
    - `uv run ruff check weatherbot/cli.py` passes
  </acceptance_criteria>
  <done>send_now delegates fetch→render to lookup_weather via the extra_placeholders seam, keeps its deliver+persist tail byte-identical, and the full existing suite stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the barrel re-export + no-import-cycle smoke test</name>
  <files>tests/test_interactive_package.py</files>
  <read_first>
    - tests/test_renderer.py (lines 1-45: pure-logic unit-test idiom)
    - weatherbot/interactive/__init__.py (the barrel from Task 1)
    - .planning/phases/06-shared-lookup-core-command-parser/06-RESEARCH.md § "Pitfall 3" (lines ~310-317) and § "Runtime State Inventory" build-artifacts row (line ~292: verify `import weatherbot.interactive`)
  </read_first>
  <action>
    Create tests/test_interactive_package.py with: (1) a test that `from weatherbot.interactive import lookup_weather, LookupResult, UnknownLocationError, parse_weather_command, Command, CommandKind` succeeds and that all six are not None; (2) a test that asserts `UnknownLocationError` is a subclass of `ValueError` (D-07 contract re-checked at the package surface); (3) a no-import-cycle test that imports both `weatherbot.cli` and `weatherbot.interactive` in the same process and asserts `weatherbot.cli.send_now` and `weatherbot.interactive.lookup_weather` are callable (guards the D-08 cli↔interactive edge — Pitfall 3). Use plain asserts, no fixtures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_interactive_package.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_interactive_package.py -x -q` exits 0
    - The suite asserts all six barrel symbols import and that UnknownLocationError is-a ValueError
    - The no-cycle test imports both weatherbot.cli and weatherbot.interactive and passes
    - `uv run ruff check tests/test_interactive_package.py` passes
  </acceptance_criteria>
  <done>The barrel re-export, the ValueError-subclass contract, and the no-import-cycle invariant are all covered by a passing smoke test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler/manual caller → send_now | Existing v1.0 boundary, unchanged — send_now still owns deliver+persist and the scheduler-timing merge. |
| cli ↔ interactive (import graph) | New D-08 edge: cli.send_now imports lookup_weather; the reverse (lookup→cli build_client) is lazy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering (silent output drift) | send_now render merge | mitigate | The `schedule_placeholders` merge is preserved via `extra_placeholders` with `values.update(...)` order intact (Pattern 1 Option A / Pitfall 1). The unmodified tests/test_send_now.py — which asserts rendered body content, dual-fetch order, metric-primary temp_display, schedule-note, and persist row count — is the byte-identical gate; `git diff` on it must be empty. |
| T-06-09 | EoP (import cycle → partial-init module) | cli↔interactive imports | mitigate | Top-level cli→interactive import is safe because the only runtime cli→interactive resolution is the lazy build_client edge on the lookup side (Pitfall 3). The no-import-cycle smoke test (Task 3) asserts both packages import in one process. |
| T-06-10 | Repudiation (exactly-once / persist ordering) | send_now deliver+persist tail | mitigate | The deliver→`if result.ok: persist`→log→return ordering is preserved verbatim (D-08); persist stays gated on result.ok so a failed delivery still writes no weather_onecall row. Verified by the persist row-count assertion in the unchanged test_send_now.py. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero `uv add`; RESEARCH § Package Legitimacy Audit not applicable). No supply-chain surface. |

No HIGH-severity threat. Output drift (T-06-08) and persist-ordering (T-06-10) are the regression risks that most directly touch a shipped v1.0 guarantee; both are gated by the unmodified test_send_now.py plus the empty-diff acceptance check.
</threat_model>

<verification>
- `uv run pytest tests/test_send_now.py -q` exits 0 with `git diff tests/test_send_now.py` empty (criterion #4 byte-identical gate).
- `uv run pytest -q` (full suite) exits 0 — no regression.
- `uv run python -c "import weatherbot.cli, weatherbot.interactive"` exits 0 (no cycle).
- `uv run pytest tests/test_interactive_package.py -x -q` exits 0.
- `uv run ruff check` and `uv run ruff format --check` pass for cli.py, the barrel, and the new test.
</verification>

<success_criteria>
- ROADMAP Success Criterion #4 satisfied: `send_now` still produces byte-identical scheduled briefings after the extraction (no regression in the v1.0 path; existing tests stay green and unmodified).
- D-08 implemented exactly: send_now delegates fetch→render to lookup_weather; only the read-only HEAD changed; delivery/persist ordering untouched.
- The `weatherbot.interactive` barrel exports all six public symbols; both P7 and P11 can import the shared core and parser from one place; no import cycle.
</success_criteria>

<output>
Create `.planning/phases/06-shared-lookup-core-command-parser/06-03-SUMMARY.md` when done.
</output>
