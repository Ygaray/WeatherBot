---
phase: 07-cli-weather-location-one-shot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [weatherbot/cli.py]
autonomous: true
requirements: [CMD-01, CMD-03, CMD-04, CMD-05]
must_haves:
  truths:
    - "`weatherbot weather home` resolves the configured location, prints the briefing to stdout, exits 0 — no daemon, no send, no DB write (CMD-01)"
    - "Bare `weatherbot weather` (no arg) returns the default/primary configured location's briefing (CMD-03)"
    - "`weatherbot weather <unknown>` prints UnknownLocationError's verbatim message (lists valid names) to stderr and exits 1 — no geocode fallback (CMD-04)"
    - "Printed briefing is `LookupResult.text` from `lookup_weather` — the exact v1 template, no separate on-demand format (CMD-05)"
    - "Bad/missing `--config` exits 2 (no traceback); exhausted-transient or 401/403 fetch failure exits 3 (stderr, never key/URL)"
    - "`weather` is quiet by default (no `lookup complete` INFO line on stdout/stderr); `-v` restores INFO (D-09)"
    - "Migrated `run`/`check`/`send-now`/`geocode` subcommands preserve their existing behavior and exit codes"
  artifacts:
    - path: "weatherbot/cli.py"
      provides: "subparser main(), run_weather handler, _cmd_weather dispatcher, quiet-logging logic"
      contains: "add_subparsers"
  key_links:
    - from: "weatherbot/cli.py run_weather"
      to: "weatherbot.interactive.lookup_weather"
      via: "Retrying wrapper (retry_if_exception(is_transient) only)"
      pattern: "retrying\\(\\s*lookup_weather"
    - from: "weatherbot/cli.py main"
      to: "_cmd_weather / do_geocode / do_check / run_send_now / daemon.run_daemon"
      via: "args.command dispatch"
      pattern: "args\\.command =="
    - from: "weatherbot/cli.py run_weather"
      to: "UnknownLocationError"
      via: "except arm placed BEFORE any broad ValueError"
      pattern: "except UnknownLocationError"
---

<objective>
Restructure `weatherbot/cli.py` `main()` from flat `--flag` + `hasattr` dispatch to argparse subparsers (D-01/D-02 clean break), and add the new `weather` subcommand handler — the actual phase deliverable (CMD-01/03/04/05, D-04..D-09).

Purpose: Ship the standalone daemon-free `weather [location]` command that reuses the Phase 6 read-only `lookup_weather` core, while migrating the four existing flags (`run`/`check`/`send-now`/`geocode`) to subcommands with byte-identical behavior. The old `--run`/`--check`/`--send-now`/`--geocode` flags are REMOVED with no aliases.
Output: `weatherbot/cli.py` with `add_subparsers(dest="command")`, a shared `--config` parent parser, `run_weather`/`_cmd_weather`, and quiet-by-default logging for `weather`.

This plan deliberately keeps all changes inside `weatherbot/cli.py` (RESEARCH Recommended Project Structure: the handler lives alongside `run_send_now`/`do_check`/`do_geocode`, avoiding a new cli↔interactive import edge). It does NOT touch tests or deploy artifacts — those are Plan 03 (which depends on this plan).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-weather-location-one-shot/07-CONTEXT.md
@.planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md
@.planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md
@weatherbot/cli.py
@weatherbot/interactive/lookup.py
@weatherbot/interactive/__init__.py
@weatherbot/reliability/retry.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add run_weather handler + _cmd_weather dispatcher</name>
  <files>weatherbot/cli.py</files>
  <read_first>
    - weatherbot/cli.py lines 27-56 (imports + `_log`; confirm `import sys` is MISSING and `UnknownLocationError` is NOT yet imported), lines 175-249 (`run_send_now` — the retry analog to copy), lines 252-291 (`do_geocode` — outcome-only logging analog), lines 367-385 (`_load_config_reporting`)
    - weatherbot/interactive/__init__.py (confirms `UnknownLocationError`, `lookup_weather`, `LookupResult` are exported from `weatherbot.interactive`)
    - weatherbot/interactive/lookup.py lines 41 (`_log`), ~103 (`resolve_location` runs before any fetch), 105-113 (`client=None` lazy build_client seam), 146 (`_log.info("lookup complete", location=location.name)` — the INFO line D-09 suppresses), and the `UnknownLocationError(ValueError)` class definition (~line 44, carries `.requested` + `.valid_names`, formats `"No location named 'X'; configured locations: a, b"`)
    - weatherbot/reliability/retry.py lines ~71 (`PERMANENT = frozenset({400,401,403,404})`), ~72 (`TRANSIENT`), 80-91 (`is_transient` — returns False for UnknownLocationError and for 401/403)
    - .planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md § "new `run_weather` handler" (lines 31-111) and § Shared Patterns (lines 281-311)
    - .planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md Pattern 2, Pitfall 5, § Code Examples
    - .planning/phases/07-cli-weather-location-one-shot/07-CONTEXT.md D-04, D-05, D-06, D-08
  </read_first>
  <behavior>
    - run_weather(name="home", config=cfg, client=fake) → prints LookupResult.text to stdout, returns 0 (CMD-01).
    - run_weather(None, config=cfg, client=fake) → resolves default/first location, returns 0 (CMD-03).
    - run_weather("nope", config=cfg, client=fake) → prints UnknownLocationError verbatim message to stderr (includes valid names), returns 1; the fake client's onecall is NEVER called (resolve_location fails before fetch, CMD-04).
    - run_weather with a client raising httpx.HTTPStatusError(401) → returns 3 on attempt 1 (no retry of permanent auth; D-08/Pitfall 5); stderr/log carries only status code, never the appid/URL.
    - run_weather with a client raising httpx.HTTPStatusError(429) every call (sleep seam patched) → returns 3 after at most _MANUAL_MAX_ATTEMPTS (=3) attempts.
    - printed stdout text == the exact v1 template render (CMD-05) — it is `result.text`, unmodified.
  </behavior>
  <action>
    Add `import sys` to the cli.py imports (it is NOT imported today; needed for `print(..., file=sys.stderr)` per D-06). Add `UnknownLocationError` to the existing `from weatherbot.interactive import ...` line (currently imports only `lookup_weather`; `UnknownLocationError` IS exported from `weatherbot.interactive.__init__`).

    Add a new `run_weather(location_name, *, config, settings=None, client=None, templates_dir=None, verbose=False) -> int` function modeled on `run_send_now` (lines 181-249) but adapted for the read-only path per D-08: build a `Retrying` reusing `_MANUAL_MAX_ATTEMPTS` (=3) for `stop_after_attempt`, `wait_exponential(multiplier=1, max=10)`, `retry=retry_if_exception(is_transient)` as the ONLY retry arm (DROP `run_send_now`'s `retry_if_result(lambda r: not r.ok)` arm and its `retry_error_callback` — there is no DeliveryResult on a read-only lookup), `reraise=True`, and `sleep=time.sleep` (the patchable test seam). Call `retrying(lookup_weather, location_name, config=config, settings=settings, client=client, templates_dir=templates_dir)`.

    Order the except arms exactly: FIRST `except UnknownLocationError as exc:` → `print(str(exc), file=sys.stderr)` then `return 1` (CMD-04; the message is reused verbatim per D-06; this arm MUST precede any broad ValueError because UnknownLocationError IS-A ValueError and is_transient returns False for it so it is reraised on attempt 1 and never retried). THEN `except httpx.HTTPStatusError as exc:` → `_log.error("weather lookup failed", status=exc.response.status_code)` then `return 3` (outcome-only logging, T-04-01 — never the appid/URL/`exc.request.url`). THEN `except (httpx.TimeoutException, httpx.ConnectError, httpx.ReadError) as exc:` → `_log.error("weather lookup failed", error=type(exc).__name__)` then `return 3`. On success, `print(result.text)` to stdout (D-06/CMD-05) and `return 0`.

    Add a `_cmd_weather(args) -> int` dispatcher: call `_load_config_reporting(args.config)`; if it returns None, `return 2` (config invalid/missing per D-05 — note this is 2, NOT the 1 the migrated flags use); else `settings = load_settings()` and `return run_weather(args.location, config=config, settings=settings, verbose=args.verbose)`.

    Do NOT call `parse_weather_command` here — argparse already supplies the positional location; the parser stays reserved for Phase 11 (RESEARCH Open Question 2). Do NOT put the retry inside `lookup_weather` — the retry locus is the attended handler (RESEARCH Anti-Patterns). Do NOT re-fetch or re-render — print `result.text` only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "weather" -x -q 2>&1 | tail -5; echo "NOTE: weather tests are authored in Plan 03; this task verifies run_weather imports cleanly and the suite still collects:"; uv run python -c "from weatherbot.cli import run_weather, _cmd_weather; import inspect; print('ok', 'sys' in inspect.getsource(__import__('weatherbot.cli', fromlist=['x'])))"</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/cli.py` contains `import sys` and imports `UnknownLocationError` from `weatherbot.interactive`.
    - `run_weather` exists with signature accepting `location_name` positional and keyword `config`, `settings`, `client`, `templates_dir`, `verbose`.
    - The `Retrying` in `run_weather` uses `retry=retry_if_exception(is_transient)` ONLY — `grep -n "retry_if_result" weatherbot/cli.py` shows it ONLY inside `run_send_now`, never inside `run_weather`.
    - The `except UnknownLocationError` arm appears textually BEFORE any `except ValueError`/`except httpx.HTTPStatusError` arm inside `run_weather`.
    - `run_weather`'s error logging calls use `status=` and `error=type(exc).__name__` only — `grep` confirms no `appid`, `webhook`, or `exc.request.url` token appears in `run_weather`.
    - `_cmd_weather` returns 2 (not 1) when `_load_config_reporting` returns None.
    - `uv run python -c "from weatherbot.cli import run_weather, _cmd_weather"` exits 0.
  </acceptance_criteria>
  <done>`run_weather` + `_cmd_weather` exist in cli.py, import cleanly, implement the 0/1/2/3 exit mapping with UnknownLocationError caught first and outcome-only error logging; retry uses only the is_transient arm.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure main() to subparsers + quiet logging, dispatch all subcommands</name>
  <files>weatherbot/cli.py</files>
  <read_first>
    - weatherbot/cli.py lines 388-491 (the `main()` being refactored: line 390 unconditional `basicConfig(INFO)`; lines 396-436 flag definitions; lines 437-490 hasattr dispatch + the four migrated branches)
    - weatherbot/cli.py lines 440-442 (geocode branch — needs only settings, NO --config), 444-450 (check branch), 452-466 (run branch — KEEP the lazy `from weatherbot.scheduler import daemon` import to avoid the cli↔daemon cycle), 468-490 (send-now branch)
    - weatherbot/interactive/lookup.py line 146 (`_log.info("lookup complete", ...)` — the INFO line quiet mode suppresses)
    - (grep already confirmed in planning) NO `structlog.configure`/`make_filtering_bound_logger` exists anywhere in weatherbot/ — structlog uses its default config which defers to the stdlib root level, so `basicConfig(level=WARNING)` suppresses the INFO line (RESEARCH Assumption A1 / Open Question 1 RESOLVED)
    - .planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md § restructured main() (lines 114-174)
    - .planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md Pattern 1, Pattern 3, Pitfall 3 (argparse exit-2 overlap), Pitfall 4 (quiet-mode timing)
    - .planning/phases/07-cli-weather-location-one-shot/07-CONTEXT.md D-01, D-02, D-07, D-09
  </read_first>
  <action>
    Replace `main()`'s flat-flag parser and `hasattr` dispatch with `add_subparsers(dest="command")` per D-01/D-02 (clean break — REMOVE `--run`/`--check`/`--send-now`/`--geocode`; no aliases). Build a parent parser via `argparse.ArgumentParser(add_help=False)` carrying the shared `--config` option (default `"config.toml"`, same help text as today's line 433-435), and attach it with `parents=[parent]` to the subcommands that need it. Create subparsers: `weather` (parents=[parent]; positional `location` with `nargs="?"`, `default=None` for CMD-03; flag `-v`/`--verbose` `action="store_true"` for D-09), `run` (parents=[parent]), `check` (parents=[parent]), `send-now` (parents=[parent]; positional `location` `nargs="?"` `default=None` mirroring the old `--send-now`), and `geocode` (NO parent — old branch loads only settings, not config; positional `query`). Use `dest="command"` so dispatch is on `args.command`.

    Implement D-09 quiet logging by MOVING the `logging.basicConfig` call to AFTER `parse_args` (today it is the unconditional `basicConfig(level=logging.INFO)` at line 390 — leaving it there defeats D-09 because basicConfig is a no-op on its second call). After parsing: set `level = logging.INFO`; if `args.command == "weather" and not getattr(args, "verbose", False)`, set `level = logging.WARNING`; then call `logging.basicConfig(level=level)` exactly once. This suppresses `lookup.py:146`'s INFO line for bare `weather` while leaving `-v` and all other subcommands at INFO (RESEARCH Pattern 3).

    Dispatch on `args.command`, preserving each migrated handler's body and EXISTING return codes (RESEARCH migration note — only `weather` uses the richer 0/1/2/3 scheme; `check`/`run`/`send-now` keep returning 1 on `config is None`): `if args.command is None:` → `parser.print_help(); return 0` (preserves bare-`weatherbot` behavior). `weather` → `return _cmd_weather(args)`. `geocode` → copy lines 440-442 (`load_settings()` then `do_geocode(args.query, settings=...)`). `check` → copy lines 444-450 (`_load_config_reporting` → None?→1 → `do_check(...)`). `run` → copy lines 452-466 KEEPING the lazy `from weatherbot.scheduler import daemon` import inside the branch (top-level import creates a cli↔daemon cycle). `send-now` → copy lines 468-490 (`run_send_now(args.location, config=..., db_path=..., settings=...)`).

    Document the D-07 exit-2 overlap in a code comment: argparse raises `SystemExit(2)` for bad usage INSIDE parse_args, while a bad config returns `2` from `_cmd_weather` — both mean "bad input," the overlap is intentionally kept (D-07), and tests distinguish them via `pytest.raises(SystemExit)` vs `main([...]) == 2`. Update `main()`'s docstring to describe the subcommand surface rather than `--send-now`.
  </action>
  <verify>
    <automated>uv run python -c "from weatherbot.cli import main; assert main(['weather','x']) in (0,1,2,3); import sys; print('dispatch ok')" ; uv run weatherbot --help 2>&1 | grep -E "weather|run|check|send-now|geocode"</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/cli.py main()` uses `add_subparsers(dest="command")` — `grep -n "add_subparsers" weatherbot/cli.py` matches.
    - The five subcommands `weather`, `run`, `check`, `send-now`, `geocode` are registered; the OLD flags are gone: `grep -E '"--run"|"--check"|"--send-now"|"--geocode"' weatherbot/cli.py` returns nothing.
    - `--config` is defined ONCE on a shared `add_help=False` parent parser and attached via `parents=[...]`.
    - `logging.basicConfig` is called AFTER `parse_args` (not before), exactly once; the level is `WARNING` for `weather` without `-v`, else `INFO`.
    - Dispatch branches for `geocode`/`check`/`run`/`send-now` return their ORIGINAL exit codes (0/1); only `weather` returns the 0/1/2/3 scheme.
    - The `run` branch retains the lazy `from weatherbot.scheduler import daemon` import inside the branch.
    - `uv run weatherbot --help` exits 0 and lists all five subcommands.
  </acceptance_criteria>
  <note>
    The live end-to-end check `uv run weatherbot weather home` (with a configured `home` + valid `.env` + network) is NOT an acceptance criterion for this task — it needs secrets/network that the offline exec/CI environment lacks. Its authoritative coverage is the Plan 03 offline matrix (07-03-02), which pins exit-0-with-briefing, byte-equality vs the v1 template, and the quiet-by-default-vs-`-v` D-09 behavior using the injected `_FakeClient`. The REAL acceptance for this task is the offline `from weatherbot.cli import main` dispatch smoke + `weatherbot --help` listing the five subcommands (above). If a live host is available, running `uv run weatherbot weather home` (quiet) and `... -v` (shows the `lookup complete` INFO line) is an optional manual confidence check — record the result in the SUMMARY if performed.
  </note>
  <done>`main()` dispatches via subparsers with a shared `--config` parent; old flags removed; quiet logging applied to `weather` only; all four migrated handlers preserved with original exit codes; `weatherbot --help` lists the five subcommands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal user → CLI | untrusted `location` argument crosses into resolve_location + lookup_weather + renderer |
| process → OpenWeather API | outbound fetch carrying the `appid`; failures must not echo the key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering / Injection | `weather` location arg → `resolve_location`/`lookup_weather`/renderer | mitigate | Preserve Phase 6's parse-don't-validate + casefold-equality match against configured names only; location is passed as DATA to `resolve_location` — NO `str.format`/`eval`/shell interpolation (T-06-01 precedent). No geocode path reachable from `weather` (CMD-04, configured-only). |
| T-07-02 | Information Disclosure | `run_weather` except arms / logging | mitigate | Outcome-only logging: `_log.error("weather lookup failed", status=...)` and `error=type(exc).__name__` ONLY — never the `appid`, webhook URL, or `exc.request.url` (T-04-01 precedent, mirrors `run_send_now`/`do_geocode`). `UnknownLocationError` message carries only names, never secrets. |
| T-07-03 | DoS (self-inflicted) | read-only retry budget | mitigate | `wait_exponential(max=10)` + `stop_after_attempt(3)` bounds the retry regardless of any `Retry-After` header; 401/403 in PERMANENT are reraised on attempt 1 (never retried). |
| T-07-04 | Elevation | subcommand surface | accept | Clean break removes flags but adds no new privileged capability; `weather` is strictly read-only (no send/persist/daemon). No privilege change vs v1. |
</threat_model>

<verification>
- `uv run weatherbot --help` exits 0 and lists `weather`, `run`, `check`, `send-now`, `geocode`.
- `grep` confirms removed flags are gone and `retry_if_result` is absent from `run_weather`.
- Full suite is exercised by Plan 03 (which rewrites the removed-flag callsites); within this plan, `uv run pytest tests/test_cli.py` will show the FIVE removed-flag callsites failing — that is EXPECTED and is fixed in Plan 03. Do not "fix" tests here.
</verification>

<success_criteria>
The standalone `weather [location]` command works end-to-end (CMD-01/03/04/05) with the 0/1/2/3 exit contract (D-05), quiet-by-default output (D-09), and read-only bounded retry (D-08); the four existing flags are migrated to subcommands with unchanged behavior (D-01/D-02).
</success_criteria>

<artifacts_this_phase_produces>
- `weatherbot/cli.py` → `run_weather(location_name, *, config, settings, client, templates_dir, verbose) -> int` (NEW handler function)
- `weatherbot/cli.py` → `_cmd_weather(args) -> int` (NEW dispatcher function)
- `weatherbot/cli.py` → restructured `main()` using `add_subparsers(dest="command")`
- New subcommand names: `weather`, `run`, `check`, `send-now`, `geocode`
- New CLI flag on the `weather` subcommand: `-v` / `--verbose`
- New positional: `weather`'s optional `location` (nargs="?")
- New `import sys` and added `UnknownLocationError` import in cli.py
- Exit-code contract for `weather`: 0 ok / 1 unknown-location / 2 bad-config / 3 fetch-failure (no new named constants; literal ints, mirroring existing handlers)
- REMOVED: `--run`, `--check`, `--send-now`, `--geocode` flags (no aliases)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-cli-weather-location-one-shot/07-02-SUMMARY.md` when done.
</output>
