---
phase: 31-send-atomicity-exactly-once-persistence-robustness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/scheduler/daemon.py
  - tests/test_scheduler.py
autonomous: true
requirements: [HARD-DELIV-01, HARD-DELIV-02]

must_haves:
  truths:
    - "A DB error in post-send bookkeeping (resolve_alert/stamp_success) AFTER a briefing is delivered never releases the won claim: the slot stays sent, catch-up/restart does not re-deliver, and no false internal_error alert fires (F01 / HARD-DELIV-01, D-01)."
    - "The F01 duplicate-send scenario is reproduced FIRST: an injection test that forces an OperationalError into post-send bookkeeping FAILS against pre-fix daemon.py, then PASSES after the swallow-wrap lands (D-01a, roadmap verify-first mandate)."
    - "fire_forecast_slot inspects the DeliveryResult returned by channel.send(): a Discord ok=False routes to _note_forecast_failure (WR-05 dead-slot streak/CRITICAL/operator alert) instead of _note_forecast_success — only a clean delivery resets the streak (F08 / HARD-DELIV-02, D-02)."
    - "a post-delivery bookkeeping failure must NEVER surface a duplicate briefing to the user (values: user-facing correctness — the whole point of the exactly-once claim)."
    - "one bad slot never kills the APScheduler worker thread: both fire_slot and fire_forecast_slot keep their outer except-return-None isolation envelope; the new branches return None, they never re-raise (edge: concurrency/isolation — T-03-07)."
    - { statement: "a forecast slot that fails via ok=False on the FIRST fire (single-element streak) increments the streak toward the dead-slot threshold rather than resetting it", verification: backstop }
  prohibitions:
    - "no code path after result.ok is confirmed in fire_slot may reach release_claim — the release + internal_error path stays reachable ONLY for pre-delivery failures (D-01)."
    - "the F08 ok=False branch must NOT duplicate the WR-05 escalation logic — it reuses the existing _note_forecast_failure helper verbatim (Don't-Hand-Roll)."
---

<objective>
Close the two send-detection seams in daemon.py, F01 verify-first. F01: in fire_slot, post-send bookkeeping (resolve_alert/stamp_success at daemon.py:339-340) runs inside the outer try opened at :182; a realistic post-delivery `database is locked` OperationalError falls to the broad `except` at :349 which — because claimed=True — calls release_claim at :356 (DELETEing the sent_log row) and records a false internal_error. The briefing was already delivered, so the slot re-fires on catch-up/restart → duplicate. F08: fire_forecast_slot at :538-543 discards the DeliveryResult from channel.send() and unconditionally calls _note_forecast_success, so a Discord ok=False resets the streak and the WR-05 dead-slot escalation never fires.

Purpose: The exactly-once claim must be the source of truth for "delivered" once result.ok — bookkeeping errors can never re-open a delivered slot. And a chronically-dead forecast slot must be operator-visible.
Output: A reproduce-first F01 regression test, the log-and-swallow bookkeeping wrap (mirroring daemon.py:1032), the F08 ok=False inspection branch, and forecast-failure regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-send-atomicity-exactly-once-persistence-robustness/31-RESEARCH.md
@.planning/phases/31-send-atomicity-exactly-once-persistence-robustness/31-PATTERNS.md
@.planning/WHOLE-PROJECT-REVIEW.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS plan:
- `tests/test_scheduler.py` → `test_post_send_db_error_keeps_claim` (F01 reproduce-first), `test_forecast_delivery_failure_escalates` (F08) — new test functions.
- No new production symbols: F01 wraps existing calls in a local try/except (mirroring the existing daemon.py:1032 idiom); F08 captures the already-returned DeliveryResult and reuses the existing `_note_forecast_failure` (daemon.py:399) and `_note_forecast_success` (daemon.py:442) helpers.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 reproduce-first tests — F01 duplicate-send (RED) + F08 forecast ok=False</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py — existing fire_slot / fire_forecast_slot tests, the _StubChannel(DeliveryResult(...)) fixtures, and the was_sent/read_alert helpers already used.
    - weatherbot/scheduler/daemon.py:182-379 (fire_slot try/except + success tail), :464-574 (fire_forecast_slot), :399-444 (_note_forecast_failure / _note_forecast_success).
    - .planning/phases/31-send-atomicity-exactly-once-persistence-robustness/31-RESEARCH.md §"Code Examples → Reproduce F01" (the injection test shape) and §"Validation Architecture" (HARD-DELIV-01/02 rows).
  </read_first>
  <behavior>
    - test_post_send_db_error_keeps_claim (F01, D-01a): init the store, fire a slot through a stub channel that returns DeliveryResult(ok=True); monkeypatch daemon.stamp_success (or resolve_alert) to raise sqlite3.OperationalError("database is locked"); after the fire, assert was_sent(...) is True (the claim stays committed — no re-fire) AND the recorded alert reason for that slot/day is NOT internal_error. This test FAILS against current daemon.py (release_claim runs, was_sent flips False, a false internal_error is recorded).
    - test_forecast_delivery_failure_escalates (F08): fire a forecast slot through a stub channel whose send() returns DeliveryResult(ok=False); assert the slot's failure streak advanced (drive _FORECAST_DEAD_AFTER consecutive ok=False fires and assert the forecast_slot_dead CRITICAL path is reached / streak not reset) — i.e. an ok=False is treated as a failure, not a success.
  </behavior>
  <action>
    Author both test functions in tests/test_scheduler.py. Reuse the existing scheduler test fixtures (a Location, a slot, an init_db'd tmp db, and the _StubChannel returning a configurable DeliveryResult). For F01, use monkeypatch.setattr on the daemon module's `stamp_success` symbol to a function that raises sqlite3.OperationalError; read the resulting alert reason via the existing store read helper the module's other alert tests use. For F08, construct a stub channel whose `send` returns DeliveryResult(ok=False) and fire the forecast slot _FORECAST_DEAD_AFTER times, asserting the streak crosses (the CRITICAL forecast_slot_dead is emitted / _note_forecast_success was never called). Confirm test_post_send_db_error_keeps_claim FAILS against current code and RECORD that RED evidence in the SUMMARY (D-01a mandate). Do NOT soften the assertions to pass early.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "post_send_db_error or forecast_delivery_failure" -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_scheduler.py` contains `def test_post_send_db_error_keeps_claim(` and `def test_forecast_delivery_failure_escalates(`.
    - Against pre-fix daemon.py, `test_post_send_db_error_keeps_claim` FAILS (asserts a duplicate-reopened slot / false internal_error) — RED evidence recorded in SUMMARY.
    - The F01 test injects `sqlite3.OperationalError` into `stamp_success` (or `resolve_alert`) via monkeypatch.
    - No watch-mode flags; both tests run in under 60s.
  </acceptance_criteria>
  <done>Both regression tests exist; the F01 test is demonstrably RED against current daemon.py (verify-first satisfied).</done>
</task>

<task type="auto">
  <name>Task 2: F01 fix — wrap post-send bookkeeping in a log-and-swallow so it can never release the claim</name>
  <files>weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py:335-348 — the success tail (resolve_alert :339 + stamp_success :340 running bare inside the outer try) and the return result.
    - weatherbot/scheduler/daemon.py:349-379 — the broad `except Exception` that calls release_claim :356 + records internal_error (the path that must become unreachable after result.ok).
    - weatherbot/scheduler/daemon.py:1032-1036 — the swallow-on-committed idiom to mirror verbatim (best-effort try/except + _log.warning, noqa BLE001).
    - .planning/phases/31-send-atomicity-exactly-once-persistence-robustness/31-PATTERNS.md §"F01" (fix shape + invariant) and 31-RESEARCH.md §"Pattern 1".
  </read_first>
  <action>
    In fire_slot's success tail (daemon.py:339-340), wrap the two bookkeeping calls resolve_alert(...) and stamp_success(...) in a single local `try/except Exception` that logs an outcome-only `_log.warning` ("post-send bookkeeping failed; briefing already delivered, claim kept" — location.name + slot.time, no secret) and does NOT release the claim, mirroring the daemon.py:1032-1036 idiom (same `# noqa: BLE001 — best-effort; already committed` shape). Keep the existing `return result` after it. Do NOT alter the pre-delivery release arms (:266, :289, :315) or the outer isolation envelope (:349-379) — those stay reachable ONLY for pre-delivery failures. The invariant to hold: once result.ok is confirmed, no code path reaches release_claim. Reference: D-01, D-01a. NOTE: the warning message wording must be plain prose passed as directive text; do not embed the literal token `internal_error` in the new warning string.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "post_send_db_error" -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_post_send_db_error_keeps_claim` passes (was_sent stays True; alert reason is not internal_error after a post-delivery bookkeeping error).
    - The success tail wraps resolve_alert + stamp_success in a local try/except that logs a warning and does not call release_claim.
    - `uv run pytest tests/test_scheduler.py -q` shows no regression in the existing pre-delivery-failure fire_slot tests (release still fires on a genuine ok=False / raised transient).
  </acceptance_criteria>
  <done>Post-send bookkeeping is best-effort log-and-swallow; a post-delivery DB error keeps the claim; the F01 reproduce test passes; pre-delivery release behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: F08 fix — inspect the forecast DeliveryResult; route ok=False to dead-slot escalation</name>
  <files>weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py:538-551 — the current unconditional `channel.send(reply.text)` (result discarded) + `_note_forecast_success` + info log.
    - weatherbot/scheduler/daemon.py:399-444 — `_note_forecast_failure` (WR-05 escalation, already self-guards its channel post) and `_note_forecast_success`.
    - weatherbot/scheduler/daemon.py:314 — the sibling fire_slot `if not result.ok:` inspection this path must mirror.
    - weatherbot/scheduler/daemon.py:552-574 — the outer except-return-None isolation envelope to keep intact.
    - .planning/phases/31-send-atomicity-exactly-once-persistence-robustness/31-PATTERNS.md §"F08" (fix shape) and 31-RESEARCH.md §"Pattern 2".
  </read_first>
  <action>
    In fire_forecast_slot, change the `if channel is not None:` block so it captures the return value: `fc_result = channel.send(reply.text)`. Immediately after, if `fc_result is not None and not fc_result.ok`, call `_note_forecast_failure(location, fc, channel=channel)` (reuse the existing helper verbatim — do NOT duplicate the escalation), emit an outcome-only `_log.warning` ("forecast slot delivery failed" — location.name/kind/variant/time, no secret), and `return None` (never raise — Pitfall 4 isolation). Only when delivery is clean (ok, or channel is None) does control reach the existing `_note_forecast_success(location, fc)` — so ONLY a clean delivery resets the streak (D-02). Leave the outer `except Exception: ... return None` envelope (:552-574) and its own `_note_forecast_failure` call untouched (a raised failure still escalates via that path). Reference: D-02.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "forecast_delivery_failure" -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_forecast_delivery_failure_escalates` passes (an ok=False forecast delivery advances the streak and the dead-slot CRITICAL fires; _note_forecast_success is not called on ok=False).
    - fire_forecast_slot captures the DeliveryResult from channel.send() and branches on `.ok` before _note_forecast_success.
    - The ok=False branch returns None (does not raise); the outer isolation envelope is unchanged.
    - `uv run pytest tests/test_scheduler.py -q` green (existing clean-delivery forecast tests still reset the streak).
  </acceptance_criteria>
  <done>fire_forecast_slot inspects the DeliveryResult; ok=False routes to _note_forecast_failure and returns None; only a clean delivery resets the streak; forecast tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite store → fire_slot bookkeeping | A post-delivery store error crosses back into fire_slot; it must not tamper with the committed delivery state. |
| Discord channel → fire_forecast_slot | The channel's DeliveryResult (ok/detail) is the only signal of forecast delivery success; ignoring it hides a dead slot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Tampering | fire_slot post-send bookkeeping (delivery state integrity) | high | mitigate | Log-and-swallow the bookkeeping tail so a post-delivery DB error cannot release the won claim (D-01); reproduce-first regression proves no duplicate / no false internal_error. |
| T-31-05 | Repudiation | fire_forecast_slot silently swallowing ok=False | medium | mitigate | Inspect the DeliveryResult; route ok=False to the WR-05 dead-slot CRITICAL + throttled operator alert so a chronically-dead slot is discoverable (D-02). |
| T-31-06 | Denial of Service | one bad slot killing the APScheduler thread | high | mitigate | Keep both outer except-return-None isolation envelopes; the new branches return None and never re-raise (T-03-07 / Pitfall 4). |
| T-31-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py -q` green.
- F01: `test_post_send_db_error_keeps_claim` was RED against pre-fix code (recorded), now GREEN.
- F08: `test_forecast_delivery_failure_escalates` GREEN.
- Per-wave merge gate: `uv run pytest -q` full suite green.
</verification>

<success_criteria>
- A post-delivery bookkeeping DB error keeps the claim: no duplicate briefing, no false internal_error (F01 verified first).
- A forecast ok=False is detected and escalates via the dead-slot path; only a clean delivery resets the streak (F08).
- Both fire paths retain their one-bad-slot isolation envelope.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/31-send-atomicity-exactly-once-persistence-robustness/31-02-SUMMARY.md` when done.
</output>
