---
phase: 02-real-config-locations-content-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/onecall_imperial_clear.json
  - tests/fixtures/onecall_metric_clear.json
  - tests/fixtures/onecall_imperial_rainy.json
  - tests/fixtures/onecall_metric_rainy.json
  - tests/fixtures/onecall_imperial_alert.json
  - tests/fixtures/onecall_imperial_multialert.json
  - tests/fixtures/onecall_imperial_highuv.json
  - tests/fixtures/onecall_imperial_extreme.json
  - tests/fixtures/geocode_austin.json
  - tests/fixtures/geocode_ambiguous.json
  - tests/test_cli.py
  - tests/test_aggregate.py
  - tests/test_review_hardening.py
  - weatherbot/weather/aggregate.py
  - weatherbot/weather/models.py
autonomous: true
requirements: [FCST-05, FCST-06, LOC-03, CONF-05]
must_haves:
  truths:
    - "Recorded One Call 3.0 fixtures exist for clear, rainy, alert, multi-alert, high-UV, and extreme-feels-like days"
    - "Geocoding fixtures exist (single match + ambiguous multi-match)"
    - "tests/test_cli.py exists with --check and --geocode test scaffolds"
    - "weatherbot/weather/aggregate.py and tests/test_aggregate.py are deleted and nothing imports them"
    - "weatherbot/weather/models.py no longer imports the deleted aggregate module; its today_aggregate call sites are severed so collection succeeds (the real One Call from_payloads rewrite is Plan 02-02's job)"
    - "tests/test_review_hardening.py is migrated: the 3 obsolete aggregation null-tolerance tests are dropped; the CR-01 renderer-hardening tests and the from_payloads null-tolerance test are kept/rewritten for the One Call path with a timezone-carrying Location"
  artifacts:
    - path: "tests/fixtures/onecall_imperial_clear.json"
      provides: "Clear-day One Call payload: no alerts key, low uvi, current+daily[0]"
      contains: "current"
    - path: "tests/fixtures/onecall_imperial_alert.json"
      provides: "One Call payload with a single active alerts[] entry"
      contains: "alerts"
    - path: "tests/fixtures/geocode_austin.json"
      provides: "/geo/1.0/direct single-match response array"
      contains: "lat"
    - path: "tests/test_cli.py"
      provides: "Test scaffolds for --check and --geocode subcommands"
      contains: "def test_"
    - path: "weatherbot/weather/models.py"
      provides: "Forecast model with the dead aggregate import/call-sites severed (collectable after aggregate.py is deleted)"
      contains: "from_payloads"
  key_links:
    - from: "tests/test_cli.py"
      to: "tests/conftest.py:load_fixture"
      via: "pytest fixture injection"
      pattern: "load_fixture"
---

<objective>
Lay the test foundation for the One Call 3.0 migration: create the recorded One Call 3.0 and Geocoding fixtures, scaffold the new `tests/test_cli.py` file for the `--check`/`--geocode` subcommands, RETIRE the 2.5 bucket-aggregation module and its tests (D-01), SEVER the now-dead `aggregate` import in `models.py` in the same wave that deletes the module so the boundary stays collectable, and MIGRATE the Phase 1 regression suite (tests/test_review_hardening.py) so it survives the One Call migration. This is Wave 0 work — every later slice depends on these fixtures and the deleted-module state.

Purpose: Subsequent slices (the One Call migration, config/template validation, the CLI subcommands) all assert against recorded payloads and must not import the retired `aggregate`. This plan makes the offline test surface exist before any production code reads One Call — and, critically, leaves the W1 boundary importable by removing the `models.py` dependency on the module it deletes (so pytest collection of test_cli.py / test_review_hardening.py cannot ModuleNotFoundError).
Output: 10 new fixtures, a scaffolded `tests/test_cli.py`, the removal of `weatherbot/weather/aggregate.py` + `tests/test_aggregate.py` + the obsolete 2.5 bucket fixtures, a `models.py` whose dead `today_aggregate` import/call-sites are severed, and a migrated `tests/test_review_hardening.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-config-locations-content-templates/02-CONTEXT.md
@.planning/phases/02-real-config-locations-content-templates/02-RESEARCH.md
@.planning/phases/02-real-config-locations-content-templates/02-PATTERNS.md
@.planning/phases/02-real-config-locations-content-templates/02-VALIDATION.md
@weatherbot/weather/models.py
</context>

<artifacts_this_phase_produces>
This plan creates (downstream source-grounding must treat these as NEW, not drift):
- New fixture files: `onecall_imperial_clear.json`, `onecall_metric_clear.json`, `onecall_imperial_rainy.json`, `onecall_metric_rainy.json`, `onecall_imperial_alert.json`, `onecall_imperial_multialert.json`, `onecall_imperial_highuv.json`, `onecall_imperial_extreme.json`, `geocode_austin.json`, `geocode_ambiguous.json`
- New test file: `tests/test_cli.py`
- Deleted files: `weatherbot/weather/aggregate.py`, `tests/test_aggregate.py`, and the obsolete 2.5 bucket fixtures (`forecast_imperial_offset_plus.json`, `forecast_imperial_offset_minus.json`)
- Modified file: `weatherbot/weather/models.py` — the `from weatherbot.weather.aggregate import today_aggregate` import (line 25) is REMOVED and the two `today_aggregate(...)` call sites in `from_payloads` (~lines 89-90) are severed/short-circuited so nothing imports the deleted module after this plan. (The proper One Call `from_payloads` rewrite — real high/low/feels_like/hint/alert from One Call `daily[0]` — is owned by Plan 02-02; here we only remove the dead dependency to keep collection green.)
- Migrated file: `tests/test_review_hardening.py` (CR-02 aggregation null-tolerance tests dropped; CR-01 renderer-hardening + `from_payloads` null-tolerance tests rewritten for the One Call path with a `timezone`-carrying `Location`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author One Call 3.0 + Geocoding test fixtures</name>
  <files>tests/fixtures/onecall_imperial_clear.json, tests/fixtures/onecall_metric_clear.json, tests/fixtures/onecall_imperial_rainy.json, tests/fixtures/onecall_metric_rainy.json, tests/fixtures/onecall_imperial_alert.json, tests/fixtures/onecall_imperial_multialert.json, tests/fixtures/onecall_imperial_highuv.json, tests/fixtures/onecall_imperial_extreme.json, tests/fixtures/geocode_austin.json, tests/fixtures/geocode_ambiguous.json</files>
  <read_first>
    - tests/fixtures/current_imperial_clear.json (Phase 1 analog — copy its real-OpenWeather field shape and the New-York `-14400` tz_offset convention; PATTERNS.md "tests/fixtures/onecall_*.json (NEW)")
    - .planning/phases/02-real-config-locations-content-templates/02-RESEARCH.md "## Validation Architecture" → "Required Test Fixtures" (the exact variant list) and "### Mapping One Call → normalized Forecast" (field paths)
    - .planning/phases/02-real-config-locations-content-templates/02-CONTEXT.md D-06 (hint thresholds the extreme/highuv/rainy fixtures must cross) and D-08 (alerts[] shape)
  </read_first>
  <action>
    Create recorded One Call 3.0 payloads shaped per the One Call 3.0 schema: top-level `lat`, `lon`, `timezone` (e.g. `"America/New_York"`), `timezone_offset` (`-14400`), `current{dt, temp, feels_like, humidity, wind_speed, uvi, weather:[{main, description}]}`, and `daily:[{dt, temp:{max, min}, pop, uvi, weather:[...]}, ...]` (at least the `daily[0]` entry). Variants and the thresholds they must cross (D-06, imperial values): `onecall_imperial_clear`/`onecall_metric_clear` = no `alerts` key at all (clear day), `daily[0].pop` ≤ 0.40, `daily[0].uvi` < 6, `feels_like` between 40 and 90, `wind_speed` ≤ 25 (zero hints). `onecall_imperial_rainy`/`onecall_metric_rainy` = `daily[0].pop` > 0.40 and `weather[0].main` = "Rain" (umbrella hint). `onecall_imperial_alert` = includes `alerts:[{sender_name, event, start, end, description, tags}]` with ONE event. `onecall_imperial_multialert` = `alerts[]` with TWO distinct `event` names (for the concise-summary path, D-08). `onecall_imperial_highuv` = `daily[0].uvi` ≥ 6 (sunscreen hint). `onecall_imperial_extreme` = `current.feels_like` < 40 (cold) OR > 90 (heat) and `current.wind_speed` > 25 (wind hint) — pick values so cold/heat AND wind both fire. Metric variants mirror the imperial ones with metric values so dual-unit display has no drift. For geocoding: `geocode_austin.json` = `/geo/1.0/direct` array with a single object `{name:"Austin", lat:30.2672, lon:-97.7431, country:"US", state:"Texas"}`; `geocode_ambiguous.json` = an array with 2+ objects sharing a name in different states/countries. Keep values plausible and JSON-valid; these are loaded via the existing `tests/conftest.py:load_fixture` (no conftest change).
  </action>
  <verify>
    <automated>uv run python -c "import json,glob; [json.load(open(f)) for f in glob.glob('tests/fixtures/onecall_*.json')+glob.glob('tests/fixtures/geocode_*.json')]; print('fixtures valid')"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 fixture files parse as valid JSON (the verify command exits 0).
    - `onecall_imperial_clear.json` has NO top-level `alerts` key; `onecall_imperial_alert.json` has an `alerts` array with ≥1 object carrying an `event` string.
    - `onecall_imperial_rainy.json` has `daily[0].pop` > 0.4; `onecall_imperial_highuv.json` has `daily[0].uvi` ≥ 6; `onecall_imperial_extreme.json` has `current.wind_speed` > 25 and `current.feels_like` outside [40, 90].
    - `geocode_austin.json` is a JSON array whose first element has numeric `lat` and `lon`.
  </acceptance_criteria>
  <done>All required One Call 3.0 + geocoding fixtures exist, are valid JSON, and cross the D-06 hint thresholds as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tests/test_cli.py, retire the 2.5 aggregation module, sever the dead models.py aggregate dependency, and migrate test_review_hardening.py</name>
  <files>tests/test_cli.py, weatherbot/weather/aggregate.py, weatherbot/weather/models.py, tests/test_aggregate.py, tests/test_review_hardening.py, tests/fixtures/forecast_imperial_offset_plus.json, tests/fixtures/forecast_imperial_offset_minus.json</files>
  <read_first>
    - tests/test_send_now.py (PATTERNS.md "tests/test_cli.py (NEW)" analog — copy the `_FakeClient`/`_FakeChannel` injectable shape and the `tmp_db`/`load_fixture` fixtures)
    - tests/test_client.py (the `httpx.MockTransport` install via monkeypatch — model for the future `--check` reachability test)
    - .planning/phases/02-real-config-locations-content-templates/02-RESEARCH.md "### Phase Requirements → Test Map" (the `-k` selectors test_cli.py must eventually satisfy) and "Wave 0 Gaps"
    - weatherbot/weather/models.py (the `from weatherbot.weather.aggregate import today_aggregate` at line 25 and the two `today_aggregate(...)` call sites in `from_payloads` at ~lines 89-90 that this task severs; the `_local_date_iso` helper and the four 2.5 raw fields stay UNTOUCHED — Plan 02-02 owns the full One Call rewrite)
    - tests/test_review_hardening.py (the Phase 1 CR-01/CR-02 regression suite this task migrates — note line 19 `from weatherbot.weather.aggregate import today_aggregate`, the 3 `today_aggregate` tests at lines 64-89, the `from_payloads(...)` 4-payload null-tolerance tests at lines 92-107, and the module-level `LOC = Location(name="X", lat=0.0, lon=0.0)` with NO timezone at line 27)
    - tests/test_renderer.py (the CR-01 renderer-hardening assertions test_review_hardening shares — the guarded `render` is signature-independent and survives the migration unchanged)
  </read_first>
  <action>
    Create `tests/test_cli.py` as a scaffold whose test names match the research `-k` selectors so later slices can fill them in: `test_geocode_prints_coords`, `test_send_now_never_geocodes`, `test_check_validates_config`, `test_check_reachability_one_call`, `test_send_now_bad_template_aborts`, `test_check_unique_names`. Each scaffolded test should be a real pytest function that either exercises an already-shippable behavior or is marked `@pytest.mark.xfail(reason="implemented in 02-03/02-04", strict=False)` so the suite stays green now and flips to passing when the feature lands (mirror Phase 1's strict-xfail-then-remove discipline from `test_send_now.py`'s module docstring). Import a `_FakeClient`/`_FakeChannel` modeled on `test_send_now.py`. Then RETIRE the 2.5 aggregation surface (D-01): delete `weatherbot/weather/aggregate.py`, delete `tests/test_aggregate.py`, and delete the now-obsolete 2.5 bucket-offset fixtures `tests/fixtures/forecast_imperial_offset_plus.json` and `tests/fixtures/forecast_imperial_offset_minus.json`. Do NOT yet delete the other 2.5 fixtures (`current_*`, `forecast_imperial_clear.json`, `forecast_imperial_rainy.json`, `forecast_metric_clear.json`) — `models.py`/`test_models.py` still construct Forecast from them until Plan 02-02 rewrites `from_payloads`; Plan 02-02 owns their removal.
    SEVER the dead `aggregate` dependency in `models.py` (Option 1 — required so the W1 boundary stays collectable once `aggregate.py` is gone): DELETE the `from weatherbot.weather.aggregate import today_aggregate` import (line 25), and remove the two `today_aggregate(...)` call sites in `from_payloads` (`agg_imp = today_aggregate(...)` / `agg_met = today_aggregate(...)`, ~lines 89-90). Because `high_imp`/`high_met`/`low_imp`/`low_met`/`rain_chance` were sourced from those aggregates, short-circuit them rather than reintroduce the dependency: assign `high_imp=high_met=low_imp=low_met=None` and `rain_chance=0` (or drop the local `agg_*` reads and inline those literals at the `cls(...)` call). This deliberately leaves `from_payloads` producing placeholder high/low/rain — the REAL One Call values (from `daily[0].temp.max/min` and `daily[0].pop`) plus the feels_like/hint/alert rewrite are supplied by Plan 02-02 Task 1. Do NOT touch `_local_date_iso`, the four `raw_*` fields, the dataclass fields, or the display properties — leave the full rewrite to Plan 02-02. The single goal here is: after this plan, `grep -rn "weather.aggregate\|import aggregate" weatherbot/ tests/` returns NOTHING, so importing `models.py` (transitively, `cli.py` and `test_review_hardening.py`) no longer ModuleNotFoundErrors at pytest collection.
    MIGRATE `tests/test_review_hardening.py` alongside `test_aggregate.py` (it currently imports the deleted `aggregate` module — leaving it would break collection): (a) DELETE its `from weatherbot.weather.aggregate import today_aggregate` import (line 19) and the THREE obsolete CR-02 aggregation null-tolerance tests `test_aggregate_tolerates_null_pop`, `test_aggregate_tolerates_null_main`, `test_aggregate_tolerates_null_city` (lines 64-89) — bucket aggregation is gone (D-01), so these tests no longer have a subject. (b) KEEP the CR-01 renderer-hardening tests (`test_attribute_access_is_not_evaluated`, `test_index_access_is_not_evaluated`, `test_positional_field_does_not_crash`, `test_unbalanced_brace_does_not_crash`, `test_known_placeholder_still_substitutes`) UNCHANGED — they exercise the guarded `render` and are signature-independent, so they stay green through every wave. (c) For the two `from_payloads(...)` 4-payload null-tolerance tests (`test_forecast_from_payloads_tolerates_null_current_fields`, `test_persist_skips_forecast_bucket_without_dt`, lines 92-107): mark BOTH `@pytest.mark.xfail(reason="rewritten for the One Call from_payloads signature + timezone Location in 02-02/02-03", strict=False)` in THIS plan — Plan 02-02 rewrites them for the One Call `from_payloads(loc, onecall_imp, onecall_met)` null-tolerance path and Plan 02-03 supplies the required `timezone` on the module-level `LOC`. Do NOT yet change the module-level `LOC = Location(...)` (Plan 02-03 makes `timezone` required and updates it); the kept CR-01 tests do not construct `from_payloads`, so the no-timezone `LOC` is harmless until Plan 02-03.
  </action>
  <verify>
    <automated>test ! -f weatherbot/weather/aggregate.py && test ! -f tests/test_aggregate.py && test "$(grep -rl 'weather.aggregate' weatherbot/ tests/ | wc -l)" = "0" && uv run pytest tests/test_cli.py tests/test_review_hardening.py -q -k "not (tolerates or skips)"</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/weather/aggregate.py` and `tests/test_aggregate.py` no longer exist (the verify `test ! -f` checks pass).
    - `tests/fixtures/forecast_imperial_offset_plus.json` and `forecast_imperial_offset_minus.json` are deleted.
    - `weatherbot/weather/models.py` no longer imports `aggregate` and has no `today_aggregate` call (`grep -c "aggregate" weatherbot/weather/models.py` returns 0); the dataclass still constructs (`uv run python -c "import weatherbot.weather.models"` exits 0).
    - `grep -rn "import aggregate\|weather.aggregate" weatherbot/ tests/` returns NOTHING — every reference to the deleted module is gone across both production and test code.
    - `tests/test_review_hardening.py` no longer imports `aggregate` (`grep -c "aggregate" tests/test_review_hardening.py` returns 0); the 3 CR-02 `today_aggregate` tests are gone; the 5 CR-01 renderer-hardening tests remain and pass; the 2 `from_payloads` null-tolerance tests are marked `xfail(strict=False)`.
    - `uv run pytest tests/test_cli.py tests/test_review_hardening.py -q` COLLECTS with no ImportError/ModuleNotFoundError and reports only pass/xfail (no failures, no errors). The scoped `-k "not (tolerates or skips)"` gate also exits 0.
  </acceptance_criteria>
  <done>tests/test_cli.py exists with named scaffolds for every research `-k` selector; the 2.5 aggregation module + its test + bucket-offset fixtures are retired; `models.py` no longer imports/calls the deleted `aggregate` (so the W1 boundary collects cleanly); `test_review_hardening.py` is migrated (3 CR-02 aggregation tests dropped, 5 CR-01 renderer tests kept, 2 from_payloads tests xfail-marked) and collects without ImportError. (Real One Call high/low/feels_like/hint/alert values are supplied by Plan 02-02; the full suite is not green until Plan 02-02 completes the One Call rewrite.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded fixture → test code | Fixtures are local, trusted test data; no live network in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | test fixtures | mitigate | Fixtures contain only OpenWeather *response* fields — never an `appid`, request URL, or webhook URL. Author them with no secrets. |
| T-02-SC | Tampering | package installs | accept | No new packages installed this phase (RESEARCH "Package Legitimacy Audit": none). No install task, so no slopcheck gate needed. |
</threat_model>

<verification>
- `uv run pytest tests/test_cli.py tests/test_review_hardening.py -q` collects and reports only pass/xfail (NO ImportError/ModuleNotFoundError, no failures). This boundary is now collectable because `models.py` no longer imports the deleted `aggregate`.
- No production OR test code imports `weatherbot.weather.aggregate` — `grep -rn "weather.aggregate" weatherbot/ tests/` is empty (this plan severs the last `models.py` import in the same wave that deletes the module).
- The FULL `uv run pytest -q` is NOT yet green at this wave (and must not be gated on it): `from_payloads` now emits placeholder high/low/rain (None/0) until Plan 02-02 supplies the real One Call `daily[0]` values, so model/store/renderer assertions that expect real aggregates still fail. The full-suite-green gate is owned by Plan 02-02 (and the renderer repoint by Plan 02-03).
- All One Call 3.0 + geocoding fixtures parse and cross their intended thresholds.
</verification>

<success_criteria>
- 10 One Call 3.0 / geocoding fixtures exist and are valid JSON crossing the D-06 thresholds.
- `tests/test_cli.py` exists with scaffolds named for every research `-k` selector.
- `aggregate.py`, `test_aggregate.py`, and the 2.5 bucket-offset fixtures are deleted.
- `weatherbot/weather/models.py` no longer imports or calls `today_aggregate`; nothing in `weatherbot/` or `tests/` references `weather.aggregate`.
- `tests/test_review_hardening.py` is migrated (3 CR-02 aggregation tests dropped, 5 CR-01 renderer tests kept, 2 from_payloads tests xfail-marked) and collects without ImportError.
- The test_cli + test_review_hardening subset is green (pass/xfail). Full-suite green is deferred to Plan 02-02, which rewrites `from_payloads` to supply real One Call values.
</success_criteria>

<output>
Create `.planning/phases/02-real-config-locations-content-templates/02-01-SUMMARY.md` when done
</output>
</content>
</invoke>
