---
phase: 02-real-config-locations-content-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/onecall_imperial_clear.json
  - tests/fixtures/onecall_metric_clear.json
  - tests/fixtures/onecall_imperial_rainy.json
  - tests/fixtures/onecall_metric_rainy.json
  - tests/fixtures/onecall_imperial_alert.json
  - tests/fixtures/onecall_imperial_multialert.json
  - tests/fixtures/onecall_imperial_highuv.json
  - tests/fixtures/onecall_imperial_extreme.json
  - tests/fixtures/geocode_austin.json
  - tests/fixtures/geocode_ambiguous.json
  - tests/test_cli.py
  - tests/test_aggregate.py
  - weatherbot/weather/aggregate.py
autonomous: true
requirements: [FCST-05, FCST-06, LOC-03, CONF-05]
must_haves:
  truths:
    - "Recorded One Call 3.0 fixtures exist for clear, rainy, alert, multi-alert, high-UV, and extreme-feels-like days"
    - "Geocoding fixtures exist (single match + ambiguous multi-match)"
    - "tests/test_cli.py exists with --check and --geocode test scaffolds"
    - "weatherbot/weather/aggregate.py and tests/test_aggregate.py are deleted and nothing imports them"
  artifacts:
    - path: "tests/fixtures/onecall_imperial_clear.json"
      provides: "Clear-day One Call payload: no alerts key, low uvi, current+daily[0]"
      contains: "current"
    - path: "tests/fixtures/onecall_imperial_alert.json"
      provides: "One Call payload with a single active alerts[] entry"
      contains: "alerts"
    - path: "tests/fixtures/geocode_austin.json"
      provides: "/geo/1.0/direct single-match response array"
      contains: "lat"
    - path: "tests/test_cli.py"
      provides: "Test scaffolds for --check and --geocode subcommands"
      contains: "def test_"
  key_links:
    - from: "tests/test_cli.py"
      to: "tests/conftest.py:load_fixture"
      via: "pytest fixture injection"
      pattern: "load_fixture"
---

<objective>
Lay the test foundation for the One Call 3.0 migration: create the recorded One Call 3.0 and Geocoding fixtures, scaffold the new `tests/test_cli.py` file for the `--check`/`--geocode` subcommands, and RETIRE the 2.5 bucket-aggregation module and its tests (D-01). This is Wave 0 work — every later slice depends on these fixtures and the deleted-module state.

Purpose: Subsequent slices (the One Call migration, config/template validation, the CLI subcommands) all assert against recorded payloads and must not import the retired `aggregate`. This plan makes the offline test surface exist before any production code reads One Call.
Output: 10 new fixtures, a scaffolded `tests/test_cli.py`, and the removal of `weatherbot/weather/aggregate.py` + `tests/test_aggregate.py` + the obsolete 2.5 bucket fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-config-locations-content-templates/02-CONTEXT.md
@.planning/phases/02-real-config-locations-content-templates/02-RESEARCH.md
@.planning/phases/02-real-config-locations-content-templates/02-PATTERNS.md
@.planning/phases/02-real-config-locations-content-templates/02-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates (downstream source-grounding must treat these as NEW, not drift):
- New fixture files: `onecall_imperial_clear.json`, `onecall_metric_clear.json`, `onecall_imperial_rainy.json`, `onecall_metric_rainy.json`, `onecall_imperial_alert.json`, `onecall_imperial_multialert.json`, `onecall_imperial_highuv.json`, `onecall_imperial_extreme.json`, `geocode_austin.json`, `geocode_ambiguous.json`
- New test file: `tests/test_cli.py`
- Deleted files: `weatherbot/weather/aggregate.py`, `tests/test_aggregate.py`, and the obsolete 2.5 bucket fixtures (`forecast_imperial_offset_plus.json`, `forecast_imperial_offset_minus.json`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author One Call 3.0 + Geocoding test fixtures</name>
  <files>tests/fixtures/onecall_imperial_clear.json, tests/fixtures/onecall_metric_clear.json, tests/fixtures/onecall_imperial_rainy.json, tests/fixtures/onecall_metric_rainy.json, tests/fixtures/onecall_imperial_alert.json, tests/fixtures/onecall_imperial_multialert.json, tests/fixtures/onecall_imperial_highuv.json, tests/fixtures/onecall_imperial_extreme.json, tests/fixtures/geocode_austin.json, tests/fixtures/geocode_ambiguous.json</files>
  <read_first>
    - tests/fixtures/current_imperial_clear.json (Phase 1 analog — copy its real-OpenWeather field shape and the New-York `-14400` tz_offset convention; PATTERNS.md "tests/fixtures/onecall_*.json (NEW)")
    - .planning/phases/02-real-config-locations-content-templates/02-RESEARCH.md "## Validation Architecture" → "Required Test Fixtures" (the exact variant list) and "### Mapping One Call → normalized Forecast" (field paths)
    - .planning/phases/02-real-config-locations-content-templates/02-CONTEXT.md D-06 (hint thresholds the extreme/highuv/rainy fixtures must cross) and D-08 (alerts[] shape)
  </read_first>
  <action>
    Create recorded One Call 3.0 payloads shaped per the One Call 3.0 schema: top-level `lat`, `lon`, `timezone` (e.g. `"America/New_York"`), `timezone_offset` (`-14400`), `current{dt, temp, feels_like, humidity, wind_speed, uvi, weather:[{main, description}]}`, and `daily:[{dt, temp:{max, min}, pop, uvi, weather:[...]}, ...]` (at least the `daily[0]` entry). Variants and the thresholds they must cross (D-06, imperial values): `onecall_imperial_clear`/`onecall_metric_clear` = no `alerts` key at all (clear day), `daily[0].pop` ≤ 0.40, `daily[0].uvi` < 6, `feels_like` between 40 and 90, `wind_speed` ≤ 25 (zero hints). `onecall_imperial_rainy`/`onecall_metric_rainy` = `daily[0].pop` > 0.40 and `weather[0].main` = "Rain" (umbrella hint). `onecall_imperial_alert` = includes `alerts:[{sender_name, event, start, end, description, tags}]` with ONE event. `onecall_imperial_multialert` = `alerts[]` with TWO distinct `event` names (for the concise-summary path, D-08). `onecall_imperial_highuv` = `daily[0].uvi` ≥ 6 (sunscreen hint). `onecall_imperial_extreme` = `current.feels_like` < 40 (cold) OR > 90 (heat) and `current.wind_speed` > 25 (wind hint) — pick values so cold/heat AND wind both fire. Metric variants mirror the imperial ones with metric values so dual-unit display has no drift. For geocoding: `geocode_austin.json` = `/geo/1.0/direct` array with a single object `{name:"Austin", lat:30.2672, lon:-97.7431, country:"US", state:"Texas"}`; `geocode_ambiguous.json` = an array with 2+ objects sharing a name in different states/countries. Keep values plausible and JSON-valid; these are loaded via the existing `tests/conftest.py:load_fixture` (no conftest change).
  </action>
  <verify>
    <automated>uv run python -c "import json,glob; [json.load(open(f)) for f in glob.glob('tests/fixtures/onecall_*.json')+glob.glob('tests/fixtures/geocode_*.json')]; print('fixtures valid')"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 fixture files parse as valid JSON (the verify command exits 0).
    - `onecall_imperial_clear.json` has NO top-level `alerts` key; `onecall_imperial_alert.json` has an `alerts` array with ≥1 object carrying an `event` string.
    - `onecall_imperial_rainy.json` has `daily[0].pop` > 0.4; `onecall_imperial_highuv.json` has `daily[0].uvi` ≥ 6; `onecall_imperial_extreme.json` has `current.wind_speed` > 25 and `current.feels_like` outside [40, 90].
    - `geocode_austin.json` is a JSON array whose first element has numeric `lat` and `lon`.
  </acceptance_criteria>
  <done>All required One Call 3.0 + geocoding fixtures exist, are valid JSON, and cross the D-06 hint thresholds as specified.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tests/test_cli.py and retire the 2.5 aggregation module</name>
  <files>tests/test_cli.py, weatherbot/weather/aggregate.py, tests/test_aggregate.py, tests/fixtures/forecast_imperial_offset_plus.json, tests/fixtures/forecast_imperial_offset_minus.json</files>
  <read_first>
    - tests/test_send_now.py (PATTERNS.md "tests/test_cli.py (NEW)" analog — copy the `_FakeClient`/`_FakeChannel` injectable shape and the `tmp_db`/`load_fixture` fixtures)
    - tests/test_client.py (the `httpx.MockTransport` install via monkeypatch — model for the future `--check` reachability test)
    - .planning/phases/02-real-config-locations-content-templates/02-RESEARCH.md "### Phase Requirements → Test Map" (the `-k` selectors test_cli.py must eventually satisfy) and "Wave 0 Gaps"
  </read_first>
  <action>
    Create `tests/test_cli.py` as a scaffold whose test names match the research `-k` selectors so later slices can fill them in: `test_geocode_prints_coords`, `test_send_now_never_geocodes`, `test_check_validates_config`, `test_check_reachability_one_call`, `test_send_now_bad_template_aborts`, `test_check_unique_names`. Each scaffolded test should be a real pytest function that either exercises an already-shippable behavior or is marked `@pytest.mark.xfail(reason="implemented in 02-03/02-04", strict=False)` so the suite stays green now and flips to passing when the feature lands (mirror Phase 1's strict-xfail-then-remove discipline from `test_send_now.py`'s module docstring). Import a `_FakeClient`/`_FakeChannel` modeled on `test_send_now.py`. Then RETIRE the 2.5 aggregation surface (D-01): delete `weatherbot/weather/aggregate.py`, delete `tests/test_aggregate.py`, and delete the now-obsolete 2.5 bucket-offset fixtures `tests/fixtures/forecast_imperial_offset_plus.json` and `tests/fixtures/forecast_imperial_offset_minus.json`. Do NOT yet delete the other 2.5 fixtures (`current_*`, `forecast_imperial_clear.json`, `forecast_imperial_rainy.json`, `forecast_metric_clear.json`) — `models.py`/`test_models.py` still import them until Plan 02 rewrites `from_payloads`; Plan 02 owns their removal. Leave the `from weatherbot.weather.aggregate import today_aggregate` line in `models.py` UNTOUCHED in this plan (Plan 02 removes it as part of the rewrite) — but confirm no NEW code references `aggregate`.
  </action>
  <verify>
    <automated>test ! -f weatherbot/weather/aggregate.py && test ! -f tests/test_aggregate.py && uv run pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/weather/aggregate.py` and `tests/test_aggregate.py` no longer exist (the verify `test ! -f` checks pass).
    - `tests/fixtures/forecast_imperial_offset_plus.json` and `forecast_imperial_offset_minus.json` are deleted.
    - `uv run pytest tests/test_cli.py -q` runs with no errors (xfail/pass only; no collection error from a missing import).
    - `uv run pytest -q` (full suite) stays green — `grep -rn "import aggregate\|weather.aggregate" weatherbot/ tests/` shows only the single existing `models.py` import (no new references).
  </acceptance_criteria>
  <done>tests/test_cli.py exists with named scaffolds for every research `-k` selector, the 2.5 aggregation module + its test + bucket-offset fixtures are retired, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded fixture → test code | Fixtures are local, trusted test data; no live network in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | test fixtures | mitigate | Fixtures contain only OpenWeather *response* fields — never an `appid`, request URL, or webhook URL. Author them with no secrets. |
| T-02-SC | Tampering | package installs | accept | No new packages installed this phase (RESEARCH "Package Legitimacy Audit": none). No install task, so no slopcheck gate needed. |
</threat_model>

<verification>
- `uv run pytest -q` is green (full suite, with the retired test removed).
- No production or test code (other than the existing `models.py` line Plan 02 will remove) imports `weatherbot.weather.aggregate`.
- All One Call 3.0 + geocoding fixtures parse and cross their intended thresholds.
</verification>

<success_criteria>
- 10 One Call 3.0 / geocoding fixtures exist and are valid JSON crossing the D-06 thresholds.
- `tests/test_cli.py` exists with scaffolds named for every research `-k` selector.
- `aggregate.py`, `test_aggregate.py`, and the 2.5 bucket-offset fixtures are deleted.
- Full suite green.
</success_criteria>

<output>
Create `.planning/phases/02-real-config-locations-content-templates/02-01-SUMMARY.md` when done
</output>
