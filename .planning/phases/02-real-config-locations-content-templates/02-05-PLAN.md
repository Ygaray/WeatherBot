---
phase: 02-real-config-locations-content-templates
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: [02-04]
files_modified:
  - weatherbot/weather/models.py
  - weatherbot/cli.py
  - config.example.toml
  - tests/test_models.py
  - tests/test_send_now.py
autonomous: true
requirements: [LOC-02, CONF-03, CONF-01]

must_haves:
  truths:
    - "A Location with units='metric' renders a metric-primary briefing (°C-then-°F, m/s-then-mph)"
    - "A Location with units='imperial' (or units unset) renders an imperial-primary briefing (°F-then-°C, mph-then-m/s)"
    - "--send-now reads each location's own units and produces the correctly-united briefing for that location"
    - "A null/missing current.feels_like no longer fabricates a false 'cold' hint (WR-01)"
  artifacts:
    - path: "weatherbot/weather/models.py"
      provides: "Forecast.from_payloads accepts a primary-unit axis; display properties + placeholders() honor it; _hints guards None feels_like"
      contains: "primary"
    - path: "weatherbot/cli.py"
      provides: "send_now threads location.units (defaulting to imperial) into Forecast.from_payloads"
      contains: "location.units"
    - path: "tests/test_models.py"
      provides: "metric-primary display assertion + null-feels_like-no-cold-hint assertion"
      contains: "metric"
  key_links:
    - from: "weatherbot/cli.py:send_now"
      to: "Forecast.from_payloads"
      via: "primary=location.units or 'imperial'"
      pattern: "location\\.units"
    - from: "Forecast.placeholders"
      to: "Forecast display properties"
      via: "primary-unit selection"
      pattern: "primary"
---

<objective>
Honor the per-location `units` override that Phase 2 shipped but left inert (verified gap, CR-01). `Location.units` is validated, documented in `config.example.toml`, and unit-tested, yet no send/check code path reads it — so a `units = "metric"` location still renders imperial-primary (`72°F (22°C)`). This plan threads `location.units` end-to-end (config → fetch/build → render) so `--send-now` produces a correctly-united briefing for EACH location based on its own `units`, with imperial as the default when `units` is unset.

Fold in WR-01 (null `current.feels_like` fabricating a false "cold" hint) because it lives on the same `_hints` path this plan already edits.

Purpose: Close Success Criterion #1's units clause and unblock LOC-02 / CONF-03 / CONF-01. A documented, validated, example-file setting that silently does nothing is a correctness/contract defect ("all user-facing settings must be editable" and behave as configured).
Output: A metric location renders metric-primary; an imperial/unset location renders imperial-primary; the suite proves it and would FAIL if the override regresses to inert.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-real-config-locations-content-templates/02-CONTEXT.md
@.planning/phases/02-real-config-locations-content-templates/02-VERIFICATION.md
@.planning/phases/02-real-config-locations-content-templates/02-REVIEW.md

@weatherbot/weather/models.py
@weatherbot/config/models.py
@weatherbot/cli.py
@tests/test_models.py
@tests/test_send_now.py
</context>

<vertical_slice_note>
This gap is a single thin vertical slice: config field (already present) → `Forecast.from_payloads`/display (Task 1) → `send_now` wiring (Task 2). After Task 1, a `Forecast` built with `primary="metric"` renders metric-primary in isolation (unit-tested). After Task 2, `--send-now <metric-location>` end-to-end delivers a metric-primary briefing. Each task ends with a user-observable behavior change, not foundation-only work.
</vertical_slice_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a primary-unit axis to Forecast (display + placeholders) and guard _hints against missing feels_like (WR-01)</name>
  <files>weatherbot/weather/models.py, tests/test_models.py</files>
  <read_first>
    - weatherbot/weather/models.py (the whole module: `_hints` ~52-71, `Forecast` dataclass fields ~91-129, `from_payloads` ~131-195, display properties `_temp_str`/`temp_display`/`feels_like_display`/`wind_display`/`high_display`/`low_display` ~199-226, `placeholders` ~228-243)
    - tests/test_models.py (existing imperial-primary assertions: `test_from_payloads_imperial_primary_displays`, `test_placeholders_is_flat_canonical_map`; hint tests `test_hints_*`)
    - weatherbot/config/models.py (`Location.units` field ~40 + `_units_valid` validator ~52-57; the valid set is `{"imperial", "metric"}`, default `None`)
  </read_first>
  <behavior>
    - A Forecast built with primary="metric" exposes `temp_display == "20°C (68°F)"` (metric value first, imperial in parens) for the clear fixture; `wind_display == "3.6 m/s (8 mph)"`; `high_display`/`low_display`/`feels_like_display` likewise metric-primary.
    - A Forecast built with primary="imperial" (and the default when primary is omitted) keeps the EXISTING imperial-primary output unchanged: `temp_display == "68°F (20°C)"`, `wind_display == "8 mph (3.6 m/s)"` — all current `test_from_payloads_imperial_primary_displays` / `test_placeholders_is_flat_canonical_map` assertions still pass byte-for-byte.
    - `placeholders()` for a metric Forecast returns metric-primary strings (e.g. `ph["temp"]` starts with the °C value); keys remain exactly the canonical set (unchanged).
    - Hints are unchanged by the primary unit: the five thresholds (rain>40, feels<40°F, feels>90°F, wind>25mph, uvi>=6) still evaluate against the IMPERIAL-scale values, because the thresholds are calibrated in °F/mph — a metric-primary location must still fire "cold" below ~40°F. (Display unit changes presentation only, not the physical threshold.)
    - WR-01: when `current.feels_like` is missing/None in BOTH payloads, `_hints` does NOT append the cold hint (no fabricated "Bundle up, it's cold" from a coalesced 0.0). The same guard applies to wind (no false "Windy" from a null wind_speed). A present-but-null `current` yields a hint string with no cold/wind line.
  </behavior>
  <action>
    Add a `primary` axis to the Forecast so display picks the configured primary unit while imperial stays the default.

    In `from_payloads`, accept a new keyword param `primary: str = "imperial"` and store it on the dataclass as a new field `primary` (add the field to the `@dataclass`). Keep reading BOTH `onecall_imp` and `onecall_met` exactly as today (dual-unit fetch is unchanged, FCST-04 no-drift) — `primary` only selects which side is shown first.

    Refactor the display so `_temp_str` (and the wind formatter) take the primary/secondary pair in order rather than hardcoding imperial-first. Concretely: have `temp_display`, `feels_like_display`, `high_display`, `low_display`, and `wind_display` choose (imp, met) vs (met, imp) and the unit labels (`°F`/`°C`, `mph`/`m/s`) based on `self.primary`. When `primary == "metric"`, the metric value and °C/m/s label lead and the imperial value sits in parens; otherwise imperial leads (existing behavior). Preserve the existing rounding conventions (whole degrees; wind imperial whole-number / metric one decimal — keep both decimals' current behavior so imperial-primary output is byte-identical, and mirror it for metric-primary: metric value rounded to one decimal for m/s, whole for °C, imperial in parens rounded as today). Preserve the `high_display`/`low_display` fallback-to-`temp_display`-when-None behavior.

    `placeholders()` needs no key changes — it already calls the display properties, which now honor `primary`.

    For WR-01: stop coalescing the hint-driving numerics to `0.0` before threshold evaluation. Read `cur_i.get("feels_like")` and `cur_i.get("wind_speed")` as raw (possibly None) values and pass them (still imperial-scale) into `_hints`. In `_hints`, change the cold/heat guards to evaluate only when feels_like is not None, and the wind guard to evaluate only when wind is not None (rain/uvi already coalesce safely from `daily[0]`). The dataclass fields `feels_imp`/`wind_imp` that feed DISPLAY may keep their `or 0.0` coalesce (display tolerates 0), but the HINT decision must use the not-None-guarded raw values so a degraded payload yields no fabricated hint rather than a false cold/wind line.

    Do NOT change the canonical placeholder set, the alert logic, the local-date logic, or the raw-payload retention. Do NOT remove the `units` field or alter the dual fetch.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `weatherbot/weather/models.py` `from_payloads` signature includes `primary: str = "imperial"` and the `Forecast` dataclass has a `primary` field; `grep -n "primary" weatherbot/weather/models.py` returns matches in both the dataclass body and the display properties.
    - Behavior (test): a NEW test in `tests/test_models.py` builds a Forecast with `primary="metric"` from the clear fixtures and asserts `temp_display == "20°C (68°F)"` and `wind_display == "3.6 m/s (8 mph)"` (metric-primary) — this directly counters the verified imperial-primary spot-check.
    - Behavior (test): the EXISTING `test_from_payloads_imperial_primary_displays` and `test_placeholders_is_flat_canonical_map` still pass unchanged (imperial remains the default; output byte-identical).
    - Behavior (test): a NEW test asserts that on a `current`-present-but-`feels_like`-null payload (both units), `fc.hint` does NOT contain "cold" / "Bundle up" (WR-01).
    - `uv run pytest tests/test_models.py -q` exits 0.
  </acceptance_criteria>
  <done>A Forecast renders metric-primary when built with primary="metric" and imperial-primary by default; a null feels_like no longer fabricates a cold hint; all prior model tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread location.units through send_now and update config.example.toml so each location's override drives its briefing</name>
  <files>weatherbot/cli.py, config.example.toml, tests/test_send_now.py</files>
  <read_first>
    - weatherbot/cli.py (`send_now` ~75-132 — specifically the fetch round + `Forecast.from_payloads(location, onecall_imp, onecall_met)` at ~106-109; `do_check` ~183-246 uses a literal "imperial" reachability probe which is correct to LEAVE as-is)
    - weatherbot/weather/models.py (the `primary` param added in Task 1)
    - weatherbot/config/models.py (`Location.units` default `None`)
    - tests/test_send_now.py (the `_FakeClient`/`_FakeChannel` harness + `test_send_now_posts_briefing`)
    - config.example.toml (Weekend location sets `units = "imperial"` at line 38; Home has the commented hint at line 30)
  </read_first>
  <behavior>
    - `send_now` computes the effective primary unit as `location.units or "imperial"` and passes it into `Forecast.from_payloads(..., primary=<effective>)`, so a `units="metric"` location delivers a metric-primary body and an imperial/unset location delivers an imperial-primary body.
    - The dual fetch is UNCHANGED: `send_now` still calls `client.fetch_onecall(location, "imperial")` then `client.fetch_onecall(location, "metric")` (both payloads still fetched for the parenthetical secondary value, DATA-03 single-round preserved). The override selects display primary, it does NOT drop a fetch.
    - End-to-end: `--send-now` for a metric location yields a delivered body containing "°C" as the leading temperature unit; for an imperial/unset location the body leads with "°F" (existing behavior).
    - config.example.toml teaches a metric override that actually works (so the example is no longer advertising an inert setting).
  </behavior>
  <action>
    In `send_now` (weatherbot/cli.py), after `location = resolve_location(...)` and the two-call fetch round, compute `primary = location.units or "imperial"` and pass it as the new keyword to `Forecast.from_payloads(location, onecall_imp, onecall_met, primary=primary)`. Do not remove either `fetch_onecall` call and do not change the persist/render/deliver order. (Leave WR-02 fetch-before-validate ordering as-is — explicitly deferred below.)

    Leave `do_check`'s reachability probe calling `fetch_onecall(config.locations[0], "imperial")` unchanged — it is a single liveness probe, not a display path, and a fixed unit there is correct.

    In `config.example.toml`, demonstrate a real, working override: set the second/"Weekend" location to `units = "metric"` (instead of the current inert `units = "imperial"`) and update the surrounding comment to state that this location now renders metric-primary, so a user copying the example sees the override take visible effect. Keep the commented `# units = "imperial"` hint on "Home" (units omitted → imperial default). Do NOT touch `avatar_url` (WR-04 is deferred).

    In `tests/test_send_now.py`, add a test that runs `send_now` for a `Location(..., units="metric")` through the existing `_FakeClient`/`_FakeChannel` harness and asserts the delivered body leads with "°C" (metric-primary) — proving the override is honored end-to-end and that the suite would FAIL if the override regressed to inert. Keep the existing imperial test passing.
  </action>
  <verify>
    <automated>uv run pytest tests/test_send_now.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "location.units" weatherbot/cli.py` returns a match inside `send_now`; `grep -n "primary=" weatherbot/cli.py` shows the param passed to `Forecast.from_payloads`.
    - Source: `send_now` still contains both `fetch_onecall(location, "imperial")` and `fetch_onecall(location, "metric")` (dual fetch preserved) — `grep -c "fetch_onecall" weatherbot/cli.py` accounts for both calls plus the unchanged `do_check` probe.
    - Source: `config.example.toml` Weekend location reads `units = "metric"` and the comment explains the metric-primary effect.
    - Behavior (test): a NEW test in `tests/test_send_now.py` sends for a `units="metric"` location and asserts the delivered body contains "°C" leading the temperature; the existing `test_send_now_posts_briefing` (imperial, asserts "°F") still passes.
    - `uv run pytest -q` exits 0 (full suite green — no regression in test_config / test_renderer / test_review_hardening).
  </acceptance_criteria>
  <done>--send-now delivers a metric-primary briefing for a metric location and an imperial-primary briefing for an imperial/unset location; config.example.toml advertises a working override; the full suite is green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New / changed symbols and test names introduced by this gap plan:

- `Forecast.from_payloads(..., primary: str = "imperial")` — new keyword param selecting the primary display unit (weatherbot/weather/models.py).
- `Forecast.primary` — new dataclass field carrying the selected primary unit.
- Refactored `Forecast` display properties (`temp_display`, `feels_like_display`, `high_display`, `low_display`, `wind_display`) — now unit-order-aware via `self.primary`; `_temp_str`/wind formatter take primary/secondary in order.
- `_hints(...)` — now guarded against None feels_like / wind (WR-01); no fabricated cold/wind hint on a degraded payload.
- `send_now` — computes `primary = location.units or "imperial"` and threads it into `from_payloads` (weatherbot/cli.py).
- `config.example.toml` — Weekend location switched to a working `units = "metric"` example.
- New tests: a metric-primary display test + a null-feels_like-no-cold-hint test in `tests/test_models.py`; a metric-primary `--send-now` end-to-end test in `tests/test_send_now.py`.
</artifacts_this_phase_produces>

<deferred_not_in_this_plan>
These review warnings are tracked but intentionally OUT OF SCOPE for this gap plan (they do not share the units/`_hints` root cause and would scope-creep the cohesive units fix):

- **WR-02:** `send_now` fetches + persists before `validate_template` runs (the cheap deterministic guard should gate the expensive network/IO). Deferred — touches send-path ordering, not the units thread.
- **WR-03:** `do_geocode` catches only `HTTPStatusError`; a network error (`httpx.RequestError`) crashes with a traceback. Deferred.
- **WR-04:** `config.example.toml` ships `avatar_url = ""` (empty string ≠ None). Deferred — left untouched by this plan.
- **WR-05:** `--geocode` hardcodes `timezone = "America/Chicago"` in every paste snippet. Deferred.
- **WR-06 / IN-01 / IN-02 / IN-03:** docstring step-order drift, duplicated `_local_date_iso`, mutable-default `allowed`, dead `sent_text` guard. Deferred (non-blocking).

WR-01 IS folded in (Task 1) because it lives on the `_hints` path this plan already rewrites for the units thread.
</deferred_not_in_this_plan>

<verification>
- `uv run pytest -q` is green (the existing 91 tests plus the new units/hint tests).
- A `Location(units="metric")` renders metric-primary at BOTH the model layer (`placeholders()`/`temp_display`) and end-to-end through `--send-now`.
- A `Location` with units unset or `units="imperial"` renders imperial-primary, byte-identical to pre-plan output.
- A null `current.feels_like` payload produces no fabricated cold hint.
- The dual One Call fetch (imperial + metric) is preserved; the override changes display primary only, not the call count.
</verification>

<success_criteria>
- The per-location `units` override is honored end-to-end: metric → metric-primary, imperial/unset → imperial-primary (closes Success Criterion #1 units clause, LOC-02, CONF-03, CONF-01).
- The test suite would FAIL if the override regressed to inert (a metric-primary assertion exists at both the model and send-now layers).
- WR-01 fixed: no false cold/wind hint on a degraded `current` payload.
- Full suite (`uv run pytest -q`) green; deferred warnings documented.
</success_criteria>

<output>
Create `.planning/phases/02-real-config-locations-content-templates/02-05-SUMMARY.md` when done.
</output>
