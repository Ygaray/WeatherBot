---
phase: 34-test-gap-backfill
plan: 07
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - tests/test_scheduler.py
autonomous: true
requirements: [HARD-TEST-02]
must_haves:
  truths:
    - "D-01: The F108 rename-safe id!=name test extends the existing tests/test_scheduler.py (no new module), colocated with the fire_slot/plan_catchup drives; it runs in Wave 2 because it shares test_scheduler.py with the F106 correction (34-01)."
    - "D-02: The F108 test (and the F14 [EXISTS] citation) names its finding id + requirement (F108 with HARD-TEST-02; F14) in the test name or docstring."
    - "D-05: F108 asserts by construction that fire_slot's sent_log/alerts rows and plan_catchup's was_sent query carry the location .id ('loc-7'), NOT the .name ('Beach House') — red against any regression that reads .name by design."
    - "D-08: F14 (catch-up across local midnight, Phase 32) is cited [EXISTS] — test_scheduler.py:312 test_catchup_prior_local_day fully pins the 23:45-slot-recovered-at-00:15 yesterday-keyed scenario incl. GRACE edges and the already-sent dedup — confirmed for SC-3 and NOT duplicated."
    - "encoding/equality edge (HARD-TEST-02): the scheduler's definition of location identity is the .id, not the .name — F108 drives a distinct id != name through fire_slot / plan_catchup / alert-dedup and asserts every persisted key is the id."
    - "concurrency/dedup edge (HARD-TEST-02): a slot that qualifies for catch-up under both today's and yesterday's candidate date emits exactly once — cited via the F14 [EXISTS] emitted_dates dedup assertion in test_catchup_prior_local_day."
  artifacts:
    - tests/test_scheduler.py
  key_links:
    - "fire_slot claim/release/alert on location.id: claim_slot(db_path, location.id, ...) (daemon.py:210), release_claim(..., location.id, ...) (daemon.py:276/299/325), record_alert(..., location.id, ...) (daemon.py:283/301/327); plan_catchup was_sent(loc.id, ...) (catchup.py:198) — the identity seam F108 pins."
    - "test_catchup_prior_local_day (test_scheduler.py:312) — the F14 [EXISTS] midnight-catch-up pin for SC-3 (D-08)."
---

<objective>
Add the missing rename-safe identity coverage and cite the already-covered midnight catch-up test,
both in `tests/test_scheduler.py`:
- **F108 (add):** `Location.id` defaults to `name` (`config/models.py:199-207`), so EVERY existing
  test has `id == name` — no test proves the scheduler reads `.id`. A regression to `.name` is
  invisible. Drive a distinct `id != name` through `fire_slot` / `plan_catchup` / alert-dedup.
- **F14 (verify [EXISTS], D-08):** `test_catchup_prior_local_day` (`:312`) ALREADY fully pins the
  midnight catch-up scenario (23:45 slot recovered at 00:15 → one yesterday-keyed MissedSlot, GRACE
  edges, dedup). Confirm it is green and cite it for SC-3 — do NOT duplicate.

Purpose: HARD-TEST-02 — rename-safe `id != name` (F108) + the SC-3 midnight catch-up citation (F14).
Runs in Wave 2 because it shares `test_scheduler.py` with the F106 correction (34-01).
Output: an F108 rename-safe test in `tests/test_scheduler.py` and a confirmed F14 [EXISTS] citation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-test-gap-backfill/34-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
@.planning/phases/34-test-gap-backfill/34-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rename-safe id != name test through fire_slot / plan_catchup / alerts (F108)</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py:817-854 — the F106 fire_slot drive kwargs (config=, db_path=, client=, channel=, scheduled_dt=) to reuse for the fire path.
    - tests/test_scheduler.py:152-176 — `_home_config` (:152, builds the single-location config; Location kwargs at :156-162 currently name=id) and the plan_catchup drive at :176 with a `_never_sent`/spy was_sent (:172).
    - weatherbot/config/models.py:199-207 — `Location.id` defaults to `name`; build `Location(name="Beach House", id="loc-7")` so id != name.
    - weatherbot/scheduler/daemon.py:210,276,283,299,301,325,327 — fire_slot claim_slot/release_claim/record_alert all keyed on location.id (logging uses location.name at :213 — correct).
    - weatherbot/scheduler/catchup.py:198 — plan_catchup queries was_sent(loc.id, slot.time, local_date).
  </read_first>
  <behavior>
    - Build a config with Location(name="Beach House", id="loc-7") (name != id).
    - Drive fire_slot: the resulting sent_log row (and any alerts row) carries location_name == "loc-7" (the id), NOT "Beach House".
    - Drive plan_catchup with a recording was_sent spy: it is queried with "loc-7" (the id), NOT "Beach House".
  </behavior>
  <action>
    Add `test_fire_and_catchup_use_location_id_not_name` to tests/test_scheduler.py. Build the config via `_home_config` (:152) style but with `Location(name="Beach House", id="loc-7")` (add id="loc-7" to the Location kwargs at :156-162) so name != id. Drive `fire_slot` with the F106 kwargs shape (:840-854: fake client from onecall_*_clear fixtures, a _FakeChannel, a scheduled_dt, db_path=tmp_db). After the fire, read the sent_log (and alerts, if the path fires one) via the store and assert `location_name == "loc-7"` (the id), NOT "Beach House". Then drive `plan_catchup(cfg, was_sent_spy, now_utc=...)` where `was_sent_spy` records its (name, time, date) args (like `_never_sent` at :172 but recording), and assert it was queried with `"loc-7"`. Tag `# F108 / HARD-TEST-02` per D-02. This is red against any regression that reads `.name`. Do not change production code.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k "rename or location_id" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scheduler.py -k "rename or location_id" -x -q` exits 0.
    - Observable (34-VALIDATION.md F108): with `Location(name="Beach House", id="loc-7")`, the `sent_log` (and any `alerts`) row carries `location_name == "loc-7"` (the id), NOT "Beach House".
    - `plan_catchup`'s recording `was_sent` spy is queried with `"loc-7"` (the id).
    - Docstring names `# F108 / HARD-TEST-02`; a `.name`-reading regression fails; no production code changed.
  </acceptance_criteria>
  <done>test_fire_and_catchup_use_location_id_not_name proves fire_slot's sent_log/alerts rows and plan_catchup's was_sent query key on location.id ("loc-7"), not name ("Beach House"); tagged F108 / HARD-TEST-02.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm F14 midnight catch-up [EXISTS] and cite it for SC-3 (D-08)</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py:312-362 — `test_catchup_prior_local_day` (# D-01 / F14 CONFIRMED): 23:45 slot recovered at 00:15 next local day → exactly ONE MissedSlot keyed on yesterday (2026-06-10); GRACE edges (scheduled==now, exactly-GRACE, one-sec-past); already-sent dedup (`_sent_yesterday` → []).
    - weatherbot/scheduler/catchup.py:157-201 — the emitted_dates candidate-date loop (today + yesterday) and per-slot dedup the [EXISTS] test pins.
  </read_first>
  <action>
    Run and confirm `test_catchup_prior_local_day` (tests/test_scheduler.py:312) is GREEN. Verify it pins the full D-08 midnight-catch-up scenario: exactly one MissedSlot keyed on YESTERDAY's local_date for a 23:45 slot scanned at 00:15 the next local day, the GRACE boundary edges, and the already-sent dedup (a slot qualifying under both candidates emits exactly once). Cite it [EXISTS] for the SC-3 ledger (D-08) in the summary — do NOT write a duplicate midnight-catch-up test. ONLY if a specific D-08 aspect is genuinely missing (e.g. the emitted_dates exactly-once dedup is unasserted), EXTEND this existing test rather than adding a new one, and note the extension. Ensure its docstring names F14 (it already carries # D-01 / F14).
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -k catchup_prior_local_day -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scheduler.py -k catchup_prior_local_day -x -q` exits 0 (test is green).
    - Observable (34-VALIDATION.md F14): a 23:45 slot scanned at 00:15 the next local day → exactly ONE MissedSlot keyed on YESTERDAY's local_date; GRACE boundary edges hold; a slot qualifying under both candidate days emits exactly once (emitted_dates dedup).
    - Cited [EXISTS] for the SC-3 F14 ledger (D-08) — NO duplicate written; the existing test is EXTENDED only if a D-08 aspect is genuinely missing.
  </acceptance_criteria>
  <done>test_catchup_prior_local_day confirmed green and cited [EXISTS] for the SC-3 F14 midnight-catch-up pin (D-08); no duplicate added (extended only if a D-08 aspect was missing).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan only adds/confirms pytest tests in `tests/test_scheduler.py`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-07 | Tampering | tests/test_scheduler.py | low | accept | No new production code, inputs, endpoints, secrets, or trust boundaries change. F108 drives the existing fire_slot/plan_catchup on a constructed id!=name config; F14 is a verify-only citation. Tests-only. |

No new attack surface — tests-only.
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py -k "rename or location_id or catchup_prior_local_day" -x -q` — F108 green + F14 [EXISTS] green.
- `uv run pytest tests/test_scheduler.py -q` — full scheduler module green (includes the 34-01 F106 tests; ignore the syrupy report line, trust exit 0).
</verification>

<success_criteria>
F108 proves the scheduler keys on `location.id` not `.name` (a `.name` regression fails), and F14 is
confirmed [EXISTS] and cited for the SC-3 midnight-catch-up ledger. Both name their finding id +
requirement.
</success_criteria>

<output>
Create `.planning/phases/34-test-gap-backfill/34-07-SUMMARY.md` when done.
</output>
