---
phase: 34-test-gap-backfill
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_store.py
autonomous: true
requirements: [HARD-TEST-02]
must_haves:
  truths:
    - "D-01: The F37/F63 transactional test extends the existing tests/test_store.py next to test_persist_onecall_writes_both_unit_rows and test_init_db_is_idempotent (no new module)."
    - "D-02: The added transactional test (and any added F01 test) names its finding id + requirement (F37/F63/F01 with HARD-TEST-02) in the test name or docstring."
    - "D-05: The transactional test asserts by construction that a mid-persist raise leaves ZERO committed weather_onecall rows (both-or-neither) — red against any non-transactional/force-commit persist by design."
    - "D-08: The SC-3 coverage ledger is pinned for the store atomicity / data-loss path — F37 (no UNIQUE / non-atomic write), F63 (executescript force-commit), and F01 (post-send bookkeeping re-fire) — F01 is confirmed [EXISTS] in test_reliability.py best-effort tests (a raise in resolve_alert/stamp_success after result.ok keeps the delivered claim) and only added if absent."
    - "idempotency edge (HARD-TEST-02): init_db is idempotent and WAL is persistent — a fresh connect reports journal_mode=wal after repeated init_db calls (extends the existing idempotency test)."
    - "concurrency edge (HARD-TEST-02): the two-INSERT persist is a single-commit transaction (both-or-neither) so an interrupted persist never leaves a half-written weather_onecall row — F37/F63 pins this atomicity."
  artifacts:
    - tests/test_store.py
  key_links:
    - "persist (store.py:211-248): two INSERTs (imperial + metric) + single conn.commit() in one `with _connect(...)` transaction — both land or neither — the F37/F63 seam the transactional test raises inside."
    - "init_db (store.py:194-208): sole DDL owner, PRAGMA journal_mode=WAL set once (F63 fix removes per-write executescript force-commit) — the idempotency/WAL-persistence assertion."
    - "F01: fire_slot post-result.ok best-effort bookkeeping (daemon.py:350-393) never releases a delivered claim — pinned [EXISTS] in test_reliability.py:428+ best-effort tests."
---

<objective>
Add the missing store-atomicity regression in `tests/test_store.py` and confirm the F01 pinning
test for the SC-3 coverage ledger (D-08):
- **F37/F63 (add):** `test_persist_onecall_writes_both_unit_rows` (`:79`) asserts both rows exist on
  success, but no test proves the two INSERTs are **transactional** (both-or-neither on a
  mid-transaction failure). Add a mid-persist-raise test asserting zero committed rows.
- **F01 (verify [EXISTS]):** confirm ≥1 best-effort test in `test_reliability.py` pins "a raise in
  `resolve_alert`/`stamp_success` after `result.ok` keeps the delivered claim." Add one only if
  absent.

Purpose: HARD-TEST-02 — the store atomicity / data-loss path (roadmap-named, id-less; D-08).
Output: a transactional both-or-neither `persist` test (+ optional WAL-persistence assertion) in
`tests/test_store.py`, and an F01 [EXISTS] citation (or a minimal added test if the specific
assertion is absent), each tagged with its finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-test-gap-backfill/34-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
@.planning/phases/34-test-gap-backfill/34-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add transactional both-or-neither persist test (F37/F63)</name>
  <files>tests/test_store.py</files>
  <read_first>
    - tests/test_store.py:79-89 — `test_persist_onecall_writes_both_unit_rows` (asserts both imperial+metric rows exist on success — the happy-path analog).
    - tests/test_store.py:73-76 — `test_init_db_is_idempotent` (idempotency analog to extend for WAL persistence).
    - weatherbot/weather/store.py:211-248 — `persist`: two INSERTs (imperial + metric) inside one `with _connect(...)` transaction + single conn.commit() — both-or-neither (comment at :228-230).
    - weatherbot/weather/store.py:194-208 — `init_db` (sole DDL owner, PRAGMA journal_mode=WAL set once).
  </read_first>
  <behavior>
    - Monkeypatch the SECOND conn.execute (the metric INSERT) inside persist to raise mid-transaction; call persist; assert ZERO committed weather_onecall rows (the imperial INSERT rolled back with the metric failure — both-or-neither).
    - A subsequent fresh connect reports journal_mode == "wal" (WAL is persistent), and init_db remains idempotent across repeated calls.
  </behavior>
  <action>
    Add `test_persist_onecall_atomic_rollback` to tests/test_store.py near test_persist_onecall_writes_both_unit_rows (:79). Arrange a fresh tmp_db (init_db bootstrapped). Monkeypatch so the SECOND INSERT executed inside persist (the metric row) raises — e.g. wrap/patch the connection's execute to raise on the metric INSERT call, or patch the metric-side write path — while the first (imperial) INSERT proceeds. Call persist and expect it to raise (or swallow, matching persist's contract). Then open a fresh read connection and assert the committed weather_onecall row count is ZERO (both-or-neither — the imperial INSERT did NOT commit alone). Add an assertion that a fresh connect reports `journal_mode == "wal"` (WAL persistence) to cover F63's DDL-owner fix. Tag `# F37 / F63 / HARD-TEST-02` per D-02. Do not change store.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_store.py -k "atomic or persist or idempotent" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_store.py -k "atomic or persist or idempotent" -x -q` exits 0.
    - Observable (34-VALIDATION.md F37/F63): with the SECOND (metric) INSERT inside `persist` monkeypatched to raise, a fresh read connection reports ZERO committed `weather_onecall` rows (both-or-neither — the imperial INSERT rolled back).
    - A fresh connect reports `journal_mode == "wal"` (WAL persistence); `init_db` stays idempotent.
    - Docstring names `# F37 / F63 / HARD-TEST-02`; `store.py` unchanged (local monkeypatch only, teardown-restored).
  </acceptance_criteria>
  <done>test_persist_onecall_atomic_rollback proves a mid-persist raise leaves zero committed weather_onecall rows (both-or-neither) and a fresh connect reports journal_mode=wal; tagged F37 / F63 / HARD-TEST-02.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the F01 post-send bookkeeping pinning test [EXISTS] (D-08); add only if absent</name>
  <files>tests/test_reliability.py</files>
  <read_first>
    - tests/test_reliability.py:428+ — the best-effort post-send bookkeeping tests around fire_slot's result.ok path (RESEARCH A3: these likely already pin "a raise after ok keeps the claim").
    - weatherbot/scheduler/daemon.py:350-393 — the post-result.ok best-effort bookkeeping (resolve_alert / stamp_success) that must NEVER release a delivered claim (F01).
    - weatherbot/weather/store.py:251 — `was_sent` so the "claim stays / slot not re-fireable" assertion can read the sent_log state.
  </read_first>
  <action>
    Read the best-effort post-send bookkeeping tests in tests/test_reliability.py (:428+). Confirm ≥1 test pins F01: after a successful delivery (result.ok), a raise in resolve_alert or stamp_success does NOT release the won claim — the slot stays sent (was_sent still True / no re-fire, no false internal_error alert). If such an assertion EXISTS, cite it [EXISTS] for the SC-3 ledger (D-08) and add a `# F01 / HARD-TEST-02` tag to its docstring if missing — do NOT duplicate. Only if the specific "raise-after-ok keeps the delivered claim" assertion is genuinely absent, add a minimal test that fires a slot, forces resolve_alert (or stamp_success) to raise after result.ok, and asserts the claim is retained (was_sent True, slot not re-fireable). Tag `# F01 / HARD-TEST-02`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reliability.py -k "best_effort or bookkeeping or resolve_alert or stamp" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reliability.py -k "best_effort or bookkeeping or resolve_alert or stamp" -x -q` exits 0.
    - Observable (34-VALIDATION.md F01): ≥1 test pins that after `result.ok`, a raise in `resolve_alert`/`stamp_success` does NOT release the won claim — the slot stays sent (`was_sent` still True / no re-fire, no false `internal_error` alert).
    - If the assertion EXISTS: cited [EXISTS] for SC-3 (D-08), `# F01 / HARD-TEST-02` tag added to its docstring if missing — NOT duplicated. Only added if genuinely absent.
  </acceptance_criteria>
  <done>The F01 "raise-after-ok keeps the delivered claim" assertion is confirmed [EXISTS] (cited for SC-3, tagged F01 / HARD-TEST-02) or minimally added if absent; no duplicate created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan only adds/confirms pytest tests in `tests/test_store.py` and `tests/test_reliability.py`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-06 | Tampering | tests/test_store.py, tests/test_reliability.py | low | accept | No new production code, inputs, endpoints, secrets, or trust boundaries change. The transactional test monkeypatches the persist connection locally (teardown-restored) to force a mid-transaction raise. Tests-only. |

No new attack surface — tests-only.
</threat_model>

<verification>
- `uv run pytest tests/test_store.py -k "atomic or persist or idempotent" -x -q` — transactional rollback + WAL persistence green.
- `uv run pytest tests/test_reliability.py -k "best_effort or bookkeeping" -x -q` — F01 [EXISTS] confirmation green.
- `uv run pytest -q` (per-wave gate) — full suite exit 0 (ignore the syrupy "N snapshots failed" report line).
</verification>

<success_criteria>
F37/F63 proves the two-INSERT persist is transactional (mid-persist raise → zero committed rows) with
WAL persistence; F01 is confirmed [EXISTS] (raise-after-ok keeps the claim) for the SC-3 ledger. Both
name their finding id + requirement.
</success_criteria>

<output>
Create `.planning/phases/34-test-gap-backfill/34-06-SUMMARY.md` when done.
</output>
