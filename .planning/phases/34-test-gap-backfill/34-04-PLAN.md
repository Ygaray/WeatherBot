---
phase: 34-test-gap-backfill
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_models.py
  - weatherbot/weather/dates.py
autonomous: true
requirements: [HARD-TEST-02]
must_haves:
  truths:
    - "D-01: The F107 confirmation/strengthening and the F109 positive test extend the existing tests/test_models.py next to test_dt_paired_briefing and test_daily0_not_today_degrades (no new module)."
    - "D-02: Each test names its finding id + requirement (F107 / F109 with HARD-TEST-02) in the test name or docstring."
    - "D-05: The F109 positive test asserts by construction that from_payloads selects TODAY's high/low when today is NOT at daily[0] — red against any positional daily[0] regression by design; F107 (if strengthened) asserts the wrong-day metric never mispairs."
    - "D-07: F109's positive case is a latent-escape watchpoint — if select_today_daily is positional/off-by-one and the test goes red against current code, the executor treats it as a real escape and folds the MINIMAL correctness fix into weatherbot/weather/dates.py (never weakens the test), escalating only if the fix is large or out of scope."
    - "D-08: F107 is cited [EXISTS] — test_dt_paired_briefing (test_models.py:141) already pins the degrade-not-mispair guard (line 163); it is confirmed for SC-3 and only strengthened if the guard is absent, not duplicated."
    - "boundary edge (HARD-TEST-02): the entry exactly at the today boundary is selected by local date, not position — F109 places today at daily[2] and asserts high_imp/low_imp equal the index-2 values, one step either side (yesterday at [0]) is rejected."
    - "precision edge (HARD-TEST-02): the dt-based metric pairing matches on the imperial day's exact dt or degrades to None — never a wrong-day precision loss — pinned by the [EXISTS] test_dt_paired_briefing mispair guard (F107)."
  artifacts:
    - tests/test_models.py
  key_links:
    - "Forecast.from_payloads (models.py:250) -> select_today_daily (weatherbot/weather/dates.py:77, called at models.py:300) — the date-anchored today selector F109 pins; the 'NEVER positional daily[0]' contract is at models.py:298-299."
    - "from_payloads dt-pairing block (models.py:310-321) — pairs the metric daily entry by the imperial day's dt or degrades to {} — the F107 [EXISTS] guard (test_models.py:163)."
---

<objective>
Cover the `Forecast.from_payloads` daily selection/pairing paths in `tests/test_models.py`:
- **F107 (verify [EXISTS], strengthen only if needed):** `test_dt_paired_briefing` (`:141`)
  already asserts dt-anchored pairing degrades rather than mispairs (guard at `:163`). Read it,
  confirm it pins "wrong-day °C never appears", cite it for SC-3, and strengthen only if the
  degrade-not-mispair assertion is genuinely absent.
- **F109 (add positive):** the negative case (`test_daily0_not_today_degrades`, `:664`) proves a
  yesterday-dated `daily[0]` degrades to None. The positive/discriminating case is missing: today
  NOT at index 0 → the selector must still pick today's entry. This is a D-07 watchpoint — it may
  go red against current code.

Purpose: HARD-TEST-02 — dt-based imperial/metric pairing (F107) and today-anchored daily[0] (F109).
Output: an F109 positive test (today at daily[2]) in `tests/test_models.py`, and an F107 [EXISTS]
citation (or minimal strengthening). If F109 reveals a real selector bug, a minimal
`weatherbot/weather/dates.py` fix folded in (D-07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-test-gap-backfill/34-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
@.planning/phases/34-test-gap-backfill/34-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm F107 dt-pairing [EXISTS]; strengthen only if the mispair guard is absent</name>
  <files>tests/test_models.py</files>
  <read_first>
    - tests/test_models.py:141-163 — `test_dt_paired_briefing`: uses the dtskew fixtures, asserts high_display == "76°F" / low_display == "58°F", and guards the mispair with `assert fc.high_display != "76°F (100°C)"` (:163).
    - weatherbot/weather/models.py:310-321 — the from_payloads dt-pairing block (matches metric daily by the imperial day's dt; degrades to {} on no dt-match).
    - tests/fixtures/onecall_imperial_dtskew.json, tests/fixtures/onecall_metric_dtskew.json — the deliberately dt-skewed pairing fixtures.
  </read_first>
  <action>
    Read `test_dt_paired_briefing` (tests/test_models.py:141-163) in full. Confirm it asserts (a) the imperial-anchored high/low ("76°F"/"58°F") AND (b) the degrade-not-mispair guard that the wrong-day °C ("76°F (100°C)") never appears (:163). If BOTH hold, mark F107 [EXISTS] in the summary and cite it for SC-3 (D-08) — do NOT duplicate. Only if the degrade-not-mispair assertion is genuinely absent, add a single assertion in the same style pinning that a dt-mismatched metric degrades to None (imperial-only render) rather than mispairing. Ensure the test docstring names `# F107 / HARD-TEST-02` (add the requirement tag if only the finding id is present).
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -k dt_pair -x -q</automated>
  </verify>
  <done>test_dt_paired_briefing confirmed to pin dt-anchored pairing + the degrade-not-mispair guard (or minimally strengthened if absent), tagged F107 / HARD-TEST-02 and cited [EXISTS] for SC-3; no duplicate added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the F109 positive test (today not at daily[0]) — D-07 watchpoint</name>
  <files>tests/test_models.py, weatherbot/weather/dates.py</files>
  <read_first>
    - tests/test_models.py:664-686 — `test_daily0_not_today_degrades` (the negative twin, tagged # D-05 / F35 / F109): uses a `_shift_daily0_back_one_day` helper and `Forecast.from_payloads(LOC, imp, met, now_utc=NY_NOW)`. Copy its construction style.
    - weatherbot/weather/models.py:298-300 — the "NEVER positional daily[0]" contract comment and the select_today_daily call site.
    - weatherbot/weather/dates.py:77 — `select_today_daily(daily, tz, local_date)`: the date-anchored today selector. If it is positional or off-by-one, the F109 positive test goes red (D-07).
  </read_first>
  <behavior>
    - Build a payload where today's daily entry is NOT at index 0 (e.g. daily[0] = yesterday, daily[2] = today).
    - Forecast.from_payloads selects the TODAY entry: fc.high_imp / fc.low_imp equal the index-2 (today) values, NOT the index-0 (yesterday) values.
    - fc.local_date == the configured-tz today.
  </behavior>
  <action>
    Add `test_daily0_today_not_at_index_zero_selects_today` to tests/test_models.py near test_daily0_not_today_degrades (:664). Construct imperial/metric payloads whose daily arrays place yesterday at index 0 and TODAY at index 2 (reuse the fixture-loading + shifting style of the negative twin; a small helper that inserts/reorders entries is fine). Call `Forecast.from_payloads(LOC, imp, met, now_utc=NY_NOW)` and assert `fc.high_imp`/`fc.low_imp` equal the index-2 (today) values — NOT index 0 — and `fc.local_date` == the configured-tz today. Tag `# F109 / HARD-TEST-02` per D-02. D-07 WATCHPOINT: if this goes RED against current code (select_today_daily positional/off-by-one), treat it as a REAL ESCAPE — fold the MINIMAL correction into weatherbot/weather/dates.py `select_today_daily` so the selector is date-anchored, KEEP the pinning test green, and record the fix in the summary. Do NOT weaken the test. Escalate to the user only if the required fix is large or clearly out of this phase's scope. If the test is GREEN as written, no dates.py change is made (leave it untouched).
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py -k daily0 -x -q</automated>
  </verify>
  <done>test_daily0_today_not_at_index_zero_selects_today passes: from_payloads selects today's high/low when today is at daily[2], proving the selector is date-anchored not positional; if a real select_today_daily escape was found, a minimal dates.py fix was folded in and the test stays green. Tagged F109 / HARD-TEST-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan adds/confirms pytest tests in `tests/test_models.py`; the only possible production change is a MINIMAL, in-scope `select_today_daily` correction folded in per D-07 IF the F109 positive test reveals a real escape.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-04 | Tampering | tests/test_models.py (+ possible minimal weatherbot/weather/dates.py fix) | low | accept | No new inputs, endpoints, secrets, or trust boundaries. A D-07 fold-in (if any) is a minimal correction to an existing pure selector on already-fetched forecast data — no new attack surface. Tests-first. |

No new attack surface — tests-only (with a possible minimal D-07 correctness fold-in to an existing pure function).
</threat_model>

<verification>
- `uv run pytest tests/test_models.py -k "dt_pair or daily0" -x -q` — F107 [EXISTS] confirmation + F109 positive both green.
- `uv run pytest tests/test_models.py -q` — full models module green (ignore the syrupy report line; trust exit 0).
- If a D-07 fold-in landed: `uv run pytest -q` full suite green to confirm the minimal dates.py change did not regress select_today_daily callers.
</verification>

<success_criteria>
F107 is confirmed [EXISTS] (or minimally strengthened) and cited for SC-3; F109 proves the today
selector is date-anchored (today at daily[2] selected correctly). Any real select_today_daily escape
is fixed minimally per D-07 with the pinning test kept green. Both name their finding id + requirement.
</success_criteria>

<output>
Create `.planning/phases/34-test-gap-backfill/34-04-SUMMARY.md` when done.
</output>
