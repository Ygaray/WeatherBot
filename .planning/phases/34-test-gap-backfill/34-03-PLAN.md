---
phase: 34-test-gap-backfill
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cache.py
  - tests/test_reload_engine.py
autonomous: true
requirements: [HARD-TEST-01]
must_haves:
  truths:
    - "D-01: The F115 cache fix extends tests/test_cache.py and the F116 reconcile-ordering fix extends tests/test_reload_engine.py — the existing per-module files, no new consolidated module."
    - "D-02: The corrected cache test and the reconcile-ordering test each name their finding id + requirement (F115 / F116 with HARD-TEST-01) in the test name or docstring."
    - "D-05: F115 asserts by construction that a distinct id != name collapses the cache key on .id (name-keyed shortcut is red), and F116 asserts by construction order.index(register) < order.index(remove:gone) (a remove-then-register engine is red) — both red against the exact bug behavior by design."
    - "encoding/equality edge (HARD-TEST-01): the cache's definition of location equality is the resolved .id, not the .name — F115 proves two 'Cabin' lookups (incl. a case variant) collapse on id-loc-42 to one fetch, and an id-mismatch does not hit."
    - "concurrency/ordering edge (HARD-TEST-01): reconcile guarantees no job-gap — register happens strictly before every remove; F116 pins order.index(register) < order.index(remove:gone) via a shared order log through injected stubs."
  artifacts:
    - tests/test_cache.py
    - tests/test_reload_engine.py
  key_links:
    - "ForecastCache.lookup (cache.py:129) keys on resolve_location(config, name).id (cache.py:151-152) — F115 must use id != name so the id-collapse is genuinely proven, not masked by the id==name default."
    - "ReloadEngine.reconcile (yahir_reusable_bot/config/reload.py:184-190) registers jobs BEFORE removing excluded ones — F116 pins the no-gap ordering via injected app-side stubs; no hub change."
---

<objective>
Correct two false-green tests, one per file:
- **F115 (fix, `tests/test_cache.py`):** existing cache tests use `_loc("home")` where `id`
  defaults to `name`, so a name-keyed cache would pass identically — the id-collapse claim is
  unproven. Use a distinct `id != name` and prove the cache collapses on `.id`.
- **F116 (fix, `tests/test_reload_engine.py`):** `test_reload_committed_success_diff_...` (`:155`)
  asserts register happened and remove happened, but NOT that register happened **before** remove
  — a remove-then-register engine (momentary job gap) passes. Pin the ordering.

Purpose: HARD-TEST-01 — strengthen two false-green assertions. Both files are independent (no
shared state), so the two tasks are colocated in one plan for context efficiency.
Output: a corrected id!=name cache test in `tests/test_cache.py` and a register-before-remove
ordering assertion in `tests/test_reload_engine.py`, each tagged with its finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-test-gap-backfill/34-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
@.planning/phases/34-test-gap-backfill/34-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the cache collapses on .id with a distinct id != name (F115)</name>
  <files>tests/test_cache.py</files>
  <read_first>
    - tests/test_cache.py:53-59 — the `_loc` helper (already accepts `_loc(name, id=...)`, just never exercised with id != name for the collapse claim).
    - tests/test_cache.py:73-88 — `test_second_lookup_within_ttl_hits_cache`: monkeypatches `cache_mod.lookup_weather` into a `fetches` list, asserts two lookups -> one fetch (the counting pattern to reuse).
    - weatherbot/interactive/cache.py:129-184 — `ForecastCache.lookup` keys on `resolve_location(config, name).id` (:151-152): `key = loc_id if suffix is None else (loc_id, suffix)` — the id-keyed contract.
  </read_first>
  <behavior>
    - Build a location with a distinct id (e.g. _loc("Cabin", id="loc-42")). Two lookups by the name "Cabin" (including a case variant like "cabin") collapse on the id -> exactly one underlying fetch.
    - A location whose name differs but whose id matches "loc-42" hits the same cache entry (no second fetch).
    - A location whose id differs (distinct id) does NOT hit the "Cabin" entry -> a new fetch occurs.
    - The resolved cache key is the id ("loc-42"), not the name.
  </behavior>
  <action>
    In tests/test_cache.py, add (or convert the existing masked test into) a test using `_loc("Cabin", id="loc-42")` — a distinct id != name — configured into the holder. Reuse the fetch-counting monkeypatch pattern from test_second_lookup_within_ttl_hits_cache (:73-88): wrap `cache_mod.lookup_weather` to append into a `fetches` list. Assert: two lookups of "Cabin" (and a case variant "cabin") -> len(fetches) == 1 (collapse on the id). Add the discriminating assertions: a location whose name differs but id == "loc-42" hits the same entry (no new fetch), while a location whose id is distinct does NOT hit (a new fetch appends). If ForecastCache exposes the resolved key, assert it equals "loc-42" (the id), not "Cabin". A name-keyed shortcut fails because "Cabin" vs "cabin" would key differently. Tag `# F115 / HARD-TEST-01` per D-02. Do not change cache.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cache.py -x -q` exits 0.
    - Observable (34-VALIDATION.md F115): with a distinct `id != name` (`_loc("Cabin", id="loc-42")`), two lookups of "Cabin" (incl. case variant "cabin") → `len(fetches) == 1` (collapse on `.id`); a distinct-id location does NOT hit (a new fetch appends).
    - A name-keyed shortcut would fail this ("Cabin" vs "cabin" key differently).
    - Docstring names `# F115 / HARD-TEST-01`; `cache.py` unchanged.
  </acceptance_criteria>
  <done>The cache test uses a distinct id != name ("Cabin"/"loc-42"), proves two lookups collapse on the id to one fetch (name-keyed shortcut would fail), and the id-mismatch case does not hit; tagged F115 / HARD-TEST-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin reconcile register-before-remove ordering (F116)</name>
  <files>tests/test_reload_engine.py</files>
  <read_first>
    - tests/test_reload_engine.py:155-177 — `test_reload_committed_success_diff_removes_excluded_and_summary`: asserts calls["register"] == [new_cfg] (:174) and fake.removed == ["gone"] (:175) but NOT ordering.
    - tests/test_reload_engine.py:42-52 — `_FakeSchedulerEngine` records list_live_ids / remove into self.removed (:51-52) — extend it to also append into a shared order log.
    - .venv (yahir_reusable_bot)/config/reload.py:184-190 — `ReloadEngine.reconcile`: _register_jobs(...) at :184 (ADD first), then the remove loop at :189 (REMOVE after) — the no-gap invariant to pin.
  </read_first>
  <behavior>
    - A shared ordered list records "register" when the injected register_jobs stub runs and "remove:{job_id}" when the fake scheduler engine's remove runs.
    - After engine.reload(...) with a diff that both registers new_cfg and removes "gone": order.index("register") < order.index("remove:gone") — register strictly before every remove (no momentary job gap).
    - The existing assertions (calls["register"] == [new_cfg], removed == ["gone"]) are retained.
  </behavior>
  <action>
    In tests/test_reload_engine.py, thread a shared `order: list[str] = []` through the injected stubs used by test_reload_committed_success_diff_removes_excluded_and_summary (:155). Make the injected `register_jobs` stub append `"register"` to `order`, and extend `_FakeSchedulerEngine.remove` (:51-52) to append `f"remove:{job_id}"` to the SAME shared `order` list (in addition to its existing self.removed). After the reload, add `assert order.index("register") < order.index("remove:gone")` (register strictly before the remove — the no-gap-in-jobs invariant). Keep the existing register/removed assertions. Tag `# F116 / HARD-TEST-01`. The ReloadEngine is a hub symbol but the test drives it through injected app-side stubs — no hub change (D-07 / ECOSYSTEM.md: a hub fix would be human-gated, but no hub fix is needed here — this pins existing correct ordering).
  </action>
  <verify>
    <automated>uv run pytest tests/test_reload_engine.py -k committed -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reload_engine.py -k committed -x -q` exits 0.
    - Observable (34-VALIDATION.md F116): `order.index("register") < order.index("remove:gone")` — register strictly before every remove (no job gap).
    - The existing `calls["register"] == [new_cfg]` and `removed == ["gone"]` assertions are retained; a remove-then-register engine would fail the new ordering assertion.
    - Docstring names `# F116 / HARD-TEST-01`; hub ReloadEngine driven via injected app-side stubs (no hub edit).
  </acceptance_criteria>
  <done>test_reload_committed_success_diff_... asserts order.index("register") < order.index("remove:gone") via a shared order log through injected stubs, retaining the existing register/removed assertions; tagged F116 / HARD-TEST-01. A remove-then-register engine would fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan only adds/corrects pytest tests in `tests/test_cache.py` and `tests/test_reload_engine.py`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-03 | Tampering | tests/test_cache.py, tests/test_reload_engine.py | low | accept | No new production code, inputs, endpoints, secrets, or trust boundaries change. F116 drives the hub ReloadEngine via injected app-side stubs — no hub source edited (hub changes are human-gated per ECOSYSTEM.md). Tests-only. |

No new attack surface — tests-only.
</threat_model>

<verification>
- `uv run pytest tests/test_cache.py tests/test_reload_engine.py -x -q` — both files green.
- `uv run pytest -q` (per-wave gate) — full suite exit 0 (ignore the syrupy "N snapshots failed" report line).
</verification>

<success_criteria>
F115 proves the cache key collapses on `.id` using a distinct id != name (name-keyed shortcut would
fail), and F116 pins register-before-remove ordering (remove-then-register would fail). Both name
their finding id + requirement.
</success_criteria>

<output>
Create `.planning/phases/34-test-gap-backfill/34-03-SUMMARY.md` when done.
</output>
