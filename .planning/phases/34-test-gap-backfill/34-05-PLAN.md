---
phase: 34-test-gap-backfill
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_multiday.py
autonomous: true
requirements: [HARD-TEST-02]
must_haves:
  truths:
    - "D-01: The F111 weekend roll-forward test and the F113 null-dt test extend the existing tests/test_multiday.py next to test_weekday_run_on_saturday_rolls_forward and test_weekend_run_returns_fri_sat_sun (no new module)."
    - "D-02: Each added test names its finding id + requirement (F111 / F113 with HARD-TEST-02) in the test name or docstring."
    - "D-05: F111 asserts by construction that a wholly-past weekend block rolls forward to next-week Fri/Sat/Sun (or horizon notices) with no IndexError, and F113 asserts by construction that a dt=None entry is skipped from the date-index map — each red against the exact uncovered-path bug by design."
    - "boundary edge (HARD-TEST-02): F111 pins the whole-block-past branch (select_days('weekend', <a Monday>) where Fri/Sat/Sun are all past) at the 7-day horizon boundary — indices resolve to next week or emit horizon notices, never IndexError."
    - "empty/null edge (HARD-TEST-02): F113 pins the null/empty case — a daily entry with dt=None (and/or a fully-null entry) is skipped in _date_index_map, never appears in by_date, and yields a notice not a TypeError/IndexError."
  artifacts:
    - tests/test_multiday.py
  key_links:
    - "select_days (multiday.py:61) whole-block roll-forward branch (multiday.py:104-107: base_deltas + 7 when base_tokens and not upcoming) — F111 pins the weekend (_WEEKEND_DAYS = fri,sat,sun) whole-block-past case."
    - "_date_index_map (multiday.py:49-58) null-dt skip (multiday.py:52-56: if dt is None: continue) — F113 pins that select_days never crashes on a null-dt entry."
---

<objective>
Add two missing high-risk `select_days` tests in `tests/test_multiday.py`:
- **F111 (add):** `test_weekday_run_on_saturday_rolls_forward` (`:168`) covers the **weekday** block
  roll-forward. No test covers the **weekend** block (`kind='weekend'`) rolling forward when the
  whole Fri-Sat-Sun block is past.
- **F113 (add):** no multiday test injects a `daily[]` entry with `dt=None`. The `_date_index_map`
  null-dt skip (`multiday.py:52-56`) is unexercised on the `select_days` path.

Purpose: HARD-TEST-02 — weekend roll-forward and null-dt date-index robustness. Both are pure
`select_days` tests in the same file, colocated in one plan.
Output: a weekend whole-block roll-forward test and a null-dt skip test in `tests/test_multiday.py`,
each tagged with its finding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-test-gap-backfill/34-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
@.planning/phases/34-test-gap-backfill/34-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add weekend whole-block roll-forward test (F111)</name>
  <files>tests/test_multiday.py</files>
  <read_first>
    - tests/test_multiday.py:168-176 — `test_weekday_run_on_saturday_rolls_forward`: `indices, notices = multiday.select_days("weekday", date(2026,6,20), daily, add=set(), drop=set(), tz=_TZ)` then `assert indices == [3,4,5,6,7]` / `assert notices == []`. Copy this call shape.
    - tests/test_multiday.py:35 — the `imp` fixture / 8-day fixture loading style; tests/test_multiday.py:157-165 — `test_weekend_run_returns_fri_sat_sun` for the weekend select_days call shape.
    - weatherbot/weather/multiday.py:33 — `_WEEKEND_DAYS = ("fri","sat","sun")`; multiday.py:104-107 — the whole-block roll-forward branch (`if base_tokens and not upcoming: base_deltas = [d+7 for d in base_deltas]`); multiday.py:127-128 — the horizon notice path.
  </read_first>
  <behavior>
    - select_days("weekend", <a Monday>, daily, add=set(), drop=set(), tz=_TZ) where Fri/Sat/Sun are all in the past relative to that Monday rolls the whole block forward to NEXT week's Fri/Sat/Sun.
    - Against an 8-day daily fixture, the resolved indices are the next-week Fri/Sat/Sun positions (or horizon notices for entries beyond the 7-day window) — never an IndexError.
    - notices reflect only genuine beyond-horizon entries (empty if all three resolve within the window).
  </behavior>
  <action>
    Add `test_weekend_run_on_monday_rolls_forward` to tests/test_multiday.py near test_weekday_run_on_saturday_rolls_forward (:168). Call `multiday.select_days("weekend", <a Monday date>, daily, add=set(), drop=set(), tz=_TZ)` using an 8-day daily fixture (the `imp` / 8-day fixture already loaded in the file) so that the current-week Fri/Sat/Sun are all in the past and the roll-forward branch (multiday.py:104-107) fires. Assert the returned indices resolve to next-week Fri/Sat/Sun positions within the 8-day horizon (or the expected horizon notices for any beyond-window day), and assert NO IndexError is raised. Choose the Monday and fixture window so the assertion is deterministic (mirror the exact index math of the weekday twin). Tag `# F111 / HARD-TEST-02` per D-02. Do not change multiday.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_multiday.py -k weekend -x -q</automated>
  </verify>
  <done>test_weekend_run_on_monday_rolls_forward proves a wholly-past weekend block rolls forward to next-week Fri/Sat/Sun (or emits horizon notices) with no IndexError; tagged F111 / HARD-TEST-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add null-dt skip test in the date-index map (F113)</name>
  <files>tests/test_multiday.py</files>
  <read_first>
    - tests/test_multiday.py:157-165 — `test_weekend_run_returns_fri_sat_sun` (the select_days call shape to reuse).
    - weatherbot/weather/multiday.py:49-58 — `_date_index_map`: iterates daily, `dt = (day or {}).get("dt")`, `if dt is None: continue` (:52-56) — skip null-dt entries; then `local = datetime.fromtimestamp(dt, tz).date(); out.setdefault(local, i)`.
    - weatherbot/weather/multiday.py:61-132 — `select_days` uses the by_date map and emits notices (multiday.py:127-128) for desired dates with no candidate.
  </read_first>
  <behavior>
    - A daily list containing an entry with "dt": None (and/or a fully-null entry) alongside valid entries: the null-dt entry is skipped in _date_index_map (never appears in by_date).
    - select_days returns valid indices for the good entries and emits a notice (not IndexError/TypeError) for any desired date whose only candidate had a null dt.
  </behavior>
  <action>
    Add `test_null_dt_entry_skipped_in_date_index` to tests/test_multiday.py near test_weekend_run_returns_fri_sat_sun (:157). Build a `daily` list that mixes valid dt-bearing entries with at least one `{"dt": None, ...}` entry (and optionally a fully-null entry). Call `multiday.select_days(...)` with a kind/date that would otherwise index onto the null-dt slot. Assert: the null-dt entry never appears in the returned valid indices (it is skipped); the good entries still resolve to correct indices; and a desired date whose only candidate was the null-dt entry produces a notice — with NO TypeError/IndexError raised. Tag `# F113 / HARD-TEST-02` per D-02. Do not change multiday.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_multiday.py -k null_dt -x -q</automated>
  </verify>
  <done>test_null_dt_entry_skipped_in_date_index proves a dt=None daily entry is skipped from the date-index map (never in valid indices), good entries still resolve, and a null-only desired date yields a notice not a crash; tagged F113 / HARD-TEST-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan only adds pytest tests in `tests/test_multiday.py`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-05 | Tampering | tests/test_multiday.py | low | accept | No new production code, inputs, endpoints, secrets, or trust boundaries change. Both tests drive the pure select_days/_date_index_map selector on constructed fixture data. Tests-only. |

No new attack surface — tests-only.
</threat_model>

<verification>
- `uv run pytest tests/test_multiday.py -k "weekend or null_dt" -x -q` — both new tests green.
- `uv run pytest tests/test_multiday.py -q` — full multiday module green (ignore the syrupy report line; trust exit 0).
</verification>

<success_criteria>
F111 pins the weekend whole-block roll-forward (no IndexError) and F113 pins the null-dt skip (no
TypeError, notice emitted). Both name their finding id + requirement.
</success_criteria>

<output>
Create `.planning/phases/34-test-gap-backfill/34-05-SUMMARY.md` when done.
</output>
