---
phase: 27-discord-adapter-panelkit-render-cycle-fix
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - weatherbot/interactive/bot.py
  - weatherbot/interactive/panel.py
  - weatherbot/interactive/__init__.py
  - weatherbot/scheduler/wiring.py
  - weatherbot/scheduler/daemon.py
autonomous: true
requirements: [SEAM-07]

must_haves:
  truths:
    - "render_embed STAYS app-side in bot.py and is injected into the module PanelKit as render — neither the panel->bot top import nor the bot->panel deferred import survives"
    - "panel.py shrinks to the app cosmetic contributors (LocationSelect, ForecastButton, the wb: literals, the contributor functions) — the PanelView machinery is gone"
    - "build_runtime / the daemon composition root constructs the MODULE BotThread + PanelKit, injecting render, the cosmetic contributors, marker='wb:', operator_id, and the panel_channel_id reader"
    - "The full existing suite + the Phase-21 panel/clone-render goldens + the custom_id byte snapshot + the embed goldens stay byte-identical (no diff)"
  artifacts:
    - path: "weatherbot/interactive/bot.py"
      provides: "render_embed (app render) + build_inbound_embed; BotThread/build_client/summon DELETED"
      contains: "def render_embed"
    - path: "weatherbot/interactive/panel.py"
      provides: "App cosmetic contributors (LocationSelect, ForecastButton) + the wb: custom_id literals + the contributor builder functions"
      contains: "wb:loc:select"
    - path: "weatherbot/scheduler/wiring.py"
      provides: "Composition root injecting render/contributors/marker/operator_id into the module adapter"
      contains: "PanelKit"
  key_links:
    - from: "weatherbot/scheduler/wiring.py"
      to: "yahir_reusable_bot/discord"
      via: "build_runtime imports and constructs the module BotThread/PanelKit with app specifics injected"
      pattern: "from yahir_reusable_bot.discord"
    - from: "weatherbot/scheduler/wiring.py"
      to: "weatherbot/interactive/bot.py"
      via: "injects render=render_embed (the app embed builder)"
      pattern: "render_embed"
    - from: "weatherbot/interactive/panel.py"
      to: "yahir_reusable_bot/discord/selection.py"
      via: "LocationSelect.callback sets the injected SelectedContext via ctx.set(...)"
      pattern: "SelectedContext"
---

<objective>
Rewire the app side onto the relocated module adapter (Plan 01). Keep `render_embed` in `bot.py` (it is irreducibly weather/house-style) and inject it as the module's `render`. Shrink `panel.py` to the app cosmetic contributors. DELETE the relocated `BotThread`/`build_client`/summon machinery from `bot.py`. Construct the MODULE `BotThread` + `PanelKit` at the single composition root (`build_runtime` / the `daemon.py` construction site), injecting render + cosmetic contributors + `marker="wb:"` + `operator_id` + the `panel_channel_id` reader.

This resolves the cycle by ownership: the `panel.py:54` module-top `from weatherbot.interactive.bot import render_embed` and the `bot.py:307`/`bot.py:583` deferred `PanelView` imports ALL vanish (SC#2). The Phase-21 goldens are the byte-identical oracle for every cut.

Purpose: SEAM-07 — land the relocation app-side, byte-identically, with the cycle resolved.
Output: modified `bot.py` (shrunk), `panel.py` (shrunk to contributors), `interactive/__init__.py`, `wiring.py` + `daemon.py` (rewired root).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-CONTEXT.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW/CHANGED symbols this PLAN introduces:
- `weatherbot/interactive/panel.py` :: contributor builder functions handed to `PanelKit` (e.g. a `build_contributors(...)` that returns the `LocationSelect` + four `ForecastButton`s as `ItemContributor` callables), an exported app-side marker constant (e.g. `PANEL_MARKER = "wb:"`), the app-side per-spec consumes-context datum (re-added `takes_location`/`meta` per Phase-26 D-02) used to drive 📍 suppression
- `weatherbot/interactive/bot.py` :: `render_embed` UNCHANGED in body but its signature/threading adapted to receive the generic `SelectedContext` (per RESEARCH Pattern 2) — the app pulls `.value` to draw its 📍 line
- `weatherbot/scheduler/wiring.py` / `daemon.py` :: the rewired `BotThread`/`PanelKit` construction injecting render/contributors/marker/operator_id/panel_channel_id reader
- DELETIONS: `BotThread`, `build_client`, `_handle_panel_summon`, `_REQUIRED_PANEL_PERMS` (if relocated), the deferred `PanelView` imports — removed from `weatherbot/interactive/bot.py`; `PanelView` + `_is_owned_panel` + `_render_view`/`_clone_child` + the module-top `render_embed` import removed from `weatherbot/interactive/panel.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Keep render_embed app-side; thread SelectedContext; delete gateway machinery from bot.py (D-01)</name>
  <files>weatherbot/interactive/bot.py</files>
  <read_first>
    - weatherbot/interactive/bot.py L194-273 (`render_embed` — the 📍 line L221-222, `BRIEFING_COLOR_INT` L228, `Updated <t:…>` stamp L223, WR-02 field-budget split L233-271; the argless-suppression `if location is not None` L221 is load-bearing for the embed golden), L276-399 (`_handle_panel_summon` — DELETE), L425-533 (`build_on_message`/the inbound path that calls render_embed L524), L536-609 (`build_client` — DELETE), L612-709 (`BotThread` — DELETE), the `__all__` at L56-62
    - weatherbot/interactive/panel.py L547 + L614 (the two `render_embed(reply, location=arg)` call sites that move to the injected render path)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md (Pattern 2 — the render signature + SelectedContext threading + the argless-suppression nuance; Pitfall 2 — 📍 drift, highest-risk)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the bot.py MODIFIED section)
  </read_first>
  <action>
    In `weatherbot/interactive/bot.py`:

    KEEP `render_embed` defined here (D-01) — it is irreducibly weather/house-style. Adapt its threading to receive the generic context per RESEARCH Pattern 2: the module invokes `render(reply, ctx)` where `ctx: SelectedContext[str]`. Preserve the EXACT body (📍, `BRIEFING_COLOR_INT`, `Updated <t:…>`, WR-02 field-budget split). The 📍 argless-suppression is load-bearing for the embed golden (Pitfall 2): the app's render must draw 📍 from the selected item only when the command consumes context, and suppress it otherwise. Per Pattern 2, the chosen mechanism is: the module threads the live `ctx` but constructs the `ctx` it passes with `.value = selection.value if consumes else None` (driven by the app-supplied per-spec datum), so `render` keeps its existing `if value is not None` suppression branch unchanged. Keep `render_embed` (or a thin compatible wrapper) usable by the inbound `build_on_message` path (bot.py:524) byte-identically. Also keep `build_inbound_embed` (L402) here.

    DELETE from `bot.py`: `_handle_panel_summon` (L276-399, moved to the module summon orchestration), `build_client` (L536-609, moved), `BotThread` (L612-709, moved), and the operator-copy panel constants / perm constants that relocated (keep only the operator-feedback COPY strings the app still needs for its thin summon — per Pattern 3 the copy strings stay app-side; only the perm-set CONSTANT may have relocated). REMOVE both deferred `from weatherbot.interactive.panel import PanelView` imports (L307, L583) — they no longer exist anywhere. Update `__all__` (drop the removed symbols; keep `render_embed`, `build_inbound_embed`, `build_on_message`).

    The app's thin summon (the operator-feedback path that resolves `panel_channel_id` + builds the idle embed + the panel-factory + calls the module orchestration) lives app-side — place it in `bot.py` or a small new app helper as the executor judges, calling the module's summon orchestration from Plan 01.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bot.py tests/test_golden_embeds.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def render_embed' weatherbot/interactive/bot.py` shows `render_embed` still defined app-side.
    - `grep -nE 'class BotThread|def build_client|def _handle_panel_summon' weatherbot/interactive/bot.py` returns NOTHING (all relocated).
    - `grep -n 'import PanelView' weatherbot/interactive/bot.py` returns NOTHING (the deferred edge is gone).
    - `tests/test_bot.py` passes, INCLUDING `test_render_embed_indicator_line` and `test_render_embed_indicator_suppressed_when_argless` (📍 on/off byte-identical).
    - `tests/test_golden_embeds.py` passes with ZERO golden diff (the embed fields + order are byte-identical).
  </acceptance_criteria>
  <done>render_embed stays app-side and renders 📍 byte-identically (suppressed when argless); BotThread/build_client/summon are deleted from bot.py; no deferred PanelView import survives; the embed + bot goldens are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Shrink panel.py to app cosmetic contributors; remove the render_embed top import (D-03, D-04)</name>
  <files>weatherbot/interactive/panel.py, weatherbot/interactive/__init__.py</files>
  <read_first>
    - weatherbot/interactive/panel.py L53-57 (the imports incl. the module-top `from weatherbot.interactive.bot import render_embed` at L54 to REMOVE), L90-129 (`_LOCATION_CMDS`, `_FORECAST_CMDS`, `_EMOJI`, `CmdButton` labels/emoji), L141 (`_PANEL_MARKER = "wb:"`), L180-279 (`CmdButton`, `ForecastButton` `wb:fc:…`, `LocationSelect` `wb:loc:select` — the app cosmetic components to KEEP), L282-764 (`PanelView` machinery + `_is_owned_panel` + `_render_view`/`_clone_child` — REMOVE, relocated to the module), L488 (`self._selected_location = value` — becomes `ctx.set(value)`), L512 (`arg = self._selected_location if spec.takes_location else None`), L520/590/680 (the per-tap `holder.current()` reads)
    - yahir_reusable_bot/discord/panelkit.py (from Plan 01 — the `ItemContributor = Callable[[SelectedContext], list[discord.ui.Item]]` shape the app must satisfy + the constructor params)
    - yahir_reusable_bot/discord/selection.py (SelectedContext — the app sets via `.set()`, reads via `.value`)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md (Pattern 1 — contributor callables; Pattern 4 — the wb: literals legitimately stay app-side; Open Question 1 — the child-order contract)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the panel.py MODIFIED section)
  </read_first>
  <action>
    In `weatherbot/interactive/panel.py`:

    REMOVE the module-top `from weatherbot.interactive.bot import render_embed` (L54) — `render` is injected at the composition root, not imported here. This kills the `panel→bot` forward edge (SC#2).

    KEEP the app cosmetic components: `LocationSelect` (`custom_id="wb:loc:select"`), the four `ForecastButton`s (`custom_id="wb:fc:weekday:detailed"` etc. — these full `wb:` literals legitimately stay app-side per D-04), the `_EMOJI`/label tables, `_LOCATION_CMDS`/`_FORECAST_CMDS`. Export an app-side marker constant `PANEL_MARKER = "wb:"` (replacing `_PANEL_MARKER`) — the app passes it to `PanelKit`.

    ADD contributor builder functions matching the module's `ItemContributor = Callable[[SelectedContext], list[discord.ui.Item]]` shape (Pattern 1a): each returns FRESH callback-bearing items per call (so the module's clone path re-invokes them safely). Build:
    - a Select contributor returning `LocationSelect` (row 0), reading the live `holder.current().locations` for options + marking `default` from `ctx.value`;
    - a forecast-grid contributor returning the four `ForecastButton`s (rows 3-4).
    The component callbacks set/read the injected `SelectedContext` — `LocationSelect.callback` calls `ctx.set(self.values[0])` (replacing `self._selected_location = value`, L488); button callbacks read `ctx.value`, NEVER re-read `Select.values` (Pitfall-3, panel.py:570). Keep the per-tap `holder.current()` reload reads (L520/590/680).

    RE-ADD the app-side per-spec consumes-context datum (the generalized `spec.takes_location`/`meta`, Phase-26 D-02) the composition root threads so the module sets `ctx.value` to `None` for argless commands (drives byte-identical 📍 suppression).

    REMOVE the `PanelView` class + `_is_owned_panel` + `_render_view`/`_clone_child` + the operator gate + envelope (all relocated to the module in Plan 01). `panel.py` shrinks to: the cosmetic components, the `wb:` literals, the contributor builders, the marker constant, and the consumes-context datum.

    Update `weatherbot/interactive/__init__.py` if it re-exports `PanelView`/`BotThread` — re-point to the module (`from yahir_reusable_bot.discord import BotThread`) or to the app contributors, keeping the daemon's `from weatherbot.interactive import BotThread, DaemonState` (daemon.py:1481) resolving (re-export `BotThread` from the module through the app `__init__` shim if needed, the Phase-22 re-export-shim pattern).
  </action>
  <verify>
    <automated>uv run pytest tests/test_panel.py tests/test_golden_custom_ids.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'from weatherbot.interactive.bot import render_embed' weatherbot/interactive/panel.py` returns NOTHING (the forward edge is gone).
    - `grep -nE 'class PanelView|def _is_owned_panel|def _render_view|def _clone_child' weatherbot/interactive/panel.py` returns NOTHING (relocated to the module).
    - `grep -n 'wb:loc:select' weatherbot/interactive/panel.py` still shows the app `LocationSelect` literal (the app cosmetic custom_ids stay app-side).
    - `tests/test_panel.py` passes (operator gate, per-callback isolation, clone-render polish — driven through the module PanelKit + app contributors).
    - `tests/test_golden_custom_ids.py` passes with ZERO diff — the assembled child order (Select row 0, cmd rows 1-2, grid rows 3-4) is byte-identical.
  </acceptance_criteria>
  <done>panel.py shrinks to app contributors + wb: literals + the marker constant; the render_embed top import and the PanelView machinery are gone; the custom_id byte snapshot + panel tests are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the module BotThread + PanelKit at the composition root (D-01, D-04, D-06)</name>
  <files>weatherbot/scheduler/wiring.py, weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py L1377-1389 (the `build_runtime(...)` call), L1471-1514 (the existing `BotThread(...)` construction with `holder`/`operator_id`/`cache`/`daemon_state`), L1575-1577 (the `finally` BotThread stop+join)
    - weatherbot/scheduler/wiring.py L20-23 (the leak-points-1+4 comment — selected-location + render_embed threaded app-side), L107-162 (`build_runtime` signature + the lazy discord import keeping discord.py off the import-time graph at L158-162)
    - yahir_reusable_bot/discord/__init__.py (Plan 01 — `BotThread`, `build_client`, `PanelKit`, `SelectedContext`)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md (the cycle resolution before/after code example; the positive injection assertion shape)
    - .planning/phases/25-lifecycle-ready-gate-composition-root/25-CONTEXT.md (D-04 — the single app-side composition root; D-05 — the positive injection assertion pattern)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the wiring.py/daemon.py MODIFIED section)
  </read_first>
  <action>
    Rewire the single composition root (the `daemon.py:1508` `BotThread(...)` site, and/or `wiring.py build_runtime` per Phase-25 D-04) to construct the MODULE adapter from `yahir_reusable_bot.discord`:

    Import `from yahir_reusable_bot.discord import BotThread, PanelKit, SelectedContext` (keep it behind the existing lazy/local-import discipline that keeps discord.py off the import-time graph — wiring.py L158-162). Import the app pieces: `from weatherbot.interactive.bot import render_embed`, and the app contributor builders + `PANEL_MARKER` from `weatherbot.interactive.panel`.

    Construct the `PanelKit` (or a `panelkit_factory` closure for `setup_hook`) injecting: `registry=` the app's thin registry singleton (Phase-26), `command_names=` the curated ordered names, `marker=PANEL_MARKER` (`"wb:"`), `operator_id=config.bot.operator_id` (baked at construction — preserve v1), `selection=SelectedContext(default_location)` (the `[str]` selected location), `contributors=` the app cosmetic-contributor list (LocationSelect + forecast grid), `render=render_embed`, plus the holder/accessor for the per-tap reads. Construct the module `BotThread` with the token + the panelkit/factory + `operator_id`, wired so `setup_hook` calls `client.add_view(panelkit)` with NO `weatherbot` import inside the module.

    Thread the `holder.current().bot.panel_channel_id` read app-side into the thin summon path (Plan 02 Task 1). Preserve the start-after-READY ordering (daemon L1471) and the `finally` stop+join (daemon L1575).

    This is the ONE greppable injection site (APP-01/APP-02) — `render`/`contributors`/`marker` are wired HERE, never baked into the module.
  </action>
  <verify>
    <automated>uv run pytest tests/test_panel.py tests/test_bot.py tests/test_import_hygiene.py tests/test_injection_registry.py -q && uv run weatherbot --help >/dev/null && echo CLI-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'from yahir_reusable_bot.discord import' weatherbot/scheduler/wiring.py weatherbot/scheduler/daemon.py` shows the module adapter imported at the composition root.
    - The construction site passes `render=render_embed`, `marker=PANEL_MARKER` (or `"wb:"`), `contributors=<app list>`, and `operator_id=config.bot.operator_id` to `PanelKit`.
    - `uv run weatherbot --help` exits 0 (no import cycle, no circular-import error).
    - `tests/test_import_hygiene.py` + `tests/test_injection_registry.py` pass.
    - The full suite `uv run pytest -q` passes with ZERO golden diff (run as the wave-merge gate).
  </acceptance_criteria>
  <done>The composition root constructs the module BotThread + PanelKit with render/contributors/marker/operator_id injected; the CLI loads with no cycle; the full suite + goldens are byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composition root → module adapter | App injects render/contributors/marker/operator_id; the module must receive (never bake) them |
| App contributor callbacks → SelectedContext | The selected-item state is set/read across the generic seam |
| panel_channel_id read → module summon | A non-secret id crosses into the module orchestration (never the token/appid) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | Tampering | injection at the composition root | mitigate | `render`/`contributors`/`marker` are wired ONLY at `build_runtime`/the daemon root and are required (no-default) module params — the positive injection assertion (Plan 03) proves they are app-supplied, not baked. |
| T-27-07 | Information Disclosure | what the root threads into the module | mitigate | Only `operator_id` (generic int), `panel_channel_id` (non-secret id), the marker, and opaque callables cross into the module. The bot token / OpenWeather appid are NEVER threaded into PanelKit/views (preserved from v1). |
| T-27-08 | Tampering | 📍 suppression drift across the render cut | mitigate | The argless 📍 suppression is preserved by threading the app per-spec consumes-context datum so `ctx.value` is `None` for argless commands — pinned byte-identically by the embed golden + `test_render_embed_indicator_suppressed_when_argless`. |
| T-27-SC | Tampering | uv/pip installs | accept | No package install in this plan (the pin landed in Plan 01); no new dependency enters the tree. |
</threat_model>

<verification>
- `uv run pytest -q` — the full ~732-test suite passes (BHV-01), ZERO golden diff (BHV-02).
- `uv run pytest tests/test_golden_custom_ids.py tests/test_golden_embeds.py tests/test_panel.py tests/test_bot.py -q` — the byte-identical oracle is unchanged.
- `grep -rn 'import render_embed\|import PanelView' weatherbot/` returns NOTHING (both cycle edges gone — SC#2).
- `uv run weatherbot --help` exits 0 (no import cycle).
</verification>

<success_criteria>
- `render_embed` stays app-side and is injected; both deferred/top cycle imports are gone (SC#2).
- `panel.py` shrinks to contributors; the module PanelKit drives the panel.
- The composition root injects render/contributors/marker/operator_id.
- The full suite + Phase-21 goldens + custom_id byte snapshot + embed goldens are byte-identical.
</success_criteria>

<output>
Create `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-02-SUMMARY.md` when done.
</output>
