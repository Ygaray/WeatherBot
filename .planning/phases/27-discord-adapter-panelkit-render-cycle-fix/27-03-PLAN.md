---
phase: 27-discord-adapter-panelkit-render-cycle-fix
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - tests/test_import_hygiene.py
  - tests/test_injection_registry.py
  - tests/test_panelkit_marker.py
autonomous: true
requirements: [SEAM-07]

must_haves:
  truths:
    - "The litmus tree-coverage assertion requires the new discord/ adapter files, so a future relocation cannot silently drop the adapter from litmus coverage"
    - "A core<->adapter import-isolation check proves no discord/ module imports weatherbot.* and that bot.py/panel.py contain no deferred render_embed/PanelView import (SC#2)"
    - "The positive injection assertion proves render + contributors + marker are required no-default PanelKit params wired at the composition root, and the module source carries no wb: literal"
    - "A generic module marker-parameterization test proves PanelKit(marker='X:') yields X:cmd:<name> ids and the module bakes no wb: (SC#3)"
    - "The full suite + the Phase-21 panel/clone-render goldens + the custom_id byte snapshot + the embed goldens are green and byte-identical, and a Gate-1 self-UAT log records the evidence"
  artifacts:
    - path: "tests/test_panelkit_marker.py"
      provides: "Generic module test: PanelKit(marker='X:') produces X:cmd:<name> + no wb: literal in module"
      contains: "def test_"
    - path: "tests/test_import_hygiene.py"
      provides: "Extended: discord/ tree-coverage guard + core<->adapter isolation + no-deferred-import grep"
      contains: "discord"
    - path: "tests/test_injection_registry.py"
      provides: "Extended: PanelKit render/contributors/marker positive injection assertion"
      contains: "PanelKit"
  key_links:
    - from: "tests/test_import_hygiene.py"
      to: "yahir_reusable_bot/discord"
      via: "scanned-tree assertion + grimp module-to-app edge check covers the adapter"
      pattern: "discord"
    - from: "tests/test_injection_registry.py"
      to: "yahir_reusable_bot/discord/panelkit.py"
      via: "required-params + no-wb-literal assertion over PanelKit"
      pattern: "PanelKit"
---

<objective>
Extend the standing test gates to the new adapter, prove SC#2 (no surviving cycle import) and SC#3 (marker parameterized + frozen custom_ids), and discharge the Gate-1 self-UAT. This plan adds NO new framework — it extends `test_import_hygiene.py` + `test_injection_registry.py` and adds one small generic module marker test, then runs the full byte-identical oracle and writes the self-UAT log.

Purpose: SEAM-07 acceptance — PKG-01 (core↔adapter isolation), APP-02 (positive injection), BHV-01/BHV-02 (suite + goldens byte-identical), the SC#2 no-deferred-import proof, the SC#3 marker-parameterization proof.
Output: extended import-hygiene + injection tests, the new marker test, a green full suite with zero golden diff, the Gate-1 self-UAT log.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-CONTEXT.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-VALIDATION.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-01-SUMMARY.md
@.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW test functions this PLAN introduces:
- `tests/test_import_hygiene.py` :: extended `test_litmus_clean` tree-coverage to require `discord/` files; a new core↔adapter isolation test (no `yahir_reusable_bot/discord/**` imports `weatherbot.*`); a new no-deferred-import grep test (`weatherbot/interactive/{bot,panel}.py` contain no `import PanelView` / `import render_embed`)
- `tests/test_injection_registry.py` :: `test_panel_cosmetics_and_render_and_marker_are_app_supplied` (PanelKit positive injection); UPDATED Phase-27 leak-point-1/4 stubs (L225-304) to reflect the relocation (SelectedContext is the generic seam, render injected)
- `tests/test_panelkit_marker.py` (NEW) :: `test_panelkit_marker_parameterized` — `PanelKit(marker="X:")` yields `X:cmd:<name>` ids + no `wb:` literal in the module source (D-04, SC#3)
- `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-SELF-UAT.md` :: the Gate-1 self-UAT log
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend import-hygiene — discord/ tree coverage + core<->adapter isolation + no-deferred-import (PKG-01, SC#2)</name>
  <files>tests/test_import_hygiene.py</files>
  <read_first>
    - tests/test_import_hygiene.py L32 (grimp counts TYPE_CHECKING edges — keep that default), L361-402 (`test_litmus_clean` — the negative grep over `yahir_reusable_bot/**`, the D-13-locked term set, the lifecycle + registry coverage-gap guards at L379-395 to mirror)
    - yahir_reusable_bot/discord/ (the new package from Plan 01 — `panelkit.py`, `gateway.py`, `selection.py`)
    - weatherbot/interactive/bot.py + panel.py (post-Plan-02 — to confirm no `import PanelView`/`import render_embed` deferred edge survives)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md (Validation Architecture → New gates; Pitfall 3 — surviving import edge)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the tree-coverage guard snippet)
  </read_first>
  <action>
    In `tests/test_import_hygiene.py`:

    EXTEND `test_litmus_clean`'s tree-coverage assertion to require the `discord/` adapter files in the scanned tree — mirror the lifecycle/registry coverage-gap guards (L379-395): assert `{"panelkit.py", "gateway.py", "selection.py"} <= {p.name for p in (MODULE_ROOT/"discord").rglob("*.py")}`, so a future relocation cannot silently drop the adapter from litmus coverage. Keep the D-13-locked term set EXACTLY (`weather|forecast|location|openweather|\buv\b|briefing`) — do NOT broaden.

    ADD the core↔adapter import-isolation check: assert NO module under `yahir_reusable_bot/discord/**` imports `weatherbot.*` (the existing grimp gate's `startswith(MODULE)` / `walk_packages` scan auto-covers this — add an explicit assertion naming the `discord` package so the intent is pinned).

    ADD the no-deferred-import proof (SC#2): read `weatherbot/interactive/bot.py` and `panel.py` source and assert NEITHER contains `import PanelView` NOR `import render_embed` (the two cycle edges). Use a grep/AST property over the source text. This is the explicit SC#2 proof the CONTEXT calls the "core↔adapter import-isolation check."

    Reference the literal import strings by concept where a negative grep would otherwise self-invalidate — if you must inline the forbidden string for the assertion, add `<!-- planner-discipline-allow -->` is NOT applicable to a test file, so instead build the forbidden token from parts (e.g. join `"import"` + the symbol name) so the test's own source does not trip a future grep over tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_import_hygiene.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_litmus_clean` (or its tree-coverage helper) asserts `panelkit.py`, `gateway.py`, `selection.py` are in the scanned `discord/` tree.
    - A test asserts NO `yahir_reusable_bot/discord/**` module imports `weatherbot.*` (grimp graph has no such edge).
    - A test asserts `weatherbot/interactive/bot.py` and `panel.py` contain neither the deferred `PanelView` import nor the `render_embed` import (SC#2 proof) and FAILS if either edge is reintroduced.
    - `uv run pytest tests/test_import_hygiene.py -q` passes.
    - The D-13 litmus term set is UNCHANGED (`grep -n 'weather|forecast|location|openweather' tests/test_import_hygiene.py` shows the same locked set).
  </acceptance_criteria>
  <done>The litmus tree-coverage requires the adapter files, the core↔adapter isolation is asserted, and the no-deferred-import SC#2 proof fails if either cycle edge returns.</done>
</task>

<task type="auto">
  <name>Task 2: PanelKit positive injection assertion + marker-parameterization test (APP-02, SC#3, D-04)</name>
  <files>tests/test_injection_registry.py, tests/test_panelkit_marker.py</files>
  <read_first>
    - tests/test_injection_registry.py L143-224 (the `_required_params_without_default` / `_module_public_symbols` helpers), L225-304 (the Phase-27 leak-point-1/4 stubs — `test_selected_location_context_originates_app_side`, `test_render_embed_is_app_side_module_owns_no_render` — which ASSUME `_selected_location` + `render_embed` stay in panel.py; these need updating now that the panel relocated and selection is `SelectedContext`)
    - yahir_reusable_bot/discord/panelkit.py (Plan 01 — the required `render`/`contributors`/`marker` params; the `CmdButton` marker-injected custom_id builder)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-RESEARCH.md (the positive injection assertion code example; Pattern 4 — the generic module marker test)
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-PATTERNS.md (the litmus + positive-injection assertion snippet)
  </read_first>
  <action>
    In `tests/test_injection_registry.py`:

    ADD `test_panel_cosmetics_and_render_and_marker_are_app_supplied` (RESEARCH code example): assert `{"render", "contributors", "marker"} <= _required_params_without_default(PanelKit.__init__)`; assert the module source `(MODULE_ROOT/"discord"/"panelkit.py").read_text()` contains no `wb:` literal; assert the composition root (`build_runtime`/daemon construction) wires `render`/`contributors`/`marker` (greppable at the single root). Include a self-proof (a baked-default stub would NOT have these as required params) mirroring the existing stubs' self-proof style.

    UPDATE the Phase-27 leak-point stubs (L225-304) to reflect the landed relocation: `test_selected_location_context_originates_app_side` should now assert the generic seam is `SelectedContext` (module owns the generic holder, app owns the `[str]` instantiation + the cosmetic Select that sets it) — keep the positive direction (no `location`-named MODULE symbol) intact; adjust the panel.py assertion to match the shrunk panel (the app still owns the selection-setting cosmetic). `test_render_embed_is_app_side_module_owns_no_render` should still pass (render_embed stays in bot.py; module owns no cosmetics render). Do NOT weaken the litmus — only realign the stubs to the actual relocated shape.

    Create `tests/test_panelkit_marker.py` with `test_panelkit_marker_parameterized` (D-04, SC#3): construct a `PanelKit(marker="X:", ...)` with a minimal registry + trivial contributors and assert its command-button children carry `X:cmd:<name>` ids (the marker flows through); assert `"wb:" not in (MODULE_ROOT/"discord"/"panelkit.py").read_text()`. This proves the module bakes no `wb:` and the marker is a real parameter.
  </action>
  <verify>
    <automated>uv run pytest tests/test_injection_registry.py tests/test_panelkit_marker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_panel_cosmetics_and_render_and_marker_are_app_supplied` passes: `render`/`contributors`/`marker` are required PanelKit params and the module source has no `wb:` literal.
    - The updated leak-point-1/4 stubs pass against the relocated shape (no `location`-named module symbol; render_embed app-side).
    - `tests/test_panelkit_marker.py::test_panelkit_marker_parameterized` passes: `PanelKit(marker="X:")` yields `X:cmd:<name>` ids.
    - `uv run pytest tests/test_injection_registry.py tests/test_panelkit_marker.py -q` passes.
  </acceptance_criteria>
  <done>The positive injection assertion proves render/contributors/marker are app-supplied (no baked wb:), the leak-point stubs realign to the relocation, and the generic marker test proves PanelKit is marker-parameterized.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite byte-identical oracle + Gate-1 self-UAT log (BHV-01, BHV-02)</name>
  <files>.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-SELF-UAT.md</files>
  <read_first>
    - .planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-VALIDATION.md (the sampling-rate + the deferred live-gateway Gate-2 item)
    - tests/test_golden_custom_ids.py + tests/test_golden_embeds.py + tests/test_panel.py + tests/test_bot.py (the byte-identical oracle to re-run)
    - $HOME/.claude/CLAUDE.md (the Two-Gate UAT policy — Gate-1 is the autonomous self-UAT log; Gate-2 live restart is deferred to Phase 28)
  </read_first>
  <action>
    Run the full byte-identical oracle and record a Gate-1 self-UAT log.

    Run `uv run pytest -q` (the full ~732-test suite — BHV-01) and confirm ZERO failures. Run the goldens explicitly: `uv run pytest tests/test_golden_custom_ids.py tests/test_golden_embeds.py tests/test_panel.py tests/test_bot.py -q` and confirm ZERO golden diff (no `.ambr` change, no re-baseline). Note the known pytest snapshot-report quirk (the suite may PRINT "N snapshots failed" while exiting 0 — trust the exit code + the `.ambr` diff, per MEMORY pytest-snapshot-report-quirk). If any golden shows a NON-EMPTY diff, STOP and investigate — a byte-identical relocation must never re-baseline a golden (Phase-21 / D-06 discipline).

    Prove the cycle is gone: `grep -rn 'import render_embed\|import PanelView' weatherbot/` returns NOTHING; `uv run weatherbot --help` exits 0.

    Write `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-SELF-UAT.md` recording, per success criterion (SC#1–SC#4 + PKG-01/APP-02/BHV-01/BHV-02): what was tested, the exact command, the evidence (test counts, zero-diff confirmation, grep output), and PASS/FAIL/PARTIAL. Mark the live `yahir-mint` `systemctl restart` panel re-routing as the single deferred Gate-2 PARTIAL (the byte-frozen custom_id snapshot + the persistent-view registration tests are the Gate-1 stand-in; the live round-trip is the Phase-28 milestone obligation per VALIDATION.md).
  </action>
  <verify>
    <automated>uv run pytest -q && grep -rn 'import render_embed' weatherbot/ | grep -v '^Binary' | wc -l | grep -qx 0 && echo SUITE-GREEN-NO-CYCLE</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` passes (full suite green, BHV-01) with no new skips and no weakened assertions.
    - `tests/test_golden_custom_ids.py` + `tests/test_golden_embeds.py` + the panel/clone-render goldens show ZERO diff (no `.ambr` re-baseline) — BHV-02.
    - `grep -rn 'import render_embed\|import PanelView' weatherbot/` returns NOTHING (cycle gone — SC#2).
    - `uv run weatherbot --help` exits 0.
    - `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-SELF-UAT.md` exists and records PASS/FAIL/PARTIAL per criterion with exact commands + evidence; the live-restart panel re-routing is the single deferred Gate-2 PARTIAL.
  </acceptance_criteria>
  <done>The full suite is green, every golden is byte-identical (no re-baseline), the cycle is provably gone, the CLI loads, and the Gate-1 self-UAT log records the evidence with the live restart deferred to Phase 28.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test gates → module/app source | The litmus + injection + isolation gates are the property oracle over the relocated boundary |
| Golden oracle → observable outputs | The byte-identical snapshot is the tamper-detector for custom_id routing + embed copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09 | Tampering | a silently re-baselined golden | mitigate | The self-UAT (Task 3) forbids any non-empty golden diff; a re-baseline is a failure to investigate, never rubber-stamped (Phase-21 / D-06). The known snapshot-report quirk is handled by trusting exit code + `.ambr` diff. |
| T-27-10 | Tampering | a reintroduced cycle import escaping the gate | mitigate | The no-deferred-import grep/AST test (Task 1) fails if `import PanelView`/`import render_embed` returns; the grimp gate fails on any `discord/`→`weatherbot` edge. |
| T-27-11 | Repudiation | unverified completion claim | mitigate | The Gate-1 self-UAT log records exact commands + evidence per criterion; the deferred live-restart Gate-2 item is explicitly tracked as a Phase-28 milestone obligation. |
| T-27-SC | Tampering | uv/pip installs | accept | No package install in this plan; test-only changes. No new dependency enters the tree. |
</threat_model>

<verification>
- `uv run pytest -q` — full suite green (BHV-01).
- `uv run pytest tests/test_import_hygiene.py tests/test_injection_registry.py tests/test_panelkit_marker.py -q` — the new/extended gates pass.
- `uv run pytest tests/test_golden_custom_ids.py tests/test_golden_embeds.py tests/test_panel.py tests/test_bot.py -q` — zero golden diff (BHV-02).
- `grep -rn 'import render_embed\|import PanelView' weatherbot/` — empty (SC#2).
- `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-SELF-UAT.md` — Gate-1 log present.
</verification>

<success_criteria>
- The litmus tree-coverage requires the adapter files; core↔adapter isolation + no-deferred-import are asserted (PKG-01, SC#2).
- The positive injection assertion + the marker-parameterization test pass (APP-02, SC#3, D-04).
- The full suite + every Phase-21 golden + the custom_id byte snapshot + the embed goldens are byte-identical (BHV-01/BHV-02).
- The Gate-1 self-UAT log records the evidence; the live restart is the single deferred Gate-2 item.
</success_criteria>

<output>
Create `.planning/phases/27-discord-adapter-panelkit-render-cycle-fix/27-03-SUMMARY.md` when done.
</output>
