---
phase: 03-always-on-scheduler
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/scheduler/context.py
  - templates/renderer.py
  - weatherbot/cli.py
  - templates/briefing-sectioned.txt
  - templates/briefing-multiline.txt
  - templates/briefing-compact.txt
  - tests/test_renderer.py
  - tests/test_send_now.py
autonomous: true
requirements: [SCHD-04]
user_setup: []

must_haves:
  truths:
    - "Every message (scheduled and manual --send-now) renders {sent_at} and {checked_at} in the location's local time"
    - "{schedule_note} is empty on on-time and manual sends and collapses cleanly like {hint}/{alert}"
    - "validate_template accepts {sent_at}/{checked_at}/{schedule_note} and still rejects an unknown token"
    - "A within-grace late send renders a populated {schedule_note} (intended-for / sent wording)"
    - "Manual --send-now (no ScheduleContext) still renders {sent_at}/{checked_at} non-empty with empty {schedule_note}"
  artifacts:
    - path: "weatherbot/scheduler/context.py"
      provides: "ScheduleContext dataclass + schedule_placeholders() merge helper"
      contains: "class ScheduleContext"
    - path: "templates/renderer.py"
      provides: "CANONICAL extended with the 3 new placeholders"
      contains: "schedule_note"
    - path: "weatherbot/cli.py"
      provides: "send_now threads schedule_ctx and merges schedule placeholders at the render call"
      contains: "schedule_ctx"
  key_links:
    - from: "weatherbot/cli.py"
      to: "weatherbot/scheduler/context.py"
      via: "schedule_placeholders() merged into render() values"
      pattern: "schedule_placeholders"
    - from: "templates/renderer.py CANONICAL"
      to: "tests/test_renderer.py canonical assertion"
      via: "CANONICAL includes sent_at/checked_at/schedule_note"
      pattern: "schedule_note"
---

<objective>
Open the render boundary to scheduler-derived timing: add the `ScheduleContext` dataclass +
`schedule_placeholders()` merge helper (D-15), extend `CANONICAL` with
`{sent_at}`/`{checked_at}`/`{schedule_note}` (D-12), thread an optional
`schedule_ctx: ScheduleContext | None = None` through `send_now` so the three placeholders
render on EVERY message (D-13/D-14), and add the footer line to all three starter templates.
This is the seam the daemon (Plan 03) populates per fire; manual `--send-now` (no context)
still renders `{sent_at}`/`{checked_at}` with an empty `{schedule_note}`.

Purpose: D-12..D-15 give the user editable, location-local timing annotations and make late
sends transparent — the render half of the phase, independently testable without the daemon.
Output: `scheduler/context.py`; extended `CANONICAL`; `send_now` schedule_ctx seam; template
footers; updated renderer/send_now tests including the critical canonical-keys gotcha.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-always-on-scheduler/03-CONTEXT.md
@.planning/phases/03-always-on-scheduler/03-RESEARCH.md
@.planning/phases/03-always-on-scheduler/03-PATTERNS.md
</context>

<artifacts_produced>
This plan creates/extends these symbols (Plan 03 supplies a real `ScheduleContext` per fire):

- `weatherbot/scheduler/context.py`:
  - `@dataclass ScheduleContext` with `scheduled_dt: datetime | None`, `tz: ZoneInfo`, `late: bool = False`.
  - `schedule_placeholders(ctx, sent_dt, checked_dt) -> dict[str,str]` returning keys `sent_at`, `checked_at`, `schedule_note` (location-local formatted; `schedule_note` empty unless `ctx.late and ctx.scheduled_dt is not None`).
- `templates/renderer.py` — `CANONICAL` gains `"sent_at"`, `"checked_at"`, `"schedule_note"`.
- `weatherbot/cli.py` — `send_now(..., schedule_ctx: ScheduleContext | None = None)`; the single `render(...)` call merges `{**forecast.placeholders(), **schedule_placeholders(ctx, sent_dt, checked_dt)}`.
- `templates/briefing-sectioned.txt`, `briefing-multiline.txt`, `briefing-compact.txt` — footer line referencing the 3 placeholders (compact stays emoji-free).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScheduleContext + schedule_placeholders + CANONICAL extension (D-12/D-15)</name>
  <read_first>
    - 03-RESEARCH.md lines 350-377 (Pattern 5: ScheduleContext + schedule_placeholders, %-I:%M %p formatting, empty-note rule) and Open Question 2 lines 540-543 (merge-at-call-site is the recommended seam)
    - 03-PATTERNS.md "scheduler/context.py" section (DeliveryResult dataclass analog, `from __future__ import annotations`)
    - weatherbot/channels/base.py DeliveryResult dataclass (the value-object analog)
    - templates/renderer.py lines 36-66 (CANONICAL set + validate_template — reads CANONICAL by reference, engine unchanged)
    - weatherbot/weather/models.py lines 261-276 (placeholders() — STAYS weather-only; {hint}/{alert} empty-collapse precedent)
  </read_first>
  <behavior>
    - schedule_placeholders(None, sent_dt, checked_dt) returns sent_at/checked_at non-empty (formatted from sent_dt/checked_dt) and schedule_note == "".
    - schedule_placeholders(ScheduleContext(scheduled_dt=None, tz=ZoneInfo("America/New_York")), ...) → schedule_note == "" (manual: no scheduled time).
    - schedule_placeholders(ScheduleContext(scheduled_dt=<07:00 local>, tz=..., late=True), sent_dt=<07:30 local>, ...) → schedule_note contains "intended for 7:00 AM" and "sent 7:30 AM" (location-local).
    - All three rendered times use the ctx.tz (or the passed tz) — location-local (D-14).
    - validate_template("{sent_at} {checked_at} {schedule_note}") returns None (no raise); validate_template("{bogus}") still raises ValueError.
  </behavior>
  <action>Create `weatherbot/scheduler/context.py` with `from __future__ import annotations`, a `@dataclass ScheduleContext` (`scheduled_dt: datetime | None`, `tz: ZoneInfo`, `late: bool = False`) mirroring `DeliveryResult`, and `schedule_placeholders(ctx, sent_dt, checked_dt) -> dict[str, str]` that formats `sent_dt`/`checked_dt` in the location tz (use `%-I:%M %p` for "7:30 AM"; A2 — Linux is the target, acceptable per D-12 Claude's discretion) and returns `{"sent_at": ..., "checked_at": ..., "schedule_note": note}` where `note` is `""` unless `ctx is not None and ctx.late and ctx.scheduled_dt is not None`, in which case `note = f"(intended for {scheduled local}, sent {sent local})"`. Keep `Forecast.placeholders()` WEATHER-ONLY (do not add the 3 keys there — the recommended merge-at-call-site seam, Open Question 2). Add `"sent_at"`, `"checked_at"`, `"schedule_note"` to `CANONICAL` in `templates/renderer.py` (no change to `validate_template`/`render` logic — they read CANONICAL by reference). Add `test_new_placeholders_validate` to `tests/test_renderer.py` (asserts validate_template accepts the 3 keys and still rejects a bogus token) and a context unit test (manual → empty note; late → populated note) to `tests/test_scheduler.py`.</action>
  <verify>
    <automated>uv run pytest tests/test_renderer.py tests/test_scheduler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ScheduleContext" weatherbot/scheduler/context.py` matches; `grep -n "def schedule_placeholders" weatherbot/scheduler/context.py` matches.
    - `grep -c "schedule_note" templates/renderer.py` >= 1 and CANONICAL also contains `sent_at` and `checked_at`.
    - `uv run pytest tests/test_renderer.py::test_new_placeholders_validate -x` exits 0.
    - `grep -n '"sent_at"\|"checked_at"\|"schedule_note"' weatherbot/weather/models.py` returns NOTHING (placeholders() stays weather-only).
  </acceptance_criteria>
  <done>ScheduleContext + schedule_placeholders render the 3 location-local keys with the correct empty/late note behavior; CANONICAL validates them; placeholders() unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread schedule_ctx through send_now + update canonical-keys gotcha test (D-13/D-15)</name>
  <read_first>
    - weatherbot/cli.py lines 77-142 (send_now keyword-only signature + the single `render(template_text, forecast.placeholders())` call at line 130 — the seam to extend), lines 41-44 (TYPE_CHECKING block for the ScheduleContext import)
    - 03-RESEARCH.md Pitfall 4 lines 430-434 (manual send must not leak the note / crash on scheduled_dt None)
    - 03-PATTERNS.md "weatherbot/cli.py" render-boundary section + the CRITICAL gotcha note in 03-PATTERNS.md "weather/models.py" (lines re: test_canonical_matches_forecast_placeholder_keys)
    - tests/test_renderer.py lines 107-110 (test_canonical_matches_forecast_placeholder_keys — asserts CANONICAL == set(placeholders().keys()) EXACTLY; this BREAKS once the 3 keys are in CANONICAL but not in placeholders())
    - tests/test_send_now.py lines 21-60 (_FakeClient/_FakeChannel + the existing happy-path call shape to extend)
  </read_first>
  <behavior>
    - send_now(..., schedule_ctx=None) (manual) renders {sent_at}/{checked_at} non-empty and {schedule_note} empty — no "(intended for …)" leaks, no AttributeError on scheduled_dt.
    - send_now(..., schedule_ctx=ScheduleContext(scheduled_dt=<07:00>, tz=..., late=True)) renders a populated {schedule_note}.
    - The rendered body still contains the weather placeholders (imperial-primary) — the merge does not drop forecast keys.
    - test_canonical_matches_forecast_placeholder_keys now reflects the call-site seam: CANONICAL == set(placeholders().keys()) | {"sent_at","checked_at","schedule_note"}.
  </behavior>
  <action>Add `schedule_ctx: ScheduleContext | None = None` to the keyword-only params of `send_now` in `weatherbot/cli.py` (import `ScheduleContext`/`schedule_placeholders` under the existing `TYPE_CHECKING`/normal imports as appropriate — `schedule_placeholders` is needed at runtime). Replace the line-130 `render(template_text, forecast.placeholders())` with `render(template_text, {**forecast.placeholders(), **schedule_placeholders(schedule_ctx, sent_dt, checked_dt)})`, where `sent_dt = datetime.now(tz)` (the moment of delivery) and `checked_dt = datetime.now(tz)` computed locally at the render call. NOTE: `send_now`/`Forecast` does NOT currently expose the fetch instant — `Forecast.from_payloads` creates and discards its `now_utc` default and retains only `local_date` (a string) — so do NOT reference a fetch-timestamp variable that is not in scope; compute `checked_dt` locally. Per D-12 this is within seconds of the actual fetch (single-fetch-at-send, DATA-03) and is the accepted freshness proxy. (If exact fetch-time fidelity is ever wanted, first add a `fetched_at` datetime field to `Forecast` — explicitly out of scope for this phase.) When `schedule_ctx is None`, derive the tz for formatting from `location.timezone` so manual sends still render location-local times (D-14). Keep `validate_template(template_text)` firing before render (unchanged). UPDATE `tests/test_renderer.py::test_canonical_matches_forecast_placeholder_keys` to assert `CANONICAL == set(placeholders().keys()) | {"sent_at","checked_at","schedule_note"}` (the merge-at-call-site seam, per the PATTERNS gotcha #1). Add `test_manual_send_schedule_placeholders` to `tests/test_send_now.py` (manual send → {sent_at}/{checked_at} present, {schedule_note} empty) reusing `_FakeClient`/`_FakeChannel`.</action>
  <verify>
    <automated>uv run pytest tests/test_send_now.py tests/test_renderer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "schedule_ctx" weatherbot/cli.py` matches in the send_now signature and at the render call.
    - `grep -n "schedule_placeholders" weatherbot/cli.py` matches (runtime import + merge).
    - `uv run pytest tests/test_send_now.py::test_manual_send_schedule_placeholders -x` exits 0.
    - `uv run pytest tests/test_renderer.py::test_canonical_matches_forecast_placeholder_keys -x` exits 0 (assertion updated to the call-site seam).
    - `uv run pytest tests/test_send_now.py::test_send_now_posts_briefing -x` still exits 0 (no regression in the existing happy path).
  </acceptance_criteria>
  <done>send_now threads an optional ScheduleContext and merges the 3 placeholders at the render call; manual send renders timing with empty note; the canonical-keys gotcha test reflects the new seam and is green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the timing footer to all three starter templates (D-15)</name>
  <read_first>
    - templates/briefing-sectioned.txt (lines 8-10: {hint}/{alert} trailing lines), templates/briefing-multiline.txt (lines 10-11), templates/briefing-compact.txt (lines 1-3 — MUST stay emoji-free)
    - 03-RESEARCH.md lines 504-512 (footer example: "— sent {sent_at} · weather checked {checked_at}" then {schedule_note} on its own line)
    - 03-PATTERNS.md "templates/*.txt" section + gotcha #2 (test_compact_template_has_no_emoji `_EMOJI` regex `[\U0001F300-\U0001FAFF☀-➿]`; the `·` middot is fine, avoid the `—` em-dash range only if it trips the regex — `—` U+2014 is OUTSIDE the `☀-➿` range so it is safe)
    - tests/test_renderer.py lines 31-32 (the _EMOJI regex), lines 75-77 (compact-no-emoji test), lines 113-125 (template-substitutes-new-placeholders test the executor should extend to the 3 keys)
  </read_first>
  <action>Add a footer to all three templates referencing the new placeholders: a line `— sent {sent_at} · weather checked {checked_at}` followed by `{schedule_note}` on its own line (so it collapses to nothing on on-time/manual sends, like {hint}/{alert}). For `briefing-compact.txt`, keep the footer emoji-free (the `·` middot and `—` em-dash are both outside the `_EMOJI` range — verify against `[\U0001F300-\U0001FAFF☀-➿]`; if uncertain, use plain ASCII "- sent ... | weather checked ..."). Extend `tests/test_renderer.py::test_templates_substitute_new_placeholders` (or add a sibling) to assert `{sent_at}`/`{checked_at}`/`{schedule_note}` appear in each raw template and substitute to non-token output; confirm `test_compact_template_has_no_emoji` and `test_validate_template_passes_all_shipped_templates` still pass.</action>
  <verify>
    <automated>uv run pytest tests/test_renderer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "{sent_at}" templates/briefing-sectioned.txt templates/briefing-multiline.txt templates/briefing-compact.txt` shows each file contains it; same for `{checked_at}` and `{schedule_note}`.
    - `uv run pytest tests/test_renderer.py::test_compact_template_has_no_emoji -x` exits 0.
    - `uv run pytest tests/test_renderer.py::test_validate_template_passes_all_shipped_templates -x` exits 0.
  </acceptance_criteria>
  <done>All three starter templates carry the location-local timing footer; compact stays emoji-free; all shipped templates still validate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editable template (.txt) → renderer | User-edited template text with `{token}` placeholders |
| scheduler timing → render values | ScheduleContext (scheduled/sent/checked times) merged into the placeholder map |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | template injection via new placeholders | mitigate | The 3 new keys are plain strings added to the existing whitelist substitution (no `str.format`/`eval`, T-03-02); same guarded `_TOKEN` regex — no new injection surface |
| T-03-05 | Information disclosure | timing strings rendered into the briefing | accept | `{sent_at}`/`{checked_at}` are non-secret local times; no key/URL crosses the render boundary (T-04-01 unchanged) |
| T-03-06 | Tampering | manual-send note leakage / None crash | mitigate | `schedule_ctx` defaults None; `{schedule_note}` empty unless `ctx.late and scheduled_dt is not None`; test_manual_send_schedule_placeholders proves no leak (Pitfall 4) |
</threat_model>

<verification>
- `uv run pytest tests/test_renderer.py tests/test_send_now.py tests/test_scheduler.py -q` green.
- `uv run pytest -q` (full suite) green — the updated canonical-keys assertion and existing send_now happy path both pass.
</verification>

<success_criteria>
- D-12/D-13/D-14: `{sent_at}`/`{checked_at}` render in location-local time on every message; `{schedule_note}` is empty on manual/on-time, populated on late.
- D-15: the 3 keys validate via CANONICAL; manual `--send-now` renders timing with an empty note (no leak/crash); the canonical-keys gotcha test is updated to the call-site seam.
- SCHD-04 (display half): a recovered late send can render its intended-vs-actual local time, proving per-location-tz timing is threaded through render.
</success_criteria>

<output>
Create `.planning/phases/03-always-on-scheduler/03-02-SUMMARY.md` when done.
</output>
