---
phase: 03-always-on-scheduler
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - weatherbot/scheduler/catchup.py
  - weatherbot/scheduler/daemon.py
  - weatherbot/scheduler/__init__.py
  - weatherbot/cli.py
  - tests/test_scheduler.py
autonomous: true
requirements: [SCHD-05, SCHD-06, SCHD-03]
user_setup: []

must_haves:
  truths:
    - "weatherbot --run reads config, announces every ENABLED slot (location, time, days, computed next_run_time) to the structlog log, then blocks in the foreground until SIGTERM/Ctrl-C and shuts the scheduler down cleanly (D-09/D-10)"
    - "One CronTrigger job is registered per enabled (location, schedule entry) at the location's own IANA timezone, so a Home (America/New_York) slot and a Weekend slot in another zone each fire at their own local wall-clock time (success criterion #2, SCHD-04 firing realized)"
    - "plan_catchup is a pure function (config + was_sent reader + injected now_utc) returning the slots whose local scheduled time passed < 90 min ago today, are due (day-of-week matches), and are NOT already in sent_log; a slot >= 90 min late is skipped and logged (success criterion #4, D-04)"
    - "Across a simulated America/New_York spring-forward (2026-03-08) and fall-back (2026-11-01) morning send, the catch-up planner + (location, send_time, local_date) idempotency key yield exactly ONE fire (success criterion #3, SCHD-03 DST exactly-once)"
    - "fire_slot checks was_sent before delivering, calls send_now with a ScheduleContext, and calls record_sent ONLY after the channel confirms result.ok (D-07); running catch-up twice against the same sent_log fires the slot once then zero times (success criterion #5, restart replay prevented)"
    - "An exception raised inside one fire_slot is caught and logged and does NOT propagate out of the callback (minimal exception isolation so one bad slot cannot crash the scheduler; full retry-then-alert is Phase 4)"
    - "A recovered (late) send shows CURRENT weather, not stale data — fire_slot re-fetches live weather at send time via send_now, so a 7:00 slot recovered at 7:30 carries 7:30 weather; recovery is about timing relevance only, not data freshness (D-05)"
    - "Manual --send-now still works unchanged alongside the daemon"
  artifacts:
    - path: "weatherbot/scheduler/catchup.py"
      provides: "plan_catchup() pure missed-send planner + MissedSlot dataclass + _fires_on() day-of-week match (D-04/D-06/D-10)"
      contains: "def plan_catchup"
    - path: "weatherbot/scheduler/daemon.py"
      provides: "run_daemon() foreground lifecycle (announce + catch-up + register CronTrigger jobs + block/shutdown) and fire_slot() callback (D-09/D-10/D-11)"
      contains: "def run_daemon"
    - path: "weatherbot/cli.py"
      provides: "--run flag branch in main() dispatching to run_daemon()"
      contains: "run_daemon"
    - path: "tests/test_scheduler.py"
      provides: "catch-up window, DST exactly-once, idempotent double-fire/restart-replay, per-tz registration, disabled-slot, and fire_slot firing tests"
      contains: "def test_catchup_window"
  key_links:
    - from: "weatherbot/scheduler/daemon.py fire_slot"
      to: "weatherbot/cli.py send_now"
      via: "send_now(..., schedule_ctx=ScheduleContext(...)) call per fire"
      pattern: "send_now\\("
    - from: "weatherbot/scheduler/daemon.py fire_slot"
      to: "weatherbot/weather/store.py was_sent/record_sent"
      via: "check-before-fire then record-after-success (D-07)"
      pattern: "record_sent"
    - from: "weatherbot/scheduler/daemon.py run_daemon"
      to: "apscheduler CronTrigger"
      via: "one job per enabled slot at timezone=loc.timezone"
      pattern: "CronTrigger"
    - from: "weatherbot/scheduler/catchup.py plan_catchup"
      to: "weatherbot/weather/store.py was_sent"
      via: "injected was_sent reader skips already-sent slots"
      pattern: "was_sent"
---

<objective>
Build the daemon spine that turns the validated building blocks (Schedule model + sent_log
from Plan 01, ScheduleContext from Plan 02) into a working always-on process. Add the
`weatherbot/scheduler/catchup.py` pure missed-send planner (D-04/D-06/D-10), the
`weatherbot/scheduler/daemon.py` `run_daemon()` foreground lifecycle + `fire_slot()` callback
(D-09/D-10/D-11), and the `weatherbot --run` CLI branch (D-09). This plan realizes the
ACTUAL scheduled firing: one APScheduler `CronTrigger` job per enabled `(location, schedule
entry)` at the location's own IANA timezone, the 90-minute startup catch-up scan, DST
exactly-once firing via the `(location, send_time, local_date)` idempotency key, and clean
SIGTERM/Ctrl-C shutdown. Manual `--send-now` keeps working unchanged.

Purpose: SCHD-05 (in-process scheduler computing next run per location tz) and SCHD-06
(missed-send recovery within the 90-min grace) are the uncovered requirements; SCHD-03's DST
exactly-once behavior is realized here by the live `CronTrigger` + per-day key. These complete
the phase — SCHD-01..07 are then all satisfied across Plans 01/02/03.
Output: `scheduler/catchup.py`; `scheduler/daemon.py`; `--run` branch in `cli.py`; the
scheduler-package barrel export; firing/catch-up/DST/idempotency tests in `test_scheduler.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-always-on-scheduler/03-CONTEXT.md
@.planning/phases/03-always-on-scheduler/03-RESEARCH.md
@.planning/phases/03-always-on-scheduler/03-PATTERNS.md
@.planning/phases/03-always-on-scheduler/03-01-PLAN.md
@.planning/phases/03-always-on-scheduler/03-02-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03 portion)

This plan creates/extends these symbols (the daemon spine; consumes Plan 01/02 symbols):

- `weatherbot/scheduler/catchup.py` (NEW):
  - `GRACE = timedelta(minutes=90)` — D-04 hardcoded grace window (module constant, NOT config).
  - `@dataclass MissedSlot` with `location`, `slot`, `scheduled_dt: datetime` (tz-aware, location zone), `local_date: str` (YYYY-MM-DD dedup-key date component).
  - `_fires_on(slot, now_local) -> bool` — day-of-week match driven from the normalized `slot.day_of_week` string (Monday-first, `date.weekday()` Mon=0) so the planner agrees with the live `CronTrigger` (Pitfall 3).
  - `plan_catchup(config, was_sent, now_utc=None) -> list[MissedSlot]` — PURE: inject `was_sent` reader + `now_utc` (default `datetime.now(timezone.utc)` per the `from_payloads` clock-injection idiom).
- `weatherbot/scheduler/daemon.py` (NEW):
  - `fire_slot(location, slot, *, config, db_path, settings=None, client=None, channel=None, scheduled_dt=None, late=False) -> DeliveryResult | None` — checks `was_sent`; builds a `ScheduleContext`; calls `send_now(...)`; on `result.ok` calls `record_sent`; wraps the body in try/except that logs and returns `None` on failure (minimal isolation, Phase 4 hardens).
  - `run_daemon(config, settings, db_path, *, client=None, channel=None) -> int` — registers jobs (Pattern 1), announces the schedule (D-10), runs the catch-up scan (Pattern 3), `scheduler.start()`, blocks on a `threading.Event` with a SIGTERM handler + `KeyboardInterrupt`, `scheduler.shutdown(wait=False)` on exit.
  - `_register_jobs`, `_announce_schedule`, `_run_catchup` private helpers.
- `weatherbot/scheduler/__init__.py` — extend the Plan 01 barrel to also export `run_daemon`, `plan_catchup`, `MissedSlot` (additive; do NOT remove Plan 01's `parse_days` export).
- `weatherbot/cli.py` — `--run` `store_true` flag + dispatch branch in `main()` calling `run_daemon(config=..., settings=..., db_path=...)`.
- `tests/test_scheduler.py` — extend (Plan 01 created the file) with the daemon/catch-up/DST/idempotency tests.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure catch-up planner — plan_catchup + MissedSlot + _fires_on (SCHD-06/SCHD-03, D-04/D-06/D-10)</name>
  <read_first>
    - 03-RESEARCH.md lines 270-311 (Pattern 3: plan_catchup signature, GRACE=90min, the due/<90min/not-sent filter chain, MissedSlot shape, the `_fires_on` Monday-first note)
    - 03-RESEARCH.md lines 412-428 (Pitfall 1 DST band reasoning, Pitfall 3 day-of-week Monday-first `date.weekday()` Mon=0 vs trigger agreement)
    - 03-RESEARCH.md lines 580-589 (DST test strategy: pass a now_utc straddling 2026-03-08 spring-forward / 2026-11-01 fall-back; no wall-clock waits)
    - 03-PATTERNS.md "scheduler/catchup.py" section (Forecast.from_payloads clock-injection analog lines 86-94; _local_date_iso tz-local-date math lines 98-112; reuse, don't add a 3rd copy)
    - weatherbot/weather/models.py Forecast.from_payloads (the `now_utc: datetime | None = None` → `if now_utc is None: now_utc = datetime.now(timezone.utc)` idiom to mirror)
    - weatherbot/weather/store.py lines 122-136 (_local_date_iso ZoneInfo-with-UTC-fallback `.astimezone(tz).date().isoformat()`)
    - weatherbot/config/models.py Schedule.parsed_time()/day_of_week (created by Plan 01 — the accessors plan_catchup consumes for hour/minute and the normalized day_of_week string)
    - tests/test_scheduler.py (Plan 01 scaffold) + tests/conftest.py lines 26-33 (tmp_db) — extension targets
  </read_first>
  <behavior>
    - plan_catchup(config, was_sent=lambda *a: False, now_utc=<a weekday 07:30 local in America/New_York>) for a Home location with an enabled mon-fri 07:00 slot returns exactly one MissedSlot whose scheduled_dt is 07:00 local that date and local_date is that date's ISO string (due, 30 min late < 90, not sent).
    - The same call with now_utc = 08:31 local (> 90 min after 07:00) returns an EMPTY list (skipped — outside the grace window, D-04).
    - The same call with now_utc = 06:45 local (before 07:00) returns an EMPTY list (not due yet — the live job will fire it).
    - With was_sent returning True for (Home, "07:00", that local_date), the 07:30 call returns an EMPTY list (already delivered, D-06).
    - A disabled slot (enabled=False) is never returned (SCHD-02 at catch-up).
    - A mon-fri slot evaluated on a Saturday now_local returns EMPTY; a weekends (sat,sun) slot evaluated on Saturday and due returns one slot — _fires_on agrees with the normalized day_of_week across all 7 weekdays.
    - DST exactly-once: plan_catchup with now_utc just after America/New_York 2026-03-08 spring-forward (a morning 07:00 slot) and just after 2026-11-01 fall-back yields exactly one MissedSlot per transition (the 07:00 morning send is outside the 01:00-02:59 transition band; the per-day local_date key would collapse any theoretical duplicate).
  </behavior>
  <action>Create `weatherbot/scheduler/catchup.py` with `from __future__ import annotations`. Define `GRACE = timedelta(minutes=90)` as a module constant (D-04 — hardcoded, never read from config). Define `@dataclass MissedSlot` with fields `location`, `slot`, `scheduled_dt: datetime`, `local_date: str`. Define `_fires_on(slot, now_local) -> bool` that maps the normalized `slot.day_of_week` string (e.g. "mon-fri", "sat,sun", "mon-sun") to a set of `date.weekday()` indices (Monday-first, Mon=0..Sun=6) and returns whether `now_local.weekday()` is in that set — expand ranges (`a-b`) and comma lists using a `_WEEKDAY_INDEX = {"mon":0,...,"sun":6}` map so it consumes the SAME normalized form the CronTrigger gets (Pitfall 3). Define `plan_catchup(config, was_sent, now_utc=None) -> list[MissedSlot]` mirroring the `from_payloads` clock-injection idiom (`if now_utc is None: now_utc = datetime.now(timezone.utc)`); for each location build `tz = ZoneInfo(loc.timezone)`, `now_local = now_utc.astimezone(tz)`; for each ENABLED slot: skip if not `_fires_on(slot, now_local)`; compute `hh, mm = slot.parsed_time()` and `scheduled = now_local.replace(hour=hh, minute=mm, second=0, microsecond=0)`; skip if `scheduled > now_local` (not due yet); skip if `now_local - scheduled > GRACE` (>90 min late); compute `local_date = now_local.date().isoformat()`; skip if `was_sent(loc.name, slot.time, local_date)`; else append `MissedSlot(loc, slot, scheduled, local_date)`. Keep the module dependency-free of apscheduler (pure logic). Reuse the existing ZoneInfo-with-fallback math rather than introducing a third `_local_date_iso` copy (import or inline the same shape per PATTERNS). Extend `tests/test_scheduler.py` with `test_catchup_window` (the <90/≥90/before/already-sent matrix), `test_disabled_slot_not_fired` (SCHD-02 at catch-up), `test_days_match_agrees_across_week` (`_fires_on` vs the normalized day_of_week for each preset across all 7 weekdays, Pitfall 3), and `test_dst_exactly_once` (spring-forward + fall-back morning send → exactly one MissedSlot) — all using inline-built `Config`/`Location`/`Schedule` and a `was_sent` stub, no wall-clock waits.</action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def plan_catchup" weatherbot/scheduler/catchup.py` matches; `grep -n "GRACE = timedelta(minutes=90)" weatherbot/scheduler/catchup.py` matches; `grep -n "class MissedSlot" weatherbot/scheduler/catchup.py` matches.
    - `grep -c "import apscheduler\|from apscheduler" weatherbot/scheduler/catchup.py` is 0 (planner is pure, recovery NOT owned by APScheduler misfire).
    - `uv run pytest tests/test_scheduler.py::test_catchup_window -x` exits 0.
    - `uv run pytest tests/test_scheduler.py::test_dst_exactly_once -x` exits 0.
    - `uv run pytest tests/test_scheduler.py::test_days_match_agrees_across_week -x` exits 0.
  </acceptance_criteria>
  <done>plan_catchup is a pure, clock-injected planner returning exactly the due, within-90-min, not-yet-sent slots; _fires_on agrees with the normalized day_of_week across all 7 days; DST morning sends yield exactly one slot; disabled slots excluded; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Daemon spine — run_daemon lifecycle + fire_slot callback (SCHD-05, D-07/D-09/D-10/D-11)</name>
  <read_first>
    - 03-RESEARCH.md lines 207-236 (Pattern 1: config-as-jobs, CronTrigger(hour, minute, day_of_week, timezone=loc.timezone), misfire_grace_time=None, coalesce=True, id=f"{loc.name}|{slot.time}|{slot.days}")
    - 03-RESEARCH.md lines 444-470 (Code Example: run_daemon foreground lifecycle — _register_jobs/_announce_schedule/_run_catchup order, scheduler.start(), threading.Event + signal.SIGTERM handler + KeyboardInterrupt, scheduler.shutdown(wait=False))
    - 03-RESEARCH.md lines 379-384 (Anti-Patterns: don't trust APScheduler misfire across restart; mark-after-success only; per-job try/except so one bad fire doesn't kill the scheduler thread)
    - 03-PATTERNS.md "scheduler/daemon.py" section (cli.send_now/do_check orchestration + outcome-only logging analog lines 134-150; injectable client/channel lines 148; the BackgroundScheduler+signal lifecycle has NO repo analog — use the RESEARCH Code Example)
    - 03-PATTERNS.md "Outcome-only structured logging" + "Injectable collaborators for the composition root" shared-pattern sections
    - weatherbot/cli.py lines 77-142 (send_now signature: keyword-only, client/channel injection, the schedule_ctx param added by Plan 02) and lines 137-142 (the `_log.info("send_now complete", location=..., delivered=...)` outcome-only style; `_log = structlog.get_logger(__name__)`)
    - weatherbot/scheduler/context.py (Plan 02 — ScheduleContext(scheduled_dt, tz, late) the value object fire_slot builds)
    - weatherbot/weather/store.py was_sent/record_sent (Plan 01 — the check-before-fire / record-after-success seam)
    - tests/test_send_now.py lines 21-47 (_FakeClient/_FakeChannel — copy verbatim to drive fire_slot without network) and tests/conftest.py lines 26-33 (tmp_db)
  </read_first>
  <behavior>
    - fire_slot(location, slot, config=cfg, db_path=tmp_db, client=_FakeClient(...), channel=_FakeChannel(...), scheduled_dt=<07:00 local>, late=True) renders via send_now (the fake channel captures one sent_text) and calls record_sent so was_sent(location.name, slot.time, local_date) is True afterward.
    - Calling fire_slot a SECOND time for the same (location, slot, local_date) sees was_sent True and SKIPS — the fake channel captures NO additional send (record-after-success + check-before-fire give restart-replay safety, success criterion #5).
    - fire_slot threads a ScheduleContext(scheduled_dt=<07:00 local>, tz=<location tz>, late=True) so the recovered send renders a populated {schedule_note} (intended-vs-actual local time) — proving SCHD-04 timing end-to-end through render.
    - If the injected channel.send_briefing raises (or returns ok=False), record_sent is NOT called (the slot stays unsent for re-fire / Phase 4), and an exception inside fire_slot is caught + logged and does NOT propagate out of the callback (minimal isolation).
    - run_daemon(config, settings=None, db_path=tmp_db, client=_FakeClient, channel=_FakeChannel) registers exactly one job per ENABLED slot (disabled slots produce no job) with timezone=loc.timezone, and each job's next_run_time is a tz-aware datetime in the location's zone (announce reads it). [Drive this WITHOUT scheduler.start()+sleep — assert on scheduler.get_jobs() after _register_jobs, or run_daemon with an immediate-stop injected so it registers/announces/catches-up then shuts down.]
    - A Home (America/New_York) slot and a second location in a different IANA zone each register a job whose trigger timezone equals that location's configured zone (success criterion #2 — per-location wall-clock firing).
  </behavior>
  <action>Create `weatherbot/scheduler/daemon.py` with `from __future__ import annotations`, `import signal`, `import threading`, `from apscheduler.schedulers.background import BackgroundScheduler`, `from apscheduler.triggers.cron import CronTrigger`, `_log = structlog.get_logger(__name__)` (reuse the cli.py acquisition verbatim), and import `send_now` from `weatherbot.cli`, `ScheduleContext` from `weatherbot.scheduler.context`, `was_sent`/`record_sent` from `weatherbot.weather.store`, and `plan_catchup` from `weatherbot.scheduler.catchup`. Define `fire_slot(location, slot, *, config, db_path, settings=None, client=None, channel=None, scheduled_dt=None, late=False)` that: computes `local_date` from `scheduled_dt` (or now in the location tz); returns early if `was_sent(location.name, slot.time, local_date)`; builds `ctx = ScheduleContext(scheduled_dt=scheduled_dt, tz=ZoneInfo(location.timezone), late=late)`; calls `result = send_now(location.name, config=config, db_path=db_path, settings=settings, client=client, channel=channel, schedule_ctx=ctx)`; if `result.ok` calls `record_sent(db_path, location.name, slot.time, local_date)`; logs `_log.info("slot fired", location=location.name, time=slot.time, late=late, delivered=result.ok)` (outcome-only — NEVER the appid/webhook). Wrap the whole body in `try/except Exception` that logs `_log.error("slot fire failed", location=location.name, time=slot.time, error=str(exc))` and returns `None` so one bad slot cannot crash the scheduler thread (Anti-Pattern: per-job isolation; Phase 4 hardens into retry-then-alert). Define `_register_jobs(scheduler, config, *, db_path, settings, client, channel)` implementing Pattern 1: for each location, for each ENABLED slot, `hh, mm = slot.parsed_time()`, `scheduler.add_job(fire_slot, trigger=CronTrigger(hour=hh, minute=mm, day_of_week=slot.day_of_week, timezone=location.timezone), kwargs={...}, id=f"{location.name}|{slot.time}|{slot.days}", misfire_grace_time=None, coalesce=True)` (misfire_grace_time=None because the sent-log owns cross-restart recovery, Anti-Pattern). Define `_announce_schedule(scheduler, config)` logging one `_log.info("scheduled slot", location=..., time=..., days=..., next_run_time=str(job.next_run_time))` per enabled job (D-10). Define `_run_catchup(config, *, db_path, settings, client, channel)` calling `plan_catchup(config, lambda name, time, date: was_sent(db_path, name, time, date))` and invoking `fire_slot(..., scheduled_dt=ms.scheduled_dt, late=True)` for each MissedSlot. Define `run_daemon(config, settings, db_path, *, client=None, channel=None) -> int`: build `BackgroundScheduler()`, `_register_jobs(...)`, `_announce_schedule(...)`, `_run_catchup(...)` (catch-up + announce BEFORE start so the log reads cleanly), `scheduler.start()`, then block on a `threading.Event`: register `signal.signal(signal.SIGTERM, lambda *_: stop.set())`, `try: stop.wait() / except KeyboardInterrupt: pass / finally: scheduler.shutdown(wait=False)`, return 0. Extend `weatherbot/scheduler/__init__.py` (Plan 01's barrel) to ALSO export `run_daemon`, `plan_catchup`, `MissedSlot` (additive — keep the existing `parse_days` export). Add to `tests/test_scheduler.py`: `_FakeClient`/`_FakeChannel` (copy from test_send_now.py), `test_fire_slot_records_after_success` (fires once, was_sent True after), `test_fire_slot_idempotent_double_fire` (second call skips, no extra send — SCHD-07/restart replay), `test_late_send_note` (within-grace fire renders a populated {schedule_note}), `test_jobs_registered_per_location_tz` (one job per enabled slot, trigger tz == loc tz, disabled slot → no job; assert via scheduler.get_jobs() after _register_jobs, NO start()/sleep), and `test_fire_slot_isolates_exception` (a raising channel does not propagate and does not record_sent).</action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py tests/test_send_now.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def run_daemon" weatherbot/scheduler/daemon.py` matches; `grep -n "def fire_slot" weatherbot/scheduler/daemon.py` matches.
    - `grep -n "CronTrigger" weatherbot/scheduler/daemon.py` matches and `grep -n "timezone=location.timezone\|timezone=loc.timezone" weatherbot/scheduler/daemon.py` matches; `grep -n "misfire_grace_time=None" weatherbot/scheduler/daemon.py` matches.
    - `grep -n "record_sent" weatherbot/scheduler/daemon.py` matches and appears AFTER a `result.ok` check (mark-after-success, D-07).
    - `grep -v '^#' weatherbot/scheduler/daemon.py | grep -c "appid\|webhook_url"` is 0 (no secret crosses the log boundary — outcome-only).
    - `uv run pytest tests/test_scheduler.py::test_fire_slot_idempotent_double_fire tests/test_scheduler.py::test_jobs_registered_per_location_tz tests/test_scheduler.py::test_fire_slot_isolates_exception -x` exits 0.
    - `uv run pytest tests/test_send_now.py::test_send_now_posts_briefing -x` still exits 0 (no regression; send_now seam unchanged for manual sends).
  </acceptance_criteria>
  <done>run_daemon registers one per-location-tz CronTrigger job per enabled slot, announces them with next_run_time, runs the catch-up scan, and blocks/shuts-down cleanly; fire_slot is check-before-fire / record-after-success / exception-isolated; double-fire and restart-replay are prevented; the recovered late send renders its intended-vs-actual note; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire weatherbot --run into the CLI (SCHD-05, D-09)</name>
  <read_first>
    - weatherbot/cli.py lines 306-314 (the `--check` store_true flag with `default=argparse.SUPPRESS`) and lines 328-333 (its dispatch branch: `_load_config_reporting` → `load_settings()` → orchestrator) — the EXACT analog for `--run`
    - weatherbot/cli.py lines 339-355 (the `--send-now` branch: `_load_config_reporting`, `load_settings()`, `db_path = DEFAULT_DB_PATH`, `db_path.parent.mkdir(parents=True, exist_ok=True)`, call into the composition root) — reuse the db_path/mkdir prep for --run
    - weatherbot/cli.py lines 280-320 (main() parser construction + arg order)
    - weatherbot/scheduler/daemon.py run_daemon (Task 2 — the target the branch dispatches to)
    - 03-PATTERNS.md "weatherbot/cli.py" CLI-flag-branch section lines 241-266
    - 03-CONTEXT.md D-09 (foreground, blocks, logs to stdout, clean Ctrl-C/SIGTERM, does NOT self-daemonize) and the Deferred note that --send-now stays available alongside --run
  </read_first>
  <action>In `weatherbot/cli.py`, add a `--run` argument to the `main()` parser as a `store_true` flag with `default=argparse.SUPPRESS`, mirroring `--check` exactly, with help text "Run the always-on scheduler in the foreground (blocks; Ctrl-C / SIGTERM to stop)." Import `run_daemon` from `weatherbot.scheduler.daemon` (top-level or local-in-branch import to avoid any import cycle with cli↔daemon — daemon imports `send_now` from cli, so import `run_daemon` INSIDE the branch or guard it; choose the cycle-safe placement). Add a dispatch branch in `main()` placed alongside the `--check` branch: `if hasattr(args, "run"):` → `config = _load_config_reporting(args.config)`; `if config is None: return 1`; `settings = load_settings()`; `db_path = DEFAULT_DB_PATH`; `db_path.parent.mkdir(parents=True, exist_ok=True)` (reuse the --send-now prep so the sent-log DB dir exists); `return run_daemon(config=config, settings=settings, db_path=db_path)`. Do NOT remove or alter the existing `--send-now`/`--check`/`--geocode` branches — manual send must keep working alongside the daemon (D-09). Extend `tests/test_scheduler.py` with `test_run_flag_dispatches_to_daemon` that calls `main(["--run", "--config", <tmp config>])` with `run_daemon` monkeypatched to a stub returning 0 (assert the stub was invoked with the loaded config and a db_path) — proving the flag is wired without starting a real blocking daemon.</action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py -x -q && uv run python -c "import weatherbot.cli; import weatherbot.scheduler.daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"--run"' weatherbot/cli.py` matches; `grep -n "run_daemon" weatherbot/cli.py` matches in the dispatch branch.
    - `grep -n 'hasattr(args, "run")' weatherbot/cli.py` matches and the branch calls `_load_config_reporting` then `run_daemon`.
    - `uv run python -c "import weatherbot.cli; import weatherbot.scheduler.daemon"` exits 0 (no circular-import error).
    - `uv run pytest tests/test_scheduler.py::test_run_flag_dispatches_to_daemon -x` exits 0.
    - `uv run pytest -q` (full suite) exits 0 — no regression in send-now/check/geocode/config/store/renderer.
  </acceptance_criteria>
  <done>`weatherbot --run` loads + validates config, prepares the db dir, and dispatches to run_daemon (foreground, no self-daemonize); --send-now/--check/--geocode still work; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml schedule → live CronTrigger jobs | Validated `time`/`days`/`enabled` (Plan 01 fail-loud) become scheduler jobs; no unvalidated token reaches the trigger |
| daemon (long-running) → structlog stdout | First always-on process; per-fire/announce/catch-up outcomes are logged continuously over days |
| daemon → data/weatherbot.db (sent_log) | `was_sent`/`record_sent` read/write the (location, send_time, local_date) key via Plan 01's parameterized helpers |
| fire_slot → send_now → OpenWeather/Discord | The daemon is a new CALLER of the existing send_now composition root; secrets stay inside the client/channel, never in scheduler code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | daemon structlog logging over a multi-day run | mitigate | Outcome-only logging carried into daemon.py: `_announce_schedule`/`fire_slot` log `location`/`time`/`days`/`next_run_time`/`delivered` only — NEVER the `appid` or webhook URL (grep gate asserts no `appid`/`webhook_url` in the module); secrets stay inside the injected client/channel |
| T-03-07 | Denial of Service | one fire_slot exception killing the scheduler thread (whole daemon stops sending) | mitigate | fire_slot wraps its body in try/except that logs and returns None — one bad slot is isolated so the scheduler keeps firing other slots (minimal isolation now; Phase 4 RELY-06 hardens into retry-then-alert); `test_fire_slot_isolates_exception` proves no propagation |
| T-03-08 | Tampering | DST fall-back / restart replay double-sending a slot | mitigate | `(location, send_time, local_date)` idempotency key: `was_sent` check-before-fire + `record_sent` mark-after-success (D-07) + Plan 01's UNIQUE backstop; `test_fire_slot_idempotent_double_fire` + `test_dst_exactly_once` prove exactly-once |
| T-03-09 | Denial of Service | catch-up burst after a long outage exhausting the OpenWeather quota | accept | D-04 bounds catch-up to TODAY's slots whose time passed < 90 min ago — at most a handful of fires, a rounding error against 60/min, 1M/month; no throttle needed for v1 (Phase 4 adds backoff) |
| T-03-10 | Tampering | config schedule input reaching the trigger unvalidated | mitigate | Plan 01's `Schedule` pydantic validators (`HH:MM` + `parse_days` whitelist) fail loud at config load before any job is registered; `_register_jobs` consumes only the validated/normalized form |

> Honest severity: this is a local single-user FOREGROUND daemon (no network listener, no inbound auth surface, no multi-user access control). No HIGH threats — the realistic risks are secret-leakage-in-logs (mitigated by outcome-only logging) and process robustness/exactly-once (mitigated by per-job isolation + the idempotency key). No ASVS L1 HIGH finding blocks this plan.
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py -q` green (catch-up window, DST exactly-once, days-match, fire_slot record/idempotent/isolation, per-tz registration, --run dispatch).
- `uv run pytest -q` (full suite) green — no regression in send-now/check/geocode/config/store/renderer.
- `uv run python -c "import weatherbot.cli; import weatherbot.scheduler.daemon; import weatherbot.scheduler.catchup"` exits 0 (no circular import).
- `apscheduler` importable (Plan 01 installed it; 3.11.x, no 4.x).
- No `appid`/webhook URL anywhere in `daemon.py` (outcome-only logging, T-04-01).
</verification>

<success_criteria>
- SCHD-05: `weatherbot --run` runs an in-process `BackgroundScheduler` that registers one job per enabled slot and computes/announces each job's next run in the location's IANA timezone (D-09/D-10/D-11).
- SCHD-06: the 90-minute startup catch-up scan (`plan_catchup`) delivers a missed slot once on recovery when < 90 min late, skips + logs it when ≥ 90 min late (D-04), and never re-sends an already-recorded slot.
- SCHD-03 (DST exactly-once realized): the live per-location `CronTrigger` + the `(location, send_time, local_date)` idempotency key make a morning send fire exactly once across a simulated spring-forward and fall-back (success criteria #2/#3).
- Phase-complete: with Plans 01 (SCHD-01/02/03/07-store) and 02 (SCHD-04 display) plus this plan, all of SCHD-01..07 are satisfied.
- Boundary respected: a failed send leaves the slot unsent (mark-after-success only) — retry-then-alert is Phase 4 (RELY-*), NOT this plan.
</success_criteria>

<output>
Create `.planning/phases/03-always-on-scheduler/03-03-SUMMARY.md` when done.
</output>
