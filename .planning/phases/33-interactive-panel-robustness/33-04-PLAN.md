---
phase: 33-interactive-panel-robustness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_panel.py
  - weatherbot/interactive/panel.py
autonomous: true
requirements: [HARD-UI-02]
must_haves:
  truths:
    # Derived from D-04/F23/F24 (authoritative edges — CONTEXT.md + WHOLE-PROJECT-REVIEW.md)
    - "With zero configured locations, `_select_contributor` returns a disabled, self-documenting placeholder Select instead of raising ValueError — so `_build_clone_view()` ALWAYS succeeds and `_safe_error_edit` renders a recovery cue rather than recursing into the same ValueError and freezing the panel (F23)."
    - "After the config is fixed (locations restored), the panel re-renders normally (the empty-locations degrade is a recoverable state, not a permanent one)."
    - "`LocationSelect.callback` captures `previous = self._selection.value` before mutating; on a failed/expired `edit_message` (discord.NotFound / discord.HTTPException) it rolls the selection back to `previous`, so a failed ack cannot leave the shared selection silently advanced (F24)."
    - "The clone is built from the NEW selection value (the dropdown shows default=True on the selected option) — the value is set before `_build_clone_view()` is called, then rolled back only on ack failure."
    - "The panel selection is read via `SelectedContext.value` (never `Select.values` outside an active select interaction)."
    # Flagged assumption — deterministic edge probe could not classify HARD-UI-02 audit text (specless fallback §A):
    - { statement: "HARD-UI-02 (panel interaction slice) audit-style requirement text is unclassified by the deterministic edge probe; its real edges are covered by the D-04/F23/F24 truths above — flagged assumption, not a silent drop.", verification: backstop }
  artifacts:
    - "weatherbot/interactive/panel.py — non-raising `_select_contributor` empty-locations degrade + ack-before-mutate roll-back in LocationSelect.callback"
    - "tests/test_panel.py — empty-locations recovery + ack-failure rollback regressions"
  key_links:
    - "_select_contributor (non-raising) → hub _build_clone_view() → hub _safe_error_edit: the app contributor must not raise so neither clone path (success or error) is poisoned."
    - "LocationSelect.callback set(new) → _build_clone_view() (reads SelectedContext.value) → edit_message: order matters — clone reflects the new value; roll back to previous only on ack failure."
  prohibitions:
    - "The F23 fix must NOT still raise inside the clone path — any residual raise recurses through `_safe_error_edit` → `_build_clone_view()` and re-freezes the panel; degrade to a non-raising placeholder, don't just move the raise."
    - "The F23/F24 fixes must NOT add another blanket `except` that swallows to a log — turn silent-swallow into visible recovery / reversible mutation, not more catches."
    - "The F24 roll-back must NOT ack with a clone built from the OLD selection — the clone must reflect the new value; only roll back the SelectedContext on a genuine ack failure."
    - "No edit under yahir_reusable_bot / .venv — the F23 recursion originates in the hub but the cure is app-side (hub is frozen, pinned wheel)."
---

<objective>
Fix the HARD-UI-02 panel-interaction slice (D-04): F23 empty-locations render recursion (a zero-locations re-render raises ValueError, and `_safe_error_edit` → `_build_clone_view()` re-invokes the same contributor → same ValueError → swallowed → frozen panel) by making `_select_contributor` non-raising (degrade to a disabled placeholder); and F24 ack ordering (`LocationSelect.callback` mutates the shared selection before acking, so a failed/expired `edit_message` leaves selection advanced with no re-render) via ack-before-mutate with roll-back. Both cures are app-side — the hub is frozen.

Purpose: The panel never freezes on empty config and never silently advances the selection on a failed interaction.
Output: Non-raising contributor + reversible select callback + regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-interactive-panel-robustness/33-CONTEXT.md
@.planning/phases/33-interactive-panel-robustness/33-RESEARCH.md
@.planning/phases/33-interactive-panel-robustness/33-PATTERNS.md
</context>

## Artifacts this phase produces (NEW symbols this plan creates)
- A disabled placeholder-Select branch in `_select_contributor` (weatherbot/interactive/panel.py) for the zero-locations case — new local logic replacing the `raise ValueError`.
- `previous`-capture + roll-back-on-`(discord.NotFound, discord.HTTPException)` in `LocationSelect.callback` — new logic on the existing callback.
- `tests/test_panel.py::test_empty_locations_recover` and `::test_ack_failure_rollback` — new test functions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED regressions — empty-locations recovery (no recursion) + ack-failure rollback</name>
  <files>tests/test_panel.py</files>
  <read_first>
    - weatherbot/interactive/panel.py (LocationSelect.callback 236-255 — set-before-ack; _select_contributor 290-301 — raise ValueError; _forecast_grid_contributor 303-330 — the non-raising sibling analog; options-build 224-229 — default= from SelectedContext.value)
    - tests/test_panel.py (existing fake_interaction driving of callbacks, ~12-26, 300-317)
    - tests/conftest.py (fake_interaction factory ~242 — response.edit_message AsyncMock; make it raise discord.NotFound for F24)
    - .planning/WHOLE-PROJECT-REVIEW.md (F23, F24 scenarios)
    - .planning/phases/33-interactive-panel-robustness/33-RESEARCH.md (Patterns 2 & 3; Pitfalls 3 & 4; discord.py 2.7.1 Interaction API)
  </read_first>
  <behavior>
    - `test_empty_locations_recover`: with `holder.current().locations == []`, invoke `_select_contributor(selection)` and assert it RETURNS a list (a disabled placeholder Select) rather than raising ValueError; assert `_build_clone_view()` succeeds; then restore locations and assert a normal LocationSelect renders again.
    - `test_ack_failure_rollback`: drive `LocationSelect.callback` with a `fake_interaction` whose `response.edit_message` raises `discord.NotFound` (expired token); assert `selection.value` reverted to the previous value (not silently advanced).
  </behavior>
  <action>
    In tests/test_panel.py add `test_empty_locations_recover`: build the contributors with a holder returning zero locations, call `_select_contributor(selection)`, assert no ValueError and a disabled placeholder Select is returned; drive `_build_clone_view()` and assert it does not raise; then swap the holder to a non-empty config and assert a real LocationSelect is produced. Add `test_ack_failure_rollback`: seed `SelectedContext` with an initial value, use `fake_interaction` with `response.edit_message` set to raise `discord.NotFound`, invoke `LocationSelect.callback`, assert `selection.value == initial` (rolled back). Both RED before Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_panel.py -k "empty_locations_recover or ack_failure_rollback" -x</automated>
  </verify>
  <done>Both tests exist and fail against current panel.py (contributor raises on empty; callback advances selection before ack with no rollback).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Non-raising empty-locations degrade (F23) + ack-before-mutate roll-back (F24)</name>
  <files>weatherbot/interactive/panel.py</files>
  <read_first>
    - weatherbot/interactive/panel.py (_select_contributor 290-301; LocationSelect.callback 246-255; _forecast_grid_contributor 303-330 non-raising analog)
    - .planning/phases/33-interactive-panel-robustness/33-PATTERNS.md (panel.py _select_contributor + LocationSelect.callback sections)
    - .planning/phases/33-interactive-panel-robustness/33-RESEARCH.md (Pattern 3 placeholder Select; Pattern 2 roll-back; Open Q 2 — roll-back is the recommendation)
  </read_first>
  <action>
    In `_select_contributor` (panel.py), replace the `raise ValueError(...)` on empty locations with a return of a disabled, self-documenting placeholder Select: a `discord.ui.Select(custom_id="wb:loc:select", placeholder="No locations configured — edit config.toml", options=[discord.SelectOption(label="(none)", value="__none__")], disabled=True, row=0)` in a one-element list — matching the non-raising contract of `_forecast_grid_contributor`. Exact recovery-cue text is planner discretion (D-04); the invariant is non-raising so `_build_clone_view()` never poisons `_safe_error_edit`.
    In `LocationSelect.callback` (panel.py:246-255), refactor to ack-before-mutate with roll-back: capture `previous = self._selection.value`; `new_value = self.values[0]`; `self._selection.set(new_value)`; then `await interaction.response.edit_message(view=panel._build_clone_view())` inside a `try` that catches `(discord.NotFound, discord.HTTPException)`, on which it calls `self._selection.set(previous)` and re-raises (letting the existing module `View.on_error` / `_safe_error_edit` backstop handle it). The clone MUST be built AFTER the set so it reflects the new selection (default=True derives from `SelectedContext.value`, panel.py:224-229). Read `SelectedContext.value` (never `Select.values`) elsewhere. Do NOT add a new blanket except; do NOT edit the hub.
  </action>
  <verify>
    <automated>uv run pytest tests/test_panel.py -x</automated>
  </verify>
  <done>Empty config degrades to a disabled placeholder (no recursion, panel recoverable); an expired/failed ack rolls the selection back; the clone reflects the new selection; no hub diff; full test_panel.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord interaction → panel state | A select interaction mutates shared SelectedContext and acks; a failed/expired ack must not leave state advanced. |
| Config (locations) → panel render | Zero locations must render a recoverable cue, never a frozen panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04-01 | Denial of Service | empty-locations render recursion (F23) | medium | mitigate | App-side degrade to a disabled, self-documenting Select so `_build_clone_view()` never raises — recoverable, never a swallowed freeze. |
| T-33-04-02 | Tampering | interaction token expiry / ack failure (F24) | medium | mitigate | Ack-before-mutate + roll-back on `discord.NotFound`/`HTTPException` so a failed/expired ack can't silently advance the shared selection. |
| T-33-04-03 | Info-disclosure | error-path exception text | low | accept | Keep the generic `_safe_error_edit`/backstop; do not render exception text into a reply (no `appid`-adjacent widening). |
| T-33-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs (discord.py already pinned); no `[ASSUMED]`/`[SUS]` packages → no install checkpoint. |
</threat_model>

<verification>
- `uv run pytest tests/test_panel.py -x` green.
- Litmus/grimp: `git diff --stat` shows no change under `.venv/` or `../Reusable/YahirReusableBot/`; `uv run pytest tests/test_import_hygiene.py -x` green.
</verification>

<success_criteria>
Zero-locations config degrades to a recoverable disabled panel (no recursion); a failed/expired ack rolls the selection back; both cures are app-side.
</success_criteria>

<output>
Create `.planning/phases/33-interactive-panel-robustness/33-04-SUMMARY.md` when done
</output>
