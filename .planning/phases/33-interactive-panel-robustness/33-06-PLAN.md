---
phase: 33-interactive-panel-robustness
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_forecast_render.py
  - tests/test_command_views.py
  - weatherbot/interactive/commands/forecast.py
  - templates/renderer.py
  - weatherbot/interactive/commands/status.py
  - weatherbot/interactive/state.py
autonomous: true
requirements: [HARD-UI-03]
must_haves:
  truths:
    # Derived from D-06/D-07/D-08/F28 (authoritative edges — CONTEXT.md + WHOLE-PROJECT-REVIEW.md)
    - "The forecast header (`{title} — {location}`) appears EXACTLY ONCE on both surfaces — the embed keeps its title and the rendered body no longer repeats the header as its first line (F28); no rendered forecast body line equals the embed title."
    - "A render token that substitutes to an empty string (e.g. `{notice}`/`{footer_note}` empty) leaves NO trailing/interior blank line in the output — empty-token blanks are stripped."
    - "Out-of-today forecast bucket labels render as weekday + abbreviated month + day (e.g. `Thu Jul 17`); Today/Tomorrow labels are unchanged (D-06)."
    - "Humanized timestamps render as local 24-hour `HH:MM` (e.g. `09:00`) with the timezone offset dropped, replacing raw ISO / `%Y-%m-%d %H:%M UTC` in the template/CLI text path (status.py, state.py, and the `{sent_at}`/`{checked_at}` path) (D-07)."
    - "The embed description's `<t:unix:R>` relative-timestamp markdown (bot.py) is left unchanged — the ISO→human fix is ONLY in the template/CLI text path, not the embed description."
    - "The F28 golden snapshot is regenerated intentionally and the `.ambr` diff is exactly the removed header line (nothing broader)."
    # Flagged assumptions — deterministic edge probe could not classify HARD-UI-03 audit text (specless fallback §A):
    - { statement: "HARD-UI-03 (render-formatting slice) audit-style requirement text is unclassified by the deterministic edge probe; its real edges are covered by the D-06/D-07/D-08/F28 truths above — flagged assumption, not a silent drop.", verification: backstop }
    - { statement: "HARD-UI-03 length/equality-of-'the header line' resolves to string equality against the rendered body lines vs. the embed title (bytes/code-points not distinguished — plain ASCII header); no normalized-form ambiguity applies to this audit fix — flagged assumption from the empty/encoding probe rows.", verification: backstop }
  artifacts:
    - "weatherbot/interactive/commands/forecast.py — drop the duplicated body header (F28); D-06 date label"
    - "templates/renderer.py — empty-token blank-line collapse"
    - "weatherbot/interactive/commands/status.py + weatherbot/interactive/state.py — ISO→local-24h formatter (D-07)"
    - "regenerated golden .ambr snapshot for the deduped forecast header"
    - "tests/test_forecast_render.py + tests/test_command_views.py — empty-token, date-label, humanized-timestamp regressions"
  key_links:
    - "CommandReply.title (embed title) vs. rendered body first line: exactly one must carry the header — drop the body copy, keep the title (Open Q 1 recommendation)."
    - "renderer.render/render_forecast → empty substitution → blank-line collapse: the fix is a post-render blank-line strip, not a template edit."
  prohibitions:
    - "The F28 dedup must NOT change more than the single duplicated header line — a golden diff broader than the removed line means the wrong copy or an over-broad edit."
    - "The D-07 timestamp fix must NOT touch the embed description `<t:>` relative markdown — only the template/CLI text ISO path."
    - "The date-label formatter must NOT use a glibc-specific `%-d`/`%-m` directive — build an explicit f-string (matches the existing `_day_label` convention, portability)."
    - "The empty-token blank collapse must NOT strip legitimate content lines — only lines that are empty solely because a token substituted to an empty string."
  # NOTE (comment-text discipline): a negative acceptance criterion asserts 'no rendered forecast body line equals the embed title'. The literal header string is data-driven (title/location), never hard-coded as a banned literal in any <action>, so no grep-gate self-invalidation risk.
---

<objective>
Fix the HARD-UI-03 render-formatting slice: F28 duplicated forecast header (title == body first line, D-08) — drop the body copy so it appears once on both surfaces and regenerate the golden; empty-token trailing/interior blank lines (D-08); out-of-today date labels → `Thu Jul 17` (D-06); and raw ISO timestamps → local 24-hour `09:00` in the template/CLI text path (D-07), leaving the embed's `<t:>` markdown untouched.

Purpose: Clean, unambiguous, human-formatted rendered output.
Output: Deduped header + blank-collapse + date/time formatters + regenerated golden + regressions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-interactive-panel-robustness/33-CONTEXT.md
@.planning/phases/33-interactive-panel-robustness/33-RESEARCH.md
@.planning/phases/33-interactive-panel-robustness/33-PATTERNS.md
</context>

## Artifacts this phase produces (NEW symbols this plan creates)
- A deduped forecast body (the header no longer rendered as the body's first line) in `weatherbot/interactive/commands/forecast.py` — new logic (planner-discretion carrier: drop the header token from the template body or from the header_values wiring).
- A `_day_label` out-of-today branch rendering `f"{abbr} {mon_abbr} {day}"` (weekday + abbreviated month + day) in forecast.py — new logic on the existing helper.
- An empty-token blank-line collapse in `templates/renderer.py` (post-render strip) — new logic.
- A local-24h `HH:MM` formatter replacing the ISO/UTC formatters in `status.py` and `state.py` (and the `{sent_at}`/`{checked_at}` path) — new logic.
- A regenerated golden `.ambr` snapshot for the deduped forecast header — updated snapshot artifact.
- `tests/test_forecast_render.py::test_empty_token_no_trailing_blank`, `::test_out_of_today_date_label`; `tests/test_command_views.py::test_humanized_timestamp` — new test functions.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED regressions — F28 dedup, empty-token blanks, D-06 label, D-07 timestamp</name>
  <files>tests/test_forecast_render.py, tests/test_command_views.py</files>
  <read_first>
    - weatherbot/interactive/commands/forecast.py (CommandReply title 165; header_values/_day_label 55-73, 145-165; render_forecast call)
    - templates/renderer.py (render 156-168; render_forecast 171-196 — empty substitution leaves a blank line)
    - weatherbot/interactive/commands/status.py (_fmt_epoch 22-26 — strftime %Y-%m-%d %H:%M UTC)
    - weatherbot/interactive/state.py (next-fires isoformat 84)
    - tests/test_forecast_render.py + tests/test_command_views.py (existing render/label test patterns)
    - tests/test_golden_embeds.py (forecast golden via render_embed — the F28 snapshot to regenerate)
    - .planning/WHOLE-PROJECT-REVIEW.md (F28 scenario)
    - .planning/phases/33-interactive-panel-robustness/33-RESEARCH.md (Pitfall 5 golden regen; A3 timestamp path; A4 empty-token)
  </read_first>
  <behavior>
    - `test_forecast_header_appears_once`: render a forecast reply; assert NO line in the rendered body equals the `CommandReply.title` string (header appears once — as the title only).
    - `test_empty_token_no_trailing_blank`: render a template where a token (e.g. `{notice}`) substitutes to ""; assert the output has no trailing/interior blank line caused by that empty token.
    - `test_out_of_today_date_label`: a bucket >1 day out renders `Thu Jul 17` (weekday + abbrev month + day), while Today/Tomorrow unchanged.
    - `test_humanized_timestamp`: an epoch/ISO renders as local `09:00` (24h, no offset), not raw ISO / `... UTC`.
  </behavior>
  <action>
    In tests/test_forecast_render.py add `test_forecast_header_appears_once` (assert no rendered body line equals the reply title), `test_empty_token_no_trailing_blank` (render with an empty token, assert no resulting blank line), and `test_out_of_today_date_label` (assert `Thu Jul 17`-shaped label for a >1-day bucket). In tests/test_command_views.py add `test_humanized_timestamp` (assert `09:00`-shaped local 24h output from the status/next-fires path). All RED before Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_forecast_render.py -k "header_appears_once or empty_token or date_label" tests/test_command_views.py -k humanized_timestamp -x</automated>
  </verify>
  <done>All four tests exist and fail against current render code (duplicated header; empty-token blanks; old label format; raw ISO/UTC timestamps).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dedup header (F28) + blank collapse + D-06 label + D-07 timestamps, regenerate golden</name>
  <files>weatherbot/interactive/commands/forecast.py, templates/renderer.py, weatherbot/interactive/commands/status.py, weatherbot/interactive/state.py</files>
  <read_first>
    - weatherbot/interactive/commands/forecast.py (165 title; _day_label 60-73; header_values 150-156)
    - templates/renderer.py (render/render_forecast — where the empty substitution leaves a blank)
    - weatherbot/interactive/commands/status.py:26 + weatherbot/interactive/state.py:84 (ISO/UTC formatter sites)
    - weatherbot/interactive/bot.py:221 (the embed `<t:>` markdown to leave UNCHANGED — A3)
    - .planning/phases/33-interactive-panel-robustness/33-PATTERNS.md (forecast.py F28 + status/state D-07 sections)
    - MEMORY: pytest-snapshot-report-quirk (trust exit code + .ambr diff, not the "N snapshots failed" banner)
  </read_first>
  <action>
    F28 (D-08): drop the DUPLICATE header from the BODY (keep the embed `CommandReply.title` as the single header) so the embed keeps a proper title and the body starts with content — carrier is planner discretion (remove the header line from the forecast template body, or stop feeding the header `title`/`location` tokens into the body's first line). Then regenerate the golden: `uv run pytest tests/test_forecast_render.py tests/test_golden_embeds.py --snapshot-update`, and confirm the `.ambr` diff is EXACTLY the removed header line (Pitfall 5). Trust the exit code + `.ambr` diff, not the "N snapshots failed" banner.
    Empty-token blanks (D-08): in templates/renderer.py, collapse blank lines that result solely from a token substituting to "" (a post-render pass that drops lines left empty by an empty substitution) — do NOT strip legitimate content lines.
    D-06 label: in `_day_label` (forecast.py:60-73), change the out-of-today branch from `f"{abbr} {month}/{day}"` to weekday + abbreviated month + day (e.g. `Thu Jul 17`) using an explicit f-string with an abbreviated-month lookup (NO glibc `%-d`/`%-m`); Today/Tomorrow branches unchanged.
    D-07 timestamps: replace the ISO/UTC render in status.py:26 (`strftime("%Y-%m-%d %H:%M UTC")`) and state.py:84 (`.isoformat()`), plus the `{sent_at}`/`{checked_at}` template/CLI path, with a local 24-hour `HH:MM` formatter (offset dropped, already localized). Leave the embed description `<t:unix:R>` markdown (bot.py:221) UNCHANGED — the fix is ONLY the template/CLI text path (A3).
  </action>
  <verify>
    <automated>uv run pytest tests/test_forecast_render.py tests/test_command_views.py tests/test_status.py -x</automated>
  </verify>
  <done>Forecast header appears once (embed title only; no body line equals it); empty tokens leave no blank; out-of-today labels render `Thu Jul 17`; timestamps render local `09:00`; the embed `<t:>` markdown is unchanged; the golden is regenerated with a diff of exactly the removed header line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reply/model → rendered surface (embed + CLI text) | Provider-derived and computed values cross into the rendered output; formatting must not duplicate, leave blanks, or leak raw internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-06-01 | Info-disclosure | raw ISO/UTC timestamp render (D-07) | low | mitigate | Render humanized local 24h times; drop raw ISO/offset that exposes internal representation — cosmetic, but keeps output clean and consistent. |
| T-33-06-02 | Tampering | golden snapshot regeneration (F28) | low | mitigate | Regenerate intentionally; assert the `.ambr` diff is exactly the removed header line so the dedup can't silently alter other output. |
| T-33-06-03 | Info-disclosure | provider text through render formatters | low | accept | Existing `_clip` on provider-controlled text (bot.py) is unchanged; the new formatters handle computed dates/temps, not raw provider strings. |
| T-33-06-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs; no `[ASSUMED]`/`[SUS]` packages → no install checkpoint. |
</threat_model>

<verification>
- `uv run pytest tests/test_forecast_render.py tests/test_command_views.py tests/test_status.py -x` green.
- `uv run pytest` full suite green — trust exit code + `.ambr` diff, not the "N snapshots failed" banner (syrupy quirk). Confirm the F28 golden diff is exactly the removed header line.
</verification>

<success_criteria>
Forecast header appears once, empty tokens leave no blanks, out-of-today labels are unambiguous (`Thu Jul 17`), timestamps are humanized local 24h (`09:00`), and the embed `<t:>` markdown is untouched.
</success_criteria>

<output>
Create `.planning/phases/33-interactive-panel-robustness/33-06-SUMMARY.md` when done
</output>
