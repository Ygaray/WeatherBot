---
phase: 25-lifecycle-ready-gate-composition-root
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - weatherbot/scheduler/wiring.py
  - weatherbot/scheduler/daemon.py
  - weatherbot/ops/sdnotify.py
  - weatherbot/ops/pidfile.py
  - weatherbot/ops/selfcheck.py
  - weatherbot/ops/__init__.py
  - weatherbot/cli.py
  - deploy/bot.service.template
  - deploy/README.md
autonomous: true
requirements: [SEAM-05, APP-01, APP-02]
must_haves:
  truths:
    - "READY=1 reaches systemd ONLY after the app probe passes — emitted strictly after the gate returns True AND after scheduler.start(), at the same call site as today's emit_online."
    - "run_daemon keeps the load-bearing lifecycle ordering: SIGTERM-handler-before-gate, single-channel-build-once, gate→scheduler.start()→online, observer armed in finally, PID written before the gate."
    - "build_runtime is the single composition root: it constructs holder + SchedulerEngine + ReloadEngine + ReadyGate + channel + the four injected leak points and returns the wired parts, with zero duplicated copy of any module mechanism."
    - "The default LifecycleIdentity reproduces /run/weatherbot/weatherbot.pid + the b'weatherbot' marker byte-identically."
    - "cli.py:do_reload exit codes stay byte-identical under the generalized proc_marker guard."
    - "The exactly-once online emit subsumes the startup heartbeat tick; the re-probe wait stays interruptible stop.wait(interval)."
    - "The rendered .service after <NAME>/<RUNTIME_DIR>/<REPO>/<USER> substitution is byte-identical to today's weatherbot.service."
  artifacts:
    - path: "weatherbot/scheduler/wiring.py"
      provides: "build_runtime(...) — the single app-side composition root"
      contains: "def build_runtime"
    - path: "weatherbot/ops/sdnotify.py"
      provides: "re-export shim for the relocated SystemdNotifier"
      contains: "from yahir_reusable_bot.lifecycle import SystemdNotifier"
    - path: "weatherbot/ops/pidfile.py"
      provides: "byte-identical default identity (PID_FILE + b'weatherbot' marker) over the generalized guard"
      contains: "is_weatherbot_pid"
    - path: "deploy/bot.service.template"
      provides: "parameterized systemd unit with <NAME>/<RUNTIME_DIR> placeholders"
      contains: "<RUNTIME_DIR>"
  key_links:
    - from: "weatherbot/scheduler/wiring.py"
      to: "yahir_reusable_bot/lifecycle/ready_gate.py"
      via: "build_runtime constructs ReadyGate with injected health_check + on_online + on_fail + identity"
      pattern: "ReadyGate\\("
    - from: "weatherbot/scheduler/daemon.py"
      to: "weatherbot/scheduler/wiring.py"
      via: "run_daemon calls build_runtime then drives the gate keeping the ordering"
      pattern: "build_runtime\\("
    - from: "weatherbot/ops/selfcheck.py"
      to: "yahir_reusable_bot/lifecycle/health.py"
      via: "boundary adapter maps CheckResult.reason → HealthResult.severity"
      pattern: "HealthResult"
---

<objective>
Wire the new lifecycle module into WeatherBot at a SINGLE composition root (APP-01) and prove the
four leak points are injected (APP-02), keeping behavior byte-identical (SEAM-05). This is a MOVE,
not a redesign (D-04 option d): lift `run_daemon`'s ~230-line wiring block into one app-side
`build_runtime(...)`; `run_daemon` keeps the load-bearing lifecycle ORDERING.

Purpose: the single greppable wiring site that injects the health probe, the config id-deriver,
the selected-location context, and render_embed/panel cosmetics — with the durable health row,
tick, and Discord ping riding injected best-effort hooks (D-02a). Identity is parameterized via a
default `LifecycleIdentity` that reproduces today's PID path + marker exactly; the `.service`
becomes a template.

Output: `wiring.py` build_runtime; daemon driving the ReadyGate; sdnotify/pidfile/selfcheck/ops/cli
byte-identical at the boundary; `.service` template + README.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-CONTEXT.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-PATTERNS.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols created by this plan (drift-verification excludes them):
- `weatherbot/scheduler/wiring.py` — the new app-side composition-root module
- `build_runtime(...)` — the single wiring function returning the wired parts
- the default `LifecycleIdentity(...)` the app constructs (reproducing `/run/weatherbot/weatherbot.pid` + `b"weatherbot"`)
- the `CheckResult → HealthResult` boundary adapter (in `weatherbot/ops/selfcheck.py`)
- `deploy/bot.service.template` + the `<NAME>` / `<RUNTIME_DIR>` placeholders
- `weatherbot/ops/sdnotify.py` becomes a re-export shim (relocated `SystemdNotifier`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Generalize pidfile + sdnotify shim + selfcheck→HealthResult adapter (byte-identical boundary)</name>
  <files>weatherbot/ops/sdnotify.py, weatherbot/ops/pidfile.py, weatherbot/ops/selfcheck.py, weatherbot/ops/__init__.py, weatherbot/cli.py</files>
  <read_first>
    - yahir_reusable_bot/lifecycle/ (the Task-1/Task-2 surface from Plan 25-01: SystemdNotifier, HealthResult, Severity/Level, LifecycleIdentity, is_running_process, write_pid_atomic, read_pid)
    - weatherbot/ops/sdnotify.py (becomes a re-export shim — mirror the 22-02 channels/base.py + 24-02 holder.py re-export-shim pattern)
    - weatherbot/ops/pidfile.py L37-139 (the constant + guard to back with the generalized module guard; keep the app default = b"weatherbot")
    - weatherbot/ops/selfcheck.py L42-123 (run_self_check + CheckResult STAY app-side, D-02a; add the boundary adapter mapping AUTH_FAILED→CRITICAL severity, else→WARNING)
    - weatherbot/ops/__init__.py (the re-export surface that must keep PID_FILE/is_weatherbot_pid/SystemdNotifier/run_self_check/CheckResult stable)
    - weatherbot/cli.py L50 (PID_FILE import) + L465-516 (do_reload — must keep byte-identical exit codes under the generalized guard)
  </read_first>
  <action>
    Re-point the app's ops surface at the relocated/generalized module symbols WITHOUT changing any
    observable behavior.

    (a) `weatherbot/ops/sdnotify.py`: replace the body with a re-export shim
    `from yahir_reusable_bot.lifecycle import SystemdNotifier` (+ `__all__`), mirroring the
    Phase-22 channels/base.py and Phase-24 holder.py shim pattern, so `daemon.py`'s
    `from weatherbot.ops import SystemdNotifier` resolves to the IDENTICAL object.

    (b) `weatherbot/ops/pidfile.py`: keep `PID_FILE = Path("/run/weatherbot/weatherbot.pid")` as the
    app's default. Re-implement `is_weatherbot_pid(pid, cmdline_reader=None)` as a thin app-side wrapper
    that delegates to the module's `is_running_process(pid, proc_marker=b"weatherbot", cmdline_reader=...)`,
    and back `write_pid_atomic`/`read_pid` with the module helpers (or keep app thin wrappers that pass
    `PID_FILE`). The app keeps the `weatherbot`-named symbols (they live in a weather path — litmus only
    scans `yahir_reusable_bot/**`); the MODULE side stays noun-free. Behavior MUST be byte-identical:
    `is_weatherbot_pid(pid)` returns exactly what it does today for the same /proc cmdline.

    (c) `weatherbot/ops/selfcheck.py`: `run_self_check` + `CheckResult` + the
    PASS/NETWORK_NOT_READY/AUTH_FAILED vocabulary STAY here unchanged (D-02a). ADD a small boundary
    adapter (a function) that maps a `CheckResult` to a `HealthResult`: copy `ok`/`reason`/`detail`
    through and set `severity` = the CRITICAL rung when `reason == AUTH_FAILED`, else the WARNING rung.
    This is the seam that preserves today's per-attempt CRITICAL-on-auth / WARNING-on-network log split
    while letting the module branch on the neutral field.

    (d) `weatherbot/ops/__init__.py`: update re-exports so `SystemdNotifier` resolves through the shim
    and the generalized guard is still surfaced as `is_weatherbot_pid` (keep `PID_FILE`, `read_pid`,
    `write_pid_atomic`, `CheckResult`, `run_self_check`, the reason constants stable — the public ops
    surface is unchanged).

    (e) `weatherbot/cli.py`: no logic change needed — confirm `do_reload` still imports `PID_FILE` and
    calls `is_weatherbot_pid(pid, cmdline_reader=...)` and that exit codes are byte-identical. If the
    import path of any symbol moved, adjust the import only (never the control flow / messages / codes).
  </action>
  <verify>
    <automated>pytest -q tests/test_pidfile.py tests/test_sdnotify.py tests/test_selfcheck.py tests/test_reload_sender.py 2>/dev/null || pytest -q -k "pidfile or sdnotify or selfcheck or reload"</automated>
  </verify>
  <acceptance_criteria>
    - The pidfile/sdnotify/selfcheck/reload-sender tests pass unchanged (no test edited to accommodate).
    - `python -c "from weatherbot.ops import SystemdNotifier; import yahir_reusable_bot.lifecycle as L; assert SystemdNotifier is L.SystemdNotifier"` succeeds (identical object via shim).
    - `python -c "from weatherbot.ops.pidfile import PID_FILE; assert str(PID_FILE)=='/run/weatherbot/weatherbot.pid'"` succeeds (byte-identical default).
    - The CheckResult→HealthResult adapter sets CRITICAL severity iff reason==AUTH_FAILED.
  </acceptance_criteria>
  <done>
    The app ops surface is re-pointed at the module with byte-identical behavior: sdnotify is a shim
    resolving to the identical object, the pid guard delegates to the marker-parameterized module guard
    with the b"weatherbot" default, the selfcheck adapter maps reason→neutral severity, and do_reload
    exit codes are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: build_runtime composition root + run_daemon drives the ReadyGate (byte-identical ordering)</name>
  <files>weatherbot/scheduler/wiring.py, weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py L1354-1762 (the WHOLE run_daemon: the wiring block to lift L1387-1561 AND the load-bearing ordering to KEEP L1476-1485 SIGTERM, L1569 SIGHUP, L1576 PID write, L1598-1611 observer, L1614-1639 gate→start→online, L1729-1761 finally)
    - weatherbot/scheduler/daemon.py L1108-1156 (gate_until_healthy) + L1159-1189 (emit_online) — the behavior the ReadyGate now drives; these app functions may stay as thin adapters or the gate replaces them
    - weatherbot/scheduler/daemon.py L1530-1561 (the ReloadEngine construction — the EXACT injection-at-root template build_runtime follows for ReadyGate)
    - weatherbot/scheduler/daemon.py L1446-1451 (the __heartbeat__ registration — owned by the gate option a, or kept here option d, per the 25-01 SUMMARY decision)
    - yahir_reusable_bot/lifecycle/ready_gate.py (ReadyGate constructor signature from Plan 25-01)
    - weatherbot/ops/selfcheck.py (run_self_check + the new CheckResult→HealthResult adapter from Task 1 — the injected health_check closure)
    - weatherbot/weather/store.py (stamp_health / stamp_tick — app-side, ride on_fail/on_online hooks, D-02a)
    - yahir_reusable_bot/config/reload.py (ReloadEngine — already constructed in the wiring block; moves into build_runtime)
  </read_first>
  <action>
    Create `weatherbot/scheduler/wiring.py` with `build_runtime(...)` and refactor `run_daemon` to call
    it while KEEPING the order-sensitive lifecycle steps. This is a MOVE (D-04 option d), not a redesign.

    `build_runtime(config, settings, db_path, *, config_path=None, client=None, channel=None)` constructs
    (lifting from run_daemon L1387-1561, the constructors only — NO order-sensitive lifecycle):
    the channel-build-once block (L1387-1393), `BackgroundScheduler` + `stop` Event + `ConfigHolder` +
    `ForecastCache` (L1402-1427), `_register_jobs` + the `__heartbeat__` registration + `_register_uvmonitor_job`
    + `_announce_schedule` + `_run_catchup` (L1429-1474), and the `ReloadEngine` + its `_on_applied`
    closure (L1504-1561). ADD the new `ReadyGate` construction here, using the L1530-1561 ReloadEngine
    block as the literal injection template:
      - `health_check` = a closure calling `run_self_check(config=..., settings=...)` and mapping its
        CheckResult through the Task-1 adapter to a `HealthResult` (the injected app probe — leak point 3).
      - `on_fail`/`on_probe` = a closure stamping `stamp_health(db_path, reason=result.reason, detail=result.detail)`
        on each outcome (D-02a durable row, app-side).
      - `on_online` = a closure firing emit_online's parts (1) `stamp_health(reason="online")`, (2)
        `stamp_tick(db_path)`, (3)/(5) the Discord ping `channel.send("WeatherBot online — ...")` — all
        app-side, best-effort, the EXACT strings + order today's emit_online uses.
      - identity = a default `LifecycleIdentity(name="weatherbot", pid_file=PID_FILE, runtime_dir=...,
        console_name="weatherbot", proc_marker=b"weatherbot")` reproducing today's path + marker EXACTLY.
      - the `__heartbeat__` tick wiring per the 25-01 heartbeat-handle decision (gate-owned option a, or
        re-registered here option d).
    Return the wired parts (a small dataclass or named tuple): scheduler, stop, holder, cache, channel,
    bot-placeholder, reload_engine, ready_gate, notifier, started_at, watching-inputs — whatever
    run_daemon needs to keep its ordering.

    Refactor `run_daemon` to: call `build_runtime(...)` for the constructors, then KEEP the load-bearing
    ordering INLINE (do NOT move these into build_runtime):
      - install the SIGTERM handler BEFORE the gate (L1476-1485, Pitfall 2);
      - install the SIGHUP → `reload_engine.request_reload()` handler (L1569);
      - `write_pid_atomic(identity.pid_file)` at startup (L1576 — was `write_pid_atomic(PID_FILE)`;
        now threads the identity's path, byte-identical default);
      - arm the file-watch observer (L1598-1611);
      - the gate → `scheduler.start()` → online ORDERING (L1614-1639): drive `ready_gate` (replacing the
        bare `gate_until_healthy` call) — if it returns False (stop preempted) return 0; else
        `scheduler.start()` then the gate's online path emits READY=1 STRICTLY after both (READY must NOT
        be emitted inside build_runtime);
      - the inbound BotThread start AFTER online (L1642-1691, unchanged);
      - the main park→poll loop (L1700-1726, unchanged);
      - the `finally` teardown (L1729-1761): `scheduler.shutdown` / `reload_engine.stop` / `bot.stop` /
        `identity.pid_file.unlink(missing_ok=True)` (was `PID_FILE.unlink`).

    CRITICAL: the single most golden-sensitive invariant is READY=1 reaching systemd ONLY after the gate
    returns True AND scheduler.start() — preserve the exact call site. If any reload-golden perturbs,
    fall back to D-04 floor (keep procedural run_daemon, just make the four injections explicit) and note
    it in the SUMMARY.
  </action>
  <verify>
    <automated>pytest -q tests/ -k "daemon or run_daemon or reload or golden or schedule" 2>/dev/null && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite `pytest -q` exits 0 (BHV-01).
    - The Phase-21 golden tests are GREEN with ZERO non-empty snapshot diff (BHV-02) — confirmed via the Plan 25-03 self-UAT, but no golden file is edited here.
    - `grep -n "build_runtime" weatherbot/scheduler/daemon.py` shows run_daemon delegates construction to it.
    - `grep -nE "notifier.ready|emit READY|READY=1" weatherbot/scheduler/wiring.py` returns nothing (READY is NOT emitted in build_runtime — only run_daemon's post-start gate path emits it).
    - `grep -n "write_pid_atomic(identity" weatherbot/scheduler/daemon.py` (or equivalent) shows the PID write threads the identity's path.
  </acceptance_criteria>
  <done>
    build_runtime is the single composition root constructing holder + SchedulerEngine + ReloadEngine +
    ReadyGate + channel + the four injected leak points and returning the wired parts; run_daemon keeps
    the SIGTERM-before-gate / gate→start→READY / observer-in-finally ordering; READY is emitted strictly
    after gate+start; the full suite is green with no golden edited.
  </done>
</task>

<task type="auto">
  <name>Task 3: .service template (<NAME>/<RUNTIME_DIR>) + README placeholders (byte-identical render)</name>
  <files>deploy/bot.service.template, deploy/README.md</files>
  <read_first>
    - deploy/weatherbot.service (the WHOLE file — the existing <REPO>/<USER> sed convention to extend; lines 14 Description, 29 ExecStart, 31 WorkingDirectory, 34 EnvironmentFile, 36 User, 44 RuntimeDirectory)
    - deploy/README.md L31-41 (the documented <REPO>/<USER> substitution + ExecStart options) + L139-154 (the RuntimeDirectory rationale)
  </read_first>
  <action>
    Create `deploy/bot.service.template` from `deploy/weatherbot.service`, extending the existing
    `<REPO>`/`<USER>` sed-placeholder convention with `<NAME>` and `<RUNTIME_DIR>` (D-03a). Parameterize
    the three identity-bearing lines: `Description=<NAME> — ...`, `RuntimeDirectory=<RUNTIME_DIR>`, and
    add `PIDFile=/run/<RUNTIME_DIR>/<NAME>.pid`. Keep `Type=notify` / `NotifyAccess=main` /
    `TimeoutStartSec=infinity` / `Restart=always` / `RestartSec=5` / the network-online block / `[Install]`
    BYTE-IDENTICAL. The rendered unit after substituting `<NAME>=WeatherBot` (Description) /
    `<RUNTIME_DIR>=weatherbot` / `<REPO>` / `<USER>` MUST be byte-identical to today's `weatherbot.service`
    (modulo the additive PIDFile line if you add it — document it).

    Keep `deploy/weatherbot.service` in place (the concrete WeatherBot rendering / install artifact);
    the template is the new generic parameterized source. Document `<NAME>`/`<RUNTIME_DIR>` in
    `deploy/README.md` alongside `<REPO>`/`<USER>` with a one-line note that a different bot supplies its
    own values (the reusability proof, success criterion 4).
  </action>
  <verify>
    <automated>test -f deploy/bot.service.template && grep -q "<RUNTIME_DIR>" deploy/bot.service.template && grep -q "<NAME>" deploy/bot.service.template && grep -q "<RUNTIME_DIR>" deploy/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/bot.service.template` exists with `<NAME>` and `<RUNTIME_DIR>` placeholders on the identity lines.
    - `Type=notify`, `NotifyAccess=main`, `TimeoutStartSec=infinity`, `Restart=always` are present and unchanged.
    - `deploy/README.md` documents the two new placeholders.
    - Substituting <NAME>=WeatherBot, <RUNTIME_DIR>=weatherbot yields the byte-identical (modulo additive PIDFile) weatherbot.service content.
  </acceptance_criteria>
  <done>
    The systemd unit ships as a parameterized template with app-supplied identity; the WeatherBot render
    is byte-identical; the README documents the placeholders — a reminder bot could supply its own.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → daemon control | `weatherbot reload` sender signals the daemon PID; /proc guard defends PID recycling |
| app → module wiring | build_runtime injects app closures (health_check/on_fail/on_online/identity) into the module engines |
| install-time substitution | sed placeholders in the .service template are operator-supplied at install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Spoofing | reload sender PID guard | mitigate | generalized is_running_process keeps the argv0-basename + -m match (byte-identical) — SIGHUP never reaches a recycled/unrelated PID |
| T-25-05 | Elevation of privilege | .service template | mitigate | keep User=<USER> (non-root) + RuntimeDirectory ownership rationale; least-privilege preserved in the template |
| T-25-06 | Tampering | online ping string | accept | fixed literal, no user/template interpolation, no @everyone (markdown-injection-safe, T-05-T) — preserved verbatim in on_online hook |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies — pure move/re-point within existing packages |
</threat_model>

<verification>
- `pytest -q` exits 0 — the full suite stays green (BHV-01).
- No Phase-21 golden file is edited (BHV-02 verified in Plan 25-03).
- `SystemdNotifier` resolves to the identical module object via the shim.
- The default PID path is `/run/weatherbot/weatherbot.pid` and the marker is `b"weatherbot"`.
- run_daemon delegates construction to build_runtime and keeps the SIGTERM-before-gate / gate→start→READY / finally ordering.
- The .service template renders byte-identical for WeatherBot.
</verification>

<success_criteria>
WeatherBot wires the module at ONE composition root (build_runtime) that injects the health probe,
the config id-deriver / exactly-once key, the selected-location context, and render_embed/panel
cosmetics — zero duplicated module mechanism (APP-01). READY=1 is gated on the app probe and emitted
strictly after gate+start (SEAM-05). Identity is parameterized via a byte-identical default
LifecycleIdentity and the .service is a template (success criterion 4). The full suite is green; no
golden is touched.
</success_criteria>

<output>
Create `.planning/phases/25-lifecycle-ready-gate-composition-root/25-02-SUMMARY.md` when done.
Record: the build_runtime/run_daemon boundary (what moved vs stayed), whether the D-04 floor fallback
was needed, and any byte-identical risks investigated.
</output>
