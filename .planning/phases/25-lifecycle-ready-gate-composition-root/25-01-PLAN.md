---
phase: 25-lifecycle-ready-gate-composition-root
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yahir_reusable_bot/lifecycle/__init__.py
  - yahir_reusable_bot/lifecycle/sdnotify.py
  - yahir_reusable_bot/lifecycle/health.py
  - yahir_reusable_bot/lifecycle/identity.py
  - yahir_reusable_bot/lifecycle/ready_gate.py
  - tests/test_lifecycle_module.py
autonomous: true
requirements: [SEAM-05]
must_haves:
  truths:
    - "The new lifecycle module imports zero app code and names zero weather noun (litmus-clean, D-13 term set unchanged)."
    - "ReadyGate's re-probe loop is interruptible via stop.wait(interval) — never time.sleep — so a stop mid-probe breaks promptly."
    - "ReadyGate branches startup-log severity on HealthResult's NEUTRAL severity field, never by comparing reason to a weather-named string."
    - "ReadyGate emits READY=1 (notifier.ready()) and the structured online log ONLY; the durable health row, tick, and ping ride the injected on_online hook."
    - "A None hook is a no-op and a raising hook is logged + swallowed and never masks the gate result."
  artifacts:
    - path: "yahir_reusable_bot/lifecycle/sdnotify.py"
      provides: "SystemdNotifier (moved verbatim, pure-stdlib READY=1 wire)"
      contains: "class SystemdNotifier"
    - path: "yahir_reusable_bot/lifecycle/health.py"
      provides: "HealthResult DTO with neutral severity field"
      contains: "class HealthResult"
    - path: "yahir_reusable_bot/lifecycle/identity.py"
      provides: "LifecycleIdentity dataclass + is_running_process proc guard + write_pid_atomic/read_pid"
      contains: "class LifecycleIdentity"
    - path: "yahir_reusable_bot/lifecycle/ready_gate.py"
      provides: "ReadyGate engine — interruptible re-probe loop + READY emit + heartbeat tick"
      contains: "class ReadyGate"
    - path: "yahir_reusable_bot/lifecycle/__init__.py"
      provides: "lifecycle barrel exporting the public surface"
      exports: ["ReadyGate", "SystemdNotifier", "HealthResult", "LifecycleIdentity"]
  key_links:
    - from: "yahir_reusable_bot/lifecycle/ready_gate.py"
      to: "yahir_reusable_bot/lifecycle/health.py"
      via: "health_check callable returns HealthResult; gate branches on .severity"
      pattern: "HealthResult"
    - from: "yahir_reusable_bot/lifecycle/ready_gate.py"
      to: "yahir_reusable_bot/lifecycle/sdnotify.py"
      via: "notifier.ready() emits READY=1"
      pattern: "notifier\\.ready"
---

<objective>
Build the new `yahir_reusable_bot/lifecycle/` module package — the genuinely-reusable
process-lifecycle layer (SEAM-05) — with ZERO app behavior change. This plan creates the
module surface only; Plan 25-02 wires it into `run_daemon` and proves byte-identical behavior.

Purpose: capture the pitfall-dense, reusable triad (interruptible re-probe loop, READY=1 emit,
heartbeat tick) plus the parameterized filesystem identity in the module so a reminder bot
reuses it with zero weather assumptions. Mirrors the Phase-23/24 constructor-injection +
symmetric-best-effort-hook engine precedent (D-01, D-02).

Output: `lifecycle/{sdnotify,health,identity,ready_gate,__init__}.py` + a focused unit test
file. Every symbol clones an exact in-repo analog (25-PATTERNS.md).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-CONTEXT.md
@.planning/phases/25-lifecycle-ready-gate-composition-root/25-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (the drift-verification pass excludes them):
- `yahir_reusable_bot/lifecycle/` (new module package + barrel)
- `SystemdNotifier` (relocated into `lifecycle/sdnotify.py` — verbatim move)
- `HealthResult(ok, reason, detail, severity)` — module-owned generic DTO with a NEUTRAL severity field
- `Severity` / `Level` (the module-owned neutral enum or int convention `HealthResult.severity` carries)
- `LifecycleIdentity(name, pid_file, runtime_dir, console_name, proc_marker)` — immutable identity struct
- `is_running_process(pid, *, proc_marker)` — the generalized `/proc` staleness guard
- `write_pid_atomic` / `read_pid` (relocated/generalized pid helpers in `lifecycle/identity.py`)
- `ReadyGate` — the lifecycle engine (re-probe loop + READY emit + heartbeat tick)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move SystemdNotifier + create HealthResult + LifecycleIdentity/identity helpers</name>
  <files>yahir_reusable_bot/lifecycle/__init__.py, yahir_reusable_bot/lifecycle/sdnotify.py, yahir_reusable_bot/lifecycle/health.py, yahir_reusable_bot/lifecycle/identity.py</files>
  <read_first>
    - yahir_reusable_bot/lifecycle/ (does not exist — create the package dir)
    - weatherbot/ops/sdnotify.py (the WHOLE 56-line file — moves verbatim; already pure-stdlib + weather-clean)
    - weatherbot/ops/selfcheck.py L42-59 (the CheckResult dataclass + the PASS/NETWORK_NOT_READY/AUTH_FAILED vocabulary HealthResult generalizes — drop the constants, keep ok/reason/detail, ADD severity)
    - weatherbot/ops/pidfile.py (the WHOLE file — PID_FILE constant L37, write_pid_atomic L40-65, read_pid L68-77, is_weatherbot_pid L80-103, _argv_is_weatherbot L106-125, _read_proc_cmdline L128-139 — generalize the weatherbot literal + marker)
    - yahir_reusable_bot/config/holder.py (the frozen-dataclass / module-constant-with-override idiom)
    - yahir_reusable_bot/scheduler/__init__.py (the barrel docstring + explicit re-export + __all__ shape to clone)
  </read_first>
  <action>
    Create the `yahir_reusable_bot/lifecycle/` package.

    (a) `lifecycle/sdnotify.py`: move `weatherbot/ops/sdnotify.py` VERBATIM (the full file incl.
    `SystemdNotifier` with `_send`/`ready()`/`watchdog()`). No parameterize delta — it is already
    weather-clean and stdlib-only. Do NOT delete `weatherbot/ops/sdnotify.py` yet (Plan 25-02 turns
    it into a re-export shim so daemon imports stay byte-identical).

    (b) `lifecycle/health.py`: define a module-owned NEUTRAL severity convention plus the generic DTO.
    Add `HealthResult(ok: bool, reason: str, detail: str = "", severity: ... = ...)` as a
    `@dataclass(frozen=True)` mirroring `CheckResult`'s shape. `reason`/`detail` are OPAQUE passthrough
    the module logs but never compares. `severity` is the NEUTRAL field the gate branches log-level on —
    define it as a module-owned `Severity`/`Level` (an IntEnum or a small enum with at least the two
    rungs today's code needs: a CRITICAL rung and a WARNING rung) with a sensible default. Drop the
    weather-reason constants entirely. The module must NEVER name `auth_failed`/`network_not_ready`.

    (c) `lifecycle/identity.py`: define `LifecycleIdentity` as a `@dataclass(frozen=True)` with the FIVE
    INDEPENDENT fields `name: str`, `pid_file: Path`, `runtime_dir: ...`, `console_name: str`,
    `proc_marker: bytes` (D-03 — the four identity facts are NOT one string). Move + generalize the pid
    helpers from `pidfile.py`: keep `write_pid_atomic(pid_file)` and `read_pid(pid_file)` BYTE-IDENTICAL
    (temp+os.replace body, re-raise posture, FileNotFoundError/ValueError contract) but with no module
    default constant (the path is supplied by the caller / identity). Generalize the guard to
    `is_running_process(pid, *, proc_marker: bytes, cmdline_reader=None)` keeping the argv0-basename +
    `-m`-module match logic EXACTLY — replace the hardcoded `prog == "weatherbot"` with
    `prog == proc_marker.decode(...)` (or equivalent) and `b"weatherbot" in argv[1:4]` with
    `proc_marker in argv[1:4]`. Rename the internal `_argv_is_weatherbot` to a generic name
    (e.g. `_argv_matches_marker`) so it carries NO weather noun (Gate-3). Keep `_read_proc_cmdline`'s
    /proc-degrade behavior; its non-Linux degrade sentinel must return the supplied `proc_marker`
    (not a baked `b"weatherbot"`). Note: do not write the literal weather-named guard name into the new
    module — the litmus negative-greps `weatherbot` over `yahir_reusable_bot/**`.

    (d) `lifecycle/__init__.py`: clone the `scheduler/__init__.py` barrel shape — module docstring +
    explicit `from .x import Y` re-exports of `ReadyGate`, `SystemdNotifier`, `HealthResult`, the
    `Severity`/`Level` enum, `LifecycleIdentity`, `is_running_process`, `write_pid_atomic`, `read_pid`,
    and an explicit `__all__`. (`ReadyGate` is created in Task 2 — import it in the barrel after that
    task lands, or stub the import to be filled by Task 2.)
  </action>
  <verify>
    <automated>python -c "from yahir_reusable_bot.lifecycle import SystemdNotifier, HealthResult, LifecycleIdentity, is_running_process, write_pid_atomic, read_pid; print('ok')"</automated>
  </verify>
  <done>
    The lifecycle package imports cleanly; `SystemdNotifier` is verbatim; `HealthResult` has a neutral
    severity field and no weather-reason constants; `LifecycleIdentity` has five independent fields;
    `is_running_process(pid, *, proc_marker=...)` preserves the argv0/-m match logic; the new module
    contains no `weatherbot` literal in any name or path default.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReadyGate engine — clone the ReloadEngine injection + symmetric-hook + interruptible-loop recipe</name>
  <files>yahir_reusable_bot/lifecycle/ready_gate.py, yahir_reusable_bot/lifecycle/__init__.py, tests/test_lifecycle_module.py</files>
  <behavior>
    - A health_check that returns ok on the first call → gate returns True immediately, invokes on_online exactly once, calls notifier.ready() exactly once.
    - A health_check that returns not-ok then ok → gate loops, invokes the per-probe/on_fail hook on the failing outcome, waits on stop.wait(interval), then returns True on the second pass.
    - stop set during the wait → stop.wait returns True → gate breaks and returns False WITHOUT calling notifier.ready() or on_online.
    - A failing outcome with severity=CRITICAL logs at critical level; severity=WARNING logs at warning level — branched on HealthResult.severity, NOT on reason string.
    - on_online raising → logged + swallowed; gate still returns True and notifier.ready() still fired (best-effort hook never masks the gate result).
    - on_fail/on_probe is None → no-op (gate still loops/returns correctly).
  </behavior>
  <read_first>
    - yahir_reusable_bot/config/reload.py L74-104 (the ReloadEngine.__init__ injection shape: collaborators positional, hooks keyword-only `| None = None`, store by reference)
    - yahir_reusable_bot/config/reload.py L312-326 (_best_effort_hook — copy VERBATIM)
    - weatherbot/scheduler/daemon.py L1108-1156 (gate_until_healthy — the interruptible re-probe loop body to lift; stop.wait NOT time.sleep is load-bearing, Pitfall 2)
    - weatherbot/scheduler/daemon.py L1159-1189 (emit_online — the five-part bundle; the module keeps only notifier.ready() + the structured log, parts 1/2/5 ride on_online)
    - weatherbot/scheduler/daemon.py L102-133 (HEARTBEAT_INTERVAL_S=600, RE_PROBE_INTERVAL_S=120 constants to lift as defaults)
    - weatherbot/scheduler/daemon.py L1446-1451 (the __heartbeat__ IntervalTrigger registration the gate may own — D-01 option a — or leave app-side, option d)
    - yahir_reusable_bot/lifecycle/health.py (HealthResult + the neutral severity enum from Task 1)
    - yahir_reusable_bot/scheduler/engine.py (SchedulerEngine.register signature, if the gate holds a handle for the heartbeat tick)
  </read_first>
  <action>
    Create `ReadyGate` in `lifecycle/ready_gate.py`, cloning the `ReloadEngine` recipe.

    Constructor (names are planner discretion per CONTEXT D-01): `health_check: Callable[[], HealthResult]`
    and `notifier: SystemdNotifier` positionally; keyword-only `re_probe_interval: float = RE_PROBE_INTERVAL_S`
    (lift the 120 default), `on_online: Callable[..., None] | None = None`, and an
    `on_fail`/`on_probe: Callable[[HealthResult], None] | None = None`. Store every collaborator by
    reference; never inspect the injected callable beyond its documented contract.

    Re-probe loop method (lift `gate_until_healthy` VERBATIM, replacing the three weather touches):
    `while not stop.is_set():` → call `self._health_check()` (NOT run_self_check) → invoke the
    per-outcome `on_fail`/`on_probe` hook with the HealthResult (the app stamps the durable health row
    there, D-02a) → `if result.ok: <break to online path / return True>`. On a non-ok result, branch the
    startup log on `result.severity` (CRITICAL rung → `_log.critical`, else `_log.warning`) — NEVER
    compare to a weather-named reason string (D-02 CRITICAL delta). Wait interruptibly with
    `if stop.wait(self._re_probe_interval): break` — NEVER `time.sleep` (Pitfall 2). Return `True` on
    pass, `False` if stop was set first.

    Online emit: when the probe first passes, the module owns ONLY (4) `self._notifier.ready()` and the
    structured online log — rename the event key so it carries no weather noun (NOT `"weatherbot online"`,
    e.g. `"bot online"`/`"ready"`). Parts (1) stamp health row, (2) stamp tick, (3)/(5) Discord ping ride
    the `on_online` hook (app-side, D-02a). PRESERVE the emit ordering exactly (the real byte-identical
    risk): invoke `on_online` and `notifier.ready()` + log at the same relative points
    `emit_online` does today.

    Copy `ReloadEngine._best_effort_hook` (L312-326) VERBATIM and route every `on_online`/`on_fail`
    invocation through it: a None hook is a no-op, a raise is logged + swallowed, never masks the gate
    result.

    Heartbeat tick: per D-01 the gate MAY hold a `SchedulerEngine` handle and own the `__heartbeat__`
    IntervalTrigger registration (option a), OR omit it and let the app re-register the tick via the
    one-liner `SchedulerEngine.register("__heartbeat__", ...)` (sanctioned option d). Pick whichever keeps
    `run_daemon` byte-identical in Plan 25-02 — document the choice in the SUMMARY. The tick callable
    itself (`_heartbeat_tick` — a DB touch) stays an injected app callable either way; the module names
    no app job id.

    Then update `lifecycle/__init__.py` to export `ReadyGate`.

    Write `tests/test_lifecycle_module.py` covering all six behaviors above with a fake health_check
    (returns scripted HealthResults), a recording fake notifier, a threading.Event for stop, and
    recording hook spies. Assert: exactly-once notifier.ready(); on_online fired once on pass; gate
    returns False + no ready() when stop preempts; severity-branched log level; best-effort swallow on a
    raising hook; None on_fail is a no-op.
  </action>
  <verify>
    <automated>pytest -q tests/test_lifecycle_module.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q tests/test_lifecycle_module.py` exits 0 with all six behaviors covered.
    - `grep -n "time.sleep" yahir_reusable_bot/lifecycle/ready_gate.py` returns nothing (interruptible stop.wait only).
    - `grep -nE "auth_failed|network_not_ready" yahir_reusable_bot/lifecycle/ready_gate.py` returns nothing (severity-branched, not reason-string-branched).
    - `grep -nE "weather|forecast|location|openweather|\\buv\\b|briefing" yahir_reusable_bot/lifecycle/ready_gate.py | grep -v '^[0-9]*: *#'` returns only incidental/prose hits, never a name.
  </acceptance_criteria>
  <done>
    `ReadyGate` exists with constructor-injected health_check + notifier + symmetric best-effort hooks;
    the re-probe loop is interruptible (no time.sleep); severity branches on the neutral field; the
    module owns only notifier.ready() + the noun-free online log; the barrel exports it; the unit test
    file is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| systemd ↔ daemon | READY=1 datagram over $NOTIFY_SOCKET; readiness must not be load-bearing for liveness |
| app ↔ module | health_check + hooks are injected app callables the module invokes opaquely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | injected hooks (on_online/on_fail) | mitigate | best-effort guard swallows + logs a raising hook; never masks gate result (copied verbatim from ReloadEngine) |
| T-25-02 | Denial of Service | re-probe loop | mitigate | stop.wait(interval) interruptible — a systemctl stop mid-probe breaks promptly; no time.sleep (Pitfall 2) |
| T-25-03 | Information disclosure | HealthResult.detail logging | accept | detail is outcome-only (status code / exception class name), never a secret — preserved from CheckResult contract (T-04-01) |
| T-25-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies — pure move/parameterize within existing stdlib + module surface |
</threat_model>

<verification>
- `pytest -q tests/test_lifecycle_module.py` exits 0.
- The lifecycle package imports cleanly with no `weatherbot` literal in any name/path default.
- No `time.sleep` in `ready_gate.py`; severity branched on the neutral field, not a reason string.
- The existing suite is NOT yet expected to exercise the new module (wiring lands in 25-02) — but it must still be GREEN (no regression from creating the new package): `pytest -q` exits 0.
</verification>

<success_criteria>
The reusable lifecycle layer exists as a clean module package: SystemdNotifier moved verbatim,
HealthResult with a neutral severity field, LifecycleIdentity with five independent fields, a
marker-parameterized proc guard, and a ReadyGate engine that owns the interruptible re-probe loop +
READY emit (and optionally the heartbeat tick) with all weather/DB/Discord touches behind injected
best-effort hooks. Litmus-clean. Zero app behavior change.
</success_criteria>

<output>
Create `.planning/phases/25-lifecycle-ready-gate-composition-root/25-01-SUMMARY.md` when done.
Record: the chosen severity representation (enum vs int), the heartbeat-handle choice (option a vs d),
and the generalized guard name.
</output>
