---
phase: 10-file-watch-auto-reload
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_filewatch.py
autonomous: true
requirements: [CFG-03]
must_haves:
  truths:
    - "All eight Phase-10 node IDs COLLECT under `uv run pytest tests/test_filewatch.py --collect-only` (no collection crash)"
    - "Every test fails RED with a real ModuleNotFoundError/AttributeError (no green-but-hollow scaffold)"
    - "Each SC#1-4 maps to a named, runnable test node"
  artifacts:
    - path: "tests/test_filewatch.py"
      provides: "Executable RED contract for the file-watch observer (SC#1-4 + idempotence + toggle + .env-filter + watch-set re-derive)"
      contains: "def test_save_triggers_reload"
      min_lines: 120
  key_links:
    - from: "tests/test_filewatch.py"
      to: "weatherbot.scheduler.daemon._run_watch_observer"
      via: "per-test deferred import wrapper"
      pattern: "from weatherbot.scheduler.daemon import _run_watch_observer"
---

<objective>
Write the Wave-0 Nyquist RED scaffold `tests/test_filewatch.py` — the executable contract that Plans 10-02 and 10-03 turn green. It references the not-yet-built observer symbols (`_run_watch_observer`, `_derive_watch_dirs`, `_make_watch_filter`) and the not-yet-added `Config.reload.watch` toggle through PER-TEST deferred-import wrappers, so all node IDs COLLECT while each fails RED.

Purpose: Lock SC#1-4 + idempotence + toggle + `.env`-filter + watch-set re-derive into runnable tests BEFORE the wiring exists, mirroring the Phase 8/9 Wave-0 lesson (a top-level import of an absent symbol crashes collection and hides every node ID).
Output: `tests/test_filewatch.py` (RED), CFG-03's verification harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-file-watch-auto-reload/10-CONTEXT.md
@.planning/phases/10-file-watch-auto-reload/10-RESEARCH.md
@.planning/phases/10-file-watch-auto-reload/10-PATTERNS.md
@tests/test_reload.py
</context>

<artifacts_this_phase_produces>
This plan (10-01) produces:
- `tests/test_filewatch.py` (NEW) — eight RED test nodes:
  `test_save_triggers_reload` (SC#1), `test_editor_save_patterns_one_reload` (SC#2),
  `test_fd_stable_and_clean_teardown` (SC#3), `test_invalid_save_keeps_old_config` (SC#4),
  `test_identical_save_zero_job_changes` (idempotence), `test_watch_toggle_off_no_observer` (D-03),
  `test_env_save_never_reloads` (Pitfall #12), `test_watch_set_rederived_on_reload` (D-04).
- Local helpers in that file: `truncate_write`, `temp_then_rename`, `multi_event_burst`, and the
  deferred-import wrappers for `_run_watch_observer` / `_derive_watch_dirs` / `_make_watch_filter`.

(Production symbols — `ReloadConfig`, `Config.reload`, `_run_watch_observer`, `_derive_watch_dirs`,
`_make_watch_filter`, `request_reload`, `WATCH_QUIET_MS`/`WATCH_DEBOUNCE_MS`/`WATCH_RUST_TIMEOUT_MS`,
the `[reload] watch` toggle, the `watchfiles` dependency — are produced by Plans 10-02 and 10-03.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold tests/test_filewatch.py with deferred-import wrappers and editor-save helpers</name>
  <files>tests/test_filewatch.py</files>
  <read_first>
    - tests/test_reload.py (THE analog — copy the module docstring rationale, the per-test deferred-import wrapper idiom at lines 46-72, the local `_loc`/`_cfg`/`_slot` builders at ~83-95, and the imports block at ~35-40 verbatim where applicable)
    - .planning/phases/10-file-watch-auto-reload/10-RESEARCH.md (Validation Architecture → Test Map for the eight node names + automated commands; "How to make each testable" for the Mock/counter trigger seam, the `psutil.Process().num_fds()` fd-delta approach, and the bounded-wait flake control)
    - .planning/phases/10-file-watch-auto-reload/10-PATTERNS.md (tests/test_filewatch.py section — required test coverage list, the deferred-import equivalents, the local config builders to copy verbatim)
  </read_first>
  <action>
    Create tests/test_filewatch.py mirroring tests/test_reload.py's Wave-0 structure. Open with a module docstring explaining these are RED until Plans 10-02/10-03 land, and that the not-yet-built symbols are referenced through per-test deferred imports so all node IDs COLLECT.

    Add three deferred-import wrappers (NOT module-top imports), each importing inside its body from weatherbot.scheduler.daemon: `_run_watch_observer`, `_derive_watch_dirs`, `_make_watch_filter`. Reuse test_reload.py's `_loc`/`_cfg`/`_slot` local builders and its imports (`from weatherbot.config import Config, Location`; `from weatherbot.config.holder import ConfigHolder`; `from weatherbot.config.models import Schedule`) — no new shared fixtures.

    Add three editor-save helpers driven by `tmp_path`: `truncate_write(path, text)` (open mode "w", write in two flushes), `temp_then_rename(path, text)` (write `path.with_suffix(".tmp")` then `os.replace`), `multi_event_burst(path, text)` (N rapid writes). Use a Mock/counter as the injected `request_reload` seam so SC#1/SC#2 assert the trigger-fire count WITHOUT standing up `_do_reload`. Use bounded `reload_requested.wait(timeout=2.0)` over `sleep`; drive the observer with `rust_timeout=500` (WATCH_RUST_TIMEOUT_MS) so no test blocks 5s.

    Write the eight test nodes named EXACTLY per RESEARCH Test Map: test_save_triggers_reload, test_editor_save_patterns_one_reload, test_fd_stable_and_clean_teardown, test_invalid_save_keeps_old_config, test_identical_save_zero_job_changes, test_watch_toggle_off_no_observer, test_env_save_never_reloads, test_watch_set_rederived_on_reload. The SC#4 test (`test_invalid_save_keeps_old_config`) wires the REAL `reload_requested` Event + real `_do_reload` against a temp config and asserts `holder.current()` is unchanged after an invalid save (reuse test_reload.py builders — no mock that always passes, per T-09-01). The SC#3 test asserts `psutil.Process().num_fds()` delta is within a small constant across >=50 inode-swapping saves (use `temp_then_rename`), then `stop.set()` and assert `thread.join(timeout=2.0)` returns with `is_alive()` False; the >=50 saves MUST include at least one watch-set-changing reload (Assumption A4 — fd stability across re-derivation, not just plain saves). The `test_watch_toggle_off_no_observer` asserts that with `[reload] watch = false` no observer thread is started AND that SIGHUP/explicit reload still works. `test_env_save_never_reloads` writes a `.env` in the same dir and asserts ZERO reloads (Pitfall #12). NO fenced implementation here — write real pytest code in the file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_filewatch.py --collect-only -q 2>&1 | grep -v '^#' | grep -c 'test_filewatch.py::test_'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_filewatch.py --collect-only -q` lists exactly 8 node IDs and does NOT error at collection (deferred imports keep collection green).
    - `uv run pytest tests/test_filewatch.py -x` FAILS RED — the first failing test errors with a real ModuleNotFoundError/AttributeError/ImportError from the absent observer symbol or the absent `Config.reload` field (NOT an AssertionError on a stub that always passes).
    - The eight node names match RESEARCH's Test Map exactly (grep each name present in the file).
    - The three editor-save helpers (`truncate_write`, `temp_then_rename`, `multi_event_burst`) and the deferred-import wrappers for `_run_watch_observer`/`_derive_watch_dirs`/`_make_watch_filter` are all present (grep).
    - No top-level `from weatherbot.scheduler.daemon import _run_watch_observer` — the import lives inside a wrapper body (grep: the import string appears only inside a `def`).
  </acceptance_criteria>
  <done>tests/test_filewatch.py collects 8 node IDs and fails RED on real missing-symbol errors; the SC#4 node uses the real `_do_reload`/`holder` path, not a pass-through mock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator edit → watched dir | The only "input" is a local file save by the OS user running the daemon; no network/external actor. This plan only asserts behaviors; it adds no runtime surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering / DoS | half-written file parsed (asserted here) | mitigate | `test_editor_save_patterns_one_reload` pins exactly-ONE-reload-never-half-parse (SC#2); `test_invalid_save_keeps_old_config` pins keep-old (SC#4). |
| T-10-02 | Information disclosure | `.env` accidentally watched | mitigate | `test_env_save_never_reloads` asserts ZERO reloads on a `.env` edit (Pitfall #12). |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan (test-only file). The `watchfiles` install + legitimacy gate live in Plan 10-02. |
</threat_model>

<verification>
- `uv run pytest tests/test_filewatch.py --collect-only -q` → 8 node IDs, no collection error.
- `uv run pytest tests/test_filewatch.py -x` → RED with a real missing-symbol error.
- Full suite (`uv run pytest`) still green EXCEPT the new RED file (expected at Wave 0).
</verification>

<success_criteria>
The RED contract exists and is runnable: 8 collected nodes, all failing on genuine absence of the Plan 10-02/10-03 symbols, with SC#1-4 + idempotence + toggle + `.env`-filter + watch-set re-derive each represented by a named node.
</success_criteria>

<output>
Create `.planning/phases/10-file-watch-auto-reload/10-01-SUMMARY.md` when done.
</output>
