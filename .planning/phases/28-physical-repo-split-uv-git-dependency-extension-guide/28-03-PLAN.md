---
phase: 28-physical-repo-split-uv-git-dependency-extension-guide
plan: 03
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - "weatherbot/cli.py"
  - "tests/test_module_provenance.py (NEW)"
autonomous: true
requirements: [PKG-02]
must_haves:
  truths:
    - "At daemon startup the bot logs a provenance line reporting the deployed module version + git sha"
    - "The sha is read from the installed module's PEP 610 direct_url.json (vcs_info.commit_id) via importlib.metadata — surviving a clean-venv git install"
    - "An editable install is distinguished from a real git deploy via the dir_info.editable flag (a dev-tree tripwire)"
    - "The provenance reader is proven by a self-disciplined unit test feeding both the git-install and editable-install direct_url.json shapes"
  artifacts:
    - path: "weatherbot/cli.py"
      provides: "startup-version-log line + _module_provenance() reader (D-06)"
      contains: "direct_url.json"
    - path: "tests/test_module_provenance.py"
      provides: "Unit test proving the provenance reader returns the embedded sha and flags editable installs"
      exports: ["test_*"]
  key_links:
    - from: "weatherbot/cli.py"
      to: "installed yahir-reusable-bot dist-info"
      via: "Distribution.from_name(\"yahir-reusable-bot\").read_text(\"direct_url.json\")"
      pattern: "direct_url.json"
---

<objective>
Wire the startup-version-log (D-06): at daemon startup, log a structured provenance line reporting the *deployed* module version + git sha, read from the installed module's PEP 610 `direct_url.json` via `importlib.metadata` (the field shape confirmed empirically by the 28-01 spike). This is the line SC#3 verifies on the live host ("runs against the pinned module sha"), and its `dir_info.editable` flag is a free dev-tree-vs-deploy tripwire. Pair it with a self-disciplined unit test.

Purpose: Makes the two-repo deploy auditable — the startup log announces which sha is running, checkable against the promotion ledger (28-04). Reading `direct_url.json` (not `uv.lock`, not git) is the mechanism that survives a clean-venv install and reports the deployed value, not a dev value (D-06 discretion, RESEARCH Pattern 4).
Output: A `_module_provenance()` reader + a once-per-boot startup-version-log line in `weatherbot/cli.py`; a new `tests/test_module_provenance.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-RESEARCH.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-PATTERNS.md
@.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-01-SUMMARY.md

# The insertion site + the house logging idiom (read the run-command path lines 936-951):
@weatherbot/cli.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _module_provenance() + the startup-version-log line to cli.py (D-06)</name>
  <read_first>
    - 28-01-SUMMARY.md — the CONFIRMED `direct_url.json` field names from the Task-1 spike (use the OBSERVED names, not the assumed ones if they differed)
    - 28-PATTERNS.md § "startup-version-log line in weatherbot/cli.py" — the insertion-point analog (`wiring.py:304` READY log / the `cli.py` run-command path lines 936-951), the structlog house idiom (`_log.info("event", key=value)`, outcome-only, never secrets), and the `_module_provenance()` core pattern
    - 28-RESEARCH.md § "Pattern 4: Runtime deployed-sha" — why direct_url.json (not uv.lock at runtime, not hatch-vcs); the editable flag as a dev-tree tripwire
  </read_first>
  <behavior>
    - `_module_provenance()` returns a dict with `module_version`, `module_sha` (the resolved commit sha), `module_ref` (the tag), and `editable` ("True"/"False") read from the installed `yahir-reusable-bot` dist-info `direct_url.json`.
    - On a git install the dict carries the exact sha and tag; on an editable install `editable == "True"` and the sha may be empty.
    - If `direct_url.json` is absent (e.g. a wheel install with no direct-url record), the function returns the version with empty sha/ref rather than raising — startup must not crash on a provenance read.
    - The startup-version-log line fires once per boot at the daemon `run` path and logs ONLY version/sha/ref/editable — never a secret (no webhook URL, no appid).
  </behavior>
  <files>weatherbot/cli.py</files>
  <action>
    Add a module-level `_module_provenance() -> dict[str, str]` helper to `weatherbot/cli.py` following the 28-PATTERNS core pattern: `Distribution.from_name("yahir-reusable-bot").read_text("direct_url.json")`, parse the JSON, return `module_version` (from `importlib.metadata.version`), `module_sha` (`vcs_info.commit_id`), `module_ref` (`vcs_info.requested_revision`), `editable` (`str(dir_info.editable)`) — using the EXACT field names confirmed by the 28-01 spike. Guard the read so a missing/empty `direct_url.json` yields empty sha/ref (never raises — a provenance read must not crash startup). Insert a `_log.info("module provenance", **_module_provenance())` line in the `run` command path (cli.py ~lines 936-951, after `_configure_logging` has run and before/at daemon launch) so it fires once per boot — the natural READY moment analog to `wiring.py:304`. Use the existing `_log = structlog.get_logger(__name__)` (line 62). Log NO secret (T-04-01 house rule — the provenance line carries only version/sha/ref/editable). Reuse `importlib.metadata` (stdlib, no new dep).
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/WeatherBot &amp;&amp; uv run pytest tests/test_module_provenance.py -q 2>&amp;1 | tail -3</automated>
  </verify>
  <done>`weatherbot/cli.py` has `_module_provenance()` reading the installed module's `direct_url.json` and a once-per-boot `_log.info("module provenance", ...)` line in the run path; it logs only version/sha/ref/editable and never raises on a missing record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit-test _module_provenance() against both direct_url.json shapes (self-proof discipline)</name>
  <read_first>
    - 28-PATTERNS.md § "unit test for _module_provenance() (NEW)" — mock `Distribution.from_name`/`read_text`; feed the git-install shape (assert it returns the embedded sha) and the editable-install shape (`dir_info.editable=true`, the format VERIFIED in the live venv) and assert `editable == "True"`
    - 28-PATTERNS.md § "Self-proof discipline" — a guard is only trustworthy if a deliberately-constructed input drives it
    - 28-01-SUMMARY.md — the observed git-install JSON shape to use as the test fixture
  </read_first>
  <files>tests/test_module_provenance.py</files>
  <action>
    Author `tests/test_module_provenance.py` following the self-proof discipline. Mock `importlib.metadata.Distribution.from_name` (and `version`) so the test never depends on a live install. Test 1 (git-install shape): feed a stub `direct_url.json` carrying `vcs_info.commit_id` = a fixture sha + `requested_revision` = "v0.1.0" (use the OBSERVED shape from the 28-01 spike SUMMARY); assert `_module_provenance()` returns that sha and ref and `editable == "False"`. Test 2 (editable-install shape): feed `{"url": "file://...", "dir_info": {"editable": true}}`; assert `editable == "True"` and the sha is empty (the dev-tree tripwire). Test 3 (missing record): `read_text` returns None; assert the function returns the version with empty sha/ref and does NOT raise (startup-safety). Keep the assertions byte-precise on the dict keys/values the startup line logs.
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/WeatherBot &amp;&amp; uv run pytest tests/test_module_provenance.py -q 2>&amp;1 | tail -3</automated>
  </verify>
  <done>`tests/test_module_provenance.py` proves `_module_provenance()` returns the embedded sha/ref on a git-install shape, flags `editable == "True"` on an editable shape, and returns empty sha (no raise) on a missing record. All three tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed dist-info → startup log | the provenance read crosses from the install record to a log line a human audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08 | Information Disclosure | the startup-version-log line leaking a secret | mitigate | The provenance line logs ONLY module version/sha/ref/editable (structlog kwargs); it never touches the webhook URL, appid, or bot token (house rule T-04-01). Verified by reading the logged dict in Task 2. |
| T-28-09 | Repudiation | a deploy running an unaudited sha | mitigate | The startup-version-log announces the deployed sha, checkable against the promotion ledger (28-04); the `editable: true` flag visibly distinguishes a dev-tree overlay from a pinned deploy (Pitfall 1 warning sign). |
| T-28-10 | DoS | a malformed/missing direct_url.json crashing startup | mitigate | `_module_provenance()` guards the read — a missing/empty record yields empty sha/ref rather than raising (Task 1 behavior + Task 2 Test 3). |
</threat_model>

<verification>
- `weatherbot/cli.py` has `_module_provenance()` reading `direct_url.json` via `importlib.metadata` and a once-per-boot startup-version-log line in the run path.
- `tests/test_module_provenance.py` is green (git-install sha, editable tripwire, missing-record safety).
- The full suite still passes (`uv run pytest`) with the new line in place.
- The provenance line logs no secret (only version/sha/ref/editable).
</verification>

<success_criteria>
The bot logs a startup provenance line reporting the deployed module version + git sha read from PEP 610 `direct_url.json` (surviving a clean-venv git install), the `editable` flag distinguishes a dev tree from a deploy, and a self-disciplined unit test proves the reader against both install shapes (SC#3 mechanism — automatable portion).
</success_criteria>

<output>
Create `.planning/phases/28-physical-repo-split-uv-git-dependency-extension-guide/28-03-SUMMARY.md` when done. RECORD the startup-log event key + the exact provenance dict keys, and note that the live-host display of this line is the deferred Gate-2 confirmation (28-04).
</output>
