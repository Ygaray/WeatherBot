---
phase: 35-cleanup-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dead_code_removed.py
autonomous: true
requirements:
  - HARD-CLEAN-01
must_haves:
  truths:
    - "A negative-grep pytest gate exists that fails if any of the dead symbols slated for removal (F16 emit_online/_do_reload, F46 _argv_is_weatherbot, F76 run_weather verbose param, F92 discarded is_transient call) reappears in the production tree (D-05)."
    - "The gate reads production source as text and asserts absence; it does NOT import the daemon or run behavior, so it stays green even while the removal plans (02/03/08) have not yet landed — it reddens only after a removed symbol drifts back (analog test_import_hygiene.py:104-119)."
    - "The gate's own source carries no self-invalidating literal of the forbidden tokens (tokens built from parts, per the test_import_hygiene precedent), so a grep over tests/ cannot false-trip."
  artifacts:
    - "tests/test_dead_code_removed.py — consolidated Wave-0 negative-grep gate"
  key_links:
    - "tests/test_import_hygiene.py:104-119 — the negative-grep analog whose token-from-parts pattern this reuses"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/ (hub is out of scope, human-gated)."
    - statement: "The gate must not import weatherbot.scheduler.daemon at module top (a hard import would red-collect before the removals land); it reads source as text only."
    - statement: "No silent drop: the gate covers exactly the four removal targets and no accepted/verify-only finding is smuggled in as a removal."
---

<objective>
Author the Wave-0 negative-grep gate `tests/test_dead_code_removed.py` that pins the dead symbols removed by Plans 02/03/08 as staying gone (F16, F46, F76, F92 — D-05). This is the sampling scaffold: it must exist before any removal lands so drift-back is loud, and it must stay green today (removals not yet applied) because it reads source and asserts *the symbol is absent OR the file does not yet exist in removed form* — see the "start-state green" rule below.

Purpose: SC-1 (HARD-CLEAN-01) needs a durable guard that the removed dead code does not creep back, mirroring how `test_import_hygiene.py` keeps `_local_date_iso` from being re-added.
Output: `tests/test_dead_code_removed.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author consolidated dead-code negative-grep gate</name>
  <files>tests/test_dead_code_removed.py</files>
  <read_first>
    - tests/test_import_hygiene.py:100-125 (the negative-grep analog: token built from parts so the test's own source carries no literal forbidden token; reads production source via `_APP_ROOT / rel`)
    - .planning/WHOLE-PROJECT-REVIEW.md §Cleanup/§Low entries for F16, F46, F76, F92 (the removal targets)
    - weatherbot/scheduler/daemon.py (confirm the exact def tokens `def emit_online(` and `def _do_reload(` — targets of F16, Plan 08)
    - weatherbot/ops/pidfile.py:124 (`def _argv_is_weatherbot(` — F46, Plan 02)
    - weatherbot/ops/selfcheck.py:142 (the discarded `is_transient(exc)` line — F92, Plan 02)
    - weatherbot/cli.py:334 (`run_weather` `verbose:` param — F76, Plan 03)
  </read_first>
  <behavior>
    - After Plans 02/03/08 land, the gate FAILS if any removed token reappears in the production tree.
    - Today (removals not yet applied) the gate GREENS via the start-state rule below: each assertion is written as "symbol is confirmed present at a KNOWN pre-removal location OR absent" — i.e. author the gate so it asserts the post-removal invariant only over the surface that will change, using the same start-state-green posture the executor expects (the removal plan flips it to enforcing). Concretely: build the forbidden tokens from parts (no literal), then assert absence; scope the daemon.py assertions behind a marker comment the F16 plan (08) removes, so the gate is inert-green until 08 deletes the defs. Prefer the simplest form that is green at HEAD and reddens on drift-back after removal.
    - The gate never imports daemon/selfcheck/pidfile/cli at module top (text read only).
  </behavior>
  <action>
    Create `tests/test_dead_code_removed.py`. Reuse the `test_import_hygiene.py` token-from-parts idiom: construct each forbidden token by concatenation (e.g. `"def " + "emit_online("`) so this test's own source is not a self-match for a grep over `tests/`. Read each production file's text via a repo-root-relative Path (mirror `_APP_ROOT` in the analog). Assert:
    (a) F46 — `_argv_is_weatherbot` token absent from `weatherbot/ops/pidfile.py` AND from `tests/` (its exclusive test is removed by Plan 02);
    (b) F76 — the `verbose` param token absent from the `run_weather` signature in `weatherbot/cli.py` (scope to the `run_weather` def region so the unrelated `-v/--verbose` argparse flag and `main()` plumbing are not matched — region-scope the read);
    (c) F92 — the bare discarded `is_transient(exc)` call token absent from `weatherbot/ops/selfcheck.py` (the classifier import/use inside `is_auth_failure` branches is NOT the target — assert only the standalone discarded-result line is gone);
    (d) F16 — `def emit_online(` and `def _do_reload(` tokens absent from `weatherbot/scheduler/daemon.py`.
    For each target that has not yet been removed at author-time, write the assertion in the start-state-green posture the plan-execute workflow expects for Wave-0 gates (the removal plan is what flips it to enforcing). Add a header docstring naming `# HARD-CLEAN-01 / F16,F46,F76,F92 (D-05)`. Add an `<!-- planner-discipline-allow -->` marker only if a literal target token must appear; prefer token-from-parts so none does.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dead_code_removed.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_dead_code_removed.py -q` exits 0 at HEAD (removals not yet applied).
    - `grep -c "def emit_online(\|def _do_reload(\|_argv_is_weatherbot" tests/test_dead_code_removed.py` returns 0 (no self-invalidating literal — tokens built from parts).
    - The file imports nothing from `weatherbot.scheduler.daemon` at module top: `! grep -qE "^from weatherbot.scheduler.daemon import|^import weatherbot.scheduler.daemon" tests/test_dead_code_removed.py`.
    - Test docstring/name cites `HARD-CLEAN-01` and the four finding ids.
  </acceptance_criteria>
  <done>The consolidated negative-grep gate exists, is green at HEAD, self-match-free, and will redden if any of the four removed symbols drifts back after Plans 02/03/08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | This plan adds a source-reading test only; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01-01 | Tampering | test-only source-read gate | low | accept | No production code, no I/O, no network; text-read of tracked source files only. No mitigation required. |
</threat_model>

<verification>
- `uv run pytest tests/test_dead_code_removed.py -q` exits 0 at HEAD.
- `uv run pytest -q` still exits 0 (878 baseline; trust exit code over the syrupy "2 snapshots failed" line).
- No `yahir_reusable_bot/` or `../Reusable/` file in the diff.
</verification>

<success_criteria>
The dead-code drift-back gate exists and is green, ready for Plans 02/03/08 to remove the symbols and flip it to enforcing.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-01-SUMMARY.md` when done.
</output>
