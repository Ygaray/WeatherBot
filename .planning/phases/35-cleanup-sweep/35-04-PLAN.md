---
phase: 35-cleanup-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/config/models.py
  - weatherbot/config/loader.py
  - tests/test_config.py
autonomous: true
requirements:
  - HARD-CLEAN-02
must_haves:
  truths:
    - "The HH:MM schedule-time validator rejects non-canonical strings like `+9:30` and ` 9:30` (leading-sign / leading-space int-parseable oddities), because the raw time string is a job-id / sent-log key and must be canonical (F74, D-01 fix, D-06 behavior-CHANGING → regression test required)."
    - "`resolve_location` matches a location by `id` first, then falls back to case-insensitive `name`, so `--send-now <id>` succeeds when id != name; name matching is never removed, only additive (F75, D-01 fix, D-06 behavior-CHANGING → regression test)."
    - "Each behavior-changing fix ships with a regression test that fails against pre-fix behavior and passes against the fix, reusing the rename-safe `_loc(name, id=...)` fixture shape from 34-PATTERNS.md, and asserts the briefing invariant is untouched where relevant (D-06)."
  artifacts:
    - "weatherbot/config/models.py (F74 tightened HH:MM validator)"
    - "weatherbot/config/loader.py (F75 id-then-name resolve_location)"
    - "tests/test_config.py (F74 + F75 regression tests)"
  key_links:
    - "config `time` string → APScheduler job-id / sent_log key — why a non-canonical HH:MM must be rejected at load (F74)"
    - "resolve_location ← --send-now / interactive default resolution — the id-stable-identity lookup path (F75)"
  prohibitions:
    - statement: "No task edits any file under yahir_reusable_bot/ or ../Reusable/."
    - statement: "F75 must be additive: never remove case-insensitive name matching; add id matching FIRST, then fall back to name, so every existing `except ValueError` / UnknownLocationError caller stays green (Pitfall 5 in loader.py docstring)."
    - statement: "Do not weaken the existing `_hhmm` acceptance of canonical `HH:MM` 24-hour strings; only ADD rejection of non-canonical int-parseable oddities (must not break valid configs)."
---

<objective>
Sweep the config cluster with two behavior-changing fixes, each with a D-06 regression test: tighten the HH:MM validator to reject non-canonical strings (F74) and widen `resolve_location` to match id-then-name (F75). Both are low-risk, both reuse existing rename-safe fixtures.

Purpose: SC-2 (HARD-CLEAN-02) — resolve low-severity latent findings with regression coverage.
Output: tightened validator + widened lookup + two regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-cleanup-sweep/35-RESEARCH.md
@.planning/phases/34-test-gap-backfill/34-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten HH:MM validator to reject non-canonical strings (F74)</name>
  <files>weatherbot/config/models.py, tests/test_config.py</files>
  <read_first>
    - weatherbot/config/models.py:57-67 (`_hhmm` field_validator: splits on ":", int-parses, checks `len(hh)==2 and len(mm)==2 and 0<=h<=23 and 0<=m<=59` — but `int(" 9")`/`int("+9")` slip through when the length check is bypassed by 2-char oddities like `+9`)
    - tests/test_config.py (existing config-model tests — slot the F74 regression test alongside the schedule-validation tests already there)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entry F74
  </read_first>
  <behavior>
    - `Schedule(time="09:30")` and other canonical 24-hour `HH:MM` strings still validate (must not regress).
    - `Schedule(time="+9:30")` raises ValueError (leading-sign non-canonical).
    - `Schedule(time=" 9:30")` raises ValueError (leading-space non-canonical).
    - `Schedule(time="9:30")` (single-digit hour) continues to raise (already covered by len==2 check — confirm).
  </behavior>
  <action>
    Tighten `_hhmm` so the hour and minute components must be all-digit canonical two-character strings before int-parsing — reject any component that `int()` would accept but that is not strictly `[0-9][0-9]` (e.g. require `hh.isdigit() and mm.isdigit()` in addition to the existing length + range checks). `parsed_time()` already re-parses correctly, so this is a load-time tightening that closes the raw-string-as-key hole. Add a regression test in `tests/test_config.py` named `# HARD-CLEAN-02 / F74` asserting canonical times pass and `+9:30` / ` 9:30` raise ValueError; assert an existing valid config (e.g. `"07:00"`, `"23:59"`) still validates so the briefing-scheduling path is unregressed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q -k "F74 or hhmm or time"</automated>
  </verify>
  <acceptance_criteria>
    - The F74 regression test exists in `tests/test_config.py`, names the finding id, and is GREEN against the tightened validator (and would fail against the pre-fix `_hhmm`).
    - Canonical times (`"07:00"`, `"09:30"`, `"23:59"`) still validate; `"+9:30"` and `" 9:30"` raise ValueError.
    - `uv run pytest tests/test_config.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>The HH:MM validator rejects non-canonical strings, canonical times still pass, and a finding-tagged regression test pins it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen resolve_location to id-then-name (F75)</name>
  <files>weatherbot/config/loader.py, tests/test_config.py</files>
  <read_first>
    - weatherbot/config/loader.py:40-60 (`resolve_location`: `name is None`→first; else casefold-match on `loc.name` only; raises UnknownLocationError)
    - tests/test_filewatch.py:346 (`_loc("Home", id="home", ...)` — the rename-safe helper shape to reuse; a location whose id != name)
    - .planning/phases/34-test-gap-backfill/34-PATTERNS.md §"rename-safe id!=name" (F108/F115 shapes)
    - .planning/WHOLE-PROJECT-REVIEW.md §Low entry F75
  </read_first>
  <behavior>
    - `resolve_location(cfg, "<id>")` returns the location whose `id == "<id>"` even when its `name` differs (e.g. name="Beach House", id="loc-7").
    - `resolve_location(cfg, "<name>")` still returns the case-insensitive name match (unchanged).
    - `resolve_location(cfg, None)` still returns the first/default location (unchanged).
    - An unknown token still raises UnknownLocationError (unchanged error contract).
  </behavior>
  <action>
    In `resolve_location`, after the `name is None` branch, match on `id` FIRST (exact or casefold — match the id's stable-identity semantics), then fall back to the existing case-insensitive `name` match, then raise UnknownLocationError as today. This is additive: name matching is preserved so every existing caller stays green (Pitfall 5). Add a regression test in `tests/test_config.py` named `# HARD-CLEAN-02 / F75` that builds a location with `name != id` (reuse the `_loc(name, id=...)` shape), asserts `resolve_location(cfg, "<id>")` returns it, asserts `resolve_location(cfg, "<name>")` still returns it, and asserts an unknown token still raises UnknownLocationError.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q -k "F75 or resolve_location"</automated>
  </verify>
  <acceptance_criteria>
    - The F75 regression test exists, names the finding id, and is GREEN (would fail against the pre-fix name-only lookup for the id case).
    - `resolve_location` still matches by name and still raises UnknownLocationError on an unknown token (name-match + error-contract regression assertions green).
    - `uv run pytest tests/test_config.py -q` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <done>`resolve_location` matches id-then-name additively; a finding-tagged regression test pins the id case, the name case, and the unknown-token error contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file→loader | Hand-edited config.toml crosses into the loader; F74 tightens input validation of the schedule time string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04-01 | Tampering | config/models.py HH:MM validator | low | mitigate | F74 tightens an existing pydantic validator (V5 input validation, ASVS L1); no new attack surface, only stricter canonicalization. Regression test pins acceptance + rejection. |
| T-35-04-02 | (none) | config/loader.py resolve_location | low | accept | F75 widens a lookup additively; no trust boundary change (config is trusted input). Regression test pins the new + preserved paths. |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py -q` exits 0 (F74 + F75 regression tests green).
- `uv run pytest -q` exits 0 (878 baseline; trust exit code over syrupy line).
- No hub-path file in the diff.
</verification>

<success_criteria>
F74 and F75 are fixed with finding-tagged regression tests; canonical/existing behavior preserved; suite green.
</success_criteria>

<output>
Create `.planning/phases/35-cleanup-sweep/35-04-SUMMARY.md` when done.
</output>
