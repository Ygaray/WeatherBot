---
phase: 07-cli-weather-location-one-shot
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified: [tests/test_cli.py, tests/test_scheduler.py, deploy/weatherbot.service, deploy/README.md]
autonomous: true
requirements: [CMD-01, CMD-03, CMD-04, CMD-05]
must_haves:
  truths:
    - "Full suite (>=206 tests) green after the clean break — five removed-flag callsites rewritten to subcommand form"
    - "New offline `weather` tests pin the exit-code matrix (0 configured / 1 unknown-lists-valid-names / 2 bad-config / 3 fetch-failure), stdout-vs-stderr split, quiet-vs-`-v` (D-09)"
    - "deploy/weatherbot.service ExecStart and deploy/README.md examples use the `run` subcommand form (no `--run`)"
  artifacts:
    - path: "tests/test_cli.py"
      provides: "rewritten check/send-now callsites + new weather subcommand tests"
      contains: "def test_weather_"
    - path: "deploy/weatherbot.service"
      provides: "ExecStart on the `run` subcommand"
      contains: "weatherbot run"
  key_links:
    - from: "tests/test_cli.py weather tests"
      to: "weatherbot.cli.run_weather"
      via: "injected _FakeClient (offline)"
      pattern: "run_weather\\("
    - from: "tests/test_scheduler.py:619"
      to: "cli.main([\"run\", ...])"
      via: "subcommand form replacing removed --run"
      pattern: "main\\(\\[\"run\""
---

<objective>
Keep the suite green across the D-02 clean break, pin the new `weather` behavior with offline tests, and update deployment artifacts to the subcommand form (D-02).

Purpose: Plan 02's flag→subparser restructure invalidates five existing `main([...])` callsites that use removed flags — rewrite them or the suite cannot be green (RESEARCH Pitfall 2). New `weather` tests lock the CMD-01/03/04/05 + D-05/D-08/D-09 contract. The deployed daemon invocation must match the new surface; the live-host redeploy is a manual UAT step.
Output: rewritten callsites in `tests/test_cli.py` + `tests/test_scheduler.py`, new `weather` tests, updated `deploy/weatherbot.service` + `deploy/README.md`, full suite green.

Depends on Plan 02 (the `run_weather`/`_cmd_weather` symbols + subparser surface must exist first). Owns tests + deploy only (cli.py is Plan 02's) — no merge conflict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-weather-location-one-shot/07-CONTEXT.md
@.planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md
@.planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md
@.planning/phases/07-cli-weather-location-one-shot/07-02-SUMMARY.md
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the five removed-flag callsites to subcommand form</name>
  <files>tests/test_cli.py, tests/test_scheduler.py</files>
  <read_first>
    - tests/test_cli.py lines 286-323 (the four `main(["--check", ...])`/`main(["--send-now", ...])` calls at 295, 308, 314, 322)
    - tests/test_scheduler.py line 619 (`cli.main(["--run", "--config", str(cfg_path)])` — the FIFTH callsite, outside test_cli.py)
    - .planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md § tests/test_cli.py (lines 232-278), esp. lines 249-254 (the fifth call)
    - .planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md Pitfall 2, Pitfall 3
    - .planning/phases/07-cli-weather-location-one-shot/07-02-SUMMARY.md (final subcommand names as built)
  </read_first>
  <action>
    Rewrite exactly five `main([...])` callsites from removed-flag form to subcommand form, preserving each test's intent and assertions: in `tests/test_cli.py`, `main(["--check", "--config", str(...)])` at ~295/308/314 → `main(["check", "--config", str(...)])`, and `main(["--send-now", "--config", str(...)])` at ~322 → `main(["send-now", "--config", str(...)])`. In `tests/test_scheduler.py`, line ~619 `cli.main(["--run", "--config", str(cfg_path)])` → `cli.main(["run", "--config", str(cfg_path)])`. Keep the `assert rc == 1` assertions unchanged — the migrated handlers keep their original exit codes (Plan 02), so a bad config still returns 1 for check/send-now/run, and these observe a RETURNED code (not an argparse usage error), so they stay `== 1`, not `pytest.raises(SystemExit)`. Then grep the whole `tests/` tree for any other `main([...])`/`cli.main([...])` using `--run`/`--check`/`--send-now`/`--geocode` to confirm these five are the only ones.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'main(\[\"--' tests/)" && echo "no removed-flag callsites" && uv run pytest tests/test_cli.py tests/test_scheduler.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'main(\["--' tests/` returns nothing.
    - The three `check` callsites and one `send-now` callsite use subcommand strings; their `assert rc == 1` are unchanged.
    - `tests/test_scheduler.py` ~619 uses `cli.main(["run", "--config", str(cfg_path)])`.
    - `uv run pytest tests/test_cli.py tests/test_scheduler.py` passes (no `SystemExit: 2` from removed flags).
  </acceptance_criteria>
  <done>All five callsites use subcommand form; migrated-handler tests pass with original assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add offline weather-subcommand tests (exit matrix, stream split, quiet-vs-verbose)</name>
  <files>tests/test_cli.py</files>
  <read_first>
    - tests/test_cli.py lines 25-41 (`_FakeClient`, records `onecall_calls`), 59-73 (`_config(...)` default location names), 75-245 (existing `do_*` injected-client tests), sleep seam (`monkeypatch.setattr("weatherbot.cli.time.sleep", ...)`, ~335-337), `_http_429()` (~355-358)
    - tests/fixtures/ (onecall_imperial_clear.json, onecall_metric_clear.json, + rainy/alert/extreme/highuv) — reuse, add NO new fixtures
    - weatherbot/interactive/lookup.py line 146 (`_log.info("lookup complete", ...)` — the line D-09 suppresses)
    - .planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md § "Offline test of the weather command", § "Phase Requirements → Test Map", § "Wave 0 Gaps"
    - .planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md § "New weather tests to add" (lines 256-277)
    - .planning/phases/07-cli-weather-location-one-shot/07-02-SUMMARY.md (final `run_weather` signature + quiet mechanism chosen)
  </read_first>
  <action>
    Add offline `weather` tests to `tests/test_cli.py`, reusing `_FakeClient`, `_config`, `load_fixture`, `capsys` (inject `client=` so `build_client` never runs — no network). Cover:
    - briefing-exit-0: `run_weather("home", config=_config(), client=_FakeClient(onecall_imp=load_fixture("onecall_imperial_clear.json"), onecall_met=load_fixture("onecall_metric_clear.json")))` → rc 0, stdout non-empty (CMD-01).
    - default-location-exit-0: `run_weather(None, ...)` → rc 0, briefing on stdout (CMD-03).
    - unknown-exit-1: `run_weather("nope", config=_config(), client=_FakeClient())` → rc 1; stderr contains `"No location named 'nope'"` AND a valid configured name (CMD-04 verbatim); assert `_FakeClient.onecall_calls` empty (no network on the unknown path).
    - template-matches-v1 (CMD-05): assert printed stdout byte-equals `lookup_weather(...).text` for the same inputs — proves no separate on-demand format.
    - bad-config-exit-2: `main(["weather", "--config", str(missing_or_bad)])` → 2 (the 2 comes from `_load_config_reporting` returning, NOT argparse — Pitfall 3).
    - fetch-failure-exit-3: patch the sleep seam, fake client raises `httpx.HTTPStatusError(429)` every call → rc 3, attempts ≤ `_MANUAL_MAX_ATTEMPTS`; plus a 401 case returning 3 on the FIRST attempt (no retry of permanent auth — D-08/Pitfall 5).
    - quiet-vs-verbose (D-09, pins RESEARCH Open Question 1): at INFO, the `weather` path WITHOUT `-v` shows NO `"lookup complete"` line; WITH `-v` it DOES.
    Also assert in at least one error test that no `appid`/webhook URL appears in stderr/logs (T-04-01).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "weather" -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover exit 0 (configured), 0 (default/bare), 1 (unknown + valid names on stderr), 2 (bad config), 3 (exhausted-transient AND first-attempt 401).
    - A test asserts the unknown path leaves `_FakeClient.onecall_calls` empty.
    - A test asserts printed briefing == v1 template render (CMD-05).
    - A test asserts quiet-by-default vs `-v` (D-09).
    - At least one error test asserts no secret in stderr/logs.
    - `uv run pytest tests/test_cli.py -k weather` passes; no new files under `tests/fixtures/`.
  </acceptance_criteria>
  <done>CMD-01/03/04/05 + D-05/D-08/D-09 pinned by offline tests using established seams; matrix + quiet-vs-`-v` green.</done>
</task>

<task type="auto">
  <name>Task 3: Update deploy artifacts to the run-subcommand form</name>
  <files>deploy/weatherbot.service, deploy/README.md</files>
  <read_first>
    - deploy/weatherbot.service lines 7-10 (header comments with both `--run` forms), line 29 (`ExecStart=/usr/bin/uv run weatherbot --run`), line 35 (least-privilege — leave)
    - deploy/README.md line 15 (`ExecStart=weatherbot --run`), line 26 (`/usr/bin/uv run weatherbot --run`), line 31 (`<REPO>/.venv/bin/python -m weatherbot --run`)
    - .planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md § deploy/weatherbot.service (201-217), § deploy/README.md (220-229)
    - .planning/phases/07-cli-weather-location-one-shot/07-CONTEXT.md D-02
  </read_first>
  <action>
    Swap every `weatherbot --run`/`python -m weatherbot --run` in deploy artifacts to the `run` subcommand form (D-02 — else `--run` fails to parse on the host). In `deploy/weatherbot.service`: line 29 → `ExecStart=/usr/bin/uv run weatherbot run`; update header-comment option lines (7-9) showing `weatherbot --run` (uv) and `<REPO>/.venv/bin/python -m weatherbot --run` (venv) to their `run` equivalents. In `deploy/README.md`: line 15 → `ExecStart=weatherbot run`, line 26 → `ExecStart=/usr/bin/uv run weatherbot run`, line 31 → `ExecStart=<REPO>/.venv/bin/python -m weatherbot run`. Change ONLY the ExecStart command string (not the service name or other directives). Add a brief `deploy/README.md` note that the host redeploy (re-`uv sync` for the console script + `systemctl daemon-reload` + restart) is required — the ops/UAT step in this plan.
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'weatherbot --run\|m weatherbot --run' deploy/)" && echo "no stale flag invocations" && grep -rn 'weatherbot run' deploy/weatherbot.service deploy/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'weatherbot --run' deploy/` returns nothing.
    - `deploy/weatherbot.service` line 29 reads `ExecStart=/usr/bin/uv run weatherbot run`; header comments show the `run` form for both uv and venv.
    - `deploy/README.md` lines 15/26/31 use the `run` subcommand form.
    - Service name and all non-ExecStart directives unchanged.
    - `deploy/README.md` notes the host redeploy (`uv sync` + `daemon-reload` + restart).
  </acceptance_criteria>
  <done>Deploy artifacts invoke `weatherbot run`; host redeploy documented as ops/UAT; no other directives changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal user → CLI (under test) | offline tests exercise the untrusted `location` path; no real network or secrets |
| operator → deployed systemd unit | the daemon invocation string is the only changed surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | error-path tests | mitigate | At least one error test asserts no `appid`/webhook URL in stderr/logs — encodes T-04-01 as a regression guard. |
| T-07-06 | Tampering | deploy ExecStart string | accept | Only the command string changes (`--run`→`run`); no broadening of unit privileges/user/EnvironmentFile. Host redeploy is a manual operator step. |
| T-07-07 | Repudiation / regression | full suite | mitigate | Full suite must stay ≥206 green after the clean break — rewritten callsites + new tests prove no behavior was silently dropped. |
</threat_model>

<verification>
- `uv run pytest` fully green (≥206 tests; new `weather` tests on top).
- `uv run weatherbot --help` exits 0 and lists the five subcommands (console script from Plan 01 + surface from Plan 02).
- No removed-flag callsites in `tests/`; no `--run` invocations in `deploy/`.
</verification>

<uat>
## Manual UAT Step (operator / host action — NOT autonomous)

After this phase merges, redeploy on live host `yahir-mint` (D-02; RESEARCH Runtime State Inventory — the deployed unit's `ExecStart` still says `--run` and the venv lacks the console script until re-synced):

1. Pull the updated repo on `yahir-mint`.
2. Run `uv sync` so `.venv/bin/weatherbot` materializes (new console script).
3. Edit `/etc/systemd/system/weatherbot.service` so `ExecStart` uses `weatherbot run` (not `--run`).
4. `sudo systemctl daemon-reload`.
5. `sudo systemctl restart weatherbot.service`.
6. Confirm `systemctl status weatherbot.service` is `active (running)` and the next scheduled briefing fires.
7. Optionally: `uv run weatherbot weather home` on the host prints a briefing and exits 0.

Captured as a UAT verification, not an autonomous task — Claude cannot mutate `/etc/systemd/system/` or restart services on the remote host.
</uat>

<success_criteria>
Suite green across the clean break (≥206 tests); CMD-01/03/04/05 + D-05/D-08/D-09 pinned by offline tests; deploy artifacts match the new `run` surface (D-02); host redeploy documented as a manual UAT step.
</success_criteria>

<artifacts_this_phase_produces>
- `tests/test_cli.py` → rewritten `check`/`send-now` callsites + NEW tests: `test_weather_prints_briefing_exit_0`, `test_weather_default_location_exit_0`, `test_weather_unknown_location_exits_1`, `test_weather_template_matches_v1`, `test_weather_bad_config_exits_2`, `test_weather_fetch_failure_exits_3`, `test_weather_quiet_by_default_and_verbose` (names indicative; executor may adjust while keeping coverage)
- `tests/test_scheduler.py` → rewritten line ~619 callsite (`run` subcommand form)
- `deploy/weatherbot.service` → `ExecStart` + header comments on the `run` subcommand
- `deploy/README.md` → invocation examples on the `run` subcommand + host-redeploy note
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-cli-weather-location-one-shot/07-03-SUMMARY.md` when done.
</output>
