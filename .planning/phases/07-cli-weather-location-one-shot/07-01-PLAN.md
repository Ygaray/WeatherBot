---
phase: 07-cli-weather-location-one-shot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml]
autonomous: true
requirements: [CMD-01]
must_haves:
  truths:
    - "`weatherbot --help` resolves as a real console command (not only `python -m weatherbot`) and exits 0 (D-03)"
    - "`.venv/bin/weatherbot` exists after `uv sync`"
  artifacts:
    - path: "pyproject.toml"
      provides: "[build-system] (hatchling) + [project.scripts] weatherbot entry point"
      contains: "[project.scripts]"
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "weatherbot.cli:main"
      via: "console_scripts entry point materialized by uv sync"
      pattern: "weatherbot = \"weatherbot.cli:main\""
---

<objective>
Make the `weatherbot` console script real so `weatherbot weather home` works verbatim (ROADMAP SC#1, D-03). Today `pyproject.toml` has NO `[build-system]`, so uv treats the project as a non-package and `[project.scripts]` would produce no console script — the single most likely silent failure in this phase (RESEARCH Pitfall 1).

Purpose: The deliverable command (the `weather` handler, Plan 02) is useless if it cannot be invoked as `weatherbot weather …`. This plan establishes the packaging so the entry point materializes in `.venv/bin/`.
Output: `pyproject.toml` gains `[build-system]` (hatchling) + `[project.scripts] weatherbot = "weatherbot.cli:main"`; `uv sync` installs the console script; `weatherbot --help` resolves.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-weather-location-one-shot/07-CONTEXT.md
@.planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md
@.planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add build-system + console-script entry point and sync</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (the full file — confirm NO [build-system] table exists today; [project] is lines 1-26)
    - .planning/phases/07-cli-weather-location-one-shot/07-RESEARCH.md § "Supporting (build/packaging)", § "Package Legitimacy Audit", Pitfall 1
    - .planning/phases/07-cli-weather-location-one-shot/07-PATTERNS.md § "pyproject.toml (config, packaging)" (lines 178-198)
    - .planning/phases/07-cli-weather-location-one-shot/07-CONTEXT.md D-03
    - weatherbot/cli.py (confirm `main` is the module-level callable the entry point targets)
  </read_first>
  <action>
    Add two tables to pyproject.toml per D-03. Add a `[build-system]` table with `requires = ["hatchling"]` and `build-backend = "hatchling.build"` (hatchling is the PyPA-approved backend per RESEARCH Package Legitimacy Audit — approved, no checkpoint needed). Add a `[project.scripts]` table with the single entry `weatherbot = "weatherbot.cli:main"`. Do NOT add any new runtime dependency — every library the phase needs (argparse stdlib, tenacity, structlog, httpx) is already in `[project].dependencies`. Do NOT use `[tool.uv] package = true` (the uv-specific alternative) — hatchling is the portable PyPA choice. After editing, run `uv sync` so the project builds and installs into the project venv, materializing `.venv/bin/weatherbot`.
  </action>
  <verify>
    <automated>uv sync && uv run weatherbot --help</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains a `[build-system]` table with `requires = ["hatchling"]` and `build-backend = "hatchling.build"`.
    - `pyproject.toml` contains `[project.scripts]` with `weatherbot = "weatherbot.cli:main"`.
    - No new entry was added to `[project].dependencies`.
    - `uv sync` completes without "project is marked as not a package".
    - `.venv/bin/weatherbot` exists (the console script materialized).
    - `uv run weatherbot --help` exits 0 and prints usage (proves the console entry point resolves; at this point `main` still has the OLD flag surface — that is expected, the restructure is Plan 02).
  </acceptance_criteria>
  <done>Console script `weatherbot` resolves via uv and `.venv/bin/weatherbot` exists; pyproject has build-system + project.scripts; no new runtime deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package build / install | hatchling build backend fetched + run by `uv sync` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | hatchling install via uv | accept | hatchling is the PyPA-maintained canonical build backend (RESEARCH Package Legitimacy Audit: HIGH-trust, approved); no [SUS]/[ASSUMED] packages introduced, so no blocking-human legitimacy checkpoint required. |
| T-07-PKG | Elevation | [project.scripts] entry point | mitigate | Only ONE entry point added (`weatherbot = "weatherbot.cli:main"`); no broadening of what is packaged/exposed beyond the single existing CLI callable (security_threat_model item c). No new dependency added. |
</threat_model>

<verification>
- `uv run weatherbot --help` exits 0 (console script resolves).
- `.venv/bin/weatherbot` present.
- `uv run pytest` still green (packaging change must not break the suite).
</verification>

<success_criteria>
The `weatherbot` console command resolves as a real installed script (D-03), unblocking the verbatim `weatherbot weather home` acceptance in Plan 02 (CMD-01 / ROADMAP SC#1).
</success_criteria>

<artifacts_this_phase_produces>
- `pyproject.toml` → new `[build-system]` table (`requires = ["hatchling"]`, `build-backend = "hatchling.build"`)
- `pyproject.toml` → new `[project.scripts]` table with entry `weatherbot = "weatherbot.cli:main"`
- Materialized console script `.venv/bin/weatherbot` (build artifact, not source)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-cli-weather-location-one-shot/07-01-SUMMARY.md` when done.
</output>
