---
phase: 32-timezone-date-boundary-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scheduler.py
  - tests/test_uv_monitor.py
  - tests/test_models.py
  - tests/test_uv.py
  - tests/test_import_hygiene.py
autonomous: true
requirements: [HARD-TZ-01, HARD-TZ-02, HARD-TZ-03, HARD-TZ-04]

must_haves:
  truths:
    - "D-01 keying is pinned by a failing-first test: a 23:45 slot recovered at 00:15 next local day yields exactly ONE MissedSlot keyed on YESTERDAY's local_date (test_catchup_prior_local_day), RED before the fix."
    - "D-02 fold-grace is pinned by a failing-first test: a fall-back 01:30 slot minutes-late inside the repeated hour stays due (grace not inflated 60 min), composed fold=0 to agree with the live CronTrigger (test_catchup_fold_grace_not_inflated)."
    - "D-03 all-clear hysteresis is pinned by a failing-first test: a momentary solar-noon UV dip (5.8 < 6.0) that climbs back does NOT post all-clear (test_allclear_not_latched_on_momentary_dip), RED before the fix."
    - "D-04 lifecycle no-never-fire-gap is pinned by a failing-first full-day tick test: prewarn/crossing/all-clear each post exactly once with no never-fire state (test_lifecycle_full_day_no_never_fire_gap)."
    - "D-05 daily-entry-by-own-local-date is pinned by two failing-first tests: a payload whose daily[0] is dated YESTERDAY degrades in models.from_payloads (test_daily0_not_today_degrades) and does not falsely report stays_below in compute_uv (test_compute_uv_daily0_today_guard)."
    - "D-06 naive-now_utc-as-UTC is pinned by a failing-first test: a naive now_utc near midnight is treated as UTC so local_date is not shifted a day (test_naive_now_utc_treated_as_utc)."
    - "D-07 hourly-sort-before-interpolation is pinned by a failing-first test: out-of-order hourly buckets yield a crossing/window computed on time-sorted points (test_hourly_points_sorted_before_interpolation)."
    - "D-08 single-helper is pinned by a failing-first test: models, store, and uvmonitor all resolve local date through ONE weatherbot.weather.dates helper with identical output for the same (now, tz) and no import cycle (test_import_hygiene dates assertion + same-output test)."
    - "EDGE boundary (HARD-TZ-01): the exactly-scheduled==now_utc and exactly now-GRACE edges are asserted — a slot exactly at now is due, a slot exactly GRACE late is due, one second past GRACE is skipped."
    - "EDGE precision (HARD-TZ-01): the DST fall-back fold=0 grace min() does not inflate lateness by up to 60 min inside the repeated hour (test_catchup_fold_grace_not_inflated encodes the concrete repeated-hour instant)."
    - "EDGE boundary/adjacency (HARD-TZ-02): UV exactly == threshold vs one step either side is asserted — a momentary dip to exactly threshold-epsilon while still peaking does not latch all-clear."
    - "EDGE empty (HARD-TZ-02): empty/None hourly[] degrades to 'do not post all-clear yet' rather than latching or raising (asserted in the lifecycle/all-clear tests)."
    - "EDGE ordering (HARD-TZ-02/D-07): equal-timestamp / out-of-order hourly points sort to a stable, time-ordered sequence before interpolation (test_hourly_points_sorted_before_interpolation)."
    - "EDGE idempotency/concurrency (HARD-TZ-04): the shared dates helper is a pure function — same (now, tz) yields the same output with no shared state, safe under the daemon/UV-tick/heartbeat callers (same-output test asserts determinism)."
  artifacts:
    - "tests/test_scheduler.py::test_catchup_prior_local_day (new, RED)"
    - "tests/test_scheduler.py::test_catchup_fold_grace_not_inflated (new, RED)"
    - "tests/test_uv_monitor.py::test_allclear_not_latched_on_momentary_dip (new, RED)"
    - "tests/test_uv_monitor.py::test_lifecycle_full_day_no_never_fire_gap (new, RED)"
    - "tests/test_models.py::test_daily0_not_today_degrades (new, RED)"
    - "tests/test_models.py::test_naive_now_utc_treated_as_utc (new, RED)"
    - "tests/test_uv.py::test_compute_uv_daily0_today_guard (new, RED)"
    - "tests/test_uv.py::test_hourly_points_sorted_before_interpolation (new, RED)"
    - "tests/test_import_hygiene.py dates single-helper assertion + same-output test (new, RED until dates.py exists)"
  key_links:
    - "Each RED test injects now_utc / now deterministically (no wall-clock) per the existing test convention."
    - "The catchup tests reuse _home_config / _utc_for_local / _never_sent / _NY from tests/test_scheduler.py:149-175."
    - "The UV tests reuse _run / _at / _clone / load_fixture from tests/test_uv_monitor.py:342."
---

<objective>
Author the nine failing-first (RED) regression tests that pin every locked decision for this
phase, per 32-VALIDATION.md. Each test encodes a concrete boundary scenario; the later
implementation waves turn each test GREEN. This is the Nyquist Wave-0 scaffold — no production
code changes here, only tests that FAIL against current behavior.

Purpose: guarantee every correctness fix ships with a regression test that fails against the
pre-fix behavior (v2.1 "each fix ships with a regression test" governing decision), and that the
two CONFIRMED scenarios (F14 catch-up-across-midnight, F15 UV all-clear latch) each have a
failing-first test.
Output: extensions to five existing test files with new RED test functions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-timezone-date-boundary-correctness/32-VALIDATION.md
@.planning/phases/32-timezone-date-boundary-correctness/32-RESEARCH.md
@.planning/phases/32-timezone-date-boundary-correctness/32-PATTERNS.md
</context>

## Artifacts this phase produces

New test symbols created by THIS plan (exclude from source-drift/new-symbol verification):
- `tests/test_scheduler.py::test_catchup_prior_local_day`
- `tests/test_scheduler.py::test_catchup_fold_grace_not_inflated`
- `tests/test_uv_monitor.py::test_allclear_not_latched_on_momentary_dip`
- `tests/test_uv_monitor.py::test_lifecycle_full_day_no_never_fire_gap`
- `tests/test_models.py::test_daily0_not_today_degrades`
- `tests/test_models.py::test_naive_now_utc_treated_as_utc`
- `tests/test_uv.py::test_compute_uv_daily0_today_guard`
- `tests/test_uv.py::test_hourly_points_sorted_before_interpolation`
- `tests/test_import_hygiene.py` dates single-helper assertion + a `dates` same-output test

Downstream symbols these tests reference (created in later plans of this phase):
`weatherbot.weather.dates.local_date_iso`, `weatherbot.weather.dates.local_date_for`,
`weatherbot.weather.dates.select_today_daily`.

<tasks>

<task type="auto">
  <name>Task 1: RED catch-up tests — prior-local-day recovery (D-01) + fold-grace (D-02)</name>
  <files>tests/test_scheduler.py</files>
  <read_first>
    - tests/test_scheduler.py:149-303 (existing `_NY`, `_home_config`, `_utc_for_local`, `_never_sent`, `test_catchup_window`, `test_dst_exactly_once`, `test_dst_transition_band_exactly_once` — copy these conventions verbatim)
    - weatherbot/scheduler/catchup.py:139-178 (`plan_catchup` compose/gates — the behavior under test; injected `now_utc` is the signature seam)
    - .planning/phases/32-timezone-date-boundary-correctness/32-RESEARCH.md §Verification / Pattern 1 (concrete UTC math for the 23:45→00:15 recovery and the fall-back grace scenario)
  </read_first>
  <action>
    Add `test_catchup_prior_local_day` to tests/test_scheduler.py, modelled on `test_catchup_window`. Configure a home location with a 23:45 daily slot (`_home_config(days="mon-sun", time="23:45")`) and `_never_sent`. Inject `now_utc = _utc_for_local` for 00:15 the NEXT local day in `America/New_York`. Assert `plan_catchup` returns exactly ONE `MissedSlot` whose `local_date` equals YESTERDAY's local date (the slot's scheduled day, `now_local.date() - 1 day`), NOT today's. Assert the recovered instant is within GRACE (30 minutes late). This test is RED today because current `plan_catchup` composes only today's date, builds a future 23:45 instant, and skips it as "not due yet" (catchup.py:170), so it returns zero MissedSlots.
    Add EDGE-boundary sub-assertions in the same or a sibling test: a slot composed to fire exactly at `now_utc` is due (returned), a slot exactly `GRACE` late is due, and a slot one second past `GRACE` is skipped (mirror `test_catchup_window`'s within/over-grace pair).
    Add `test_catchup_fold_grace_not_inflated` next to `test_dst_transition_band_exactly_once`. Encode the DST fall-back scenario from 32-RESEARCH.md: a slot inside the `America/New_York` fall-back repeated hour (e.g. 01:30 on the November fall-back date) that is only minutes late measured against the fold=0 (pre-transition, UTC-4) instant the live CronTrigger fires. Assert the slot is still returned as due (within GRACE), i.e. the lateness is NOT inflated by ~60 minutes by comparing against the fold=1 (post-transition, UTC-5) instant. This is RED today because current `plan_catchup` uses a bare `now_utc - scheduled > GRACE` that can inflate by up to 60 min across the repeated hour (F91). Add a comment tag `# D-01`/`# D-02`/`# F14`/`# F91` on the relevant asserts for traceability.
    Do NOT modify any production code. Do NOT add xfail markers — these tests are expected RED and turn GREEN when plan 32-03 lands.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler.py::test_catchup_prior_local_day tests/test_scheduler.py::test_catchup_fold_grace_not_inflated -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_scheduler.py` contains `def test_catchup_prior_local_day(` and `def test_catchup_fold_grace_not_inflated(`
    - Both tests currently FAIL (RED) — `uv run pytest tests/test_scheduler.py::test_catchup_prior_local_day tests/test_scheduler.py::test_catchup_fold_grace_not_inflated` exits non-zero with an assertion failure (not a collection/import error)
    - `test_catchup_prior_local_day` asserts a single MissedSlot keyed on YESTERDAY's local_date and asserts the scheduled==now and now-GRACE boundary edges
    - `test_catchup_fold_grace_not_inflated` asserts the fall-back-hour slot stays due (lateness not inflated ~60 min)
    - No production file under `weatherbot/` is modified by this task
  </acceptance_criteria>
  <done>Two RED catch-up tests exist, fail with assertion errors (not import/collection errors), and key on the candidate day + boundary edges.</done>
</task>

<task type="auto">
  <name>Task 2: RED UV-monitor tests — all-clear hysteresis (D-03) + lifecycle no-gap (D-04)</name>
  <files>tests/test_uv_monitor.py</files>
  <read_first>
    - tests/test_uv_monitor.py (the `_run(payload, *, tmp_db, now_utc, uv, channel)` helper at :342, `_at(hh,mm)` builders, `_clone(...)`, `load_fixture("onecall_imperial_highuv.json")`, and the existing `test_daily0_prior_day_skips_decision` at :251 — copy these conventions)
    - weatherbot/scheduler/uvmonitor.py:256-323 (the three `_decide` branches; branch 3 all-clear latch at :318 is the F15 bug under test)
    - weatherbot/weather/uv.py:34-53 (`UvSummary` fields `peak_time`/`window_end`/`crossing_time`/`hourly_points` the fix will gate on)
  </read_first>
  <action>
    Add `test_allclear_not_latched_on_momentary_dip` to tests/test_uv_monitor.py, modelled on the existing decide-branch tests. Build a payload where today's UV is still peaking (peak_time is later than now, window_end is later than now) but `current.uvi` momentarily dips to 5.8 against a 6.0 threshold, with a prior `crossing` claim already recorded for today (INSERT a `crossing` row into `tmp_db` via the same helper the existing tests use, or run an earlier tick that crosses). Drive one tick at solar noon via `_run(...)`. Assert NO all-clear message was posted to the channel (the `✅ ... protect window over` string is absent) and NO `allclear` row was claimed in `tmp_db`. This is RED today because branch 3 latches on the instantaneous `current < threshold` dip (uvmonitor.py:318). Add an EDGE-boundary assertion: a dip to exactly `threshold - epsilon` while `now < window_end` and `now < peak_time` also does NOT latch.
    Add `test_lifecycle_full_day_no_never_fire_gap`. Walk a full sequence of ticks across a modelled high-UV day (pre-warn window → crossing → peak → genuine end-of-window) via repeated `_run(...)` calls with advancing `now_utc`. Assert each expected post fires exactly once: exactly one pre-warn (or one crossing if already-high at first poll), exactly one all-clear once the day's window genuinely ends, and that no tick leaves a state where none of the branches can fire for the day (D-04). Add an EDGE-empty assertion: a tick with empty/None `hourly[]` (no window_end/peak_time) does NOT post all-clear (degrades to "don't post yet") and does not raise.
    Do NOT modify production code. These are expected RED until plan 32-05 lands the hysteresis + lifecycle audit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_uv_monitor.py::test_allclear_not_latched_on_momentary_dip tests/test_uv_monitor.py::test_lifecycle_full_day_no_never_fire_gap -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_uv_monitor.py` contains `def test_allclear_not_latched_on_momentary_dip(` and `def test_lifecycle_full_day_no_never_fire_gap(`
    - `test_allclear_not_latched_on_momentary_dip` currently FAILS (RED) with an assertion error asserting all-clear was posted/claimed when it should not have been
    - The all-clear assertion checks BOTH the absence of the posted `protect window over` message AND the absence of an `allclear` claim row
    - `test_lifecycle_full_day_no_never_fire_gap` asserts each of prewarn/crossing/all-clear posts exactly once and includes the empty-hourly no-latch edge
    - No production file under `weatherbot/` is modified by this task
  </acceptance_criteria>
  <done>Two RED UV-monitor tests exist and fail with assertion errors; the momentary-dip test (F15 CONFIRMED scenario) and the full-day lifecycle walk are both present.</done>
</task>

<task type="auto">
  <name>Task 3: RED models/uv/import-hygiene tests — D-05 / D-06 / D-07 / D-08</name>
  <files>tests/test_models.py, tests/test_uv.py, tests/test_import_hygiene.py</files>
  <read_first>
    - tests/test_models.py (existing `load_fixture`-driven `from_payloads` tests + injected `now_utc` convention)
    - tests/test_uv.py (existing `compute_uv` tests + injected `now` convention)
    - tests/test_import_hygiene.py:26-40 (`APP = "weatherbot"`, `Path(__file__).resolve().parent.parent / APP` source-reading substring-gate style — copy for the dates assertion)
    - weatherbot/weather/models.py:296-303 (`from_payloads` daily[0] hard-index — the F35 defect under test) and :388 (local_date write, the F33/D-06 seam)
    - weatherbot/weather/uv.py:214-241 (`compute_uv` daily0 read at :219 + stays_below path) and :98-146 (`_today_daytime_points` append order — the F32 seam)
  </read_first>
  <action>
    In tests/test_models.py add `test_daily0_not_today_degrades`: build a payload whose `daily[0]` has a `dt`/`sunrise` dated YESTERDAY in the configured tz (and no today entry), inject a `now_utc` for today, call `Forecast.from_payloads(...)`, and assert the briefing does NOT ship yesterday's high/low/rain/UV as today's — i.e. it degrades down the existing empty/None path (`high` is None or the fields collapse) rather than reporting yesterday's numbers. RED today because `from_payloads` hard-indexes `daily[0]` (models.py:302) with no local-date check.
    In tests/test_models.py add `test_naive_now_utc_treated_as_utc`: inject a NAIVE `datetime(...)` `now_utc` near midnight (e.g. 23:30 UTC wall-clock as a naive value) and assert the resulting `local_date` matches the UTC-interpreted date, NOT a host-tz-shifted date. RED today because `_local_date_iso` calls `.astimezone(tz)` on the naive value, which reinterprets it in the HOST tz (F33). Reference `weatherbot.weather.dates.local_date_for` in a comment as the downstream fix seam.
    In tests/test_uv.py add `test_compute_uv_daily0_today_guard`: build a payload whose `daily[0]` is dated YESTERDAY but whose `hourly[]` contains a genuine today crossing; assert `compute_uv(...)` does NOT falsely report `stays_below=True` off the stale sunset (F31). RED today because `compute_uv` trusts `daily[0]` (uv.py:219) without a today guard.
    In tests/test_uv.py add `test_hourly_points_sorted_before_interpolation`: build a payload whose today `hourly[]` buckets are OUT OF timestamp order (or contain a DST fall-back duplicate hour), and assert the crossing_time/window computed by `compute_uv` matches the time-sorted interpretation (a specific expected crossing instant), not the bogus straddle an unsorted list produces (F32/D-07). Add the EDGE-ordering assertion that equal-timestamp points sort to a stable order. RED today because `_today_daytime_points` appends in raw payload order (uv.py:145).
    In tests/test_import_hygiene.py add a `dates` assertion in the source-reading substring-gate style: read the source of `weatherbot/weather/models.py`, `weatherbot/weather/store.py`, and `weatherbot/scheduler/uvmonitor.py` and assert each imports from `weatherbot.weather.dates` (the ONE helper) and that NONE of the three still defines its own `def _local_date_iso(`. Add a same-output test: import `local_date_iso`/`local_date_for` from `weatherbot.weather.dates` and assert identical output for the same `(now, tz)` across the wrapper and the primitive, and that the helper is deterministic (idempotency edge). RED today because `weatherbot/weather/dates.py` does not exist yet and all three files still define `_local_date_iso`.
    Do NOT modify production code. All are expected RED until plans 32-02/32-04 land.
  </action>
  <verify>
    <automated>uv run pytest tests/test_models.py::test_daily0_not_today_degrades tests/test_models.py::test_naive_now_utc_treated_as_utc tests/test_uv.py::test_compute_uv_daily0_today_guard tests/test_uv.py::test_hourly_points_sorted_before_interpolation "tests/test_import_hygiene.py" -k "dates or daily0 or naive or hourly or sorted" -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_models.py` contains `def test_daily0_not_today_degrades(` and `def test_naive_now_utc_treated_as_utc(`
    - `tests/test_uv.py` contains `def test_compute_uv_daily0_today_guard(` and `def test_hourly_points_sorted_before_interpolation(`
    - `tests/test_import_hygiene.py` contains a dates single-helper assertion (all three files import `weatherbot.weather.dates`; none defines `def _local_date_iso(`) and a `local_date_iso`/`local_date_for` same-output/determinism test
    - All five tests currently FAIL (RED) — the models/uv tests fail with assertion errors; the import-hygiene/same-output test fails because `weatherbot/weather/dates.py` does not exist (import/assert error is acceptable RED for this one)
    - No production file under `weatherbot/` is modified by this task
  </acceptance_criteria>
  <done>Five RED tests exist across models/uv/import-hygiene covering D-05, D-06, D-07, D-08; each fails against current behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenWeather payload → test fixtures | Tests synthesize malformed/adversarial `daily[]`/`hourly[]` payloads (yesterday-dated daily[0], out-of-order hourly, empty hourly) to pin the defensive-degrade posture. No live network. |
| injected `now_utc` → date math | Tests inject naive and boundary-adjacent instants to prove the local-date math is robust. No real clock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering | RED tests assert defensive-degrade on malformed `daily[]`/`hourly[]` | low | mitigate | Tests encode the malformed-payload scenarios (yesterday daily[0], out-of-order/empty hourly, naive now_utc) that the later fixes must degrade on rather than ship wrong data — no new external input introduced. |
| T-32-02 | Tampering (of exactly-once invariant) | catch-up dedup keying | low | mitigate | `test_catchup_prior_local_day` asserts the recovered slot keys on the candidate (yesterday) local_date so exactly-once dedup cannot mis-route. |
| T-32-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (stdlib + already-pinned pytest/apscheduler). No `[ASSUMED]`/`[SUS]` packages; no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler.py tests/test_uv_monitor.py tests/test_models.py tests/test_uv.py tests/test_import_hygiene.py -q` collects all new tests without collection errors (RED failures are expected and correct at this wave).
- Each of the nine new tests FAILS with an assertion error (or, for the dates same-output/import-hygiene test, an import/assert error because `dates.py` does not exist yet) — never a collection/syntax error.
- No file under `weatherbot/` is modified.
</verification>

<success_criteria>
- All nine failing-first tests exist across the five test files with the exact names in 32-VALIDATION.md.
- The two CONFIRMED scenarios (F14 → `test_catchup_prior_local_day`, F15 → `test_allclear_not_latched_on_momentary_dip`) each have a RED test.
- Every test injects `now_utc`/`now` deterministically (no wall-clock waits).
- Running the quick suite shows the nine new tests RED and the rest of the suite unaffected.
</success_criteria>

<output>
Create `.planning/phases/32-timezone-date-boundary-correctness/32-01-SUMMARY.md` when done.
</output>
