---
phase: 14-uv-index-on-demand-daily-briefing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/config/models.py
  - tests/test_config_uv.py
  - tests/test_client.py
  - tests/fixtures/onecall_imperial_highuv.json
  - tests/fixtures/onecall_imperial_uvcross.json
  - tests/fixtures/onecall_imperial_uvbelow.json
autonomous: true
requirements: [UV-03]
must_haves:
  truths:
    - "config.toml can carry a [uv] table with threshold + pre_warn_lead_minutes, validated fail-loud, hot-reloaded via the existing whole-Config reload"
    - "An absent [uv] table loads as defaults (threshold 6.0) under extra='forbid' — zero migration for existing configs"
    - "An out-of-range threshold or an unknown [uv] key aborts config load with a clear error"
    - "client.fetch_onecall returns a non-empty hourly[] carrying uvi (the Phase-12 exclude widening is intact) — the regression canary"
    - "UV test fixtures with a populated hourly[] array exist for the compute_uv work in Plan 14-02"
  artifacts:
    - path: "weatherbot/config/models.py"
      provides: "UvConfig frozen model + Config.uv field"
      contains: "class UvConfig"
    - path: "tests/test_config_uv.py"
      provides: "UV-03 load/validate/reload/unknown-key tests"
    - path: "tests/fixtures/onecall_imperial_uvcross.json"
      provides: "UV-crosses-6-mid-morning fixture with hourly[].uvi"
      contains: "hourly"
    - path: "tests/fixtures/onecall_imperial_uvbelow.json"
      provides: "stays-below-threshold-all-day fixture with hourly[].uvi"
      contains: "hourly"
  key_links:
    - from: "weatherbot/config/models.py Config"
      to: "UvConfig"
      via: "uv: UvConfig = Field(default_factory=UvConfig)"
      pattern: "default_factory=UvConfig"
---

<objective>
Lay the Phase-14 foundation: the `[uv]` config table (UV-03) and the critical
Wave-0 test fixtures carrying `hourly[].uvi` that every UV interpolation test in
Plan 14-02 depends on, plus the regression canary proving `client.fetch_onecall`
still returns a non-empty `hourly[]` (the Phase-12 `exclude` widening must stay
intact — `client.py` itself is VERIFY-ONLY, no edit).

Purpose: UV crossing/window/peak interpolation has zero data and zero testable
fixtures without this plan. The `[uv]` threshold also unifies three downstream
consumers (the sunscreen hint, the briefing UV line, the Phase-15 monitor), so it
must land first as a validated, hot-reloadable field.

Output: `UvConfig` model + `Config.uv` field; three `hourly[]`-bearing fixtures;
`tests/test_config_uv.py`; a `hourly[]` regression test in `tests/test_client.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-uv-index-on-demand-daily-briefing/14-CONTEXT.md
@.planning/phases/14-uv-index-on-demand-daily-briefing/14-RESEARCH.md
@.planning/phases/14-uv-index-on-demand-daily-briefing/14-PATTERNS.md
@weatherbot/config/models.py
@weatherbot/weather/client.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 14-01 portion)

- **Config model:** `UvConfig` (frozen, `extra="forbid"`) in `weatherbot/config/models.py`
- **Config field:** `Config.uv: UvConfig = Field(default_factory=UvConfig)`
- **Config fields inside UvConfig:** `threshold: float = 6.0`, `pre_warn_lead_minutes: int = 30`
- **Config table:** `[uv]` in `config.toml` (`threshold`, `pre_warn_lead_minutes`)
- **Fixtures:** `tests/fixtures/onecall_imperial_uvcross.json`, `tests/fixtures/onecall_imperial_uvbelow.json`, plus `hourly[]` added to `tests/fixtures/onecall_imperial_highuv.json`
- **Tests:** `tests/test_config_uv.py`; `hourly[]` regression test in `tests/test_client.py`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UvConfig frozen table + Config.uv field (UV-03)</name>
  <files>weatherbot/config/models.py, tests/test_config_uv.py</files>
  <read_first>
    - weatherbot/config/models.py (the `ForecastSchedule` frozen model at lines 88-110, the `cloud_threshold` field + `_cloud_threshold_in_range` validator at lines 398-410, the `Config` class at lines 381-410, and the imports at lines 8-15)
    - .planning/phases/14-uv-index-on-demand-daily-briefing/14-PATTERNS.md (the `config/models.py` section — UvConfig shape mirrors WebhookIdentity/Reliability + cloud_threshold precedent)
    - .planning/phases/14-uv-index-on-demand-daily-briefing/14-RESEARCH.md (Assumptions A4/A5 — `pre_warn_lead_minutes` int default 30; threshold 6.0 preserves the hardcoded hint)
  </read_first>
  <behavior>
    - An empty config (no `[uv]` table) loads → `config.uv.threshold == 6.0` and `config.uv.pre_warn_lead_minutes == 30` (defaults, zero migration).
    - A `[uv]` table with `threshold = 4.0` loads → `config.uv.threshold == 4.0`.
    - `threshold = 25` (out of 0..20 range) → config load raises ValueError mentioning `threshold`.
    - `pre_warn_lead_minutes = -5` (negative) → config load raises ValueError.
    - An unknown key under `[uv]` (e.g. `[uv]\nfoo = 1`) → ValueError (extra="forbid").
    - Editing the `[uv]` threshold and re-reading the whole Config (the reload path) picks up the new value with no extra reload wiring.
  </behavior>
  <action>
    Add a frozen `UvConfig(BaseModel)` to `weatherbot/config/models.py` mirroring the
    `ForecastSchedule`/`cloud_threshold` precedent: `model_config =
    ConfigDict(extra="forbid", frozen=True)`; fields `threshold: float = 6.0` and
    `pre_warn_lead_minutes: int = 30`. Add a `@field_validator("threshold")` named
    `_threshold_in_range` rejecting values outside `0 <= v <= 20` with a clear
    `ValueError` (mirror `_cloud_threshold_in_range`'s message style). Add a
    `@field_validator("pre_warn_lead_minutes")` named `_lead_non_negative` rejecting
    `v < 0`. Add the field `uv: UvConfig = Field(default_factory=UvConfig)` to the
    `Config` class — use `default_factory=UvConfig` (NOT `| None = None`) so an absent
    `[uv]` table means "defaults", matching the `webhook`/`reliability`/`reload`
    precedent (D-01: the table absence MUST mean defaults, never "no UV"). Phase 14
    only STORES + VALIDATES `pre_warn_lead_minutes`; Phase 15 gives it behavior
    (Open Q1). Write `tests/test_config_uv.py` covering every behavior case above,
    loading config via the existing loader entry point used by `tests/test_config.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_uv.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class UvConfig" weatherbot/config/models.py` → 1
    - `grep -c "default_factory=UvConfig" weatherbot/config/models.py` → 1
    - `uv run pytest tests/test_config_uv.py -x` passes (defaults, range-fail, lead-fail, unknown-key-fail, reload-pickup)
    - `uv run python -c "from weatherbot.config.models import Config; print(Config(locations=[]).uv.threshold)"` prints `6.0`
  </acceptance_criteria>
  <done>The `[uv]` table loads/validates/hot-reloads; an absent table defaults to threshold 6.0; out-of-range threshold, negative lead, and unknown keys all fail loud.</done>
</task>

<task type="auto">
  <name>Task 2: hourly[] UV fixtures + client regression canary (Wave-0 deliverable, D-05)</name>
  <files>tests/fixtures/onecall_imperial_highuv.json, tests/fixtures/onecall_imperial_uvcross.json, tests/fixtures/onecall_imperial_uvbelow.json, tests/test_client.py</files>
  <read_first>
    - weatherbot/weather/client.py (the `fetch_onecall` function lines 42-68 — `"exclude": "minutely"` already keeps `hourly`; docstring lines 43-51 names Phases 14/15 + the regression canary)
    - tests/fixtures/onecall_imperial_highuv.json (the existing fixture shape — note it currently LACKS `hourly[]`)
    - .planning/phases/14-uv-index-on-demand-daily-briefing/14-RESEARCH.md (Pitfall 1 + the "Selecting today's daytime hourly UV points" Code Example lines 224-247 — each hourly bucket carries `{dt, uvi}`; daily[0] carries `sunrise`/`sunset`)
    - .planning/phases/14-uv-index-on-demand-daily-briefing/14-PATTERNS.md (the fixtures section + the client.py VERIFY-ONLY note — do NOT edit client.py)
  </read_first>
  <action>
    Add a populated `hourly[]` array to each of the three UV fixtures (do NOT edit
    `client.py` — the `exclude` widening is already in place from Phase 12; this is
    fixture + test work only). Each `hourly[i]` element carries at minimum `dt` (Unix
    UTC epoch, consecutive hours spanning today's local daytime) and `uvi` (float).
    Ensure each fixture's `daily[0]` carries `sunrise`/`sunset` epochs consistent with
    the hourly timestamps and the location tz, and a `daily[0].uvi` (the day's max).
    Build three deterministic scenarios:
    (a) `onecall_imperial_uvcross.json` — UV rises from below the default threshold 6
    through it mid-morning, peaks, then drops back below before sunset (exercises
    up-cross, peak, and down-cross/window-end);
    (b) `onecall_imperial_uvbelow.json` — UV never reaches 6 across the whole daytime
    (exercises stays-below);
    (c) extend `onecall_imperial_highuv.json` with an `hourly[]` whose first daytime
    point is already at/above 6 (exercises the already-above-at-sunrise branch).
    Keep timestamps anchored to a fixed reference date so Plan 14-02 can pin `now=` and
    assert exact interpolated minute values. Add a regression test to
    `tests/test_client.py` (the canary the `client.py` docstring references) asserting
    that a `fetch_onecall` call (with a mocked httpx response echoing the request) does
    NOT send `hourly` in `exclude`, i.e. the returned payload retains a non-empty
    `hourly[]` carrying `uvi` — fail loud here if Phase 12's `exclude` change ever
    regressed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_client.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import json; d=json.load(open('tests/fixtures/onecall_imperial_uvcross.json')); print(len(d['hourly']), 'uvi' in d['hourly'][0])"` → a count > 0 and `True`
    - `uv run python -c "import json; d=json.load(open('tests/fixtures/onecall_imperial_uvbelow.json')); print(max(h['uvi'] for h in d['hourly']) < 6)"` → `True`
    - `uv run python -c "import json; d=json.load(open('tests/fixtures/onecall_imperial_highuv.json')); print('hourly' in d and len(d['hourly'])>0)"` → `True`
    - `uv run pytest tests/test_client.py -x` passes (incl. the new `hourly`-retained regression test)
    - `grep -c '"exclude"' weatherbot/weather/client.py` → 1 and `grep -c 'minutely,hourly' weatherbot/weather/client.py` → 0 (VERIFY-ONLY; not re-edited)
  </acceptance_criteria>
  <done>Three fixtures carry a deterministic `hourly[].uvi`; the client regression test proves `hourly[]` is retained; `client.py` is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → loader | Operator-edited `[uv]` table is untrusted input crossing into the validated Config |
| OpenWeather → client | Remote payload (now incl. `hourly[]`) crosses into the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `[uv]` config table | mitigate | UvConfig `extra="forbid"` + range/non-negative validators fail loud at load; a malformed `[uv]` aborts the load, the existing reload keeps the last-good Config (UV-03 fail-loud) |
| T-14-02 | DoS | malformed `[uv]` crashing the 9am send | mitigate | Validation is at load/reload only; a bad edit never reaches the briefing spine (reload-keeps-old-on-bad-config, existing `validate_config_and_templates`). **Briefing-spine isolation:** Config defaults mean an absent/blank `[uv]` can never gate a send |
| T-14-03 | Information Disclosure | client request URL carrying `appid` | accept | Unchanged from Phase 2 — `httpx` logger already at WARNING (`client.py:39`); the fixture/test work alters no logging and `client.py` is not edited |
</threat_model>

<verification>
- `uv run pytest tests/test_config_uv.py tests/test_client.py -x` green.
- Full suite (`uv run pytest`) green — no regression from the new config field or fixtures.
- `client.py` is byte-for-byte unchanged (VERIFY-ONLY).
</verification>

<success_criteria>
- `[uv]` table loads, validates (range + non-negative + unknown-key), and hot-reloads (UV-03).
- Three fixtures carry a deterministic `hourly[].uvi` + `daily[0]` sunrise/sunset/uvi.
- The `hourly[]` regression canary passes in `tests/test_client.py`.
</success_criteria>

<output>
Create `.planning/phases/14-uv-index-on-demand-daily-briefing/14-01-SUMMARY.md` when done
</output>
