---
phase: 23-scheduler-engine-occurrencestore-jobstore-seam
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - weatherbot/scheduler/daemon.py
autonomous: true
requirements:
  - SEAM-02
must_haves:
  truths:
    - "Every job type (briefing, forecast, uvmonitor, heartbeat) re-registers through engine.register(...) instead of a bare scheduler.add_job(...)."
    - "The _register_jobs enumeration loop stays app-side as the Phase-24 desired_jobs seed; only the per-job add_job call swaps to engine.register."
    - "run_daemon startup ordering (announce → register → catch-up → scheduler.start()) stays byte-identical; the app still constructs and starts/shuts down the scheduler."
    - "_reconcile_jobs reads live ids via engine.list_live_ids() (with the same __heartbeat__/__uvmonitor__ exclusion) and removes via engine.remove(...)."
    - "The full ~740-test suite + every Phase-21 golden (schedule plan, sent_log DB rows, 25 DST/catch-up, exactly-once-across-reload) stay byte-identical green."
  prohibitions:
    - "MUST NOT modify fire_slot / fire_forecast_slot — D-06a define-only: they keep calling concrete claim_slot/was_sent/release_claim byte-identically. No OccurrenceStore instance is threaded through fire_slot."
    - "MUST NOT move or modify _desired_job_ids, _reconcile_jobs body, _restore_jobs, _do_reload (Phase 24 / D-16) — only the list_live_ids/remove read-throughs inside reconcile rebind."
    - "MUST NOT change catchup.py's plan_catchup signature or its was_sent(name, time, date) call shape (Pitfall 3 — 25 DST goldens pin the arg order)."
    - "MUST NOT import SchedulerEngine/ports at weatherbot/scheduler/__init__.py top level — import inside daemon.py (Pitfall 4 — PEP-562 cycle)."
    - "MUST NOT alter any sent_log row, UNIQUE key, or the INSERT OR IGNORE primitive (store.py untouched)."
    - "MUST NOT add max_instances/coalesce/misfire_grace_time at the daemon call sites — they now live only in engine.register()."
  artifacts:
    - path: "weatherbot/scheduler/daemon.py"
      provides: "All 4 job registrations + reconcile read-throughs routed through SchedulerEngine"
      contains: "SchedulerEngine"
  key_links:
    - from: "weatherbot/scheduler/daemon.py _register_jobs"
      to: "yahir_reusable_bot.scheduler.SchedulerEngine.register"
      via: "engine.register(job_id, trigger, callback, args=, kwargs=, replace_existing=)"
      pattern: "engine\\.register\\("
    - from: "weatherbot/scheduler/daemon.py _reconcile_jobs"
      to: "yahir_reusable_bot.scheduler.SchedulerEngine.list_live_ids / remove"
      via: "engine.list_live_ids() with the internal-id exclusion + engine.remove(job_id)"
      pattern: "engine\\.list_live_ids\\(|engine\\.remove\\("
---

<objective>
Adapt `weatherbot/scheduler/daemon.py` (adapt-don't-rewrite) to route every job registration through
the new `SchedulerEngine`, splitting `_register_jobs` into app-side enumeration + engine-side
registration, and rebinding the reconcile `get_jobs()`/`remove_job` read-throughs onto the engine —
all byte-identically. Then drive a self-UAT proving the full suite + every Phase-21 golden stays
byte-identical.

Purpose: This is the consuming half of the seam. `_register_jobs`'s enumeration loop is the future
Phase-24 `desired_jobs` seed and stays app-side; the engine absorbs the 4 copy-pasted invariant
kwargs. D-06a is honored: `fire_slot`/`fire_forecast_slot` are NOT modified (the ports stay
defined-but-unconsumed type contracts) and `catchup.py` is NOT touched. The byte-identical oracle
is the full ~740-test suite + the Phase-21 goldens — any non-empty snapshot diff is a failure to
investigate, never rubber-stamped.
Output: rebound `weatherbot/scheduler/daemon.py`; a self-UAT log.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-CONTEXT.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-RESEARCH.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-PATTERNS.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-VALIDATION.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-01-SUMMARY.md

# The file being adapted + its boundaries:
@weatherbot/scheduler/daemon.py
@weatherbot/scheduler/__init__.py
@weatherbot/scheduler/catchup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route _register_jobs (briefing + forecast) + heartbeat + uvmonitor through SchedulerEngine</name>
  <files>weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py: _register_jobs (briefing loop ~614-644, forecast loop ~655-680), uvmonitor add_job (~751-764), heartbeat add_job (~1433-1440), the import block (~L60-79), run_daemon startup ordering (~1419-1447)
    - weatherbot/scheduler/__init__.py (PEP-562 lazy run_daemon — Pitfall 4)
    - 23-PATTERNS.md § daemon.py (controller — ADAPT, DON'T REWRITE)
    - 23-RESEARCH.md Pattern 1, Pitfall 1/2/4, § Code Examples (splitting _register_jobs)
  </read_first>
  <action>
    Add `from yahir_reusable_bot.scheduler import SchedulerEngine` INSIDE `daemon.py` (in its import
    block, NOT at `weatherbot/scheduler/__init__.py` top level — Pitfall 4: the barrel runs during
    `weatherbot.config.models`'s `parse_days` import and an eager engine import there could
    re-introduce the cycle the PEP-562 lazy export dodges).

    In `_register_jobs`: build `engine = SchedulerEngine(scheduler)` once at the top of the function
    (the enumeration loop STAYS — it is the Phase-24 `desired_jobs` seed, D-15). Replace the
    briefing-loop `scheduler.add_job(fire_slot, trigger=..., kwargs=..., args=..., id=...,
    replace_existing=..., misfire_grace_time=None, coalesce=True)` (~620-644) with
    `engine.register(f"{location.name}|{slot.time}|{slot.days}", CronTrigger(hour=hh, minute=mm,
    day_of_week=slot.day_of_week, timezone=location.timezone), fire_slot, args=[location, slot],
    kwargs={...same dict...}, replace_existing=replace_existing)` — the 3 invariant kwargs DISAPPEAR
    from the call site (they now live in `engine.register`). Apply the identical swap to the forecast
    loop (~659-680: `fire_forecast_slot`, `_forecast_job_id(location, fc)`, `args=[location, fc]`).
    Pass the SAME trigger object the caller builds straight through (D-01 — keeps the schedule-plan
    golden `next_run_time` byte-identical). Do NOT change the kwargs dict contents, the id format, or
    the enumeration/enabled-slot filter.

    Apply the same engine.register swap to the uvmonitor registration (~751-764, `__uvmonitor__`,
    `IntervalTrigger`) and the heartbeat registration (~1433-1440, `__heartbeat__`,
    `IntervalTrigger`) — every job type re-registers through the engine (D-04). For the uvmonitor
    site that currently passes `max_instances=1` explicitly, drop it (now baked in); for heartbeat
    (which omits it today) the default-of-1 baking is byte-identical (proven by Plan 01's read-back
    test). Build a `SchedulerEngine(scheduler)` at each of these registration sites (or reuse one if
    in the same scope) — keep it a thin local wrapper; do NOT change which scheduler instance is
    used.

    Do NOT touch `run_daemon`'s startup ordering (announce → register → catch-up →
    `scheduler.start()`): the app keeps constructing the scheduler and calling
    `scheduler.start()`/`scheduler.shutdown(wait=False)` (engine is a thin registrar, A4/D-15). Do
    NOT modify `fire_slot`/`fire_forecast_slot` (D-06a). Do NOT touch `_desired_job_ids`,
    `_restore_jobs`, `_do_reload` (D-16).
  </action>
  <verify>
    <automated>uv run pytest tests/test_golden_schedule.py tests/test_reliability.py tests/test_scheduler_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All 4 registration sites (briefing, forecast, uvmonitor, heartbeat) call `engine.register(...)`; no bare `scheduler.add_job(` remains in `_register_jobs`/uvmonitor/heartbeat registration.
    - No `misfire_grace_time`/`coalesce`/`max_instances` kwarg remains at any daemon registration call site.
    - `SchedulerEngine` imported inside `daemon.py`, not at the scheduler package barrel top.
    - `fire_slot`/`fire_forecast_slot` bodies unchanged; `_desired_job_ids`/`_restore_jobs`/`_do_reload` unchanged.
    - `tests/test_golden_schedule.py` byte-identical green.
  </acceptance_criteria>
  <done>All 4 job types register through the engine; schedule-plan golden + engine read-back green.</done>
</task>

<task type="auto">
  <name>Task 2: Rebind _reconcile_jobs read-throughs onto engine.list_live_ids()/remove()</name>
  <files>weatherbot/scheduler/daemon.py</files>
  <read_first>
    - weatherbot/scheduler/daemon.py: _reconcile_jobs live_ids comprehension (~804-808) and remove_job loop (~830-833)
    - 23-PATTERNS.md § daemon.py "Reconcile read" (D-04 — read-through rebind only, function body stays)
    - 23-RESEARCH.md § Code Examples (list_live_ids consumed by reconcile)
  </read_first>
  <action>
    In `_reconcile_jobs` ONLY rebind the two scheduler read-throughs (the function body, diff logic,
    and the `_register_jobs(..., replace_existing=True)` add/replace call all stay — this is NOT
    moving reconcile, which is Phase 24 / D-16). Build `engine = SchedulerEngine(scheduler)` at the
    top of `_reconcile_jobs`. Replace the live-ids comprehension `{j.id for j in
    scheduler.get_jobs() if j.id not in ("__heartbeat__", "__uvmonitor__")}` (~804-808) with
    `{jid for jid in engine.list_live_ids() if jid not in ("__heartbeat__", "__uvmonitor__")}` —
    the `__heartbeat__`/`__uvmonitor__` exclusion stays an APP-side convention the engine never
    learns (D-04). Replace `scheduler.remove_job(job_id)` (~832) with `engine.remove(job_id)`. Leave
    every other line of `_reconcile_jobs` and the returned counters byte-identical.
  </action>
  <verify>
    <automated>uv run pytest tests/test_catchup*.py tests/test_golden_db.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_reconcile_jobs` reads live ids via `engine.list_live_ids()` and removes via `engine.remove(...)`.
    - The `__heartbeat__`/`__uvmonitor__` exclusion filter is unchanged and stays app-side.
    - The reconcile add/replace path (`_register_jobs(..., replace_existing=True)`) and returned counters are unchanged.
    - DST/catch-up + DB-row goldens byte-identical green.
  </acceptance_criteria>
  <done>Reconcile read-throughs route through the engine; reconcile body otherwise byte-identical; goldens green.</done>
</task>

<task type="auto">
  <name>Task 3: Self-UAT — full suite + every Phase-21 golden byte-identical</name>
  <files>.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-02-SELF-UAT.md</files>
  <read_first>
    - 23-VALIDATION.md § Sampling Rate + § Per-Task Verification Map
    - CLAUDE.md § Verification Policy — Two-Gate UAT (Gate 1 agent self-UAT)
  </read_first>
  <action>
    Run the full byte-identical oracle and record a persistent self-UAT log (Gate 1, autonomous —
    per the project two-gate policy, do NOT emit a blocking human-verify checkpoint; the live
    `yahir-mint` restart-catch-up is a deferred Gate-2 milestone obligation). Execute, capturing the
    exact command + output + PASS/FAIL per criterion:
    (1) `uv run pytest -q` — full ~740-test suite green, zero-flake.
    (2) `uv run pytest tests/test_golden_schedule.py tests/test_golden_db.py tests/test_catchup*.py -q`
        — schedule-plan, sent_log DB-row, and the 25 DST/catch-up goldens byte-identical (any
        non-empty snapshot diff is a FAIL to investigate, never rubber-stamped).
    (3) `uv run pytest tests/test_import_hygiene.py -q` — grimp (module imports zero app code) +
        isolated-import smoke + litmus (no weather noun in the new scheduler/ports signatures) all
        green, auto-scaled to the new files (PKG-01 / APP-02 litmus).
    (4) `uv run pytest tests/test_scheduler_engine.py tests/test_ports.py -q` — the Wave-0 read-back
        + structural-satisfaction oracle green.
    Write `23-02-SELF-UAT.md` recording, per criterion: what was tested, the exact command,
    evidence/output tail, and PASS/FAIL/PARTIAL. A fully-passing log completes the phase
    autonomously. On any FAIL, halt and escalate with the diff/evidence (a non-empty golden diff is
    a behavior regression to root-cause, not to overwrite).
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (~740 tests, zero-flake).
    - Every Phase-21 golden byte-identical (schedule plan, sent_log DB rows, 25 DST/catch-up, exactly-once-across-reload).
    - `test_import_hygiene.py` green (PKG-01 grimp + APP-02 litmus).
    - `23-02-SELF-UAT.md` written with per-criterion command + evidence + verdict.
  </acceptance_criteria>
  <done>Self-UAT log shows full suite + all goldens byte-identical green; phase boundary met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app orchestration → module engine | `daemon.py` passes already-validated `(job_id, trigger, callback, args, kwargs)` into `engine.register`. No new external input crosses; relocation only. |
| app → SQLite `sent_log` | The `OccurrenceStore` adapter body (`claim_slot`/`was_sent`/`release_claim`) stays in `store.py`, parameterized `?`-only, untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | `sent_log` exactly-once SQL (`INSERT OR IGNORE … rowcount==1`) | mitigate | D-06a/D-09 keep `store.py`'s parameterized `?`-only binds byte-identical; the daemon rebind never inline-formats a key into SQL (no SQL touched at all). SQLi mitigation T-03-01 preserved. |
| T-23-04 | Information disclosure | engine/daemon job logging | accept | Registration logging stays outcome-only; the engine never names a secret; `kwargs` client/channel hold secrets internally and are never logged. No new log surface (T-04-01 preserved). |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed — relocation within the frozen, already-locked stack (RESEARCH § Package Legitimacy Audit: empty by design). No install checkpoint required. |
</threat_model>

<verification>
- `uv run pytest -q` — full ~740-test suite green, zero-flake.
- `uv run pytest tests/test_golden_schedule.py tests/test_golden_db.py tests/test_catchup*.py -q` — all Phase-21 goldens byte-identical.
- `uv run pytest tests/test_import_hygiene.py -q` — grimp + isolated-import smoke + litmus green (PKG-01 / APP-02).
- `uv run pytest tests/test_scheduler_engine.py tests/test_ports.py -q` — Wave-0 oracle green.
- `git diff weatherbot/scheduler/catchup.py weatherbot/weather/store.py` — empty (no diff; D-14/D-09 untouched).
</verification>

<success_criteria>
- Every WeatherBot job (briefing/forecast/uvmonitor/heartbeat) registers through `SchedulerEngine.register(job_id, trigger, callback)` (SEAM-02 success criterion 1).
- `run_daemon` startup ordering + the app-owned scheduler lifecycle stay byte-identical.
- `_reconcile_jobs` consumes `engine.list_live_ids()`/`engine.remove()` with the app-side exclusion intact; reconcile/reload functions otherwise untouched (D-16).
- `fire_slot`/`fire_forecast_slot`/`catchup.py`/`store.py` unchanged (D-06a/D-14/D-09).
- Full suite + every Phase-21 golden byte-identical; self-UAT log records the evidence.
</success_criteria>

<output>
Create `.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-02-SUMMARY.md` when done.
</output>
