---
phase: 23-scheduler-engine-occurrencestore-jobstore-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - yahir_reusable_bot/scheduler/__init__.py
  - yahir_reusable_bot/scheduler/engine.py
  - yahir_reusable_bot/ports/occurrence.py
  - yahir_reusable_bot/ports/jobstore.py
  - yahir_reusable_bot/ports/__init__.py
  - tests/test_scheduler_engine.py
  - tests/test_ports.py
autonomous: true
requirements:
  - SEAM-02
  - SEAM-03
must_haves:
  truths:
    - "A SchedulerEngine wraps a host-owned BackgroundScheduler and registers a job with the proven invariant kwargs baked in once."
    - "A registered job (including a briefing-shaped job that omits max_instances at its call site) reads back with misfire_grace_time=None, coalesce=True, max_instances=1."
    - "engine.list_live_ids() equals {j.id for j in scheduler.get_jobs()} and engine.remove(id) drops the job."
    - "OccurrenceStore and JobStore are runtime_checkable Protocols structurally satisfiable with no subclassing."
    - "MemoryJobStore is importable from the ports barrel and carries jobs without serializing."
    - "The new module files import zero weatherbot code and name no weather noun in their signatures."
  prohibitions:
    - "MUST NOT import anything from weatherbot inside any yahir_reusable_bot file (grimp gate)."
    - "MUST NOT name weather|forecast|location|openweather|uv|briefing in any def/class/param/annotation under yahir_reusable_bot (litmus gate)."
    - "MUST NOT add engine.cron()/interval()/date() trigger sugar (D-02)."
    - "MUST NOT build any durable JobStore backend — Protocol + MemoryJobStore only (D-10)."
    - "MUST NOT make the engine own scheduler.start()/shutdown() — thin registrar only (D-15 / A4)."
    - "MUST NOT import the engine or ports at yahir_reusable_bot/scheduler/__init__.py top level beyond sibling imports."
  artifacts:
    - path: "yahir_reusable_bot/scheduler/engine.py"
      provides: "SchedulerEngine thin registrar (register/remove/list_live_ids)"
      contains: "class SchedulerEngine"
    - path: "yahir_reusable_bot/ports/occurrence.py"
      provides: "OccurrenceStore Protocol (claim/was_fired/release)"
      contains: "class OccurrenceStore"
    - path: "yahir_reusable_bot/ports/jobstore.py"
      provides: "JobStore Protocol + MemoryJobStore impl"
      contains: "class JobStore"
    - path: "tests/test_scheduler_engine.py"
      provides: "Job-options read-back + list_live_ids/remove coverage"
    - path: "tests/test_ports.py"
      provides: "Protocol runtime_checkable + structural-satisfaction coverage"
  key_links:
    - from: "yahir_reusable_bot/scheduler/engine.py"
      to: "apscheduler BackgroundScheduler.add_job"
      via: "self._scheduler.add_job with the 3 invariant kwargs"
      pattern: "add_job\\("
    - from: "yahir_reusable_bot/ports/__init__.py"
      to: "yahir_reusable_bot/ports/occurrence.py, ports/jobstore.py"
      via: "barrel re-export"
      pattern: "from .occurrence import|from .jobstore import"
---

<objective>
Create the module-side artifacts for the scheduler seam: the `SchedulerEngine` thin registrar,
the `OccurrenceStore` and `JobStore` Protocols (+ `MemoryJobStore` impl), the barrel exports, and
the two Wave-0 safety-net test files that pin the byte-identical job-options contract BEFORE the
daemon rebind (Plan 02) consumes them.

Purpose: This is the additive, app-code-free half of the extraction. It defines the seam contracts
and the read-back oracle that proves centralizing the 4 copy-pasted `add_job` invariant kwargs into
`register()` is behavior-preserving (the schedule-plan golden does NOT cover those kwargs — Pitfall
1/2). D-06a is honored: the ports ship as defined-but-unconsumed type contracts (the Phase-22
`AlertSink` precedent); `fire_slot` is NOT touched here.
Output: `yahir_reusable_bot/scheduler/{__init__,engine}.py`, `yahir_reusable_bot/ports/{occurrence,jobstore}.py`,
extended `ports/__init__.py`, `tests/test_scheduler_engine.py`, `tests/test_ports.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-CONTEXT.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-RESEARCH.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-PATTERNS.md
@.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-VALIDATION.md

# Clone targets (read before writing — exact recipe to mirror):
@yahir_reusable_bot/ports/alerts.py
@yahir_reusable_bot/ports/__init__.py
@yahir_reusable_bot/channels/__init__.py

# Adapter bodies the OccurrenceStore Protocol shapes to (DO NOT modify here):
@weatherbot/weather/store.py

# The 4 add_job call sites the engine absorbs (read for the invariant kwarg set):
@weatherbot/scheduler/daemon.py

# Read-back test analog:
@tests/test_reliability.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SchedulerEngine thin registrar + scheduler barrel</name>
  <files>yahir_reusable_bot/scheduler/engine.py, yahir_reusable_bot/scheduler/__init__.py</files>
  <read_first>
    - yahir_reusable_bot/channels/__init__.py (barrel shape to clone exactly)
    - weatherbot/scheduler/daemon.py lines 620-644 (briefing add_job — the invariant kwarg set), 659-680 (forecast), 751-764 (uvmonitor — passes max_instances=1), 1433-1440 (heartbeat)
    - 23-PATTERNS.md § engine.py + § scheduler/__init__.py
  </read_first>
  <action>
    Create `yahir_reusable_bot/scheduler/engine.py` defining a `SchedulerEngine` class per
    D-01/D-03/D-15 (thin registrar over a host-owned scheduler — the app keeps constructing and
    starting/shutting down the scheduler, the engine only registers/removes/lists). `__init__`
    takes the scheduler instance and stores it on a private attribute. Implement three methods with
    neutral names only (litmus): `register(self, job_id, trigger, callback, *, args=None,
    kwargs=None, replace_existing=False) -> None` forwarding to `self._scheduler.add_job(callback,
    trigger=trigger, id=job_id, args=args, kwargs=kwargs, replace_existing=replace_existing,
    misfire_grace_time=None, coalesce=True, max_instances=1)` — the native trigger object passes
    THROUGH untouched (D-01, keeps the schedule-plan golden byte-identical); the three invariant
    kwargs are baked in here so they cannot drift per D-03. `remove(self, job_id) -> None` forwards
    to `self._scheduler.remove_job(job_id)`. `list_live_ids(self) -> set[str]` returns
    `{job.id for job in self._scheduler.get_jobs()}`. Use `from __future__ import annotations` and
    type the trigger/callback/args/kwargs loosely (Any/Callable) so the engine imports nothing from
    weatherbot. Write a class docstring naming the centralized-invariants rationale and that the
    host owns start/shutdown (do NOT name a weather noun). Do NOT add cron/interval/date sugar
    methods (D-02). Then create `yahir_reusable_bot/scheduler/__init__.py` as a barrel cloned from
    `channels/__init__.py`: module docstring, `from __future__ import annotations`, `from .engine
    import SchedulerEngine`, `__all__ = ["SchedulerEngine"]`. The barrel imports only from its
    sibling — never from weatherbot (grimp gate).
  </action>
  <verify>
    <automated>uv run python -c "from yahir_reusable_bot.scheduler import SchedulerEngine; print(SchedulerEngine)"</automated>
  </verify>
  <acceptance_criteria>
    - `SchedulerEngine` importable from the `yahir_reusable_bot.scheduler` barrel.
    - `register` forwards `misfire_grace_time=None`, `coalesce=True`, `max_instances=1` to `add_job`.
    - `remove` and `list_live_ids` present; no `start`/`shutdown` on the class.
    - No `import` of any `weatherbot.*` symbol anywhere in either new file.
  </acceptance_criteria>
  <done>SchedulerEngine + barrel created, importable, app-code-free, with the 3 baked invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Create OccurrenceStore + JobStore Protocols and extend ports barrel</name>
  <files>yahir_reusable_bot/ports/occurrence.py, yahir_reusable_bot/ports/jobstore.py, yahir_reusable_bot/ports/__init__.py</files>
  <read_first>
    - yahir_reusable_bot/ports/alerts.py (exact Protocol recipe to clone — header, @runtime_checkable, neutral param names, str | os.PathLike handle)
    - yahir_reusable_bot/ports/__init__.py (barrel to extend)
    - weatherbot/weather/store.py claim_slot/was_sent/release_claim signatures (the adapter bodies the OccurrenceStore shapes to; DO NOT modify)
    - 23-PATTERNS.md § occurrence.py + § jobstore.py + § ports/__init__.py
    - 23-RESEARCH.md Pattern 2, Pattern 3, Pitfall 5, Pitfall 6
  </read_first>
  <action>
    Create `yahir_reusable_bot/ports/occurrence.py` cloning `alerts.py` structure exactly:
    `from __future__ import annotations`, `import os`, `from typing import Protocol,
    runtime_checkable`, then `@runtime_checkable class OccurrenceStore(Protocol)`. Name the full
    claim lifecycle per D-07 with NEUTRAL params only (Pitfall 5 — the store's native
    `location_name` would trip the `location` litmus, so use `key`): `claim(self, handle: str |
    os.PathLike[str], key: str, occurrence: str) -> bool` (the atomic check-and-mark taken before
    the side-effecting work; True iff this caller won), `was_fired(self, handle, key, occurrence)
    -> bool` (the read), `release(self, handle, key, occurrence) -> None` (re-opens a claim when
    later work fails so the occurrence stays re-fireable). Method bodies are `...`. Write a Protocol
    docstring describing the generic exactly-once gate keyed on `(job identity, occurrence)` —
    structurally satisfied by a host INSERT-OR-IGNORE adapter, no subclassing (D-08). Name no
    weather noun.

    Create `yahir_reusable_bot/ports/jobstore.py` with the same header/decorator pattern: a minimal
    `@runtime_checkable class JobStore(Protocol)` whose body is intentionally minimal — the PAYLOAD
    is the docstring (D-11/D-12/D-13). The docstring encodes the three serialization constraints
    that are already true of today's jobs: (1) importable module-level callback referenceable by
    import path; (2) picklable identity-style args — plain-string id, plain-data positional args,
    never a live client/socket/channel/threading primitive closed over args; (3) per-fire keyword
    data carries a holder/registry re-resolved at fire time, never a baked snapshot. Then a separate
    DURABLE-STORE BOUNDARY paragraph (D-13) stating — named here, built nowhere — that today's jobs
    thread non-picklable runtime handles (a live client, a delivery channel, a stop signal, a config
    holder) through per-fire keyword data, and a durable impl that serializes would relocate those
    to a process-level registry resolved BY ID at fire time. Describe these by concept; do not echo
    the literal raw litmus tokens. Below the Protocol add `class MemoryJobStore` (the shipped
    config-rederive impl): a trivial class with a docstring saying it holds jobs directly and never
    serializes (so the non-picklable handles are carried as-is), the job set re-derived from config
    on each restart; minimal body (e.g. an empty `__init__` or `...`). All public names
    (`JobStore`, `MemoryJobStore`, `claim`, `was_fired`, `release`, `occurrence`, `handle`, `key`,
    `job_id`) are litmus-clean.

    Extend `yahir_reusable_bot/ports/__init__.py`: add `from .occurrence import OccurrenceStore` and
    `from .jobstore import JobStore, MemoryJobStore`, and update `__all__` to
    `["AlertSink", "OccurrenceStore", "JobStore", "MemoryJobStore"]`.
  </action>
  <verify>
    <automated>uv run python -c "from yahir_reusable_bot.ports import OccurrenceStore, JobStore, MemoryJobStore; from typing import runtime_checkable; print(OccurrenceStore, JobStore, MemoryJobStore)"</automated>
  </verify>
  <acceptance_criteria>
    - `OccurrenceStore`, `JobStore`, `MemoryJobStore` all importable from the ports barrel.
    - `OccurrenceStore` names `claim`/`was_fired`/`release` with `handle`/`key`/`occurrence` params.
    - `JobStore` docstring encodes the 3 serialization constraints + names the durable-store boundary (build nothing).
    - No `weatherbot` import in any of the three files.
  </acceptance_criteria>
  <done>Both ports + MemoryJobStore created, barrel extended, app-code-free, litmus-clean names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave-0 safety-net tests — engine read-back + port structural satisfaction</name>
  <files>tests/test_scheduler_engine.py, tests/test_ports.py</files>
  <read_first>
    - tests/test_reliability.py::test_heartbeat_job_registered_with_slots (the read-back analog)
    - 23-RESEARCH.md Pitfall 1, Pitfall 2, Pitfall 6, § Wave 0 Gaps
    - 23-PATTERNS.md § test_scheduler_engine.py + § test_ports.py
  </read_first>
  <behavior>
    - test_scheduler_engine: register a briefing-shaped job (one that does NOT pass max_instances at the call site) through SchedulerEngine on a non-started BackgroundScheduler; read it back via scheduler.get_jobs() and assert job.misfire_grace_time is None, job.coalesce is True, job.max_instances == 1. This proves the centralization (Pitfall 1) AND the default-of-1 baking (Pitfall 2).
    - test_scheduler_engine: assert engine.list_live_ids() == {j.id for j in scheduler.get_jobs()} after registering ≥2 jobs.
    - test_scheduler_engine: assert engine.remove(job_id) drops that id from list_live_ids().
    - test_scheduler_engine: confirm APScheduler's bare add_job default max_instances == 1 on a job registered WITHOUT the kwarg (the assumption the centralization rests on, A1).
    - test_ports: assert OccurrenceStore and JobStore are runtime_checkable Protocols.
    - test_ports: assert MemoryJobStore() is structurally usable; assert a small class instance exposing claim/was_fired/release passes isinstance(obj, OccurrenceStore). Per Pitfall 6, test against a class INSTANCE, not the bare store module.
  </behavior>
  <action>
    Create `tests/test_scheduler_engine.py` modeled on
    `test_reliability.py::test_heartbeat_job_registered_with_slots`. Construct a non-started
    `BackgroundScheduler`, wrap it in `SchedulerEngine`, and `register(...)` a job with a
    `CronTrigger` and `args`/`kwargs` shaped like a briefing job (a plain callback function defined
    in the test, plain-data args). Read the job back via `scheduler.get_jobs()` and assert the three
    job options (`misfire_grace_time is None`, `coalesce is True`, `max_instances == 1`). Add a
    separate test that registers a bare `scheduler.add_job(fn, trigger=..., id=...)` with NO
    `max_instances` and asserts the read-back `max_instances == 1` (confirms the APScheduler default
    the centralization relies on — A1/Pitfall 2). Add tests for `list_live_ids()` equality with
    `get_jobs()` ids and for `remove()` dropping a job. Never call `scheduler.start()` (registration
    + read-back only; keeps the test fast and deterministic). Create `tests/test_ports.py` writing
    asserts directly from the Protocol contracts (no `AlertSink` test analog exists): assert
    `OccurrenceStore`/`JobStore` are `runtime_checkable` (define a tiny conforming class with
    `claim`/`was_fired`/`release` methods and assert `isinstance(instance, OccurrenceStore)` is
    True; per Pitfall 6 use a class INSTANCE, not the bare `store` module), and assert
    `MemoryJobStore()` instantiates. Keep both files import-clean (they may import from
    `yahir_reusable_bot.*` and `apscheduler`; the engine test imports zero `weatherbot` symbols).
  </action>
  <verify>
    <automated>uv run pytest tests/test_scheduler_engine.py tests/test_ports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_scheduler_engine.py` proves a briefing-shaped registered job reads back with all 3 invariant options.
    - A test confirms bare `add_job` default `max_instances == 1` (A1/Pitfall 2).
    - `list_live_ids()` / `remove()` covered.
    - `tests/test_ports.py` proves both Protocols are `runtime_checkable` and structurally satisfiable (instance-based, Pitfall 6).
    - Both files green.
  </acceptance_criteria>
  <done>Both new test files exist and pass — the byte-identical job-options oracle for Plan 02 is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → module | The app constructs/owns the BackgroundScheduler and passes already-validated `(job_id, trigger, callback)` into the engine. No new external input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information disclosure | `SchedulerEngine` logging / job repr | accept | Engine logging is outcome-only and the engine never names a secret; `args` carry plain-data models, `kwargs` client/channel hold secrets internally and are never logged here. No new log surface added (T-04-01 preserved). |
| T-23-02 | Tampering / DoS | `JobStore` serialization contract | mitigate | The `JobStore` docstring contract forbids closing a live client/socket/channel into `args` and names the durable-store registry boundary (build nothing). In-memory `MemoryJobStore` never serializes → no v2.0 attack surface. |
| T-23-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase — relocation within the frozen, already-locked stack (RESEARCH § Package Legitimacy Audit: empty by design). No `[ASSUMED]`/`[SUS]` package → no install checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/test_scheduler_engine.py tests/test_ports.py -q` green.
- `uv run pytest tests/test_import_hygiene.py -q` green (grimp gate confirms the new module files import zero app code; litmus gate confirms no weather noun in the new signatures — auto-scales by directory prefix, no gate edit).
- `uv run python -c "from yahir_reusable_bot.scheduler import SchedulerEngine; from yahir_reusable_bot.ports import OccurrenceStore, JobStore, MemoryJobStore"` succeeds.
- Wave-0 confirm: `[tool.coverage.run] source` already lists `yahir_reusable_bot` (PATTERNS L18, pyproject:53) → relocated code auto-covered; no pyproject change.
</verification>

<success_criteria>
- `SchedulerEngine`, `OccurrenceStore`, `JobStore`, `MemoryJobStore` exist, are importable, and import zero weatherbot code.
- The engine bakes the 3 invariant kwargs; a briefing-shaped read-back proves byte-identical job options.
- Both Protocols are `runtime_checkable` and structurally satisfiable with no subclassing.
- `test_import_hygiene.py` (grimp + litmus) green with the new files present.
</success_criteria>

<output>
Create `.planning/phases/23-scheduler-engine-occurrencestore-jobstore-seam/23-01-SUMMARY.md` when done.
</output>
