---
phase: 08-configholder-fire-slot-reads-from-holder-refactor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_config_holder.py
  - tests/test_models.py
autonomous: true
requirements: none (prerequisite — unblocks CFG-01/CFG-05 in Phase 9)
must_haves:
  truths:
    - "A NEW tests/test_config_holder.py exists with the full per-task test map from VALIDATION.md as importable, collectable test functions."
    - "tests/test_models.py has a frozen-mutation guard test parametrized over all five config models."
    - "The new tests land RED (fail/error because ConfigHolder and frozen=True do not exist yet), proving the scaffold actually exercises the not-yet-built behavior."
    - "The 215 pre-existing tests stay green (the scaffold adds RED cases without breaking any passing test)."
  artifacts:
    - path: "tests/test_config_holder.py"
      provides: "Holder unit + concurrency + mid-job snapshot + override + unchanged-job-renders-after-replace tests (SC#1/SC#2/D-01/D-04)"
      contains: "def test_concurrent_read_swap_safe"
      min_lines: 90
    - path: "tests/test_models.py"
      provides: "frozen=True mutation-guard test for all 5 models (D-02)"
      contains: "def test_frozen_rejects_mutation"
  key_links:
    - from: "tests/test_config_holder.py"
      to: "weatherbot.config.holder.ConfigHolder"
      via: "import (expected to fail RED until Plan 03 lands)"
      pattern: "from weatherbot.config.holder import ConfigHolder"
    - from: "tests/test_config_holder.py"
      to: "weatherbot.scheduler.daemon.fire_slot"
      via: "holder= keyword call in the integration tests"
      pattern: "fire_slot\\(.*holder="
---

<objective>
Lay down the complete Nyquist test scaffold for Phase 8 BEFORE any production code exists.
Create the NEW `tests/test_config_holder.py` (holder unit tests, concurrency safety,
mid-job snapshot retention, override-wins, and the core "unchanged job renders new config
after replace" proof) and EXTEND `tests/test_models.py` with a `frozen=True` mutation guard
across all five config models. These tests are written to the exact node IDs named in
08-VALIDATION.md and land RED (they import `ConfigHolder` and call `fire_slot(holder=...)`,
neither of which exists yet) — turning them GREEN is the acceptance gate for Plans 02–04.

Purpose: Wave 0 establishes the falsifiable specification. Every later plan is judged by
flipping a named subset of these RED tests to GREEN — no plan "looks done"; it is proven done.
Output: `tests/test_config_holder.py` (NEW) + a parametrized frozen-guard test added to
`tests/test_models.py`, with the 215 existing tests still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-CONTEXT.md
@.planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-RESEARCH.md
@.planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-PATTERNS.md
@.planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan (Wave 0) creates these test symbols. They reference symbols built later in the phase:
- NEW file `tests/test_config_holder.py` with: `test_current_returns_held`, `test_replace_rebinds`,
  `test_concurrent_read_swap_safe`, `test_inflight_job_keeps_snapshot`,
  `test_unchanged_job_renders_after_replace`, `test_config_override_wins`.
- `tests/test_models.py` extension: `test_frozen_rejects_mutation` (parametrized over 5 models).
- These tests reference (and intentionally fail-import until later plans ship): `ConfigHolder`,
  `ConfigHolder.current`, `ConfigHolder.replace`, and the `holder=` param on `fire_slot`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_config_holder.py with the full holder + fire_slot test map (RED)</name>
  <files>tests/test_config_holder.py</files>
  <read_first>
    - .planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-VALIDATION.md (the Per-Task Verification Map node IDs + concurrency/mid-job guidance — THIS is the spec)
    - .planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-PATTERNS.md (the `tests/test_config_holder.py` section: helper-reuse + recording-fake `send_now` patterns)
    - tests/test_reliability.py lines 355-414 (`_RecordingStop`, `_Channel`, `_config`, `_slot`, `_patch_send_now`) and lines 427-499 (a real `fire_slot` integration test to copy the call shape) and lines 54-58 (`_connect`)
    - tests/conftest.py lines 20-33 (`load_fixture`, `tmp_db` fixtures — reuse, do not re-invent)
    - weatherbot/config/models.py lines 31-224 (the 5 models the tests construct; `model_copy(update=...)` target fields)
  </read_first>
  <behavior>
    - test_current_returns_held: ConfigHolder(config_a).current() is config_a (SC#1a).
    - test_replace_rebinds: after holder.replace(config_b), holder.current() is config_b (SC#1b).
    - test_concurrent_read_swap_safe: ~8 reader threads loop `assert holder.current() is config_a or holder.current() is config_b` while one writer alternates replace(config_a)/replace(config_b) for a few thousand bounded iterations; all threads join(); the test fails if any thread recorded an exception or a torn/None read (SC#1c).
    - test_inflight_job_keeps_snapshot: inject a recording fake send_now (via _patch_send_now) that on first call records kwargs["config"], signals the test (threading.Event), and blocks on a second Event; the test calls holder.replace(config_b), releases the block, joins, and asserts the recorded config is config_a (SC#2a, single-read-per-fire).
    - test_unchanged_job_renders_after_replace: register/run a fire_slot reading the holder; replace(config_b); the SAME job now passes config_b into send_now (SC#2b / D-04, the phase's core proof).
    - test_config_override_wins: call fire_slot with explicit config=config_a while holder holds config_b; the recording send_now sees config_a (D-01).
  </behavior>
  <action>
    Create `tests/test_config_holder.py`. Import `ConfigHolder` via `from weatherbot.config.holder import ConfigHolder` (this import will FAIL RED until Plan 03 — that is intended and is the Nyquist signal). Import `weatherbot.scheduler.daemon as daemon_mod` and call `daemon_mod.fire_slot(loc, slot, holder=holder, ...)` in the integration tests. Reuse `_config`, `_slot`, `_RecordingStop`, `_Channel`, `_patch_send_now`, `_connect` by importing them from `tests.test_reliability` (or copy-importing the same construction shape) and reuse `tmp_db`/`load_fixture` from conftest — DO NOT invent new fixtures (VALIDATION Wave 0 #3). Build "config B" ONLY via `config_a.model_copy(update={"template": "other.txt"})` (Pitfall 5 — never hand-build, never mutate the frozen original). Write all SIX functions named EXACTLY as in 08-VALIDATION.md so the per-task acceptance node IDs resolve: `test_current_returns_held`, `test_replace_rebinds`, `test_concurrent_read_swap_safe`, `test_inflight_job_keeps_snapshot`, `test_unchanged_job_renders_after_replace`, `test_config_override_wins`. For the recording-fake pattern, the fake `send_now` appends `kwargs["config"]` to a list and returns `DeliveryResult(ok=True)`. For concurrency, collect exceptions into a shared list and `assert not errors` after `join()`; bound the iteration count (deterministic, no real sleeps). Do NOT add any code that hashes/sets/dict-keys a Config (Pitfall 1). NO production code in this plan — tests only.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_config_holder.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_config_holder.py` exists and is COLLECTABLE: `.venv/bin/python -m pytest tests/test_config_holder.py --collect-only -q` lists all six node IDs (`test_current_returns_held`, `test_replace_rebinds`, `test_concurrent_read_swap_safe`, `test_inflight_job_keeps_snapshot`, `test_unchanged_job_renders_after_replace`, `test_config_override_wins`).
    - Running the file lands RED (import error on `weatherbot.config.holder` and/or `fire_slot` missing `holder`): `.venv/bin/python -m pytest tests/test_config_holder.py -x` exits NON-zero. (RED is correct here — this is the Nyquist scaffold; Plans 03/04 turn it green.)
    - The file contains `def test_concurrent_read_swap_safe`, uses `model_copy(update=` to build config B, calls `fire_slot(` with `holder=`, and reuses `tmp_db` (no new SQLite fixture defined).
    - File is ≥ 90 lines.
  </acceptance_criteria>
  <done>All six named tests collect; the file fails RED for the right reason (ConfigHolder / holder param not yet implemented); no new fixtures introduced; config B built via model_copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/test_models.py with a frozen-mutation guard over all 5 models (RED)</name>
  <files>tests/test_models.py</files>
  <read_first>
    - tests/test_models.py lines 1-30 (existing imports + the module-level `LOC = Location(...)` analog at line 18 to reuse)
    - weatherbot/config/models.py lines 31-224 (the five models: Schedule line 31, Location line 82, WebhookIdentity line 120, Reliability line 132, Config line 212 — and a safe field to rebind on each)
    - .planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-RESEARCH.md Pitfall 2 (the guard raises `pydantic.ValidationError` type `frozen_instance`, NOT `dataclasses.FrozenInstanceError`)
    - .planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-PATTERNS.md (the `tests/test_models.py` EXTEND section — parametrize over the 5 models)
  </read_first>
  <behavior>
    - test_frozen_rejects_mutation: for EACH of Schedule, Location, WebhookIdentity, Reliability, Config — construct a valid instance, then attempt to rebind one existing field (e.g. Config.template = "other.txt", Schedule.time = "07:00", Location.name = "X", WebhookIdentity.username = "X", Reliability.attempts_per_burst = 9) and assert it raises `pydantic.ValidationError`.
  </behavior>
  <action>
    Add `import pydantic` and `import pytest` to `tests/test_models.py` if not present. Add a parametrized `test_frozen_rejects_mutation` that, for each of the five models, builds a minimal valid instance (reuse the existing module-level `LOC` for the Location/Config cases; build `Schedule(time="07:00", days="daily")`, `WebhookIdentity()`, `Reliability()` directly) and asserts `with pytest.raises(pydantic.ValidationError): instance.<field> = <new_value>`. Use `pytest.mark.parametrize` with one case per model so each is independently visible. Assert on `pydantic.ValidationError` — NEVER `dataclasses.FrozenInstanceError` (Pitfall 2). This test lands RED now because the models are not yet `frozen=True` (the mutation SUCCEEDS, so `pytest.raises` fails) — Plan 02 turns it green.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_models.py::test_frozen_rejects_mutation --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_models.py::test_frozen_rejects_mutation` collects with five parametrized cases: `.venv/bin/python -m pytest "tests/test_models.py::test_frozen_rejects_mutation" --collect-only -q` lists 5 items.
    - It lands RED now (models not yet frozen → mutation succeeds → `DID NOT RAISE`): `.venv/bin/python -m pytest "tests/test_models.py::test_frozen_rejects_mutation" -x` exits NON-zero.
    - The test asserts on `pydantic.ValidationError` (grep `tests/test_models.py` for `pydantic.ValidationError`; it must NOT reference `FrozenInstanceError`).
    - The rest of `tests/test_models.py` and the full pre-existing suite remain green: `.venv/bin/python -m pytest tests/test_models.py -q -k "not test_frozen_rejects_mutation"` passes.
  </acceptance_criteria>
  <done>The parametrized frozen guard collects 5 cases, fails RED for the right reason (models not yet frozen), asserts on `pydantic.ValidationError`, and no previously-passing model test regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds only test code. No external input, network, auth, or persisted-state surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Test scaffold mis-encodes the security-relevant invariant (torn read / lost swap under concurrency) | mitigate | `test_concurrent_read_swap_safe` is written in Wave 0 as the falsifiable spec for the holder's data-race correctness (the integrity property of this phase); it must collect and exercise `current()`/`replace()` concurrently. |
| T-08-02 | Information Disclosure | Secrets (`.env`/`Settings`) leaking into the reloadable surface | accept (N/A here) | No test in this plan places `Settings`/secrets into a holder; the holder owns `Config` only (Pitfall #12). Verified by absence — no `Settings`/`appid`/webhook-URL construction in the new test file. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `uv.lock` unchanged (RESEARCH Package Legitimacy Audit: not applicable). No install task in this phase. |

Injection/Spoofing/Repudiation/Elevation: N/A — test-only change, no runtime surface.
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_config_holder.py tests/test_models.py --collect-only -q` lists all new node IDs.
- New tests land RED for the intended reason (holder/frozen not yet built); the 215 pre-existing tests stay green: `.venv/bin/python -m pytest -q -k "not test_frozen_rejects_mutation" --ignore=tests/test_config_holder.py` passes (baseline preserved).
</verification>

<success_criteria>
- `tests/test_config_holder.py` exists with all six VALIDATION node IDs, collectable, RED.
- `tests/test_models.py::test_frozen_rejects_mutation` collects 5 parametrized cases, RED, asserting `pydantic.ValidationError`.
- No previously-passing test regresses.
</success_criteria>

<output>
Create `.planning/phases/08-configholder-fire-slot-reads-from-holder-refactor/08-01-SUMMARY.md` when done
</output>
