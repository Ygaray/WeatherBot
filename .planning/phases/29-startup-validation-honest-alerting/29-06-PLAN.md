---
phase: 29-startup-validation-honest-alerting
plan: 06
type: execute
wave: 1
depends_on: [29-02]
files_modified:
  - deploy/weatherbot.service
  - .planning/HUB-FINDINGS-HANDOFF.md
autonomous: true
requirements: [HARD-STARTUP-02, HARD-STARTUP-03]
must_haves:
  truths:
    - "`deploy/weatherbot.service` sets Restart=on-failure (no longer Restart=always)"
    - "`deploy/weatherbot.service` has StartLimitIntervalSec=300 and StartLimitBurst=5 in the [Unit] section"
    - "`deploy/weatherbot.service` still has TimeoutStartSec=infinity in [Service] (transient slow-key path protected)"
    - "The deferred hub ReadyGate first-class fatal-outcome enhancement is appended to HUB-FINDINGS-HANDOFF.md (not implemented)"
    - "D-06: the live effect of the weatherbot.service change (redeploy + systemctl daemon-reload on host yahir-mint) is NOT performed this phase — it is recorded as a deferred Gate-2 milestone-close obligation"
  artifacts:
    - deploy/weatherbot.service
    - .planning/HUB-FINDINGS-HANDOFF.md
  key_links:
    - "The static directive test in tests/test_service_unit.py (29-02) turns green when this unit edit lands"
    - "No hub source is edited; the ReadyGate fatal-outcome is recorded in HUB-FINDINGS-HANDOFF for a future v0.1.2 tag"
---

<objective>
Land the two in-repo, no-code, no-live-effect changes: (1) the `deploy/weatherbot.service` restart-policy change (D-05) that converts a fatal config-exit crash-loop into a loud parked `failed` unit while keeping the transient slow-key path alive; (2) append the deferred hub `ReadyGate` first-class fatal-outcome enhancement (D-10 hub half) to `HUB-FINDINGS-HANDOFF.md`. NEITHER edits hub source, and the unit's LIVE effect is a deferred Gate-2 obligation (redeploy + daemon-reload on yahir-mint) — this plan does NOT restart the live daemon.

Purpose: Bound restart churn / alert-spam at the OS layer (D-04) and record the clean long-term hub design without shipping it autonomously.
Output: Amended systemd unit (static test green) + a new handoff entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@deploy/weatherbot.service
@.planning/HUB-FINDINGS-HANDOFF.md
</context>

<artifacts_this_phase_produces>
NEW / MODIFIED artifacts (exclude from drift verification):
- `deploy/weatherbot.service`: `[Unit]` gains `StartLimitIntervalSec=300` + `StartLimitBurst=5`; `[Service]` `Restart=always`→`Restart=on-failure`; `RestartSec=5` and `TimeoutStartSec=infinity` KEPT.
- `.planning/HUB-FINDINGS-HANDOFF.md`: a new appended finding entry for the ReadyGate first-class fatal outcome (documentation only).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Amend deploy/weatherbot.service restart policy (D-05, HARD-STARTUP-02/03)</name>
  <files>deploy/weatherbot.service</files>
  <read_first>
    - deploy/weatherbot.service — full file: `[Unit]` (:13-18, where Wants=/After= live — the correct home for StartLimit*), `[Service]` (:20), `TimeoutStartSec=infinity` (:24-27, KEEP — the comment explains it protects the never-exiting transient re-probe path), `Restart=always` (:45) + `RestartSec=5` (:46).
    - .planning/phases/29-startup-validation-honest-alerting/29-RESEARCH.md — Discretion answer 2 (values), Pitfall 1 (requested-stop exemption), Pitfall 2 (Timeout vs restart orthogonal), Pitfall 3 (StartLimit* MUST be in [Unit]).
    - tests/test_service_unit.py — the static assertions this edit must satisfy (29-02).
  </read_first>
  <action>
    In the `[Unit]` section (alongside `Wants=`/`After=`), add two directives: `StartLimitIntervalSec=300` and `StartLimitBurst=5` (Pitfall 3: they are silently ignored in `[Service]` — they MUST be in `[Unit]`). In the `[Service]` section, change `Restart=always` to `Restart=on-failure` (a clean `systemctl stop`/SIGTERM returning 0 is exempt from restart per the requested-stop exemption; only a non-zero fatal exit restarts, tripping the start-limit and parking the unit `failed`). KEEP `RestartSec=5`. KEEP `TimeoutStartSec=infinity` (line 27) UNCHANGED — it governs the never-exiting transient slow-key path and is orthogonal to restart policy (Pitfall 2). Add a brief inline comment near the start-limit directives explaining "5 fatal-exit restarts within 300s parks the unit `failed` instead of an infinite 5s crash-loop + infinite Discord alerts (D-04/D-05)". Do NOT touch ExecStart, EnvironmentFile, User, RuntimeDirectory, or the placeholders.
  </action>
  <verify>
    <automated>uv run pytest tests/test_service_unit.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_service_unit.py -x -q` passes (all three static-directive tests green).
    - `grep -c 'Restart=on-failure' deploy/weatherbot.service` returns 1 and `grep -c 'Restart=always' deploy/weatherbot.service` returns 0.
    - `StartLimitIntervalSec=300` and `StartLimitBurst=5` are within the `[Unit]` section (before the `[Service]` header).
    - `TimeoutStartSec=infinity` is still present and unchanged.
    - The live daemon on yahir-mint is NOT restarted or redeployed (deferred Gate-2, D-06).
  </acceptance_criteria>
  <done>The systemd unit uses Restart=on-failure + [Unit]-placed start-limit while keeping TimeoutStartSec=infinity; the static directive test passes; no live redeploy attempted.</done>
</task>

<task type="auto">
  <name>Task 2: Append the deferred hub ReadyGate fatal-outcome enhancement to HUB-FINDINGS-HANDOFF.md (D-10 hub half)</name>
  <files>.planning/HUB-FINDINGS-HANDOFF.md</files>
  <read_first>
    - .planning/HUB-FINDINGS-HANDOFF.md — header + existing finding entry format (e.g. `### H01 — <path> · <severity> · <status> · <category>` followed by a one-line summary, `*Scenario:*`, and optional `*Evidence:*`); append in the same shape.
    - .planning/phases/29-startup-validation-honest-alerting/29-CONTEXT.md D-09/D-10 and the `<deferred>` block — the exact enhancement wording (first-class "fatal outcome" in `ReadyGate.run` so the app no longer overloads `stop`).
    - ../Reusable/YahirReusableBot/ECOSYSTEM.md — jurisdiction: hub fixes are human-gated; this is documentation only. Do NOT edit any file under ../Reusable/YahirReusableBot/.
  </read_first>
  <action>
    Append ONE new finding entry to `.planning/HUB-FINDINGS-HANDOFF.md` (place it under the appropriate severity grouping — treat it as an enhancement/medium; match the existing `### H<NN> — yahir_reusable_bot/lifecycle/ready_gate.py:<line> · <sev> · enhancement · lifecycle` heading style and body shape). Content: `ReadyGate.run` has no first-class fatal outcome — every non-ok probe re-probes forever and it only branches log level on severity. WeatherBot's Phase 29 works around this app-side by overloading the `stop` Event plus a separate `fatal` marker read after `run()` returns. The clean long-term design is a first-class fatal outcome in `ReadyGate.run` (break the loop + signal fatal distinctly from a clean stop) so consumers don't overload `stop`. Note: routes upstream, human-gated tag cut (v0.1.2); after the hub ships it, WeatherBot repins and de-hacks the app-side `stop`-overload (reference the app sites: `wiring.py:_on_fail` fatal branch + `daemon.py` gate-return exit-code check). Do NOT implement anything in the hub; do NOT touch hub source. Update the handoff's total-count/severity-tally line at the top if it maintains one.
  </action>
  <verify>
    <automated>grep -c "ready_gate.py" .planning/HUB-FINDINGS-HANDOFF.md</automated>
  </verify>
  <acceptance_criteria>
    - A new entry referencing `yahir_reusable_bot/lifecycle/ready_gate.py` and the fatal-outcome enhancement exists in HUB-FINDINGS-HANDOFF.md.
    - The entry states it is deferred / human-gated and names the app-side de-hack path for after repin.
    - No file under `../Reusable/YahirReusableBot/` is modified (verify `git -C ../Reusable/YahirReusableBot status --porcelain` is unchanged by this plan, or simply do not touch it).
    - The handoff header count/tally is consistent with the appended entry.
  </acceptance_criteria>
  <done>The ReadyGate first-class fatal-outcome enhancement is recorded in HUB-FINDINGS-HANDOFF.md as a deferred human-gated hub item; no hub source touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| systemd unit directives → OS supervision | The restart policy + start-limit decide crash-loop behavior; mis-sectioned or wrong directives defeat the bound. |
| cross-repo (WeatherBot → hub) | Hub changes are human-gated (ECOSYSTEM); this phase must record, not ship, the hub enhancement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-08 | Denial of Service (crash-loop / alert-spam) | `weatherbot.service` restart policy | medium | mitigate | Restart=on-failure + StartLimit* in [Unit] bounds a fatal config-exit to ≤5 restarts / 300s then parks `failed`; TimeoutStartSec=infinity kept so the transient path is not turned into a disguised crash-loop. Static test enforces placement. |
| T-29-09 | Tampering (cross-repo jurisdiction) | hub source | high | mitigate | Task 2 is documentation-only; acceptance verifies no `../Reusable/YahirReusableBot/` file changes — honors ECOSYSTEM human-gated rule (D-09). |
</threat_model>

<verification>
- `uv run pytest tests/test_service_unit.py -x -q` green.
- HUB-FINDINGS-HANDOFF.md carries the deferred ReadyGate enhancement; hub source untouched.
</verification>

<success_criteria>
- Unit: Restart=on-failure + [Unit] start-limit + TimeoutStartSec=infinity kept; static test green.
- Handoff: deferred hub enhancement appended; no hub edit; no live redeploy.
</success_criteria>

<output>
Create `.planning/phases/29-startup-validation-honest-alerting/29-06-SUMMARY.md` when done.
</output>
