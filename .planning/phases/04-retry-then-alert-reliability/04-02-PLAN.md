---
phase: 04-retry-then-alert-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - weatherbot/weather/store.py
  - weatherbot/config/models.py
  - config.toml
  - config.example.toml
  - tests/test_store.py
  - tests/test_config.py
autonomous: true
requirements: [RELY-03, RELY-04, RELY-05]
user_setup: []

must_haves:
  truths:
    - "A briefing_missed alert is durably recorded in the DB, at most once per (location, slot, local_date)"
    - "An alert can be stamped resolved when the slot later succeeds"
    - "Liveness is recorded as a single heartbeat row carrying last_tick and last_success, upserted in place"
    - "Malformed retry config fails loud at load (pydantic) in the Phase-2 fail-loud tradition"
  artifacts:
    - path: "weatherbot/weather/store.py"
      provides: "alerts + heartbeat tables in _SCHEMA; record_alert / resolve_alert / stamp_tick / stamp_success helpers"
      contains: "def record_alert"
    - path: "weatherbot/config/models.py"
      provides: "Reliability pydantic model with validated retry-timing fields, attached to Config"
      contains: "class Reliability"
    - path: "config.example.toml"
      provides: "Documented optional [reliability] section with D-07 defaults"
      contains: "[reliability]"
  key_links:
    - from: "weatherbot/weather/store.py record_alert"
      to: "alerts table"
      via: "INSERT OR IGNORE on UNIQUE(location_name, slot_time, local_date)"
      pattern: "INSERT OR IGNORE INTO alerts"
    - from: "weatherbot/config/models.py Config"
      to: "Reliability"
      via: "Field(default_factory=Reliability)"
      pattern: "default_factory=Reliability"
---

<objective>
Add the durable state primitives Phase 4 needs: the `alerts` and `heartbeat` tables (+ their helpers) in `store.py`, and the `Reliability` retry-config model (+ documented `[reliability]` TOML section) in the config layer — all additive, all modeled verbatim on existing Phase 1-3 idioms.

This plan is pure storage + config foundation (no orchestration wiring). It runs in parallel with Plan 01 (different files entirely). Plans 03/04 consume these helpers and this config model.

Purpose: Implement the persistence half of RELY-03 (durable missed-briefing record), RELY-04 (dedup anti-loop primitive), RELY-05 (heartbeat liveness state), and D-09 (load-validated, `--check`-surfaced retry config). Decisions implemented: D-01 (alert delivered via log + durable DB record — this plan builds the durable `alerts` table half), D-03 (alerts shape), D-05 (heartbeat shape), D-09 (Reliability model), D-11 (INSERT-OR-IGNORE dedup), D-13 (resolve flag).
Output: modified `store.py`, `config/models.py`, `config.toml`, `config.example.toml`; extended `tests/test_store.py`, `tests/test_config.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retry-then-alert-reliability/04-CONTEXT.md
@.planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md
@.planning/phases/04-retry-then-alert-reliability/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (04-02) creates the following NEW symbols. The plan-review source-grounding pass MUST treat these as newly-created, not drift:

- `store.py` NEW tables appended to `_SCHEMA`:
  - `alerts` (columns: `id`, `location_name`, `slot_time`, `local_date`, `reason`, `severity`, `created_at_utc`, `resolved_at_utc`, `UNIQUE(location_name, slot_time, local_date)`) — per D-03
  - `heartbeat` (single-row: `id INTEGER PRIMARY KEY CHECK (id = 1)`, `last_tick_utc`, `last_success_utc`, seeded via `INSERT OR IGNORE ... VALUES (1, NULL, NULL)`) — per D-05
- `store.py` NEW helpers:
  - `def record_alert(db_path, location_name, slot_time, local_date, reason, severity="critical") -> bool` (returns rowcount==1 = "this caller was first")
  - `def resolve_alert(db_path, location_name, slot_time, local_date) -> None`
  - `def stamp_tick(db_path) -> None`
  - `def stamp_success(db_path) -> None`
- `config/models.py` NEW model:
  - `class Reliability(BaseModel)` with fields `attempts_per_burst: int = 8`, `burst_spread_seconds: int = 600`, `mid_pause_seconds: int = 2700` (+ field validators) and attached to `Config` as `reliability: Reliability = Field(default_factory=Reliability)`
- `config.example.toml` / `config.toml` NEW optional `[reliability]` section
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add alerts + heartbeat tables and helpers to store.py</name>
  <files>weatherbot/weather/store.py, tests/test_store.py</files>
  <read_first>
    - weatherbot/weather/store.py (FULL — `_SCHEMA` lines 36-116 incl. the `sent_log` table to copy; `claim_slot` lines 238-275 = the INSERT-OR-IGNORE + rowcount==1 dedup primitive to copy for `record_alert`; `release_claim` lines 278-301 = the parameterized UPDATE/DELETE shape to copy for `resolve_alert`; the schema-on-connect `conn.executescript(_SCHEMA)` discipline and the T-03-01/T-04-01 secret-hygiene docstrings)
    - tests/test_store.py (lines 1-90 — `_connect` sqlite3.Row helper + the `tmp_db` arrange-act-assert-over-rows pattern; the secret-leak grep assertion at lines ~135-147)
    - .planning/phases/04-retry-then-alert-reliability/04-PATTERNS.md (### weatherbot/weather/store.py — exact schema additions + dedup/resolve/stamp helper shapes)
    - .planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md (Pattern 2 alerts table, Pattern 5 heartbeat upsert)
  </read_first>
  <behavior>
    - Test: `record_alert(tmp_db, "NYC", "09:00", "2026-06-10", "transient_exhausted")` writes exactly one `alerts` row with that reason and a non-null `created_at_utc`, `resolved_at_utc` NULL
    - Test: calling `record_alert` twice for the SAME (location, slot, local_date) leaves exactly ONE row (D-11 INSERT-OR-IGNORE), and the first call returns True / the second returns False
    - Test: `resolve_alert(tmp_db, ...)` sets `resolved_at_utc` non-null only for the matching unresolved row; it is a no-op when no row exists
    - Test: `stamp_tick` then `stamp_success` leave exactly one `heartbeat` row (id=1) with both `last_tick_utc` and `last_success_utc` populated; repeated stamps update in place (still one row)
    - Test: no stored alerts/heartbeat row or value contains `"appid"` or `"api.openweathermap.org"` (T-04-01)
  </behavior>
  <action>
    Append two tables to the single `_SCHEMA` string (so every helper's `conn.executescript(_SCHEMA)` creates them idempotently — additive `CREATE TABLE IF NOT EXISTS` only, no destructive migration). `alerts` mirrors `sent_log`'s key columns verbatim — `location_name TEXT NOT NULL`, `slot_time TEXT NOT NULL` ("HH:MM"), `local_date TEXT NOT NULL` (YYYY-MM-DD in the location's tz) — plus `reason TEXT NOT NULL`, `severity TEXT NOT NULL`, `created_at_utc INTEGER NOT NULL`, `resolved_at_utc INTEGER` (NULL = unresolved, D-13), and `UNIQUE(location_name, slot_time, local_date)` (D-03/D-11). `heartbeat` is the single-row table from Pattern 5: `id INTEGER PRIMARY KEY CHECK (id = 1)`, `last_tick_utc INTEGER`, `last_success_utc INTEGER`, followed in the schema by `INSERT OR IGNORE INTO heartbeat (id, last_tick_utc, last_success_utc) VALUES (1, NULL, NULL);` so row 1 always exists for the monitor to read (D-05).
    Add `record_alert(...)` as a structural copy of `claim_slot`: `conn.executescript(_SCHEMA)`, then a parameterized `INSERT OR IGNORE INTO alerts (location_name, slot_time, local_date, reason, severity, created_at_utc) VALUES (?, ?, ?, ?, ?, ?)`, `created_at_utc = int(datetime.now(timezone.utc).timestamp())`, return `cur.rowcount == 1` (the "this caller was first" signal Plan 03 uses to decide whether to ALSO emit the CRITICAL log). Default `severity="critical"`.
    Add `resolve_alert(...)` as a copy of `release_claim`'s parameterized shape: `UPDATE alerts SET resolved_at_utc=? WHERE location_name=? AND slot_time=? AND local_date=? AND resolved_at_utc IS NULL` (D-13).
    Add `stamp_tick(db_path)` = `UPDATE heartbeat SET last_tick_utc=? WHERE id=1` and `stamp_success(db_path)` = `UPDATE heartbeat SET last_success_utc=? WHERE id=1` (D-05). Every value bound as `?` — never f-string'd into SQL (T-03-01). No row ever carries a key or URL (T-04-01) — only location/slot/date/reason/severity/timestamps.
    Extend `tests/test_store.py` with the behaviors above, reusing `_connect` and `tmp_db`, including the secret-leak grep assertion.
  </action>
  <verify>
    <automated>uv run pytest tests/test_store.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/weather/store.py` contains `def record_alert(`, `def resolve_alert(`, `def stamp_tick(`, `def stamp_success(`
    - `grep -c "INSERT OR IGNORE INTO alerts" weatherbot/weather/store.py` is >= 1 (D-11 dedup primitive)
    - The `_SCHEMA` contains `CREATE TABLE IF NOT EXISTS alerts` and `CREATE TABLE IF NOT EXISTS heartbeat` with `UNIQUE(location_name, slot_time, local_date)` on alerts and `CHECK (id = 1)` on heartbeat
    - Behavior: a second `record_alert` for the same slot/day leaves exactly one row and returns False (D-11 anti-loop)
    - Secret hygiene: `grep -nE "appid|api.openweathermap.org|webhook|_key|api_key" weatherbot/weather/store.py` shows no secret bound into the new alert/heartbeat inserts (T-04-01); the secret-leak test asserts absence in stored rows
    - SQLi: every new insert/update uses `?` placeholders (no f-string SQL) — assert by `grep -n "f\"" `/`f'` near the new SQL returns nothing
    - `uv run pytest tests/test_store.py -q -x` exits 0
  </acceptance_criteria>
  <done>The `alerts` + `heartbeat` tables and their dedup/resolve/upsert helpers exist, are additive, parameterized, secret-clean, and tested; the dedup primitive guarantees at most one alert per slot/day.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Reliability retry-config model + documented [reliability] TOML section</name>
  <files>weatherbot/config/models.py, config.toml, config.example.toml, tests/test_config.py</files>
  <read_first>
    - weatherbot/config/models.py (FULL — `Schedule` model lines 25-73 with `model_config = ConfigDict(extra="forbid")` + `@field_validator` fail-loud validators to copy; `Config` lines 126-138 where `webhook: WebhookIdentity = Field(default_factory=WebhookIdentity)` shows the optional-default attach pattern to mirror)
    - weatherbot/config/loader.py (lines 18-27 — `load_config` calls `Config.model_validate`, so a bad `[reliability]` value raises `ValidationError` at load with no extra code)
    - config.example.toml (the commented `[[locations]]` / `[[locations.schedule]]` blocks — the comment-above-each-key documentation style to mirror for `[reliability]`)
    - tests/test_config.py (the existing field-validation test shape to copy for `test_retry_config_validation`)
    - .planning/phases/04-retry-then-alert-reliability/04-PATTERNS.md (### weatherbot/config/models.py + ### config.toml — exact field names + the under-90-min belt-and-suspenders validator)
    - .planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md (D-09, Pitfall 5 budget-vs-grace, Security Domain V5 input validation)
  </read_first>
  <behavior>
    - Test: a `Config` with no `[reliability]` section loads and exposes `config.reliability` with D-07 defaults (8 / 600 / 2700)
    - Test: a `[reliability]` with `attempts_per_burst = 0` (or negative spread/pause) raises `ValidationError` at load (fail-loud, D-09)
    - Test: a `[reliability]` whose `2*burst_spread_seconds + mid_pause_seconds` exceeds the 90-min (5400s) grace window raises `ValidationError` (Pitfall 5 belt-and-suspenders)
    - Test: an unknown key inside `[reliability]` raises (extra="forbid")
  </behavior>
  <action>
    Add `class Reliability(BaseModel)` to `config/models.py` with `model_config = ConfigDict(extra="forbid")` and fields `attempts_per_burst: int = 8`, `burst_spread_seconds: int = 600`, `mid_pause_seconds: int = 2700` (defaults match D-07). Add `@field_validator` per field enforcing positivity (`> 0`), copying the `Schedule._hhmm` fail-loud style (raise `ValueError(f"...")`). Add a `@model_validator(mode="after")` (belt-and-suspenders, Pitfall 5) asserting `2*burst_spread_seconds + mid_pause_seconds < 5400` (under Phase 3's 90-min catch-up grace, D-07) and raising a clear `ValueError` otherwise. Attach to `Config` as `reliability: Reliability = Field(default_factory=Reliability)` (mirrors the `webhook` default) so existing configs without the section still load with D-07 defaults.
    Add a documented optional `[reliability]` table to BOTH `config.example.toml` and `config.toml` with a comment above each key explaining attempts/spread/pause and the "~65-min total, must stay under the 90-min catch-up grace" constraint (mirror the existing `[[locations.schedule]]` comment style). Keep it optional/commentable so `--check` passes on an un-edited file.
    Extend `tests/test_config.py` with `test_retry_config_validation` covering the four behaviors above (copy the existing validator-test shape; build minimal in-memory dicts via `Config.model_validate`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/config/models.py` contains `class Reliability` with fields `attempts_per_burst`, `burst_spread_seconds`, `mid_pause_seconds` and `model_config = ConfigDict(extra="forbid")`
    - `grep -n "default_factory=Reliability" weatherbot/config/models.py` matches (Reliability attached to Config with safe defaults)
    - `config.example.toml` contains a `[reliability]` section documented with the budget/grace comment
    - Behavior: `attempts_per_burst = 0` and an over-grace total each raise `ValidationError` at load (D-09 / Pitfall 5)
    - Behavior: a config with no `[reliability]` loads with defaults 8/600/2700
    - `tests/test_config.py` defines `test_retry_config_validation` and it passes
    - `uv run pytest tests/test_config.py -q -x` exits 0
  </acceptance_criteria>
  <done>The retry budget is config-driven, validated fail-loud at load (incl. the under-90-min-grace guard), documented in the example TOML with safe defaults, and tested; existing configs load unchanged with D-07 defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → pydantic | User-edited (untrusted-shape) retry timings cross into the runtime budget |
| caller args → SQL | location/slot/reason values cross into new table writes |
| stored rows → future monitor | DB rows are read by a future log-monitoring bot — must be secret-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | new alerts/heartbeat rows | mitigate | Rows carry only location/slot/date/reason/severity/timestamps; secret-leak grep test asserts no `appid`/host in stored values (Task 1) |
| T-03-01 | Tampering | new INSERT/UPDATE on alerts/heartbeat | mitigate | Parameterized `?` only, mirroring store.py; no f-string SQL (Task 1 acceptance) |
| T-04-CFG | Tampering / Denial of Service | malformed [reliability] mis-tuning the budget | mitigate | pydantic validators (positivity + under-90-min-grace) fail loud at load (Task 2); V5 input validation |
| T-04-DB | Tampering | schema change to existing DB | mitigate | Additive `CREATE TABLE IF NOT EXISTS` only; existing `sent_log`/`weather_onecall` rows untouched (no destructive migration) |
</threat_model>

<verification>
- `uv run pytest tests/test_store.py tests/test_config.py -q -x` passes
- `uv run pytest -q` passes (no regression)
- `grep -nE "appid|api.openweathermap.org|webhook" weatherbot/weather/store.py` shows no secret bound into new inserts (T-04-01)
- The new `_SCHEMA` tables are all `CREATE TABLE IF NOT EXISTS` (additive; T-04-DB)
</verification>

<success_criteria>
- RELY-03 (storage half): a missed briefing is durably recorded via `record_alert`
- RELY-04 (primitive): `record_alert` is INSERT-OR-IGNORE on the slot key — at most one alert per (location, slot, local_date)
- RELY-05 (storage half): `stamp_tick`/`stamp_success` maintain a single authoritative heartbeat row
- D-09: the retry budget is config-driven and fails loud at load on malformed/over-grace values
- D-13: an alert can be stamped resolved on later success
</success_criteria>

<output>
Create `.planning/phases/04-retry-then-alert-reliability/04-02-SUMMARY.md` when done. Record: the final `alerts`/`heartbeat` column names and the `Reliability` field names + defaults (Plans 03/04 must match these exactly).
</output>
