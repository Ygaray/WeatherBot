---
phase: 04-retry-then-alert-reliability
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - weatherbot/cli.py
  - tests/test_cli.py
autonomous: true
requirements: [RELY-01]
user_setup: []

must_haves:
  truths:
    - "Manual --send-now does a tight bounded retry on a transient blip and reports failure immediately to the terminal"
    - "A failed manual --send-now writes NO alerts row and NO heartbeat row (those are daemon-liveness concerns)"
    - "weatherbot --check surfaces the resolved retry budget (attempts/spread/pause/approx total) and still validates config without sending"
  artifacts:
    - path: "weatherbot/cli.py"
      provides: "Manual tight retry around the --send-now send (no liveness rows); --check echoes the retry budget"
      contains: "build_retrying"
  key_links:
    - from: "weatherbot/cli.py main (--send-now branch)"
      to: "weatherbot.reliability"
      via: "a tight Retrying around send_now, record_liveness=False"
      pattern: "stop_after_attempt"
    - from: "weatherbot/cli.py do_check"
      to: "config.reliability"
      via: "echoes attempts/spread/pause budget"
      pattern: "config.reliability"
---

<objective>
Implement the manual (attended) half of the daemon-vs-manual split (D-10): `--send-now` does a short bounded retry on a transient blip, reports failure immediately to the terminal, and writes NO `alerts`/`heartbeat` rows (those exist only for the unattended daemon). Surface the resolved retry budget in `--check` (D-09) so a mis-tuned config is visible without sending.

This is the thin manual-path slice — different file (`cli.py`) from Plan 03's daemon path, so it runs in parallel. Depends on Plan 01 (retry helpers) and Plan 02 (the `Reliability` config model). It deliberately does NOT touch `send_now`'s body: `send_now` stays the single-attempt shared composition root (Open Question 1), and the tight retry wraps it in `main`'s `--send-now` branch.

Purpose: Implement RELY-01 for the manual path (bounded retry on transient failure) within the D-10 constraints (terminal-only, no liveness rows) and D-09 (`--check` surfaces the budget). Decisions: D-10 (manual tight/terminal-only path, no alert/heartbeat rows), D-09 (`--check` surfaces retry config).
Output: modified `weatherbot/cli.py`; extended `tests/test_cli.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retry-then-alert-reliability/04-CONTEXT.md
@.planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md
@.planning/phases/04-retry-then-alert-reliability/04-PATTERNS.md
@.planning/phases/04-retry-then-alert-reliability/04-01-SUMMARY.md
@.planning/phases/04-retry-then-alert-reliability/04-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (04-04) MODIFIES `weatherbot/cli.py` and CONSUMES Plan 01/02 symbols (not drift — produced earlier this phase):
- from `weatherbot.reliability`: `build_retrying` (or a small tight `Retrying` built from tenacity's `stop_after_attempt`/`wait_exponential`), `is_transient`
- from `weatherbot.config.models`: `Config.reliability` (the budget fields)

NEW behavior this plan introduces in cli.py:
- the `--send-now` branch in `main` wraps `send_now(...)` in a SHORT bounded retry (terminal-only, no liveness writes)
- `do_check` gains a step that echoes the resolved retry budget (attempts / spread / pause / approx total minutes)
No new alerts/heartbeat writes appear on the manual path (the explicit D-10 negative guarantee).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manual --send-now tight retry (terminal-only, no liveness rows) + --check budget surface</name>
  <files>weatherbot/cli.py, tests/test_cli.py</files>
  <read_first>
    - weatherbot/cli.py (FULL — `send_now` lines 81-165 (DO NOT add retry inside it — it stays single-attempt); `do_check` lines 216-279 (the ordered-validation + clean-failure pattern to extend; the 401/403 distinguishing block); `main`'s `--send-now` branch lines 387-409 (the `send_now(...)` call + the existing terminal report `_log.error("briefing delivery failed", detail=result.detail)` to wrap); the `--check` branch lines 359-365)
    - weatherbot/reliability/retry.py + __init__.py (Plan 01 — `is_transient`; tenacity `Retrying`, `stop_after_attempt`, `wait_exponential` for the tight profile; OR `build_retrying` if a thin wrapper is preferred — but the manual path wants a SHORT bound, e.g. 3 attempts, not the two-burst)
    - weatherbot/config/models.py (Plan 02 — `config.reliability.attempts_per_burst` / `burst_spread_seconds` / `mid_pause_seconds` for the --check echo)
    - tests/test_cli.py (existing test shapes — how send_now/channel are injected; how the manual path is exercised; the conventions to copy for test_send_now_no_liveness_rows)
    - .planning/phases/04-retry-then-alert-reliability/04-PATTERNS.md (### weatherbot/cli.py — manual tight retry wraps the send call, record_liveness=False; do_check budget echo)
    - .planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md (Pattern 8 daemon-vs-manual split; Pitfall 4 no liveness row on manual path; D-10)
  </read_first>
  <behavior>
    - Test (`test_send_now_no_liveness_rows`): a manual --send-now path whose send fails (non-ok) after the tight retry writes NO `alerts` row and NO `heartbeat` row in the tmp_db, and returns exit 1 reporting `detail` to the terminal (the existing `_log.error("briefing delivery failed", ...)`)
    - Test: a manual --send-now whose send fails transiently once then succeeds returns exit 0 (the tight retry recovered the blip), still with NO liveness rows
    - Test: the tight retry uses a SHORT bound (e.g. <= 3 attempts) — NOT the ~65-min two-burst schedule (assert attempt count, mock the sleep)
    - Test (--check budget): `do_check` with a valid config returns 0 and its output/log includes the resolved retry budget (attempts/spread/pause); a probe 401/403 still reports the subscription-not-active message (unchanged)
  </behavior>
  <action>
    In `main`'s `--send-now` branch only (NOT inside `send_now`), wrap the `send_now(...)` call in a SHORT bounded retry so a transient blip recovers without the daemon's patient schedule: build a tight `Retrying(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, max=10), retry=(retry_if_result(lambda r: not r.ok) | retry_if_exception(is_transient)), reraise=True)` (import from tenacity / via Plan 01's module). Do NOT pass a daemon stop_event and do NOT thread the two-burst builder — manual is attended and terminal-bound (D-10). On final non-ok / raised result keep the EXISTING terminal report path (`_log.error("briefing delivery failed", detail=result.detail)`, exit 1) — manual failures report to the terminal, not to the alert path. CRITICALLY: the manual path must NOT call `record_alert`, `stamp_tick`, or `stamp_success` — it writes NO `alerts`/`heartbeat` rows (D-10 / Pitfall 4). (`send_now` already only `persist`s weather data — confirm no liveness write sneaks in.)
    In `do_check`, add a validation/echo step that prints (or `_log.info(...)`) the resolved retry budget from `config.reliability`: `attempts_per_burst`, `burst_spread_seconds`, `mid_pause_seconds`, and the computed approx total minutes (`(2*spread + mid_pause)/60`), so a mis-tuned budget is visible at check time (D-09). Keep all four existing do_check steps and the 401/403 distinguishing message unchanged; the new echo is additive and must not send anything. The budget is already load-validated by Plan 02's `Reliability` model — `--check` only surfaces it.
    Extend `tests/test_cli.py` with `test_send_now_no_liveness_rows` and the tight-retry-recovers + budget-echo tests, injecting a fake client/channel and a `tmp_db`, mocking sleep so the tight retry runs fast.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/cli.py` `main`'s `--send-now` branch wraps `send_now(...)` in a tight `Retrying` with `stop_after_attempt(3)` (or similar SHORT bound) — NOT the two-burst schedule
    - `grep -nE "record_alert|stamp_tick|stamp_success" weatherbot/cli.py` returns NOTHING (D-10 — manual path writes no liveness/alert rows)
    - `grep -n "config.reliability" weatherbot/cli.py` matches inside `do_check` (D-09 budget surface)
    - Behavior: a failed manual --send-now writes zero `alerts` and zero `heartbeat` rows in tmp_db and returns exit 1 with the terminal detail report (test_send_now_no_liveness_rows)
    - Behavior: a transient-then-success manual send returns exit 0 via the tight retry (no liveness rows)
    - Behavior: `do_check` returns 0 and surfaces the retry budget; the 401/403 subscription message is unchanged
    - `send_now`'s body is unchanged in retry behavior (`grep -n "build_retrying\|Retrying" ` does not appear inside the `send_now` function) — retry locus stays in `main`/daemon, send_now is single-attempt
    - `uv run pytest tests/test_cli.py -q -x` exits 0 and `uv run pytest -q` exits 0 (no regression)
  </acceptance_criteria>
  <done>Manual --send-now recovers a transient blip with a short bounded retry, reports any final failure to the terminal, and writes no alerts/heartbeat rows; --check surfaces the resolved retry budget; send_now stays single-attempt; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transient I/O failure → manual retry | Untrusted send/fetch outcome drives the tight retry (terminal-bound) |
| manual path → DB | The manual path must NOT pollute the daemon-only alerts/heartbeat state |
| do_check output → terminal | Budget echo must not leak a secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | --check budget echo / manual failure report | mitigate | Echo only numeric budget fields; the existing manual report logs `detail` only (credential-free per DeliveryResult contract); no key/URL printed |
| T-04-NOISE | Tampering | manual path writing daemon liveness rows | mitigate | Grep gate asserts no `record_alert`/`stamp_*` in cli.py; test asserts zero alerts/heartbeat rows after a manual failure (D-10 / Pitfall 4) |
| T-04-DoS | Denial of Service | unbounded manual retry | mitigate | Tight `stop_after_attempt(3)` bound — manual path never runs the long patient schedule |
</threat_model>

<verification>
- `uv run pytest tests/test_cli.py -q -x` passes
- `uv run pytest -q` passes (no regression)
- `grep -nE "record_alert|stamp_tick|stamp_success" weatherbot/cli.py` returns nothing (D-10 negative guarantee)
- `grep -n "config.reliability" weatherbot/cli.py` matches in do_check (D-09)
</verification>

<success_criteria>
- RELY-01 (manual half): --send-now recovers a transient blip via a short bounded retry
- D-10: manual path is tight + terminal-only and writes NO alerts/heartbeat rows
- D-09: --check surfaces the resolved retry budget so a mis-tune is visible without sending
- send_now remains the single-attempt shared composition root (retry locus stays in main/daemon)
</success_criteria>

<output>
Create `.planning/phases/04-retry-then-alert-reliability/04-04-SUMMARY.md` when done. Record: the manual tight-retry bound chosen (attempts/wait); confirmation that no liveness write exists on the manual path; the exact --check budget output format.
</output>
