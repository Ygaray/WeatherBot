---
phase: 04-retry-then-alert-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - weatherbot/reliability/__init__.py
  - weatherbot/reliability/retry.py
  - tests/test_reliability.py
autonomous: false
requirements: [RELY-01, RELY-02]
user_setup: []

must_haves:
  truths:
    - "A transient fetch/send failure (5xx / timeout / connect / Discord ok=False) is retried; a 401/403 auth failure is never retried"
    - "The retry schedule is the two-burst shape: 8 attempts across ~10 min, ~45-min pause, 8 attempts across ~10 min (16 total)"
    - "A Retry-After header (seconds OR HTTP-date) is parsed and capped so it cannot blow the ~65-min budget"
    - "The whole retry schedule (including the 45-min pause) abandons immediately when the daemon stop_event is set"
  artifacts:
    - path: "weatherbot/reliability/retry.py"
      provides: "Two-burst Retrying builder, transient/permanent classifier, capped Retry-After parser, reason taxonomy constants"
      contains: "def is_transient"
    - path: "weatherbot/reliability/__init__.py"
      provides: "Package barrel re-exporting the public retry surface"
    - path: "tests/test_reliability.py"
      provides: "Wave-0 test scaffold + retry-engine unit tests (RELY-01/02, D-07)"
      contains: "def test_two_burst_wait_shape"
  key_links:
    - from: "weatherbot/reliability/retry.py"
      to: "tenacity.Retrying"
      via: "build_retrying(stop_event) returns a configured Retrying"
      pattern: "Retrying\\("
    - from: "weatherbot/reliability/retry.py"
      to: "stop_event.wait"
      via: "sleep= callable for interruptible pause"
      pattern: "sleep="
---

<objective>
Establish the Phase 4 retry foundation: add the `tenacity` dependency (gated behind a human-verify legitimacy checkpoint), build the `weatherbot/reliability/` package with the two-burst retry engine + transient/permanent classifier + capped `Retry-After` parser + reason-taxonomy constants, and create the `tests/test_reliability.py` Wave-0 scaffold that every later plan extends.

This is the shared contract for the whole phase — Plans 03 (daemon patient path) and 04 (manual tight path) both import the builder and constants from here. Interface-first: this plan defines the retry surface before any consumer wires it.

Purpose: Implement RELY-01 (bounded retry on transient failure) and RELY-02 (never retry 401/403; honor capped Retry-After) as a single tested orchestration helper, so the daemon/manual wiring plans consume a contract rather than re-deriving classification logic (per D-07/D-08/D-09).
Output: `pyproject.toml` (tenacity added), `weatherbot/reliability/__init__.py`, `weatherbot/reliability/retry.py`, `tests/test_reliability.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-retry-then-alert-reliability/04-CONTEXT.md
@.planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md
@.planning/phases/04-retry-then-alert-reliability/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (04-01) creates the following NEW symbols. The plan-review source-grounding pass MUST treat these as newly-created, not drift:

- `weatherbot/reliability/` — NEW package
- `weatherbot/reliability/__init__.py` — barrel re-exporting the public retry surface in `__all__`
- `weatherbot/reliability/retry.py` — NEW module containing:
  - Module constants: `BURST_SIZE` (=8), `BURST_SPREAD_S` (=600), `MID_PAUSE_S` (=2700), `RETRY_AFTER_CAP_S` (Claude's discretion, e.g. 120), `PERMANENT` (={400,401,403,404}), `TRANSIENT` (={429,500,502,503,504})
  - Reason-taxonomy string constants: `REASON_TRANSIENT_EXHAUSTED = "transient_exhausted"`, `REASON_AUTH_FAILED = "auth_failed"`, `REASON_INTERNAL_ERROR = "internal_error"` (consumed by Plans 03/04)
  - `def is_transient(exc) -> bool`
  - `def is_auth_failure(exc) -> bool` — True for `HTTPStatusError` 401/403 (used to choose `reason=auth_failed`)
  - `def parse_retry_after(resp) -> float | None`
  - `def two_burst_wait(retry_state) -> float`
  - `def build_retrying(stop_event, *, attempts_per_burst=BURST_SIZE, burst_spread_s=BURST_SPREAD_S, mid_pause_s=MID_PAUSE_S) -> Retrying`
- `tests/test_reliability.py` — NEW test file (scaffold + this plan's engine tests; later plans add behavior tests)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify tenacity legitimacy, then add it as a dependency</name>
  <what-built>Nothing yet — this checkpoint gates the `tenacity` install. RESEARCH (04-RESEARCH.md "Package Legitimacy Audit") tagged tenacity `[ASSUMED]` because slopcheck was unavailable in the research environment; per protocol the install must be human-verified before it runs.</what-built>
  <read_first>
    - .planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md (## Package Legitimacy Audit + ## Standard Stack — tenacity 9.1.4 verified on PyPI, repo github.com/jd/tenacity, ~9 yrs history)
    - pyproject.toml (the `[project] dependencies` array, lines 6-13 — where the pin goes)
  </read_first>
  <how-to-verify>
    1. Open https://pypi.org/project/tenacity/ and confirm: package name is exactly `tenacity`, latest version is 9.1.x (>=9.1.4), the project links to source repo `github.com/jd/tenacity`, and it shows a long publish history (first release ~2016) with very high download counts.
    2. Confirm the source repo `github.com/jd/tenacity` exists and is the canonical retry library (this matches CLAUDE.md / STACK.md which already pin `tenacity 9.x`).
    3. Confirm `requires_python` is `>=3.10` (compatible with this project's `requires-python = ">=3.12"`).
  </how-to-verify>
  <action>
    This is the package-legitimacy gate (threat T-04-SC). Do NOT auto-approve — this checkpoint ignores `workflow.auto_advance`. After human approval, run `uv add "tenacity>=9.1.4"` so it is appended to the `[project] dependencies` array in pyproject.toml AND `uv.lock` is updated, then `uv sync`. If the human REJECTS or the registry facts do not match, do NOT install: instead implement the documented zero-dependency fallback (a hand-rolled loop calling `stop_event.wait(backoff)` for the same two-burst timing) noted in 04-RESEARCH.md "Alternatives Considered" / "Environment Availability". The fallback preserves identical interruptibility via `stop_event.wait`.
  </action>
  <verify>
    <automated>uv run python -c "import tenacity; print(tenacity.__version__)"</automated>
  </verify>
  <resume-signal>Type "approved" to install tenacity, or "rejected — use fallback" to build the hand-rolled loop instead.</resume-signal>
  <acceptance_criteria>
    - `pyproject.toml` `[project] dependencies` array contains a `tenacity>=9.1.4` entry (OR, on rejection, no tenacity entry and retry.py uses the hand-rolled `stop_event.wait` loop)
    - `uv.lock` is updated (tenacity resolved) when approved
    - `uv run python -c "import tenacity"` exits 0 when approved
  </acceptance_criteria>
  <done>tenacity is verified-and-installed (approved path) or explicitly rejected with the fallback selected. The decision is recorded in the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the reliability package — classifier, Retry-After parser, two-burst builder</name>
  <files>weatherbot/reliability/__init__.py, weatherbot/reliability/retry.py</files>
  <read_first>
    - weatherbot/weather/client.py (lines 1-65 — the httpx error surface analog: `fetch_onecall` calls `response.raise_for_status()`, so callers receive `httpx.HTTPStatusError` carrying `.response.status_code` and `.response.headers`; the module-docstring + CAPS-constant + secret-hygiene style to mirror)
    - weatherbot/config/__init__.py (the barrel `__all__` re-export shape to mirror in reliability/__init__.py)
    - weatherbot/scheduler/daemon.py (lines 319-332 — `stop = threading.Event()`; this is the SAME event whose `.wait` becomes tenacity's `sleep=` callable)
    - weatherbot/channels/base.py (lines 23-34 — `DeliveryResult(ok, detail)`; a send returns `ok=False` rather than raising, so the builder must retry on a non-ok RESULT as well as on a transient EXCEPTION)
    - .planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md (Pattern 1 two-burst wait, Pattern 4 classification + capped Retry-After, Pattern 7 interruptible sleep, Code Examples "Imperative retry of a callable that returns DeliveryResult")
    - .planning/phases/04-retry-then-alert-reliability/04-PATTERNS.md (### weatherbot/reliability/retry.py — analog assignments + interruptible-sleep wiring)
  </read_first>
  <behavior>
    - Test: `two_burst_wait` returns a value < ~150s for attempts 1..7 and 9..15 (within-burst spread), and exactly `MID_PAUSE_S` after attempt 8 (the burst-1→burst-2 long pause)
    - Test: `is_transient` returns True for `httpx.TimeoutException`, `httpx.ConnectError`, `httpx.ReadError`, and `HTTPStatusError` with status in {429,500,502,503,504}; returns False for status in {400,401,403,404}
    - Test: `is_auth_failure` returns True only for `HTTPStatusError` 401/403, False for 400/404/500/timeout
    - Test: `parse_retry_after` parses a seconds value ("30" → 30.0), parses an HTTP-date, returns None when absent, and caps any value at `RETRY_AFTER_CAP_S`
    - Test: `build_retrying(stop_event)` returns a `Retrying` whose `.sleep` is `stop_event.wait` and whose `.stop` halts at 16 attempts (2*BURST_SIZE); a transient-then-success callable run through it succeeds; a 401-raising callable is NOT retried (raises out immediately, attempt_number==1)
    - Test: with a recording mock passed as `sleep=`, the full retry of an always-transient callable records the two-burst durations in milliseconds (no real sleeping)
  </behavior>
  <action>
    Create `weatherbot/reliability/retry.py` with `from __future__ import annotations` and the secret-hygiene module docstring (mirror client.py). Define CAPS constants exactly: `BURST_SIZE = 8`, `BURST_SPREAD_S = 600`, `MID_PAUSE_S = 2700`, `RETRY_AFTER_CAP_S = 120` (Claude's discretion per D-08 — keep total under 90 min), `PERMANENT = frozenset({400, 401, 403, 404})`, `TRANSIENT = frozenset({429, 500, 502, 503, 504})`, and reason strings `REASON_TRANSIENT_EXHAUSTED = "transient_exhausted"`, `REASON_AUTH_FAILED = "auth_failed"`, `REASON_INTERNAL_ERROR = "internal_error"`.
    Implement `is_transient(exc)`, `is_auth_failure(exc)` (HTTPStatusError 401/403 only), and `parse_retry_after(resp)` using `email.utils.parsedate_to_datetime` for the HTTP-date form, capping at `RETRY_AFTER_CAP_S` per D-08/Pattern 4.
    Implement `two_burst_wait(retry_state)` per Pattern 1: spread = `burst_spread_s/(attempts_per_burst-1)` plus bounded jitter for within-burst attempts; return `mid_pause_s` after attempt number == `attempts_per_burst`. Implement `build_retrying(stop_event, *, attempts_per_burst=..., burst_spread_s=..., mid_pause_s=...)` returning `Retrying(wait=two_burst_wait_closure, stop=stop_after_attempt(2*attempts_per_burst), retry=(retry_if_result(lambda r: not getattr(r, "ok", True)) | retry_if_exception(is_transient)), sleep=stop_event.wait, reraise=True)` — the `sleep=stop_event.wait` is the locked interruptibility constraint (D-07 / Pitfall 1); do NOT use `time.sleep`. Add an optional `before_sleep` logging hook (structlog `_log.info("retry_attempt", attempt=..., burst=...)`) — outcome-only, NEVER a secret.
    DO NOT add a second retry for Discord 429 here: the channel already sets `rate_limit_retry=True`; treat a Discord `DeliveryResult(ok=False)` as ONE transient unit (the `retry_if_result` predicate). The capped `Retry-After` applies to the OpenWeather fetch 429 path only (document this in the docstring per Pitfall 2 / Anti-Patterns).
    If tenacity was REJECTED in Task 1, implement `build_retrying`'s callable-runner as a hand-rolled loop using `stop_event.wait(two_burst_wait(...))` with the identical attempt budget, classification, and reraise behavior — keep the same public function names so consumers are unaffected.
    Create `weatherbot/reliability/__init__.py` mirroring `config/__init__.py`: re-export `build_retrying`, `is_transient`, `is_auth_failure`, `parse_retry_after`, and the `REASON_*` constants in `__all__`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_reliability.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `weatherbot/reliability/retry.py` contains `def is_transient(`, `def is_auth_failure(`, `def parse_retry_after(`, `def two_burst_wait(`, and `def build_retrying(`
    - `grep -n "sleep=" weatherbot/reliability/retry.py` shows the `sleep=stop_event.wait` wiring (interruptible pause, D-07) — and `grep -n "time.sleep" weatherbot/reliability/retry.py` returns nothing
    - `weatherbot/reliability/__init__.py` re-exports `build_retrying` and the three `REASON_*` constants in `__all__`
    - Behavior: a callable raising `HTTPStatusError(401)` run through `build_retrying(...)` is NOT retried (the test asserts a single attempt)
    - Behavior: `parse_retry_after` caps an oversized value at `RETRY_AFTER_CAP_S` (DoS-of-self mitigation)
    - Secret hygiene: `grep -nE "appid|api.openweathermap.org|webhook" weatherbot/reliability/retry.py` returns nothing (T-04-01 — no secret in the retry layer or its log fields)
    - `uv run pytest tests/test_reliability.py -q -x` exits 0
  </acceptance_criteria>
  <done>The reliability package builds the two-burst interruptible retry with correct transient/auth classification and capped Retry-After; all engine tests pass; no secret appears in the module.</done>
</task>

<task type="auto">
  <name>Task 3: Establish the test_reliability.py Wave-0 scaffold for downstream plans</name>
  <files>tests/test_reliability.py</files>
  <read_first>
    - tests/test_store.py (lines 1-90 — the `_connect(db_path)` `sqlite3.Row` helper + arrange-act-assert-over-rows pattern, and the `tmp_db`/`load_fixture` fixture usage to copy)
    - tests/conftest.py (the `tmp_db` and `load_fixture` fixtures available to every test)
    - .planning/phases/04-retry-then-alert-reliability/04-RESEARCH.md (## Validation Architecture — the Req→Test map naming the exact test functions; ## Wave 0 Gaps)
    - .planning/phases/04-retry-then-alert-reliability/04-VALIDATION.md (the contract this scaffold fulfills)
  </read_first>
  <action>
    In the same `tests/test_reliability.py` file, alongside Task 2's engine tests, add a local `_connect(db_path)` helper (sqlite3.Row, copied from test_store.py) and clearly-marked placeholder test stubs for the behavior tests that Plans 03/04 will implement, each named EXACTLY as the RESEARCH Req→Test map specifies and each marked `pytest.mark.skip(reason="implemented in Plan 04-03")` (or 04-04) so the file collects green now and the downstream plan just removes the skip + fills the body. Stubs to scaffold: `test_transient_retries_then_succeeds`, `test_auth_no_retry`, `test_retry_after_capped`, `test_exhaustion_alerts`, `test_alert_dedup_no_loop`, `test_heartbeat_upsert`, `test_exception_isolation`, `test_pause_interruptible`. (Engine-level tests `test_two_burst_wait_shape`, the classifier tests, and the `build_retrying` 401/transient tests from Task 2 are real, not stubs.) Add a module docstring stating the secret-hygiene assertion convention (grep stored rows / captured logs for `"appid"` and `"api.openweathermap.org"`, assert absent — copied from test_store.py lines 135-147).
  </action>
  <verify>
    <automated>uv run pytest tests/test_reliability.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_reliability.py` defines (real or skipped) functions named `test_transient_retries_then_succeeds`, `test_auth_no_retry`, `test_retry_after_capped`, `test_exhaustion_alerts`, `test_alert_dedup_no_loop`, `test_heartbeat_upsert`, `test_exception_isolation`, `test_pause_interruptible`
    - The file contains a `_connect(` helper using `sqlite3.Row`
    - `uv run pytest tests/test_reliability.py -q` exits 0 (Task 2 tests pass; downstream stubs are skipped, not failing)
    - `uv run pytest -q` exits 0 (full suite still green — no regressions)
  </acceptance_criteria>
  <done>The Wave-0 test scaffold exists with every RELY-01..06 + D-07 test name present (real or skip-marked), so each downstream plan removes a skip rather than inventing test names; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenWeather → client → retry layer | Untrusted HTTP responses (incl. the `Retry-After` header) cross into the classifier |
| package registry → build | A new third-party dependency (`tenacity`) enters the supply chain |
| retry layer → logs | Structured retry/log fields could leak a secret if mis-authored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | `uv add tenacity` (supply chain) | mitigate | Blocking human-verify checkpoint (Task 1) confirms registry/repo/history before install; `[ASSUMED]` per slopcheck-unavailable protocol; rejection path uses zero-dep fallback |
| T-04-01 | Information Disclosure | retry.py log fields / constants | mitigate | Outcome-only `before_sleep` logging (attempt/burst); grep gate asserts no `appid`/host/`webhook` in retry.py (acceptance criteria, Task 2) |
| T-04-DoS | Denial of Service | untrusted `Retry-After` header | mitigate | `parse_retry_after` caps at `RETRY_AFTER_CAP_S`; `stop_after_attempt(16)` bounds the whole schedule (V5 input validation) |
</threat_model>

<verification>
- `uv run pytest tests/test_reliability.py -q -x` passes (engine tests green, stubs skipped)
- `uv run pytest -q` passes (no regression to Phases 1-3 suites)
- `grep -nE "appid|api.openweathermap.org|webhook" weatherbot/reliability/` returns nothing (T-04-01)
- `grep -n "time.sleep" weatherbot/reliability/retry.py` returns nothing (interruptible pause, D-07)
</verification>

<success_criteria>
- RELY-01: a transient exception OR a non-ok `DeliveryResult` is retryable through `build_retrying`; the schedule is the two-burst shape (8 / ~10 min / ~45 min / 8)
- RELY-02: 401/403 short-circuits with no retry; `Retry-After` is parsed and capped
- The 45-min mid-pause is interruptible via `sleep=stop_event.wait` (D-07 locked constraint)
- The reliability package exposes a stable public contract (`build_retrying` + classifier + `REASON_*`) for Plans 03/04
- tenacity install was human-verified (or the fallback was deliberately chosen)
</success_criteria>

<output>
Create `.planning/phases/04-retry-then-alert-reliability/04-01-SUMMARY.md` when done. Record: whether tenacity was approved or the fallback used; the final `RETRY_AFTER_CAP_S` value chosen; the exact public symbols exported from `weatherbot/reliability/`.
</output>
