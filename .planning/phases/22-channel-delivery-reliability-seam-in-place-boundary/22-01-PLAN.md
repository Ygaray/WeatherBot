---
phase: 22-channel-delivery-reliability-seam-in-place-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - yahir_reusable_bot/__init__.py
  - yahir_reusable_bot/channels/__init__.py
  - yahir_reusable_bot/reliability/__init__.py
  - yahir_reusable_bot/ports/__init__.py
  - tests/test_import_hygiene.py
autonomous: true
requirements: [PKG-01]
must_haves:
  truths:
    - "The flat-sibling package `yahir_reusable_bot/` exists at the repo root and imports cleanly"
    - "`grimp>=3.14` is installed in the dev group and importable"
    - "The wheel build config explicitly lists both `weatherbot` and `yahir_reusable_bot` packages"
    - "The three import-hygiene gates exist as standing pytest tests and pass on the (empty) scaffold"
    - "Each gate has a self-proof meta-test proving it TRIPS on a deliberately-injected leak"
    - "The full 732-test suite stays green with zero golden snapshot diff"
  artifacts:
    - path: "yahir_reusable_bot/__init__.py"
      provides: "Package marker for the final-named reusable module (D-01)"
      min_lines: 1
    - path: "tests/test_import_hygiene.py"
      provides: "grimp import-graph gate, isolated-import smoke, AST litmus + self-proofs (D-09/D-10/D-11/D-13)"
      min_lines: 120
    - path: "pyproject.toml"
      provides: "hatchling wheel `packages` block, grimp dev-dep, coverage source extension (D-02)"
      contains: "tool.hatch.build.targets.wheel"
  key_links:
    - from: "tests/test_import_hygiene.py"
      to: "yahir_reusable_bot"
      via: "grimp.build_graph('yahir_reusable_bot') + sys.meta_path blocker + ast walk of the package tree"
      pattern: "build_graph|meta_path|ast\\.walk"
    - from: "pyproject.toml"
      to: "yahir_reusable_bot"
      via: "[tool.hatch.build.targets.wheel] packages list + [tool.coverage.run] source"
      pattern: "yahir_reusable_bot"
---

<objective>
Stand up the clean in-place package boundary `yahir_reusable_bot/` (named its FINAL name now,
per D-01, so Phase 28 is a `git mv` not a rename) and wire the three standing import-hygiene
gates as red-then-green pytest infrastructure — BEFORE any code moves into the package. This is
the foundation the channel seam (Plan 02) and reliability seam (Plan 03) land against, so each
move is validated by a live gate the instant it happens.

Purpose: PKG-01 demands a clean in-place boundary enforced by an import-lint/grep gate, anchored
here and re-run on phases 23–27. Building the gates against an empty-but-real package first means
the gates are proven (via self-proofs) before they guard real moved code.

Output: the `yahir_reusable_bot/` package skeleton (`__init__.py` + `channels/`, `reliability/`,
`ports/` subpackages), the `grimp>=3.14` dev dependency, the `[tool.hatch.build.targets.wheel]
packages` block + extended coverage source in `pyproject.toml`, and `tests/test_import_hygiene.py`
with three gates + three self-proof meta-tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-CONTEXT.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-RESEARCH.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-PATTERNS.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-VALIDATION.md

# The self-proof structure to mirror (Phase-21 oracle self-proof)
@tests/test_oracle_selfproof.py

# The pyproject surface being edited (build-system=hatchling VERIFIED, no [tool.hatch] block today)
@pyproject.toml
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols/paths (the rest of the phase's artifacts land in Plans 02/03):

- **Package skeleton:** `yahir_reusable_bot/` (final import root, D-01) with `__init__.py` and the
  `channels/`, `reliability/`, `ports/` subpackages (empty `__init__.py` markers this plan; the
  real code lands in Plans 02/03).
- **`pyproject.toml` `[tool.hatch.build.targets.wheel]` block:** `packages = ["weatherbot",
  "yahir_reusable_bot"]` (D-02 — without it hatchling silently drops the 2nd package).
- **`grimp>=3.14`** added to the existing `[dependency-groups] dev` list (D-09, dev-only).
- **`[tool.coverage.run] source`** extended with `"yahir_reusable_bot"` (Pitfall 5).
- **`tests/test_import_hygiene.py`** with these test functions (the standing gate documented as a
  23–27 re-run criterion, D-13):
  - `test_module_imports_zero_app_code` — grimp graph assert (D-09)
  - `test_module_imports_with_app_blocked` — isolated-import smoke (D-10)
  - `test_litmus_clean` — AST signature-only litmus (D-11/D-13)
  - `test_selfproof_import_gate_catches_injected_app_edge` — proves the grimp gate trips
  - `test_selfproof_isolated_import_catches_app_import` — proves the smoke trips
  - `test_selfproof_litmus_catches_weather_noun` — proves the litmus trips on a name, ignores prose
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the package skeleton and wire pyproject build/coverage/dev-dep config</name>
  <read_first>
    - `pyproject.toml` lines 18-65 (build-system=hatchling VERIFIED; `[dependency-groups] dev`
      lines 26-33; `[tool.coverage.run] source` lines 41-49 — all read this session)
    - `weatherbot/__init__.py` (role-analog for the package marker)
    - 22-RESEARCH.md § "pyproject.toml additions" and 22-PATTERNS.md § "pyproject.toml (MODIFIED)"
  </read_first>
  <files>
    yahir_reusable_bot/__init__.py,
    yahir_reusable_bot/channels/__init__.py,
    yahir_reusable_bot/reliability/__init__.py,
    yahir_reusable_bot/ports/__init__.py,
    pyproject.toml
  </files>
  <action>
    Create the flat-sibling package `yahir_reusable_bot/` at the repo root (sibling to
    `weatherbot/`) with its FINAL name in place from day one (D-01). Create four package-marker
    files: `yahir_reusable_bot/__init__.py` (a short module docstring describing the reusable bot
    module boundary; no app-specific re-exports), and empty `__init__.py` markers for the
    `channels/`, `reliability/`, and `ports/` subpackages. Keep these `__init__.py` files free of
    any weather noun in identifier/signature names (the litmus gate runs over this tree) — a
    module docstring may describe purpose in prose (the AST litmus ignores prose per D-11). The
    real Channel/retry/AlertSink code lands in Plans 02/03; this plan only stands up the shape.

    Then make three ADDITIVE edits to `pyproject.toml` (do NOT touch `requires-python`,
    `[build-system]`, `[project.scripts]`, `[tool.pytest.ini_options]`, or
    `[tool.coverage.report] exclude_also` — D-01 keeps test/coverage config byte-undisturbed):
    (1) Add a NEW `[tool.hatch.build.targets.wheel]` table with `packages = ["weatherbot",
    "yahir_reusable_bot"]` (D-02 — hatchling auto-discovery only finds the package matching the
    dist name; a differently-named sibling is silently dropped from the wheel without this).
    (2) Append `"grimp>=3.14"` to the EXISTING `[dependency-groups] dev` list (D-09, dev-only —
    the gate runs in tests, never ships in the wheel or at app runtime).
    (3) Append `"yahir_reusable_bot"` to the `[tool.coverage.run] source` list (Pitfall 5 — so the
    soon-to-be-moved code is still covered where it now lives).

    Run `uv sync` (or `uv add --dev grimp`) so grimp `>=3.14` is installed and importable.
  </action>
  <verify>
    <automated>uv sync && uv run python -c "import yahir_reusable_bot, yahir_reusable_bot.channels, yahir_reusable_bot.reliability, yahir_reusable_bot.ports, grimp; print('grimp', grimp.__version__)" && uv run python -c "import grimp,sys; v=tuple(int(x) for x in grimp.__version__.split('.')[:2]); assert v>=(3,14), grimp.__version__"</automated>
  </verify>
  <acceptance_criteria>
    - `yahir_reusable_bot` and all three subpackages import without error.
    - `grimp.__version__` is `>= 3.14`.
    - `pyproject.toml` contains `[tool.hatch.build.targets.wheel]` with both packages listed,
      `grimp>=3.14` in the dev group, and `yahir_reusable_bot` in `[tool.coverage.run] source`.
    - `[project.scripts]` still has only the `weatherbot` console script (the module ships none).
  </acceptance_criteria>
  <done>
    The package skeleton exists, grimp is installed, and the build/coverage config explicitly
    covers both packages — verified by the import smoke command above.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the three import-hygiene gates + their self-proofs in tests/test_import_hygiene.py</name>
  <read_first>
    - `tests/test_oracle_selfproof.py` (WHOLE file — copy its STRUCTURE: a standing green test
      whose green-ness depends on a real perturbation tripping the guard via
      `with pytest.raises(AssertionError): ...`; copy the module-docstring discipline)
    - 22-RESEARCH.md § "Code Examples" (VERIFIED grimp gate body) + Patterns 4, 5, 6 (VERIFIED
      grimp / sys.meta_path blocker / AST litmus bodies, all probed against the real tree)
    - 22-PATTERNS.md § "tests/test_import_hygiene.py" (maps each gate to its verified body)
    - 22-VALIDATION.md § "What 'fully covered' means per NEW gate"
  </read_first>
  <files>tests/test_import_hygiene.py</files>
  <behavior>
    - test_module_imports_zero_app_code: builds `grimp.build_graph("yahir_reusable_bot")` (KEEP
      the default `exclude_type_checking_imports=False` so TYPE_CHECKING edges are graphed) and
      asserts no module in the graph directly imports `weatherbot` or `weatherbot.*`. On the
      empty/clean scaffold → zero leaks → passes. Failure message includes the offending
      (importer, imported) pair and `get_import_details` line numbers.
    - test_module_imports_with_app_blocked: inserts a `sys.meta_path` finder that raises
      `ImportError` for any `weatherbot`/`weatherbot.*` name, then `importlib.import_module`s every
      module under `yahir_reusable_bot` (via `pkgutil.walk_packages`); asserts no ImportError. A
      `finally:` removes the blocker AND purges `sys.modules` keys starting with
      `yahir_reusable_bot` so other tests re-import cleanly.
    - test_litmus_clean: walks every `.py` under `yahir_reusable_bot/`, AST-extracts public
      identifier names (`FunctionDef`/`AsyncFunctionDef`/`ClassDef` names, `arg` names + their
      `ast.unparse`d annotations, function `returns`) — NOT docstrings/comments — and asserts none
      matches `/weather|forecast|location|openweather|\buv\b|briefing/i` (the D-13 locked pattern).
    - test_selfproof_import_gate_catches_injected_app_edge: runs the same leak-scan logic against a
      synthetic edge set containing a `(importer, "weatherbot.weather.models")` pair and asserts
      the scan flags it (proves the gate is not a no-op).
    - test_selfproof_isolated_import_catches_app_import: with the blocker installed, asserts that
      `importlib.import_module("weatherbot.weather.models")` (or a tiny temp module that imports
      app code) raises ImportError — proving the blocker actually blocks.
    - test_selfproof_litmus_catches_weather_noun: feeds a synthetic
      `def send_briefing(forecast): ...` source through the public-name extractor and asserts the
      pattern matches; feeds a docstring-only weather noun and asserts the extractor does NOT
      surface it (prose is ignored per D-11).
  </behavior>
  <action>
    Create `tests/test_import_hygiene.py`. Open with a module docstring (mirroring
    `test_oracle_selfproof.py` lines 1-38) stating: what each gate protects (the one-way
    app→module dependency + the weather-noun-free public surface), that each gate has TWO halves
    (the real scaffold must pass / a deliberately-injected leak must fail), why these are standing
    pytest asserts and NOT xfail markers, and that this file is the STANDING gate phases 23–27
    re-run (D-13). Implement the three gates using the VERIFIED bodies from 22-RESEARCH.md (do not
    re-derive them — they were probed against the real tree this session). For the grimp gate, keep
    `exclude_type_checking_imports` at its DEFAULT (False) — passing True would HIDE the very
    TYPE_CHECKING leak the gate exists to catch (Anti-pattern, RESEARCH Pitfall 1). The prefix
    check (`target == "weatherbot" or target.startswith("weatherbot.")`) auto-scales as the module
    grows across 23–27 with no per-module edit. Refactor the leak-scan and the public-name
    extractor into small module-level helper functions so the self-proof meta-tests can exercise
    the SAME logic the gates use (not a copy). For the litmus pattern, record an inline comment
    noting the known `\buv\b` underscore gap (`uv_index`-style names slip through — document, do
    NOT "fix"; the pattern is the D-13 locked literal). Include the `finally:` cleanup that purges
    `sys.modules["yahir_reusable_bot*"]` after the blocker test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_import_hygiene.py -x</automated>
  </verify>
  <acceptance_criteria>
    - All six tests pass against the empty scaffold.
    - The three gates use shared helper functions that the three self-proofs also call (proving
      the gate logic — not a copy — trips on injected leaks).
    - The grimp gate uses `build_graph` with the default TYPE_CHECKING inclusion (no
      `exclude_type_checking_imports=True`).
    - The blocker test restores `sys.meta_path` and purges `sys.modules` in a `finally:`.
    - `grep -n 'exclude_type_checking_imports' tests/test_import_hygiene.py` shows no `=True`.
  </acceptance_criteria>
  <done>
    `tests/test_import_hygiene.py` exists with three standing gates + three self-proofs, all green,
    and each self-proof proves its gate trips on a deliberately-injected leak/noun.
  </done>
</task>

<task type="auto">
  <name>Task 3: Self-UAT — confirm the full oracle suite is byte-identical green with the scaffold in place</name>
  <read_first>
    - 22-VALIDATION.md § "Sampling Rate" (after every plan wave: full suite + zero golden diff)
    - The user CLAUDE.md Two-Gate UAT policy (autonomous self-UAT writes a persistent log; no
      per-phase human pause)
  </read_first>
  <files>.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-01-SELFUAT.md</files>
  <action>
    Run the FULL suite (`uv run pytest`) and confirm 732 tests pass with ZERO golden snapshot diff
    (no `tests/__snapshots__/` change). The scaffold adds an empty package + config + a new test
    file; it must NOT perturb any existing behavior or golden. Write a persistent self-UAT log
    recording, per success criterion: what was checked, the exact command, the observed
    PASS/FAIL/PARTIAL, and the live test count. Record the deferred Gate-2 host obligation (on
    `yahir-mint`: `uv sync` → `systemctl restart weatherbot` → confirm `import yahir_reusable_bot`
    resolves + daemon comes online) as a milestone-close item, NOT a phase blocker. If ANY golden
    diff appears, HALT and investigate — never `--snapshot-update` it away (Phase-21 D-04).
  </action>
  <verify>
    <automated>uv run pytest -q && git -c core.pager=cat status --porcelain tests/__snapshots__/ | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (732 tests, up from the 649 v1.3 figure — use the live count).
    - `git status --porcelain tests/__snapshots__/` is EMPTY (zero golden diff).
    - `22-01-SELFUAT.md` exists with per-criterion evidence and the deferred host Gate-2 item.
  </acceptance_criteria>
  <done>
    The scaffold + gates land with the full oracle suite green and zero golden diff, logged in the
    self-UAT file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev toolchain → repo | The new `grimp` dev dependency is resolved/installed by `uv` from PyPI |
| build config → wheel | The `[tool.hatch...packages]` block determines what ships in the wheel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering (supply chain) | `grimp` dev dep | mitigate | Legitimacy verified in 22-RESEARCH.md Package Legitimacy Audit (PyPI, authored by David Seddon = import-linter author, the engine import-linter is built on; OK verdict). Dev-only — never enters the app runtime or the `yahir_reusable_bot` wheel surface. |
| T-22-02 | Tampering (packaging) | `[tool.hatch.build.targets.wheel]` block | mitigate | Without the explicit `packages` list hatchling silently drops `yahir_reusable_bot` from the wheel (Pitfall 4). Task 1 adds the block now; the Phase-28 `uv build --no-sources` leak gate is the eventual backstop. |
| T-22-03 | Information Disclosure | module public surface | accept | No credential (`DISCORD_WEBHOOK_URL`/`appid`) is referenced by the scaffold; secrets stay app-side in `DiscordWebhookChannel._url` (unmoved). No PII, no runtime behavior added this plan. |
| T-22-SC | Tampering | npm/pip installs (`uv add --dev grimp`) | mitigate | grimp is `[OK]`-verified (not `[ASSUMED]`/`[SUS]`/`[SLOP]`) in the RESEARCH Package Legitimacy Audit; no human-verify checkpoint required. |
</threat_model>

<verification>
- `uv run pytest tests/test_import_hygiene.py -x` — all six gate + self-proof tests green.
- `uv run pytest` — full 732-test suite green.
- `git status --porcelain tests/__snapshots__/` — empty (zero golden diff).
- `import yahir_reusable_bot` + subpackages resolve; `grimp.__version__ >= 3.14`.
</verification>

<success_criteria>
- The `yahir_reusable_bot/` flat-sibling package exists with its final name and three subpackages.
- `grimp>=3.14` installed; `pyproject.toml` lists both packages in the wheel target + coverage source.
- The three import-hygiene gates + three self-proofs pass; each self-proof proves its gate trips.
- Full suite green, zero golden snapshot diff, self-UAT log written.
</success_criteria>

<output>
Create `.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-01-SUMMARY.md` when done.
</output>
