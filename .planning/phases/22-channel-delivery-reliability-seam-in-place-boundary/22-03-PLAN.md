---
phase: 22-channel-delivery-reliability-seam-in-place-boundary
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - yahir_reusable_bot/reliability/retry.py
  - yahir_reusable_bot/reliability/__init__.py
  - yahir_reusable_bot/ports/alerts.py
  - yahir_reusable_bot/ports/__init__.py
  - weatherbot/reliability/__init__.py
  - weatherbot/reliability/retry.py
  - weatherbot/scheduler/daemon.py
autonomous: true
requirements: [SEAM-01, PKG-01]
must_haves:
  truths:
    - "The retry engine (`build_retrying`, classifiers, `parse_retry_after`, `REASON_*`) lives in `yahir_reusable_bot.reliability` with zero weather coupling"
    - "An `AlertSink` port (Protocol) is defined in `yahir_reusable_bot.ports` with weather-clean parameter names (no `location_name` in the port signature)"
    - "`weatherbot.reliability` re-exports the moved surface so `daemon.py`/`cli.py` imports + Phase-21 exception-identity pins stay byte-identical"
    - "`fire_slot` is ADAPTED not rewritten — its retry/alert orchestration body is byte-identical"
    - "The import-graph + isolated-import + litmus gates are all green over the full module (channels + reliability + ports)"
    - "Full 732-test suite green with zero golden snapshot diff"
  artifacts:
    - path: "yahir_reusable_bot/reliability/retry.py"
      provides: "Verbatim move of the tenacity retry engine (D-06)"
      contains: "def build_retrying"
      min_lines: 150
    - path: "yahir_reusable_bot/ports/alerts.py"
      provides: "AlertSink Protocol — record_alert/resolve_alert, weather-clean param names (D-07)"
      contains: "class AlertSink"
  key_links:
    - from: "weatherbot/reliability/__init__.py"
      to: "yahir_reusable_bot/reliability/__init__.py"
      via: "re-export of the full retry surface (build_retrying, is_transient, ... REASON_*)"
      pattern: "from yahir_reusable_bot.reliability import"
    - from: "weatherbot/scheduler/daemon.py"
      to: "yahir_reusable_bot/reliability"
      via: "build_retrying/is_auth_failure resolve through the weatherbot.reliability shim (no call-site change)"
      pattern: "from weatherbot.reliability import"
---

<objective>
Move the entire retry engine from `weatherbot/reliability/retry.py` into
`yahir_reusable_bot.reliability` VERBATIM (it is already 100% app-clean — VERIFIED zero
`weatherbot.*` edges, pure tenacity/httpx/structlog/stdlib composition, D-06), define the
`AlertSink` port (Protocol) in the module shaped to what `fire_slot` actually calls (D-07), and
re-export the moved surface through `weatherbot.reliability` so the daemon/CLI imports and the
Phase-21 exception-identity pins stay byte-identical. `fire_slot` is ADAPTED, never rewritten —
its retry-then-alert orchestration body is the byte-identical-locked surface (D-07 / no
`DeliveryGuard`).

Purpose: SEAM-01 (delivery-reliability wrapper + out-of-band alert port in the module, zero
weather coupling) + PKG-01 (module imports zero app code). Heartbeat is OUT (D-08 — Phase 25).

Output: `yahir_reusable_bot/reliability/{retry,__init__}.py`, `yahir_reusable_bot/ports/alerts.py`
(the `AlertSink` Protocol), the `weatherbot.reliability` re-export shim, and the (byte-identical)
daemon adaptation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-CONTEXT.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-RESEARCH.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-PATTERNS.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-VALIDATION.md
@.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-02-SUMMARY.md

# The exact source moving (verbatim) + its re-export surface
@weatherbot/reliability/retry.py
@weatherbot/reliability/__init__.py

# The store functions the AlertSink port mirrors (read the signatures only)
@weatherbot/weather/store.py

# The fire_slot orchestration that consumes retry + alert (ADAPT, do NOT rewrite)
@weatherbot/scheduler/daemon.py
</context>

<artifacts_this_phase_produces>
- **`yahir_reusable_bot/reliability/retry.py`** — verbatim move of `weatherbot/reliability/retry.py`
  (constants `BURST_SIZE`/`BURST_SPREAD_S`/`MID_PAUSE_S`/`RETRY_AFTER_CAP_S`, `PERMANENT`/
  `TRANSIENT` frozensets, `REASON_TRANSIENT_EXHAUSTED`/`REASON_AUTH_FAILED`/`REASON_INTERNAL_ERROR`,
  and functions `is_transient`, `is_auth_failure`, `parse_retry_after`, `_within_burst_wait`,
  `two_burst_wait`, `build_retrying`), including the `# pragma: no cover - <reason>` comment.
- **`yahir_reusable_bot/reliability/__init__.py`** — re-exports the public retry surface
  (the same name list as today's `weatherbot/reliability/__init__.py`).
- **`yahir_reusable_bot/ports/alerts.py`** — the `AlertSink` Protocol (D-07): `record_alert(...)
  -> bool` and `resolve_alert(...) -> None` with weather-CLEAN parameter names (the litmus
  constraint: rename `location_name` to e.g. `target` / `location_id` in the PORT signature even
  though the app impl keeps `location_name`). NO `briefing_missed`, NO heartbeat method (D-08).
- **`weatherbot/reliability/{retry,__init__}.py`** — re-export shims pointing at the module.
- **Daemon adaptation note** — `fire_slot` consumes `build_retrying`/`is_auth_failure` through the
  shim (no call-site source change) and the alert path is documented as flowing through the
  `AlertSink` port contract; the orchestration body is byte-identical.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move the retry engine verbatim into yahir_reusable_bot.reliability + shim weatherbot.reliability</name>
  <read_first>
    - `weatherbot/reliability/retry.py` (WHOLE file — the import block lines 39-54 is all
      third-party/stdlib, allowed in the module; the public surface lines 57-184; the
      `# pragma: no cover` at line 125 moves verbatim)
    - `weatherbot/reliability/__init__.py` (lines 8-26 — the re-export body moves unchanged into
      the module's `__init__`; the app-side file becomes a shim)
    - 22-PATTERNS.md § "yahir_reusable_bot/reliability/retry.py" and § "...reliability/__init__.py"
      and § "weatherbot/reliability/__init__.py (MODIFIED)"
    - The daemon/CLI consumers: `weatherbot/scheduler/daemon.py:58-64` and `weatherbot/cli.py`
      `from weatherbot.reliability import ...` — the shim keeps these byte-identical (Phase-21
      exception-identity pins depend on it)
  </read_first>
  <files>
    yahir_reusable_bot/reliability/retry.py,
    yahir_reusable_bot/reliability/__init__.py,
    weatherbot/reliability/retry.py,
    weatherbot/reliability/__init__.py
  </files>
  <action>
    Move `weatherbot/reliability/retry.py` VERBATIM into `yahir_reusable_bot/reliability/retry.py`
    — it is already weather-clean (zero `weatherbot.*` edges; the docstring weather-noun prose is
    IGNORED by the signature-only litmus, D-11; do NOT scrub it — that is Phase 28 / DOCS-01).
    Carry the import block (`from __future__ import annotations`, `random`,
    `email.utils.parsedate_to_datetime`, `datetime`, `httpx`, `structlog`, the `tenacity` imports,
    `structlog.get_logger(__name__)`) and the full public surface unchanged, including the
    `# pragma: no cover - <reason>` comment at the current line 125 (Phase-21 D-09 convention).

    Copy the re-export body of the current `weatherbot/reliability/__init__.py` (lines 8-26) into
    `yahir_reusable_bot/reliability/__init__.py` UNCHANGED (it already re-exports from `.retry`).

    Convert `weatherbot/reliability/__init__.py` into a thin re-export SHIM: `from
    yahir_reusable_bot.reliability import (REASON_AUTH_FAILED, REASON_INTERNAL_ERROR,
    REASON_TRANSIENT_EXHAUSTED, build_retrying, is_auth_failure, is_transient, parse_retry_after)`
    with the IDENTICAL `__all__`. For `weatherbot/reliability/retry.py`: grep first for any
    `from weatherbot.reliability.retry import` or `weatherbot.reliability.retry` consumer; if any
    exist, leave a shim that re-exports the full surface from the module so that import path still
    resolves; if none exist, the file may re-export the surface from the module for safety. Prefer
    the shim that keeps BOTH `weatherbot.reliability` AND `weatherbot.reliability.retry` resolvable
    to avoid any call-site churn (the Phase-21 exception-identity pins catch any drift).

    Do NOT touch `fire_slot` or any alert code in this task (Task 2 defines the port; the daemon
    body stays byte-identical here).
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from yahir_reusable_bot.reliability import build_retrying; m=__import__('yahir_reusable_bot.reliability.retry', fromlist=['x']); assert 'weatherbot' not in inspect.getsource(m), 'module retry.py must not reference weatherbot'; from weatherbot.reliability import build_retrying as b2; assert build_retrying is b2, 'app shim must re-export the SAME object'; print('reliability moved + shimmed')" && uv run pytest tests/test_reliability.py tests/test_import_hygiene.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `yahir_reusable_bot/reliability/retry.py` source contains no `weatherbot` reference.
    - `weatherbot.reliability.build_retrying IS yahir_reusable_bot.reliability.build_retrying`
      (the shim re-exports the same object, not a copy).
    - `tests/test_reliability.py` green; the import-graph gate reports zero
      `yahir_reusable_bot.reliability.* -> weatherbot.*` edges.
    - The `# pragma: no cover - <reason>` comment moved verbatim with its function.
  </acceptance_criteria>
  <done>
    The retry engine lives in the module weather-clean, `weatherbot.reliability` is a byte-identical
    shim, and the reliability tests + import-hygiene gate are green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the AlertSink port (weather-clean) and adapt fire_slot to consume it</name>
  <read_first>
    - `weatherbot/weather/store.py` — `record_alert` (line ~317): `(db_path, location_name,
      slot_time, local_date, reason, severity="critical") -> bool`; `resolve_alert` (line ~411):
      `(db_path, location_name, slot_time, local_date) -> None` (read the signatures only)
    - `weatherbot/scheduler/daemon.py` `fire_slot` (lines ~131-364) — the `build_retrying` call
      (~231), the `record_alert` calls (~264/282/308/349), the `resolve_alert` call (~330), and the
      reason-taxonomy except branches (254-364). This is the byte-identical-locked surface.
    - 22-PATTERNS.md § "yahir_reusable_bot/ports/alerts.py" (the two callable-port shape sources +
      the litmus constraint to rename `location_name` in the PORT) and § "weatherbot/scheduler/
      daemon.py (MODIFIED — fire_slot, ADAPT not rewrite)"
    - 22-RESEARCH.md Pattern 3 (minimal port) + Open Question 1 (ship the minimal version) + the
      anti-pattern "Rewriting fire_slot"
    - `pyproject.toml:59-63` `exclude_also` already covers `raise NotImplementedError` + bare `...`
      Protocol stub bodies (the port's stubs inherit this — no extra coverage handling needed)
  </read_first>
  <files>
    yahir_reusable_bot/ports/alerts.py,
    yahir_reusable_bot/ports/__init__.py,
    weatherbot/scheduler/daemon.py
  </files>
  <behavior>
    - `yahir_reusable_bot.ports.AlertSink` is a `typing.Protocol` exposing `record_alert(...) ->
      bool` and `resolve_alert(...) -> None` whose parameter and annotation NAMES contain NO
      litmus noun (so the AST litmus stays clean over the module surface) — in particular the
      store's `location_name` parameter is renamed (e.g. `target`/`location_id`) in the PORT
      signature, while the app store impl keeps `location_name`.
    - The port has NO `briefing_missed` and NO heartbeat method (D-08).
    - `fire_slot`'s observable behavior is byte-identical: same retry bursts, same `Retry-After`
      honoring, same no-retry-on-401/403, same `record_alert`/`resolve_alert` calls with the same
      args, same reason taxonomy. `test_scheduler.py` + `test_reliability.py` + the DB-row golden
      are the arbiter.
  </behavior>
  <action>
    Create `yahir_reusable_bot/ports/alerts.py` defining an `AlertSink` `typing.Protocol` (D-07)
    shaped to the two `fire_slot` consumes — `record_alert` and `resolve_alert` — using the
    store signatures as the arg-list reference. CRITICAL litmus constraint (D-11): the port's
    parameter/annotation NAMES must NOT contain a weather noun, so rename the store's
    `location_name` parameter to a weather-clean name (e.g. `target` or `location_id`) IN THE PORT
    SIGNATURE; arg TYPES are `str | Path` + `str` + `bool` (no `Forecast`/weather types). Keep the
    Protocol minimal: `record_alert(self, db_path, target, slot_time, local_date, reason, severity
    = "critical") -> bool` and `resolve_alert(self, db_path, target, slot_time, local_date) ->
    None`, with `...` stub bodies (covered by `exclude_also`). Do NOT add any method beyond these
    two. Export `AlertSink` from `yahir_reusable_bot/ports/__init__.py`.

    Adapt `fire_slot` per D-07 — ADAPT, do NOT rewrite. `fire_slot` is APP code and MAY keep
    importing `record_alert`/`resolve_alert` directly from `weatherbot.weather.store`; the
    `AlertSink` port matters for the MODULE's surface (it documents the delivery-lane alert contract
    the module owns), not for forcing an injection at this composition root (full composition-root
    wiring is Phase 25 / APP-02). The minimal byte-identical move: ship the port + a one-line app
    adapter (or simply document that the existing `weatherbot.weather.store` functions satisfy the
    `AlertSink` Protocol structurally) and keep the `fire_slot` body — the `build_retrying` call,
    the four `record_alert` calls, the `resolve_alert` call, and every reason-taxonomy except branch
    — EXACTLY as-is. Do NOT refactor the 3-line retry inside the weather-coupled orchestration
    (the rejected `DeliveryGuard`). Do NOT touch `_heartbeat_tick` or the `__heartbeat__`/
    `stamp_tick` lifecycle (D-08 — Phase 25).
  </action>
  <verify>
    <automated>uv run python -c "from yahir_reusable_bot.ports import AlertSink; import inspect, ast; src=inspect.getsource(__import__('yahir_reusable_bot.ports.alerts', fromlist=['x'])); names=[n.name for n in ast.walk(ast.parse(src)) if isinstance(n,(ast.FunctionDef,ast.ClassDef))]+[a.arg for a in ast.walk(ast.parse(src)) if isinstance(a,ast.arg)]; import re; bad=[x for x in names if re.search(r'weather|forecast|location|openweather|\\\\buv\\\\b|briefing', x, re.I)]; assert not bad, f'litmus noun in AlertSink signature: {bad}'; assert hasattr(AlertSink,'record_alert') and hasattr(AlertSink,'resolve_alert') and not hasattr(AlertSink,'briefing_missed'); print('AlertSink clean')" && uv run pytest tests/test_scheduler.py tests/test_reliability.py tests/test_import_hygiene.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `AlertSink` exposes `record_alert`/`resolve_alert`, NOT `briefing_missed`/heartbeat.
    - No litmus noun (`weather|forecast|location|openweather|\buv\b|briefing`) appears in any
      `def`/`class`/param/annotation name in `yahir_reusable_bot/ports/alerts.py`.
    - `test_scheduler.py` + `test_reliability.py` green — `fire_slot` orchestration byte-identical.
    - The import-hygiene gate (all three) is green over the full module (channels + reliability + ports).
  </acceptance_criteria>
  <done>
    The weather-clean `AlertSink` port is defined in the module, `fire_slot` is adapted (not
    rewritten) and byte-identical, and all three gates are green over the full module surface.
  </done>
</task>

<task type="auto">
  <name>Task 3: Self-UAT — full oracle suite byte-identical green over the complete extraction</name>
  <read_first>
    - 22-VALIDATION.md § "Sampling Rate" (per-wave: full suite + zero golden diff) and § "What
      'fully covered' means" (byte-identical oracle: zero diff across ALL `tests/__snapshots__/`)
    - The user CLAUDE.md Two-Gate UAT policy (autonomous self-UAT log; no per-phase human pause;
      record the deferred host Gate-2 obligation)
  </read_first>
  <files>.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-03-SELFUAT.md</files>
  <action>
    Run the FULL suite (`uv run pytest`) and confirm 732 tests pass with ZERO golden snapshot diff,
    AND all three import-hygiene gates + their self-proofs are green over the COMPLETE moved surface
    (channels + reliability + ports). Confirm the litmus is clean, the grimp gate reports zero
    `yahir_reusable_bot.* -> weatherbot.*` edges, and the isolated-import smoke passes with the app
    namespace blocked. Write a persistent self-UAT log recording, per SEAM-01/PKG-01/BHV-01/BHV-02
    criterion: what was checked, the exact command, the observed PASS/FAIL/PARTIAL, and the live
    test count. Record the STANDING-gate status (the three gates are documented as a criterion
    phases 23–27 re-run, D-13) and the deferred Gate-2 host obligation (on `yahir-mint`:
    `uv sync` → `systemctl restart weatherbot` → confirm `import yahir_reusable_bot` resolves +
    daemon comes online) as a milestone-close item, NOT a phase blocker. If ANY golden diff appears,
    HALT and investigate — never `--snapshot-update` (Phase-21 D-04).
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest tests/test_import_hygiene.py tests/test_oracle_selfproof.py -x && git -c core.pager=cat status --porcelain tests/__snapshots__/ | (! grep .)</automated>
  </verify>
  <acceptance_criteria>
    - Full suite green (live count, ~732).
    - All three import-hygiene gates + self-proofs green; `test_oracle_selfproof.py` green.
    - `git status --porcelain tests/__snapshots__/` empty (zero golden diff).
    - `22-03-SELFUAT.md` records per-criterion evidence, the standing-gate status, and the deferred
      host Gate-2 item.
  </acceptance_criteria>
  <done>
    The complete channel + reliability + AlertSink extraction lands byte-identical with the full
    oracle suite green, all three gates green over the full module, zero golden diff, logged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → module | `weatherbot.reliability` + `fire_slot` consume the module's retry surface (one-way) |
| untrusted header → parser | `parse_retry_after` treats the `Retry-After` response header as untrusted input |
| credential → retry surface | The webhook URL / `appid` never enter the retry engine or the `AlertSink` port |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Denial of Service | `parse_retry_after` | mitigate | Caps `Retry-After` at `RETRY_AFTER_CAP_S=120` and clamps `max(base, ra)` to the cap (VERIFIED) — moves VERBATIM, behavior byte-identical. |
| T-22-08 | Information Disclosure (log hygiene) | `retry.py` `before_sleep` | mitigate | The moved `before_sleep` logs outcome-only (attempt/burst), never a key/URL (VERIFIED) — moves verbatim. The webhook URL stays in the UNMOVED app-side `DiscordWebhookChannel._url`. |
| T-22-09 | Tampering (architecture drift) | `yahir_reusable_bot.reliability` / `ports` import surface | mitigate | The standing grimp gate (TYPE_CHECKING-inclusive) + isolated-import smoke fail loud on any new `reliability.*`/`ports.* -> weatherbot.*` edge — this IS the boundary-integrity control for 23–27. |
| T-22-10 | Spoofing/Tampering (no-retry-on-auth) | `is_auth_failure` classifier | accept | The 401/403-never-retry behavior moves verbatim (no auth surface change); `test_reliability.py` + the DB golden arbitrate. |
</threat_model>

<verification>
- `uv run pytest tests/test_reliability.py tests/test_scheduler.py tests/test_import_hygiene.py -x` green.
- `uv run pytest` — full 732-test suite green.
- `uv run pytest tests/test_oracle_selfproof.py -x` green.
- `git status --porcelain tests/__snapshots__/` empty (zero golden diff).
- grimp gate: zero `yahir_reusable_bot.* -> weatherbot.*` edges over the FULL module; litmus clean; isolated-import smoke passes.
</verification>

<success_criteria>
- The retry engine lives in `yahir_reusable_bot.reliability` weather-clean (D-06); `weatherbot.reliability` is a byte-identical shim.
- The weather-clean `AlertSink` port is defined in the module (D-07); `fire_slot` adapted not rewritten, byte-identical.
- Heartbeat untouched (D-08); no `briefing_missed`/heartbeat method on the port.
- All three import-hygiene gates green over the full module; full suite green; zero golden diff.
</success_criteria>

<output>
Create `.planning/phases/22-channel-delivery-reliability-seam-in-place-boundary/22-03-SUMMARY.md` when done.
</output>
