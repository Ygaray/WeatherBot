---
phase: 26-command-registry-dispatcher-seam
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - weatherbot/interactive/registry.py
  - weatherbot/interactive/dispatch.py
  - weatherbot/interactive/command.py
  - weatherbot/scheduler/wiring.py
  - tests/test_import_hygiene.py
  - tests/test_injection_registry.py
autonomous: true
requirements:
  - SEAM-06
user_setup: []

must_haves:
  truths:
    - "(D-03) The app keeps a THIN weatherbot/interactive/registry.py that builds its singleton via the module build_registry(_SPECS) and re-exports COMMANDS / BY_NAME / COMMANDS_BY_KEYWORD_LEN_DESC / render_help / CommandSpec byte-for-byte — tests/test_registry.py + tests/test_command_views.py + the panel's import-time BY_NAME assert pass by construction, not by re-baselining."
    - "(D-01) Each app bind closure is a verbatim lift of one old dispatch_reply ladder arm, authored at wiring.py build_runtime, reading thresholds (cloud_threshold, uv.threshold) LIVE per-tap from the dispatch context (NOT curried at registration — the hot-reload contract)."
    - "(D-01 follow-through) The app's _SPECS carry needs_flags=True for the two forecast commands; the module dispatcher reads that neutral signal and the app-injected parse_flags/cache_suffix hooks — no Forecast group string reaches the module."
    - "(D-04) The Discord text path resolves commands through the module's match_command(text, COMMANDS_BY_KEYWORD_LEN_DESC); the forecast grammar stays app-side in command.py."
    - "(SEAM-06 criterion 1+2) CLI / Discord / help all derive from the one registry; the Phase-21 CLI + help goldens + the anti-drift tests stay byte-identical green; a NEW positive injection-registry assertion proves the command set is app-SUPPLIED, not baked."
    - "(SEAM-06 criterion 3) The APP-02 litmus grep over yahir_reusable_bot/registry/** is clean and the coverage-gap assertion names the new registry files so a future relocation cannot silently drop them."
  artifacts:
    - path: "weatherbot/interactive/registry.py"
      provides: "Thin re-exporting singleton — builds via module build_registry, re-exports the 4 globals + CommandSpec byte-for-byte; keeps app _SPECS + _wire_handlers"
      contains: "build_registry"
    - path: "weatherbot/interactive/dispatch.py"
      provides: "Thin app dispatch shim — re-exports/wraps the module dispatcher with the forecast parse_flags/cache_suffix hooks injected so the three call sites stay byte-identical"
      contains: "dispatch_spec"
    - path: "weatherbot/scheduler/wiring.py"
      provides: "build_runtime authors each bind closure (verbatim lift of one dispatch_reply arm, live per-tap config reads)"
      contains: "bind"
    - path: "tests/test_injection_registry.py"
      provides: "Positive injection-registry assertion — proves the command set is app-supplied (build_registry requires specs; module bakes no weather command name)"
      contains: "build_registry"
  key_links:
    - from: "weatherbot/interactive/registry.py"
      to: "yahir_reusable_bot/registry/registry.py"
      via: "app builds its singleton via the module build_registry(_SPECS)"
      pattern: "build_registry"
    - from: "weatherbot/interactive/command.py"
      to: "yahir_reusable_bot/registry/match.py"
      via: "parse_command delegates to the module match_command"
      pattern: "match_command"
    - from: "weatherbot/scheduler/wiring.py"
      to: "weatherbot/interactive/registry.py"
      via: "build_runtime authors the bind closures over the app command set"
      pattern: "bind"
---

<objective>
Rewire WeatherBot onto the Plan-01 module package while holding the byte-identical oracle green. The app keeps a THIN `weatherbot/interactive/registry.py` (D-03) that builds its singleton via the module `build_registry(_SPECS)` and re-exports the four globals + `CommandSpec` byte-for-byte (the oracle imports them directly; the panel asserts `registry.BY_NAME` at import time — they MUST pass by construction). The arm-ladder weather branching moves into app `bind` closures authored at `wiring.py build_runtime`; the parser delegates to the module `match_command`; the dispatcher's `group=="Forecast"` fetch branch is driven by the neutral `needs_flags` signal + app-injected forecast hooks. Then extend + re-run the gates: the litmus coverage-gap assertion names the new registry files, and a NEW positive injection-registry assertion proves the command set is app-supplied not baked.

Purpose: completes SEAM-06 — CLI / Discord / `help` all derive from the one module-owned registry/dispatch, drift structurally impossible, the module litmus-clean, behavior byte-identical (the ~649-test suite + the Phase-21 CLI + help goldens + the anti-drift tests are the oracle).

Output: 4 app files rewired (registry.py, dispatch.py, command.py, wiring.py) + 2 test files extended; the full oracle re-run green with zero re-baselining.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-command-registry-dispatcher-seam/26-CONTEXT.md
@.planning/phases/26-command-registry-dispatcher-seam/26-PATTERNS.md
@.planning/phases/26-command-registry-dispatcher-seam/26-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW / CHANGED symbols in THIS plan (the app half — weather concepts allowed, they live app-side):
- `weatherbot/interactive/registry.py` → app KEEPS its 5-field `CommandSpec` (name/group/summary/takes_location/handler) for the oracle, PLUS each spec gains `bind` (the closure) + `needs_flags` (True only for the two forecast specs). Builds `_registry = build_registry(_SPECS)`; re-exports `COMMANDS = _registry.commands`, `BY_NAME = _registry.by_name`, `COMMANDS_BY_KEYWORD_LEN_DESC = _registry.by_keyword_len_desc`, `render_help = _registry.render_help` (exact names). Keeps `_wire_handlers` lazy-import + the `replace(spec, ...)` pattern.
- `weatherbot/interactive/dispatch.py` → thin app shim: `dispatch_reply(spec, *, result, config, flags, daemon_state)` bundles a module `DispatchContext` and calls the module `dispatch_reply`; `dispatch_spec(...)` keeps its EXACT current signature (`spec, arg, *, cache, config, loop, daemon_state, flags=None`) and delegates to the module `dispatch_spec` with `parse_flags=parse_forecast_flags` + `cache_suffix=forecast_cache_suffix` injected — so bot.py / panel.py (×2) / cli.py call sites stay byte-identical.
- `weatherbot/interactive/command.py` → `parse_command` delegates to the module `match_command(text, registry.COMMANDS_BY_KEYWORD_LEN_DESC)`; the forecast grammar (parse_forecast_flags / forecast_cache_suffix / ForecastFlags / _day_token) STAYS verbatim.
- `weatherbot/scheduler/wiring.py` → `build_runtime` authors each `bind` closure (one per old ladder arm) reading thresholds live from the ctx.
- `tests/test_import_hygiene.py` → extend the L382 coverage-gap assertion to require the registry files in the scan tree.
- `tests/test_injection_registry.py` → NEW positive assertion: the command set is app-supplied (module `build_registry`/`CommandRegistry.__init__` REQUIRE specs; no module symbol bakes a weather command name), paired with a deliberately-broken self-proof.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Thin app registry re-export + bind/needs_flags on _SPECS + match_command delegation</name>
  <files>weatherbot/interactive/registry.py, weatherbot/interactive/command.py</files>
  <read_first>
    - weatherbot/interactive/registry.py (FILE TO MODIFY — keep _SPECS L50-82, _wire_handlers L85-117, the four globals L120-155 byte-for-byte; the spec gains bind + needs_flags)
    - weatherbot/interactive/command.py (FILE TO MODIFY — parse_command L91-114 delegates to module match_command; forecast grammar L117-207 unchanged)
    - yahir_reusable_bot/registry/registry.py (ANALOG/DEP — the module build_registry the app calls)
    - yahir_reusable_bot/registry/match.py (ANALOG/DEP — the module match_command the parser delegates to)
    - tests/test_registry.py (ORACLE — imports COMMANDS, BY_NAME, COMMANDS_BY_KEYWORD_LEN_DESC, CommandSpec, render_help from weatherbot.interactive.registry; asserts handler identity + takes_location + group strings — these MUST keep passing by construction)
  </read_first>
  <action>
    In `weatherbot/interactive/registry.py`: KEEP the app's own 5-field `CommandSpec` (name/group/summary/takes_location/handler) — the oracle (test_registry.py) imports it and asserts `.handler`/`.takes_location`/`.group`, so it must NOT shrink app-side (D-03 — the generic 4+bind spec lives in the MODULE; the app spec is the richer one). ADD two fields to the app `CommandSpec`: `bind: Callable | None = None` and `needs_flags: bool = False`. Set `needs_flags=True` ONLY on the two `_SPECS` forecast entries (weekday-forecast / weekend-forecast); leave it False on the other nine. Keep `_SPECS` content + `_wire_handlers`'s lazy handler import + the `replace(spec, handler=...)` pattern byte-for-byte (the `bind` closures are authored in wiring.py, Task 2 — here the field defaults to None and the wiring fills it; verify with the executor whether bind must be present before COMMANDS is consumed, and if so wire a lazy bind alongside the handler in _wire_handlers OR thread it from build_runtime — keep imports acyclic, the lazy-import discipline must survive). Replace the assembly of the three views with the module: `_registry = build_registry(_wire_handlers(_SPECS))`, then re-export `COMMANDS = _registry.commands`, `BY_NAME = _registry.by_name`, `COMMANDS_BY_KEYWORD_LEN_DESC = _registry.by_keyword_len_desc`, `render_help = _registry.render_help` (EXACT names — all 6 consumers + the oracle pass by construction). `render_help` must remain callable with the same result; if a default-arg call signature is relied on anywhere, preserve it.

    In `weatherbot/interactive/command.py`: rewrite `parse_command` (L91-114) to delegate to the module: `from yahir_reusable_bot.registry import match_command` and `return match_command(text, registry.COMMANDS_BY_KEYWORD_LEN_DESC)` — translating the module `ParsedCommand` to the app's `ParsedCommand` if their types differ (keep the app `ParsedCommand` type so `bot.py` reads `parsed.spec`/`parsed.arg` byte-identically; simplest is to have the app re-export or adapt the module result). Leave `parse_forecast_flags` / `forecast_cache_suffix` / `ForecastFlags` / `_day_token` (L117-207) and `parse_weather_command` / `Command` / `CommandKind` UNCHANGED.
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py tests/test_command_views.py tests/test_command.py -q</automated>
  </verify>
  <done>The app registry.py builds via the module build_registry and re-exports the four globals + CommandSpec; the app CommandSpec keeps takes_location+handler and gains bind+needs_flags (forecast specs only); parse_command delegates to module match_command; test_registry + test_command_views + test_command pass with zero re-baselining.</done>
</task>

<task type="auto">
  <name>Task 2: bind closures at build_runtime + thin app dispatch shim (de-weather both coupling sites)</name>
  <files>weatherbot/interactive/dispatch.py, weatherbot/scheduler/wiring.py</files>
  <read_first>
    - weatherbot/scheduler/wiring.py (FILE TO MODIFY — build_runtime L107-336; the ReloadEngine closure-injection block L208-260 is the idiom to mirror for the bind closures)
    - weatherbot/interactive/dispatch.py (FILE TO MODIFY — dispatch_reply L60-102 + dispatch_spec L105-186 become thin shims over the module dispatcher)
    - yahir_reusable_bot/registry/dispatch.py (ANALOG/DEP — the module dispatch_reply/dispatch_spec + DispatchContext)
    - weatherbot/interactive/bot.py (CONSUMER — build_on_message L489 parse_command + L500-525 dispatch_spec call; MUST stay byte-identical)
    - weatherbot/interactive/panel.py (CONSUMER — the two dispatch_spec calls L522 + L592; MUST stay byte-identical)
    - weatherbot/cli.py (CONSUMER — _run_registry_command L557-639 calls dispatch_reply; reads spec.group=="Forecast" L580 app-side; MUST stay byte-identical, CLI goldens)
  </read_first>
  <action>
    In `weatherbot/interactive/dispatch.py`: turn both functions into thin shims over the module dispatcher (mirrors the registry re-export pattern, D-03). `dispatch_reply(spec, *, result, config, flags, daemon_state)` builds a module `DispatchContext(result=result, config=config, flags=flags, daemon_state=daemon_state)` and returns the module `dispatch_reply(spec, ctx)` (which calls `spec.bind(ctx)`). `dispatch_spec(spec, arg, *, cache, config, loop, daemon_state, flags=None)` keeps its EXACT signature (the three call sites are byte-identical) and delegates to the module `dispatch_spec`, injecting the app forecast hooks: `parse_flags=parse_forecast_flags`, `cache_suffix=forecast_cache_suffix` (both still imported app-side from command.py — they STAY app-side, litmus-tripping). The module reads `spec.needs_flags` (set in Task 1) instead of `spec.group=="Forecast"`. Keep the off-loop run_in_executor behavior + the UnknownLocationError BUBBLE (D-06) — the module shell already preserves both; the app shim adds no catch. The CLI's app-side `spec.group == "Forecast"` read (cli.py L580) and the subparser build (cli.py L839) STAY app-side, byte-identical — only the MODULE dispatcher is forbidden from naming the group.

    In `weatherbot/scheduler/wiring.py`: in `build_runtime`, author each `bind` closure as a VERBATIM lift of one old `dispatch_reply` arm (dispatch.py L88-102), mirroring the ReloadEngine closure-injection idiom (L208-260). Each closure takes the module `DispatchContext` ctx and reads LIVE config from it per-tap (NOT a value curried at build time — D-01 rejects currying because thresholds are read per-tap via holder.current() so a SIGHUP reload is not frozen stale): the next-cloudy arm → `lambda ctx: weather_views.next_cloudy(ctx.result, ctx.config.cloud_threshold)`; the uv arm → `lambda ctx: weather_views.uv(ctx.result, ctx.config.uv.threshold)`; the forecast arms → `lambda ctx: forecast.weekday_forecast(ctx.result, ctx.flags)` (and weekend); the plain location arms → `lambda ctx: handler(ctx.result)`; status → `lambda ctx: status.status(ctx.daemon_state)`; locations → `lambda ctx: info.locations(ctx.config)`; help → `lambda ctx: info.help_cmd()`. The weather names + threshold reads are ALLOWED here (the app composition root). Attach each bind to its spec — decide with Task 1 whether bind is wired in `_wire_handlers` (beside the handler, lazy import) or threaded from build_runtime; keep imports acyclic and the panel's import-time BY_NAME assert satisfied (the registry must be fully built at import time). The arm ORDER + exact handler arg shapes are load-bearing (the contractual reply suite + CLI/help goldens prove byte-identical).
  </action>
  <verify>
    <automated>uv run pytest tests/test_dispatch.py tests/test_bot.py tests/test_panel.py tests/test_cli.py -q</automated>
  </verify>
  <done>dispatch.py is a thin shim delegating to the module dispatcher with the forecast hooks injected; the three dispatch call sites are byte-identical; build_runtime authors the bind closures (verbatim arm lifts, live per-tap config reads); the module dispatcher names no Forecast group; test_dispatch + test_bot + test_panel + test_cli pass with zero re-baselining.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the gates — litmus coverage-gap + positive injection-registry assertion — and run the full oracle</name>
  <files>tests/test_import_hygiene.py, tests/test_injection_registry.py</files>
  <read_first>
    - tests/test_import_hygiene.py (FILE TO MODIFY — the L381-384 coverage-gap assertion pattern; the litmus L361-391 auto-scans, only the coverage-gap names need the registry files added)
    - tests/test_injection_registry.py (FILE TO MODIFY — the _required_params_without_default helper L59-77, the _module_public_symbols helper L126-134, the _BakedGate/_BakedReload self-proof discipline L162-169 / L210-216 to mirror for commands)
    - yahir_reusable_bot/registry/registry.py (DEP — build_registry / CommandRegistry.__init__ require specs; the assertion target)
    - yahir_reusable_bot/registry/dispatch.py (DEP — proof the module bakes no weather command name/handler)
  </read_first>
  <action>
    In `tests/test_import_hygiene.py`: extend the coverage-gap assertion (the `{...} <= scanned` pattern at L382, which currently names the lifecycle files) so it ALSO requires the registry files in the scanned tree — add `"spec.py", "registry.py", "match.py", "dispatch.py"` (the registry package's files) to the asserted-present set so a future relocation cannot silently drop the registry package from litmus coverage. Do NOT broaden the D-13-locked litmus term set (`weather|forecast|location|openweather|\buv\b|briefing`) — generic seam names (CommandRegistry / CommandSpec / match_command / group / bind / needs_flags / DispatchContext) are allowed and pass the AST name scan. If the registry filenames collide with lifecycle filenames in a flat name-set assertion, scope the assertion to the registry subtree (e.g. assert the registry files are present under the registry package path) so the proof is unambiguous.

    In `tests/test_injection_registry.py`: ADD a positive assertion (extending the Phase-25 D-05 pattern to commands) proving the command set is app-SUPPLIED, not baked. Two halves, both using the existing introspection helpers: (a) `build_registry` / `CommandRegistry.__init__` REQUIRE their `specs` param (use `_required_params_without_default` exactly as `test_health_check_is_injected_no_module_default_probe` asserts ReadyGate requires health_check — so the module has NO baked default command set); (b) NO module public symbol bakes a weather command name/handler (use `_module_public_symbols` + the "no weather-named symbol" pattern at L241-245 / L282-286, asserting the module names none of weather/forecast/uv/next-cloudy/etc. as a def/class name). PAIR every positive assertion with a deliberately-broken self-proof (a stub registry that BAKES a default spec tuple → proven to trip the SAME `_required_params_without_default` check — exactly the `_BakedGate`/`_BakedReload` discipline) so a green run proves the gate BITES. Add NO new dependency (stdlib inspect/ast + existing fakes only); add NO weather noun to the module surface.

    Then run the FULL oracle (verify below). Any non-empty snapshot diff in the CLI / help goldens or the anti-drift tests is a FAILURE to investigate — never rubber-stamp a re-baseline.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest tests/test_import_hygiene.py tests/test_injection_registry.py -q</automated>
  </verify>
  <done>The litmus coverage-gap assertion names the registry files; the positive injection-registry assertion proves build_registry requires specs + the module bakes no weather command name, each paired with a biting self-proof; the FULL suite (~649+) is green; the CLI + help goldens + anti-drift tests are byte-identical (zero re-baselined snapshot); the litmus over yahir_reusable_bot/registry/** is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app composition root → module registry/dispatch | build_runtime authors the bind closures + injects the forecast hooks; the app registry.py builds the singleton via the module build_registry. The weather names + threshold reads live on the app side of this boundary. |
| user command text → match_command (via app parse_command) | Raw Discord text crosses into the module matcher — same untrusted surface as today's parse_command, unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | bind closures reading live config from ctx | mitigate | Closures read ctx.config per-tap (holder.current() snapshot) — no registration-time capture, so a SIGHUP reload is never served stale config; behavior pinned by the contractual reply suite. |
| T-26-05 | Information Disclosure | thin dispatch shim injecting forecast hooks | accept | The shim adds no logging, no new I/O, no secret handling — it delegates to the module dispatcher with two app callables injected; the off-loop/bubble discipline is byte-identical (proven by test_dispatch/test_bot/test_panel/test_cli). |
| T-26-06 | Spoofing | operator-gated panel callbacks | accept | The operator gate + per-callback failure-isolation envelope in panel.py are UNCHANGED by this relocation (the call sites stay byte-identical); no auth surface is touched. |
| T-26-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (stdlib + existing deps only). RESEARCH flag is No; no Package Legitimacy Audit needed. |

No high-severity threat is introduced: the rewire keeps every call site byte-identical and adds only additive test assertions; the only behavioral surface that moves (the arm ladder + the matcher) is proven unchanged by the Phase-21 goldens + the ~649-test oracle.
</threat_model>

<verification>
- `uv run pytest` — the FULL ~649+ suite green, byte-identical (zero oracle re-baselined). This is the headline gate: a non-empty CLI/help golden or anti-drift diff is a failure to investigate.
- `uv run pytest tests/test_registry.py tests/test_command_views.py` — the oracle that imports the module globals directly passes by construction (D-03 re-export).
- `uv run pytest tests/test_dispatch.py tests/test_bot.py tests/test_panel.py tests/test_cli.py` — the contractual reply suite + the three call sites stay byte-identical through the relocation.
- `uv run pytest tests/test_import_hygiene.py` — grimp + isolated-import + AST litmus clean over the registry package; the coverage-gap assertion now names the registry files.
- `uv run pytest tests/test_injection_registry.py` — the positive command-injection assertion passes and its self-proof bites.
</verification>

<success_criteria>
- SEAM-06 criterion 1: registry + dispatcher live in the module, WeatherBot registers its set at build_runtime, CLI/Discord/help derive from the one registry; Phase-21 CLI + help goldens + anti-drift green (byte-identical).
- SEAM-06 criterion 2: adding/removing a command surfaces uniformly with no parallel hardcoded list — drift structurally impossible (single dispatch path, asserted by the positive injection-registry test + the derive-from-one-list oracle).
- SEAM-06 criterion 3: the module registry/dispatch carries no weather command name/handler/group — the litmus grep over the registry seam is clean; the positive assertion proves the set is app-supplied.
- Both coupling sites de-weathered (bind closures + needs_flags/injected hooks); the hot-reload contract preserved (live per-tap config reads).
- `uv run pytest` fully green, zero re-baselining.
</success_criteria>

<output>
Create `.planning/phases/26-command-registry-dispatcher-seam/26-02-SUMMARY.md` when done.
</output>
