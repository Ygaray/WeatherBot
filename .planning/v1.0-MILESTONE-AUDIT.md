---
milestone: v1.0
audited: 2026-06-15
status: gaps_found
scores:
  requirements: 36/37
  phases: 5/5
  integration: 7/7 seams wired (0 blockers)
  flows: 3/3 (1 with deferred live observation)
gaps:
  requirements:
    - id: "OPS-01"
      status: "partial"
      phase: "05-deployment-reboot-survival"
      claimed_by_plans: ["05-02-PLAN.md"]
      completed_by_plans: []   # 05-02-SUMMARY lists OPS-01 under requirements-pending-uat, not requirements-completed
      verification_status: "human_needed"
      evidence: "Implementation complete: deploy/weatherbot.service ships Type=notify + Restart=always + WantedBy=multi-user.target; on host yahir-mint the unit is enabled (symlinked into multi-user.target.wants) and active (running). Code+systemd reboot-survival path verified (16/16 code criteria, 186/186 tests pass). The ONLY outstanding item is SC#1 — a live `sudo reboot` power-cycle observation — DEFERRED at the operator's request (would power-cycle their primary workstation). This is a pending observation, not a missing implementation. OPS-02 (self-check + online signal) is fully SATISFIED and confirmed on host via journal ordering."
  integration: []
  flows:
    - flow: "reboot → systemd auto-start → self-check → online ping (OPS-01 SC#1)"
      status: "code-complete; live post-reboot auto-start not yet directly observed (operator-deferred)"
tech_debt:
  - phase: 03-always-on-scheduler
    items:
      - "RESOLVED (quick task 260615-fac, commit 7a03da3): Orphaned dead code `record_sent` deleted from weatherbot/weather/store.py; tests/test_scheduler.py idempotency test migrated to assert via the live atomic `claim_slot` path. `was_sent` retained (still live in daemon.py/catchup.py). 186 tests pass, ruff clean."
  - phase: milestone-wide
    items:
      - "OPEN (by design): DATA-03 interpretation — persistence is gated on successful delivery (cli.py:176-177), so a fetch whose delivery ultimately fails is NOT retained ('exactly one persisted round per delivered briefing', WR-04). Consistent across manual + daemon paths. Confirm the requirement wording allows the delivered-only interpretation."
      - "RESOLVED (quick task 260615-fac, commit 7842e9e): `requirements-completed` frontmatter backfilled on all 11 plan SUMMARYs (01-01/02/03, 02-01..04, 04-01..04) from each phase VERIFICATION.md coverage table."
      - "OPEN (deferred): REQUIREMENTS.md still marks OPS-01 as `[~]` 'Met (pending reboot UAT)'. Flip to `[x]` once the live reboot UAT is observed — depends on the OPS-01 SC#1 observation."
nyquist:
  compliant_phases: [01, 03, 04]
  partial_phases: [02, 05]
  missing_phases: []
  overall: partial
---

# Milestone v1.0 Audit — WeatherBot

**Audited:** 2026-06-15
**Status:** ⚠ gaps_found (one partial requirement — operator-deferred live observation; no code blockers)
**Definition of done:** A hands-off, always-on morning weather-briefing daemon — correct, correctly-located briefings fetched from OpenWeather, persisted to a long-term SQLite store, rendered imperial-primary, delivered to Discord, scheduled per-location with DST-safe exactly-once firing, retry-then-alert reliability, and reboot survival under a supervisor.

## Summary

All 5 phases are verified and all cross-phase seams are wired end-to-end. **36 of 37 requirements are fully satisfied.** The single outstanding item is **OPS-01 SC#1** — a live `sudo reboot` power-cycle observation that the operator deferred (it would reboot their primary workstation). The reboot-survival implementation itself is complete, installed, `enabled`, and `active (running)` on host `yahir-mint`; only the post-reboot auto-start has not yet been directly *observed*. This is a verification observation, not a code gap.

## Phase Verification Roll-up

| Phase | Status | Score | Note |
|-------|--------|-------|------|
| 1. First Briefing End-to-End | ✅ passed | 7/7 | 2 review criticals fixed + 10 regression tests; advisory warnings remain |
| 2. Real Config — Locations, Content & Templates | ✅ passed | 5/5 | Re-verified after units-override gap closed (02-05); live UAT passed |
| 3. Always-On Scheduler | ✅ passed | 5/5 | Re-verified after DST + exactly-once gaps closed (03-04/03-05) |
| 4. Retry-then-Alert Reliability | ✅ passed | 4/4 | All 3 live UAT items passed; 1 major + 1 cosmetic bug fixed inline |
| 5. Deployment & Reboot Survival | ⚠ human_needed | 16/16 code | Code complete; OPS-01 SC#1 live reboot deferred by operator |

## Requirements Coverage (3-source cross-reference)

Cross-referenced across phase VERIFICATION.md, SUMMARY.md `requirements-completed` frontmatter, and the REQUIREMENTS.md traceability table.

| Group | IDs | Status |
|-------|-----|--------|
| Forecast | FCST-01..06 | ✅ satisfied (6/6) |
| Data Persistence | DATA-01/02/03 | ✅ satisfied (3/3) — see DATA-03 interpretation note |
| Locations | LOC-01/02/03 | ✅ satisfied (3/3) |
| Scheduling | SCHD-01..07 | ✅ satisfied (7/7) |
| Delivery | DELV-01/02/03 | ✅ satisfied (3/3) |
| Templating | TMPL-01/02 | ✅ satisfied (2/2) |
| Reliability | RELY-01..06 | ✅ satisfied (6/6) |
| Config & Operation | CONF-01/02/03/04/05 | ✅ satisfied (5/5) |
| Ops — self-check/online | OPS-02 | ✅ satisfied — confirmed on host |
| Ops — reboot survival | **OPS-01** | ⚠ **partial** — code complete + enabled + active; live reboot SC#1 deferred |

**Satisfied: 36/37. Partial: 1 (OPS-01). Unsatisfied: 0. Orphaned: 0.**

Every REQ-ID in the traceability table appears in at least one phase VERIFICATION.md requirements table — no orphans. The frontmatter `requirements-completed` field is empty on several plan SUMMARYs (doc-hygiene tech debt below), but the authoritative phase VERIFICATION tables account for all 37 IDs.

## Cross-Phase Integration

Integration checker traced all 7 seams through actual code (not SUMMARYs); 186/186 tests pass; **0 blockers**.

- **Seam 1** — Scheduler `fire_slot` reuses the exact `--send-now` pipeline (`daemon.py:193` → `cli.py:send_now`); no drift between manual and daemon paths. ✓
- **Seam 2** — Per-location units override (`cli.py:132`) honored on the daemon path because both paths share `send_now`. ✓
- **Seam 3** — Retry/alert wrapper wraps daemon `fire_slot` (`daemon.py:182-187`); out-of-band alert path (`record_alert` + CRITICAL log) is independent of the Discord channel. ✓
- **Seam 4** — `run_daemon` builds ONE channel on the `channel=None` fallback (`daemon.py:568-574`) and threads it into briefing jobs, catch-up, and `emit_online` — the 05-03 gap-closure fix, covered by regression tests. ✓
- **Seam 5** — `gate_until_healthy` blocks `scheduler.start()`/`emit_online`/READY=1 until the self-check passes; systemd never sees the unit active on a bad key/network. ✓
- **Seam 6** — Persistence reuses the single 2-call fetch on the scheduled path (`cli.py:177`); no extra API call. ✓
- **Seam 7** — E2E flows (a) config→check→daemon→fire→deliver+persist, (b) Discord-broken→retry→out-of-band alert, (c) reboot→restart→self-check→online — all wired; (c)'s post-reboot auto-start is the deferred live observation. ✓

**WARNING (non-blocking):** `record_sent` (`store.py:242-264`) is orphaned dead code superseded by atomic `claim_slot`. → tracked as tech debt.

## Nyquist Validation Coverage

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 1 | exists (validated) | true | true | COMPLIANT |
| 2 | exists (draft) | false | false | PARTIAL |
| 3 | exists (validated) | true | true | COMPLIANT |
| 4 | exists (validated) | true | true | COMPLIANT |
| 5 | exists (draft) | true | false | PARTIAL |

Phases 2 and 5 have draft VALIDATION.md with incomplete Wave 0. Consider `/gsd-validate-phase 2` and `/gsd-validate-phase 5` to close the Nyquist gaps (note: their functional VERIFICATION already passed — this is validation-coverage completeness, not a functional gap).

## Tech Debt

**Phase 3:** Delete orphaned `record_sent` (`store.py:242-264`); migrate `tests/test_scheduler.py:119-128` to assert via `claim_slot`.

**Milestone-wide:**
- DATA-03 "delivered-only" persistence interpretation — confirm requirement wording allows it.
- SUMMARY `requirements-completed` frontmatter missing on 01-01/02/03, 02-01..04, 04-01..04 (coverage unaffected).
- REQUIREMENTS.md OPS-01 row still `[~]` — flip to `[x]` after the reboot UAT.
