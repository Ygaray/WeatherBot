---
phase: quick-260626-rd8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-extract-shared-dispatch-spec/16-01-SUMMARY.md
  - .planning/phases/20-isolation-hardening-polish/20-02-SUMMARY.md
  - .planning/phases/20-isolation-hardening-polish/20-03-SUMMARY.md
  - tests/test_cache.py
  - tests/test_reload.py
  - tests/test_dispatch.py
  - tests/test_scheduler.py
  - weatherbot/**  # ruff-format whitespace only; WR-02 comment already present
autonomous: true
requirements: [PANEL-10, PANEL-12, PANEL-13]
must_haves:
  truths:
    - "All three target SUMMARYs carry a requirements-completed field matching the 18-01 style"
    - "uv run ruff check weatherbot tests is clean (0 errors)"
    - "uv run ruff format --check weatherbot tests is clean (0 would-reformat)"
    - "dispatch_spec's forecast-flags + cache-suffix path has a focused unit test"
    - "The WR-03 hanging-callback test cleans up its wedge thread (no dangling thread)"
    - "Full suite stays green (>= 649 passed) with ZERO production behavior change"
  artifacts:
    - path: ".planning/phases/16-extract-shared-dispatch-spec/16-01-SUMMARY.md"
      provides: "requirements-completed: [PANEL-10]"
      contains: "requirements-completed: [PANEL-10]"
    - path: ".planning/phases/20-isolation-hardening-polish/20-02-SUMMARY.md"
      provides: "requirements-completed: [PANEL-12, PANEL-13]"
      contains: "requirements-completed: [PANEL-12, PANEL-13]"
    - path: ".planning/phases/20-isolation-hardening-polish/20-03-SUMMARY.md"
      provides: "requirements-completed: [PANEL-12, PANEL-13]"
      contains: "requirements-completed: [PANEL-12, PANEL-13]"
  key_links:
    - from: "tests/test_dispatch.py"
      to: "weatherbot/interactive/dispatch.py"
      via: "new focused test drives dispatch_spec's flags=None forecast path"
      pattern: "dispatch_spec"
---

<objective>
Resolve the five non-blocking tech-debt items from `.planning/v1.3-MILESTONE-AUDIT.md`
(the v1.3 "Discord Control Panel" milestone). Doc-only frontmatter backfill, a repo-wide
ruff format/lint sweep, one new isolated unit test, a verify-only comment check, and a
test-hygiene cleanup.

Purpose: Clear the audit's deferred cleanups so the v1.3 milestone closes clean. This is
maintenance debt, not feature work.

Output: 3 SUMMARYs with `requirements-completed` frontmatter, a clean ruff check/format,
a new `dispatch_spec` unit test, and a self-cleaning WR-03 scheduler test.

INVARIANT (load-bearing): ZERO production behavior change. The only `weatherbot/` source
edits permitted are ruff-format whitespace/formatting on existing lines. (WR-02's
explanatory comment is ALREADY present in `dispatch.py` from a prior pass — this plan
VERIFIES it, it does not re-add it.) Everything else is test files + planning-doc
frontmatter. Full suite (`uv run pytest -q`, currently 649 passed) must stay green;
`uv run ruff check` + `ruff format --check` must be clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.3-MILESTONE-AUDIT.md
@./CLAUDE.md

# WR-01 — existing pattern to extend (study test_dispatch_spec_* tests):
@tests/test_dispatch.py
# WR-01/WR-02 targets (WR-02 comment already at lines 130-137 + 174-178):
@weatherbot/interactive/dispatch.py
# WR-03 target — the hanging-callback test at line ~1991:
@tests/test_scheduler.py
# Reference frontmatter style (requirements-completed: [PANEL-09] at line 50):
@.planning/phases/18-persistence-summon-lifecycle-restart-durability/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SUMMARY frontmatter backfill + repo-wide ruff format/lint sweep (commit atomically)</name>
  <files>
    .planning/phases/16-extract-shared-dispatch-spec/16-01-SUMMARY.md,
    .planning/phases/20-isolation-hardening-polish/20-02-SUMMARY.md,
    .planning/phases/20-isolation-hardening-polish/20-03-SUMMARY.md,
    tests/test_cache.py,
    tests/test_reload.py,
    (plus ~30 files reformatted by ruff format — whitespace only)
  </files>
  <action>
Two independent doc/format cleanups, committed together (both are non-behavioral hygiene).

(1) FRONTMATTER BACKFILL (item #1 of the audit). Add a `requirements-completed:` field to
three SUMMARY frontmatter blocks. Match the EXACT style used in
`18-01-SUMMARY.md:50` — hyphenated key `requirements-completed`, inline bracket-list form,
placed as a standalone line inside the `---` frontmatter block (in 18-01 it sits after the
`patterns-established:` block and before `# Metrics`). For each target, insert the line at
the analogous position — after the last dependency/patterns key, before any metrics/closing.
Use Edit (scoped), not Write, on these existing files.
  - `16-01-SUMMARY.md` → `requirements-completed: [PANEL-10]` (16-VERIFICATION.md:62 maps
    PANEL-10 → 16-01, SATISFIED).
  - `20-02-SUMMARY.md` → `requirements-completed: [PANEL-12, PANEL-13]`
  - `20-03-SUMMARY.md` → `requirements-completed: [PANEL-12, PANEL-13]`
    Both 20-02 and 20-03 carry PANEL-12 AND PANEL-13 per 20-VERIFICATION.md:69-70
    (PANEL-12 → 20-02,20-03; PANEL-13 → 20-02,20-03). This was re-verified against
    20-VERIFICATION.md during planning — use the lists exactly as above.
  Note: 16-01 and 20-02/20-03 use different surrounding frontmatter shapes than 18-01
  (16-01 has bare `requires:`; 20-02/03 nest under `dependency-graph:`). That is fine — only
  the `requirements-completed` line itself must match 18-01's key name + inline-list format;
  place it within each file's own frontmatter block at a sensible standalone position.

(2) RUFF SWEEP (item #2 of the audit). The audit's named lines (models.py:149,
test_panel.py:194 F841, etc.) are STALE — the tree has evolved. The current drift, confirmed
during planning, is: ~30 files need `ruff format`, and `ruff check` reports 4 F401
unused-import errors (NOT the audited F841). Absorb whatever the current tree actually shows:
  - Run `uv run ruff check --fix weatherbot tests` to auto-remove the 4 unused imports
    (`pytest` in tests/test_cache.py; `datetime`, `BackgroundScheduler`, `ConfigHolder` in
    tests/test_reload.py — all verified genuinely unused, all in test files, no production
    code touched).
  - Run `uv run ruff format weatherbot tests` to absorb all format drift. Per the INVARIANT,
    any `weatherbot/` changes here MUST be whitespace/formatting on existing lines only — if
    ruff format proposes anything that looks like a logic change, STOP and surface it (it
    won't; ruff format is whitespace-only by design).
  - If `ruff check --fix` surfaces any non-F401 error that is NOT auto-fixed, fix it the
    minimal non-behavioral way (e.g. assign-to-`_` for an F841) and note it; do not weaken any
    assertion. (Expectation from planning: only the 4 F401s exist, all auto-fixed.)
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/WeatherBot && uv run ruff check weatherbot tests && uv run ruff format --check weatherbot tests && grep -q 'requirements-completed: \[PANEL-10\]' .planning/phases/16-extract-shared-dispatch-spec/16-01-SUMMARY.md && grep -q 'requirements-completed: \[PANEL-12, PANEL-13\]' .planning/phases/20-isolation-hardening-polish/20-02-SUMMARY.md && grep -q 'requirements-completed: \[PANEL-12, PANEL-13\]' .planning/phases/20-isolation-hardening-polish/20-03-SUMMARY.md && uv run pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>
All three SUMMARYs carry the correct `requirements-completed` line in 18-01 style;
`ruff check` and `ruff format --check` both report clean on weatherbot+tests; full pytest
suite stays green (>= 649 passed); no `weatherbot/` change beyond whitespace/formatting.
Committed atomically as `chore(quick-rd8): backfill SUMMARY reqs + ruff sweep`.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-01 dispatch_spec unit test + WR-02 comment verify + WR-03 test cleanup (commit atomically)</name>
  <files>
    tests/test_dispatch.py,
    tests/test_scheduler.py,
    weatherbot/interactive/dispatch.py  # WR-02: VERIFY-ONLY, no edit expected
  </files>
  <action>
Three test/doc items, committed together (all test-hygiene + a doc verify; zero production
behavior change).

(A) WR-01 — add ONE focused unit test to `tests/test_dispatch.py` for `dispatch_spec`'s
async forecast-flags path. Study the existing pattern: the file already has the `_SpyCache`
spy (records `lookup(name, config, *rest)`), the `_FakeSpec`/`_FakeConfig`/`_recording_handler`
fakes, and the Phase-19 tests `test_dispatch_spec_forecast_widens_cache_key_with_3arg_lookup`,
`test_dispatch_spec_flags_passthrough_skips_parse`, `test_dispatch_spec_flags_none_is_byte_identical`.
Reuse those fakes exactly — do NOT add new fixtures. Add a new test (suggested name
`test_dispatch_spec_text_forecast_parses_flags_and_widens_suffix`) that exercises the
drift-prone HALF the audit calls out specifically: the `flags=None` (text-command) path that
forces `parse_forecast_flags` to run, deriving `lookup_name` from the parsed `flags.location`
and the cache `suffix` from `forecast_cache_suffix(spec.name, flags)`, then asserting the
3-arg `cache.lookup` form. Drive a `Forecast`-group `_FakeSpec` through `dispatch_spec(spec,
"<loc> +<token>", cache=..., ..., daemon_state=None)` (NO `flags=` kwarg, so the text parse
path runs) on a real `asyncio` loop. Assert: (1) exactly one recorded `cache.lookup` call;
(2) its `name` equals the location parsed out of the arg (e.g. arg `"travel +sun"` → name
`"travel"`); (3) its `rest` is a 1-tuple with a non-None suffix (the A5 widening); (4) the
handler received the fetched result + a non-None `ForecastFlags` whose `.location` matches.
This pins the text-command forecast lookup-name + cache-suffix derivation at the unit level
rather than only transitively via test_bot.py.

(B) WR-02 — VERIFY the off-loop divergence comment already exists in
`weatherbot/interactive/dispatch.py`; do NOT add a duplicate. Planning confirmed it is
already present in two spots: the docstring "Off-loop scope (deliberate widening — WR-02)"
block (around lines 130-137) and the inline comment at the final `loop.run_in_executor`
tail call (around lines 174-178), both explaining that the ENTIRE arg-adaptation ladder now
runs off the loop and why that is safe (handlers are pure in-memory reads of an
already-fetched payload; replies stay byte-identical). Grep to confirm both `WR-02` mentions
are present. If — and only if — they are somehow absent, add a concise comment at the
`run_in_executor` tail call noting the whole-ladder off-loop dispatch and its safety; comment
only, no behavior change. Make NO edit if the comment is already there (the expected case),
so dispatch.py stays byte-identical to its committed state aside from the Task-1 ruff format.

(C) WR-03 — clean up the deliberate daemon-thread leak in
`test_hanging_callback_never_stops_live_briefing` (tests/test_scheduler.py, ~line 1991). The
test wedges a panel callback on `await asyncio.Event().wait()` driven via `asyncio.run` on a
`daemon=True` thread (`wedge_thread`) and currently leaves it running after the assertions.
Make the test self-cleaning WITHOUT weakening what it proves (the sentinel briefing still
fires on its `IntervalTrigger` while the callback is wedged, and `scheduler.running is True`).
The wedge awaits `asyncio.Event().wait()` on its OWN loop created by `asyncio.run`, so the
cleanest deterministic shutdown is: capture that loop/event from inside the coroutine and
signal it from the test thread via `loop.call_soon_threadsafe(...)` to unblock the await, OR
— if threading that loop handle out is awkward — restructure the wedge to await a module-level
/ closure `asyncio.Event` whose loop is captured so the test can set it via
`call_soon_threadsafe`. After signaling, `wedge_thread.join(timeout=...)` and assert it
actually terminated. Wrap the scheduler lifecycle AND the wedge teardown in a `try/finally`
so that even on assertion failure the scheduler is `shutdown(wait=False)` and the wedge thread
is explicitly joined within a timeout — no thread left dangling. If a fully clean join proves
impractical given the `asyncio.Event().wait()` shape, at MINIMUM: keep the existing isolation
assertions, then in a `finally` shut the scheduler down and `wedge_thread.join(timeout=<small>)`,
and assert the thread is no longer alive (signaling the event first so the join can succeed).
The test must still pass and still assert the isolation guarantee (sentinel fired + scheduler
running while the callback was provably wedged — `callback_entered.wait(...)` stays).
  </action>
  <verify>
    <automated>cd /home/yahir/Projects/WeatherBot && grep -c 'WR-02' weatherbot/interactive/dispatch.py && uv run pytest tests/test_dispatch.py tests/test_scheduler.py -q 2>&1 | tail -5 && uv run pytest -q 2>&1 | tail -3 && uv run ruff check weatherbot tests && uv run ruff format --check weatherbot tests</automated>
  </verify>
  <done>
New `dispatch_spec` text-forecast test passes and asserts the correct lookup name + non-None
3-arg cache suffix + parsed flags. The WR-02 off-loop comment is confirmed present in
dispatch.py (>= 2 `WR-02` mentions). The WR-03 hanging-callback test cleans up its wedge
thread (signals the event, joins within a timeout in a `finally`, asserts not alive) while
still proving the sentinel briefing fired and the scheduler stayed running. Full suite green
(>= 650 passed — one new test); ruff check + format --check clean. Committed atomically as
`test(quick-rd8): WR-01 dispatch_spec test + WR-03 thread cleanup`.
  </done>
</task>

</tasks>

<verification>
Full-phase checks (run after both tasks):
- `uv run ruff check weatherbot tests` → clean (0 errors)
- `uv run ruff format --check weatherbot tests` → clean (0 would-reformat)
- `uv run pytest -q` → green, >= 650 passed (baseline 649 + 1 new WR-01 test)
- `git diff --stat weatherbot/` → only whitespace/formatting changes (no logic diff)
- Three SUMMARYs contain their `requirements-completed` line
- No daemon thread left dangling after the WR-03 test (it joins within timeout)
</verification>

<success_criteria>
- [ ] 16-01, 20-02, 20-03 SUMMARYs each carry a correct `requirements-completed` line in 18-01 style
- [ ] `ruff check` + `ruff format --check` both clean on weatherbot + tests
- [ ] New `dispatch_spec` text-forecast unit test exists and passes
- [ ] WR-02 off-loop comment confirmed present in dispatch.py (verify-only)
- [ ] WR-03 test self-cleans its wedge thread without weakening its isolation proof
- [ ] Full pytest suite green, >= 650 passed
- [ ] ZERO production behavior change (only whitespace/formatting in weatherbot/)
- [ ] Each task committed atomically
</success_criteria>

<output>
Create `.planning/quick/260626-rd8-resolve-v1-3-milestone-audit-tech-debt/260626-rd8-SUMMARY.md` when done
</output>